# Журнал проекта — часть 4 (пункты 166–182)

> **Часть 4 из 4.** Журнал разбит: одна часть не длиннее 1 500 строк.
>
> * пункты этой части — **166–182**: планы, сверки документации с кодом, правила
>   документирования, навигация и структура папок;
> * **точка входа — [указатель](README.md)**: там таблица «симптом → пункты» и
>   номера строк. Искать пункт перебором частей не нужно;
> * соседние части: [часть 1](../архив/журнал/ЖУРНАЛ_1.md) · [часть 2](../архив/журнал/ЖУРНАЛ_2.md) · [часть 3](ЖУРНАЛ_3.md);
> * в конце файла — **сводные разделы 4–7** по всему журналу: таблица
>   «проблема → причина → исправление», ключевые решения, текущее состояние.

<!-- ОГЛАВЛЕНИЕ · сгенерировано tools/переносимое/docmap.py — руками не править -->
> **Оглавление** — номер строки · раздел. Открывать нужный раздел, а не файл
> целиком (`Read` с `offset`). Пересобрать: `python tools/переносимое/docmap.py`.
>
> `  24` Хронология
> ` 810` 4. Возникшие проблемы и исправления
> ` 847` 5. Ключевые решения (с учётом обсуждения)
> ` 864` 6. Текущее состояние (сделано)
> ` 901` 7. Что осталось / возможные улучшения
<!-- /ОГЛАВЛЕНИЕ -->

## Хронология

166. **Составлен план 4 — всё незакрытое в одном месте. ✅**
     Планы 1–3 закрыты (рельеф, интерфейс с движением, сборка `.exe`), но остатки были
     рассыпаны по журналу и по концовкам планов. Собраны в
     [4_ПЛАН_ДОРАБОТКИ.md](../планы/4_ПЛАН_ДОРАБОТКИ.md) — семь пунктов:

     | № | что | откуда |
     |---|---|---|
     | Д1 | цвет запретной зоны конфликтует с красным у возвышенностей | остаток П5, п. 162 |
     | Д2 | сверка векторных слоёв с весовой картой | П7 плана 2, не делали вовсе |
     | Д3 | движение при запасе 150 км ломаное: изломов 22.7 против 11, набирается 112 из 150 | остаток П6, п. 160 |
     | Д4 | маршруты уходят от цели до 8 км при большом запасе | остаток П1, п. 158 |
     | Д5 | рельеф: усилить `k` до 0.10…2.50 и/или поднять пороги насыщения | задел плана 1 |
     | Д6 | калибровка глазом: пороги рельефа и сила предвидения | задел плана 1 |
     | Д7 | две модели движения на выбор | задел, делается параметрами |

     **Порядок обоснован, а не по номерам.** Сначала Д1 (одна строка, видно сразу), затем
     **Д2 — сверка слоёв**: это единственный пункт, проверяющий ИСХОДНЫЕ ДАННЫЕ, а не
     поведение модели. Если карта врёт, все замеры после неё считаются по кривым данным, а
     ошибка выглядит как «странные маршруты». Д5 (усиление рельефа) поставлен **последним**:
     он меняет саму весовую карту и обесценивает замеры, сделанные до него.
     Отдельно помечено то, что в план НЕ входит и требует решения заказчика: высота полёта
     (модель плоская), разнотипные датчики, экспорт результата, пересборка `.docx`.

167. **Сквозная сверка документации с кодом. ✅ Найдено и исправлено пять расхождений.**
     Заказчик: проверить, что документация бьётся с фактическим функционалом; лишнее
     убрать, работоспособность не трогать.

     **Как проверялось.** Написан скрипт сверки, который сравнивает шесть вещей:
     параметры, упомянутые в текстах, против `config.py` и модульных констант; ЗНАЧЕНИЯ
     параметров в тексте против кода; функции и классы; файлы кода; параметры кода,
     нигде не описанные; ссылки между документами. Охват — 33 файла кода и 23 документа,
     72 параметра `config.py` плюс 48 полей `Params`.

     **Скрипт пришлось чинить трижды — сам давал ложные срабатывания:**
     * сверял имена только с функциями и классами, забыв про КОНСТАНТЫ модулей —
       `LAYER_STYLE`, `PLACE_CODES`, `FINAL_APPROACH_KM` попали в «отсутствующие»;
     * не знал про атрибуты `self.X = …`, аргументы функций и переменные окружения —
       `_urban`, `_place_grade`, `UAV_GEO_CACHE` тоже оказались «призраками»;
     * считал расхождением фразы вида «`X = 0` → прежнее поведение»: это разбор ВАРИАНТА,
       а не утверждение о текущем значении. Добавлен разбор соседнего текста.
     Вывод на будущее: сверка имён по регулярным выражениям без учёта всех видов
     объявлений даёт больше шума, чем сигнала.

     **Что нашлось на самом деле (пять расхождений):**
     | что | где | суть |
     |---|---|---|
     | §4.2 описывал УДАЛЁННЫЙ роутинг | МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ | раздел «Маршруты по costmap (реализовано)» излагал детерминированный Дейкстра с константами `base_cost`, `weight_scale`, `urban_cost`. Их нет в коде — удалены вместе с тем роутингом, о чём прямо сказано в `config.py`. Раздел переписан под действующую модель: стохастическое блуждание, ДВА поля расстояний (километры для бюджета, стоимость для спуска), цена шага через `THREAT_COST_POWER` |
     | `THREAT_URBAN_IN_WEIGHT` | там же, §4.6 | параметра в коде нет: от «минуса городам» отказались, вес города обнуляется. Помечено как отклонённое решение с причиной |
     | сводная формула стоимости | там же | `base_cost + weight_scale·(…) + urban_cost·[город]` заменена на действующую |
     | `tile_cache_root()` | СБОРКА_EXE | функция называется `cache_root()` и лежит в `view_qt/geomap.py` |
     | `why_route.py`, `check_empty_nature.py` | ОГРАНИЧЕНИЯ | ссылки на разовые скрипты замеров, которых нет в репозитории — заменены на «разовый замер» |

     **Устаревшее нашлось и в моём собственном плане 4**, составленном накануне: пункт
     Д6 предлагал откалибровать `THREAT_LOOKAHEAD_POWER = 1.5` «величину на глаз», хотя
     она уже поднята до **3.0** по замеру (при 3.0 лучше сразу по всем метрикам: вес под
     маршрутом 13.46 → 14.88, доля пути по возвышенностям 24.1 → 17.1 %, высота 90.9 →
     82.7 м, разброс теряет 0.2 п.п.). Пункт исправлен.

     **Уточнено:** флаги `DEMO_*` живут в `config.py` **ветки `demo-build`**, в рабочей
     их нет вовсе — в `CLAUDE.md` это теперь сказано явно. Добавлен радиус влияния
     деревни в правило 3.4 (≈ √площади пятна, зажат в 1…4 км) — единственный параметр
     модели, который не был описан ни по имени, ни по сути.

     **Ложные тревоги, проверенные и отклонённые:** `THREAT_LMAX_CORRIDOR_FRAC = 35 %` —
     законная запись `0.35`; `SPREAD_EPS`, `THREAT_LMAX_AUTO_FRAC`, `THREAT_MAX_GAP_KM`,
     `THREAT_CORRIDOR_SLACK_KM` описаны по сути или через поля `Params` в нижнем регистре.

     **Код не тронут вовсе** — изменялись только `.md`. Проверено прогоном: коридоров
     11 101, компонент 42, городская зона 3 441 ячейка, 250/250 возможных маршрутов,
     15/15 датчиков — совпадает с эталоном [ОГРАНИЧЕНИЯ.md §6](../ОГРАНИЧЕНИЯ.md).
     Битых ссылок между документами: 0 из 23 файлов.

168. **Полная сверка документации С ЗАМЕРОМ, а не только по именам. ✅**
     Предыдущая сверка (п. 167) проверяла ИМЕНА: существуют ли упомянутые параметры,
     функции, файлы. Заказчик попросил повторить с учётом журнала и «чтобы всё било».
     Поэтому сверялись уже ЧИСЛА: снят фактический прогон и сопоставлен с тем, что
     написано в текстах. Это нашло другой класс ошибок — имена верны, а величины нет.

     **Что нашлось (четыре расхождения, все — числа):**

     *1. Эталон §6 был от старых умолчаний.* Таблица требовала «датчиков 12 из 12» и
     «средняя кратность ~8», хотя умолчания давно другие: `threat_N` = 15,
     `threat_R` = **2 км** (было 12). При R = 2 км кратность физически не может быть 8 —
     замер даёт ~6. Строка «засечка 100 %» тоже недостижима: три прогона подряд дали
     **100.0 / 98.7 / 100.0 %** — это стохастика выборки, а не поломка. Эталон, который
     не проходит на умолчаниях, обесценивает сам себя: к нему перестают обращаться.
     Исправлено: указаны ПАРАМЕТРЫ прогона (без них числа не воспроизводятся), нормы
     приведены к реальным, добавлены структура карты, область залёта и длины маршрутов.

     *2. Внутреннее противоречие в одной таблице.* Правило 2.0.1 утверждало «вес под
     маршрутом 11.60 → 22.42», а правило 2.0.8 в той же таблице — «вес 14.89». Оба
     верны, но для РАЗНЫХ состояний: 22.42 мерилось ОДНИМ стоимостным полем, а 2.0.8
     ввело несколько полей жадности, которые намеренно СНИЗИЛИ вес ради разброса.
     Проверено переключением: одно поле — вес 21.3 при покрытии 18.8 %, несколько —
     вес 15.8 при покрытии 36.5 %. Числа 20…23 в правилах 2.0.1, 2.0.1а и 2.0.5
     помечены как «замер одним полем», добавлено правило **2.0.13 с действующими**
     числами и указанием, ЧЕМ мерить: через `plan_routes` (то, что видно на экране), а
     не прямой выборкой — жадный отбор берёт не самые «весовые» линии намеренно.

     *3. Числа без условия замера невоспроизводимы.* Доли зон рельефа «37.3 / 43.7 /
     18.9 %» верны, но только при полосе нейтральности `k = 1 ± 0.10`; при ± 0.02
     выходит 45.1 / 50.6 / 4.3 %. Порог нигде не был указан — дописан.

     *4. Исторические строки эталона выдавались за действующие.* «Коридоров без рельефа
     10 079», «компонент 68» — состояние до пола штрафа деревни и градации пунктов.
     Заменены на действующие (**11 238 / 36 / 10 985** без рельефа, **11 101 / 42 /
     9 583** с рельефом), исторические вынесены в отдельную таблицу с пометкой.

     **Вывод для будущих сверок.** Проверять имена мало: расхождение чаще прячется в
     числах, и особенно — в числах, замеренных на промежуточном состоянии. Полезное
     правило: рядом с каждым числом писать УСЛОВИЯ замера (параметры прогона, режим,
     порог), иначе через месяц его нельзя ни повторить, ни опровергнуть.

     **Код не тронут** — правились только `.md`. Контрольный прогон на чистых
     умолчаниях: 250/250 маршрутов, 150/150 итераций, 15/15 датчиков, 0 на воде,
     0 без пролётов, 0 превышений хода, карта 11 101 / 42 / 9 583. Битых ссылок: 0.

169. **Аудит ВСЕГО журнала с записи 15: что описано, но больше не существует. ✅**
     Заказчик: пройти журнал целиком и там, где механизма уже нет, пометить «не
     актуально, убрано тогда-то» — иначе старая запись читается как действующее описание.

     **Как искалось.** Скрипт разбирает журнал на записи, вытаскивает из каждой имена в
     обратных кавычках и сверяет с текущим кодом (функции, классы, константы модулей,
     поля `Params`, атрибуты `self.*`, аргументы, переменные окружения). Отдельно
     отмечает, есть ли рядом слова «устарело / удалено / отклонено» — то есть не
     помечена ли запись уже.

     Из 154 проверенных записей имена «из прошлого» нашлись в 36. **Почти все —
     законны:** это либо псевдонимы библиотек (`pg`, `np`, `heapq`), либо имена модулей
     и файлов, либо внешний API (Qt, shapely), либо сами записи ОБ УДАЛЕНИИ (п. 115
     «удалён мёртвый код `greedy_placement`», п. 119 и 121 про мёртвые константы).
     Помечать их значило бы засорить журнал.

     **Настоящих мест оказалось четыре — все помечены:**
     | запись | что подано как действующее | как на самом деле |
     |---|---|---|
     | 60 (и 59) | `THREAT_TURN_INTERVAL_KM` = 2.5 км; режимы `heavy/balanced/light/mix`; сглаживание Chaikin | константы нет (убрана в п. 121) — живое поле `Params.threat_turn_interval_km` = 8 км; режимы переименованы в `max/medium/min/mix`; Chaikin заменён на RDP + Катмулл–Ром (п. 108) |
     | 82 | «`THREAT_URBAN_IN_WEIGHT` пока False» — то есть параметр есть, просто выключен | параметра НЕТ вовсе: от «минуса городам» отказались, вес города обнуляется (`THREAT_CITY_ZERO_WEIGHT`), запрет держит маска |
     | 116 | «дымовые прогоны `smoke_all` и `smoke_gui` проходят без ошибок» | скриптов в репозитории нет, они были разовыми; их роль выполняет контрольный прогон §6 |
     | таблица §4 | «исключение города по плотности застройки в окне 3×3 км, вес → штраф» | плотности в окне больше нет: тип пункта из метки `place` + пятна + контуры с собственной градацией (п. 143, 149), вес не штрафуется, а обнуляется |

     **Разделы «Текущее состояние» и «Что осталось» переписаны.** Первый утверждал
     «Qt с ДВУМЯ вкладками» и не упоминал вкладку 3 — то есть бо́льшую часть работы;
     теперь перечислено, что в ней сделано, со ссылками на записи. Второй указывал
     мёртвую ветку разработки и «Этап 3 — маршруты A\*/RRT\* от „Заречья“ к „Аэропорту“»:
     маршруты сделаны, но принципиально иначе — стохастической выборкой, а не поиском
     одного оптимального пути. Помечено как снятое с повестки, действующий список работ
     перенаправлен на [план 4](../планы/4_ПЛАН_ДОРАБОТКИ.md).

     **Карта проекта** ([СТРУКТУРА_ПРОЕКТА.md](../СТРУКТУРА_ПРОЕКТА.md)): фантомных файлов
     не было, но четыре существующих не были описаны — `check_env.py`,
     `inspect_tile_cache.py`, `make_user_guide_docx.py` и `__init__.py`. Добавлены; у
     `build_threat_grid.py` дописано предупреждение о пересборке кэша после правки
     `PLACE_CODES`.

     **Код не тронут** — только `.md`. Прогон: карта 11 101 / 42 / 9 583, 250/250
     маршрутов, 150/150 итераций, 15/15 датчиков. Битых ссылок: 0.

170. **Мёртвый код и дублирование: найдено замером, убрано с проверкой. ✅**

     **Как искалось.** Два скрипта. Первый строит список всех определений через разбор
     кода и считает обращения к каждому имени вне его собственного определения;
     отдельно ищет неиспользуемые импорты, параметры `config.py`, которые не читает ни
     одна строка, и модули, которые никто не импортирует. Второй ищет повторы: одинаковые
     блоки от 6 значащих строк, функции с совпадающим «скелетом» (виды узлов кода без
     имён) и длинные выражения, встречающиеся 4+ раза.

     **МЁРТВЫЙ КОД — убрано 5 функций и 4 импорта.**
     | что | где | почему мёртвое |
     |---|---|---|
     | `bridge_cells_km` | threat_grid | вытеснена `bridge_points_km` — мосты по реальным координатам (п. 45) |
     | `urban_cells_km` | threat_grid | вытеснена `urban_mask` |
     | `no_fly_polys` | threat_grid | запретные зоны сделаны иначе (п. 162) |
     | `add_point_layer` | threat_grid | точечных слоёв в модели не осталось |
     | `_sagitta_from_angle` | trajectories | построение дуги через стрелу прогиба: при θ > 90° дуга вырождалась, заменена `make_arc_by_angle` |
     | импорты `PARAM_SPECS`, `MODE_LABELS`, `_cell_of`, `numpy` | controller, area_controller, threat_grid, area_view | по одному вхождению — сама строка импорта |

     **ОСТАВЛЕНО по решению заказчика:**
     * `save_npz` / `load_npz` — не вызываются из программы, но это **документированный
       API**: методичка §8 объясняет ими решение «карту храним растрово». Удалять функцию,
       на которой стоит раздел документации, нельзя;
     * **курское наследие** — `city_points_km`, `KURSK_CITIES`, `OBLAST_BBOX`,
       `oblast_outline_km`, `reach_ellipse_outline`. Участок давно другой (Северодонецк →
       Луганск), но это демонстрационная схема для работы БЕЗ загруженных данных.
       ⚠️ **Помечено на будущее:** если демо-схема больше не нужна, это ~5 функций и 3
       константы к удалению.

     **Ложная тревога:** `THREAT_MAX_GAP_KM`, `THREAT_CORRIDOR_SLACK_KM`,
     `MOTION_PROFILES` скрипт счёл непрочитанными — они используются ВНУТРИ самого
     `config.py` как значения по умолчанию для полей `Params`, а поиск шёл по остальным
     файлам. Урок: проверяя «читается ли параметр», нельзя исключать файл, где он объявлен.

     **ДУБЛИРОВАНИЕ — вынесено в новый `view_qt/ui_common.py`.**
     * `_apply_stylesheet` был скопирован в `view_qt.py` и `threat_view.py` **побайтово**
       (1153 символа); правка цвета в одной вкладке не доходила до другой. Теперь
       `apply_dark_theme(widget)`, цвета по-прежнему из `THEME`;
     * создание правой панели — **три копии** в `area_view`, `threat_view`, `view_qt`, с
       повторёнными числами 360 и 394. Теперь `make_side_panel()`, а числа стали
       именованными (`PANEL_MIN_WIDTH`, `PANEL_SCROLL_WIDTH`) с объяснением, почему их
       два: панель уже области прокрутки ровно на ширину полосы, иначе полоса налезает
       на поля ввода.

     **Проверено:** все 19 модулей импортируются; все три вкладки строятся offscreen и
     получают ОДИНАКОВЫЙ стиль (907 символов после нормализации Qt) и панель 394 px;
     карта совпала с эталоном §6 побитово — 11 101 / 42 / 9 583, город 3 441; прогон
     250 маршрутов, 150 итераций, 15 датчиков.

     **Не тронуто намеренно:** `geo_cache_root` и `cache_root` — одинаковая логика «путь
     от модуля, переопределяется переменной окружения» под разными именами в разных
     файлах (на этом уже спотыкались: в инструкции по сборке значилось несуществующее
     `tile_cache_root()`). Их объединение затрагивает пути к данным и сборку `.exe` —
     отдельная работа. Мельче: `_threat_cmap`/`_heat_cmap`, три одинаковых обработчика
     `on_iter_mode/spread/spend`, пары `sample_maneuver`/`sample_polyline`.

171. **Сверка ФОРМУЛ и разделов «задел». ✅ Найден класс ошибок, который проверки по
     именам пропустить не могли в принципе.**
     Заказчик спросил прямо: уверен ли я, что вся теория бьётся и ничего важного не
     упущено. Честный ответ был — нет: проверялись имена, значения, ссылки и ключевые
     числа, но не проверялись ДВЕ вещи.

     **1. Математика.** Сверены численно семь формул из методичек с тем, что реально
     считает код: радиус разворота `R_min = V²/(g·tg φ)` (0.5465 км при 180 км/ч и 25°),
     эллипс достижимости (сумма расстояний до фокусов равна `L_max` — проверено в четырёх
     точках), вклад линии в ячейку пропорционально длине пересечения (отрезок в целую
     ячейку даёт полный вес, в половину — половину), гарантия жадного отбора (1−1/e),
     отклик рельефа `f(x)` с асимметрией «вверх круче, вниз слабее» и зажимом в
     [0.25, 1.40], цена шага двумя независимыми сомножителями, полезность датчиков.
     **Все семь сошлись.** Одно «расхождение» оказалось ошибкой в моём тесте: у
     `_cost_mul` ЧЕТЫРЕ параметра (сила по весу и отдельно по рельефу), я передал три —
     рельеф молча остался выключенным.

     **2. Разделы «задел» — вот где нашлось.** Четыре места объявляли заделом на будущее
     то, что уже сделано. Проверки по именам их пропустить не могли: имена там все живые,
     врало только УТВЕРЖДЕНИЕ.
     | где | что было написано | как на самом деле |
     |---|---|---|
     | РАДИОВИДИМОСТЬ_ЗАДЕЛ §6 | «задел: планировщик обхода по стоимости, второе поле Дейкстры» | СДЕЛАНО в п. 151 и 153. Раздел верно предсказал и способ, и все три трудности — включая «поменяются ВСЕ маршруты», что и потребовало правки 2.0.8 |
     | ВКЛАДКА_3 §задел | «радиус разворота, скорость и крен НЕ ограничивают построение» | ограничивают: `TURN_MAX_DEG` = 45° выведен прямо из R_min, плюс окно разворота 3 км / 90° (п. 160) |
     | МЕТОДИЧКА_ВКЛАДКА3_ДОРАБОТКИ §8 | «не реализовано: стохастическая генерация маршрутов» | сделана ещё в п. 60 и с тех пор трижды доведена |
     | МЕТОДИЧКА_КАРТА §11 | «задел: DEM, запретные зоны, costmap» | всё сделано (п. 147–153, 162); маршруты построены иначе, чем там предполагалось — не A\*/RRT\*, а стохастической выборкой |

     Плюс `теория/README.md` называл планировщик заделом. Все пять мест исправлены: не
     удалены, а помечены как реализованные — с указанием, что именно оправдалось из
     предсказаний и чем решение отличается от задуманного.

     **Вывод, который стоит запомнить.** Документация врёт тремя способами, и каждый
     ловится своей проверкой: **имена** — сверкой с кодом (п. 167), **числа** — повторным
     замером (п. 168), **утверждения о готовности** — только чтением. Первые два
     автоматизируются, третий нет: фраза «пока не реализовано» состоит из совершенно
     живых слов. Проверять разделы «задел / что дальше / не сделано» надо отдельно и
     глазами, при каждом крупном изменении.

172. **Методичка вкладок 1–2 сверена численно. ✅ Найдена ловушка единиц угла в коде.**

     **Что было не так.** Методичка вкладок 1–2 (`МЕТОДИЧКА.md`) — единственная, которую
     ещё не сверяли численно: проверки п. 167–171 шли по вкладке 3, где велась работа.
     Формулы (1)–(14) и обещанные ограничения оставались на слово.

     **Что сделано.** Скрипт-сверка (`verify_tabs12.py`) проверял не имена, а
     УТВЕРЖДЕНИЯ — считал по формуле из текста и по коду, сравнивал:
     * формула (6) `R_min = V²/(g·tg φ)` — **0.5465 км** при 180 км/ч и 25°, `g = 9.81`
       совпадает с текстом;
     * формула (8) длина дуги `c·θ/sin θ` — сходится до шестого знака; радиус построенной
       дуги **100.00** при формуле **100.00**; `θ = 0` даёт прямую;
     * формула (1) полезность `φ = α₁·min(n,k)/k + α₂·m/L_seg` — насыщение и нормировка
       на месте; формула (3) SAA-усреднение по выборке — на месте;
     * раздел 6.2 на прогоне: датчиков в зоне старта **0 из 6**, «за спиной» **0**, за
       целью B **0** (правило 6.3);
     * раздел 6.5 на 400 маршрутах: нарушений `R_min` (0.547 км) — **0**.

     **Методичка сошлась целиком** — ни одного расхождения. Зато сверка вскрыла настоящую
     ловушку В КОДЕ: две соседние функции одного модуля берут угол в РАЗНЫХ единицах —
     `arc_length_from_angle(c_len, theta_rad)` в радианах, `make_arc_by_angle(A, B,
     theta_deg, n)` в градусах. Мой тест подставил радианы во вторую и получил радиус
     **5471** вместо **100** — ошибка в 55 раз, молча, без исключения. Записано
     предупреждением в `МЕТОДИЧКА.md` после формулы (8).

     Туда же — порядок работы вкладки 2: без `set_route()` метод `reset()` выходит сразу
     (`if not self.has_route: return`), кэш покрытия не создаётся и `run_batch()` падает.
     Не дефект, но со стороны выглядит поломкой.

173. **Единое правило достоверности и обновления документации. ✅**

     **Зачем.** Проверки п. 167–172 каждый раз изобретались заново, и каждый раз я
     наступал на те же грабли: скрипт давал десятки ложных срабатываний, а разделы
     «задел» проверить забывал. Порядок действий надо было закрепить так, чтобы он
     читался автоматически в начале любой сессии.

     **Что сделано.** В `CLAUDE.md` (читается сам) и `README.md` (для человека) записано
     правило из двух частей:

     **(а) По команде «проверь на достоверность» — пять шагов, все подряд:** имена →
     значения из `config.py` → повторный замер → численная сверка формул → утверждения о
     готовности. Пятый шаг помечен как неавтоматизируемый: проверять только чтением.
     Плюс три предостережения, выведенные из собственных ошибок: скрипт врёт чаще
     документации (из ~90 «расхождений» настоящими были единицы); нельзя исключать из
     поиска файл, где параметр объявлен; рядом с числом писать условия замера. Заканчивать
     контрольным прогоном (`ОГРАНИЧЕНИЯ.md §6`) и записью в журнал.

     **(б) После крупных правок документацию правлю сам, спрашивая разрешения** — не
     только журнал, но и методички. Таблица «что изменилось → что обновить» покрывает
     механику модели, пороги и правила, маршруты, интерфейс, состав файлов и отдельной
     строкой — разделы «задел», если сделано то, что там числилось будущим.

174. **Проверка нового правила на нём самом. ✅ Нашлось расхождение — там, где я
     сказал «всё сходится».**

     **Что было не так.** Сразу после записи правила (п. 173) я прогнал по нему проверку
     свежих текстов — и нашёл настоящее расхождение, хотя за два пункта до этого
     отчитался, что документация сходится. Проверялись ссылки (23 файла) и цвета.

     * **Ссылки — чисто.** Две «битые» оказались ложными: разбор принял Overpass-запрос
       `way["waterway"="river"](48.335,…)` внутри блока кода за markdown-ссылку. Ровно
       тот класс ложных срабатываний, о котором предупреждает правило.
     * **Цвета — настоящее расхождение.** `МЕТОДИЧКА_КАРТА_УГРОЗ.md` §12.0 описывала
       ПРОМЕЖУТОЧНОЕ состояние: цвета `#992d00` / `#565304` / `#960096` и контрасты
       4.54 / 4.70 / 4.53. В коде давно другое: `#9e4825` / `#8a5725` / `#69428c`, плюс
       белый кант, гашение подложки `BASEMAP_FADE = (0.20, 0.10)`, разные штрихи и явный
       порядок `z`. То же устаревшее число тянулось в `ОГРАНИЧЕНИЯ.md`.
     * Два цвета в `НАСТРОЙКА_ИНТЕРФЕЙСА.md` (маджента `#b5179e`, индиго `#3d3bd6`) —
       снова ложная тревога: это таблица ОТВЕРГНУТЫХ кандидатов, помеченных ✗.

     **Почему так вышло.** Правка «одного цвета мало» (кант, гашение, штрихи, порядок)
     была подробно записана в ЖУРНАЛ — п. на строках 1901–1932, вплоть до смягчённого
     порога 3.6 для слоёв с кантом. А методичку и `ОГРАНИЧЕНИЯ.md` тогда не тронули.
     Это ровно тот случай, против которого заведена вторая половина правила 173:
     журнала МАЛО, методички живут отдельно и устаревают молча.

     **Что сделано.** Замерены фактические контрасты на погашенной подложке (худший фон —
     лес `#add19e`): `road_major` **3.71**, `road_local` **3.65**, `railway` **3.62**,
     `river` **3.65** — все с кантом, порог 3.6; `power` **4.59**, `pipeline` **4.54**,
     `tree_row` **4.58**, `built_up` **4.54** — без канта, порог 4.5. **Все восемь слоёв
     норму проходят, код в порядке.** §12.0 переписан по факту: вместо «подобрали тёмный
     оттенок» — четыре независимых приёма против четырёх причин каши, с таблицей замеров.
     В `ОГРАНИЧЕНИЯ.md` число исправлено на 3.62–4.59 с оговоркой про два порога.

     **Заодно найдено слабое место, которого методичка не знала.** Пара «магистраль ↔
     местная» различается всего на **35 единиц RGB** (текст обещал 77) — по цвету они
     почти неразличимы и держатся на толщине (**1.88** раза, текст обещал 1.6) и белом
     канте. Записано прямо в §12.0: разводить их дальше надо толщиной или штрихом, место
     в тёплом секторе кончилось.

     **Вывод.** «Всё сходится» — утверждение, которое стоит ровно столько, сколько
     последняя проверка. Правило 173 окупилось на первом же применении, причём поймало
     мою собственную недоработку.

> **Ниже — новый формат записи** (правило введено в п. 176): суть в четыре строки,
> подробности ссылкой. Разбор и таблицы замеров живут в методичках, не здесь.

175. **Навигация: ссылка ведёт в РАЗДЕЛ, а не в файл. ✅**

     **Проблема.** Указатель в `CLAUDE.md` вёл на файл целиком, а методички большие
     (карта угроз — **1 300** строк): каждый вопрос начинался с поиска грепом.
     **Причина.** Указатель строился по составу документации, а не по вопросам, которые
     реально задаёт заказчик.
     **Решение.** Две таблицы: «вопрос → файл §раздел» (**18** симптомов) и «механизм →
     `файл :: имя`» (**18** точек входа в код).
     **Результат.** Проверка имён прогоном нашла две ошибки в самом указателе:
     `iter_batch` живёт в `threat_grid.py`, а не в `threat_routes.py`; `HELP_HTML` есть
     **только в ветке `demo-build`** — в рабочей кнопки «Справка» нет вовсе, а `CLAUDE.md`
     этого не оговаривал. Обе исправлены.
     **Подробно:** [CLAUDE.md](../../CLAUDE.md), раздел «Навигация».

176. **Правило ведения документации: журнал коротко, методичка подробно. ✅**

     **Проблема.** Журнал разрастался разбором и таблицами замеров, дублируя методички,
     — а те при этом молча устаревали.
     **Причина.** Роли не были разделены: журнал отвечает «когда и почему меняли»,
     методичка — «как работает сейчас», и обе писались как попало.
     **Решение.** Формат пункта: **проблема → причина → решение → результат** (с числами)
     и ссылки на разделы, где раскрыто подробно. Плюс жёсткое требование: **при каждом
     обновлении журнала сверять методички** на актуальность.
     **Результат.** Пункты стали 8–12 строк вместо 30–50 (этот — образец формата).
     **Подробно:** [CLAUDE.md](../../CLAUDE.md) «Правило документирования»;
     [README.md](../../README.md) — то же для человека.

177. **Нумерация `ОГРАНИЧЕНИЯ.md` приведена в порядок. ✅**

     **Проблема.** Подразделы §2.0 и §2.1 физически стояли ПОСЛЕ §3; заголовок «2.1 Два
     понятия» спорил с правилом 2.1 из таблицы; строки таблиц шли вразнобой
     (2.4 → 2.13 → 2.14 → 2.5). Ссылаться по номеру было нельзя.
     **Причина.** Разделы дописывались в конец файла, а номер получали по смыслу.
     **Решение.** Блоки перенесены в §2, спорный заголовок стал безномерным пояснением,
     строки таблиц отсортированы. **Номера самих правил не менялись** — на них ссылаются
     `МЕТОДИЧКА_КАРТА.md`, `РАДИОВИДИМОСТЬ_ЗАДЕЛ.md` и журнал (проверено грепом до правки).
     **Результат.** §2 — 18 перестановок, §3 — 5; было 542 строки, стало 542, отличается
     ровно один переименованный заголовок; повторов номеров в файле больше нет.
     **Подробно:** [ОГРАНИЧЕНИЯ.md §2](../ОГРАНИЧЕНИЯ.md) — врезка «О нумерации».

178. **Указатель по журналу и переносимая методичка по документации. ✅**

     **Проблема.** Журнал вырос до **2 785 строк / 172 пунктов**: открывать его целиком
     ради одного вопроса — тратить контекст на 90 % ненужного.
     **Причина.** У журнала не было оглавления, а размер рос линейно с проектом.
     **Решение.** [указатель журнала](README.md): 13 разделов «диапазон
     пунктов → тема → номер строки → ключевые слова», таблица «симптом → пункты» и
     правило обновлять его **раз в 5 пунктов**. Журнал теперь открывается кусками.
     **Результат.** Оглавление на 94 строки заменяет чтение 2 785. Заодно записано, что
     номера 96 и 100 в нумерации пропущены — их незачем искать.
     **Подробно:** [указатель журнала](README.md); переносимая в другие
     проекты схема — [переносимые правила](../переносимое/README.md).

179. **Оглавления с номерами строк — генерируются скриптом. ✅**

     **Проблема.** Замер показал: вся документация — **≈160 тыс. токенов, это 80 % окна
     в 200k**. Три файла дают половину расхода: журнал ≈50 тыс., карта угроз ≈17 тыс.,
     ОГРАНИЧЕНИЯ ≈14 тыс. (всего 548 строк, но плотные таблицы). Указатель решал вопрос
     только по журналу; к методичкам приходилось открывать файл целиком.
     **Причина.** Ссылки вели в раздел, но НАЙТИ его можно было лишь прочитав весь файл:
     номера строк нигде не хранились.
     **Решение.** `tools/переносимое/docmap.py` (~130 строк) вставляет в документы длиннее 300 строк
     блок `<!-- ОГЛАВЛЕНИЕ -->` — «номер строки · раздел». Генератор, а не ручная работа:
     номера сдвигаются при каждой правке середины файла, и рукописное оглавление начнёт
     врать. Заголовки внутри ``` не считаются; блок вырезается перед пересчётом, чтобы
     не сдвигать сам себя; `--check` возвращает 1, если устарело.
     **Результат.** Оглавления в **10** документах. Проверено: повторный запуск меняет
     **0 файлов** (идемпотентность), `--check` даёт код 0, содержимое сверх блока не
     тронуто ни в одном файле. Вопрос теперь стоит 15 строк оглавления + нужный раздел
     вместо 1 289 строк карты угроз.
     **Подробно:** [переносимые правила](../переносимое/README.md) —
     что автоматизировать (зависящее от расположения текста), а что нельзя (зависящее от
     смысла); [СТРУКТУРА_ПРОЕКТА.md](../СТРУКТУРА_ПРОЕКТА.md) — место скрипта в проекте.

180. **Документация разложена по папкам, журнал разбит на части. ✅**

     **Проблема.** 25 документов лежали в одной папке `теория/` плоским списком, а журнал
     разросся до **2 862 строк**. Поиск шёл перебором имён файлов; открыть журнал ради
     одного пункта означало прочитать ≈50 тыс. токенов.
     **Причина.** Структура не менялась с трёх файлов, а документов стало 25.
     **Решение.** Папки по темам: `журнал/`, `вкладка_3/`, `карты/`, `интерфейс/`,
     `планы/`, `архив/`; **общее для всех вкладок оставлено в корне** `теория/`
     (ОГРАНИЧЕНИЯ, обе методички, структура). У каждой папки **один `README.md`** —
     от файлов-спутников «Ссылки_ИМЯ.md» на каждый документ отказались: 25 лишних файлов
     стали бы 25 источниками правды, устаревающими молча. Журнал разбит на три части по
     порогу **1 500 строк** (`ЖУРНАЛ_1..3.md`), точка входа — указатель.
     **Результат.** Перенос сделан скриптом: **159 ссылок** пересчитано автоматически,
     23 подписи приведены к новым путям, ещё **12** перенаправлены на указатель после
     разбиения журнала. Проверено: битых ссылок **0** из 31 файла, пункты журнала —
     **177**, ни один не потерян и не задвоен, порядок сохранён, сводные разделы 4–7 на
     месте. Файлы перенесены через `git mv` — история переименований сохранена.
     **Заодно** `tools/переносимое/docmap.py` расширен: кроме оглавлений проверяет битые ссылки,
     папку без `README` и файл длиннее 1 500 строк. Он же сразу нашёл 12 битых ссылок,
     которые я иначе оставил бы. Починена и его собственная поломка: печать русского
     текста в консоль Windows (cp1251) роняла скрипт с `UnicodeEncodeError`.
     **Подробно:** [теория/README.md](../README.md) — структура и почему не по вкладкам;
     [переносимые правила](../переносимое/README.md) — переносимая схема;
     [журнал/README.md](README.md) — указатель по частям.

181. **Оглавления врали на свою собственную высоту. ✅ Нашёл заказчик, не проверка.**

     **Проблема.** Все 13 сгенерированных оглавлений указывали неверные строки: в плане
     рельефа «Состав работы» значился на **16**, а лежал на **54**; «Что вскрылось при
     реализации» — **26** против **64**. Сдвиг одинаковый внутри файла и разный между
     файлами: **38** строк в плане рельефа, **20** в ОГРАНИЧЕНИЯХ, **44** в карте угроз.
     **Причина.** Номера считались по тексту БЕЗ блока оглавления — «чтобы блок не
     сдвигал сам себя». Верно наполовину: читатель открывает файл ВМЕСТЕ с блоком, и всё
     ниже места вставки уезжает вниз ровно на его высоту (4 строки шапки + по строке на
     заголовок + закрытие + пустая). Отсюда и разные сдвиги: 32, 14 и 38 заголовков.
     **Решение.** К номерам заголовков ниже места вставки прибавляется высота блока —
     она известна заранее. Добавлена **сверка результата**: скрипт открывает готовый файл
     и проверяет, лежит ли раздел на обещанной строке.
     **Результат.** Расхождений было **84** в трёх проверенных файлах, стало **0** во всех
     31. Сверка сразу нашла и вторую поломку — задвоенный заголовок «## 3. Этапы
     реализации» в `ЖУРНАЛ_1.md`, оставшийся от деления журнала на части.

     **Урок, ради которого пункт написан.** `--check` до этого рапортовал «актуально»:
     он сверял блок с тем, что генератор считает правильным, — то есть подтверждал, что
     генератор согласен сам с собой. **Проверять надо РЕЗУЛЬТАТ, а не намерение**: открыть
     файл так же, как его откроет читатель. Систематическая ошибка иначе не ловится —
     она одинаково искажает и то, что проверяют, и то, чем проверяют.
     **Подробно:** [переносимые правила](../переносимое/README.md) —
     ловушка и требование к автоматике; там же §2.1–2.2 дописаны раскладкой по папкам и
     порогом деления файлов.

182. **Сверка после реорганизации: документация цела, эталон воспроизводится. ✅**

     **Проблема.** За две сессии документация пережила перенос по папкам, деление журнала
     на три части и правку генератора оглавлений. Проверить надо было всё сразу: не
     разъехались ли ссылки, имена, числа и сам расчёт.
     **Причина.** Крупные структурные правки ломают связи молча — ошибка всплывает через
     недели, когда причина уже забыта.
     **Решение.** Прогнаны все пять шагов сверки: ссылки и оглавления (`docmap --check`),
     имена против кода, числа против `config.py`, контрольный прогон, чтение разделов
     «задел».
     **Результат.** **Битых ссылок 0** из 32 файлов, расхождений в номерах строк **0**,
     структура папок в порядке. Контрольный прогон совпал с эталоном §6 **полностью**:
     коридоров **11 101**, город **3 441**, маршрутов **250**, итераций **150**, датчиков
     **15**, на воде **0**, засечка **100 %** (эталон 98.7–100 %), время 18 с.

     **Ложные срабатывания — как и предупреждает правило.** Из **146** «пропавших имён»
     настоящих нет ни одного: библиотеки (`numpy`, `pyrosm`), Qt-классы, исключения
     Python, строковые ЗНАЧЕНИЯ режимов и тегов (`town`, `road_major`, `late`), имена
     папок, удалённый код — о котором журнал сам пишет «удалено». Из **50** «расхождений
     в числах» — тоже ни одного: однобуквенные `N`, `R`, `k`, `T` ловятся внутри чужих
     слов и в условиях чужих замеров, плюс знакомый класс «`X = 0` → прежнее поведение».

     **Настоящая находка одна — в тексте эталона.** §6 обещал прогон «на ЧИСТЫХ
     умолчаниях `Params()`… рельеф ВКЛЮЧЁН», но `THREAT_DEM_ON_START = False`: на чистых
     умолчаниях рельеф ВЫКЛЮЧЕН, и коридоров выходит **11 238** вместо 11 101. Я сам на
     это попался и решил, что сломал карту. В §6 добавлен порядок вызовов: `set_relief(True)`
     до `build()`, `iter_batch()` без цикла, проверка воды методом модели `_on_water`
     (ручной пересчёт `x / h` не учитывает смещение `ox`, `oy` и врёт).
     **Подробно:** [ОГРАНИЧЕНИЯ.md §6](../ОГРАНИЧЕНИЯ.md) — эталон и порядок прогона.

183. **Переносимый комплект: скрипты в одной папке, правила — в другой. ✅**

     **Проблема.** Скрипты сверки существовали только в виде разовых команд — нигде не
     сохранялись, и каждая проверка изобреталась заново. Из-за этого трижды повторялись
     одни и те же ошибки: то не собраны атрибуты, то забыты строковые значения. Правила
     работы лежали одним файлом на 290 строк вперемешку с планом внедрения.
     **Причина.** Инструмент и инструкция к нему жили порознь и не были оформлены как
     комплект, годный к переносу.
     **Решение.** Всё обслуживание документации собрано в [`tools/переносимое/`](../../tools/переносимое/README.md)
     (`docmap.py`, `check_names.py`, `check_numbers.py` + пояснение ко всем трём), а
     правила разложены по темам в [`теория/переносимое/`](../переносимое/README.md):
     контекст и навигация, оглавления и поиск, сверка с кодом, план внедрения. В шапке
     каждой методички — ссылка на её скрипт и команды запуска.
     **Результат.** Фильтры настроены по замеру: имена **146 → 0** находок, числа
     **50 → 0**; настоящих ошибок нашлось две — `angle_step` вместо `angle_step_deg` в
     методичке и мёртвая ссылка на несуществующий документ `etap2`. Перенос путей
     починен автоматически в **23** файлах, битых ссылок **0** из 37. В другой проект
     комплект переносится копированием двух папок.

     **Попутно найдены две поломки в самих скриптах.** Первая: после переезда в
     `tools/переносимое/` корень проекта считался на уровень ниже — `docmap` увидел **0** файлов
     вместо 37. Вторая тоньше: имя новой папки `docs` совпало с исключением для корневой
     `docs/` (там генераторы `.docx`), и проверка перестала видеть собственные скрипты —
     **18** ложных находок. Теперь исключение действует только в корне.
     **Подробно:** [tools/переносимое/README.md](../../tools/переносимое/README.md) — что отсеивают
     фильтры и что делать с находкой; [переносимое/README.md](../переносимое/README.md) —
     как переносить.

184. **Утилиты командной строки: описание собрано в одно место. ✅**

     **Проблема.** Заказчик спросил, что делают файлы в `tools/`. Описания были, но
     разбросаны по трём методичкам: сборка карты — в карте угроз §7, тайлы — в методичке
     карты, чужой кэш — в вопросах по картам. Единого ответа «какая утилита за что и
     когда её запускать» не существовало, хотя папку видно с первого взгляда на проект.
     **Причина.** Каждую утилиту документировали там, где она понадобилась по ходу
     работы, а сводки по папке никто не заводил.
     **Решение.** [tools/README.md](../../tools/README.md): таблица «утилита → когда
     запускать → что делает», команды, предупреждение о пересборке кэша после правки
     `PLACE_CODES` и ссылки на подробности. Отдельным разделом — что за папка
     `__pycache__` и почему её можно удалять.
     **Результат.** Ссылка добавлена в `CLAUDE.md`, `теория/README.md` и
     `СТРУКТУРА_ПРОЕКТА.md`; битых ссылок 0 из 38 файлов. Правило «папка → README»
     теперь выполняется и вне `теория/`.

185. **Полная сверка перед сжатием. ✅ Три расхождения — все пятого класса.**

     **Проблема.** После двух сессий структурных правок нужна была проверка целиком,
     с точкой восстановления: код, документация и указатели правились много раз.
     **Причина.** Шаги 1–4 сверки автоматизированы и дали чистый результат — но именно
     они не видят класс ошибок «утверждение о готовности»: имена живые, чисел нет, врёт
     одно предложение.
     **Решение.** Прогнаны все пять шагов, пятый — чтением всех разделов «задел / что
     дальше / не реализовано» (18 мест в 14 файлах).
     **Результат.** Автоматика чиста: битых ссылок **0** из 38, номеров строк **0**,
     имён **0**, чисел **0**; контрольный прогон совпал с эталоном полностью
     (11 101 / 3 441 / 250 / 150 / 15, на воде 0, засечка **0.993** при норме 0.987–1.0).
     Чтением найдено **три** расхождения:

     | где | что утверждалось | как на самом деле |
     |---|---|---|
     | МЕТОДИЧКА_ЗАГРУЗКА_КАРТ, растровые слои | «рельеф/землепокрытие — отдельный этап, не реализован» | рельеф СДЕЛАН (п. 148): `dem.tif`, `rasterio`, множитель и отсечка. Не сделано только землепокрытие |
     | МЕТОДИЧКА_КАРТА_УГРОЗ §15 | «`R_min`, скорость и крен служат ориентиром при сглаживании, а не ограничением построения» | ограничивают с п. 160: `TURN_MAX_DEG` = 45°, окно разворота 3 км / 90° |
     | МЕТОДИЧКА_КАРТА_УГРОЗ §15 | «тяга к цели — по чистой ГЕОМЕТРИИ, ни вес, ни рельеф в неё не входят; полное решение — второе поле Дейкстры» | ровно это сделано в п. 151 и 153: `gt_geo` + `gt`, `_dijkstra_cost`, `_cost_mul`. Раздел противоречил РАДИОВИДИМОСТЬ_ЗАДЕЛ §6, где уже стояло «больше не задел» |

     Все три помечены реализованными с указанием, **что именно осталось** незакрытым:
     землепокрытие, непрерывное планирование (Дубинс/RRT\*), глобальный обход массива.
     Попутно убран дублирующийся абзац в МЕТОДИЧКА_ЗАГРУЗКА_КАРТ (два почти одинаковых
     объяснения демо-схемы подряд).

     **Вывод, третий раз подряд один и тот же.** Пятый шаг не автоматизируется и делать
     его надо **при каждой крупной правке**: два из трёх расхождений жили с августа, хотя
     сверки по именам и числам проходили чисто. Более того, один раздел противоречил
     другому — то есть проверить можно было и сопоставлением текстов между собой.
     **Подробно:** [ОГРАНИЧЕНИЯ.md §6](../ОГРАНИЧЕНИЯ.md) — эталон и порядок прогона;
     [3_СВЕРКА_С_КОДОМ.md](../переносимое/3_СВЕРКА_С_КОДОМ.md) — пять шагов целиком.

186. **Шестой шаг сверки: документы против друг друга. ✅ Идея заказчика.**

     **Проблема.** Расхождения из п. 185 можно было поймать не чтением 27 файлов, а
     сопоставлением текстов: §15 методички объявлял заделом стоимостную тягу, а
     РАДИОВИДИМОСТЬ_ЗАДЕЛ §6 в это же время был помечен «✅ РЕАЛИЗОВАН». Документы прямо
     противоречили друг другу, но никто их не сравнивал — пять шагов сверки проверяли
     только «текст против кода».
     **Причина.** Класс ошибок «текст против текста» в правилах отсутствовал как таковой.
     **Решение.** `tools/переносимое/check_claims.py`: для каждой ТЕМЫ (механизма) собираются
     абзацы с тоном «сделано» и «не сделано»; если стороны в РАЗНЫХ файлах — это кандидат
     в противоречие. Тем задано **9**, при переносе в другой проект меняется только
     словарь `TOPICS`.
     **Результат.** На чистой документации **1** кандидат (проверен, законный) вместо
     сплошного чтения 27 файлов. Шаг записан шестым в `CLAUDE.md` и в переносимую
     методичку.

     **Скрипт не поймал то, ради чего написан, — и это выяснил тест, а не рассуждение.**
     Первая версия помечала абзац «…вынесено отдельно, **не сделано**» как ГОТОВЫЙ:
     маркер `сделан` находился внутри `не сделано`. Добавлена проверка отрицания перед
     маркером и встроенная самопроверка `--selftest` на реальном абзаце из §15 — теперь
     регрессия этого рода видна сразу.
     **Подробно:** [tools/переносимое/README.md](../../tools/переносимое/README.md) — все шесть проверок;
     [3_СВЕРКА_С_КОДОМ.md](../переносимое/3_СВЕРКА_С_КОДОМ.md) — шаги и ложные срабатывания.

187. **Обзор задачи размещения датчиков — заготовка для статьи. ✅**

     **Проблема.** Материал для публикации был разнесён по четырём документам: формулы
     полезности в `МЕТОДИЧКА.md §7–8`, постановка weighted maximum coverage в
     `МЕТОДИЧКА_КАРТА_УГРОЗ.md §6`, ограничения на позиции в `ОГРАНИЧЕНИЯХ §1`, числа
     замеров в `§6`. Собрать это в статью значило заново обойти всю документацию.
     **Причина.** Документация писалась под работу («как починить»), а не под изложение
     («в чём метод»), — разные жанры и разный порядок изложения.
     **Решение.** [СТАТЬЯ_МАТЕРИАЛЫ.md](../вкладка_3/СТАТЬЯ_МАТЕРИАЛЫ.md),
     **315 строк**, 11 разделов в порядке научной работы: цель и задачи → постановка →
     дискретизация → вес ячейки → формальная задача → метод и гарантия → метрики →
     параметры → результаты → **соответствие «формула → функция кода»** → честные
     ограничения и направления развития. Модель порождения маршрутов намеренно опущена:
     это отдельная тема, взято только то, без чего не сформулировать вход.
     **Результат.** Все числа сверены прогоном, не по памяти: кандидатов **4 436**,
     сочетаний `C(4436,15)` ≈ **10⁴³** (считано, а не «порядка»), сетка 189 × 151 =
     **28 539** ячеек, засечка **98.7–100 %**. Имена всех **23** функций в таблице
     реализации проверены на существование в указанных модулях.

     **Попутно найдено расхождение в ОГРАНИЧЕНИЯХ.** Правило 1.8 обещало «N = 12,
     R = 12 км», тогда как в `config.py` **15** и **2.0 км**, и эталон §6 того же файла
     говорил 15 и 2. Сверка чисел это пропускала: значения были записаны как «N=12»,
     а скрипт ищет имя параметра (`threat_N`). Правило переписано именами — теперь
     попадает под автоматическую проверку.

     **Файл намеренно не встроен в навигацию** (нет в `README` папки): это не рабочая
     методичка, а материал для публикации, живущий своей жизнью. Отмечен только в
     `СТРУКТУРА_ПРОЕКТА.md` и здесь.

188. **Датчики ставятся по МАРШРУТАМ, а не по весовой карте. ✅ Поймал заказчик.**

     **Проблема.** Материал для статьи (п. 187) утверждал, что расстановка на вкладке 3 —
     это покрытие взвешенных ячеек, частный случай `T` = 1. Заказчик возразил: сначала
     строятся предполагаемые маршруты, датчики ставятся по ним, а при итерациях
     подстраиваются. Проверка кода подтвердила его правоту.
     **Причина.** Описание тянулось из §6.2 методички, написанного на этапе «пока без
     итераций». С п. 98 расстановка переведена на выборку маршрутов, а §6.2 остался — и
     полгода противоречил правилу 1.7 ОГРАНИЧЕНИЙ: «ставятся по МАРШРУТАМ, а не по весу».
     **Решение.** Восстановлен фактический порядок (`place_sensors`):

     ```
     маршрутов < 5          → сначала построить предварительные «возможные маршруты»
     выборка = iter_routes, если их ≥ 5; иначе routes
     выборка ≥ 5 маршрутов  → _place_by_routes   ← ОСНОВНОЙ путь
     выборки нет            → _place_by_weight   ← запасной, это и есть T = 1
     ```

     Плюс пересчёт каждые `THREAT_SENSOR_REFRESH_EVERY` = 5 накопленных маршрутов.
     **Результат.** Исправлены оба документа: §6.2 методички (помечен как запасной путь,
     с порядком выбора режима) и раздел 5.3 материалов для статьи — там теперь три стадии
     работы и правила расстановки по маршрутам (кандидаты только между `A` и `B`, два
     якоря, разнос 1.6·`R`). В раздел 4 добавлена **основная** целевая функция
     `F_t(S) = (1/t)·Σ φ(S, γ_i)`, прежняя (по весу ячеек) стала вырожденной.

     **Почему шестой шаг это не поймал.** В `check_claims.py` не было темы «по чему
     ставятся датчики» — а без темы противоречие невидимо. Тема добавлена. Урок: словарь
     тем и есть слабое место проверки, его надо пополнять при каждом крупном механизме.
     **Подробно:** [МЕТОДИЧКА_КАРТА_УГРОЗ.md §6.2](../вкладка_3/МЕТОДИЧКА_КАРТА_УГРОЗ.md);
     [СТАТЬЯ_МАТЕРИАЛЫ.md §4–5](../вкладка_3/СТАТЬЯ_МАТЕРИАЛЫ.md);
     [ОГРАНИЧЕНИЯ.md 1.7 и 1.9](../ОГРАНИЧЕНИЯ.md).

189. **Словарь тем проверки противоречий: 10 → 19. ✅**

     **Проблема.** Проверка `check_claims.py` знает только те механизмы, что перечислены
     в её словаре `TOPICS`. Их было **10**, и механизма «по чему ставятся датчики» среди
     них не оказалось — расхождение из п. 188 проверка молча пропустила, отчитавшись
     «противоречий нет».
     **Причина.** Словарь заполняется вручную, а список механизмов проекта давно шире.
     **Решение.** Добавлено девять тем, описанных сразу в нескольких документах и потому
     склонных разъезжаться: свободные зоны, мосты и переправы, область залёта, сглаживание
     линии, шаг сетки кандидатов, якорные датчики и разнос, подложка и тайлы, сборка
     `.exe`, кэш `threat_layers.npz`.
     **Результат.** Тем **19**, кандидат в противоречия по-прежнему **1** (проверен,
     законный). Отдельно проверено, что **ни одна тема не молчит**: каждая находит от 1
     до 167 абзацев — тема с нулём находок означала бы опечатку в шаблоне и ложное
     ощущение проверенности.

     **Попутно — ловушка регистра.** Шаблон `threat_layers` ловил и кэш
     `threat_layers.npz`, и словарь весов `THREAT_LAYERS`: поиск нечувствителен к
     регистру, а вещи разные. Расширение пришлось указывать явно.
     **Подробно:** [tools/переносимое/README.md](../../tools/переносимое/README.md) — раздел «Слабое
     место проверки противоречий» и правило пополнения словаря.

190. **Учебное пособие из четырёх частей: теория с нуля, 23 рисунка. ✅**

     **Проблема.** Теория проекта существовала в двух несовместимых видах: рабочие
     методички отвечали «как устроено сейчас», а четыре документа Word в `теория/основа`
     (**102 тыс. знаков**) — «что это вообще значит», но были написаны в разное время и
     местами противоречили коду. Новичок не мог войти в задачу, а для статьи материал
     приходилось собирать заново.
     **Причина.** Пояснительный слой никогда не сверялся с кодом: методички
     обновлялись, исходники Word — нет.
     **Решение.** Собрано пособие [теория/пособие/](../пособие/README.md) из четырёх
     частей (**≈3 800 строк**): теория оптимизации, язык математической записи и
     геометрия, обзор вкладок 1–2, обзор вкладки 3. Каждое понятие даётся дважды —
     строгая формулировка, затем обиходная, плюс числовой пример и типовая ловушка.
     **Результат.** **23 рисунка** (`make_figures.py`), каждый **считается по той же
     формуле, что записана в тексте**, — картинка не может разойтись с формулой.
     При сборке найдено **пять расхождений исходников с кодом**: военная постановка
     («Shahed-136») против гражданской; прогон при `L_max` = 130, `R` = 14 при
     умолчаниях 150 и 12; структура кода по устаревшей tkinter-версии; «перекрытие
     ≈ 10 %» без указания знаменателя (21 % от круга против 12 % от объединения);
     разделы про фильтр Калмана и энтропию, написанные как реализованные.
     Числа вкладки 1 перезамерены на текущих умолчаниях: покрытие пути **46.6–66.1 %**,
     средняя кратность **3.27–4.92**, доля маршрутов с ≥ `k` — **81–100 %**.

     **Подробно:** [теория/пособие/README.md](../пособие/README.md) — состав, порядок
     чтения и таблица расхождений; [теория/основа/README.md](../основа/README.md) — что
     в исходниках устарело и что намеренно не перенесено.

191. **Папка `tools` разделена: переносимое отдельно от программного. ✅**

     **Проблема.** Скрипты, применимые в **любом** проекте (проверки документации,
     генератор рисунков), лежали вперемешку с теми, что знают про весовую сетку и тайлы.
     Правила к ним жили в `теория/переносимое`, скрипты — в `tools/docs`: чтобы
     перенести комплект в новый проект, нужно было помнить оба места.
     **Причина.** Папки заводились по мере появления, признак деления не был назван.
     **Решение.** Признак — привязка к проекту. Переносимые скрипты собраны в
     [tools/переносимое/](../../tools/переносимое/README.md), программные остались в
     корне `tools/`; правила — в [теория/переносимое/](../переносимое/README.md), туда
     же добавлены два новых. Пересчитано **122 ссылки в 34 файлах**.
     **Результат.** Битых ссылок **0** из 52 файлов. Попутно исправлена ссылка
     `../docmap.py`, указывавшая мимо ещё до переезда.

     **Попутно — поиск корня по маркеру.** Скрипты вычисляли корень счётом уровней
     вверх (`dirname` трижды). При переносе такая запись врёт **молча**: скрипт
     запускается и проверяет не те файлы — это уже случалось при переезде в `tools/docs`
     («нашёл 0 файлов вместо 37»). Заменено на подъём до ближайшей папки с маркером
     (`CLAUDE.md` либо `config.py`); при переносе меняется только список маркеров.
     **Подробно:** [tools/переносимое/README.md](../../tools/переносимое/README.md) —
     раздел «Где скрипт ищет корень проекта».

192. **Разбор остановок по транскрипту: 76 % команд не проходили список разрешений. ✅**

     **Проблема.** Работа часто прерывалась запросом разрешения, в том числе на
     безобидные действия — открыть файл, посмотреть настройки, запустить свой
     вспомогательный скрипт.
     **Причина.** Установлена разбором транскрипта сессии (**12 079 строк, 4 249 ходов,
     757 команд**): список разрешений покрывал лишь **24 %** вызовов, но главная причина
     оказалась не в его длине — **656 команд из 757 начинались с `cd "путь" &&`**, а
     правило сверяется с НАЧАЛОМ команды, и разрешение на `python …` просто не
     срабатывало. Рабочая директория и так корень проекта: приставка была лишней.
     **Решение.** Действия разделены на три категории: разведка и свои скрипты — молча;
     затратные прогоны (проверки документации, сборка сетки, тайлы) — по решению;
     необратимое и git — только по команде. `.claude/settings.json`: **38 правил в
     `allow`, 12 в `ask`**.
     **Результат.** Отдельно посчитаны содержательные остановки: **25 ходов** окончены
     вопросом, из них около **десяти лишних** — «Начинаю?», «Исправлять?»,
     «Пересобирать кэш?» — заданных после того, как план уже изложен и ответ ничего не
     менял. Правило проверки вопроса на нужность: **изменит ли ответ мои действия?**

     **Подробно:** [5_РАЗРЕШЕНИЯ_КОМАНД.md](../переносимое/5_РАЗРЕШЕНИЯ_КОМАНД.md) —
     категории, оформление списка и ловушка с `cd`;
     [6_КОММИТЫ_GIT.md](../переносимое/6_КОММИТЫ_GIT.md) — правила работы с историей.

193. **План 5 на комплексную правку; журнал сокращён вдвое переносом в архив. ✅**

     **Проблема.** Предстоит работа из шести связанных задач (новая векторная карта,
     сектор появления БПЛА, правила и два типа датчиков, порядок расстановки, превью),
     затрагивающая **шесть файлов кода**. Без плана она не переживёт сжатие контекста:
     на середине станет непонятно, что уже сделано и почему выбрана именно эта схема.
     **Причина.** Задачи связаны зависимостями (порядок расстановки опирается на сектор
     и на два типа датчиков), и произвольный порядок работ приведёт к переделкам.
     **Решение.** Составлен [5_ПЛАН_СЕКТОР_И_ДАТЧИКИ.md](../планы/5_ПЛАН_СЕКТОР_И_ДАТЧИКИ.md):
     границы работы (что меняется, а что НЕТ), карта кода с номерами строк по каждой
     задаче, порядок работ с обоснованием, ловушки и контрольные проверки.
     **Результат.** Разбором кода установлены два факта, определяющие трудоёмкость:
     (1) поля Дейкстры считаются **от цели**, поэтому пять точек входа **не требуют**
     пересчёта дорогой части контекста; (2) немодальное окно в проекте уже есть
     (`InputDataDialog`) — новое делается по образцу. Пять открытых вопросов к
     заказчику собраны в §10 плана, чтобы задать разом.

     **Попутно — журнал сокращён.** Части 1 и 2 (пункты 1–151, история каркаса и
     завершённых этапов) вынесены в `теория/архив/журнал/`. Живой журнал: **3 311 →
     1 793 строки** (≈50 → ≈27 тыс. токенов), архив 1 518 строк. Ничего не удалено:
     ссылки из «быстрого поиска» ведут в архив, пункты доступны как раньше.
     **Подробно:** [указатель журнала](README.md) — деление на живую часть и архив.

     **⛳ ТОЧКА ВОССТАНОВЛЕНИЯ — коммит `33e86a0`, отправлен на GitHub.**
     Ветка `relief-fixes`, `d734352..33e86a0`, **96 файлов, +80 932 / −2 778**.
     Зафиксировано всё, что описано в пунктах **190–193**: учебное пособие с рисунками,
     разделение `tools` на переносимое и программное, правила разрешений и коммитов,
     сокращение журнала, план 5.

     ⚠️ **Коммит захватил и незакоммиченную работу прошлых сессий** — правки
     `model/threat_grid.py`, `view_qt/*.py`, `controller/*.py` и перемещения
     документации по папкам. Они лежали в рабочем дереве и ушли вместе с остальным;
     это отмечено сознательно, чтобы при разборе истории не искать, откуда они взялись.

     **Не вошли в коммит:** личные заметки `теория/мне.txt` (156 КБ) и
     `теория/ошибка сборки.txt` — по правилу
     [6_КОММИТЫ_GIT.md §5](../переносимое/6_КОММИТЫ_GIT.md) черновики в историю не идут.
     Кэши (`geo_cache/`, `tile_cache/`, `__pycache__`) отсечены `.gitignore` — проверено
     до коммита.

     **Дальше работа идёт в новой ветке `sector-sensors`**, заведённой от `33e86a0` под
     план 5. На сервер она не отправлена: публиковать пустую ветку незачем, push пойдёт
     вместе с первым содержательным коммитом.

194. **Задача 1 плана 5: участок карты стал переключаемым, новый район подключён. ✅**
     **Проблема.** Заказчик дал новый кусок местности (`geo_cache/APX.osm.pbf`, район
     Плесецк — Мирный), а участок был зашит одной рамкой: `THREAT_BBOX_LONLAT` плюс
     жёстко заданные `threat_layers.npz` и `dem`. Смена рамки означала бы затирание
     кэша прежнего района, на котором сняты **все 11 101 коридора эталона**.
     **Причина.** Участок считался одной величиной (границы), хотя на деле это
     **четыре связанные вещи**: границы, исходник `.osm.pbf`, файл разобранных слоёв
     и рельеф. Рельеф и подложка от прежнего района подставлялись бы молча — файл на
     месте, ошибок нет, но описывает другую местность.
     **Решение.** Реестр `THREAT_AREAS` в `config.py` с активным ключом `THREAT_AREA`:
     обе записи рабочие, переключение — одна строка. Кэш слоёв у каждого участка свой
     (`layers_cache_path`), `dem_files` терпит участок без DEM, `build_threat_grid.py`
     берёт исходник и приёмник из участка.
     **Результат.** Новый участок 76.8 × 59.3 км, сетка **154 × 119 = 18 326 ячеек**,
     коридоров **1 576**, ячеек с весом 21.4 %. Полный прогон: маршрутов **250/250**,
     итераций **150/150**, датчиков **15/15**, на воде **0**, засечка ≥ k — **100 %**,
     **11.2 с**. Прежний участок цел: `threat_layers.npz` не тронут, копия
     `threat_layers_2026-08-28_severodonetsk.npz`.
     ⚠️ **Две дыры участка:** рельефа нет (нужны `N62E040…N63E041.hgt`), подложка в
     `tile_cache/` от прежнего района.
     **Побочный урок.** Раздел плана про конвертер шести векторных форматов (60 строк)
     оказался лишним целиком: формат прежний. Вопрос был задан ПОСЛЕ написания раздела.
     Правило вынесено в переносимые: [7_СОСТАВЛЕНИЕ_ПЛАНА.md](../переносимое/7_СОСТАВЛЕНИЕ_ПЛАНА.md)
     — разведка → вопросы → текст, у каждого вопроса умолчание, чтобы работа не вставала.
     **Подробно:** [план 5 §3](../планы/5_ПЛАН_СЕКТОР_И_ДАТЧИКИ.md) — что сделано, числа
     двух участков и что осталось; [МЕТОДИЧКА_КАРТА_УГРОЗ §1.1](../вкладка_3/МЕТОДИЧКА_КАРТА_УГРОЗ.md)
     — границы и различия участков; [ОГРАНИЧЕНИЯ §5.1, 5.1а](../ОГРАНИЧЕНИЯ.md) — правило
     реестра; [§6](../ОГРАНИЧЕНИЯ.md) — предупреждение, что эталон снят на прежнем участке;
     [МЕТОДИЧКА_ЗАГРУЗКА_КАРТ §1, §4.1](../карты/МЕТОДИЧКА_ЗАГРУЗКА_КАРТ.md) — выгрузка
     нового bbox и какие тайлы DEM нужны.

     ✅ **Ответы заказчика на вопросы плана** записаны в [§12](../планы/5_ПЛАН_СЕКТОР_И_ДАТЧИКИ.md):
     формат прежний; «перекрытие цели ≤ 30 %» считать от зоны датчика, цель не в центре;
     сектор — 45° в каждую сторону от оси «цель → клик»; старт из пяти точек равномерно
     случайно, вес — только при отборе самих точек; **якоря должны работать внутри
     жадного алгоритма** (`greedy(anchors=…)`), а не отдельной процедурой. Открыт один
     вопрос — картинка для превью.

195. **Порог «город» подогнан под новый участок; адаптивный показатель — в план. ⏳**
     **Проблема.** Пятна населённых пунктов нового участка — 0.25 … 4.0 км² против
     0.25 … 42.75 на прежнем, и порог `THREAT_TOWN_CITY_KM2` = 4.0 (подобранный по
     прежнему участку) встал ровно на край распределения: городами считались **2 пятна
     общей площадью 1.8 км²**.
     **Причина.** Это **единственное абсолютное число** в цепочке разметки НП: рядом
     `settlement_stats`, `place_profiles`, `grade_places` давно считают пороги по данным
     участка, а «город или деревня» решается константой.
     **Решение.** Временно снижен до **2.0 км²** (решение заказчика: «пока поменяй на 2,
     потом сделаем адаптивным»). В `config.py` рядом с числом записано, что это подгон и
     где его замена.
     **Результат.** Новый участок: пятен-городов **2 → 4**, их площадь **1.8 → 6.2 км²**;
     городская зона с буфером не изменилась (**227 ячеек**, 56.8 км²) и коридоров
     осталось **1 500** — новые города попали туда, где буфер соседних уже запрещал
     пролёт. ⚠️ Прежний участок от того же порога: городская зона **4 805 → 5 175**
     ячеек (+7.7 %), зон 47 → 51, коридоров 3 655 → 3 557. Эталон §6 снят при 4.0.
     ⚠️ **Исправление пункта 194:** там было сказано «городов на участке нет ни одного» —
     неверно. Правило сравнивает `≥`, а крупнейшее пятно ровно 4.0 км², поэтому города
     были и при старом пороге. Ошибка родилась из чтения порога как строгого «крупнее».
     **Подробно:** [план 5 §9](../планы/5_ПЛАН_СЕКТОР_И_ДАТЧИКИ.md) — предлагаемый
     показатель по РАЗРЫВУ в распределении площадей, отвергнутые варианты и проверка
     «не подгон» на прежнем участке; [§10](../планы/5_ПЛАН_СЕКТОР_И_ДАТЧИКИ.md) —
     задача про рельеф нового участка и его влияние на вес.

196. **Сектор появления БПЛА: пять точек входа вместо одной. ✅**
     **Проблема.** Точка появления была ОДНА и жёстко задана меткой участка — модель
     описывала лишь сценарий «известно, откуда летит». Реальная постановка другая:
     известно только НАПРАВЛЕНИЕ, откуда угроза может прийти.
     **Причина.** Вход был скаляром на всех уровнях: `entry_km()` → `flight_envelope`
     → `build_iter_context` → `ctx["start"]`. Пять входов «в лоб» означали бы пять
     контекстов, то есть впятеро больше Дейкстр.
     **Решение.** Клик по краю карты задаёт ось «цель → точка», от неё сектор. По краю
     внутри сектора отбираются 5 точек: по весу, с разносом, без непроходимых. В ядре
     маршрутов дорогие поля (Дейкстры от цели и от каждой VIA) считаются ОДИН раз на
     все входы, а зависящее от входа — ось, `via_minlen`, достижимость — сложено в
     `ctx["entries"]`. Старт разыгрывается равномерно (вес участвует при отборе точек,
     а не при выборе старта).
     **Результат.** 5 точек, все на краю (**≤ 0.75 км** от рамки), углы от оси
     **3.5–23.9°** при пределе 30°; маршруты стартуют ровно из них, в выборке
     представлены **все пять**. Прогон: 250 маршрутов, 150 итераций, 15 датчиков,
     засечка ≥ k **100 %**, **10.3 с** против 11.2 с с одним входом — пять входов НЕ
     подорожали.
     **Правки заказчика по ходу.** (1) Раствор **60°, а не 90°**: ±45° захватывал
     слишком широкую дугу края. (2) **Своя палитра**: зелёная разметка сливалась с
     весовой картой — взят белый (в палитре turbo белого нет вовсе) с тёмной обводкой
     и подложкой под пунктиром. (3) **Чекбокс «Сектор появления»** — прячет разметку,
     не отменяя сектор. (4) **Пауза повтора** `THREAT_SECTOR_ENTRY_COOLDOWN` = 2:
     точка не повторяется, пока не отработают две другие — равный жребий давал серии,
     и на коротком прогоне часть точек не участвовала вовсе. Замер на 40 розыгрышах:
     повторов подряд **0**, через один **0**, отработали **все 5**. (5) **Достижимость
     проверяется при отборе точек** (одно поле Дейкстры от цели): из точки, откуда до
     цели нет пути, БПЛА не полетит — прежде такая точка попадала бы в пятёрку и просто
     съедала попытки. (6) Зафиксировано, что **клик — ЦЕНТР сектора**, а не его край:
     вариант «сектор от клика в одну сторону» отклонён — указанное направление логичнее
     видеть серединой, иначе сектор уезжает вбок от того, куда указали.
     ⚠️ **Qt-часть проверена компиляцией и сверкой имён, а не запуском:** в окружении
     разработки не поднимается плагин `offscreen` («no Qt platform plugin could be
     initialized»). Работоспособность кнопки подтвердил заказчик запуском программы.
     **Подробно:** [МЕТОДИЧКА_КАРТА_УГРОЗ §14.0](../вкладка_3/МЕТОДИЧКА_КАРТА_УГРОЗ.md)
     — геометрия сектора, отбор точек, почему пять входов не дороже одного;
     [ОГРАНИЧЕНИЯ 2.0а–2.0в](../ОГРАНИЧЕНИЯ.md) — правила и параметры;
     [НАСТРОЙКА_ИНТЕРФЕЙСА §10б](../интерфейс/НАСТРОЙКА_ИНТЕРФЕЙСА.md) — палитра и
     почему белый; [план 5 §4](../планы/5_ПЛАН_СЕКТОР_И_ДАТЧИКИ.md) — что сделано и
     где что лежит.

197. **Рельеф нового участка; кэш разложен по папкам участков. ✅**
     **Проблема.** На участке `plesetsk` рельефа не было: `dem.tif` покрывал прежний
     район (lon 38.30–39.70), и механизм, дающий **до +4 % коридоров**, простаивал.
     Заказчик положил `ARX_hh.tif`, но в корне `geo_cache/` уже лежали вперемешку файлы
     ДВУХ участков — по имени было не понять, чей это растр и какой `.npz` к какому
     району.
     **Причина.** Кэш проектировался под один участок. Подстановка чужого рельефа при
     этом НЕ ВИДНА: файл читается, ошибок нет, просто описывает другую местность.
     **Решение.** У участка появилась своя папка (`THREAT_AREAS[...]["dir"]`), поиск
     файлов идёт через `area_cache_dir` / `area_file` — сначала в папке участка, потом
     в корне кэша (старые раскладки и `.hgt`-тайлы продолжают работать). Файлы
     разложены: `geo_cache/plesetsk/` и `geo_cache/severodonetsk/` (+ `архив/` для
     старых версий слоёв — ничего не удалено).
     **Результат.** Высоты легли во **все 18 326 ячеек**; высоты участка 22 … 203 м,
     медиана 103. Множитель `k`: медиана **1.013**, диапазон **0.250 … 1.400**, ячеек
     с `k` = 1.0 — **0 %**, возвышенностей 12.4 %, укрытий 13.4 %. Коридоров
     **1 576 → 1 645** (+4.4 %), прогон 12.7 с, засечка 100 %. Прежний участок после
     перекладки цел: 28 539 из 28 539 ячеек с высотами, коридоров 3 917.
     ⚠️ **Отсечка «очень высокая гора» не срабатывает нигде** (0 ячеек): её пороги
     80/50 м на равнине недостижимы. Правило спит — для этой местности это верно.
     ⚠️ **Опасение о перекалибровке порогов не подтвердилось.** В плане было записано,
     что на равнине рельеф может «молча выключиться» (`k` ≈ 1 везде) — замер показал
     обратное: превышения на 15-км и 2-км масштабах есть, множитель работает.
     **Подробно:** [план 5 §10](../планы/5_ПЛАН_СЕКТОР_И_ДАТЧИКИ.md) — таблица замеров;
     [МЕТОДИЧКА_ЗАГРУЗКА_КАРТ](../карты/МЕТОДИЧКА_ЗАГРУЗКА_КАРТ.md) — раскладка папок и
     параметры файла; [ОГРАНИЧЕНИЯ 5.1б, 5.7](../ОГРАНИЧЕНИЯ.md) — правило папки
     участка; [МЕТОДИЧКА_КАРТА_УГРОЗ §1.1](../вкладка_3/МЕТОДИЧКА_КАРТА_УГРОЗ.md) —
     чем равнинный рельеф отличается от донецкого.

198. **Пересчёт по «Применить», а не по каждому клику; сектор и точки — разные слои. ✅**
     **Проблема.** Цель и сектор задают ПОДРЯД («поставил цель — очертил сектор»), а
     пересчёт запускался на каждом клике: **два полных прогона** вместо одного, причём
     первый — по промежуточной обстановке (новая цель со старым сектором). Заказчик:
     «поменяли цель — перерасчёт, поменяли сектор — перерасчёт, не очень понятно».
     **Причина.** Обработчики кликов сами вызывали `_run_async`, хотя кнопка «Применить»
     ровно для этого и существует.
     **Решение.** Клик только задаёт обстановку и ставит флаг `_pending_recalc`; карта
     сразу показывает новую цель и точки входа, а маршруты и датчики убираются — они
     построены по прежней обстановке. Расчёт запускает «Применить», один раз по
     итоговому состоянию; заголовок прямо об этом говорит.
     **Результат.** Прогонов на связку «цель + сектор» **2 → 1**; исчезло состояние,
     когда на карте маршруты от старого сектора.
     **Заодно — два слоя показа вместо одного.** Было: одна галка «Сектор появления» на
     всё. Стало **«Точки входа»** (включена — они участвуют в расчёте) и **«Границы
     сектора»** (выключена — служебная разметка «откуда отбирали»): после отбора линии
     свою работу сделали, а на карте важны точки.
     **Подробно:** [МЕТОДИЧКА_КАРТА_УГРОЗ §14.0](../вкладка_3/МЕТОДИЧКА_КАРТА_УГРОЗ.md)
     — «Когда считаются маршруты» и таблица слоёв;
     [НАСТРОЙКА_ИНТЕРФЕЙСА §10б](../интерфейс/НАСТРОЙКА_ИНТЕРФЕЙСА.md) — чекбоксы;
     [план 5 §4](../планы/5_ПЛАН_СЕКТОР_И_ДАТЧИКИ.md) — решения 7–8.

---

## 4. Возникшие проблемы и исправления

| Проблема | Причина | Исправление |
|---|---|---|
| Вкладки 1 и 2 падали в «Пакетно»/«Шаг» (TypeError) | обобщение `CoverageCache.greedy` под вкладку 3 сменило `anchor_idx`→`anchors` (список), а вызовы в `simulation.py`/`area_start.py` не обновили | 4 вызова переведены на `anchors=[ai] if ai is not None else None` (п. 113) |
| Дуга вырождалась при θ>90° | построение через стрелу прогиба давало малую дугу | прямая параметризация центральным углом (`make_arc_by_angle`) |
| «Манёвр» шёл по прямой | целевой случайный блужданий схлопывался к прямой | переход на боковые гармоники, затем на модель по путевым точкам |
| Точка B рисовалась не там (вкладка 2) | `_sync_geometry` брал A/B из `p` (дефолт), а не из клика | брать `model.A/model.B` |
| Повторное «Создать маршрут» не сбрасывало | старый маршрут оставался в модели/графике | `on_create` сбрасывает модель и графику |
| Пропали легенды в Qt | не были добавлены элементы легенды | `ColorBarItem` + `LegendItem` |
| «Could not find Qt platform plugin windows» | не находился каталог плагинов на Windows | `_ensure_qt_plugin_path()` + инструкция переустановки |
| Кривой масштаб вкладки 2 при старте | размер виджета не задан на неактивной вкладке | `showEvent`/`resizeEvent` → `_apply_range(force=True)` |
| Маршруты кучковались в центре | смешение «разброса» с числом точек | разброс = АМПЛИТУДА (линза по режиму), число точек — отдельный диапазон |
| Датчики «кучей» за/у цели B | кандидаты за B + без рубежа у цели | `filter_not_past_target` + якорный датчик + развязка 1.35·R |
| Синус игнорировал R_min | амплитуда не связана с физикой | суммарная кривизна ≤ 1/R_min (формула 13) |
| Редкие точки попадали в зону старта при выносе | проверяли точку выхода луча, а не «тень» эллипса | опорная проекция эллипса на ось S₀→B (формула 14) |
| Разброс «сложный» снова жался к центру, видны прямые | амплитуда от хорды D + узкий зигзаг при многих точках не влезали в L_max и схлопывались фитом | база амплитуды → b (полу-малая ось); сложный = 1–2 крупных лепестка к краю эллипса |
| Датчики появлялись «за спиной» зоны старта | исключали только внутренность эллипса зоны | + отсечение позади задней кромки (180°): (P−A)·û ≥ −depth/2 |
| Карта не перерисовывалась при смене расстояния | B бралась от клика и не зависела от поля \|AB\| | B = A + \|AB\|·направление в reset; on_apply форсит перемасштаб |
| Слой «10 маршрутов» (по точкам) уходил в одну сторону | случайная выборка немногих + крупные односторонние лепестки | стратификация по боковому отклонению — обе стороны и оба края |
| Маршруты выходили за показанный эллипс | рисовали эллипс фокусов A,B, а маршрут из S0≠A лежит в эллипсе S0,B | показываем огибающую (объединение эллипсов S0,B по зоне) |
| Карта оставалась размытой (п. 34 не помог) | `_pick_zoom` брал зум под ПОТОЛОК тайлов → мозаика < виджета → растяжение | зум по ПОЛУ (мозаика ≥ экрана) + `autoDownsample=True` (п. 36) |
| Слои вкладки 3 сместились бы относительно подложки | `_km_per_deg_lon` — константа от `KURSK_LAT`, игнорировала `lat0` | масштаб долготы от фактической широты якоря (п. 37) |
| Датчик мог встать на реку (вкладка 3) | вода — аттрактор веса, но не исключалась для датчиков | маска воды + буфер, кандидаты на воде исключаются до жадного (п. 38) |
| Бесконечная рекурсия схемы после рефакторинга | хвост старого `_draw_scheme` (`_set_scheme_visible(True)`) прицепился к новому хуку `_set_scheme_extra_visible` | убрана лишняя строка; хук лишь переключает рамку области (п. 41) |
| Зависание на реальных OSM (45 МБ) | `_detect_bridges` O(N²) по парам ломаных + Python-растеризация + point-in-polygon по всей сетке; всё в UI-потоке | мост по сетке (river∧road), векторная растеризация, bbox-полигоны, фоновый поток (п. 45) |
| Интерфейс тормозит ПОСЛЕ показа карты | тысячи OSM-линий (сотни тысяч точек) со сглаживанием + тысячи маркеров-мостов перерисовывались каждый кадр | прореживание линий, `antialias=False`, мосты магистраль/ж-д×река ≤400, убран autoDownsample, чекбокс полностью очищает слой (п. 46) |
| Карта «замельчала», подписи не того размера | `_pick_zoom` брал первый зум ≥ экрана (мозаика до 2× → ужимали, подписи мельче) + домножение на DPR | зум под масштаб ~1:1 (`min|log2(nx)−log2(need)|`), логическая ширина (п. 47) |
| Город — самый «горячий» на карте (fig 7) | много дорог/перекрёстков в застройке + funnel-бонус | исключение города по плотности застройки в окне 3×3 км: вес→штраф, обход маршрутом (п. 48). 🚫 **Способ сменился:** плотности в окне БОЛЬШЕ НЕТ — тип пункта берётся из метки `place` + пятна застройки + контуров, с собственной градацией по данным участка (п. 143, 149); вес города не штрафуется, а ОБНУЛЯЕТСЯ (`THREAT_CITY_ZERO_WEIGHT`) |
| Маршруты шли прямыми линиями (не по карте) | нормировка стоимости на max(W): один тяжёлый узел уравнивал остальных | нормировка по p90 — маршрут идёт по коридорам (п. 52) |
| Реки в городе без тепла/непроходимы | исключение города зануляло и речные ячейки | `urban &= ~river` — река остаётся коридором сквозь город; исключаем только крупные города (п. 53) |
| Маршрутов ровно 14, «нельзя обойти» | показывали фикс. число линий, а не область достижимости | огибающая `g_вход+g_цель≤L_max` — ВСЕ места пролёта, длинные обходы (п. 56) |
| Нет окна ввода ТТХ на вкладке 3 | параметры маршрута нельзя было менять из UI | немодальное окно «Входные данные…», Enter применяет без блокировки (п. 57) |
| Перекрёстки не видны отдельно | funnel-бонус усиливал узлы, но их нельзя было выделить | `crossing_cells_km` (≥2 разных слоёв) + чекбокс-подсветка, скрыт по умолчанию (п. 58) |

---

## 5. Ключевые решения (с учётом обсуждения)

* **Разброс ≠ число точек.** «Разброс» — это амплитуда отклонения от прямой
  (малый/средний/сложный + смешанный). Число точек задаёт пользователь
  диапазоном [n_min, n_max] (рандом внутри, случайные расстояния между точками).
* **Модель «по точкам» как основная.** Реальные карты маршрутов коммерческих БПЛА
  самолётного типа — ломаные с плавными доворотами, а не гладкие синусоиды. Синус
  оставлен как опция в выпадающем списке.
* **Крен и скорость — это ТТХ БПЛА** → радиус разворота R_min = V²/(g·tan φ).
* **Зона старта — область неопределённости, не препятствие.** Поэтому положение
  путевых точек относительно зоны — мягкое условие (переключатель), а вот
  размещение ДАТЧИКОВ в зоне запрещено жёстко (свои датчики нельзя ставить в
  районе пуска противника).
* **Якорь у цели + развязка** убирают вырожденную «кучу» датчиков у B.

---

## 6. Текущее состояние (сделано)

* Геометрия, эллипс, коридор, зона старта — готово.
* Модели движения: дуга, петля, манёвр «по точкам», манёвр «по синусу» (с R_min),
  дуга из старта, ломаная — готово.
* Обнаружение, метрики, жадная субмодулярная оптимизация (SAA, якорь, вторичный
  критерий) — готово.
* Два интерфейса (matplotlib + Qt с **тремя** вкладками), анимация, слои, легенды,
  пакетный расчёт, сравнение режимов — готово.
* Ограничения: точки за/в зоне (переключатель), датчики вне зоны (жёстко), без
  позиций за B, R_min для точек и синуса — готово.
* Документация (методичка/журнал/структура/README/docx) — синхронизирована.

**Вкладка 3 «Карта угроз» — основная часть работы, сделана целиком:**

* весовая карта по реальным данным OSM: слои, funnel-бонус, мосты, вода;
* **населённые пункты** — город запрещён, деревня обходится по краю с полом штрафа,
  собственная градация пунктов по данным участка вместо доверия тегу (п. 147–167);
* **рельеф** из `dem.tif`: превышение в двух масштабах, множитель к весу, отсечка
  «очень высокой горы», два режима показа, кнопка «Добавить рельеф» (п. 147–153);
* **маршруты**: стохастическое блуждание, тяга к цели ПО СТОИМОСТИ (два поля —
  километры для бюджета, стоимость для спуска), несколько полей жадности ради
  разброса, движение по физике аппарата с пределом поворота (п. 151–155, 160);
* **запретные зоны**, рисуемые мышью; шаг сетки датчиков от радиуса (п. 162–163);
* **интерфейс**: группы слоёв, окно векторных слоёв, шкалы, прозрачность (п. 159);
* **демонстрационная сборка `.exe`** для показа на чужом ПК (п. 164, ветка `demo-build`).

Проверено офскрин-тестами: синус соблюдает R_min; ноль датчиков и (в режиме
«за зоной») ноль путевых точек внутри зоны старта; маршруты приходят в B и
укладываются в L_max; оба закона и оба режима зоны прогоняются без ошибок.
Контрольный прогон вкладки 3 (умолчания, ~17 с): 250/250 возможных маршрутов,
150/150 итераций, 15/15 датчиков, 0 на воде, 0 превышений запаса хода, карта
11 101 коридор / 42 компоненты / 9 583 крупнейшая — эталон в
[ОГРАНИЧЕНИЯ.md §6](../ОГРАНИЧЕНИЯ.md).

---

## 7. Что осталось / возможные улучшения

> **Действующий список — [4_ПЛАН_ДОРАБОТКИ.md](../планы/4_ПЛАН_ДОРАБОТКИ.md).**
> Там всё незакрытое с обоснованным порядком работ. Ниже — только общая рамка.

* **Рабочая ветка — `relief-fixes`.** Демо-режим для `.exe` живёт отдельно в
  `demo-build`; архив демо-варианта с разбором отличий лежит рядом с папкой проекта.
* `docs/Теория_и_математика.docx` стоит пересобрать под текущие формулы и ограничения
  (скрипт `docs/make_theory_docx.py`).
* **Вкладка 3 — что осталось** (подробно в плане 4): сверка векторных слоёв с весовой
  картой; движение при большом запасе хода остаётся ломаным; единичные маршруты уходят
  от цели; калибровка порогов рельефа глазом; цвет запретной зоны конфликтует с красным
  у возвышенностей.
* Возможные улучшения: 3D (высота полёта — модель плоская), разнотипные датчики
  (разные R), стоимость позиций, экспорт результата (CSV/гео), доверительные
  интервалы SAA; при желании — унифицировать тайловую подложку вкладки 2 на общий
  `basemap_mixin` (сейчас у вкладки 2 своя копия, чтобы не трогать рабочий код без
  живого GUI-теста).

> 🚫 **Снято с повестки:** «Этап 3 — маршруты A\*/RRT\* по costmap от точки появления
> („Заречье“) к цели („Аэропорт“)». Маршруты СДЕЛАНЫ, но иначе: не поиском одного
> оптимального пути, а стохастической выборкой правдоподобных (п. 60, 151–155) — маршрут
> БПЛА заранее неизвестен, поэтому нужна выборка, а не один путь. Названия «Заречье» и
> «Аэропорт» — из демонстрационной схемы, на реальных данных вход и цель задаются
> координатами и кликом по карте.
