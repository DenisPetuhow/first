# Журнал проекта — часть 6 (пункты 244–…)

> **Правился:** 12.09.2026 — отметка заведена; ⚠️ содержание не сверялось с 10.09.2026.

> **Живая часть журнала.** Открыта 10.09.2026: план 8 закрыт почти целиком, работа
> переходит к организации самого проекта (план 9).
>
> * **точка входа — [указатель](README.md)**: там таблица «симптом → пункты» и номера
>   строк. Искать пункт перебором частей не нужно;
> * **все закрытые части — в архиве:** [часть 1](../архив/журнал/ЖУРНАЛ_1.md) ·
>   [часть 2](../архив/журнал/ЖУРНАЛ_2.md) · [часть 3](../архив/журнал/ЖУРНАЛ_3.md) ·
>   [часть 4](../архив/журнал/ЖУРНАЛ_4.md) ·
>   [часть 5](../архив/журнал/ЖУРНАЛ_5.md) (пункты 193–243, переехала 12.09.2026);
> * ⚠️ **сводные разделы 4–7** в конце части 5 **ЗАМОРОЖЕНЫ 12.09.2026** (задача 9.2):
>   раздел 4 — таблица прошлых дефектов, историчен и верен; разделы 5–7 описывают
>   состояние на начало сентября и устарели. Действующее: правила —
>   [ОГРАНИЧЕНИЯ](../правила/ОГРАНИЧЕНИЯ.md), незакрытое —
>   [реестр плана 10 §0.7](../планы/10_ПЛАН_ПРОЕКЦИЯ_И_ОСТАТКИ.md).

## Хронология

244. **План 8 закрыт на 8 задач из 9; журнал разделён, правила по тайлам уточнены. ✅**

     **Проблема.** Часть 5 журнала доросла до **1 569 строк** при пороге деления 1 500,
     а сводка плана 8 отвечала состоянию на 05.09.2026 — по ней нельзя было сказать,
     что осталось. Вдобавок правило [ОГРАНИЧЕНИЯ 5.1](../правила/ОГРАНИЧЕНИЯ.md) называло
     активным участок `plesetsk_wide` (95 × 55 км), тогда как в коде уже полгода стоит
     `THREAT_AREA = "arh"` — область 291 × 250 км: документ врал о том, на какой
     местности идёт работа.
     **Причина.** Журнал и сводки правятся дописыванием, а пороги и статусы — только по
     отдельному поводу; повода не возникало с 05.09.2026, за который сделаны задачи
     8.6, 8.9 и вся сборка.
     **Решение.** Заведена часть 6 журнала (эта). В план 8 добавлен §3.2 — построчный
     итог: что закрыто, что осталось. Правило 5.1 переписано на `arh` с разделением
     «область показа ⇄ район расчёта»; 5.1д дополнено тем, что папка тайлов привязана к
     ОБЛАСТИ, а не к району, и что сборка `.exe` обязана брать оба места поиска.
     **Результат.** По плану 8 закрыто **8 задач из 9** (8.10 в объём не входила) плюс
     три довеска сверх плана; осталось четыре некрупных пункта: масштаб 1 : M (8.4,
     не начата), опорные точки мышью (8.5-а), нумерация больших датчиков (8.8-а) и
     новая 8.1-а — выбор папки тайлов в программе. Проверки после правок: имена 0,
     числа 0, оглавления и 220+ ссылок без расхождений.
     **Подробно:** [план 8, §3.2](../планы/8_ПЛАН_КАРТА_И_ИНТЕРФЕЙС.md) — таблица
     остатка · [§8.1.8](../планы/8_ПЛАН_КАРТА_И_ИНТЕРФЕЙС.md) — новая задача о выборе
     папки тайлов, с ловушками · [ОГРАНИЧЕНИЯ 5.1, 5.1д, 5.1к](../правила/ОГРАНИЧЕНИЯ.md).

245. **Тайлы качаются на ОБЛАСТЬ и годятся любому району внутри неё — правило записано. ✅**

     **Проблема.** После правки сбора тайлов (п. 243) возник вопрос экономии: сборка
     несёт **32 933 тайла (109.9 МБ)** по рамке области, тогда как рабочему району
     хватило бы **5 796**. Соблазн урезать до района экономил бы около 90 МБ.
     **Причина.** «Область» и «район» легко спутать: область — это показ и данные
     (`THREAT_BBOX_LONLAT`), район расчёта задаётся мышью внутри неё уже в запущенной
     программе (`THREAT_WORK_LONLAT`, правило 5.1к), и заранее неизвестно, какой.
     **Решение.** Решение заказчика 10.09.2026: раскладку кэша НЕ трогать, а правило
     записать явно — папка тайлов принадлежит ОБЛАСТИ, качается один раз и обслуживает
     любой район внутри; сборка берёт рамку области. Отдельно записана задача 8.1-а:
     вынести выбор папки тайлов в окно «Исходные данные», чтобы менять место хранения
     без правки кода.
     **Результат.** Урезание до района отклонено: экономия 90 МБ ценой карты, которая
     обрывается на границе при сдвиге района. Правило закреплено в трёх местах:
     ОГРАНИЧЕНИЯ 5.1д, СБОРКА_EXE §0а, CLAUDE.md §3а.
     **Подробно:** [ОГРАНИЧЕНИЯ 5.1д, 5.1к](../правила/ОГРАНИЧЕНИЯ.md) — область против района ·
     [СБОРКА_EXE §0а](../сборка/СБОРКА_EXE.md) — почему по рамке области, с замером.

246. **Составлен план 9; сверка противоречий врала тремя темами из пяти. ✅**

     **Проблема.** Прибираться в проекте собирались «по ощущению», без замеров, а две
     проверки при этом молча врали: `check_claims` показывал **5 тем** с расхождениями,
     `docmap --check` возвращал **код 1** при отчёте, который прочли как «расхождений
     нет». Заодно выяснилось, что памятью считали тайлы (401 МБ), не глядя на остальное.
     **Причина.** Скрипт читал ячейки `нет` в таблице сравнения готовых сборок
     (СБОРКА_EXE, строка 104) как утверждение «механизма нет сейчас», хотя они про
     ПРЕЖНИЕ сборки. Отчёт `docmap` проверяли глазами по первым трём разделам, а
     четвёртый — длину файлов — пропускали: код возврата никто не смотрел.
     **Решение.** В `check_claims.py` добавлен фильтр `CMP_CELLS` (стр. 117): абзац
     пропускается, если это таблица, где не меньше трёх ячеек целиком «да» либо «нет».
     Составлен [план 9](../планы/9_ПЛАН_ОРГАНИЗАЦИЯ_ПРОЕКТА.md) — семь задач по
     организации проекта, все числа сняты скриптами 11.09.2026.
     **Результат.** `check_claims` **5 тем → 3**, самопроверка скрипта пройдена;
     оставшиеся три разобраны глазами и законны. Замеры: документации **73 файла /
     29 343 строки** (1.70 млн символов — вчетверо больше, чем обещает `CLAUDE.md`),
     кода **61 файл / 26 511 строк**, локальных данных вне git **4.0 ГБ** при
     репозитории **3.38 МиБ**. Главное: `geo_cache/карты_картинки` — **2 780 МБ**, из
     них **1 459 МБ перестраиваемый кэш** `.dat`, то есть вся подложка (401 МБ) в семь
     раз меньше одной папки с картинками. Отдельно вскрыто: **семь веток, включая
     рабочую `ui-sensors` со всем планом 8, существуют только на этом диске**.
     **Подробно:** [план 9 §0](../планы/9_ПЛАН_ОРГАНИЗАЦИЯ_ПРОЕКТА.md) — ответы на все
     замечания с замерами · [§0.6](../планы/9_ПЛАН_ОРГАНИЗАЦИЯ_ПРОЕКТА.md) — куда на
     самом деле уходит память · [§9.4](../планы/9_ПЛАН_ОРГАНИЗАЦИЯ_ПРОЕКТА.md) —
     почему картинки нельзя просто переложить (абсолютный путь в `ui_state.json`).

247. **Что максимизирует вкладка 3 и сколько всего маршрутов — три числа оказались неверными. ✅**

     **Проблема.** Три утверждения в коде и документации не выдержали проверки:
     методичка §5.3 подавала расстановку по КЛЕТКАМ веса как работу вкладки 3; докстринг
     `count_possible_routes` обещал **~1e107** возможных маршрутов; докстринг
     `render_routes` называл линии-примеры **розовыми**.
     **Причина.** Все три писались при прежнем участке и прежнем коде и с тех пор не
     пересматривались: §5.3 начиналась с ЗАПАСНОГО пути расстановки, число снято на
     Плесецке, а цвет линий сменился на голубой позже текста.
     **Решение.** Разобрано по коду и замерено прогоном модели на ДВУХ участках; все три
     места переписаны с датой замера и пометкой, что именно считается.
     **Результат.** Вкладка 3 максимизирует **сопровождение БПЛА**: основной путь —
     `_place_by_routes` (единица покрытия МАРШРУТ, вторичный ключ — доля длины пути под
     наблюдением); по клеткам веса считает только запасной `_place_by_weight`, и лишь
     когда маршрутов меньше пяти. Число возможных маршрутов замерено 11.09.2026:
     **`arh` — 7.24e+162**, **`plesetsk_wide` — 8.24e+104**. Прежнее «1e107» относилось к
     Плесецку и для действующего участка занижено **на 58 порядков**; ветвление тоже
     замерено — **3.69** соседа в среднем (медиана 4, максимум 8), а не «~3».
     **Отвергнут вариант «250 → 650 возможных маршрутов»:** модель не ломается (разошлось
     только само число, структурные показатели точны, стохастические в допусках), но
     прогон дорожает **27 → 51.9 с** — вернули 250.
     **Подробно:** [МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ §5.3](../модель/МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md) —
     два пути расстановки и три ключа выбора · [ОГРАНИЧЕНИЯ §6.0а](../правила/ОГРАНИЧЕНИЯ.md) —
     отвергнутый вариант 650 с числами прогона · `threat_routes.count_possible_routes` —
     как считается и что именно (монотонные по стоимости пути).

248. **Навигация вынесена из CLAUDE.md; проверка структуры снова что-то значит. ✅**

     **Проблема.** `CLAUDE.md` читается в КАЖДОЙ сессии и вырос до **429 строк / 32 956
     символов** при правиле 400. Из них **52 % (164 строки, 17 008 символов)** — раздел
     навигации, нужный не всегда, а под конкретный вопрос. Вдобавок `docmap --check`
     возвращал **1** из-за трёх файлов сверх порога 1 500, которые решено не делить, —
     то есть красный код горел бы постоянно и перестал бы отличать поломку от нормы.
     **Причина.** Указатель и правила лежали в одном файле, хотя читаются по-разному:
     правила — всегда, навигация — по вопросу. А порог длины был записан как команда
     резать, тогда как решение заказчика 11.09.2026 — не резать связные документы.
     **Решение.** Две таблицы («по вопросу» и «по механизму в коде») плюс стоимость
     документов вынесены в новый [теория/НАВИГАЦИЯ.md](../структура/НАВИГАЦИЯ.md); в `CLAUDE.md`
     остались ссылки. Стоимость КОДА не дублируется вовсе — она в карте кода, которая
     пересобирается скриптом и не устаревает. В `docmap.py` заведён список `LONG_OK`:
     файл сверх порога с записанной ПРИЧИНОЙ не считается замечанием.
     **Результат.** `CLAUDE.md` **429 → 348 строк**, **32 956 → 21 310 символов** (−35 %
     в каждой сессии). `docmap --check` — **EXIT = 0 впервые за сессию**, ссылок битых 0
     из 77 файлов, имена 0, числа 0. Заодно закрыты 9.11 (правило обновления документации
     расширено: методичка по коду, затронутая теория, «нет документа — спросить о
     создании»), 9.15 (сборка начинается с документа — правило записано там, откуда
     начинают работу) и 9.16 (заведён [теория/РАБОТА.md](../РАБОТА.md)).
     **Подробно:** [план 9, задачи 9.9, 9.11, 9.15, 9.16](../планы/9_ПЛАН_ОРГАНИЗАЦИЯ_ПРОЕКТА.md) ·
     [НАВИГАЦИЯ](../структура/НАВИГАЦИЯ.md) — обе таблицы · `docmap.py` :: `LONG_OK` — почему порог
     стал сигналом, а не командой.

249. **Восьмая проверка: «файл правили, а записать забыли». ✅**

     **Проблема.** Семь проверок сверяют СОДЕРЖАНИЕ — имена, числа, ссылки, противоречия —
     и все бессильны против документа, где всё верно, кроме одного утверждения. Живой
     случай накануне: §5.3 методички маршрутов подавала ЗАПАСНОЙ путь расстановки как
     основной. Имена живые, числа верные, ссылки целы, другие документы не спорят — а
     раздел врал о главном. Узнать это можно было только открыв код.
     **Причина.** Дата правки документа жила **только в git**, и по самому документу
     нельзя было понять, насколько он свеж. Ловить надо не содержание, а разрыв во
     времени.
     **Решение.** Заведена **отметка в шапке**: `> **Правился:** ДД.ММ.ГГГГ — что
     изменено`, проставлена в **68 документов** датой из git (честно: содержание не
     сверялось, это точка отсчёта). Написан `tools/переносимое/check_dates.py` —
     восьмой шаг сверки: сравнивает отметку с датой коммита, ловит случай `шапка < git`.
     Он же генерирует `ЖУРНАЛ_ПАПКИ.md` в живых папках.
     ⚠️ **Мини-журналы решено ГЕНЕРИРОВАТЬ, а не вести руками** — развилка вскрылась при
     реализации: черновик засеивал их из `git log`, то есть рукописный файл оказывался
     копией уже имеющегося, а такая копия устаревает первой (в тот же день на этом
     обожглись трижды: указатель журнала, план 7, §5.3 методички).
     **Результат.** Отметки в **68 документах**, **9 мини-журналов** сгенерировано,
     `check_dates.py` — **0 расхождений, EXIT = 0**. По ходу он же нашёл две СВОИ ошибки:
     битую ссылку в корневом журнале папки (жёсткий `../журнал/` вместо расчёта глубины)
     и незнакомое имя `dateutil`. Итог сверки: docmap **EXIT = 0**, битых ссылок **0 из
     88 файлов**, имена 0, числа 0. Заодно: удалён дубль методички из корня (9.5 — был от
     08.08, актуальный от 10.09 в 1.7 раза полнее), пересобрана карта кода (9.14),
     заведена папка [теория/код/](../код/README.md) с первым разбором (начало 9.12).
     **Подробно:** [теория/код/СКРИПТ_ОТМЕТКИ_О_ПРАВКЕ.md](../код/СКРИПТ_ОТМЕТКИ_О_ПРАВКЕ.md) —
     сквозной разбор кода, как запускать и как читать вывод · [план 9, задача 9.10](../планы/9_ПЛАН_ОРГАНИЗАЦИЯ_ПРОЕКТА.md) —
     почему генерируем, а не пишем руками.

250. **Ссылки с §-номером и переезд журнала: проверка нашла ошибки в первый же прогон. ✅**

     **Проблема.** Ссылка, у которой в тексте стоит «§14.1», а в адресе — файл, ведёт на
     СУЩЕСТВУЮЩИЙ файл, поэтому битой не считается — а раздела §14.1 в нём может уже не
     быть. Такие ссылки уводят молча, и «битых ссылок 0» о них ничего не говорит. Замер:
     только на одну методичку ведут **137 ссылок**, почти все с номером раздела.
     **Причина.** `docmap` проверял лишь существование ФАЙЛА. Номер раздела в проекте
     живёт в двух видах — заголовком (`## 14.`) и строкой таблицы (`| 5.1 |` в
     ОГРАНИЧЕНИЯХ) — и ни один не сверялся.
     **Решение.** В `docmap.py` добавлена проверка `check_sections`: ищет `§N` в ТЕКСТЕ
     ссылки и убеждается, что такой раздел в файле есть — в обоих видах сразу, иначе
     половина ссылок на ОГРАНИЧЕНИЯ оказалась бы «битой» на ровном месте.
     **Результат.** Первый же прогон нашёл **1 настоящую** битую ссылку — в этом самом
     журнале: `§9.4.3` после того, как задача 9.4 была снята заказчиком. Ложных
     срабатываний **0 из 89 файлов**.
     Заодно сделано по плану 9: **часть 5 журнала переехала в архив** (144 относительные
     ссылки внутри переписаны с `../` на `../../`, 7 ссылок снаружи поправлены); сводные
     разделы 4–7 **заморожены с разбором**: раздел 4 — таблица прошлых дефектов, историчен
     и верен; 5–7 описывают состояние на начало сентября и помечены устаревшими, но НЕ
     удалены — журнал задним числом не правится. Написана
     [методичка о форматах `.osm.pbf` ⇄ `.npz`](../карты/МЕТОДИЧКА_ФОРМАТЫ_PBF_И_NPZ.md)
     (замеры: **17.1 МБ / 4.3 с** против **7.2 МБ / 0.03 с** — в 140 раз быстрее).
     `МЕТОДИЧКА_ДАТЧИКИ_ДВА_ТИПА` переименована в `МЕТОДИЧКА_ДАТЧИКИ_ТИПЫ`: **типов
     четыре** с 05.09.2026, имя врало; поправлено **57 ссылок в 18 документах**.
     ⚠️ **Курица и яйцо:** коммит, которым отметки «Правился» заводились, сам стал
     последней правкой каждого файла — и проверка честно показала **55 «отставших» шапок**.
     Переписаны 66 отметок: дата = последняя правка, прежняя переехала в пояснение
     «содержание не сверялось с …». Врать «проверено 12.09» было нельзя.
     **Проверки:** docmap **EXIT = 0** (89 файлов, битых ссылок 0, §-разделов 0),
     check_dates **EXIT = 0** (59 документов), codemap **EXIT = 0**, имена 0, числа 0.
     **Подробно:** [план 9, задачи 9.1, 9.2, 9.7, 9.13](../планы/9_ПЛАН_ОРГАНИЗАЦИЯ_ПРОЕКТА.md) ·
     `docmap.py` :: `check_sections` — как отличается заголовок от строки таблицы.

251. **Корень теории разобран: было 12 файлов, стало 4; 443 ссылки пересчитаны. ✅**

     **Проблема.** В корне `теория/` лежало **двенадцать** файлов вперемешку — правила,
     методички, указатели, установка библиотек, разбор скрипта. Найти нужное можно было
     только перечитав список целиком.
     **Причина.** Папки заводились по темам, а всё «общее» оседало в корне: пока документов
     было пять, это работало, на двенадцати перестало.
     **Решение.** Раскладка по смыслу: **три новые папки** — `структура/` (НАВИГАЦИЯ,
     СТРУКТУРА_ПРОЕКТА, СТРУКТУРА_ТЕОРИИ — все отвечают «где искать»), `правила/`
     (ОГРАНИЧЕНИЯ — правила и эталон), `модель/` (общее для ВСЕХ вкладок); плюс
     УСТАНОВКА_БИБЛИОТЕК → `сборка/`, РАЗБОР_DOCMAP → `код/`. В корне осталось четыре
     места: `README`, `МЕТОДИЧКА`, `РАБОТА`, `журнал/` — то, что открывают чаще всего.
     ⚠️ **Ссылки пересчитаны в ТРИ прохода, и порядок принципиален:** сперва все ссылки
     переведены в абсолютные пути (пока файлы на местах), потом перенос, потом сборка
     заново от новых мест. Простая замена строк здесь ломается: `../ОГРАНИЧЕНИЯ.md` из
     подпапки и `ОГРАНИЧЕНИЯ.md` из корня — один файл, но разный текст, а меняться может
     и цель, и точка отсчёта сразу.
     **Результат.** Перенесено **8 документов**, пересчитано **443 ссылки** в 48 файлах,
     отдельным проходом поправлены ссылки в **7 файлах КОДА** (докстринги — их `docmap` не
     проверяет вовсе). Заведена недостающая
     [КАК_КОД_РЕШАЕТ_ЗАДАЧУ](../код/КАК_КОД_РЕШАЕТ_ЗАДАЧУ.md): замысел целиком за один
     проход — что загружается, какие алгоритмы, в каком порядке и почему. Проверки:
     `docmap --check` **EXIT = 0** (96 файлов, битых ссылок **0**), `check_dates` 0,
     имена 0, числа 0, `codemap --check` 0.
     ⚠️ **Три ловушки, каждая поймана проверкой:** новой папке нужен `README.md`, иначе
     она выпадает из навигации · пример ссылки, записанный рабочей разметкой, попал в
     «битые» · запись в `LONG_OK` живёт по ПУТИ файла и рассыпалась после переезда
     журнала.
     **Подробно:** [план 9, задача 9.17](../планы/9_ПЛАН_ОРГАНИЗАЦИЯ_ПРОЕКТА.md) — приём
     пересчёта ссылок в три прохода · [теория/README](../README.md) — новая раскладка и
     почему в корне оставлены именно эти четыре.
