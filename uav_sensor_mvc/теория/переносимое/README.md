# Переносимые правила — комплект для любого проекта

> **Правился:** 19.09.2026 — правки серии 16–19.09 (журнал п. 276–278); подробности —
> в самой записи журнала.
> 16.09.2026 — пересобрано оглавление (`docmap.py`), содержание не менялось.
> 14.09.2026 (коммит `30eec6d`) — в коммит вошли правки 13.09, описанные ниже; нового содержания нет.
> 19.09.2026 — добавлено правило 10_ПРОВЕРКИ_И_ПРОГОНЫ.
> 13.09.2026 — добавлено правило 9_ПОЯСНЕНИЯ_С_ПРИМЕРОМ; ⚠️ остальное не
> сверялось с 03.09.2026.

Здесь лежит то, что **не зависит от предметной области**: как вести документацию, чтобы
ИИ-помощник её находил, не тратил на неё весь контекст и не выдумывал по устаревшему
тексту, а также как распределить решения между ним и человеком. Ни слова про БПЛА,
датчики и карты — только про работу с документами, кодом и историей изменений.

**Правила — здесь, скрипты к ним — в [tools/переносимое/](../../tools/переносимое/README.md).**
Деление простое: текст живёт в документации, код — в `tools`.

> **Как перенести в другой проект.** Скопировать эту папку целиком и папку
> [`tools/переносимое/`](../../tools/переносимое/README.md) со скриптами. Дальше пройти чек-лист из
> [4_ПЛАН_ВНЕДРЕНИЯ.md](4_ПЛАН_ВНЕДРЕНИЯ.md) — он рассчитан именно на новый проект.
> Скрипты предметной области не знают и работают сразу.

---

## Что где лежит

| Документ | О чём | Скрипт |
|---|---|---|
| [1_КОНТЕКСТ_И_НАВИГАЦИЯ.md](1_КОНТЕКСТ_И_НАВИГАЦИЯ.md) | почему контекст кончается, три уровня документации, раскладка по папкам, журнал как точка восстановления, порог деления файлов | — |
| [2_ОГЛАВЛЕНИЯ_И_ПОИСК.md](2_ОГЛАВЛЕНИЯ_И_ПОИСК.md) | указатели вместо файлов, оглавления с номерами строк, **карта кода и срок годности документации**, как писать, чтобы находилось | [`docmap.py`](../../tools/переносимое/docmap.py), [`codemap.py`](../../tools/переносимое/codemap.py) |
| [3_СВЕРКА_С_КОДОМ.md](3_СВЕРКА_С_КОДОМ.md) | семь шагов проверки на достоверность, ложные срабатывания и как их гасить | [`check_names.py`](../../tools/переносимое/check_names.py), [`check_numbers.py`](../../tools/переносимое/check_numbers.py), [`check_dead_code.py`](../../tools/переносимое/check_dead_code.py) |
| [4_ПЛАН_ВНЕДРЕНИЯ.md](4_ПЛАН_ВНЕДРЕНИЯ.md) | пошаговый план: что завести в новом проекте и в каком порядке | все три |
| [5_РАЗРЕШЕНИЯ_КОМАНД.md](5_РАЗРЕШЕНИЯ_КОМАНД.md) | что помощник делает молча, что спрашивает, что только по команде; как оформить список разрешений и почему он не срабатывает | — |
| [6_КОММИТЫ_GIT.md](6_КОММИТЫ_GIT.md) | коммиты только по команде, проверка перед коммитом, формат сообщения, что не должно попасть в историю | — |
| [7_СОСТАВЛЕНИЕ_ПЛАНА.md](7_СОСТАВЛЕНИЕ_ПЛАНА.md) | порядок «разведка → вопросы → текст», обязательные разделы плана, открытые вопросы с умолчаниями, чек-лист перед сдачей | — |
| [8_КОММЕНТАРИИ_В_КОДЕ.md](8_КОММЕНТАРИИ_В_КОДЕ.md) | как пояснять НОВЫЙ код: до 3 строк на функцию (что делает · принимает · отдаёт, ⚠️ **с единицами**), переменная — в своей строке, перед формулой — «почему так» | — |
| [10_ПРОВЕРКИ_И_ПРОГОНЫ.md](10_ПРОВЕРКИ_И_ПРОГОНЫ.md) | ⭐ **единое правило проверок** (заказчик 19.09.2026): пять видов и что каждый ловит, порядок «числа раньше картинки», шесть правил прогонов (зерно, одна выборка, числа из замера, условия рядом с числом, код возврата, выключаемость в ноль), когда заводить новую проверку | — |
| [9_ПОЯСНЕНИЯ_С_ПРИМЕРОМ.md](9_ПОЯСНЕНИЯ_С_ПРИМЕРОМ.md) | **пояснение ПЛЮС пример**: сначала «что не так и почему» целиком, потом одна-две строки «на что влияет» — «круг радиуса 30 км накрывает 31.1 км местности» | — |

Пояснение к скриптам — [tools/переносимое/README.md](../../tools/переносимое/README.md).

---

## Пять правил в одну строку

1. **Журнал коротко, методичка подробно.** Журнал отвечает «когда и почему меняли»,
   методичка — «как это работает сейчас». Разбор и таблицы замеров идут в методичку, в
   журнале остаётся суть и ссылка.
2. **Ссылка ведёт в раздел, а не в файл.** Разница между «прочитать 40 строк» и
   «прочитать 1 300».
3. **«Всё сходится» — утверждение со сроком годности.** Оно стоит ровно столько, сколько
   последняя проверка.
4. **Разведка — молча, затратное — по решению, необратимое — по команде.** Вопрос
   нужен тогда, когда ответ меняет действия; «начинаю?» после изложенного плана их не
   меняет.
5. **История пишется по команде.** Коммит и push — отдельные решения человека, чтение
   истории — нет.
6. **План пишется после разведки, а не вместо неё.** Раздел, написанный до ответа на
   вопрос «а как оно сейчас», выбрасывается целиком — так уже было
   ([7_СОСТАВЛЕНИЕ_ПЛАНА.md §1](7_СОСТАВЛЕНИЕ_ПЛАНА.md)).
7. **Разбор кода живёт в ОДНОМ месте, остальные ссылаются.** Копия расходится с кодом
   вдвое быстрее оригинала: правят одну, вторая молча врёт
   ([8_КОММЕНТАРИИ_В_КОДЕ.md](8_КОММЕНТАРИИ_В_КОДЕ.md), папка `теория/код/`).

---

## Откуда это взялось

Всё выведено из практики проекта `uav_sensor_mvc`: 180 пунктов журнала, 34 документа,
около 160 тысяч токенов текста. **Каждая рекомендация появилась после конкретной
поломки**, и поломки названы прямо — так их проще узнать у себя:

* методичка полгода описывала цвета слоёв, которых в коде уже не было;
* четыре раздела числили «заделом на будущее» то, что работало давно;
* генератор оглавлений врал на 20–44 строки во всех файлах сразу, а его собственная
  проверка это подтверждала как норму;
* проверка имён давала 146 находок, из которых настоящей была одна.
