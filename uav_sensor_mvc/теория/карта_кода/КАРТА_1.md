<!-- КАРТА КОДА · сгенерировано tools/переносимое/codemap.py — руками не править -->

# Карта кода — часть 1 из 1

> Указатель со сводкой — [README.md](README.md). Здесь: `model/threat_grid.py`, `view_qt/threat_view.py`, `model/threat_routes.py`, `tools/переносимое/make_figures.py`, `controller_qt/threat_controller.py`, `config.py`, `model/trajectories.py`, `view_qt/view_qt.py`, `view_qt/geomap.py`, `tools/переносимое/codemap.py`, `view/view.py`, `model/area_start.py`, `main_qt.py`, `view_qt/area_view.py`.

> Найти строку → открыть файл `Read` с `offset`. Файл целиком не читать.


## model/threat_grid.py

**Отдаёт наружу:** `ThreatModel`, `_label8`, `area_cache_dir`, `area_file`, `bbox_anchor_lonlat`, `bbox_lonlat_to_km`, `dem_files`, `geo_cache_root`, `layers_from_osm`, `load_layers`.

| стр. | что | зачем |
|---:|---|---|
| 93 | `bbox_anchor_lonlat()` | Якорь локального км-фрейма вкладки 3 = ЮГО-ЗАПАДНЫЙ УГОЛ участка. |
| 113 | `geo_cache_root()` | Каталог кэша ГЕОДАННЫХ (векторные слои весовой карты) — по аналогии с |
| 123 | `area_cache_dir()` | Папка АКТИВНОГО УЧАСТКА внутри кэша: `geo_cache/<THREAT_AREA_DIR>`. |
| 130 | `area_file(name)` | Путь к файлу участка по имени. Ищется СНАЧАЛА в папке участка, затем в корне |
| 147 | `_accumulate_segment(x0, y0, x1, y1, ox, oy, …)` | Добавить длину отрезка (x0,y0)->(x1,y1), попавшую в каждую ячейку, в out[iy,ix]. |
| 183 | `_accumulate_polyline(poly, ox, oy, h, nx, ny, …)` | ТОЧНЫЙ (посегментный) вариант — эталон/для тестов. В рабочем пути (реальные |
| 192 | `_rasterize_polylines(polylines, ox, oy, h, nx, ny, …)` | БЫСТРАЯ векторная растеризация: приближённая длина линий в каждой ячейке. |
| 220 | `_splat(x0, y0, dx, dy, seglen, nsamp, …)` | Раскидать длины сегментов по ячейкам (дискретизация + np.add.at). Внутренняя. |
| 237 | **ThreatGrid** | Регулярная сетка ячеек cell_km×cell_km по прямоугольнику bbox_km (в локальных |
| 242 | `__init__(bbox_km, cell_km)` |  |
| | **· геометрия ячеек ·** | |
| 266 | `cell_centers_km()` |  |
| 272 | `extent_km()` |  |
| | **· наложение слоёв ·** | |
| 277 | `add_line_layer(name, polylines, weight, attractor)` | Линейный слой: вклад ячейки += weight × (длина пересечения / размер ячейки). |
| 292 | `add_area_layer(name, polygons, weight, attractor, mark_urban)` | Площадной слой (застройка): вклад в ячейки внутри полигона. weight обычно |
| 321 | `place_masks_from_polygons(polys, settled)` | Растеризовать КОНТУРЫ населённых пунктов: вернуть (маска города, маска |
| 372 | `_free_zone_mask(free_points, city, village)` | Свободные зоны у точки вылета и у цели: где запрет города НЕ действует. |
| 406 | `apply_settlements(places_km, min_area_km2, town_city_km2, buffer_km, village_core, village_edge, …)` | Разметить населённые пункты и применить их к весу и проходимости. |
| 544 | `own_place_area_km2(buf_cells)` | Для КАЖДОЙ ячейки — площадь её «родного» населённого пункта (км²), то есть |
| 579 | `_own_place_small(city, max_km2, buf_cells)` | Маска «ячейка принадлежит НЕБОЛЬШОМУ населённому пункту» — по площади его |
| 586 | `_village_size_filter(village, skip_factor, townlike_med)` | Убрать из маски деревни пятна, которые по СОБСТВЕННОЙ градации участка |
| 620 | `settlement_stats()` | РАСПРЕДЕЛЕНИЕ пятен застройки по площади на рабочем участке (км²): |
| 642 | `place_profiles(lab, nlab, area, close_km)` | ПАСПОРТ каждого пятна застройки — два измеримых признака: |
| 673 | `grade_places(tag, area, dens, nlab, tol, min_sample, …)` | ПЕРЕСЧЁТ типа пункта по данным рабочей области. Возвращает (новый тег, отчёт). |
| 824 | `_classify_components(lab, nlab, area, places_km, min_area_km2, town_city_km2, …)` | Тип каждой компоненты застройки: 2 — город, 1 — деревня, 0 — не учитываем. |
| 893 | `_village_field(village_mask, core, edge, r_min_km, r_max_km)` | Поле штрафа деревень: `core` в центре, `edge` на ПОЛОВИНЕ радиуса, 0 на его |
| 924 | `add_relief_layer(dem_km, area_km, local_km, area_full_m, local_full_m, area_s, …)` | Рельеф: МНОЖИТЕЛЬ к уже накопленному ПОЛОЖИТЕЛЬНОМУ весу. |
| 980 | `relief_height()` | Высоты поверхности на сетке, м (None — рельеф не загружен). |
| 984 | `relief_k()` | Множитель рельефа: < 1 возвышенность, > 1 укрытие (None — рельеф не применён). |
| 988 | `relief_cut()` | Маска «уж очень высокая гора»: там вес снят полностью (None — рельефа нет). |
| 992 | `set_bridge_mask_from_layer()` | Ячейки-ПЕРЕПРАВЫ: мост из OSM (`bridge=yes`) НАД ВОДОЙ. Прежде мост |
| 1012 | `bridge_points_km(bridge_polys)` | ТОЧНЫЕ координаты переправ: середина каждой мостовой линии OSM, проходящей |
| 1039 | `crossing_cells_km(min_layers)` | Центры ячеек-ПЕРЕСЕЧЕНИЙ: где сходятся ≥ min_layers РАЗНЫХ слоёв-аттракторов |
| 1049 | `mark_water(polylines, buffer_km)` | Отметить ячейки воды (по руслам) + буфер — для ЗАПРЕТА датчиков (Задача 1 |
| 1058 | `add_intersection_bonus(bonus_per_extra)` | Funnel-бонус: за каждый слой-аттрактор в ячейке СВЕРХ первого — +bonus. |
| | **· ЗАПРЕТНЫЕ ЗОНЫ (рисует пользователь) ·** | |
| 1068 | `set_no_fly_zones(polys)` | Задать запретные зоны — список многоугольников [(x, y), …] в км. |
| 1079 | `no_fly_mask()` | Маска запретных зон (ny, nx) либо None, если зон нет. |
| | **· выгрузка ·** | |
| 1097 | `water_mask()` |  |
| 1100 | `urban_mask()` |  |
| 1103 | `flat_cells(positive_only)` | Центры ячеек (M,2) и веса (M,). positive_only — только аттракторные (w>0): |
| 1115 | `totals_by_layer()` |  |
| | **· сохранение растровой карты (ответ на открытый вопрос §9.5: растрово) ·** | |
| 1119 | `save_npz(path)` |  |
| 1128 | `load_npz(path)` |  |
| 1140 | `_box_mean(a, r)` | Среднее по квадратному окну (2r+1)² вокруг каждой ячейки — через интегральное |
| 1155 | `_relief_f(x)` | Отклик на превышение (аргумент — превышение, делённое на порог насыщения). |
| 1167 | `_points_in_polygon(px, py, poly)` | Векторный ray-casting: маска точек (px,py) внутри замкнутого полигона poly. |
| 1184 | `_label8(mask)` | Разметка связных компонент булевой маски (8-связность). Возвращает (lab, n): |
| 1208 | `_dilate(mask, r)` | Дилатация булевой маски на r ячеек (квадратная окрестность, чистый numpy). |
| 1224 | `_erode(mask, r)` | Эрозия булевой маски на r ячеек — обратная к `_dilate`. Нужна для ЗАМЫКАНИЯ |
| 1247 | `load_layers(lon0, lat0, data_path, bbox_lonlat)` | Слои цифровой карты в локальных км (якорь lon0,lat0). Возвращает (dict, источник). |
| 1275 | `_clip_layers_km(layers, bbox_km, margin_km)` | Оставить в слоях только то, что попало в рамку района плюс запас (всё в км). |
| 1306 | `layers_cache_path()` | Путь к кэшу слоёв активного участка: `geo_cache/<участок>/<THREAT_LAYERS_FILE>` |
| 1375 | `layers_from_osm(pbf_path, lon0, lat0, bbox_lonlat)` | Прочитать локальный .osm.pbf и вернуть слои в км-фрейме (lon0,lat0). |
| 1438 | `load_dem_grid(grid, lon0, lat0, path)` | Высоты рельефа, приведённые к ячейкам сетки: массив (ny,nx) в метрах, NaN — нет |
| 1473 | `dem_files()` | Файлы рельефа участка: `THREAT_DEM_FILE` + .tif/.tiff/.hgt в папке участка (с |
| 1495 | `load_dem_display(lon0, lat0, bbox_km, max_px)` | Высоты для ОТРИСОВКИ: окно участка в исходном разрешении растра, прорежённое до |
| 1549 | `_sample_geotiff(path, lon, lat, cell_deg)` | Выборка высот GeoTIFF в точках (lon,lat). |
| 1586 | `_block_mean_at(band, bad, r, c, hy, hx)` | Среднее значение растра по прямоугольнику (2hy × 2hx пикселей) вокруг каждой точки |
| 1613 | `_sample_hgt(path, lon, lat)` | Выборка высот из сырого SRTM .hgt (int16 big-endian, тайл 1°×1°). |
| 1637 | `_clip_box_km(lon0, lat0, bbox_lonlat, margin_km)` | Рамка обрезки (x0,x1,y0,y1) в км: участок плюс запас со всех сторон. |
| 1644 | `_clip_parts(parts, clip)` | Оставить только куски геометрии, попадающие в рамку. |
| 1680 | `_edge_point(p_in, p_out, clip)` | Точка на отрезке p_in→p_out, лежащая на границе рамки clip (p_in внутри). |
| 1693 | `_places_from_osm(osm, lon0, lat0, clip)` | Населённые пункты -> (точки, контуры). |
| 1740 | `_place_name(tags)` | Название населённого пункта из тегов OSM. Пустая строка — имени нет. |
| 1759 | `_geom_to_kms(geom, lon0, lat0)` | shapely-геометрия -> список массивов (N,2) в км (линии и кольца полигонов). |
| 1793 | `_load_layers_npz(path)` | Слои из .npz, подготовленного офлайн-скриптом. Формат: ключи '<layer>__<i>' |
| 1806 | `_ll(seq, lon0, lat0)` | [(lon,lat),...] -> (N,2) км в фрейме (lon0,lat0). |
| 1813 | `_synthetic_layers(lon0, lat0)` | Демо-схема условного участка местности: русло реки (аттрактор+вода), |
| 1876 | `build_threat_grid(layers, bbox_km, cell_km, enabled, dem, free_points)` | Собрать ThreatGrid: наложить слои с весами из THREAT_LAYERS, funnel-бонус, |
| 1953 | **ThreatModel** | Состояние вкладки 3: цифровая карта угроз + расстановка датчиков по ней. |
| 1956 | `__init__(params)` |  |
| 2015 | `_drop_read_cache()` | Забыть прочитанное с диска: район или источник сменились. |
| | **· РАБОЧИЙ РАЙОН (план 8, задача 8.2) ·** | |
| 2034 | `set_area(bbox_lonlat)` | Задать РАБОЧИЙ РАЙОН (lon_min, lat_min, lon_max, lat_max). |
| 2084 | `clear_area()` | Убрать рабочий район: карта, слои, датчики и маршруты обнуляются. |
| 2119 | `area_size_km()` | Размер рабочего района (ширина, высота) в км — для проверок и подписей. |
| | **· КООРДИНАТЫ НАРУЖУ (план 8, задача 8.3) ·** | |
| 2130 | `km_to_lonlat_arr(pts_km)` | Массив (N,2) км -> массив (N,2) градусов (долгота, широта). |
| 2138 | `sensors_lonlat(big)` | Позиции датчиков в градусах: малые (по умолчанию) или большие. |
| 2142 | `routes_lonlat(routes)` | Маршруты в градусах: список массивов (M,2). По умолчанию — накопленные. |
| 2147 | `cells_lonlat()` | ЦЕНТРЫ ячеек сетки в градусах, (M,2) — вместе с `flat_cells` дают |
| | **· ЗАПРЕТНЫЕ ЗОНЫ (рисует пользователь на карте) ·** | |
| 2158 | `_sync_no_fly()` | Перенести зоны в сетку (она их вычитает из проходимости) и сбросить то, что |
| 2170 | `add_no_fly_zone(poly)` | Добавить зону: список вершин [(x, y), …] в км. Меньше трёх вершин — не зона. |
| 2179 | `clear_no_fly_zones()` | Убрать все зоны — карта и веса при этом не меняются. |
| 2187 | `undo_no_fly_zone()` | Убрать последнюю зону (для случая «нарисовал не туда»). |
| 2195 | `no_fly_check()` | Что зоны сделали с задачей. Возвращает словарь с полями: |
| | **· источник данных / выбор слоёв ·** | |
| 2228 | `set_data_source(path)` | Задать файл цифровых карт (.npz или .osm.pbf); None -> авто (кэш/схема). |
| 2233 | `set_enabled_layers(names)` | Ограничить набор накладываемых слоёв (iterable имён); None -> все. |
| | **· рельеф ·** | |
| 2239 | `has_dem()` | Есть ли файл высот (по нему кнопка «Добавить рельеф» активна или нет). |
| 2243 | `set_relief(on)` | Включить/выключить учёт рельефа в весовой карте (кнопка «Добавить рельеф»). |
| 2263 | `relief_display()` | Высоты в родном разрешении для отрисовки: (массив, extent_km) или None. |
| | **· построение карты (наложение цифровых слоёв на сетку) ·** | |
| 2271 | `build()` | Загрузить цифровые слои (из файла/кэша/демо) и наложить их на сетку 500 м — |
| 2351 | `_route_gap_km()` | РАЗРЫВ между весовыми секторами (км) — задаётся в интерфейсе («Входные данные»). |
| 2358 | `_route_slack_km()` | УХОД ОТ ОРИЕНТИРА (км) — насколько маршрут отклоняется вбок от реки или дороги, |
| 2364 | `_ctx_key()` | От чего зависит кэш контекста выборки: разрыв и уход от ориентира (оба меняют |
| 2371 | `_ensure_ctx(entry, target)` | Контекст выборки (проходимость, поля расстояний, VIA). Пересобирается, только |
| 2384 | `_route_sig(poly)` | Огрублённая подпись маршрута (для дедупликации почти одинаковых путей). |
| | **· маршруты пролёта вход->цель ·** | |
| 2392 | `plan_routes()` | ВОЗМОЖНЫЕ маршруты вход→цель — показываются ДО итераций, вместе с областью залёта. |
| 2446 | `_spread_over_area(pool, want, spread_w)` | Жадно отобрать `want` маршрутов, покрывающих область залёта КАК МОЖНО ШИРЕ. |
| 2495 | `count_routes()` | ТОЧНОЕ число возможных маршрутов вход→цель внутри области залёта (без перебора). |
| 2505 | `_spend()` | Профиль расхода запаса хода — повадка БПЛА для ИТЕРАЦИЙ (панель справа). |
| | **· ИТЕРАЦИОННЫЕ маршруты (стохастические пути по коридорам) ·** | |
| 2512 | `iter_reset()` | Начать выборку заново: пересчитать авто-запас хода и огибающую, подготовить |
| 2523 | `iter_step()` | Одна итерация: сгенерировать ОДИН маршрут (несколько попыток) и добавить его в |
| 2543 | `iter_batch()` | Сгенерировать T маршрутов сразу (без анимации). T = p.threat_iter_routes. |
| 2580 | `iterate_routes()` |  |
| 2583 | `_refresh_envelope()` | Пересчитать авто-запас хода и огибающую мест пролёта. Возвращает (вход, цель). |
| 2600 | `sync_auto_L_max(min_corridor_km)` | Если запас хода НЕ задан руками — авто: \|AB\|·(1+THREAT_LMAX_AUTO_FRAC), но не меньше |
| 2617 | `ensure_built()` | Гарантировать, что карта построена (построить, если ещё нет), и вернуть сетку. |
| | **· кандидатные позиции датчика (сетка минус вода) ·** | |
| 2624 | `auto_cand_step_km()` | Каким шаг сетки ДОЛЖЕН быть при текущем радиусе: половина R, 0.5…6 км. |
| 2628 | `sync_cand_step(force)` | Подогнать шаг сетки под радиус — но только КОГДА РАДИУС СМЕНИЛСЯ. |
| 2648 | `cand_step_km()` | Шаг сетки кандидатных позиций, км (не меньше 0.5). |
| 2652 | `candidate_positions()` | Возможные места установки датчика — узлы регулярной сетки с шагом |
| 2667 | `_on_water(pts)` | Маска кандидатов, попавших в водную ячейку (или её буфер) — запрет датчика. |
| | **· расстановка датчиков (ЭТАП 4: по тепловой карте МАРШРУТОВ, §4.6) ·** | |
| 2679 | `place_sensors()` | Расставить датчики по тепловой карте МАРШРУТОВ (где реально/вероятно летает БПЛА), |
| 2699 | `_place_by_weight(cand)` | Расстановка по СТАТИЧЕСКОЙ весовой карте (единица покрытия — клетка веса). |
| 2712 | `_place_by_routes(cand, routes)` | Расстановка по ВЫБОРКЕ маршрутов (частота пролёта). Правила: |
| 2791 | `_place_big(routes)` | БОЛЬШИЕ датчики — «круговой щит» у цели: `threat_N_big` штук радиусом |
| 2883 | `_sample_for_sensors()` | Выборка маршрутов, по которой считаются датчики и 2-я тепловая карта (§4.6): |
| 2890 | `generalized_sample(frac)` | ОБОБЩЁННАЯ выборка итерационных маршрутов для показа: ~frac (10 %) от УЖЕ |
| 2932 | `route_density_field()` | 2-я ТЕПЛОВАЯ КАРТА — частота пролёта БПЛА: сколько маршрутов проходит через |
| 2948 | `_evaluate(cells_xy, cells_w)` | Показатели: суммарный вес карты, покрытый вес, доля, разбивка по слоям и |
| 2979 | `metrics()` |  |
| 2982 | `reachable_stats()` | Корректная мера «всех возможных мест пролёта» — размер огибающей (граф-обход): |
| | **· точка входа (фиксирована у реки) и цель (можно задать кликом) ·** | |
| 2992 | `set_target(x_km, y_km)` | Задать целевую точку кликом по карте (режим «указать цель»). Запас хода |
| 3009 | `entry_km()` | ПРЕДСТАВИТЕЛЬ точки входа — для показателей и авто-запаса хода. При заданном |
| 3019 | `entry_points_km()` | ВСЕ точки входа списком: пять точек сектора либо одна демо-точка. |
| 3023 | `target_only_km()` | Цель в км: заданная кликом либо демо-метка участка. |
| 3031 | `entry_target_km()` | (вход, цель) в км — ОДНОЙ точкой. Вход: представитель (середина точек сектора) |
| 3037 | `entries_target_km()` | (входы, цель): вход — СПИСОК точек, если задан сектор, иначе одна точка. |
| | **· СЕКТОР ПОЯВЛЕНИЯ БПЛА ·** | |
| 3045 | `set_sector(x_km, y_km)` | Задать сектор кликом по краю карты: ось «цель → клик», по |
| 3056 | `clear_sector()` | Убрать сектор: вход снова один — демо-точка участка. |
| 3065 | `_reset_after_entry_change()` | Сброс после смены входа: контекст, маршруты, область залёта, запас хода. |
| 3075 | `sector_edges_km()` | Две крайние точки сектора на рамке участка — для отрисовки. None, если |
| 3094 | `_ray_to_bbox(origin, u)` | Длина луча из `origin` по орту `u` до рамки участка (км). |
| 3107 | `compute_entry_points()` | Отобрать точки входа на краю карты внутри сектора — с учётом весовой карты. |
| 3201 | `_entry_window_score(xs, ys, target_km)` | ПЕРСПЕКТИВА ДВИЖЕНИЯ из точки: средний вес прямоугольника, вытянутого от неё |


## view_qt/threat_view.py

**Отдаёт наружу:** `ThreatMapView`.

| стр. | что | зачем |
|---:|---|---|
| 212 | `_hillshade(h, az_deg, alt_deg, z, lo, hi)` | Теневая отмывка карты высот: множитель яркости по наклону поверхности к источнику |
| 230 | **ShiftedAxis** | Ось, у которой ноль можно перенести в произвольную точку (план 8, задача 8.2). |
| 243 | `__init__(orientation, **kwargs)` |  |
| 247 | `set_origin(value)` |  |
| 252 | `tickStrings(values, scale, spacing)` |  |
| 259 | `_threat_cmap()` |  |
| 268 | `_threat_lut()` |  |
| 274 | `_iter_heat_lut()` | LUT для 2-й карты (частота пролёта БПЛА) — magma, отличается от turbo весов. |
| 280 | `_iter_heat_cmap()` | Та же magma, но как ColorMap — для цветовой шкалы у правого края. |
| 289 | **ThreatMapView** |  |
| 330 | `__init__(model_bbox_km, lon0, lat0, params, area_max_km)` |  |
| | **· режим «указать цель» (клик по карте) ·** | |
| 431 | `_begin_target()` |  |
| | **· режим «задать сектор появления» (клик по краю карты) ·** | |
| 437 | `_begin_sector()` |  |
| 443 | `_show_click_coords(ev)` | Координаты точки клика — в строку под картой (план 8, задача 8.3). |
| 458 | `_on_scene_click(ev)` |  |
| | **· режим «ЗАДАТЬ РАЙОН МОДЕЛИРОВАНИЯ» (план 8, задача 8.2) ·** | |
| 487 | `_begin_area()` |  |
| 494 | `_end_area(apply_it)` |  |
| 513 | `_area_click(ev)` |  |
| 539 | `_clear_area()` | Кнопка «Убрать» рядом с районом — сброс к состоянию «район не задан». |
| 544 | `_draw_area_preview()` | Прямоугольник, который пользователь тянет прямо сейчас. |
| | **· режим рисования ЗАПРЕТНОЙ ЗОНЫ ·** | |
| 556 | `_begin_zone()` | Включить рисование: клики ставят вершины, нажатие колёсика замыкает зону. |
| 563 | `_end_zone(apply_it)` | Закончить рисование. apply_it=False — бросить начатое, ничего не меняя. |
| 573 | `_zone_click(ev)` | Клик в режиме рисования: левая кнопка — вершина, КОЛЁСИКО — замкнуть зону. |
| 592 | `keyPressEvent(ev)` | Esc — бросить начатое рисование (зоны или района). |
| 603 | `_draw_zone_preview()` | Показать зону, которую пользователь рисует прямо сейчас. |
| 621 | `_hatch_polygon(poly, step_km, angle_deg)` | Диагональные штрихи ВНУТРИ многоугольника: списки x, y с разрывами (NaN). |
| 655 | `render_no_fly(polys, toggles)` | Отрисовать УЖЕ ЗАДАННЫЕ запретные зоны (список полигонов в км). |
| | **· окно выбора цифровых карт (источник + слои) ·** | |
| 679 | `_open_data_dialog()` |  |
| | **· своя карта картинкой (план 8, задача 8.5) ·** | |
| 686 | `_open_map_image_dialog()` | Окно «Своя карта». Не модальное: координаты углов сверяют по самой карте. |
| 695 | `_apply_map_image(path, lon_min, lat_min, lon_max, lat_max, hide_tiles, …)` | Показать картинку по координатам углов. Возвращает строку для окна. |
| 753 | `_clear_map_image()` | Убрать картинку и вернуть тайловую подложку. |
| | **· окно «Входные данные» (не блокирует программу) ·** | |
| 761 | `_open_input_dialog()` |  |
| 768 | `_open_sensors_dialog()` | Окно «Датчики» — параметры обоих типов. Не блокирует основное окно. |
| 776 | `refresh_sensors_dialog()` | Обновить поля окна «Датчики» (шаг сетки мог пересчитаться под радиус). |
| 781 | `set_ab_distance(km)` |  |
| 785 | `refresh_input_dialog()` | Обновить поля окна «Входные данные» под текущие параметры (авто-L_max и пр.). |
| 790 | `set_iter_checked(on)` | Включить/выключить чекбокс «итерации» без повторного запуска расчёта. |
| | **· опорные точки-ориентиры для схемы/подписей ·** | |
| 797 | `_orient_points()` |  |
| 803 | `set_callbacks(**cbs)` |  |
| 812 | `_legend_html(scale)` | HTML-легенда (что каким цветом). ВСЕ цвета берутся из палитр THREAT_COLORS и |
| 860 | `_relief_legend_html(scale)` | Легенда режима «приоритет по высоте»: три цвета — три строки, без пояснений. |
| 882 | `_apply_stylesheet()` | Оформление — общее для трёх вкладок (`view_qt/ui_common.py`). |
| 886 | `_header(text)` |  |
| 890 | `_tint(btn, color)` |  |
| 896 | `_build_ui(params)` |  |
| 1276 | `_build_layer_box(col)` | Блок «ПОКАЗ»: слои сгруппированы по смыслу и разложены в две колонки. |
| 1349 | `_opacity_items(attrs)` | Развернуть цели в реальные элементы сцены (включая словарные слои). |
| 1363 | `_add_group_opacity(col, items)` | Ползунок прозрачности на группу — гасит всё, что эта группа рисует. |
| 1378 | `_set_group_opacity(attrs, value)` | Прозрачность слоёв-заливок. Модель не трогаем — это чистая отрисовка. |
| 1394 | `_layer_toggled()` | Слой переключили. Во время группового переключения молчим — перерисуем один |
| 1404 | `_apply_axis_origin()` | Перенести ноль осей: в угол района или в угол всей области. |
| 1419 | `_toggle_group(kids, on)` | Переключатель группы: гасит/зажигает детей одной перерисовкой. |
| 1430 | `_sync_master(master, kids)` | Шапка группы показывает, включён ли хоть один её слой. |
| 1438 | `set_layer_enabled(**state)` | Погасить слои, которых ещё нет: «галка стоит, а на карте ничего» — это вопрос |
| 1469 | `_build_map_combo(col, params)` |  |
| 1489 | `_map_changed(i)` |  |
| 1494 | `_offline_changed(_s)` |  |
| 1499 | `_iter_mode_changed(i)` |  |
| 1502 | `_gen_frac_changed()` |  |
| 1505 | `get_iter_gen_frac()` | Доля обобщённой выборки как число 0..1 (поле задаётся в процентах). При ошибке — |
| 1514 | `_spread_changed(i)` |  |
| 1517 | `_spend_changed(i)` |  |
| 1520 | `_on_field_submit()` |  |
| 1525 | `_build_scene_items()` | Создать ВСЕ постоянные слои-элементы карты один раз (потом только меняем данные, |
| 1757 | `get_toggles()` |  |
| 1776 | `get_param_values()` |  |
| 1783 | `set_param_values(params)` |  |
| 1791 | `_work_buttons()` | Кнопки, которым нужен заданный РАЙОН МОДЕЛИРОВАНИЯ (план 8, задача 8.2). |
| 1798 | `_panel_widgets()` | ВСЕ управляющие элементы правой панели: кнопки, галочки, поля, списки. |
| 1810 | `set_busy(busy)` | Заблокировать кнопки действий на время фонового расчёта (чтобы не запускать |
| 1821 | `set_relief_shown(on)` | Включить/выключить показ рельефа вместе с самим рельефом. |
| 1836 | `set_area_rect(bbox_km)` | Перерисовать СИНЮЮ рамку по текущему району и перенести на него ноль осей. |
| 1852 | `set_area_ready(ready, size_km, bbox_km)` | Район задан или нет: от этого зависит вся остальная панель. |
| 1878 | `set_source(text)` |  |
| 1881 | `set_metrics(lines)` |  |
| 1884 | `flash_title(text, color)` |  |
| 1887 | `set_title(text)` |  |
| 1890 | `frame_bbox(whole_area)` | Вписать в окно район (по умолчанию) или ВСЮ область показа. |
| 1902 | `process_pending()` |  |
| | **· отрисовка данных карты ·** | |
| 1912 | `_polys_to_xy(polys, view_box)` | Список ломаных -> массивы X,Y с NaN-разрывами, с ПРОРЕЖИВАНИЕМ (иначе |
| 1958 | `_view_box_km()` | Видимая область в км (x0,x1,y0,y1) — по ней прореживаются слои. |
| 1966 | `_zoomed_in(view_box)` | Приблизились ли настолько, что застройку пора рисовать НАСТОЯЩИМИ контурами |
| 1975 | `render_layers(layers, bridge_pts, toggles, built_mask, extent)` | Отрисовать векторные слои. Чекбокс «векторные слои» ПОЛНОСТЬЮ скрывает их |
| | **· НАЗВАНИЯ НАСЕЛЁННЫХ ПУНКТОВ ·** | |
| 2028 | `_render_place_labels(layers, toggles)` | Подписать населённые пункты своим шрифтом. Имена приходят слоем `place_names` |
| 2069 | `_label_rank(code)` | Ранг пункта ДЛЯ ПОДПИСИ (не то же, что код типа в расчёте). |
| 2080 | `_set_place_labels(items)` | Обновить пул текстовых элементов. Пул переиспользуется: создавать и удалять |
| 2106 | `_note_scale_layer(key, on)` | Запомнить, какой слой включили последним (по нему выбирается шкала). |
| 2115 | `_update_scale()` | Показать шкалу того слоя, который включён последним. Ни одного — скрыть. |
| 2135 | `_set_casing(name, xs, ys, visible)` | Обводке — те же данные, что и самой линии (она рисуется под ней). |
| 2143 | `apply_vector_visibility()` | Показать/скрыть отдельные векторные слои ПО СОХРАНЁННЫМ данным. |
| 2167 | `_open_vector_dialog()` | Окно со списком векторных слоёв. Не модальное: с ним можно работать, глядя |
| 2178 | `set_vector_visible(name, on)` | Переключить один слой (вызывается из окна) и сразу перерисовать. |
| 2183 | `vector_visible()` |  |
| 2186 | `reposition_legends()` | Прижать обе легенды к углам ТЕКУЩЕГО вида: общую — влево вниз, легенду |
| 2222 | `_scale_markers(scale)` |  |
| 2237 | `_scale_zone_widths(k)` | Толщина линий запретной зоны: на общем виде полная, при максимальном |
| 2269 | `_scale_line_widths(k)` | Подогнать толщину линий и кантов под масштаб (k = 1 вблизи, меньше — дальше). |
| 2287 | `_render_builtup_raster(mask, extent)` | Застройка растровой маской (общий вид). None — скрыть слой. |
| 2302 | `render_threat(weight, extent, toggles)` |  |
| 2316 | `render_relief(dem, extent, toggles)` | КАРТА ВЫСОТ: привычная топография (низины зелёные → вершины светлые) со |
| 2349 | `render_relief_priority(k, cut, extent, toggles)` | ПРИОРИТЕТ ПО ВЫСОТЕ: что рельеф сделал с весом. Синим — укрытия (множитель > 1), |
| 2384 | `set_relief_button(active, enabled)` | Состояние кнопки рельефа: подпись «Добавить/Убрать», подсветка и доступность |
| 2396 | `render_exclusions(water_mask, urban_mask, extent, toggles)` | Показать зоны-исключения: вода (синий — запрет датчика) и населённые пункты |
| 2412 | `render_routes(routes, route_area, extent, toggles)` | ВСЕ возможные места пролёта: розовый ФОН = огибающая (route_area) — КОРРЕКТНО |
| 2440 | `render_iter_routes(routes, toggles)` | Показать НАКОПЛЕННЫЕ итерационные маршруты (статично). Пошаговую анимацию полёта |
| 2446 | `render_generalized(routes, toggles)` | ОБОБЩЁННАЯ выборка (~10 % итераций): частые, но распределённые маршруты — |
| 2461 | `render_iter_heat(density, extent, toggles)` | 2-я тепловая карта — частота пролёта БПЛА (плотность маршрутов), LUT magma. |
| 2478 | `iter_show_accumulated(routes)` | Отрисовать все накопленные маршруты тонкими линиями (одним item, через NaN). |
| 2491 | `iter_setup_flight(route)` | Начать анимацию полёта по маршруту: текущий путь и маркер БПЛА — в старте. |
| 2497 | `iter_update_flight(route, j)` | Прорисовать текущий маршрут до точки j и передвинуть маркер БПЛА в неё. |
| 2503 | `iter_clear_current()` | Убрать текущий (летящий) маршрут и маркер БПЛА (накопленные остаются). |
| 2508 | `get_iter_T()` | Число итераций (T) из поля ввода; при ошибке — прежнее из params. |
| 2515 | `set_iter_running(running)` | Текст кнопки «Пуск»/«Пауза» (как во вкладке 2). |
| 2519 | `render_crossings(crossings, toggles)` | Пересечения (перекрёстки дорог/рек/ЛЭП) — узлы развилок. Скрыто по умолчанию. |
| 2527 | `render_candidates(cand, toggles)` |  |
| 2535 | `render_sensors(sensors, R, sensors_big, R_big)` | Нарисовать датчики ДВУХ типов: у каждого — КОЛЬЦО зоны обзора (залитый круг) и |
| 2570 | `render_entry_target(entry, target)` |  |
| 2574 | `render_sector(sector, entry_points, toggles)` | Сектор появления БПЛА: `sector` = (вершина-цель, край, край) или None, |
| 2602 | `showEvent(e)` |  |
| 2613 | **VectorLayersDialog** | Окно «Векторные слои»: какие именно ориентиры показывать на карте. |
| 2626 | `__init__(view)` |  |
| 2676 | `sync()` | Подтянуть галки из представления (окно открыли повторно или сменили общий |
| 2689 | `_master_toggled(on)` |  |
| 2693 | `_update_enabled()` |  |
| 2698 | `_set_all(on)` |  |
| 2707 | **DigitalMapsDialog** | Отдельное окно: выбор ИСТОЧНИКА цифровых карт и набора накладываемых СЛОЁВ. |
| 2714 | `__init__(parent, data_path, enabled_layers)` |  |
| 2756 | `_src_text()` |  |
| 2760 | `_pick_file()` |  |
| 2768 | `_use_auto()` |  |
| 2772 | `result_choices()` | (data_path\|None, enabled_layers_set). Все галочки сняты -> пустой набор |
| 2779 | **InputDataDialog** | НЕмодальное окно «Входные данные» маршрута: запас хода, скорость, крен, длина прямого |
| 2814 | `__init__(parent, params, on_apply)` |  |
| 2849 | `refresh(params)` |  |
| 2858 | `set_ab(km)` |  |
| 2861 | `_auto_changed(*_)` | Авто-запас: поле L_max ВСЕГДА показывает текущее значение (и при снятии галочки |
| 2877 | `_apply()` | Применить поля. Нечисловое поле берётся из текущих параметров (не роняем ввод). |
| 2894 | **SensorsDialog** | НЕмодальное окно «Датчики»: параметры ДВУХ типов сразу. |
| 2933 | `__init__(parent, params, on_apply)` |  |
| 2966 | `_fmt(v)` |  |
| 2969 | `refresh(params)` |  |
| 2974 | `_apply()` | Собрать поля. Нечисловое значение берётся из текущих параметров — ошибка |


## model/threat_routes.py

| стр. | что | зачем |
|---:|---|---|
| 51 | `_cell_of(grid, xy)` | Точка (x, y) в км -> индекс ячейки (iy, ix), обрезанный по границам сетки. |
| 58 | `_dijkstra_dist(passable, start, h)` | Поле ГЕОМЕТРИЧЕСКИХ расстояний (км) от start по проходимым ячейкам (8-связно). |
| 83 | `_cost_mul(grid, wnorm, power, relief_power)` | Множитель цены прохода через ячейку — ПРОИЗВЕДЕНИЕ двух независимых сомножителей: |
| 115 | `_dijkstra_cost(passable, start, h, mul)` | Поле СТОИМОСТИ пути от start (в км-эквивалентах) по проходимым ячейкам. |
| 146 | `passable_mask(grid, max_gap_km, slack_km)` | Проходимые ячейки для маршрута: коридоры (вес>0), СШИТЫЕ через провалы ≤ max_gap_km, |
| 189 | `_as_entry_list(entry_km)` | Точки входа списком. Принимает и одну точку (x, y), и список точек — так вызовы |
| 198 | `_open_endpoints(pas, cells_yx, grid, radius_km)` | Открыть проходимость в окрестности ~radius_km вокруг заданных ячеек (вход/цель), |
| 208 | `flight_envelope(grid, entry_km, target_km, L_max, max_gap_km, slack_km)` | ВСЕ ВОЗМОЖНЫЕ МЕСТА ПРОЛЁТА при запасе хода L_max. Ячейка попадает в «возможные |
| 240 | `count_possible_routes(env, gt, entry_cell, goal_cell)` | СКОЛЬКО ВСЕГО маршрутов вход→цель существует внутри области залёта — точное число, |
| | **· ИТЕРАЦИОННАЯ (стохастическая) генерация множества маршрутов ·** | |
| 285 | `_mode_corridor_pref(wnorm, wmode)` | Предпочтение соседа по ВЕСУ клетки (тепловая карта), зависит от РЕЖИМА. «Пол» у |
| 302 | `_lookahead_fields(grid, look_km)` | Средний множитель РЕЛЬЕФА впереди по каждому из 8 направлений: `ahead[d][y,x]` — |
| 339 | `_catmull_rom(points, seg_km, alpha)` | ИНТЕРПОЛЯЦИЯ ломаной гладкой кривой (центростремительный сплайн Катмулла–Рома): |
| 370 | `_rdp(points, eps)` | Упрощение ломаной (Ramer–Douglas–Peucker): убирает точки ближе eps к хорде — снимает |
| 398 | `_poly_len_km(poly)` | Длина ломаной (км) — сумма длин звеньев. Нужна для проверки «уложились в запас хода». |
| 404 | `_cells_yx_to_km(grid, cells_yx)` | Список ячеек (y,x) -> точки-центры (M,2) в км. |
| | **· ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ / ЗАДЕЛ НА БУДУЩЕЕ (движение маршрута) ·** | |
| 533 | `_spend_tau(slack_km, slack0_km, frac_done, profile)` | «Температура» выбора соседа: чем она меньше, тем жёстче маршрут идёт к цели. |
| 560 | `_walk_field(ctx, dfield, start, goal, budget, turn_interval_km, …)` | Стохастический спуск по полю расстояний dfield от start к goal (надёжно приходит): |
| 768 | `_nearest_passable_cell(pas, grid, y_km, x_km, max_km)` | Ближайшая проходимая ячейка к точке (км). None, если ничего в радиусе max_km. |
| 783 | `_seg_dist_km(pt, a, b)` | Расстояние от точки pt до ОТРЕЗКА a-b (км). Для сортировки via по «дальности обхода». |
| 791 | `build_iter_context(grid, entry_km, target_km, max_gap_km, n_grid, slack_km)` | Подготовить (один раз, кэшируется) контекст выборки. |
| 1002 | `_pick_via(ctx, spread, rng, L_max)` | Выбрать индекс VIA по режиму РАЗБРОСА (по дальности обхода segd): center — близкие к |
| 1032 | `_off_corridor(poly, grid, pas)` | Сколько точек линии вышло за ПРОХОДИМУЮ зону (город / провал шире разрыва). |
| 1039 | `_mean_weight(poly, grid)` | Средний вес карты под линией — по ячейкам, через которые она проходит. |
| 1047 | `_round_corners(poly, raw, rounds, max_shift)` | Скруглить остаточную угловатость линии, не уводя её от расчётного пути. |
| 1072 | `_smooth_safe(raw, grid, pas, eps0)` | Сгладить ломаную, НЕ ВЫВОДЯ её из коридора и НЕ УВОДЯ с весовой карты. |
| 1119 | `sample_one_route(ctx, mode, L_max, turn_interval_km, rng, spread, …)` | ОДИН стохастический маршрут вход→цель. mode = приоритет клетки по ВЕСУ |


## tools/переносимое/make_figures.py

| стр. | что | зачем |
|---:|---|---|
| 29 | `_find_root(markers)` | Корень проекта — ближайшая папка ВВЕРХ, содержащая любой из маркеров. |
| 73 | `save(fig, name)` |  |
| 84 | `fig_diminishing()` | Прирост от каждого следующего датчика падает, накопленное покрытие растёт. |
| 152 | `fig_saturation()` |  |
| 180 | `fig_guarantee()` |  |
| 206 | `fig_saa()` |  |
| 234 | `fig_extrema()` |  |
| 266 | `fig_concavity()` |  |
| 297 | `fig_ellipse()` |  |
| 353 | `fig_arc()` |  |
| 418 | `fig_corridor()` |  |
| 482 | `fig_start_zone()` |  |
| 533 | `fig_turn_radius()` |  |
| 596 | `fig_segments()` |  |
| 637 | `fig_relief()` | relief_k = 1 − 0.75·f(A/60) − 0.45·f(B/35), зажат в [0.25, 1.40]. |
| 676 | `fig_greedy_steps()` |  |
| 732 | `fig_distributions()` | Три закона распределения, которыми задаётся случайность в модели. |
| 790 | `fig_expectation()` | Матожидание — центр тяжести; дисперсия — разброс вокруг него. |
| 816 | `fig_risk_criteria()` | Три разных критерия на одном распределении: среднее, квантиль, худший случай. |
| 845 | `fig_chance_constraint()` | Вероятностное ограничение: не «в среднем хорошо», а «плохо — редко». |
| 876 | `fig_sets()` | Операции над множествами — язык, на котором записаны все ограничения. |
| 923 | `fig_integral()` | Сумма и интеграл — одно и то же действие при разном шаге. |
| 951 | `fig_convex()` | Выпуклое множество: отрезок между любыми точками не выходит наружу. |
| 982 | `fig_gradient()` | Линии уровня и градиент: как выглядит «идти вверх» в непрерывной задаче. |
| 1013 | `fig_scenario_tree()` | Откуда берётся выборка сценариев и что с ней делают. |
| 1076 | `main()` |  |


## controller_qt/threat_controller.py

**Отдаёт наружу:** `ThreatController`.

| стр. | что | зачем |
|---:|---|---|
| 23 | **_TaskSignals** | Мостик «фоновый поток → интерфейс». Qt-объекты из потока трогать нельзя, поэтому |
| 30 | **_Task** | Тяжёлый расчёт (чтение OSM / наложение слоёв / расстановка) в ФОНОВОМ потоке, |
| 35 | `__init__(kind, work_fn, sig)` |  |
| 39 | `run()` |  |
| 51 | **ThreatController** | Связывает МОДЕЛЬ (ThreatModel — чистый numpy) и ПРЕДСТАВЛЕНИЕ (ThreatMapView — Qt). |
| 62 | `__init__(model, view)` |  |
| | **· ЗАПРЕТНЫЕ ЗОНЫ ·** | |
| 109 | `on_zone_added(poly)` | Пользователь нарисовал зону: пролёт там запрещён, всё зависящее — пересчитать. |
| 115 | `on_zone_undo()` | Убрать последнюю зону. |
| 122 | `_after_zone_change()` | Общее для добавления и отмены: пересчёт и ЧЕСТНОЕ сообщение о последствиях. |
| | **· РЕЛЬЕФ ·** | |
| 144 | `on_relief()` | Кнопка «Добавить рельеф» / «Убрать рельеф». |
| 172 | `_on_view_changed()` | Вид изменился — перерисовать только векторные слои (остальное не зависит |
| | **· ИТЕРАЦИИ (симуляция как во вкладке 2) ·** | |
| 189 | `_iter_ready()` |  |
| 196 | `on_iter_speed(v)` |  |
| 199 | `_iter_title()` |  |
| 213 | `on_iter_play()` |  |
| 230 | `_iter_finished()` |  |
| 233 | `_maybe_refresh_sensors(force)` | Пересчитать датчики по УЖЕ накопленной выборке пролётов — периодически, а не |
| 251 | `_anim_stop()` |  |
| 256 | `_anim_tick()` | Один кадр анимации (таймер 33 мс). Если текущий маршрут «долетел» — берём из |
| 298 | `on_iter_step()` |  |
| 318 | `on_iter_batch()` |  |
| | **· окно «Входные данные»: применить сразу (не блокирует программу) ·** | |
| 329 | `on_sensors_apply(vals)` | Окно «Датчики»: применить параметры ОБОИХ типов и переставить датчики. |
| 351 | `on_input_apply(vals)` |  |
| | **· смена приоритета по весу (max/medium/min/mix) ·** | |
| 381 | `on_iter_mode(key)` |  |
| | **· смена разброса по карте (center/middle/edge/mix) ·** | |
| 388 | `on_iter_spread(key)` |  |
| | **· смена профиля расхода запаса хода (late/early/even) ·** | |
| 397 | `on_iter_spend(key)` |  |
| | **· запуск тяжёлого расчёта в фоне ·** | |
| 404 | `_run_async(kind, work_fn)` | Запустить тяжёлую функцию модели `work_fn` в ФОНОВОМ потоке (пул QThreadPool), |
| 421 | `_on_task_done(payload)` | Приходит в ОСНОВНОЙ поток по сигналу завершения фоновой задачи. Разбирает метку |
| | **· построение карты (в фоне) ·** | |
| 486 | `_work_build()` |  |
| 490 | `_work_place()` |  |
| 499 | `on_build()` |  |
| | **· РАБОЧИЙ РАЙОН (план 8, задача 8.2) ·** | |
| 503 | `on_set_area(bbox_lonlat)` | Задать рабочий район: сетка, веса, маршруты и датчики — только по нему. |
| 532 | `on_clear_area()` | Убрать рабочий район: всё расчётное обнуляется, остаётся только показ. |
| | **· выбор цифровых карт (источник + слои) из отдельного окна ·** | |
| 549 | `on_choose_data(path, enabled)` |  |
| | **· «указать цель» кликом по карте ·** | |
| 561 | `on_set_target(x, y)` | Клик ТОЛЬКО задаёт цель. Пересчёт — по кнопке «Применить». |
| | **· «задать сектор появления» кликом по краю карты ·** | |
| 573 | `on_set_sector(x, y)` | Клик задаёт ось «цель → точка»; модель отбирает по весам точки входа. |
| 587 | `on_clear_sector()` |  |
| 593 | `_render_after_input(msg)` | Показать заданное (цель, сектор, точки входа) и напомнить про «Применить». |
| | **· сброс: убрать датчики и заданную цель (карта остаётся) ·** | |
| 607 | `on_reset()` |  |
| 631 | `on_place()` |  |
| | **· параметры / режим ·** | |
| 636 | `on_apply()` |  |
| 672 | `on_mode(key)` |  |
| 677 | `on_toggle()` |  |
| 692 | `on_map_layer(key)` |  |
| 695 | `on_map_offline(flag)` |  |
| 698 | `on_reset_view()` |  |
| | **· отрисовка ·** | |
| 702 | `_render_all()` | Собрать текущее состояние модели и целиком перерисовать карту с учётом галочек |
| 787 | `on_iter_gen_frac(frac)` | Пользователь сменил долю обобщённой выборки (поле «обобщ. %»). Запоминаем и, если |
| 794 | `_refresh_generalized(toggles)` | Обновить слой ОБОБЩЁННОЙ выборки (доля задаётся полем «обобщ. %», по умолч. 10 %): |
| | **· показатели ·** | |
| 806 | `_idle_metrics()` |  |
| 823 | `_relief_lines()` | Блок показателей по рельефу. Пока рельеф не добавлен — одна строка-подсказка, |
| 845 | `_full_metrics()` |  |
| 892 | `run()` |  |


## config.py

**Отдаёт наружу:** `AREA_TRAJ_LABELS`, `MANEUVER_LAW_LABELS`, `MODES`, `MODE_LABELS`, `MOTION_LABELS`, `Params`, `SPLASH_AUTHORS`, `SPLASH_AUTHORS_LABEL`, `SPLASH_EMBLEM_FRAC`, `SPLASH_EMBLEM_TEXT`, `SPLASH_EMBLEM_TEXT_COLOR`, `SPLASH_EMBLEM_TEXT_PT`, `SPLASH_EMBLEM_TEXT_Y`, `SPLASH_FULLSCREEN`, `SPLASH_IMAGE`, `SPLASH_MAX_PX`, `SPLASH_PAD`, `SPLASH_SECONDS`, `SPLASH_TITLE`, `SPREAD_EPS`, `THEME`, `THREAT_AREA`, `THREAT_AREAS`, `THREAT_AREA_DIR`, `THREAT_AREA_PAD_KM`, `THREAT_BBOX_LONLAT`, `THREAT_BBOX_POINTS`, `THREAT_CELL_M`, `THREAT_CITY_ZERO_WEIGHT`, `THREAT_COST_MIX_BIAS`, `THREAT_COST_MIX_STEPS`, `THREAT_COST_POWER`, `THREAT_COST_RELIEF`, `THREAT_DEM_AREA_FULL_M`, `THREAT_DEM_AREA_KM`, `THREAT_DEM_AREA_S`, `THREAT_DEM_CUT_AREA_M`, `THREAT_DEM_CUT_LOCAL_M`, `THREAT_DEM_FILE`, `THREAT_DEM_FLOOR`, `THREAT_DEM_K_MAX`, `THREAT_DEM_K_MIN`, `THREAT_DEM_LOCAL_FULL_M`, `THREAT_DEM_LOCAL_KM`, `THREAT_DEM_LOCAL_S`, `THREAT_DEM_ON_START`, `THREAT_ENTRY`, `THREAT_ENTRY_FREE_KM`, `THREAT_INTERSECTION_BONUS`, `THREAT_ITER_MODE_LABELS`, `THREAT_ITER_SPREAD_LABELS`, `THREAT_LAYERS`, `THREAT_LAYERS_FILE`, `THREAT_LAYER_ORDER`, `THREAT_LMAX_AUTO_FRAC`, `THREAT_LMAX_CORRIDOR_FRAC`, `THREAT_LOOKAHEAD_KM`, `THREAT_LOOKAHEAD_POWER`, `THREAT_PLACE_CLOSE_KM`, `THREAT_PLACE_GRADE_MIN_SAMPLE`, `THREAT_PLACE_GRADE_MODE`, `THREAT_PLACE_GRADE_ON`, `THREAT_PLACE_GRADE_OUTLIER`, `THREAT_PLACE_GRADE_STRICT_UP`, `THREAT_PLACE_GRADE_TOL`, `THREAT_RIVER_CORRIDOR_MAX_KM2`, `THREAT_ROUTE_COUNT`, `THREAT_ROUTE_MODE_SRC`, `THREAT_ROUTE_POOL_MULT`, `THREAT_ROUTE_SPREAD_W`, `THREAT_SECTOR_EDGE_KM`, `THREAT_SECTOR_ENTRIES`, `THREAT_SECTOR_ENTRY_COOLDOWN`, `THREAT_SECTOR_HALF_DEG`, `THREAT_SECTOR_MIN_ANGLE_FRAC`, `THREAT_SECTOR_MIN_SEP_KM`, `THREAT_SECTOR_WIN_ACROSS`, `THREAT_SECTOR_WIN_ALONG`, `THREAT_SENSOR_REFRESH_EVERY`, `THREAT_SOURCE_FILE`, `THREAT_SPEND_LABELS`, `THREAT_TARGET`, `THREAT_TARGET_FREE_KM`, `THREAT_TOWN_CITY_KM2`, `THREAT_URBAN_BUFFER_KM`, `THREAT_URBAN_MIN_AREA_KM2`, `THREAT_VIEW_PAD_KM`, `THREAT_VILLAGE_FLOOR`, `THREAT_VILLAGE_PENALTY_CORE`, `THREAT_VILLAGE_PENALTY_EDGE`, `THREAT_VILLAGE_RADIUS_MAX_KM`, `THREAT_VILLAGE_RADIUS_MIN_KM`, `THREAT_VILLAGE_SKIP_FACTOR`, `THREAT_VILLAGE_TOWNLIKE_MED`, `THREAT_WATER_BUFFER_M`, `THREAT_WEIGHT_GAIN`, `THREAT_WORK_LONLAT`, `TRAJ_LABELS`, `WAYPOINT_ZONE_LABELS`.

| стр. | что | зачем |
|---:|---|---|
| | **· СЕКТОР ПОЯВЛЕНИЯ БПЛА (кнопка «Задать сектор», клик по краю карты) ·** | |
| | **· Населённые пункты: ГОРОД запрещён, ДЕРЕВНЯ обходится по краю ·** | |
| | **· СОБСТВЕННАЯ ГРАДАЦИЯ НАСЕЛЁННЫХ ПУНКТОВ (тип по данным, а не по тегу) ·** | |
| | **· СВОБОДНЫЕ ЗОНЫ у точки вылета и у цели ·** | |
| | **· РЕЛЬЕФ (карта высот) ·** | |
| | **· ПРЕДВИДЕНИЕ ПРЕПЯТСТВИЯ (обход холма) ·** | |
| | **· ТЯГА К ЦЕЛИ: ПО СТОИМОСТИ, А НЕ ПО КИЛОМЕТРАМ ·** | |
| | **· Построение маршрутов вход→цель по весовой карте (вкладка 3) ·** | |
| 723 | **Params** | Входные параметры модели (все редактируются в интерфейсе). |
| | **· вкладка 3 (цифровая карта угроз) ·** | |
| 803 | `A()` |  |
| 807 | `B()` |  |
| 810 | `validate()` |  |


## model/trajectories.py

| стр. | что | зачем |
|---:|---|---|
| 28 | `make_arc(A, B, sagitta, n_points)` |  |
| 49 | `polyline_length(traj)` |  |
| 56 | `arc_length_from_angle(c_len, theta_rad)` |  |
| 63 | `make_arc_by_angle(A, B, theta_deg, n_points)` | Дуга A->B по знаковому касательно-хордовому углу theta (градусы). |
| 89 | `max_deflection_angle(A, B, L_max)` | Предельный угол theta_max (градусы): длина дуги = L_max (бинарный поиск). |
| 102 | `_sample_angle(rng, theta_max, sigma, profile)` | Случайный угол отклонения по профилю разброса. |
| 118 | `sample_arc_trajectory(A, B, L_max, rng, sigma_frac, n_points, …)` | Случайная дуга по профилю. Возвращает (traj, theta_deg). |
| 130 | `arc_fan(A, B, L_max, angle_step_deg, sigma_frac, n_points, …)` | Веер вероятных дуг с шагом угла. Вес = относительная частота при профиле. |
| 165 | `_reach_ellipse_halves(A, B, L_max, n_points)` | Верхняя/нижняя половины эллипса достижимости A<->B (для манёвра). |
| 182 | `corridor_outline(A, B, L_max, traj_model, n_points)` | Верхняя и нижняя границы коридора движения: (upper, lower) — массивы точек. |
| 200 | `corridor_polygon(A, B, L_max, traj_model, n_points)` | Замкнутый многоугольник коридора движения. |
| 206 | `_points_in_polygon(pts, poly)` | Векторизованный точечно-в-многоугольнике (алгоритм лучевого пересечения). |
| 222 | `corridor_filter(pts, A, B, L_max, traj_model, n_points)` | Маска точек, попадающих в коридор движения. |
| 228 | `corridor_bbox(A, B, L_max, traj_model, n_points)` | Габариты коридора (x0, x1, y0, y1) для подгонки масштаба карты. |
| 238 | `ellipse_b(A, B, L_max)` |  |
| 244 | `make_serpentine(A, B, amplitude, n_lobes, phase, n_points)` |  |
| 257 | `max_serpentine_amplitude(ab_distance, L_max, n_lobes, n_points)` | Предельная амплитуда петли (для данного числа лепестков), при которой |
| 273 | `sample_serpentine_trajectory(A, B, L_max, rng, sigma_frac, n_points, …)` | Случайная петляющая траектория по профилю. Возвращает (traj, feature). |
| 301 | `signed_max_lateral(traj, A, B)` |  |
| 312 | `_profile_quantiles(profile, lim, sigma, n, seed)` | n квантилей величины (угла/амплитуды) по профилю разброса. |
| 323 | `_arc_density(theta, profile, theta_max, sigma)` |  |
| 331 | `frequent_arcs(A, B, L_max, profile, sigma_frac, n, …)` | 10 наиболее вероятных дуг (квантили распределения углов по профилю). |
| 343 | `frequent_serpentines(A, B, L_max, profile, sigma_frac, n, …)` | 10 представительных петель (квантили амплитуды по профилю). |
| 358 | `serpentine_fan(A, B, L_max, k, n_points)` | Веер петель: амплитуды от 0 до предельной (крайняя — пунктир). |
| 385 | `turn_radius_km(speed_ms, bank_deg)` | Минимальный радиус координированного разворота R_min = V²/(g·tan φ), км. |
| 411 | `_pick_mode(profile, rng)` | «сбалансированный» = случайный выбор режима каждую итерацию (есть и прямые). |
| 419 | `_resample_polyline(verts, n_points)` | Равномерно по длине пересэмплировать ломаную к n_points точкам. |
| 431 | `_waypoints(S0, B, L_max, rng, mode, r_min, …)` | Путевые точки. Число точек — СЛУЧАЙНО в пользовательском диапазоне nrange |
| 485 | `_round_corners(verts, r_min, turn_frac, n_points)` | Скруглить углы ВИДИМЫМИ дугами: радиус = доля отрезка (turn_frac, пологость), |
| 518 | `_waypoint_path(S0, B, L_max, rng, profile, n_points, …)` | Путь по точкам с укладкой в запас хода (уменьшение выноса до прямой). |
| 547 | `_sine_path(S0, B, L_max, rng, profile, n_points, …)` | Манёвр по СИНУСОИДАЛЬНОМУ закону: боковое отклонение = сумма гармоник |
| 587 | `maneuver_path(S0, B, L_max, rng, profile, n_points, …)` | Манёвр БПЛА. law="points" — путь по ТОЧКАМ со скруглением углов (дуги |
| 597 | `polyline_path(S0, B, L_max, rng, profile, n_points, …)` | Ломаная БПЛА: путь по точкам БЕЗ скругления (угловой манёвр; law не влияет). |
| 604 | `sample_maneuver_trajectory(A, B, L_max, rng, sigma_frac, n_points, …)` | Случайный манёвр A->B (по точкам или по синусу). Возвращает (traj, feature). |
| 612 | `sample_polyline_trajectory(A, B, L_max, rng, sigma_frac, n_points, …)` | Случайная ломаная A->B. Возвращает (traj, feature). |
| 620 | `frequent_maneuvers(A, B, L_max, profile, sigma_frac, n, …)` | n представительных манёвров/ломаных, РАВНОМЕРНО по боковому отклонению (обе |
| 636 | `maneuver_fan(A, B, L_max, profile, n_points, k, …)` | Веер манёвров/ломаных (геометрия пространства манёвров). |
| 653 | `_kmeans(X, k, iters, seed)` | Простой k-means (numpy, без внешних зависимостей). Возвращает (labels, C). |
| 672 | `representative_trajectories(trajectories, n, m, seed)` | 10 наиболее вероятных маршрутов как ОБОБЩЕНИЕ накопленной выборки. |


## view_qt/view_qt.py

| стр. | что | зачем |
|---:|---|---|
| 32 | `_qcolor(hex_or_rgba, alpha)` |  |
| 39 | `_viridis_qcolor(w)` |  |
| 44 | `_heat_cmap()` |  |
| 53 | **_SpeedAdapter** | Совместимость с контроллером: тот читает view.slider_speed.val. |
| 55 | `__init__(slider)` |  |
| 59 | `val()` |  |
| 63 | **SimulationView** | Qt-представление. Сигнатуры публичных методов совпадают с matplotlib-View. |
| 66 | `__init__(A, B, outline, bbox, params, speed, …)` |  |
| 98 | `_apply_stylesheet()` | Оформление — общее для трёх вкладок (`view_qt/ui_common.py`). |
| 102 | `_build_ui(params, speed, speed_max)` |  |
| 199 | `_header(text)` |  |
| 203 | `_build_law_combo(col, params)` | Выпадающий список «Закон манёвра»: по точкам / по синусу. |
| 219 | `_radio_group(_title, labels_map, active_key, slot)` |  |
| 233 | `_wrap_group(title, _labels_map, grp)` |  |
| 242 | `_active_key(grp)` |  |
| 248 | `_tint(btn, color)` |  |
| 257 | `_build_scene_items()` |  |
| 316 | `_set_flight_visible(vis)` |  |
| 320 | `_clear_list(items)` |  |
| 328 | `get_param_specs()` |  |
| 331 | `set_callbacks(**cbs)` |  |
| 335 | `get_toggles()` |  |
| 340 | `set_running_label(running)` |  |
| 343 | `get_param_values()` |  |
| 350 | `set_param_values(params)` |  |
| 358 | `_on_field_submit()` |  |
| 362 | `_mode_changed(key)` |  |
| 365 | `_traj_changed(key)` |  |
| 368 | `_prof_changed(key)` |  |
| 371 | `update_geometry(A, B, outline, bbox)` |  |
| 384 | `_apply_range(force)` | Подогнать масштаб под текущий bbox (force=True — даже если bbox не менялся). |
| 393 | `showEvent(e)` |  |
| 398 | `resizeEvent(e)` |  |
| | **· слои ·** | |
| 403 | `_render_density(density, show)` |  |
| 413 | `_update_legend(freq, fan, t, flight)` |  |
| 423 | `_render_paths(paths, show)` |  |
| 439 | `_render_sensors(sensors, R)` |  |
| 457 | `_render_static(sensors, R, freq, fan, density, t)` |  |
| | **· кадры ·** | |
| 467 | `draw_clear(freq, fan, density, toggles, title)` |  |
| 474 | `draw_static_frame(sensors, R, freq, fan, density, toggles, …)` |  |
| 479 | `setup_flight(traj, sensors, R, freq, fan, density, …)` |  |
| 492 | `update_flight(j, hud)` |  |
| 501 | `set_metrics(lines)` |  |
| 504 | `flash_title(text, color)` |  |
| 507 | `_set_title(text, color)` |  |
| 510 | `process_pending()` |  |
| 513 | `show()` |  |


## view_qt/geomap.py

| стр. | что | зачем |
|---:|---|---|
| 44 | `km_to_lonlat(x_km, y_km, lon0, lat0)` | Локальные км (восток, север от якоря) -> (долгота, широта). |
| 49 | `lonlat_to_km(lon, lat, lon0, lat0)` | (долгота, широта) -> локальные км (восток, север от якоря). |
| 57 | `deg2num(lon, lat, z)` | (долгота, широта, зум) -> дробные тайловые координаты (xt, yt). |
| 66 | `num2deg(xt, yt, z)` | Дробные тайловые координаты -> (долгота, широта) угла тайла. |
| 115 | `discover_local_layers()` | Папки в кэше, не входящие в TILE_LAYERS (например, ваша 'Sat' с тайлами |
| 143 | `cache_root()` | Каталог дискового кэша тайлов. |
| 172 | `_area_dir(area)` | Имя папки участка: явный аргумент, иначе UAV_TILE_AREA, иначе '' (старая |
| 179 | `_tms_y(z, y)` | XYZ <-> TMS: ось Y инвертирована (TMS считает строки снизу вверх). |
| 184 | `tile_cache_path(layer, z, x, y, ext, area)` | Путь тайла в кэше. y уже в РАСКЛАДКЕ КЭША (если слой TMS — см. _cache_y). |
| 192 | `_cache_y(layer, z, y, layer_tms)` | y из XYZ (как считает вся остальная математика) -> y на диске для этого |
| 201 | `find_cached_tile(layer, z, x, y, area)` | Найти файл тайла в кэше, перебирая известные расширения и XYZ/TMS. |
| 218 | `_sniff_ext(data)` | Расширение по магическим байтам (для корректного имени файла в кэше). |
| 229 | `_decode_tile(data)` | Байты тайла (PNG или JPEG) -> RGBA float [0..1] (H,W,4). |
| 249 | `fetch_tile_bytes(layer, z, x, y, allow_net, timeout, …)` | Байты тайла: сперва оффлайн-кэш (перебор расширений + XYZ/TMS для слоя), |
| 296 | `_decoded_tile_u8(layer, z, x, y, allow_net, area)` | Тайл как uint8 RGBA (256,256,4) из памяти/диска/сети, либо None. |
| 323 | `_pick_zoom(lon_min, lon_max, zmin, zmax, target_px, min_tiles_x)` | Зум, при котором тайлы отображаются ~1:1 к экрану (родной размер подписей). |
| 354 | `build_raster_basemap(kx0, kx1, ky0, ky1, layer, allow_net, …)` | Собрать подложку для км-окна [kx0,kx1]×[ky0,ky1]. |
| 442 | `oblast_outline_km(lon0, lat0)` | Прямоугольная рамка охвата области в км (замкнутый контур, для ориентира). |
| 453 | `bbox_lonlat_to_km(bbox_lonlat, lon0, lat0)` | (lon_min, lat_min, lon_max, lat_max) -> (kx0, kx1, ky0, ky1) в локальных км |
| 463 | `graticule_km(kx0, kx1, ky0, ky1, lon0, lat0, …)` | Линии сетки широт/долгот в км для текущего окна. Возвращает список |


## tools/переносимое/codemap.py

| стр. | что | зачем |
|---:|---|---|
| 60 | `find_root(start)` |  |
| 71 | `py_files(root)` |  |
| 79 | `_first_doc_line(node)` |  |
| 85 | `_sig(node)` |  |
| 95 | `scan_file(path)` | [(строка, вид, подпись, назначение)] + список якорей разделов. |
| 120 | `module_name(root, path)` |  |
| 125 | `public_names(root, files)` | {модуль: {имя: [кто импортирует]}} — что реально берут снаружи. |
| 142 | `git_counts(root, path, since_rev)` | Сколько коммитов трогали путь (весь, либо после ревизии). |
| 154 | `last_commit(root, path)` |  |
| 164 | `module_block(root, name, items, anchors, err, pub)` | Раздел карты по одному модулю. |
| 184 | `build(root)` | {имя файла: текст} — README-указатель и части КАРТА_N.md. |
| | **· разложить модули по частям ·** | |
| | **· README: краткое описание и характеристики ·** | |
| 284 | `uncommitted(root)` | Файлы, изменённые в рабочем дереве (правка есть, коммита ещё нет). |
| 319 | `verify_offsets(out_files)` | Сверить строки из README с содержимым частей: на указанной строке обязан стоять |
| 342 | `stale_report(root, docs_dir)` | Документы против кода, КОТОРЫЙ ОНИ САМИ УПОМИНАЮТ. |
| 384 | `main(argv)` |  |


## view/view.py

**Отдаёт наружу:** `PARAM_SPECS`.

| стр. | что | зачем |
|---:|---|---|
| 39 | `fit_axes(ax, bbox, margin)` |  |
| 53 | `draw_static(ax, A, B, outline, bbox)` |  |
| 74 | `draw_density(ax, H, extent)` | Тепловая карта плотности. Слабые значения подняты гаммой и яркой палитрой, |
| 84 | `draw_probable(ax, paths, show_labels)` | Линии маршрутов: вес -> цвет/толщина; предельные (is_extreme) -> пунктир. |
| 102 | `draw_sensors(ax, sensors, R)` |  |
| 112 | **SimulationView** |  |
| 113 | `__init__(A, B, outline, bbox, params, speed, …)` |  |
| 145 | `_on_resize(_event)` |  |
| | **· легенда тепловой карты ·** | |
| 149 | `_build_colorbar()` |  |
| | **· панель ·** | |
| 165 | `_panel_bg()` |  |
| 171 | `_header(x, y, text)` |  |
| 175 | `_labeled_box(x, y, w, h, name, label, …)` |  |
| 186 | `_style_button(btn, color)` |  |
| 191 | `_style_radio(radio)` |  |
| 197 | `_build_controls(params, speed, speed_max)` |  |
| 271 | `_mode_clicked(label)` |  |
| 276 | `_traj_clicked(label)` |  |
| 281 | `_prof_clicked(label)` |  |
| | **· API для контроллера ·** | |
| 287 | `get_param_specs()` |  |
| 290 | `set_callbacks(**cbs)` |  |
| 294 | `get_toggles()` |  |
| 298 | `set_running_label(running)` |  |
| 302 | `get_param_values()` |  |
| 309 | `set_param_values(params)` |  |
| 317 | `update_geometry(A, B, outline, bbox)` |  |
| | **· полная отрисовка ·** | |
| 322 | `_compose(sensors, R, freq, fan, density, t, …)` |  |
| 342 | `draw_clear(freq, fan, density, toggles, title)` |  |
| 349 | `draw_static_frame(sensors, R, freq, fan, density, toggles, …)` |  |
| | **· быстрая анимация (блиттинг) ·** | |
| 358 | `setup_flight(traj, sensors, R, freq, fan, density, …)` |  |
| 379 | `update_flight(j, hud)` |  |
| 397 | `set_metrics(lines)` |  |
| 401 | `process_pending()` | Принудительно отрисовать отложенные изменения (общий метод с Qt-View). |
| 409 | `flash_title(text, color)` |  |
| 413 | `show()` |  |


## model/area_start.py

**Отдаёт наружу:** `AreaStartModel`.

| стр. | что | зачем |
|---:|---|---|
| 35 | `_frame(A, B)` | Орт оси A->B (u) и нормали (n). |
| 45 | `start_zone_outline(A, B, depth, width, n)` | Контур зоны старта (эллипс с центром в A): глубина вдоль оси, ширина поперёк. |
| 54 | `reach_ellipse_outline(A, B, L_max, n)` | Граница эллипса достижимости A<->B (предел движения по запасу хода). |
| 64 | `reach_envelope_outline(A, B, depth, width, L_max, n_ang)` | Огибающая ДОСТИЖИМОСТИ с учётом ЗОНЫ СТАРТА: объединение эллипсов с фокусами |
| 89 | `sample_start_point(A, B, depth, width, rng)` | Случайная точка ВНУТРИ зоны старта (равномерно в эллипсе). |
| 99 | `_zone_exit_frac(S0, A, B, depth, width)` | Доля пути S0->B, ЗА которой путевые точки гарантированно ВНЕ ЗОНЫ СТАРТА. |
| 122 | `points_outside_start_zone(pts, A, B, depth, width)` | Маска точек ВНЕ зоны старта (эллипс с центром A): True = снаружи. |
| 139 | `sample_area_arc(A, B, depth, width, L_max, rng, …)` | Дуга из случайной точки старта в B (угол по профилю разброса). |
| 151 | `sample_maneuver(A, B, depth, width, L_max, rng, …)` | Манёвр из СЛУЧАЙНОЙ точки старта зоны в B (по точкам или по синусу). |
| 162 | `sample_polyline(A, B, depth, width, L_max, rng, …)` | Ломаная из СЛУЧАЙНОЙ точки старта зоны в B (общее ядро polyline_path). |
| 175 | **AreaStartModel** | Размещение датчиков при старте из зоны и движении к цели B (вкладка 2). |
| 181 | `__init__(params)` |  |
| | **· настройка ·** | |
| 201 | `set_movement(movement)` |  |
| 204 | `set_route(A, B)` | Зафиксировать маршрут (центр зоны старта A и цель B) и сбросить выборку. |
| | **· геометрия ·** | |
| 215 | `corridor_outline()` |  |
| 218 | `corridor_bbox()` |  |
| 221 | `view_bbox()` |  |
| 224 | `start_zone()` |  |
| 228 | `_reachable_mask(P)` | Точки, достижимые из зоны старта при движении в B за <= L_max. |
| 242 | `_build_candidates()` |  |
| 253 | `reset(seed)` |  |
| 288 | `_compute_view_bbox(n, margin)` |  |
| | **· порождение и накопление ·** | |
| 301 | `_sample(rng)` |  |
| 317 | `sample_trajectory()` |  |
| 320 | `_anchor_idx()` | Индекс кандидата для «якорного» датчика у цели B (1/3 R заходит за B). |
| | **· слои ·** | |
| 333 | `frequent_paths(n)` | 10 наиболее вероятных маршрутов. |
| 352 | `fan_paths()` |  |
| 363 | `density_field(nbins)` |  |
| | **· показатели ·** | |
| 376 | `evaluate(sensors, trajectories)` |  |


## main_qt.py

| стр. | что | зачем |
|---:|---|---|
| 21 | `_ensure_qt_plugin_path()` | Указать Qt путь к платформенным плагинам PyQt5. |
| 44 | `_resource_path(rel)` | Путь к файлу-ресурсу и в обычном запуске, и в собранном .exe. |
| 58 | `_make_splash(QtWidgets, QtGui, QtCore, screen_size)` | Заставка на `SPLASH_SECONDS` секунд. None, если картинки нет. |
| 177 | `selftest()` | Самопроверка данных: `UAV_SELFTEST=1` перед запуском — программа печатает, что |
| 254 | `main()` |  |


## view_qt/area_view.py

**Отдаёт наружу:** `AreaStartView`.

| стр. | что | зачем |
|---:|---|---|
| 30 | **AreaStartView** |  |
| 32 | `__init__(A, B, outline, bbox, params, speed, …)` |  |
| 47 | `get_param_specs()` |  |
| 50 | `_move_changed(key)` |  |
| 53 | `_build_zone_combo(col, params)` | Выпадающий список «Точки маршрута»: в зоне старта / только за зоной. |
| 69 | `_build_map_combo(col, params)` | Выпадающий список «Карта»: схема (оффлайн) / OSM / топо / спутник / тёмная |
| 100 | `_map_changed(i)` |  |
| 105 | `_map_offline_changed(_state)` |  |
| | **· панель вкладки 2 ·** | |
| 111 | `_build_ui(params, speed, speed_max)` |  |
| | **· сцена: карта-подложка (общий BasemapMixin), зона старта, ось, клик ·** | |
| 206 | `_build_scene_items()` |  |
| 229 | `_draw_scheme_extra(kx0, kx1, ky0, ky1)` |  |
| 233 | `_set_scheme_extra_visible(vis)` |  |
| 236 | `_mark_ab(pts, kinds)` | Красивые маркеры точек: A — зелёный кружок (центр зоны), B — красная |
| 247 | `update_geometry(A, B, outline, bbox)` |  |
| 254 | `_set_kursk_view(half)` | Начальный вид: Курск в центре, окно ~±half км. |
| 258 | `showEvent(e)` |  |
| | **· постановка маршрута кликами ·** | |
| 268 | `begin_create()` |  |
| 273 | `prompt_create()` |  |
| 288 | `_on_scene_click(ev)` |  |

