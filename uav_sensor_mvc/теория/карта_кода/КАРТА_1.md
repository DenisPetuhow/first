<!-- КАРТА КОДА · сгенерировано tools/переносимое/codemap.py — руками не править -->

# Карта кода — часть 1 из 1

> Указатель со сводкой — [README.md](README.md). Здесь: `view_qt/threat_view.py`, `model/threat_grid.py`, `controller_qt/threat_controller.py`, `model/threat_routes.py`, `tools/gui_check.py`, `tools/переносимое/make_figures.py`, `view_qt/input_window.py`, `config.py`, `view_qt/map_image.py`, `model/trajectories.py`, `model/map_fit.py`, `view_qt/geomap.py`, `view_qt/view_qt.py`, `tools/переносимое/codemap.py`, `view/view.py`, `tools/reference_cases.py`, `tools/переносимое/docmap.py`, `model/area_start.py`, `tools/pack_area.py`, `view_qt/basemap_mixin.py`, `main_qt.py`, `tools/flow_check.py`, `view_qt/tile_layer.py`, `model/optimization.py`, `view_qt/area_view.py`.

> Найти строку → открыть файл `Read` с `offset`. Файл целиком не читать.


## view_qt/threat_view.py

**Отдаёт наружу:** `LAYER_STYLE`, `ThreatMapView`.

| стр. | что | зачем |
|---:|---|---|
| 307 | `_hillshade(h, az_deg, alt_deg, z, lo, hi)` | Теневая отмывка карты высот: множитель яркости по наклону поверхности к источнику |
| 325 | **ShiftedAxis** | Ось, у которой ноль можно перенести в произвольную точку (план 8, задача 8.2). |
| 338 | `__init__(orientation, **kwargs)` |  |
| 342 | `set_origin(value)` |  |
| 347 | `tickStrings(values, scale, spacing)` |  |
| 354 | `_threat_cmap()` |  |
| 363 | `_threat_lut()` |  |
| 369 | `_iter_heat_lut()` | LUT для 2-й карты (частота пролёта БПЛА) — magma, отличается от turbo весов. |
| 375 | `_iter_heat_cmap()` | Та же magma, но как ColorMap — для цветовой шкалы у правого края. |
| 384 | **ThreatMapView** |  |
| 407 | `__init__(model_bbox_km, lon0, lat0, params, area_max_km)` |  |
| 553 | `_on_view_settled()` | Вид перестал двигаться: подтянуть детализацию своей карты и слои. |
| | **· режим «указать цель» (клик по карте) ·** | |
| 562 | `_begin_target()` |  |
| | **· режим «задать сектор появления» (клик по краю карты) ·** | |
| 569 | `_begin_sector()` |  |
| 576 | `_show_click_coords(ev)` | Координаты точки клика — в строку под картой (план 8, задача 8.3). |
| | **· режим «ДОБАВИТЬ ДАТЧИК ПО КЛИКУ» (задача 8.7) ·** | |
| 592 | `_begin_pick_sensor()` | Включить или выключить постановку датчиков мышью (кнопка-переключатель). |
| 613 | `_lock_panel_for_pick(lock)` | Погасить панель на время постановки датчиков мышью. |
| 637 | `_begin_edit_sensor()` | Включить или выключить правку датчиков мышью (кнопка-переключатель). |
| 659 | `_lock_panel_for_edit(lock)` | Погасить панель на время правки — та же причина, что и при постановке: |
| 672 | `_end_edit_sensor()` |  |
| 681 | `_drop_edit_handle()` | Убрать метку выбранного датчика с карты. |
| 692 | `_edit_sensor_click(ev)` | Щелчок в режиме правки: выбрать ближайший датчик и дать его тащить. |
| 714 | `_km_per_pixel()` | Сколько километров в одной точке экрана при текущем масштабе. |
| 723 | `_show_edit_handle(row)` | Показать подвижную метку на выбранном датчике. |
| 742 | `_edit_handle_moved()` | Метку отпустили — сообщить контроллеру новую позицию датчика. |
| 756 | `_delete_selected_sensor()` | DELETE в режиме правки — убрать выбранный датчик. |
| 768 | `_right_button_exit()` | ПРАВАЯ КНОПКА В РЕЖИМАХ МЫШИ — завершить режим (заказчик 05.09.2026). |
| 785 | `_grab_right_button(grab)` | Забрать правую кнопку себе (или вернуть её pyqtgraph). |
| 792 | `_inside_work_area(x_km, y_km)` | Точка внутри рабочего района (синей рамки)? |
| 803 | `_ask_clear_sensors()` | Кнопка «Очистить датчики»: убрать С КАРТЫ ВСЕ датчики — и заданные вручную, |
| | **· ИСТОРИЯ ПОЛЁТОВ: выгрузка/загрузка (задача 8.6) ·** | |
| 825 | `_save_flights()` |  |
| 834 | `_load_flights()` |  |
| 842 | `_clear_flights()` |  |
| | **· окно «Посмотреть маршруты» (задача 8.6, довесок 06.09.2026) ·** | |
| 846 | `_open_route_viewer()` | Открыть окно списком/таблицей маршрутов. Создаётся один раз, дальше только |
| | **· окно «Анализ моделирования» (план 8, задача 8.9, довесок 06.09.2026) ·** | |
| 864 | `_open_analysis_window()` | Открыть/обновить окно полной статистики — снимок на момент нажатия кнопки. |
| 874 | `refresh_route_viewer()` | Подтянуть в открытое окно «Посмотреть маршруты» свежие данные — вызывается |
| 885 | `_end_pick_sensor()` |  |
| 893 | `_pick_sensor_click(ev)` | Двойной клик в режиме постановки: спросить тип и режим, добавить датчик. |
| 925 | `_on_scene_click(ev)` |  |
| | **· режим «ЗАДАТЬ РАЙОН МОДЕЛИРОВАНИЯ» (план 8, задача 8.2) ·** | |
| 982 | `_begin_area()` |  |
| 990 | `_end_area(apply_it)` |  |
| 1009 | `_area_click(ev)` |  |
| 1035 | `_clear_area()` | Кнопка «Убрать» рядом с районом — сброс к состоянию «район не задан». |
| 1040 | `_draw_area_preview()` | Прямоугольник, который пользователь тянет прямо сейчас. |
| | **· режим рисования ЗАПРЕТНОЙ ЗОНЫ ·** | |
| 1052 | `_begin_zone()` | Включить рисование: клики ставят вершины, нажатие колёсика замыкает зону. |
| 1060 | `_end_zone(apply_it)` | Закончить рисование. apply_it=False — бросить начатое, ничего не меняя. |
| 1070 | `_zone_click(ev)` | Клик в режиме рисования: левая кнопка — вершина, КОЛЁСИКО — замкнуть зону. |
| 1089 | `keyPressEvent(ev)` | Esc — бросить начатое рисование (зоны, района) или выйти из режима мыши. |
| | **· ПАНЕЛЬ РЕЖИМОВ (задел на будущее, заказчик 06.09.2026) ·** | |
| 1121 | `_build_mode_bar()` |  |
| 1162 | `eventFilter(obj, event)` |  |
| 1167 | `_position_mode_bar()` |  |
| 1180 | `_toggle_measure()` |  |
| 1186 | `_begin_measure()` |  |
| 1204 | `_end_measure()` |  |
| 1214 | `_measure_click(ev)` |  |
| 1237 | `_draw_measure()` | Перерисовать линейку целиком: ломаную, точки и подпись на каждом отрезке. |
| 1273 | `_draw_zone_preview()` | Показать зону, которую пользователь рисует прямо сейчас. |
| 1291 | `_hatch_polygon(poly, step_km, angle_deg)` | Диагональные штрихи ВНУТРИ многоугольника: списки x, y с разрывами (NaN). |
| 1325 | `render_no_fly(polys, toggles)` | Отрисовать УЖЕ ЗАДАННЫЕ запретные зоны (список полигонов в км). |
| | **· окно выбора цифровых карт (источник + слои) ·** | |
| 1349 | `_open_data_dialog()` |  |
| 1359 | `apply_digital_maps(path, enabled)` |  |
| | **· своя карта картинкой (план 8, задача 8.5) ·** | |
| 1366 | `_open_map_image_dialog()` | Окно «Своя карта». Не модальное: координаты углов сверяют по самой карте. |
| 1376 | `_fit_map_image(path, bbox)` | Подобрать привязку картинки по слоям участка -> (опорные точки, отчёт). |
| 1417 | `_apply_map_image(path, lon_min, lat_min, lon_max, lat_max, hide_tiles, …)` | Показать картинку по координатам углов. Возвращает строку для окна. |
| 1510 | `_apply_map_vector(path, lon_min, lat_min, lon_max, lat_max, hide_tiles, …)` | Показать ВЕКТОРНУЮ карту (SVG) по той же привязке, что и растровую. |
| 1577 | `_clear_map_vector()` | Снять векторную карту со сцены (элемент нельзя переиспользовать). |
| 1587 | `_refresh_map_detail()` | Показать видимый кусок своей карты в ПОЛНОМ разрешении. |
| 1640 | `_clear_map_image()` | Убрать картинку и вернуть тайловую подложку. |
| | **· окно «Исходные данные» (не блокирует программу) ·** | |
| 1653 | `_open_input_window()` | Открыть окно «Исходные данные». Создаётся один раз и живёт до конца сеанса: |
| 1678 | `_area_info_text()` |  |
| 1689 | `_pick_tile_root(dlg, reset)` |  |
| 1695 | `refresh_sensors_dialog()` | Обновить поля окна «Исходные данные» (шаг сетки мог пересчитаться под радиус). |
| 1703 | `set_ab_distance(km)` |  |
| 1707 | `refresh_input_dialog()` | Обновить поля окна под текущие параметры (авто-L_max и пр.). |
| 1711 | `set_manual_count(n)` | Сколько датчиков задано вручную — для кнопки «Очистить датчики». |
| 1715 | `refresh_sensor_table(rows)` | Показать в таблице окна текущие датчики. |
| 1727 | `set_iter_checked(on)` | Включить/выключить чекбокс «итерации» без повторного запуска расчёта. |
| 1733 | `set_loaded_routes_checked(on)` | Включить/выключить чекбокс «загруженная выборка» (задача 8.6) без повторного |
| | **· опорные точки-ориентиры для схемы/подписей ·** | |
| 1741 | `_orient_points()` |  |
| 1747 | `set_callbacks(**cbs)` |  |
| 1756 | `_legend_html(scale)` | HTML-легенда (что каким цветом). ВСЕ цвета берутся из палитр THREAT_COLORS и |
| 1824 | `_relief_legend_html(scale)` | Легенда режима «приоритет по высоте»: три цвета — три строки, без пояснений. |
| 1846 | `_apply_stylesheet()` | Оформление — общее для трёх вкладок (`view_qt/ui_common.py`). |
| 1850 | `_header(text)` |  |
| 1854 | `_tint(btn, color)` |  |
| 1860 | `_build_ui(params)` |  |
| 2359 | `_build_layer_box(col)` | Блок «ПОКАЗ»: слои сгруппированы по смыслу и разложены в две колонки. |
| 2433 | `_opacity_items(attrs)` | Развернуть цели в реальные элементы сцены (включая словарные слои). |
| 2447 | `_add_group_opacity(col, items)` | Ползунок прозрачности на группу — гасит всё, что эта группа рисует. |
| 2462 | `_set_group_opacity(attrs, value)` | Прозрачность слоёв-заливок. Модель не трогаем — это чистая отрисовка. |
| 2478 | `_layer_toggled()` | Слой переключили. Во время группового переключения молчим — перерисуем один |
| 2488 | `_apply_axis_origin()` | Перенести ноль осей: в угол района или в угол всей области. |
| 2503 | `_toggle_group(kids, on)` | Переключатель группы: гасит/зажигает детей одной перерисовкой. |
| 2514 | `_sync_master(master, kids)` | Шапка группы показывает, включён ли хоть один её слой. |
| 2522 | `set_layer_enabled(**state)` | Погасить слои, которых ещё нет: «галка стоит, а на карте ничего» — это вопрос |
| 2553 | `_build_map_combo(col, params)` |  |
| 2573 | `_map_changed(i)` |  |
| 2578 | `_offline_changed(_s)` |  |
| 2583 | `_iter_mode_changed(i)` |  |
| 2586 | `_gen_frac_changed()` |  |
| 2589 | `get_iter_gen_frac()` | Доля обобщённой выборки как число 0..1 (поле задаётся в процентах). При ошибке — |
| 2598 | `_spread_changed(i)` |  |
| 2601 | `_spend_changed(i)` |  |
| 2605 | `_build_scene_items()` | Создать ВСЕ постоянные слои-элементы карты один раз (потом только меняем данные, |
| 2863 | `get_toggles()` |  |
| 2886 | `get_param_values()` | Параметры, набранные В ПАНЕЛИ. С 04.09.2026 их там нет: все поля переехали |
| 2895 | `set_param_values(params)` | Показать текущие параметры. Показывать их теперь негде, кроме окна |
| 2901 | `_work_buttons()` | Кнопки, которым нужен заданный РАЙОН МОДЕЛИРОВАНИЯ (план 8, задача 8.2). |
| 2908 | `_panel_widgets()` | ВСЕ управляющие элементы правой панели: кнопки, галочки, поля, списки. |
| 2920 | `set_busy(busy)` | Заблокировать кнопки действий на время фонового расчёта (чтобы не запускать |
| 2933 | `set_relief_shown(on)` | Включить/выключить показ рельефа вместе с самим рельефом. |
| 2948 | `set_area_rect(bbox_km)` | Перерисовать СИНЮЮ рамку по текущему району и перенести на него ноль осей. |
| 2965 | `set_area_ready(ready, size_km, bbox_km)` | Район задан или нет: от этого зависит вся остальная панель. |
| 2993 | `set_source(text)` |  |
| 2996 | `set_metrics(lines)` |  |
| 2999 | `flash_title(text, color)` |  |
| 3002 | `set_title(text)` |  |
| 3012 | `_view_aspect()` |  |
| 3023 | `_fit_frame(box, aspect)` |  |
| 3038 | `_apply_view_limits(extra_km, only_extra)` |  |
| 3068 | `frame_bbox(whole_area)` | Вписать в окно район (по умолчанию) или ВСЮ область показа. |
| 3082 | `process_pending()` |  |
| | **· отрисовка данных карты ·** | |
| 3097 | `_polys_to_xy(polys, view_box)` | Список ломаных -> массивы X,Y с NaN-разрывами, с ПРОРЕЖИВАНИЕМ (иначе |
| 3143 | `_view_box_km()` | Видимая область в км (x0,x1,y0,y1) — по ней прореживаются слои. |
| 3151 | `_zoomed_in(view_box)` | Приблизились ли настолько, что застройку пора рисовать НАСТОЯЩИМИ контурами |
| 3160 | `render_layers(layers, bridge_pts, toggles, built_mask, extent)` | Отрисовать векторные слои. Чекбокс «векторные слои» ПОЛНОСТЬЮ скрывает их |
| | **· НАЗВАНИЯ НАСЕЛЁННЫХ ПУНКТОВ ·** | |
| 3213 | `_render_place_labels(layers, toggles)` | Подписать населённые пункты своим шрифтом. Имена приходят слоем `place_names` |
| 3254 | `_label_rank(code)` | Ранг пункта ДЛЯ ПОДПИСИ (не то же, что код типа в расчёте). |
| 3265 | `_set_place_labels(items)` | Обновить пул текстовых элементов. Пул переиспользуется: создавать и удалять |
| 3291 | `_note_scale_layer(key, on)` | Запомнить, какой слой включили последним (по нему выбирается шкала). |
| 3300 | `_update_scale()` | Показать шкалу того слоя, который включён последним. Ни одного — скрыть. |
| 3320 | `_set_casing(name, xs, ys, visible)` | Обводке — те же данные, что и самой линии (она рисуется под ней). |
| 3328 | `apply_vector_visibility()` | Показать/скрыть отдельные векторные слои ПО СОХРАНЁННЫМ данным. |
| 3352 | `_open_vector_dialog()` | Окно со списком векторных слоёв. Не модальное: с ним можно работать, глядя |
| 3363 | `set_vector_visible(name, on)` | Переключить один слой (вызывается из окна) и сразу перерисовать. |
| 3373 | `vector_visible()` |  |
| 3376 | `reposition_legends()` | Прижать обе легенды к углам ТЕКУЩЕГО вида: общую — влево вниз, легенду |
| 3412 | `_scale_markers(scale)` |  |
| 3427 | `_scale_zone_widths(k)` | Толщина линий запретной зоны: на общем виде полная, при максимальном |
| 3459 | `_scale_line_widths(k)` | Подогнать толщину линий и кантов под масштаб (k = 1 вблизи, меньше — дальше). |
| 3477 | `_render_builtup_raster(mask, extent)` | Застройка растровой маской (общий вид). None — скрыть слой. |
| 3492 | `render_threat(weight, extent, toggles)` |  |
| 3506 | `render_relief(dem, extent, toggles)` | КАРТА ВЫСОТ: привычная топография (низины зелёные → вершины светлые) со |
| 3539 | `render_relief_priority(k, cut, extent, toggles)` | ПРИОРИТЕТ ПО ВЫСОТЕ: что рельеф сделал с весом. Синим — укрытия (множитель > 1), |
| 3574 | `set_relief_button(active, enabled)` | Состояние кнопки рельефа: подпись «Добавить/Убрать», подсветка и доступность |
| 3586 | `render_exclusions(water_mask, urban_mask, extent, toggles)` | Показать зоны-исключения: вода (синий — запрет датчика) и населённые пункты |
| 3602 | `render_routes(routes, route_area, extent, toggles)` | ВСЕ возможные места пролёта: розовый ФОН = огибающая (route_area) — КОРРЕКТНО |
| 3634 | `render_iter_routes(routes, toggles)` | Показать НАКОПЛЕННЫЕ итерационные маршруты (статично). Пошаговую анимацию полёта |
| 3640 | `render_generalized(routes, toggles)` | ОБОБЩЁННАЯ выборка (~10 % итераций): частые, но распределённые маршруты — |
| 3655 | `render_route_preview(route_km)` | Подсветить ОДИН маршрут (км, как у `iter_show_accumulated`) или снять подсветку |
| 3667 | `render_loaded_routes(routes, toggles)` | ЗАГРУЖЕННАЯ ИЗ ФАЙЛА выборка пролётов (задача 8.6) — жёлтым пунктиром, своим |
| 3683 | `render_iter_heat(density, extent, toggles)` | 2-я тепловая карта — частота пролёта БПЛА (плотность маршрутов), LUT magma. |
| 3700 | `iter_show_accumulated(routes)` | Отрисовать все накопленные маршруты тонкими линиями (одним item, через NaN). |
| 3713 | `iter_setup_flight(route)` | Начать анимацию полёта по маршруту: текущий путь и маркер БПЛА — в старте. |
| 3719 | `iter_update_flight(route, j)` | Прорисовать текущий маршрут до точки j и передвинуть маркер БПЛА в неё. |
| 3725 | `iter_clear_current()` | Убрать текущий (летящий) маршрут и маркер БПЛА (накопленные остаются). |
| 3730 | `get_iter_T()` | Число итераций (T) из поля ввода; при ошибке — прежнее из params. |
| 3737 | `set_iter_running(running)` | Текст кнопки «Пуск»/«Пауза» (как во вкладке 2). |
| 3741 | `render_crossings(crossings, toggles)` | Пересечения (перекрёстки дорог/рек/ЛЭП) — узлы развилок. Скрыто по умолчанию. |
| 3749 | `render_candidates(cand, toggles)` |  |
| 3757 | `_sensors_hidden()` | Галочка «Датчики» снята? (до построения панели считаем, что показываем). |
| 3766 | `render_sensors(sensors, R, sensors_big, R_big, types, static, …)` | Нарисовать датчики ЧЕТЫРЁХ типов: у каждого — КОЛЬЦО зоны обзора (залитый |
| 3875 | `render_sensor_track(matches, type_radii)` | «Анализ размещения» (задача 8.9): серым — позиции и радиусы ПЕРВОГО |
| 3947 | `_track_arrow_head(tip, direction)` |  |
| 3963 | `render_sector_compass(stats, target_xy)` | Сектора «анализа моделирования» по направлениям (довесок 06.09.2026): |
| 3996 | `render_entry_target(entry, target)` |  |
| 4000 | `render_sector(sector, entry_points, toggles)` | Сектор появления БПЛА: `sector` = (вершина-цель, край, край) или None, |
| 4028 | `showEvent(e)` |  |
| 4041 | `resizeEvent(e)` |  |
| 4050 | **VectorLayersDialog** | Окно «Векторные слои»: какие именно ориентиры показывать на карте. |
| 4063 | `__init__(view)` |  |
| 4113 | `sync()` | Подтянуть галки из представления (окно открыли повторно или сменили общий |
| 4126 | `_master_toggled(on)` |  |
| 4130 | `_update_enabled()` |  |
| 4135 | `_set_all(on)` |  |
| 4144 | **DigitalMapsDialog** | Окно «Цифровые карты»: источник данных и набор слоёв — для ПОКАЗА и для РАСЧЁТА. |
| 4156 | `__init__(view)` |  |
| 4229 | `sync()` | Подтянуть галки из представления: они же — видимость слоёв на карте. |
| 4239 | `result_choices()` | (data_path\|None, enabled_layers\|None). Все отмечены -> None, то есть «все слои». |
| 4246 | `_toggled(name, on)` |  |
| 4251 | `_set_all(on)` |  |
| 4260 | `_apply()` |  |
| 4266 | `_update_state()` | Строка состояния: сколько слоёв в расчёте и не расходится ли с применённым. |
| 4281 | `_src_text()` |  |
| 4285 | `_pick_file()` |  |
| 4294 | `_use_auto()` |  |


## model/threat_grid.py

**Отдаёт наружу:** `OSM_LAYER_FILTERS`, `PLACE_LAYER`, `PLACE_NAME_LAYER`, `PLACE_POLY_LAYER`, `ThreatModel`, `_label8`, `_load_layers_npz`, `area_cache_dir`, `area_file`, `bbox_anchor_lonlat`, `bbox_lonlat_to_km`, `dem_files`, `geo_cache_root`, `layers_cache_path`, `layers_from_osm`, `load_layers`, `packed_npz_arrays`.

| стр. | что | зачем |
|---:|---|---|
| 94 | `bbox_anchor_lonlat()` | Якорь локального км-фрейма вкладки 3 = ЮГО-ЗАПАДНЫЙ УГОЛ участка. |
| 114 | `geo_cache_root()` | Каталог кэша ГЕОДАННЫХ (векторные слои весовой карты) — по аналогии с |
| 124 | `area_cache_dir()` | Папка АКТИВНОГО УЧАСТКА внутри кэша: `geo_cache/<THREAT_AREA_DIR>`. |
| 131 | `area_file(name)` | Путь к файлу участка по имени. Ищется СНАЧАЛА в папке участка, затем в корне |
| 148 | `_accumulate_segment(x0, y0, x1, y1, ox, oy, …)` | Добавить длину отрезка (x0,y0)->(x1,y1), попавшую в каждую ячейку, в out[iy,ix]. |
| 184 | `_accumulate_polyline(poly, ox, oy, h, nx, ny, …)` | ТОЧНЫЙ (посегментный) вариант — эталон/для тестов. В рабочем пути (реальные |
| 193 | `_rasterize_polylines(polylines, ox, oy, h, nx, ny, …)` | БЫСТРАЯ векторная растеризация: приближённая длина линий в каждой ячейке. |
| 221 | `_splat(x0, y0, dx, dy, seglen, nsamp, …)` | Раскидать длины сегментов по ячейкам (дискретизация + np.add.at). Внутренняя. |
| 238 | **ThreatGrid** | Регулярная сетка ячеек cell_km×cell_km по прямоугольнику bbox_km (в локальных |
| 243 | `__init__(bbox_km, cell_km)` |  |
| | **· геометрия ячеек ·** | |
| 267 | `cell_centers_km()` |  |
| 273 | `extent_km()` |  |
| | **· наложение слоёв ·** | |
| 278 | `add_line_layer(name, polylines, weight, attractor)` | Линейный слой: вклад ячейки += weight × (длина пересечения / размер ячейки). |
| 293 | `add_area_layer(name, polygons, weight, attractor, mark_urban)` | Площадной слой (застройка): вклад в ячейки внутри полигона. weight обычно |
| 322 | `place_masks_from_polygons(polys, settled)` | Растеризовать КОНТУРЫ населённых пунктов: вернуть (маска города, маска |
| 373 | `_free_zone_mask(free_points, city, village)` | Свободные зоны у точки вылета и у цели: где запрет города НЕ действует. |
| 407 | `apply_settlements(places_km, min_area_km2, town_city_km2, buffer_km, village_core, village_edge, …)` | Разметить населённые пункты и применить их к весу и проходимости. |
| 545 | `own_place_area_km2(buf_cells)` | Для КАЖДОЙ ячейки — площадь её «родного» населённого пункта (км²), то есть |
| 580 | `_own_place_small(city, max_km2, buf_cells)` | Маска «ячейка принадлежит НЕБОЛЬШОМУ населённому пункту» — по площади его |
| 587 | `_village_size_filter(village, skip_factor, townlike_med)` | Убрать из маски деревни пятна, которые по СОБСТВЕННОЙ градации участка |
| 621 | `settlement_stats()` | РАСПРЕДЕЛЕНИЕ пятен застройки по площади на рабочем участке (км²): |
| 643 | `place_profiles(lab, nlab, area, close_km)` | ПАСПОРТ каждого пятна застройки — два измеримых признака: |
| 674 | `grade_places(tag, area, dens, nlab, tol, min_sample, …)` | ПЕРЕСЧЁТ типа пункта по данным рабочей области. Возвращает (новый тег, отчёт). |
| 825 | `_classify_components(lab, nlab, area, places_km, min_area_km2, town_city_km2, …)` | Тип каждой компоненты застройки: 2 — город, 1 — деревня, 0 — не учитываем. |
| 894 | `_village_field(village_mask, core, edge, r_min_km, r_max_km)` | Поле штрафа деревень: `core` в центре, `edge` на ПОЛОВИНЕ радиуса, 0 на его |
| 925 | `add_relief_layer(dem_km, area_km, local_km, area_full_m, local_full_m, area_s, …)` | Рельеф: МНОЖИТЕЛЬ к уже накопленному ПОЛОЖИТЕЛЬНОМУ весу. |
| 981 | `relief_height()` | Высоты поверхности на сетке, м (None — рельеф не загружен). |
| 985 | `relief_k()` | Множитель рельефа: < 1 возвышенность, > 1 укрытие (None — рельеф не применён). |
| 989 | `relief_cut()` | Маска «уж очень высокая гора»: там вес снят полностью (None — рельефа нет). |
| 993 | `set_bridge_mask_from_layer()` | Ячейки-ПЕРЕПРАВЫ: мост из OSM (`bridge=yes`) НАД ВОДОЙ. Прежде мост |
| 1013 | `bridge_points_km(bridge_polys)` | ТОЧНЫЕ координаты переправ: середина каждой мостовой линии OSM, проходящей |
| 1040 | `crossing_cells_km(min_layers)` | Центры ячеек-ПЕРЕСЕЧЕНИЙ: где сходятся ≥ min_layers РАЗНЫХ слоёв-аттракторов |
| 1050 | `mark_water(polylines, buffer_km)` | Отметить ячейки воды (по руслам) + буфер — для ЗАПРЕТА датчиков (Задача 1 |
| 1059 | `add_intersection_bonus(bonus_per_extra)` | Funnel-бонус: за каждый слой-аттрактор в ячейке СВЕРХ первого — +bonus. |
| | **· ЗАПРЕТНЫЕ ЗОНЫ (рисует пользователь) ·** | |
| 1069 | `set_no_fly_zones(polys)` | Задать запретные зоны — список многоугольников [(x, y), …] в км. |
| 1080 | `no_fly_mask()` | Маска запретных зон (ny, nx) либо None, если зон нет. |
| | **· выгрузка ·** | |
| 1098 | `water_mask()` |  |
| 1101 | `urban_mask()` |  |
| 1104 | `flat_cells(positive_only)` | Центры ячеек (M,2) и веса (M,). positive_only — только аттракторные (w>0): |
| 1116 | `totals_by_layer()` |  |
| | **· сохранение растровой карты (ответ на открытый вопрос §9.5: растрово) ·** | |
| 1120 | `save_npz(path)` |  |
| 1129 | `load_npz(path)` |  |
| 1141 | `_box_mean(a, r)` | Среднее по квадратному окну (2r+1)² вокруг каждой ячейки — через интегральное |
| 1156 | `_relief_f(x)` | Отклик на превышение (аргумент — превышение, делённое на порог насыщения). |
| 1168 | `_points_in_polygon(px, py, poly)` | Векторный ray-casting: маска точек (px,py) внутри замкнутого полигона poly. |
| 1185 | `_label8(mask)` | Разметка связных компонент булевой маски (8-связность). Возвращает (lab, n): |
| 1209 | `_dilate(mask, r)` | Дилатация булевой маски на r ячеек (квадратная окрестность, чистый numpy). |
| 1225 | `_erode(mask, r)` | Эрозия булевой маски на r ячеек — обратная к `_dilate`. Нужна для ЗАМЫКАНИЯ |
| 1248 | `load_layers(lon0, lat0, data_path, bbox_lonlat)` | Слои цифровой карты в локальных км (якорь lon0,lat0). Возвращает (dict, источник). |
| 1276 | `_clip_layers_km(layers, bbox_km, margin_km)` | Оставить в слоях только то, что попало в рамку района плюс запас (всё в км). |
| 1319 | `layers_cache_path()` | Путь к кэшу слоёв активного участка: `geo_cache/<участок>/<THREAT_LAYERS_FILE>` |
| 1405 | `layers_from_osm(pbf_path, lon0, lat0, bbox_lonlat, only_layers, with_places)` | Прочитать локальный .osm.pbf и вернуть слои в км-фрейме (lon0,lat0). |
| 1476 | `load_dem_grid(grid, lon0, lat0, path)` | Высоты рельефа, приведённые к ячейкам сетки: массив (ny,nx) в метрах, NaN — нет |
| 1511 | `dem_files()` | Файлы рельефа участка: `THREAT_DEM_FILE` + .tif/.tiff/.hgt в папке участка (с |
| 1533 | `load_dem_display(lon0, lat0, bbox_km, max_px)` | Высоты для ОТРИСОВКИ: окно участка в исходном разрешении растра, прорежённое до |
| 1587 | `_sample_geotiff(path, lon, lat, cell_deg)` | Выборка высот GeoTIFF в точках (lon,lat). |
| 1648 | `_block_mean_at(band, bad, r, c, hy, hx)` | Среднее значение растра по прямоугольнику (2hy × 2hx пикселей) вокруг каждой точки |
| 1675 | `_sample_hgt(path, lon, lat)` | Выборка высот из сырого SRTM .hgt (int16 big-endian, тайл 1°×1°). |
| 1699 | `_clip_box_km(lon0, lat0, bbox_lonlat, margin_km)` | Рамка обрезки (x0,x1,y0,y1) в км: участок плюс запас со всех сторон. |
| 1706 | `_clip_parts(parts, clip)` | Оставить только куски геометрии, попадающие в рамку. |
| 1742 | `_edge_point(p_in, p_out, clip)` | Точка на отрезке p_in→p_out, лежащая на границе рамки clip (p_in внутри). |
| 1755 | `_places_from_osm(osm, lon0, lat0, clip)` | Населённые пункты -> (точки, контуры). |
| 1802 | `_place_name(tags)` | Название населённого пункта из тегов OSM. Пустая строка — имени нет. |
| 1821 | `_geom_to_kms(geom, lon0, lat0)` | shapely-геометрия -> список массивов (N,2) в км (линии и кольца полигонов). |
| 1863 | `packed_npz_arrays(layers)` |  |
| 1881 | `_load_layers_npz(path)` |  |
| 1901 | `_ll(seq, lon0, lat0)` | [(lon,lat),...] -> (N,2) км в фрейме (lon0,lat0). |
| 1908 | `_synthetic_layers(lon0, lat0)` | Демо-схема условного участка местности: русло реки (аттрактор+вода), |
| 1971 | `build_threat_grid(layers, bbox_km, cell_km, enabled, dem, free_points)` | Собрать ThreatGrid: наложить слои с весами из THREAT_LAYERS, funnel-бонус, |
| 2048 | **ThreatModel** | Состояние вкладки 3: цифровая карта угроз + расстановка датчиков по ней. |
| 2051 | `__init__(params)` |  |
| 2129 | `_drop_read_cache()` | Забыть прочитанное с диска: район или источник сменились. |
| | **· РАБОЧИЙ РАЙОН (план 8, задача 8.2) ·** | |
| 2148 | `set_area(bbox_lonlat)` | Задать РАБОЧИЙ РАЙОН (lon_min, lat_min, lon_max, lat_max). |
| 2197 | `clear_area()` | Убрать рабочий район: карта, слои, датчики и маршруты обнуляются. |
| 2231 | `area_size_km()` | Размер рабочего района (ширина, высота) в км — для проверок и подписей. |
| | **· КООРДИНАТЫ НАРУЖУ (план 8, задача 8.3) ·** | |
| 2242 | `km_to_lonlat_arr(pts_km)` | Массив (N,2) км -> массив (N,2) градусов (долгота, широта). |
| 2250 | `sensors_lonlat(big)` | Позиции датчиков в градусах: малые (по умолчанию) или большие. |
| 2254 | `routes_lonlat(routes)` | Маршруты в градусах: список массивов (M,2). По умолчанию — накопленные. |
| | **· ЗАГРУЖЕННАЯ ИЗ ФАЙЛА ВЫБОРКА ПРОЛЁТОВ (задача 8.6, model/flight_log.py) ·** | |
| 2260 | `load_routes_lonlat(routes_lonlat, groups)` | Загрузить маршруты В ГРАДУСАХ (из файла) → сохранить в километрах ТЕКУЩЕГО |
| 2279 | `_apply_generalization()` |  |
| 2294 | `set_generalize_loaded(on)` |  |
| 2300 | `loaded_repeat_info()` |  |
| 2305 | `clear_loaded_routes()` | Убрать загруженную выборку и погасить чекбокс показа. Возвращает, сколько |
| 2313 | `loaded_routes_lonlat()` | Загруженные маршруты в градусах — для окна «Посмотреть маршруты» (задача 8.6, |
| 2318 | `iterations_complete()` | Все ли T итераций прошли — условие, при котором осмысленно выгружать историю |
| 2324 | `cells_lonlat()` | ЦЕНТРЫ ячеек сетки в градусах, (M,2) — вместе с `flat_cells` дают |
| | **· ЗАПРЕТНЫЕ ЗОНЫ (рисует пользователь на карте) ·** | |
| 2335 | `_sync_no_fly()` | Перенести зоны в сетку (она их вычитает из проходимости) и сбросить то, что |
| 2347 | `add_no_fly_zone(poly)` | Добавить зону: список вершин [(x, y), …] в км. Меньше трёх вершин — не зона. |
| 2356 | `clear_no_fly_zones()` | Убрать все зоны — карта и веса при этом не меняются. |
| 2364 | `undo_no_fly_zone()` | Убрать последнюю зону (для случая «нарисовал не туда»). |
| 2372 | `no_fly_check()` | Что зоны сделали с задачей. Возвращает словарь с полями: |
| | **· источник данных / выбор слоёв ·** | |
| 2405 | `set_data_source(path)` | Задать файл цифровых карт (.npz или .osm.pbf); None -> авто (кэш/схема). |
| 2410 | `set_enabled_layers(names)` | Ограничить набор накладываемых слоёв (iterable имён); None -> все. |
| | **· рельеф ·** | |
| 2416 | `has_dem()` | Есть ли файл высот (по нему кнопка «Добавить рельеф» активна или нет). |
| 2420 | `set_relief(on)` | Включить/выключить учёт рельефа в весовой карте (кнопка «Добавить рельеф»). |
| 2440 | `relief_display()` | Высоты в родном разрешении для отрисовки: (массив, extent_km) или None. |
| | **· построение карты (наложение цифровых слоёв на сетку) ·** | |
| 2448 | `build()` | Загрузить цифровые слои (из файла/кэша/демо) и наложить их на сетку 500 м — |
| 2534 | `_route_gap_km()` | РАЗРЫВ между весовыми секторами (км) — задаётся в интерфейсе («Входные данные»). |
| 2541 | `_route_slack_km()` | УХОД ОТ ОРИЕНТИРА (км) — насколько маршрут отклоняется вбок от реки или дороги, |
| 2547 | `_ctx_key()` | От чего зависит кэш контекста выборки: разрыв и уход от ориентира (оба меняют |
| 2554 | `_ensure_ctx(entry, target)` | Контекст выборки (проходимость, поля расстояний, VIA). Пересобирается, только |
| 2567 | `_route_sig(poly)` | Огрублённая подпись маршрута (для дедупликации почти одинаковых путей). |
| | **· маршруты пролёта вход->цель ·** | |
| 2575 | `plan_routes()` | ВОЗМОЖНЫЕ маршруты вход→цель — показываются ДО итераций, вместе с областью залёта. |
| 2629 | `_spread_over_area(pool, want, spread_w)` | Жадно отобрать `want` маршрутов, покрывающих область залёта КАК МОЖНО ШИРЕ. |
| 2678 | `count_routes()` | ТОЧНОЕ число возможных маршрутов вход→цель внутри области залёта (без перебора). |
| 2692 | `_spend()` | Профиль расхода запаса хода — повадка БПЛА для ИТЕРАЦИЙ (панель справа). |
| | **· ИТЕРАЦИОННЫЕ маршруты (стохастические пути по коридорам) ·** | |
| 2699 | `iter_reset()` | Начать выборку заново: пересчитать авто-запас хода и огибающую, подготовить |
| 2710 | `iter_step()` | Одна итерация: сгенерировать ОДИН маршрут (несколько попыток) и добавить его в |
| 2730 | `iter_batch()` | Сгенерировать T маршрутов сразу (без анимации). T = p.threat_iter_routes. |
| 2767 | `iterate_routes()` |  |
| 2770 | `_refresh_envelope()` | Пересчитать авто-запас хода и огибающую мест пролёта. Возвращает (вход, цель). |
| 2787 | `sync_auto_L_max(min_corridor_km)` | Если запас хода НЕ задан руками — авто: \|AB\|·(1+THREAT_LMAX_AUTO_FRAC), но не меньше |
| 2804 | `ensure_built()` | Гарантировать, что карта построена (построить, если ещё нет), и вернуть сетку. |
| | **· кандидатные позиции датчика (сетка минус вода) ·** | |
| 2811 | `auto_cand_step_km()` | Каким шаг сетки ДОЛЖЕН быть при текущих радиусах: половина R, 0.5…6 км. |
| 2822 | `sync_cand_step(force)` | Подогнать шаг сетки под радиус — но только КОГДА РАДИУС СМЕНИЛСЯ. |
| 2850 | `cand_step_km()` | Шаг сетки кандидатных позиций, км (не меньше 0.5). |
| 2854 | `candidate_positions()` | Возможные места установки датчика — узлы регулярной сетки с шагом |
| 2869 | `_on_water(pts)` | Маска кандидатов, попавших в водную ячейку (или её буфер) — запрет датчика. |
| | **· датчики, заданные человеком (задача 8.7) ·** | |
| 2881 | `_reset_sensors()` | Обнулить расстановку — координаты И ИХ СПУТНИКИ разом. |
| 2912 | `_tag_sensors()` | Проставить спутники расстановки: тип датчика, его режим и кто выбрал место. |
| 2935 | `_match_manual(pts, small, tol_km, only_static)` | Маска: какие из позиций `pts` названы ЧЕЛОВЕКОМ. |
| 2957 | `sensors_table()` | ВСЕ датчики на карте одной таблицей — то, что показывает окно «Исходные |
| 2987 | `add_manual_sensor(type_id, static, x_km, y_km)` | Добавить датчик, заданный человеком (клик по карте, строка таблицы, файл). |
| 3000 | `_find_placed(x_km, y_km, tol_km)` | Найти датчик РАССТАНОВКИ в этой точке: `("small"\|"big", индекс)` или None. |
| 3016 | `move_placed_sensor(old_xy, new_xy, type_id, static)` | Перенести датчик РАССТАНОВКИ в новую точку (правка мышью или строкой таблицы). |
| 3063 | `remove_placed_sensor(x_km, y_km)` | Убрать датчик РАССТАНОВКИ в этой точке (клавиша Delete в режиме правки). |
| 3083 | `_mark_manual_placed()` | Пометить заданные датчики как УЧТЁННЫЕ этой расстановкой. |
| 3094 | `remove_manual_sensor(index)` | Убрать запись по номеру строки таблицы. Неверный номер — молча ничего. |
| 3101 | `clear_manual_sensors()` | Убрать все заданные вручную датчики (кнопка «очистить» в таблице). |
| 3107 | `in_iterations()` | Идёт ли моделирование: датчики считаются по НАКОПЛЕННЫМ пролётам, а не по |
| 3113 | `manual_locked(type_id)` | Заданные человеком датчики, чью позицию СЕЙЧАС двигать нельзя. |
| 3130 | `manual_lonlat()` | Заданные вручную датчики в градусах — для таблицы и для записи в файл. |
| 3141 | `add_manual_lonlat(type_id, static, lon, lat)` | Добавить датчик по ГРАДУСАМ — так приходят записи из файла (формат 8.7.7). |
| 3146 | `update_manual_lonlat(rec, type_id, static, lon, lat)` | Изменить УЖЕ ЗАДАННЫЙ датчик: тип, режим и координаты (кнопка «Изменить»). |
| 3163 | `manual_outside_area()` | Сколько заданных вручную датчиков лежит ВНЕ рабочего района. |
| | **· расстановка датчиков (ЭТАП 4: по тепловой карте МАРШРУТОВ, §4.6) ·** | |
| 3175 | `place_sensors()` | Расставить датчики по тепловой карте МАРШРУТОВ (где реально/вероятно летает БПЛА), |
| 3252 | `sensor_movement()` | Сопоставление ПЕРВОГО построения с ТЕКУЩИМ — «анализ размещения» (задача 8.9). |
| 3274 | `_manual_dynamic_count()` | Сколько заданных человеком датчиков — ДИНАМИЧЕСКИЕ (их место можно менять). |
| 3278 | `_manual_specs()` | Типы для режима «Задать позиции»: сколько датчиков КАЖДОГО типа в таблице. |
| 3295 | `_place_manual_iter()` | Режим «Задать позиции» ВО ВРЕМЯ МОДЕЛИРОВАНИЯ: статические стоят, динамические |
| 3322 | `_place_manual()` | Датчики РОВНО ТАМ, где их задал человек (режим «Задать позиции»). |
| 3359 | `_place_by_weight(cand)` | Расстановка по СТАТИЧЕСКОЙ весовой карте (единица покрытия — клетка веса). |
| 3372 | `_place_by_routes(cand, routes)` | Расстановка по ВЫБОРКЕ маршрутов (частота пролёта). Правила: |
| 3480 | `_place_multi_type(cand, routes, types)` | Расстановка, когда работает НЕ ОДИН малый тип либо есть закреплённые датчики. |
| 3564 | `_place_big(routes, n_big)` | БОЛЬШИЕ датчики — «круговой щит» у цели: `threat_N_big` штук радиусом |
| 3673 | `_sample_for_sensors()` | Выборка маршрутов, по которой считаются датчики и 2-я тепловая карта (§4.6): |
| 3687 | `generalized_sample(frac)` | ОБОБЩЁННАЯ выборка итерационных маршрутов для показа: ~frac (10 %) от УЖЕ |
| 3729 | `route_density_field()` | 2-я ТЕПЛОВАЯ КАРТА — частота пролёта БПЛА: сколько маршрутов проходит через |
| 3745 | `_sensor_radii()` | Радиус КАЖДОГО малого датчика — по его типу. |
| 3763 | `_evaluate(cells_xy, cells_w)` | Показатели: суммарный вес карты, покрытый вес, доля, разбивка по слоям и |
| 3847 | `_evaluate_current()` | Пересчитать показатели по ТЕКУЩЕЙ расстановке, не трогая сами позиции. |
| 3858 | `metrics()` |  |
| 3862 | `_approach_bearing(route, target_xy)` | Азимут ОТ ЦЕЛИ на КОНЕЧНУЮ часть маршрута — направление ЗАХОДА, а не старта |
| 3874 | `sector_stats()` | Статистика прихода БПЛА по 12 направлениям КОМПАСА, шаг |
| 3928 | `reachable_stats()` | Корректная мера «всех возможных мест пролёта» — размер огибающей (граф-обход): |
| | **· точка входа (фиксирована у реки) и цель (можно задать кликом) ·** | |
| 3938 | `set_target(x_km, y_km)` | Задать целевую точку кликом по карте (режим «указать цель»). Запас хода |
| 3955 | `entry_km()` | ПРЕДСТАВИТЕЛЬ точки входа — для показателей и авто-запаса хода. При заданном |
| 3965 | `entry_points_km()` | ВСЕ точки входа списком: пять точек сектора либо одна демо-точка. |
| 3969 | `target_only_km()` | Цель в км: заданная кликом либо демо-метка участка. |
| 3977 | `entry_target_km()` | (вход, цель) в км — ОДНОЙ точкой. Вход: представитель (середина точек сектора) |
| 3983 | `entries_target_km()` | (входы, цель): вход — СПИСОК точек, если задан сектор, иначе одна точка. |
| | **· СЕКТОР ПОЯВЛЕНИЯ БПЛА ·** | |
| 3991 | `set_sector(x_km, y_km)` | Задать сектор кликом по краю карты: ось «цель → клик», по |
| 4002 | `clear_sector()` | Убрать сектор: вход снова один — демо-точка участка. |
| 4011 | `_reset_after_entry_change()` | Сброс после смены входа: контекст, маршруты, область залёта, запас хода. |
| 4021 | `sector_edges_km()` | Две крайние точки сектора на рамке участка — для отрисовки. None, если |
| 4040 | `_ray_to_bbox(origin, u)` | Длина луча из `origin` по орту `u` до рамки участка (км). |
| 4053 | `compute_entry_points()` | Отобрать точки входа на краю карты внутри сектора — с учётом весовой карты. |
| 4147 | `_entry_window_score(xs, ys, target_km)` | ПЕРСПЕКТИВА ДВИЖЕНИЯ из точки: средний вес прямоугольника, вытянутого от неё |


## controller_qt/threat_controller.py

**Отдаёт наружу:** `ThreatController`.

| стр. | что | зачем |
|---:|---|---|
| 25 | **_TaskSignals** | Мостик «фоновый поток → интерфейс». Qt-объекты из потока трогать нельзя, поэтому |
| 32 | **_Task** | Тяжёлый расчёт (чтение OSM / наложение слоёв / расстановка) в ФОНОВОМ потоке, |
| 37 | `__init__(kind, work_fn, sig)` |  |
| 41 | `run()` |  |
| 53 | **ThreatController** | Связывает МОДЕЛЬ (ThreatModel — чистый numpy) и ПРЕДСТАВЛЕНИЕ (ThreatMapView — Qt). |
| 64 | `__init__(model, view)` |  |
| | **· ЗАПРЕТНЫЕ ЗОНЫ ·** | |
| 135 | `on_zone_added(poly)` | Пользователь нарисовал зону: пролёт там запрещён, всё зависящее — пересчитать. |
| 141 | `on_zone_undo()` | Убрать последнюю зону. |
| 148 | `_after_zone_change()` | Общее для добавления и отмены: пересчёт и ЧЕСТНОЕ сообщение о последствиях. |
| | **· РЕЛЬЕФ ·** | |
| 170 | `on_relief()` | Кнопка «Добавить рельеф» / «Убрать рельеф». |
| 198 | `_on_view_changed()` | Вид изменился — перерисовать только векторные слои (остальное не зависит |
| | **· ИТЕРАЦИИ (симуляция как во вкладке 2) ·** | |
| 215 | `_iter_ready()` |  |
| 222 | `on_iter_speed(v)` |  |
| 225 | `_iter_title()` |  |
| 239 | `on_iter_play()` |  |
| 257 | `_iter_finished()` |  |
| 260 | `_maybe_refresh_sensors(force)` | Пересчитать датчики по УЖЕ накопленной выборке пролётов — периодически, а не |
| 276 | `_anim_stop()` |  |
| 281 | `_anim_tick()` | Один кадр анимации (таймер 33 мс). Если текущий маршрут «долетел» — берём из |
| 324 | `on_iter_step()` |  |
| 346 | `on_iter_batch()` |  |
| | **· «ОЧИСТИТЬ»: назад к состоянию ДО МОДЕЛИРОВАНИЯ (заказчик 05.09.2026) ·** | |
| 358 | `_snapshot_before_iter()` | Запомнить расстановку и заданные датчики ПЕРЕД запуском итераций. |
| 385 | `on_clear_iter()` | Кнопка «Очистить»: убрать итерации и вернуть состояние до моделирования. |
| | **· ИСТОРИЯ ПОЛЁТОВ: выгрузка/загрузка (задача 8.6) ·** | |
| 423 | `on_flights_save(path)` | Кнопка «Выгрузить историю полётов». Путь уже выбран диалогом во View — |
| 445 | `on_flights_load(path)` | Кнопка «Загрузить историю полёта». Маршруты идут В ОТДЕЛЬНОЕ поле |
| 475 | `on_flights_clear()` | Кнопка «Удалить выборку» — убирает ТОЛЬКО загруженную историю (заказчик |
| | **· окно «Посмотреть маршруты» (задача 8.6, довесок 06.09.2026) ·** | |
| 489 | `_routes_data_for_viewer()` | Данные для окна просмотра: свои пройденные маршруты и загруженная история, |
| 494 | `_generalize_state_for_viewer()` |  |
| 500 | `on_generalize_loaded(on)` |  |
| 514 | `on_route_preview(is_loaded, index)` | Подсветить на карте ОДИН выбранный маршрут (км — тот же фрейм, что у карты). |
| 521 | `on_route_preview_reset()` |  |
| | **· окно «Исходные данные»: применить сразу (не блокирует программу) ·** | |
| 533 | `on_sensors_apply(vals)` | Применить поля окна «Исходные данные». |
| 578 | `on_input_apply(vals, gap_changed)` | Пересчёт после правки параметров МАРШРУТА (запас хода, разрыв, скорость…). |
| 612 | `_render_sensors(empty)` | Отдать датчики на отрисовку вместе с типами и признаком «закреплён». |
| | **· ДАТЧИКИ, ЗАДАННЫЕ ЧЕЛОВЕКОМ (задача 8.7) ·** | |
| 663 | `on_manual_add(type_id, static, lon, lat)` | Добавить датчик по координатам (клик по карте, строка таблицы или файл). |
| 668 | `on_manual_remove(index)` | Убрать датчик по номеру строки таблицы. |
| 691 | `on_manual_edit(index, type_id, static, lon, lat)` | Изменить датчик строкой таблицы: тип, режим и координаты — одним действием. |
| 721 | `on_sensor_delete(lon, lat)` | Удалить датчик, выбранный на карте в режиме правки (клавиша Delete). |
| 742 | `_manual_at(x_km, y_km, tol)` | Заданная человеком запись в этой точке — или None, если её поставил алгоритм. |
| 751 | `on_manual_clear()` |  |
| 755 | `on_clear_sensors()` | Кнопка «Очистить датчики»: убрать с карты ВСЕ — и подобранные, и заданные. |
| 768 | `on_manual_mode(manual)` | Переключение «Рассчитать позиции» ⇄ «Задать позиции». |
| 773 | `on_mode_analysis(checked)` | Галочка «режим анализа» плашки над картой (план 8, задача 8.9, довесок |
| 779 | `_sensor_rows()` | Чем заполнять таблицу окна. |
| 796 | `_left_to_place()` | Сколько датчиков алгоритму осталось доставить к заданным человеком. |
| 812 | `_after_manual_change()` | Список заданных датчиков изменился: обновить таблицу и карту. |
| | **· смена приоритета по весу (max/medium/min/mix) ·** | |
| 844 | `on_iter_mode(key)` |  |
| | **· смена разброса по карте (center/middle/edge/mix) ·** | |
| 851 | `on_iter_spread(key)` |  |
| | **· смена профиля расхода запаса хода (late/early/even) ·** | |
| 860 | `on_iter_spend(key)` |  |
| | **· запуск тяжёлого расчёта в фоне ·** | |
| 867 | `_run_async(kind, work_fn)` | Запустить тяжёлую функцию модели `work_fn` в ФОНОВОМ потоке (пул QThreadPool), |
| 884 | `_on_task_done(payload)` | Приходит в ОСНОВНОЙ поток по сигналу завершения фоновой задачи. Разбирает метку |
| | **· построение карты (в фоне) ·** | |
| 965 | `_work_build()` |  |
| 969 | `_work_place()` |  |
| 978 | `on_build()` |  |
| | **· РАБОЧИЙ РАЙОН (план 8, задача 8.2) ·** | |
| 982 | `on_set_area(bbox_lonlat)` | Задать рабочий район: сетка, веса, маршруты и датчики — только по нему. |
| 1011 | `on_clear_area()` | Убрать рабочий район: всё расчётное обнуляется, остаётся только показ. |
| | **· выбор цифровых карт (источник + слои) из отдельного окна ·** | |
| 1028 | `on_choose_data(path, enabled)` |  |
| | **· «указать цель» кликом по карте ·** | |
| 1040 | `on_set_target(x, y)` | Клик ТОЛЬКО задаёт цель. Пересчёт — по кнопке «Применить». |
| | **· «задать сектор появления» кликом по краю карты ·** | |
| 1052 | `on_set_sector(x, y)` | Клик задаёт ось «цель → точка»; модель отбирает по весам точки входа. |
| 1066 | `on_clear_sector()` |  |
| 1072 | `_render_after_input(msg)` | Показать заданное (цель, сектор, точки входа) и напомнить про «Применить». |
| | **· сброс: убрать датчики и заданную цель (карта остаётся) ·** | |
| 1086 | `on_reset()` |  |
| 1122 | `on_place()` |  |
| | **· параметры / режим ·** | |
| 1127 | `on_apply()` |  |
| 1163 | `on_mode(key)` |  |
| 1168 | `on_toggle()` |  |
| 1186 | `on_map_layer(key)` |  |
| 1189 | `on_map_offline(flag)` |  |
| 1192 | `on_reset_view()` |  |
| | **· отрисовка ·** | |
| 1196 | `_render_all()` | Собрать текущее состояние модели и целиком перерисовать карту с учётом галочек |
| 1284 | `_render_sector_compass(toggles)` | Сектора «анализа моделирования» по направлениям (довесок 06.09.2026): галка |
| 1293 | `_render_sensor_track(toggles)` | «Анализ размещения» (задача 8.9) — сравнение ПЕРВОГО построения с ТЕКУЩИМ. |
| 1305 | `on_iter_gen_frac(frac)` | Пользователь сменил долю обобщённой выборки (поле «обобщ. %»). Запоминаем и, если |
| 1312 | `_refresh_generalized(toggles)` | Обновить слой ОБОБЩЁННОЙ выборки (доля задаётся полем «обобщ. %», по умолч. 10 %): |
| | **· показатели ·** | |
| 1324 | `_idle_metrics()` |  |
| 1341 | `_relief_lines()` | Блок показателей по рельефу. Пока рельеф не добавлен — одна строка-подсказка, |
| 1363 | `_full_metrics()` |  |
| | **· отчёт по ТИПАМ датчиков (задача 8.7) ·** | |
| 1409 | `_resource_lines(p)` | Блок РЕСУРС: строка на каждый работающий тип. |
| 1433 | `_placement_lines(me)` | Блок РАССТАНОВКА: сколько поставлено, по типам и сколько закреплено. |
| 1454 | `_by_type_lines(me)` | Засечка ОТДЕЛЬНО ПО ТИПАМ: свой радиус, своя доля, свои простаивающие. |
| | **· окно «Анализ моделирования» (план 8, задача 8.9, довесок 06.09.2026) ·** | |
| 1477 | `_analysis_html()` | HTML-текст окна: сумма засечки, разбивка по типам датчиков (те же числа, |
| 1534 | `run()` |  |


## model/threat_routes.py

| стр. | что | зачем |
|---:|---|---|
| 51 | `_cell_of(grid, xy)` | Точка (x, y) в км -> индекс ячейки (iy, ix), обрезанный по границам сетки. |
| 58 | `_dijkstra_dist(passable, start, h)` | Поле ГЕОМЕТРИЧЕСКИХ расстояний (км) от start по проходимым ячейкам (8-связно). |
| 83 | `_cost_mul(grid, wnorm, power, relief_power)` | Множитель цены прохода через ячейку — ПРОИЗВЕДЕНИЕ двух независимых сомножителей: |
| 115 | `_dijkstra_cost(passable, start, h, mul)` | Поле СТОИМОСТИ пути от start (в км-эквивалентах) по проходимым ячейкам. |
| 146 | `passable_mask(grid, max_gap_km, slack_km)` | Проходимые ячейки для маршрута: коридоры (вес>0), СШИТЫЕ через провалы ≤ max_gap_km, |
| 189 | `_as_entry_list(entry_km)` | Точки входа списком. Принимает и одну точку (x, y), и список точек — так вызовы |
| 198 | `_open_endpoints(pas, cells_yx, grid, radius_km)` | Открыть проходимость в окрестности ~radius_km вокруг заданных ячеек (вход/цель), |
| 208 | `flight_envelope(grid, entry_km, target_km, L_max, max_gap_km, slack_km)` | ВСЕ ВОЗМОЖНЫЕ МЕСТА ПРОЛЁТА при запасе хода L_max. Ячейка попадает в «возможные |
| 240 | `count_possible_routes(env, gt, entry_cell, goal_cell)` | СКОЛЬКО ВСЕГО маршрутов вход→цель существует внутри области залёта — точное число, |
| | **· ИТЕРАЦИОННАЯ (стохастическая) генерация множества маршрутов ·** | |
| 300 | `_mode_corridor_pref(wnorm, wmode)` | Предпочтение соседа по ВЕСУ клетки (тепловая карта), зависит от РЕЖИМА. «Пол» у |
| 317 | `_lookahead_fields(grid, look_km)` | Средний множитель РЕЛЬЕФА впереди по каждому из 8 направлений: `ahead[d][y,x]` — |
| 354 | `_catmull_rom(points, seg_km, alpha)` | ИНТЕРПОЛЯЦИЯ ломаной гладкой кривой (центростремительный сплайн Катмулла–Рома): |
| 385 | `_rdp(points, eps)` | Упрощение ломаной (Ramer–Douglas–Peucker): убирает точки ближе eps к хорде — снимает |
| 413 | `_poly_len_km(poly)` | Длина ломаной (км) — сумма длин звеньев. Нужна для проверки «уложились в запас хода». |
| 419 | `_cells_yx_to_km(grid, cells_yx)` | Список ячеек (y,x) -> точки-центры (M,2) в км. |
| | **· ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ / ЗАДЕЛ НА БУДУЩЕЕ (движение маршрута) ·** | |
| 548 | `_spend_tau(slack_km, slack0_km, frac_done, profile)` | «Температура» выбора соседа: чем она меньше, тем жёстче маршрут идёт к цели. |
| 575 | `_walk_field(ctx, dfield, start, goal, budget, turn_interval_km, …)` | Стохастический спуск по полю расстояний dfield от start к goal (надёжно приходит): |
| 783 | `_nearest_passable_cell(pas, grid, y_km, x_km, max_km)` | Ближайшая проходимая ячейка к точке (км). None, если ничего в радиусе max_km. |
| 798 | `_seg_dist_km(pt, a, b)` | Расстояние от точки pt до ОТРЕЗКА a-b (км). Для сортировки via по «дальности обхода». |
| 806 | `build_iter_context(grid, entry_km, target_km, max_gap_km, n_grid, slack_km)` | Подготовить (один раз, кэшируется) контекст выборки. |
| 1017 | `_pick_via(ctx, spread, rng, L_max)` | Выбрать индекс VIA по режиму РАЗБРОСА (по дальности обхода segd): center — близкие к |
| 1047 | `_off_corridor(poly, grid, pas)` | Сколько точек линии вышло за ПРОХОДИМУЮ зону (город / провал шире разрыва). |
| 1054 | `_mean_weight(poly, grid)` | Средний вес карты под линией — по ячейкам, через которые она проходит. |
| 1062 | `_round_corners(poly, raw, rounds, max_shift)` | Скруглить остаточную угловатость линии, не уводя её от расчётного пути. |
| 1087 | `_smooth_safe(raw, grid, pas, eps0)` | Сгладить ломаную, НЕ ВЫВОДЯ её из коридора и НЕ УВОДЯ с весовой карты. |
| 1134 | `sample_one_route(ctx, mode, L_max, turn_interval_km, rng, spread, …)` | ОДИН стохастический маршрут вход→цель. mode = приоритет клетки по ВЕСУ |


## tools/gui_check.py

| стр. | что | зачем |
|---:|---|---|
| | **· пути к плагинам Qt: строго ДО импорта QtWidgets (см. шапку) ·** | |
| 43 | `check(ok, what, detail)` |  |
| 50 | `main()` |  |


## tools/переносимое/make_figures.py

| стр. | что | зачем |
|---:|---|---|
| 29 | `_find_root(markers)` | Корень проекта — ближайшая папка ВВЕРХ, содержащая любой из маркеров. |
| 73 | `save(fig, name)` |  |
| 84 | `fig_diminishing()` | Прирост от каждого следующего датчика падает, накопленное покрытие растёт. |
| 152 | `fig_saturation()` |  |
| 180 | `fig_guarantee()` |  |
| 206 | `fig_saa()` |  |
| 234 | `fig_extrema()` |  |
| 266 | `fig_concavity()` |  |
| 297 | `fig_ellipse()` |  |
| 353 | `fig_arc()` |  |
| 418 | `fig_corridor()` |  |
| 482 | `fig_start_zone()` |  |
| 533 | `fig_turn_radius()` |  |
| 596 | `fig_segments()` |  |
| 637 | `fig_relief()` | relief_k = 1 − 0.75·f(A/60) − 0.45·f(B/35), зажат в [0.25, 1.40]. |
| 676 | `fig_greedy_steps()` |  |
| 732 | `fig_distributions()` | Три закона распределения, которыми задаётся случайность в модели. |
| 790 | `fig_expectation()` | Матожидание — центр тяжести; дисперсия — разброс вокруг него. |
| 816 | `fig_risk_criteria()` | Три разных критерия на одном распределении: среднее, квантиль, худший случай. |
| 845 | `fig_chance_constraint()` | Вероятностное ограничение: не «в среднем хорошо», а «плохо — редко». |
| 876 | `fig_sets()` | Операции над множествами — язык, на котором записаны все ограничения. |
| 923 | `fig_integral()` | Сумма и интеграл — одно и то же действие при разном шаге. |
| 951 | `fig_convex()` | Выпуклое множество: отрезок между любыми точками не выходит наружу. |
| 982 | `fig_gradient()` | Линии уровня и градиент: как выглядит «идти вверх» в непрерывной задаче. |
| 1013 | `fig_scenario_tree()` | Откуда берётся выборка сценариев и что с ней делают. |
| 1076 | `main()` |  |


## view_qt/input_window.py

**Отдаёт наружу:** `DIR_LOAD`, `DIR_SAVE`, `default_file_name`, `read_sensors_file`, `sensors_dir`, `write_sensors_file`.

| стр. | что | зачем |
|---:|---|---|
| | **· имена разделов памяти полей (`ui_state`): что подставится при следующем открытии ·** | |
| | **· СВОЯ ПАПКА ФАЙЛОВ ДАТЧИКОВ (требование заказчика 05.09.2026) ·** | |
| 64 | `sensors_dir(kind)` | Папка файлов датчиков: `датчики/загрузка` или `датчики/выгрузка`. |
| 82 | `default_file_name(when)` | Имя файла выгрузки по дате и времени: `2026_09_05_09_15_sensor.txt`. |
| 91 | `mode_word(static)` |  |
| 95 | `parse_mode(word)` | Слово режима → True (статический) / False (динамический) / None (не режим). |
| 105 | `write_sensors_file(path, rows, params)` | Записать датчики в текстовый файл с шапкой. |
| 127 | `read_sensors_file(path)` | Прочитать файл датчиков → `(записи, отчёт)`. |
| 175 | `_parse_header_type(text, types)` | Строка таблицы типов из шапки: `1  малый  20  2.0  3` → types[1] = (20, 2.0, 3). |
| 196 | **SensorRowDialog** | Тип (1–4), режим (статический / динамический) и координаты одного датчика. |
| 206 | `__init__(parent, lon, lat, title)` |  |
| 255 | `_select(combo, value)` |  |
| 260 | `values()` | Введённое: `(тип, статический, lon, lat)` — или None, если координаты пусты. |
| 270 | `_accept()` |  |
| 286 | `_params_of(widget)` | Достать Params у окна-владельца (для подписи радиусов в списке типов). |
| 301 | **InputDataWindow** | Окно «Исходные данные»: параметры четырёх типов датчиков, параметры маршрута и |
| 354 | `__init__(parent, params, on_apply)` |  |
| | **· построение окна ·** | |
| 382 | `_build_ui()` |  |
| 423 | `_build_area()` |  |
| 449 | `set_area_info(html)` |  |
| 454 | `_build_top()` | Режим работы и кратность — вверху: они меняют смысл полей ниже. |
| 522 | `_build_types()` | Левая колонка — четыре типа датчиков, каждый своей группой. |
| 565 | `_build_route()` | Правая колонка — параметры маршрута и расстановки. |
| 599 | `_field(name, tip)` |  |
| 607 | `_sep()` |  |
| 613 | `_build_table()` | Таблица датчиков во всю ширину — с прокруткой и кнопками справа. |
| | **· заполнение и чтение полей ·** | |
| 691 | `refresh(params)` | Подставить в поля текущие параметры модели. |
| 717 | `set_ab(km)` |  |
| 720 | `is_manual_mode()` | Окно в режиме «Задать позиции»? |
| 724 | `_auto_changed(*_)` | Поля в авто-режиме показывают значение, но правке не поддаются. |
| 740 | `_k_mode_changed(*_)` | «Одна на всех» — поля кратности у типов 2–4 показывают общее число и закрыты |
| 753 | `_k_edited(text)` | Кратность вводится в одной группе и СРАЗУ дублируется в остальные — человек |
| 767 | `_mode_changed(*_)` |  |
| 778 | `_sync_counts()` | В режиме «Задать позиции» КОЛИЧЕСТВО задаёт таблица, а не человек. |
| 804 | `_apply()` | Собрать поля и отдать контроллеру. |
| | **· таблица ·** | |
| 834 | `set_rows(rows)` | Показать датчики в таблице. `rows` — как отдаёт `ThreatModel.sensors_table()`. |
| 857 | `_selected()` |  |
| 861 | `_add_row()` |  |
| 867 | `_edit_row()` |  |
| 885 | `_del_row()` |  |
| 892 | `_clear_rows()` | Кнопка «Очистить» под таблицей. |
| 919 | `_pick_mode()` |  |
| | **· файл ·** | |
| 923 | `_start_dir(kind)` | Откуда начинать выбор файла: СВОЯ папка (`датчики/загрузка`, `датчики/выгрузка`). |
| 937 | `_remember_dir(kind, path)` | Запомнить, куда человек ушёл сам. ⚠️ Раздел памяти пишется ЦЕЛИКОМ |
| 945 | `_load_file()` |  |
| 972 | `_save_file()` |  |
| 993 | `_warn(text)` |  |


## config.py

**Отдаёт наружу:** `AREA_TRAJ_LABELS`, `MANEUVER_LAW_LABELS`, `MODES`, `MODE_LABELS`, `MOTION_LABELS`, `Params`, `SPLASH_AUTHORS`, `SPLASH_AUTHORS_LABEL`, `SPLASH_EMBLEM_FRAC`, `SPLASH_EMBLEM_TEXT`, `SPLASH_EMBLEM_TEXT_COLOR`, `SPLASH_EMBLEM_TEXT_PT`, `SPLASH_EMBLEM_TEXT_Y`, `SPLASH_FULLSCREEN`, `SPLASH_IMAGE`, `SPLASH_MAX_PX`, `SPLASH_PAD`, `SPLASH_SECONDS`, `SPLASH_TITLE`, `SPREAD_EPS`, `THEME`, `THREAT_AREA`, `THREAT_AREAS`, `THREAT_AREA_DIR`, `THREAT_AREA_ERROR`, `THREAT_AREA_LABEL`, `THREAT_AREA_PAD_KM`, `THREAT_AREA_SOURCE`, `THREAT_BBOX_LONLAT`, `THREAT_BBOX_POINTS`, `THREAT_CELL_M`, `THREAT_CITY_ZERO_WEIGHT`, `THREAT_COMPASS_APPROACH_KM`, `THREAT_COMPASS_LINE_KM`, `THREAT_COMPASS_STEP_DEG`, `THREAT_COST_MIX_BIAS`, `THREAT_COST_MIX_STEPS`, `THREAT_COST_POWER`, `THREAT_COST_RELIEF`, `THREAT_DEM_AREA_FULL_M`, `THREAT_DEM_AREA_KM`, `THREAT_DEM_AREA_S`, `THREAT_DEM_CUT_AREA_M`, `THREAT_DEM_CUT_LOCAL_M`, `THREAT_DEM_FILE`, `THREAT_DEM_FLOOR`, `THREAT_DEM_K_MAX`, `THREAT_DEM_K_MIN`, `THREAT_DEM_LOCAL_FULL_M`, `THREAT_DEM_LOCAL_KM`, `THREAT_DEM_LOCAL_S`, `THREAT_DEM_ON_START`, `THREAT_ENTRY`, `THREAT_ENTRY_FREE_KM`, `THREAT_INTERSECTION_BONUS`, `THREAT_ITER_MODE_LABELS`, `THREAT_ITER_SPREAD_LABELS`, `THREAT_LAYERS`, `THREAT_LAYERS_FILE`, `THREAT_LAYER_MARGIN_KM`, `THREAT_LAYER_ORDER`, `THREAT_LMAX_AUTO_FRAC`, `THREAT_LMAX_CORRIDOR_FRAC`, `THREAT_LOOKAHEAD_KM`, `THREAT_LOOKAHEAD_POWER`, `THREAT_PLACE_CLOSE_KM`, `THREAT_PLACE_GRADE_MIN_SAMPLE`, `THREAT_PLACE_GRADE_MODE`, `THREAT_PLACE_GRADE_ON`, `THREAT_PLACE_GRADE_OUTLIER`, `THREAT_PLACE_GRADE_STRICT_UP`, `THREAT_PLACE_GRADE_TOL`, `THREAT_RIVER_CORRIDOR_MAX_KM2`, `THREAT_ROUTE_COUNT`, `THREAT_ROUTE_MODE_SRC`, `THREAT_ROUTE_POOL_MULT`, `THREAT_ROUTE_SPREAD_W`, `THREAT_SECTOR_EDGE_KM`, `THREAT_SECTOR_ENTRIES`, `THREAT_SECTOR_ENTRY_COOLDOWN`, `THREAT_SECTOR_HALF_DEG`, `THREAT_SECTOR_MIN_ANGLE_FRAC`, `THREAT_SECTOR_MIN_SEP_KM`, `THREAT_SECTOR_WIN_ACROSS`, `THREAT_SECTOR_WIN_ALONG`, `THREAT_SENSOR_REFRESH_EVERY`, `THREAT_SOURCE_FILE`, `THREAT_SPEND_LABELS`, `THREAT_SPREAD_PAIR`, `THREAT_TARGET`, `THREAT_TARGET_FREE_KM`, `THREAT_TILE_AREA`, `THREAT_TOWN_CITY_KM2`, `THREAT_URBAN_BUFFER_KM`, `THREAT_URBAN_MIN_AREA_KM2`, `THREAT_VILLAGE_FLOOR`, `THREAT_VILLAGE_PENALTY_CORE`, `THREAT_VILLAGE_PENALTY_EDGE`, `THREAT_VILLAGE_RADIUS_MAX_KM`, `THREAT_VILLAGE_RADIUS_MIN_KM`, `THREAT_VILLAGE_SKIP_FACTOR`, `THREAT_VILLAGE_TOWNLIKE_MED`, `THREAT_WATER_BUFFER_M`, `THREAT_WEIGHT_GAIN`, `THREAT_WORK_LONLAT`, `TRAJ_LABELS`, `WAYPOINT_ZONE_LABELS`.

| стр. | что | зачем |
|---:|---|---|
| | **· СЕКТОР ПОЯВЛЕНИЯ БПЛА (кнопка «Задать сектор», клик по краю карты) ·** | |
| | **· Населённые пункты: ГОРОД запрещён, ДЕРЕВНЯ обходится по краю ·** | |
| | **· СОБСТВЕННАЯ ГРАДАЦИЯ НАСЕЛЁННЫХ ПУНКТОВ (тип по данным, а не по тегу) ·** | |
| | **· СВОБОДНЫЕ ЗОНЫ у точки вылета и у цели ·** | |
| | **· РЕЛЬЕФ (карта высот) ·** | |
| | **· ПРЕДВИДЕНИЕ ПРЕПЯТСТВИЯ (обход холма) ·** | |
| | **· ТЯГА К ЦЕЛИ: ПО СТОИМОСТИ, А НЕ ПО КИЛОМЕТРАМ ·** | |
| | **· Построение маршрутов вход→цель по весовой карте (вкладка 3) ·** | |
| 804 | **Params** | Входные параметры модели (все редактируются в интерфейсе). |
| | **· вкладка 3 (цифровая карта угроз) ·** | |
| 927 | `A()` |  |
| 931 | `B()` |  |
| 934 | `validate()` |  |


## view_qt/map_image.py

| стр. | что | зачем |
|---:|---|---|
| 107 | `detail_paths(image_path)` | Пути кэша детализации: (файл данных, файл описания). Кладутся в подпапку рядом |
| 115 | `detail_ready(image_path)` | Готов ли кэш детализации и не устарел ли он (сверяется с размером картинки). |
| 120 | `read_detail_meta(image_path)` | Описание кэша либо None. Проверяет, что данные на месте и того самого размера. |
| 138 | `build_detail(image_path, on_progress)` | Развернуть картинку в несжатый файл для быстрого чтения кусками. |
| 186 | `read_detail_window(meta, box_frac, max_px)` | Кусок картинки в полном разрешении -> (RGBA (H, W, 4), фактическая доля кадра). |
| 229 | `read_detail_px(meta, box_px, max_px)` | Кусок картинки по ПИКСЕЛЬНОМУ прямоугольнику -> (RGBA, x0, y0, step). |
| 259 | `read_for_fit(path, want_px)` | Картинка для ПОДБОРА привязки -> (RGB, шаг от оригинала, (ширина, высота)). |
| 281 | `images_dir(geo_root)` | Папка картинок карт; создаётся при первом обращении. |
| 291 | `coords_from_name(path)` | Координаты из имени файла (lon_min, lat_min, lon_max, lat_max) либо None. |
| 307 | `load_image_rgba(path, max_px)` | Картинка -> (RGBA uint8 (H, W, 4), (исходная ширина, высота)). |
| 358 | **MapImageDialog** | НЕмодальное окно «Своя карта»: файл, координаты углов, применение. |
| 366 | `__init__(parent, start_dir, on_apply, on_clear, current, on_fit)` |  |
| 492 | `_show_anchor_state()` | Подпись под кнопкой: чем сейчас привязана карта. |
| 504 | `_auto_fit()` | Подобрать привязку по слоям и сразу показать результат. |
| 522 | `_run_fit(path, lon_min, lat_min, lon_max, lat_max)` | Собственно подбор. -> True, если получилось; сообщение уже показано. |
| 540 | `_reset_anchor()` | Убрать опорные точки — вернуться к привязке четырьмя числами. |
| 547 | `_build_detail()` | Построить кэш детализации для выбранной карты (см. `build_detail`). |
| 609 | `_pick()` |  |
| 632 | `_sync_detail_button()` | Подпись кнопки под выбранный файл: подготовлена резкость или ещё нет. |
| 648 | `_values()` | Числа из полей либо None, если что-то не заполнено или не число. |
| 658 | `_apply()` |  |
| 712 | `_clear()` |  |
| 717 | `make_image_item()` | Элемент сцены для своей карты. |
| 736 | `place_image(item, rgba, bbox_km)` | Положить картинку в километровый прямоугольник (kx0, kx1, ky0, ky1). |
| 744 | `place_image_affine(item, rgba, coeffs)` | Положить картинку по АФФИННОЙ привязке (`map_anchor.scene_transform`). |


## model/trajectories.py

| стр. | что | зачем |
|---:|---|---|
| 28 | `make_arc(A, B, sagitta, n_points)` |  |
| 49 | `polyline_length(traj)` |  |
| 56 | `arc_length_from_angle(c_len, theta_rad)` |  |
| 63 | `make_arc_by_angle(A, B, theta_deg, n_points)` | Дуга A->B по знаковому касательно-хордовому углу theta (градусы). |
| 89 | `max_deflection_angle(A, B, L_max)` | Предельный угол theta_max (градусы): длина дуги = L_max (бинарный поиск). |
| 102 | `_sample_angle(rng, theta_max, sigma, profile)` | Случайный угол отклонения по профилю разброса. |
| 118 | `sample_arc_trajectory(A, B, L_max, rng, sigma_frac, n_points, …)` | Случайная дуга по профилю. Возвращает (traj, theta_deg). |
| 130 | `arc_fan(A, B, L_max, angle_step_deg, sigma_frac, n_points, …)` | Веер вероятных дуг с шагом угла. Вес = относительная частота при профиле. |
| 165 | `_reach_ellipse_halves(A, B, L_max, n_points)` | Верхняя/нижняя половины эллипса достижимости A<->B (для манёвра). |
| 182 | `corridor_outline(A, B, L_max, traj_model, n_points)` | Верхняя и нижняя границы коридора движения: (upper, lower) — массивы точек. |
| 200 | `corridor_polygon(A, B, L_max, traj_model, n_points)` | Замкнутый многоугольник коридора движения. |
| 206 | `_points_in_polygon(pts, poly)` | Векторизованный точечно-в-многоугольнике (алгоритм лучевого пересечения). |
| 222 | `corridor_filter(pts, A, B, L_max, traj_model, n_points)` | Маска точек, попадающих в коридор движения. |
| 228 | `corridor_bbox(A, B, L_max, traj_model, n_points)` | Габариты коридора (x0, x1, y0, y1) для подгонки масштаба карты. |
| 238 | `ellipse_b(A, B, L_max)` |  |
| 244 | `make_serpentine(A, B, amplitude, n_lobes, phase, n_points)` |  |
| 257 | `max_serpentine_amplitude(ab_distance, L_max, n_lobes, n_points)` | Предельная амплитуда петли (для данного числа лепестков), при которой |
| 273 | `sample_serpentine_trajectory(A, B, L_max, rng, sigma_frac, n_points, …)` | Случайная петляющая траектория по профилю. Возвращает (traj, feature). |
| 301 | `signed_max_lateral(traj, A, B)` |  |
| 312 | `_profile_quantiles(profile, lim, sigma, n, seed)` | n квантилей величины (угла/амплитуды) по профилю разброса. |
| 323 | `_arc_density(theta, profile, theta_max, sigma)` |  |
| 331 | `frequent_arcs(A, B, L_max, profile, sigma_frac, n, …)` | 10 наиболее вероятных дуг (квантили распределения углов по профилю). |
| 343 | `frequent_serpentines(A, B, L_max, profile, sigma_frac, n, …)` | 10 представительных петель (квантили амплитуды по профилю). |
| 358 | `serpentine_fan(A, B, L_max, k, n_points)` | Веер петель: амплитуды от 0 до предельной (крайняя — пунктир). |
| 385 | `turn_radius_km(speed_ms, bank_deg)` | Минимальный радиус координированного разворота R_min = V²/(g·tan φ), км. |
| 411 | `_pick_mode(profile, rng)` | «сбалансированный» = случайный выбор режима каждую итерацию (есть и прямые). |
| 419 | `_resample_polyline(verts, n_points)` | Равномерно по длине пересэмплировать ломаную к n_points точкам. |
| 431 | `_waypoints(S0, B, L_max, rng, mode, r_min, …)` | Путевые точки. Число точек — СЛУЧАЙНО в пользовательском диапазоне nrange |
| 485 | `_round_corners(verts, r_min, turn_frac, n_points)` | Скруглить углы ВИДИМЫМИ дугами: радиус = доля отрезка (turn_frac, пологость), |
| 518 | `_waypoint_path(S0, B, L_max, rng, profile, n_points, …)` | Путь по точкам с укладкой в запас хода (уменьшение выноса до прямой). |
| 547 | `_sine_path(S0, B, L_max, rng, profile, n_points, …)` | Манёвр по СИНУСОИДАЛЬНОМУ закону: боковое отклонение = сумма гармоник |
| 587 | `maneuver_path(S0, B, L_max, rng, profile, n_points, …)` | Манёвр БПЛА. law="points" — путь по ТОЧКАМ со скруглением углов (дуги |
| 597 | `polyline_path(S0, B, L_max, rng, profile, n_points, …)` | Ломаная БПЛА: путь по точкам БЕЗ скругления (угловой манёвр; law не влияет). |
| 604 | `sample_maneuver_trajectory(A, B, L_max, rng, sigma_frac, n_points, …)` | Случайный манёвр A->B (по точкам или по синусу). Возвращает (traj, feature). |
| 612 | `sample_polyline_trajectory(A, B, L_max, rng, sigma_frac, n_points, …)` | Случайная ломаная A->B. Возвращает (traj, feature). |
| 620 | `frequent_maneuvers(A, B, L_max, profile, sigma_frac, n, …)` | n представительных манёвров/ломаных, РАВНОМЕРНО по боковому отклонению (обе |
| 636 | `maneuver_fan(A, B, L_max, profile, n_points, k, …)` | Веер манёвров/ломаных (геометрия пространства манёвров). |
| 653 | `_kmeans(X, k, iters, seed)` | Простой k-means (numpy, без внешних зависимостей). Возвращает (labels, C). |
| 672 | `representative_trajectories(trajectories, n, m, seed)` | 10 наиболее вероятных маршрутов как ОБОБЩЕНИЕ накопленной выборки. |


## model/map_fit.py

| стр. | что | зачем |
|---:|---|---|
| 77 | `_box_blur(a, r)` | Скользящее среднее квадратом 2r+1 через кумулятивные суммы (быстро, без scipy). |
| 92 | `_phase_corr(a, b)` | Фазовая корреляция: (сдвиг строк, сдвиг столбцов, острота пика в сигмах). |
| 120 | `locate(dark, step_px, built_km, area_km, scales, cell_km, …)` | Где на участке лежит карта, если координат нет вовсе. |
| 177 | `locate_candidates(dark, step_px, built_km, area_km, scales, cell_km, …)` | Все правдоподобные положения карты — по одному на пробный масштаб. |
| 221 | `locate_and_fit(dark, step_px, places_km, built_km, area_km, try_n)` | Найти карту на участке и сразу подобрать привязку. -> отчёт как у `fit`. |
| 263 | `layers_for_fit(npz_path)` | Из кэша слоёв участка — точки пунктов и точки застройки, в километрах. |
| 283 | `dark_mask(rgb, threshold)` | Маска «чёрного» на топокарте: кварталы, строения, подписи, ж/д. |
| 290 | `_km_to_px(A_inv, kx, ky)` |  |
| 295 | `_search_one(dark, step, A_inv, cx, cy, tpl_x, …)` | Лучший сдвиг шаблона в окне ±rad_px. -> (dx, dy, доля чёрного, резкость). |
| 317 | `find_pairs(dark, step_px, A, places_km, built_km, search_km, …)` | Найти соответствия «пиксель картинки ↔ километры» по пятнам застройки. |
| 359 | `solve_affine(pairs, tol_km, iters, seed)` | Аффинное «пиксель → км» по парам, устойчиво к промахам (RANSAC + МНК). |
| 399 | `solve_model(P, model)` | Матрица «пиксель → км» по парам `P` (N, 4) для одной из трёх моделей. |
| 423 | `cross_val_m(P, model)` | Ошибка на ОТЛОЖЕННОЙ точке (leave-one-out), метры: медиана и максимум. |
| 463 | `model_is_sane(A)` | Похоже ли преобразование на настоящую карту (а не на переобучение). |
| 476 | `choose_model(P, cover)` | Какая модель лучше описывает ЭТУ карту — по проверке на отложенной точке. |
| 505 | `fit(dark, step_px, A0, places_km, built_km, passes)` | Полный подбор: грубый проход, уточнение, выбор модели по данным. |


## view_qt/geomap.py

| стр. | что | зачем |
|---:|---|---|
| 44 | `km_to_lonlat(x_km, y_km, lon0, lat0)` | Локальные км (восток, север от якоря) -> (долгота, широта). |
| 49 | `lonlat_to_km(lon, lat, lon0, lat0)` | (долгота, широта) -> локальные км (восток, север от якоря). |
| 57 | `deg2num(lon, lat, z)` | (долгота, широта, зум) -> дробные тайловые координаты (xt, yt). |
| 66 | `num2deg(xt, yt, z)` | Дробные тайловые координаты -> (долгота, широта) угла тайла. |
| 115 | `discover_local_layers()` | Папки в кэше, не входящие в TILE_LAYERS (например, ваша 'Sat' с тайлами |
| 143 | `cache_root()` | Каталог дискового кэша тайлов. |
| 172 | `_area_dir(area)` | Имя папки участка: явный аргумент, иначе UAV_TILE_AREA, иначе '' (старая |
| 179 | `_tms_y(z, y)` | XYZ <-> TMS: ось Y инвертирована (TMS считает строки снизу вверх). |
| 184 | `tile_cache_path(layer, z, x, y, ext, area)` | Путь тайла в кэше. y уже в РАСКЛАДКЕ КЭША (если слой TMS — см. _cache_y). |
| 192 | `_cache_y(layer, z, y, layer_tms)` | y из XYZ (как считает вся остальная математика) -> y на диске для этого |
| 201 | `find_cached_tile(layer, z, x, y, area)` | Найти файл тайла в кэше, перебирая известные расширения и XYZ/TMS. |
| 218 | `_sniff_ext(data)` | Расширение по магическим байтам (для корректного имени файла в кэше). |
| 229 | `_decode_tile(data)` | Байты тайла (PNG или JPEG) -> RGBA float [0..1] (H,W,4). |
| 249 | `fetch_tile_bytes(layer, z, x, y, allow_net, timeout, …)` | Байты тайла: сперва оффлайн-кэш (перебор расширений + XYZ/TMS для слоя), |
| 296 | `clear_tile_memory()` |  |
| 305 | `_decoded_tile_u8(layer, z, x, y, allow_net, area)` | Тайл как uint8 RGBA (256,256,4) из памяти/диска/сети, либо None. |
| 332 | `tile_rgba(layer, z, x, y, allow_net, area)` |  |
| 340 | `tile_bounds_km(z, x, y, lon0, lat0)` |  |
| 353 | `tiles_for_box(kx0, kx1, ky0, ky1, z, lon0, …)` |  |
| 368 | `pick_zoom_for_box(kx0, kx1, ky0, ky1, target_px, lon0, …)` |  |
| 377 | `_pick_zoom(lon_min, lon_max, zmin, zmax, target_px, min_tiles_x)` | Зум, при котором тайлы отображаются ~1:1 к экрану (родной размер подписей). |
| 408 | `build_raster_basemap(kx0, kx1, ky0, ky1, layer, allow_net, …)` | Собрать подложку для км-окна [kx0,kx1]×[ky0,ky1]. |
| 496 | `oblast_outline_km(lon0, lat0)` | Прямоугольная рамка охвата области в км (замкнутый контур, для ориентира). |
| 507 | `bbox_lonlat_to_km(bbox_lonlat, lon0, lat0)` | (lon_min, lat_min, lon_max, lat_max) -> (kx0, kx1, ky0, ky1) в локальных км |
| 517 | `graticule_km(kx0, kx1, ky0, ky1, lon0, lat0, …)` | Линии сетки широт/долгот в км для текущего окна. Возвращает список |


## view_qt/view_qt.py

| стр. | что | зачем |
|---:|---|---|
| 32 | `_qcolor(hex_or_rgba, alpha)` |  |
| 39 | `_viridis_qcolor(w)` |  |
| 44 | `_heat_cmap()` |  |
| 53 | **_SpeedAdapter** | Совместимость с контроллером: тот читает view.slider_speed.val. |
| 55 | `__init__(slider)` |  |
| 59 | `val()` |  |
| 63 | **SimulationView** | Qt-представление. Сигнатуры публичных методов совпадают с matplotlib-View. |
| 66 | `__init__(A, B, outline, bbox, params, speed, …)` |  |
| 98 | `_apply_stylesheet()` | Оформление — общее для трёх вкладок (`view_qt/ui_common.py`). |
| 102 | `_build_ui(params, speed, speed_max)` |  |
| 199 | `_header(text)` |  |
| 203 | `_build_law_combo(col, params)` | Выпадающий список «Закон манёвра»: по точкам / по синусу. |
| 219 | `_radio_group(_title, labels_map, active_key, slot)` |  |
| 233 | `_wrap_group(title, _labels_map, grp)` |  |
| 242 | `_active_key(grp)` |  |
| 248 | `_tint(btn, color)` |  |
| 257 | `_build_scene_items()` |  |
| 316 | `_set_flight_visible(vis)` |  |
| 320 | `_clear_list(items)` |  |
| 328 | `get_param_specs()` |  |
| 331 | `set_callbacks(**cbs)` |  |
| 335 | `get_toggles()` |  |
| 340 | `set_running_label(running)` |  |
| 343 | `get_param_values()` |  |
| 350 | `set_param_values(params)` |  |
| 358 | `_on_field_submit()` |  |
| 362 | `_mode_changed(key)` |  |
| 365 | `_traj_changed(key)` |  |
| 368 | `_prof_changed(key)` |  |
| 371 | `update_geometry(A, B, outline, bbox)` |  |
| 384 | `_apply_range(force)` | Подогнать масштаб под текущий bbox (force=True — даже если bbox не менялся). |
| 393 | `showEvent(e)` |  |
| 398 | `resizeEvent(e)` |  |
| | **· слои ·** | |
| 403 | `_render_density(density, show)` |  |
| 413 | `_update_legend(freq, fan, t, flight)` |  |
| 423 | `_render_paths(paths, show)` |  |
| 439 | `_render_sensors(sensors, R)` |  |
| 457 | `_render_static(sensors, R, freq, fan, density, t)` |  |
| | **· кадры ·** | |
| 467 | `draw_clear(freq, fan, density, toggles, title)` |  |
| 474 | `draw_static_frame(sensors, R, freq, fan, density, toggles, …)` |  |
| 479 | `setup_flight(traj, sensors, R, freq, fan, density, …)` |  |
| 492 | `update_flight(j, hud)` |  |
| 501 | `set_metrics(lines)` |  |
| 504 | `flash_title(text, color)` |  |
| 507 | `_set_title(text, color)` |  |
| 510 | `process_pending()` |  |
| 513 | `show()` |  |


## tools/переносимое/codemap.py

| стр. | что | зачем |
|---:|---|---|
| 60 | `find_root(start)` |  |
| 71 | `py_files(root)` |  |
| 79 | `_first_doc_line(node)` |  |
| 85 | `_sig(node)` |  |
| 95 | `scan_file(path)` | [(строка, вид, подпись, назначение)] + список якорей разделов. |
| 120 | `module_name(root, path)` |  |
| 125 | `public_names(root, files)` | {модуль: {имя: [кто импортирует]}} — что реально берут снаружи. |
| 142 | `git_counts(root, path, since_rev, since_date)` | Сколько коммитов трогали путь (весь, после ревизии либо после ДАТЫ). |
| 167 | `verified_since(text)` | Дата в формате git (ГГГГ-ММ-ДД) из отметки «содержание не сверялось с …», либо None. |
| 179 | `last_commit(root, path)` |  |
| 189 | `module_block(root, name, items, anchors, err, pub)` | Раздел карты по одному модулю. |
| 209 | `build(root)` | {имя файла: текст} — README-указатель и части КАРТА_N.md. |
| | **· разложить модули по частям ·** | |
| | **· README: краткое описание и характеристики ·** | |
| 309 | `uncommitted(root)` | Файлы, изменённые в рабочем дереве (правка есть, коммита ещё нет). |
| 344 | `verify_offsets(out_files)` | Сверить строки из README с содержимым частей: на указанной строке обязан стоять |
| 367 | `stale_report(root, docs_dir)` | Документы против кода, КОТОРЫЙ ОНИ САМИ УПОМИНАЮТ. |
| 415 | `main(argv)` |  |


## view/view.py

**Отдаёт наружу:** `PARAM_SPECS`.

| стр. | что | зачем |
|---:|---|---|
| 39 | `fit_axes(ax, bbox, margin)` |  |
| 53 | `draw_static(ax, A, B, outline, bbox)` |  |
| 74 | `draw_density(ax, H, extent)` | Тепловая карта плотности. Слабые значения подняты гаммой и яркой палитрой, |
| 84 | `draw_probable(ax, paths, show_labels)` | Линии маршрутов: вес -> цвет/толщина; предельные (is_extreme) -> пунктир. |
| 102 | `draw_sensors(ax, sensors, R)` |  |
| 112 | **SimulationView** |  |
| 113 | `__init__(A, B, outline, bbox, params, speed, …)` |  |
| 145 | `_on_resize(_event)` |  |
| | **· легенда тепловой карты ·** | |
| 149 | `_build_colorbar()` |  |
| | **· панель ·** | |
| 165 | `_panel_bg()` |  |
| 171 | `_header(x, y, text)` |  |
| 175 | `_labeled_box(x, y, w, h, name, label, …)` |  |
| 186 | `_style_button(btn, color)` |  |
| 191 | `_style_radio(radio)` |  |
| 197 | `_build_controls(params, speed, speed_max)` |  |
| 271 | `_mode_clicked(label)` |  |
| 276 | `_traj_clicked(label)` |  |
| 281 | `_prof_clicked(label)` |  |
| | **· API для контроллера ·** | |
| 287 | `get_param_specs()` |  |
| 290 | `set_callbacks(**cbs)` |  |
| 294 | `get_toggles()` |  |
| 298 | `set_running_label(running)` |  |
| 302 | `get_param_values()` |  |
| 309 | `set_param_values(params)` |  |
| 317 | `update_geometry(A, B, outline, bbox)` |  |
| | **· полная отрисовка ·** | |
| 322 | `_compose(sensors, R, freq, fan, density, t, …)` |  |
| 342 | `draw_clear(freq, fan, density, toggles, title)` |  |
| 349 | `draw_static_frame(sensors, R, freq, fan, density, toggles, …)` |  |
| | **· быстрая анимация (блиттинг) ·** | |
| 358 | `setup_flight(traj, sensors, R, freq, fan, density, …)` |  |
| 379 | `update_flight(j, hud)` |  |
| 397 | `set_metrics(lines)` |  |
| 401 | `process_pending()` | Принудительно отрисовать отложенные изменения (общий метод с Qt-View). |
| 409 | `flash_title(text, color)` |  |
| 413 | `show()` |  |


## tools/reference_cases.py

| стр. | что | зачем |
|---:|---|---|
| | **· ЧЕТЫРЕ ОБСТАНОВКИ ·** | |
| | **· ЭТАЛОН ·** | |
| 190 | `check(ok, what, detail)` |  |
| 197 | `_apply(p, params)` | Обстановку — в параметры. Поля, которых нет в случае, остаются прежними. |
| 203 | `_manual_xy(m, case)` | Заданные человеком позиции этого случая в километрах рамки района. |
| 214 | `_nearest(pts, x, y)` | Расстояние от (x, y) до ближайшей из позиций, км. Пусто — большое число. |
| 222 | `run_case(m, case)` | Один прогон одного случая: расставить датчики и снять показатели. |
| 261 | `main(argv)` |  |


## tools/переносимое/docmap.py

| стр. | что | зачем |
|---:|---|---|
| 55 | `_find_root(markers)` | Корень проекта — ближайшая папка ВВЕРХ, содержащая любой из маркеров. |
| 93 | `norm(p)` |  |
| 97 | `walk_md()` | Все документы проекта (относительные пути от корня). |
| | **· ОГЛАВЛЕНИЯ ·** | |
| 109 | `build_toc(lines, pos)` | Заголовки 2–3 уровня с номерами строк. Внутри ``` не считаются. |
| 148 | `apply_toc(rel, check_only)` |  |
| 172 | `verify_toc(docs)` | Сверка обещанного номера строки с фактическим — по готовому файлу. |
| | **· ССЫЛКИ ·** | |
| 205 | `check_links(docs)` | Битые ссылки между документами. Возвращает список (файл, цель). |
| | **· ТЕКСТ ССЫЛКИ ПРОТИВ ЕЁ ПУТИ ·** | |
| 235 | `check_labels(docs)` | Ссылки, где текст называет папку, отличную от настоящей. Возвращает (файл, текст, путь). |
| | **· §-РАЗДЕЛЫ В ССЫЛКАХ ·** | |
| 274 | `_has_section(text, num)` | Есть ли в документе раздел с таким номером — заголовком либо строкой таблицы. |
| 282 | `check_sections(docs)` | Ссылки с §-номером, ведущие на файл, где такого раздела нет. |
| | **· СТРУКТУРА ·** | |
| 307 | `check_structure(docs)` | Папка с документами обязана иметь README, файл — укладываться в MAX_LINES. |
| 337 | `main()` |  |


## model/area_start.py

**Отдаёт наружу:** `AreaStartModel`.

| стр. | что | зачем |
|---:|---|---|
| 35 | `_frame(A, B)` | Орт оси A->B (u) и нормали (n). |
| 45 | `start_zone_outline(A, B, depth, width, n)` | Контур зоны старта (эллипс с центром в A): глубина вдоль оси, ширина поперёк. |
| 54 | `reach_ellipse_outline(A, B, L_max, n)` | Граница эллипса достижимости A<->B (предел движения по запасу хода). |
| 64 | `reach_envelope_outline(A, B, depth, width, L_max, n_ang)` | Огибающая ДОСТИЖИМОСТИ с учётом ЗОНЫ СТАРТА: объединение эллипсов с фокусами |
| 89 | `sample_start_point(A, B, depth, width, rng)` | Случайная точка ВНУТРИ зоны старта (равномерно в эллипсе). |
| 99 | `_zone_exit_frac(S0, A, B, depth, width)` | Доля пути S0->B, ЗА которой путевые точки гарантированно ВНЕ ЗОНЫ СТАРТА. |
| 122 | `points_outside_start_zone(pts, A, B, depth, width)` | Маска точек ВНЕ зоны старта (эллипс с центром A): True = снаружи. |
| 139 | `sample_area_arc(A, B, depth, width, L_max, rng, …)` | Дуга из случайной точки старта в B (угол по профилю разброса). |
| 151 | `sample_maneuver(A, B, depth, width, L_max, rng, …)` | Манёвр из СЛУЧАЙНОЙ точки старта зоны в B (по точкам или по синусу). |
| 162 | `sample_polyline(A, B, depth, width, L_max, rng, …)` | Ломаная из СЛУЧАЙНОЙ точки старта зоны в B (общее ядро polyline_path). |
| 175 | **AreaStartModel** | Размещение датчиков при старте из зоны и движении к цели B (вкладка 2). |
| 181 | `__init__(params)` |  |
| | **· настройка ·** | |
| 201 | `set_movement(movement)` |  |
| 204 | `set_route(A, B)` | Зафиксировать маршрут (центр зоны старта A и цель B) и сбросить выборку. |
| | **· геометрия ·** | |
| 215 | `corridor_outline()` |  |
| 218 | `corridor_bbox()` |  |
| 221 | `view_bbox()` |  |
| 224 | `start_zone()` |  |
| 228 | `_reachable_mask(P)` | Точки, достижимые из зоны старта при движении в B за <= L_max. |
| 242 | `_build_candidates()` |  |
| 253 | `reset(seed)` |  |
| 288 | `_compute_view_bbox(n, margin)` |  |
| | **· порождение и накопление ·** | |
| 301 | `_sample(rng)` |  |
| 317 | `sample_trajectory()` |  |
| 320 | `_anchor_idx()` | Индекс кандидата для «якорного» датчика у цели B (1/3 R заходит за B). |
| | **· слои ·** | |
| 333 | `frequent_paths(n)` | 10 наиболее вероятных маршрутов. |
| 352 | `fan_paths()` |  |
| 363 | `density_field(nbins)` |  |
| | **· показатели ·** | |
| 376 | `evaluate(sensors, trajectories)` |  |


## tools/pack_area.py

| стр. | что | зачем |
|---:|---|---|
| 53 | `log(msg)` |  |
| 59 | `_files(folder, pattern)` |  |
| 72 | `_part_path(folder, pbf)` |  |
| 85 | `merge_dem(folder, out_name)` |  |
| 157 | `_save_layers(path, layers, packed)` |  |
| 175 | `_load_layers(path)` |  |
| 197 | `_groups(names, jobs)` |  |
| 210 | `_worker(args)` |  |
| 221 | `build_part(pbf, folder, lon0, lat0, bbox, force, …)` |  |
| 259 | `_key(arr)` |  |
| 266 | `_length_km(arr)` |  |
| 272 | `merge_parts(parts, out_path)` |  |
| 314 | `pack_layers(folder, rec, pbf_only, force, jobs)` |  |
| 341 | `main(argv)` |  |


## view_qt/basemap_mixin.py

**Отдаёт наружу:** `TILES_MOSAIC`, `ZOOM_SNAP`.

| стр. | что | зачем |
|---:|---|---|
| 47 | **_BasemapSignals** |  |
| 51 | **_BasemapTask** | Сборка растровой подложки в ФОНОВОМ потоке (чтение/декод/склейка тайлов), |
| 55 | `__init__(req_id, layer, box, target_px, allow_net, lon0, …)` |  |
| 63 | `run()` |  |
| 79 | **BasemapMixin** | Подмешивается к QWidget-представлению карты. Ожидает self.plot/self.pi/self.vb. |
| 90 | `_init_basemap(lon0, lat0, get_layer, get_offline, scheme_points, step_deg, …)` | Инициализация тайловой подложки и офлайн-схемы. |
| 153 | `_set_view_limits(bbox_km, pad_km, min_ref)` | РАЙОН ОТОБРАЖЕНИЯ: рамка участка плюс `pad_km` километров поля вокруг. |
| 192 | `_deep_zoom_span_km()` |  |
| 203 | `_refresh_basemap(force)` |  |
| 255 | `snap_view_to_zoom()` | Подогнать масштаб вида так, чтобы тайлы рисовались ПИКСЕЛЬ В ПИКСЕЛЬ. |
| 290 | `_on_basemap_ready(payload)` |  |
| 307 | `_fade_basemap(img)` | Обесцветить и осветлить тайлы — «выцветшая» подложка под своими слоями. |
| 330 | `_set_scheme_visible(vis)` |  |
| 336 | `_draw_scheme(kx0, kx1, ky0, ky1)` |  |
| 353 | `_draw_scheme_extra(kx0, kx1, ky0, ky1)` |  |
| 356 | `_set_scheme_extra_visible(vis)` |  |
| 360 | `gm_qcolor(hex_or_rgba, alpha)` |  |


## main_qt.py

| стр. | что | зачем |
|---:|---|---|
| 26 | `_ensure_qt_plugin_path()` | Указать Qt путь к платформенным плагинам PyQt5. |
| 49 | `_resource_path(rel)` | Путь к файлу-ресурсу и в обычном запуске, и в собранном .exe. |
| 63 | `_make_splash(QtWidgets, QtGui, QtCore, screen_size)` | Заставка на `SPLASH_SECONDS` секунд. None, если картинки нет. |
| 182 | `selftest()` | Самопроверка данных: `UAV_SELFTEST=1` перед запуском — программа печатает, что |
| 259 | `main()` |  |


## tools/flow_check.py

| стр. | что | зачем |
|---:|---|---|
| 43 | `check(ok, what, detail)` | Записать результат проверки; возвращает ok, чтобы можно было ветвиться. |
| 51 | `step(fn, *a, **kw)` | Выполнить шаг и вернуть (результат, секунды). |
| 58 | `main(argv)` |  |


## view_qt/tile_layer.py

**Отдаёт наружу:** `MAX_ITEMS`.

| стр. | что | зачем |
|---:|---|---|
| 45 | `set_span_x(vb, want, y_center)` | Выставить диапазон по X ФАКТИЧЕСКИ, а не «попросить». |
| 75 | **SteppedViewBox** | ViewBox, у которого колесо мыши меняет масштаб ровно ВДВОЕ — на один уровень тайлов. |
| 88 | `wheelEvent(ev, axis)` |  |
| 107 | `_zoom_axis(lo, hi, at, factor)` |  |
| 116 | **_TileImage** | Картинка тайла, которая рисуется СО СГЛАЖИВАНИЕМ. |
| 125 | `paint(p, *args)` |  |
| 134 | **_TileSignals** |  |
| 138 | **_TileTask** | Чтение и декод ОДНОГО тайла в фоновом потоке. |
| 144 | `__init__(layer, z, x, y, allow_net, area, …)` |  |
| 150 | `run()` |  |
| 162 | **TileLayer** | Подложка из отдельных тайлов поверх `PlotItem`. |
| 168 | `__init__(pi, lon0, lat0, get_layer, get_offline, area, …)` |  |
| 197 | `set_view(box_km, width_px)` | Пересчитать, какие тайлы нужны для видимого окна. |
| 238 | `set_visible(on)` | Показать или спрятать всю подложку (режим «только своя карта»). |
| 244 | `clear()` | Убрать все тайлы со сцены — при смене слоя или участка. |
| 254 | `_on_tile(payload)` | Тайл доехал из потока: положить в кэш и на сцену, если он ещё нужен. |
| 273 | `_place(key, img)` | Положить готовую картинку тайла в его километры. |
| 286 | `_z_for(z)` |  |
| 295 | `_restack()` | Переставить глубину всех тайлов после смены зума: текущий — наверх. |
| 300 | `_new_item()` | Новый элемент сцены под один тайл. |
| 308 | `_drop_extra(need, z)` | Убрать то, что уже не нужно: чужой зум и уехавшие за край тайлы. |
| 327 | `_recycle(key)` | Снять тайл со сцены, элемент оставить в пуле. |


## model/optimization.py

**Отдаёт наружу:** `CoverageCache`.

| стр. | что | зачем |
|---:|---|---|
| 27 | `_popcount(a)` |  |
| 41 | `filter_not_past_target(pts, A, B, max_s)` | Убрать кандидатов ЗА целью B (проекция на ось A->B больше max_s·\|AB\|). |
| 55 | `candidate_grid(A, B, L_max, step, corridor, traj_model)` | Сетка кандидатных позиций (M, 2). |
| 79 | **CoverageCache** | Инкрементальная структура покрытия по накопленной выборке маршрутов. |
| 88 | `__init__(candidates, R, L_seg, k, placed)` |  |
| 106 | `n_traj()` |  |
| 109 | `_add_placed_row(traj)` | Вклад УЖЕ ПОСТАВЛЕННЫХ датчиков (других типов) в покрытие этой траектории. |
| 134 | `add_trajectory(traj)` |  |
| 154 | `routes_covered_by_candidate()` | Сколько маршрутов выборки засекает КАЖДЫЙ кандидат — вектор (C,). |
| 162 | `greedy(N, weights, anchors, anchor_sep, min_sep)` | Жадный выбор N позиций. Возвращает до min(N, C) датчиков. |
| 284 | `set_weighted_cells(cells_xy, cells_w, block)` | Задать взвешенные клетки-цели. Строит булеву матрицу покрытия |
| 300 | `greedy_weighted(N, weights, min_sep)` | Жадная weighted-max-coverage по клеткам. weights=(a1,a2): a1 — кратность |


## view_qt/area_view.py

**Отдаёт наружу:** `AreaStartView`.

| стр. | что | зачем |
|---:|---|---|
| 30 | **AreaStartView** |  |
| 32 | `__init__(A, B, outline, bbox, params, speed, …)` |  |
| 47 | `get_param_specs()` |  |
| 50 | `_move_changed(key)` |  |
| 53 | `_build_zone_combo(col, params)` | Выпадающий список «Точки маршрута»: в зоне старта / только за зоной. |
| 69 | `_build_map_combo(col, params)` | Выпадающий список «Карта»: схема (оффлайн) / OSM / топо / спутник / тёмная |
| 100 | `_map_changed(i)` |  |
| 105 | `_map_offline_changed(_state)` |  |
| | **· панель вкладки 2 ·** | |
| 111 | `_build_ui(params, speed, speed_max)` |  |
| | **· сцена: карта-подложка (общий BasemapMixin), зона старта, ось, клик ·** | |
| 206 | `_build_scene_items()` |  |
| 229 | `_draw_scheme_extra(kx0, kx1, ky0, ky1)` |  |
| 233 | `_set_scheme_extra_visible(vis)` |  |
| 236 | `_mark_ab(pts, kinds)` | Красивые маркеры точек: A — зелёный кружок (центр зоны), B — красная |
| 247 | `update_geometry(A, B, outline, bbox)` |  |
| 254 | `_set_kursk_view(half)` | Начальный вид: Курск в центре, окно ~±half км. |
| 258 | `showEvent(e)` |  |
| | **· постановка маршрута кликами ·** | |
| 268 | `begin_create()` |  |
| 273 | `prompt_create()` |  |
| 288 | `_on_scene_click(ev)` |  |

