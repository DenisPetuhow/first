# Структура проекта: файлы, классы, функции

Карта кода в архитектуре **MVC** (Model–View–Controller). Теория и формулы —
в `МЕТОДИЧКА.md`; этапы, проблемы и история — в [журнале](журнал/README.md)
(живая часть 5, части 1–4 в архиве; вход через указатель).

Два интерфейса на **общей** модели (`model/`) и общей логике контроллера:
* matplotlib — `main.py`, `view/`, `controller/` (вкладка 1);
* PyQt5 + pyqtgraph — `main_qt.py`, `view_qt/`, `controller_qt/` (вкладки 1, 2, 3).

Вся документация — в папке `теория/`: общее в корне, остальное по папкам, у каждой
свой `README.md`. Начинать поиск с [теория/README.md](README.md).

---

<!-- ОГЛАВЛЕНИЕ · сгенерировано tools/переносимое/docmap.py — руками не править -->
> **Оглавление** — номер строки · раздел. Открывать нужный раздел, а не файл
> целиком (`Read` с `offset`). Пересобрать: `python tools/переносимое/docmap.py`.
>
> `  40` Дерево файлов
> ` 171` config.py
> ` 186` model/geometry.py
> ` 195` model/trajectories.py
> ` 231` model/detection.py
> ` 240` model/optimization.py
> ` 252` model/simulation.py — SimulationModel (вкладка 1)
> ` 262` model/area_start.py — AreaStartModel (вкладка 2)
> ` 283` view/view.py — matplotlib (вкладка 1)
> ` 292` view_qt/view_qt.py — Qt/pyqtgraph (вкладка 1, базовый класс)
> ` 299` view_qt/area_view.py — Qt вкладки 2 (наследник)
> ` 308` view_qt/geomap.py — карта (гео/тайлы/кэш)
> ` 319` controller/controller.py — SimulationController
> ` 326` controller_qt/controller_qt.py — QtSimulationController
> ` 330` controller_qt/area_controller.py — AreaStartController
> ` 338` Вкладка 3 · цифровая карта угроз (новые модули)
> ` 453` main.py / main_qt.py
> ` 464` Поток данных (одна итерация)
<!-- /ОГЛАВЛЕНИЕ -->

## Дерево файлов

```
uav_sensor_mvc/
├── config.py                 параметры (Params), режимы, подписи, тема
├── main.py                   запуск matplotlib-версии (вкладка 1) + CLI
├── main_qt.py                запуск Qt-версии (две вкладки в QTabWidget)
├── model/
│   ├── geometry.py           эллипс достижимости, гео-привязка
│   ├── trajectories.py       генераторы маршрутов БПЛА, коридор, физика R_min
│   ├── detection.py          обнаружение и метрики (эталонные реализации)
│   ├── optimization.py       сетка кандидатов, CoverageCache (+ weighted-coverage),
│   │                         жадный выбор
│   ├── route_model.py        ОБЩИЙ базовый класс моделей вкладок 1 и 2 (RouteModelBase:
│   │                         накопление выборки → жадная расстановка; DRY)
│   ├── simulation.py         модель сцены вкладки 1 (точка A → цель B; наследник базы)
│   ├── area_start.py         модель сцены вкладки 2 (зона старта → цель B; наследник базы)
│   ├── threat_grid.py        ВКЛАДКА 3: весовая сетка 500 м, наложение слоёв,
│   │                         вода, исключение городов, расстановка (ThreatGrid/ThreatModel)
│   └── threat_routes.py      ВКЛАДКА 3: огибающая (2 поля Дейкстры) + стохастич. маршруты
│                             (Монте-Карло: блуждание по коридорам, RDP + Катмулл-Ром)
├── view/view.py              представление matplotlib (вкладка 1)
│   geo_frame.py             КООРДИНАТНЫЙ ФРЕЙМ: единственное место, где градусы
│                            переводятся в километры (и обратно) + формат координат
│                            для показа. Раньше формула жила ДВУМЯ копиями — в
│                            threat_grid.py и geomap.py (план 8, задача 8.3)
├── view_qt/
│   ├── view_qt.py            представление Qt/pyqtgraph (вкладка 1, базовое)
│   ├── area_view.py          представление Qt вкладки 2 (наследник, + карта)
│   ├── basemap_mixin.py      общий слой тайловой подложки (+ пределы обзора)
│   ├── threat_view.py        ВКЛАДКА 3: карта угроз (слои, тепло весов, датчики)
│   ├── map_image.py          СВОЯ КАРТА КАРТИНКОЙ: чтение .png/.jpg, привязка по
│   │                         координатам углов, окно ввода. Кладётся поверх слоёв
│   │                         (Z = −19, ПОД слоями), на расчёт не влияет (план 8, 8.5)
│   └── geomap.py             карта: гео-привязка, тайлы, кэш, оффлайн-схема
│   ui_common.py             ОБЩИЕ детали трёх вкладок: тёмная тема (apply_dark_theme)
│                            и правая панель с прокруткой (make_side_panel).
│                            Были скопированы в каждую вкладку — правка не доходила
├── tools/                    УТИЛИТЫ командной строки (интерфейс их не вызывает).
│   │                         Что когда запускать — tools/README.md
│   ├── download_tiles.py     предзагрузка тайлов в оффлайн-кэш
│   ├── build_threat_grid.py  офлайн: OSM (.osm.pbf) → слои geo_cache/ для вкладки 3
│   │                         ⚠️ ПЕРЕСОБИРАТЬ после правки PLACE_CODES: кэш хранит
│   │                         уже закодированный тип пункта (ОГРАНИЧЕНИЯ.md 3.10г)
│   ├── inspect_tile_cache.py разбор ЧУЖОЙ папки тайлов: XYZ или TMS, форматы
│   ├── reference_run.py      КОНТРОЛЬНЫЙ ПРОГОН: все показатели §6 и сверка их
│   │                         с эталоном участка — «сошлось» либо список расхождений
│   ├── area_bounds.py        границы .osm.pbf и рельефа, рамка с отступом внутрь,
│   │                         готовая строка bbox=(…) для config.THREAT_AREAS
│   ├── tile_coverage.py      покрытие кэша тайлами по рамке участка, по зумам
│   │                         ↑ шесть скриптов ВЫШЕ знают про эту программу (сетка,
│   │                         тайлы, .osm.pbf) — в другом проекте бесполезны
│   └── переносимое/          ПЕРЕНОСИМЫЕ: про документацию вообще, не про БПЛА.
│       │                     Копируются в любой проект; корень ищут по маркеру
│       │                     (CLAUDE.md/config.py), а не счётом уровней вверх
│       ├── README.md         пояснение ко всем пяти + что отсеивают фильтры
│       ├── docmap.py         оглавления с номерами строк, битые ссылки, структура
│       ├── check_names.py    имена из текстов против имён в коде (шаг 1 сверки)
│       ├── check_numbers.py  числа из текстов против config.py (шаг 2 сверки)
│       ├── check_claims.py   документы против друг друга (шаг 6): «сделано» ↔ «задел»
│       ├── check_dead_code.py функции без единого вызова и параметры, которые никто
│       │                     не читает (шаг 7): от кода к коду, а не от текстов
│       ├── codemap.py        КАРТА КОДА: оглавление исходников (строка · сигнатура ·
│       │                     назначение) + --stale: какие документы отстали от кода
│       └── make_figures.py   23 рисунка пособия: КАЖДЫЙ считается по той же формуле,
│                             что записана в тексте → картинка не расходится с ней
├── check_env.py              проверка окружения: какие библиотеки есть и каких нет
├── controller/controller.py  контроллер (анимация, события, метрики)
├── controller_qt/
│   ├── controller_qt.py      контроллер Qt (таймер QTimer)
│   ├── area_controller.py    контроллер вкладки 2
│   └── threat_controller.py  ВКЛАДКА 3: построить карту / расставить датчики
├── docs/
│   ├── Теория_и_математика.docx   развёрнутая теория (Word)
│   ├── make_theory_docx.py        генератор docx
│   └── make_user_guide_docx.py    методичка ПОЛЬЗОВАТЕЛЯ для передачи заказчику
│                                  (кладётся внутрь демонстрационной сборки .exe)
├── теория/                   ВСЯ документация. В КОРНЕ — общее для всех вкладок,
│   │                         в папках — по темам; у каждой папки свой README.md
│   ├── README.md                  ГЛАВНЫЙ указатель: с чего начинать поиск
│   ├── карта_кода/                ОГЛАВЛЕНИЕ ИСХОДНИКОВ — генерируется codemap.py
│   │   ├── README.md              сводка: модули, размеры, в какой части искать
│   │   └── КАРТА_N.md             сами таблицы «строка · сигнатура · назначение»
│   ├── ОГРАНИЧЕНИЯ.md             ЕДИНЫЙ справочник правил модели — ЧИТАТЬ ПЕРЕД
│   │                              правкой логики (датчики, маршрут, НП, рельеф);
│   │                              §6 — эталон контрольного прогона
│   ├── МЕТОДИЧКА.md               математика вкладок 1–2
│   ├── МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md  СВОДНО: все модели движения/маршрутов
│   │                                 вкладок 1/2/3 + расстановка + формулы
│   ├── переносимое/               ПРАВИЛА ДЛЯ ЛЮБОГО ПРОЕКТА (копируется целиком
│   │                              вместе с tools/переносимое/): контекст и навигация,
│   │                              оглавления и поиск, сверка с кодом, план внедрения,
│   │                              разрешения на команды (5), работа с git (6)
│   ├── пособие/                   УЧЕБНОЕ: объясняет, а не документирует
│   │   ├── 1_ТЕОРИЯ_ОПТИМИЗАЦИИ.md    критерий/показатель, субмодулярность, гарантия
│   │   │                              (1−1/e), §7 случайность и риск, §9 постановка
│   │   ├── 2_ГЕОМЕТРИЯ_И_ОБОЗНАЧЕНИЯ.md  §1 язык записи (множества, интегралы, ∇,
│   │   │                              вероятность, O-нотация), далее геометрия задачи
│   │   ├── 3_ОБЗОР_ВКЛАДКИ_1_2.md     постановка «старт → цель» целиком
│   │   ├── 4_ОБЗОР_ВКЛАДКА_3.md      постановка «известна только местность» целиком
│   │   └── рис/                       23 SVG, пересобираются make_figures.py
│   ├── основа/                    исходники пособия (.doc/.docx) — АРХИВ, устарели
│   ├── Стиль оформления/          как ПИСАТЬ статьи: регистр, шаблоны постановки
│   ├── СТРУКТУРА_ПРОЕКТА.md       этот файл
│   ├── журнал/                    ЧТО И КОГДА МЕНЯЛИ — открывать только через
│   │   ├── README.md              указатель: тема → пункты → строки
│   │   └── ЖУРНАЛ_5.md            ЖИВАЯ часть, пункты 193–208 (629 строк): участок,
│   │                              сектор, два типа датчиков, сборки .exe, подписи;
│   │                              в конце — сводные разделы 4–7 по всему журналу
│   │                              (части 1–4 закрыты и лежат в архив/журнал/)
│   ├── вкладка_3/                 карта угроз: механика весов, рельеф, история
│   │   ├── МЕТОДИЧКА_КАРТА_УГРОЗ.md      главный документ вкладки
│   │   ├── ВКЛАДКА_3_КАК_РАБОТАЕТ.md     то же простыми словами
│   │   ├── РАДИОВИДИМОСТЬ_ЗАДЕЛ.md       физика скрытности, отклонённые варианты
│   │   ├── МЕТОДИЧКА_ВКЛАДКА3_ДОРАБОТКИ.md  что было/исправлено/почему
│   │   ├── ГРАДАЦИЯ_СПИСОК_ПРОВЕРКИ.md   список НП для сверки глазами
│   │   └── СТАТЬЯ_МАТЕРИАЛЫ.md     ЗАГОТОВКА ДЛЯ СТАТЬИ: цель, задачи,
│   │                                    формулы, метод (1−1/e), функции, замеры.
│   │                                    Самостоятельный документ, в навигацию
│   │                                    не встроен — материал для публикации
│   ├── карты/                     подложка, тайлы, где брать данные
│   ├── интерфейс/                 настройка вида руками + СБОРКА_EXE.md
│   ├── планы/                     что делаем дальше (действующий — 4_ПЛАН_ДОРАБОТКИ.md)
│   └── архив/                     ВЫПОЛНЕННЫЕ планы: хранятся ради записанных
│                                  в них ОТКЛОНЁННЫХ вариантов
├── README.md
└── requirements.txt
```

---

## config.py

* `MODES`, `MODE_LABELS` — режимы оптимизации и веса (α₁, α₂).
* `TRAJ_LABELS` — модели движения вкладки 1 (дуги/петли/манёвр).
* `AREA_TRAJ_LABELS` — модели движения вкладки 2 (дуга из старта/ломаная/манёвр).
* `MANEUVER_LAW_LABELS` — закон манёвра: «по точкам» / «по синусу».
* `WAYPOINT_ZONE_LABELS` — точки маршрута: «в зоне старта» / «только за зоной».
* `MOTION_PROFILES`, `MOTION_LABELS` — профили разброса (амплитуды).
* `THEME` — цветовая тема.
* `Params` (dataclass) — все входные параметры (см. таблицу §10 методички), в т.ч.
  `speed_kmh`, `bank_deg`, `n_min/n_max`, `maneuver_law`, `waypoint_zone`. Свойства
  `A`, `B`; метод `validate()`.

---

## model/geometry.py

* `ellipse_geometry(A,B,L_max)` — центр, полуоси a,b,c, угол эллипса достижимости.
* `point_in_ellipse / points_in_ellipse(P,A,B,L_max)` — проверка принадлежности.
* `auto_axes_limits(geom, margin)` — границы осей карты.
* `geo_to_local_km(A_geo,B_geo)` — перевод широта/долгота → локальные км.

---

## model/trajectories.py

Геометрия дуг:
* `make_arc_by_angle(A,B,θ,n)` — дуга по касательно-хордовому углу θ.
* `arc_length_from_angle`, `max_deflection_angle` — длина дуги и предельный угол θ_max.
  (`_sagitta_from_angle` — построение через стрелу прогиба — **удалена**: дуга при θ > 90°
  вырождалась в малую, заменена прямой параметризацией `make_arc_by_angle`.)
* `_sample_angle(rng,θ_max,σ,profile)` — случайный угол по профилю.
* `sample_arc_trajectory(...)` — случайная дуга; `arc_fan`, `frequent_arcs` — веер/частые.

Коридор движения:
* `corridor_outline / corridor_polygon / corridor_filter / corridor_bbox` — границы и фильтр.
* `_reach_ellipse_halves` — половины эллипса (для манёвра).

Петли/змейки:
* `make_serpentine`, `max_serpentine_amplitude`, `sample_serpentine_trajectory`,
  `frequent_serpentines`, `serpentine_fan`.

Физика и манёвр по точкам/синусу:
* `turn_radius_km(V,φ)` — **R_min = V²/(g·tan φ)** (км).
* `_WP_MODE`, `_BALANCED_P`, `_pick_mode` — параметры режимов разброса (амплитуда ρ,
  пологость, чередование) и выбор «смешанного».
* `_waypoints(S0,B,L_max,rng,mode,r_min,nrange,s_min)` — путевые точки: число из
  nrange, продольная стратификация, боковой вынос-линза с базой `b` (полу-малая
  ось эллипса); сложный — 1–2 крупных лепестка к краю; `s_min` — нижняя доля.
* `_round_corners(verts,r_min,turn_frac,n)` — скругление углов дугами ≥ R_min.
* `_waypoint_path(...,rounded,s_min)` — путь по точкам с укладкой в L_max.
* `_sine_path(...)` — манёвр по гармоникам с ограничением кривизны ≤ 1/R_min.
* `maneuver_path(...,law,s_min)` — диспетчер: «sine» → `_sine_path`, иначе по точкам.
* `polyline_path(...,s_min)` — путь по точкам без скругления.
* `sample_maneuver_trajectory`, `sample_polyline_trajectory`, `frequent_maneuvers`,
  `maneuver_fan` — сэмплеры/слои.
* `SAMPLERS` — словарь сэмплеров по ключу модели.

---

## model/detection.py

* `n_detections(traj,sensors,R)` — кратность n (число засёкших датчиков).
* `continuous_coverage(traj,sensors,R)` — доля длины пути под наблюдением.
* `segment_coverage(traj,sensors,R,L_seg)` — доля покрытых сегментов m/L_seg.
* `utility(...)` — полезность φ (формула 1); `avg_utility(...)` — среднее по выборке.

---

## model/optimization.py

* `filter_not_past_target(pts,A,B,max_s)` — убрать кандидатов ЗА целью B.
* `candidate_grid(A,B,L_max,step,corridor,traj_model)` — сетка кандидатов в коридоре.
* `CoverageCache` — структура покрытия:
  * `add_trajectory(traj)` — добавить маршрут (hit/segmask/covfrac по кандидатам);
  * `greedy(N,weights,anchor_idx,anchor_sep)` — жадный выбор N позиций с якорем и
    развязкой; вторичный критерий распределения при насыщении основного.
* `greedy_placement(...)` — обёртка «список маршрутов → размещение».

---

## model/simulation.py — `SimulationModel` (вкладка 1)

Старт — точка A. `reset()` считает `r_min` (из ТТХ) и фильтрует кандидатов
(`filter_not_past_target`). `sample_trajectory` → сэмплер по `traj_model` с
`r_min`, `(n_min,n_max)`, `maneuver_law`. `recompute_placement` — жадный выбор с
якорем у B (`_anchor_idx`, `anchor_sep=1.35R`). `step / run_batch / evaluate /
compare_modes / frequent_paths / fan_paths / density_field`.

---

## model/area_start.py — `AreaStartModel` (вкладка 2)

Старт — случайная точка `S0` из эллипса зоны. Векторная геометрия и условия:
* `_frame(A,B)` — орты оси и нормали; `start_zone_outline`, `reach_ellipse_outline`;
* `reach_envelope_outline(A,B,depth,width,L_max)` — огибающая достижимости с учётом
  зоны старта (объединение эллипсов S0,B; маршруты не выходят за контур);
* `sample_start_point(A,B,depth,width,rng)` — точка старта в эллипсе зоны;
* `_zone_exit_frac(S0,A,B,depth,width)` — доля выхода из зоны (опорная проекция,
  формула 14) → `s_min` для путевых точек «только за зоной»;
* `points_outside_start_zone(pts,A,B,depth,width)` — маска кандидатов ВНЕ эллипса
  зоны (датчики в зоне не размещаются);
* `sample_area_arc / sample_maneuver / sample_polyline` — генераторы маршрутов;
  `zone_exclude` решает, ставить ли точки только за зоной (по `waypoint_zone`).

Класс `AreaStartModel`: `set_route(A,B)` (запоминает направление `_dir`),
`reset()` (B = A + |AB|·_dir — следует за полем; фильтрует кандидатов: не за B,
вне зоны старта И не за её задней кромкой 180°), `_sample` (диспетчер + флаг
зоны), `_anchor_idx`, `recompute_placement`, `step / run_batch / evaluate / слои`.

---

## view/view.py — matplotlib (вкладка 1)

`PARAM_SPECS` — единый список параметров. `SimulationView`: построение панели,
карта, анимация полёта (блиттинг), слои, метрики; контракт методов
(`update_geometry`, `draw_clear`, `draw_static_frame`, `setup_flight`,
`update_flight`, `set_metrics`, `get/set_param_values`, `get_toggles`).

---

## view_qt/view_qt.py — Qt/pyqtgraph (вкладка 1, базовый класс)

`QT_EXTRA` — доп. поля Qt (скорость, крен, точек от/до). `SimulationView(QWidget)`:
тот же контракт методов, аппаратная анимация, ColorBarItem + LegendItem,
`_build_law_combo` (список «Закон манёвра»), `showEvent/resizeEvent` →
`_apply_range(force=True)`.

## view_qt/area_view.py — Qt вкладки 2 (наследник)

`AREA_PARAM_SPECS` (+ глубина/ширина зоны). `AreaStartView(SimulationView)`:
панель вкладки 2; кнопка «Создать маршрут» и постановка кликами
(`_on_scene_click`, `begin_create`, `prompt_create`); `_build_zone_combo` (список
«Точки маршрута»), `_build_map_combo` (список «Карта»); подложка-карта
(`basemap` + схема `graticule/oblast/cities`, `_refresh_basemap`); отрисовка зоны
старта (эллипс) и оси A→B; callbacks `on_zone`, `on_map_layer`.

## view_qt/geomap.py — карта (гео/тайлы/кэш)

Гео-привязка к Курску (`km_to_lonlat`/`lonlat_to_km`), slippy-математика
(`deg2num`/`num2deg`), слои `TILE_LAYERS`/`LAYER_LABELS`, дисковый кэш
(`cache_root`, `fetch_tile_bytes` cache-first→сеть), ограничение зума
`ZOOM_MIN..ZOOM_MAX` (10 уровней), сборка подложки `build_raster_basemap`,
оффлайн-схема (`KURSK_CITIES`, `oblast_outline_km`, `graticule_km`).
Подробно — в `МЕТОДИЧКА_КАРТА.md`.

---

## controller/controller.py — `SimulationController`

Связывает модель и представление: события виджетов (`on_apply`, `on_mode`,
`on_traj`, `on_profile`, `on_law`, `on_speed`, `on_toggle`), анимация (`_tick`,
блиттинг), пакетный расчёт, панель метрик (`_update_metrics_idle/full`).
`_make_timer()` — фабрика таймера (переопределяется в Qt).

## controller_qt/controller_qt.py — `QtSimulationController`

Наследник: таймер на `QTimer` (`_make_timer`), адаптация под Qt-представление.

## controller_qt/area_controller.py — `AreaStartController`

Наследник Qt-контроллера для вкладки 2: создание маршрута кликами (`on_create`,
`on_route_ready`), выбор модели движения (`on_movement`), выбор зоны точек
(`on_zone`), блокировка «Пуск/Шаг/Пакетно» до создания маршрута, метрики зоны.

---

## Вкладка 3 · цифровая карта угроз (новые модули)

Подробная механика — в `теория/МЕТОДИЧКА_КАРТА_УГРОЗ.md`. Кратко по коду:

* `config.py` — **`THREAT_AREAS`** (реестр участков: границы + исходник `.osm.pbf` +
  файл слоёв `.npz` + рельеф + точки входа/цели) и **`THREAT_AREA`** (активный ключ);
  из него берутся `THREAT_BBOX_POINTS/LONLAT`, `THREAT_LAYERS_FILE`,
  `THREAT_SOURCE_FILE`, `THREAT_DEM_FILE`, `THREAT_ENTRY/TARGET`. Далее —
  `THREAT_CELL_M` (500 м), `THREAT_LAYERS` (слои и веса), `THREAT_LAYER_ORDER`,
  `THREAT_INTERSECTION_BONUS` (funnel), `THREAT_WATER_BUFFER_M` (запрет датчика на
  воде), **`THREAT_SECTOR_*`** (сектор появления БПЛА: полураствор 30°, 5 точек входа,
  разнос, ширина полосы края); поля `Params.threat_*`.
* `model/threat_grid.py`:
  * `ThreatGrid` — сетка 500 м; `add_line_layer` (вклад ∝ длине пересечения /
    размер ячейки), `add_area_layer` (застройка), `mark_water` (маска запрета +
    буфер), `add_intersection_bonus` (funnel),
    `flat_cells`, `save_npz`/`load_npz` (растровое хранение);
  * **рельеф**: `add_relief_layer` — превышение над районом (15 км) и окрестностью
    (2 км), вклад МНОЖИТЕЛЕМ к положительному весу + пол + отсечка «очень высокая
    гора»; применяется ПОСЛЕДНИМ, после населённых пунктов; `_relief_f` (асимметричный
    отклик с насыщением), `relief_height`/`relief_k`/`relief_cut` (доступ для показа),
    `_erode` (эрозия с паддингом «занято» — для замыкания коридоров);
  * **населённые пункты**: `apply_settlements` (город запрещён и обнулён, деревня —
    градиент штрафа, свободные зоны у вылета и цели), `place_masks_from_polygons`
    (контуры НП из OSM), `_classify_components` (тип по тегу `place` + площадь),
    `_village_field` (поле штрафа), `_free_zone_mask`, `_label8` (связные компоненты);
  * **мосты**: `set_bridge_mask_from_layer` (ячейки переправ из слоя `bridge`),
    `bridge_points_km` (точные координаты маркеров — середины мостов над водой);
  * `_accumulate_segment/_accumulate_polyline` — точная длина пересечения линии с
    ячейками (эталон); `_rasterize_polylines` — быстрый векторный вариант (рабочий);
  * `load_layers` — реальные слои OSM из файла слоёв АКТИВНОГО УЧАСТКА ИЛИ
    синтетическая СХЕМА (демо, офлайн); `layers_cache_path` — путь к этому файлу,
    у каждого участка свой; `area_cache_dir` / `area_file` — папка участка
    `geo_cache/<dir>/` и поиск файла в ней с откатом в корень кэша;
  * **высоты**: `load_dem_grid` (GeoTIFF через rasterio или сырой SRTM `.hgt`, высота
    ячейки = СРЕДНЕЕ по ячейке), `_sample_geotiff` + `_block_mean_at` (усреднение через
    интегральное изображение по растру), `_sample_hgt`, `dem_files` (поиск файлов),
    `load_dem_display` (окно растра в родном разрешении — только для отрисовки);
  * `ThreatModel` — обёртка для контроллера: `build`, `candidate_positions`
    (сетка минус вода), `place_sensors`, `plan_routes`, `iter_batch`, `metrics`,
    `entry_target_km`, `set_target` (пересобирает карту — свободная зона у цели),
    `set_relief`/`has_dem`/`relief_display` (кнопка «Добавить рельеф»),
    `_route_gap_km`/`_route_slack_km` (разрыв и уход от ориентира — оба в ключе кэша
    контекста выборки).
* `model/optimization.py` — `CoverageCache.set_weighted_cells` / `greedy_weighted`:
  обобщение жадного алгоритма на «покрытие веса клеток» (weighted max coverage, T=1).
* `view_qt/basemap_mixin.py` — `BasemapMixin`: тайловая подложка + `_set_view_limits`
  (жёсткие пределы пана/зума bbox); переиспользуется вкладкой 3.
* `model/threat_grid.py` — также: `layers_from_osm` (чтение `.osm.pbf` → слои,
  единый код для CLI и окна выбора), `load_layers(data_path)` (источник .npz/.osm.pbf/
  авто), `ThreatModel.set_data_source/set_enabled_layers/set_target`,
  `build_threat_grid(enabled=…)` (набор включённых слоёв).
* **СЕКТОР ПОЯВЛЕНИЯ БПЛА** (`model/threat_grid.py`): `ThreatModel.set_sector` /
  `clear_sector` (клик по краю карты), `compute_entry_points` (отбор пяти точек входа
  по весу с разносом), `sector_edges_km` (границы для отрисовки), `entry_points_km` /
  `entries_target_km` (список входов для маршрутов) против `entry_target_km`
  (представитель — для показателей и правил датчиков), `target_only_km`.
  В `model/threat_routes.py`: `_as_entry_list`, `ctx["entries"]` — надстройки по
  точкам входа в `build_iter_context`; розыгрыш старта — в `sample_one_route`.
  Интерфейс: `threat_view.py` :: `btn_sector`, `_begin_sector`, `render_sector`,
  чекбоксы `chk_entries` / `chk_sector_lines`; контроллер: `on_set_sector` /
  `on_clear_sector`, отложенный пересчёт по «Применить» (`_pending_recalc`).
* **ЧЕТЫРЕ ТИПА ДАТЧИКОВ** (с 05.09.2026, задача 8.7): типы 1–3 — малые
  (`ThreatModel.sensors`, `_place_by_routes`, `_place_multi_type`), тип 4 — большие,
  кольцевой щит у цели (`sensors_big`, `_place_big`). Параметры собирает
  `model/sensors.py` :: `sensor_types(p)` из полей `Params` (`threat_N`/`threat_R` —
  тип 1, `threat_N2`/`threat_R2` — тип 2, `threat_N3`/`threat_R3` — тип 3,
  `threat_N_big`/`threat_R_big` — тип 4; `threat_k_same` — общая кратность).
  Датчики, поставленные человеком, — `ThreatModel.manual_sensors`
  (`model/sensors.py` :: `ManualSensor`), они же служат якорями в расстановке.
  Интерфейс: немодальное окно `view_qt/input_window.py` :: `InputDataWindow`
  (кнопка `btn_input_data`, `_open_input_window`, `refresh_sensors_dialog`), постановка
  мышью — `btn_pick_sensor`, `_begin_pick_sensor`; отрисовка — `render_sensors(...)`
  с цветами `THREAT_COLORS["sensor"]`, `sensor_t2`, `sensor_t3`, `sensor_big`;
  контроллер: `on_sensors_apply`, `on_manual_add`, `_sensor_rows`.
* `model/threat_routes.py` — `passable_mask(grid, max_gap_km, slack_km)`: сшивка провалов
  ЗАМЫКАНИЕМ (дилатация + эрозия — не расширяет коридор) и отдельный буфер ухода от
  ориентира; `_lookahead_fields` — 8 полей «что ждёт впереди по этому курсу» (предвидение
  препятствия, поднимает горизонт решения с 500 м до 2 км), используется в `_walk_field`;
  `flight_envelope`/`build_iter_context` принимают `slack_km`.
* `view_qt/threat_view.py` — `ThreatMapView`: ограниченная карта, векторные слои,
  тепловой слой весов, датчики, вход/цель, чекбоксы показа; кнопки «Выбрать цифровые
  карты…», **«Добавить рельеф»**, «Указать цель» (клик), «Сброс»; `DigitalMapsDialog` —
  окно выбора источника данных и слоёв.
  **Рельеф:** `render_relief` (карта высот: шкала `RELIEF_RAMP` по перцентилям +
  `_hillshade` — теневая отмывка), `render_relief_priority` (множитель: синим укрытия,
  красным возвышенности, чёрным отсечка), `set_relief_button` (подпись/доступность);
  два `ImageItem` с Z = −9.0 и −8.5 — под весовой картой и векторными слоями.
* `controller_qt/threat_controller.py` — `ThreatController`: «Построить карту» →
  наложение слоёв; «Расставить датчики» → `greedy_weighted`; `on_choose_data`
  (источник+слои), `on_set_target` (цель кликом), `on_reset`; показатели по слоям;
  `on_relief` (переключение рельефа с пересборкой карты и сбросом выборки),
  `_relief_lines` (блок показателей по рельефу).
* `view_qt/basemap_mixin.py` — общий слой тайлов; хуки `_draw_scheme_extra`/
  `_set_scheme_extra_visible` (вкладка 2 рисует рамку области), `step_deg`.
* `tools/build_threat_grid.py` — тонкая CLI-обёртка над `layers_from_osm`:
  `.osm.pbf` → файл слоёв активного участка. Без аргументов берёт исходник и приёмник
  из `THREAT_AREAS[THREAT_AREA]`; принимает и явные `<исходник> [имя.npz]`.
* `tools/переносимое/docmap.py` — обслуживает САМУ документацию, а не данные: пересобирает блоки
  `<!-- ОГЛАВЛЕНИЕ -->` с номерами строк в документах длиннее 300 строк. Идемпотентен
  (повторный запуск не меняет ни одного файла); `--check` возвращает 1, если оглавление
  устарело, — им заканчивается проверка на достоверность.
* `tools/переносимое/make_figures.py` — 23 рисунка пособия в `теория/пособие/рис/*.svg`.
  Каждый **считается по той же формуле, что записана в тексте** (убывающая отдача — жадным
  покрытием реального облака точек, `θ_max` — бинарным поиском, `R_min` — по формуле
  крена), поэтому рисунок не может разойтись с формулой. `--list` — что есть и где
  используется.
* ⚠️ Все пять переносимых скриптов ищут корень проекта **подъёмом до ближайшей папки с
  маркером** (`CLAUDE.md`/`config.py`), а не счётом уровней вверх: при переносе папки
  счётчик врёт молча — скрипт запускается и проверяет не те файлы.
* Документация по данным: `теория/МЕТОДИЧКА_ЗАГРУЗКА_КАРТ.md` (где/как скачать со
  ссылками, отображение, расширение на вкладку 2, смена виджета).

---

## main.py / main_qt.py

* `main.py` — запуск matplotlib-версии (вкладка 1) + разбор CLI-аргументов.
* `main_qt.py` — запуск Qt-версии: `QTabWidget` с **тремя** вкладками (A→B, зона
  старта → цель, карта угроз); модель/вид/контроллер для каждой;
  `_ensure_qt_plugin_path()` (фикс плагинов на Windows); **заставка при запуске** —
  `_make_splash` (эмблема + подписи, параметры `config.SPLASH_*`) и `_resource_path`
  (путь к ресурсу и в обычном запуске, и в собранном `.exe` через `sys._MEIPASS`).

---

## Поток данных (одна итерация)

```
sample_trajectory() ─► маршрут γ
        │
        ▼
CoverageCache.add_trajectory(γ)   (hit / segmask / covfrac по кандидатам)
        │
        ▼
recompute_placement()  = greedy(N, weights, якорь у B, развязка)
        │
        ▼
View: анимация полёта + датчики + метрики
```

Накопление `T` итераций → выборочное среднее F_t стабилизируется → расстановка
датчиков перестаёт меняться.
