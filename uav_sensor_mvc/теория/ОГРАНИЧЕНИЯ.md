# Ограничения и правила модели — единый справочник

**Зачем этот файл.** Здесь в одном месте собрано ВСЁ, что программа обязана соблюдать:
где можно и нельзя ставить датчики, куда может и не может лететь БПЛА, как обходятся
города, что происходит у точки вылета и у цели. Каждое правило — со значением, местом в
коде и объяснением, почему оно такое.

**Как пользоваться.** Меняете правило — правьте параметр в `config.py` (не в коде) и
дописывайте строку сюда. Если правило нарушилось на прогоне — смотрите столбец «где
задано» и идите туда. Контрольный прогон проверяет отмеченные ✓ правила автоматически.

Связанные документы: механика весов — [`МЕТОДИЧКА_КАРТА_УГРОЗ.md`](МЕТОДИЧКА_КАРТА_УГРОЗ.md);
модели маршрутов и расстановки — [`МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md`](МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md);
история решений — [`ЖУРНАЛ_ПРОЕКТА.md`](ЖУРНАЛ_ПРОЕКТА.md).

---

## 1. ДАТЧИКИ — где ставить нельзя и почему

| № | Правило | Значение | Где задано | Почему |
|---|---|---|---|---|
| 1.1 | ✓ **Не ставить на воду** и в буфер вокруг русла | буфер 250 м | `THREAT_WATER_BUFFER_M`, `threat_exclude_water` | датчик физически негде разместить; при этом вода остаётся «плюсом» для маршрута — двойная роль |
| 1.2 | **Только между вылетом и целью** по оси A→B | доля пути 0…1 | `_place_by_routes` | датчик за целью или позади старта не перехватывает никого |
| 1.3 | **Два якорных датчика** — у вылета и у цели | ~0.45·R внутрь коридора | `_place_by_routes` | рубеж на входе и рубеж у объекта; из ближних берётся тот, что накрывает больше маршрутов, чтобы не «залипали» в самих точках |
| 1.4 | **Разнос между датчиками** | желаемый 1.6·R, минимум `threat_min_sep_frac`·R (0.75·R) | `_place_by_routes` | перекрытие зон ≤ ~10 %, датчики не кучкуются. Если при желаемом разносе не набирается N — ослабляется до минимума |
| 1.5 | ✓ **Не ставить бесполезные** (не ловящие ни одного пролёта) | третичный ключ в жадном выборе | `CoverageCache.greedy` | без него при насыщении критериев выбор брал первого кандидата по порядку массива — 4 из 12 датчиков уходили в угол карты |
| 1.6 | **Кандидаты — узлы сетки** | шаг `threat_cand_step_km` = 3 км | `candidate_positions` | перебирать континуум невозможно; 3 км при R=12 км даёт достаточную свободу |
| 1.7 | **Ставятся по МАРШРУТАМ, а не по весу карты** | — | `place_sensors` | вес показывает, где местность удобна для полёта вообще; ловить надо конкретные пролёты из точки вылета в цель. Замер: частота пролёта под датчиком выше средней в 2.0 раза, вес — лишь в 1.3 |
| 1.8 | Число, радиус, кратность | N=12, R=12 км, k=3 | `threat_N`, `threat_R`, `threat_k` | задаётся в интерфейсе |
| 1.9 | **Пересчёт по ходу итераций** | каждые `THREAT_SENSOR_REFRESH_EVERY` = 5 накопленных маршрутов | `_maybe_refresh_sensors` | раньше пересчёт был только в конце прогона, и датчики казались неподвижными. Видно, как расстановка сходится: сдвиг между обновлениями падает с ~5 км до нуля к 30-й итерации |

---

## 2. МАРШРУТ БПЛА — куда лететь можно и нельзя

| № | Правило | Значение | Где задано | Почему |
|---|---|---|---|---|
| 2.1 | ✓ **Не длиннее запаса хода** | `threat_L_max`; авто = max(\|AB\|·1.25, кратчайший·1.35) | `sync_auto_L_max`, `THREAT_LMAX_*` | физическое ограничение топлива/заряда |
| 2.2 | ✓ **Не выходить за область залёта** | огибающая по двум полям Дейкстры | `flight_envelope` | всё, что вне, недостижимо при данном запасе хода. Проверяется и после сглаживания линии |
| 2.3 | ✓ **Не проходить через город** | маска `_urban` | `apply_settlements` | над плотной застройкой БПЛА не идёт (см. §3) |
| 2.4 | **Пустое место перелетается, если короче разрыва** | `threat_max_gap_km` = 2 км | `build_iter_context` | между коридорами бывают провалы; короткий БПЛА перелетит, длинный — коридор разорван. Управляет и областью залёта, и набором маршрутов ещё ДО итераций |
| 2.5 | **Плавность: длина прямого участка** | `threat_turn_interval_km` = 8 км | `sample_one_route` | маршрут не дёргается на каждой ячейке |
| 2.6 | **Радиус разворота** | из скорости и крена: `threat_speed_kmh` 180 км/ч, `threat_bank_deg` 25° | R_min = V²/(g·tan φ) | физика полёта |
| 2.7 | **Сглаживание не имеет права срезать угол** через город или за область | подбор допуска | `_smooth_safe` | рисуется и считается по СГЛАЖЕННОЙ линии; раньше она срезала углы в застройку (828 ячеек вне области, 2721 точка в непроходимом) |
| 2.8 | **Профиль расхода запаса хода** | `late` / `early` / `even` | `threat_spend`, `_spend_tau` | БПЛА может вилять, пока есть запас, а на исходе идти прямо — или наоборот |
| 2.9 | ✓ **Не уходить назад от старта** дальше лимита | `START_BACK_KM` = 1 км | `_walk_field` (по `axis_s`) | немного назад в начале нормально — БПЛА отходит от точки вылета и ищет коридор; дальше это уже «полетел не туда» (было до 9.8 км, на карте — полоса в обратную сторону) |
| 2.10 | ✓ **У цели не отворачивать** от неё | зона `FINAL_APPROACH_KM` = 10 км, допуск `FINAL_DRIFT_KM` = 0.25 км за шаг | `_walk_field` (по `dgoal`) | подошёл к объекту — заходит на него, а не разведывает местность (было: 19 % маршрутов, подойдя на 15 км, снова удалялись до 5.3 км). Обход по дуге разрешён — запрещено именно удаление |
| 2.11 | **Заход ЗА цель с возвратом — РАЗРЕШЁН** | VIA до `VIA_BEYOND_KM` = 10 км за целью | `build_iter_context` | облететь объект и зайти с другой стороны — штатный манёвр, а не ошибка |
| 2.12 | **VIA-точка не позади старта и не «сбоку у цели»** | позади ≤ `VIA_BEHIND_KM` = 1 км; в 10 км от цели — только если ЗА целью | `build_iter_context` | VIA позади старта уводила маршрут в обратную сторону, VIA сбоку у цели заставляла дёрнуться в пяти километрах от объекта |

### 2.1 Два понятия, без которых правила 2.9–2.12 непонятны

**Что такое VIA-точка.** Если строить маршрут просто «из старта в цель», все маршруты
лягут почти по одной линии — разброса не будет. Поэтому программа заранее раскидывает
по карте два десятка промежуточных точек («via» = «через») и строит путь в два участка:
**старт → VIA → цель**. Выбор VIA и задаёт разброс: ближняя к прямой даёт почти прямой
маршрут, дальняя — широкий обход и заход с другой стороны. Это и есть механизм за
списком **«разброс по карте»** в панели итераций (центр / середина / края / смесь).
Подробно — [МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md, «Как устроен разброс: VIA-точки»](МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md).

Отсюда и дефект: точки раскидываются по всей проходимой территории, и часть из них
оказалась **позади старта** и **за целью**. Маршрут честно шёл к такой точке — на карте
это выглядело как «БПЛА полетел не туда». Поэтому ограничивать пришлось не только само
движение (правила 2.9–2.10), но и отбор точек (2.11–2.12): позади старта — нельзя, за
целью — можно, потому что это осмысленный манёвр захода с другой стороны.

**Два разных расстояния до цели.** Их легко перепутать, и на этом я один раз ошибся:

| | Что это | Пример |
|---|---|---|
| **Прямое** (`dgoal`) | по линейке на карте, как видит глаз | 8 км |
| **По коридорам** (`gt`) | сколько реально лететь с учётом обхода городов, запретов и разрывов | 15 км |

Правило «рядом с целью не отворачивать» сначала было написано через расстояние по
коридорам. Из-за этого БПЛА в восьми километрах от цели по прямой считался «ещё
далеко» (по коридорам-то 15 км), ограничение не включалось — и на карте было видно,
как он отворачивает у самого объекта. После перевода правила на **прямое** расстояние
оно стало срабатывать там, где это видно глазу.

---

## 3. ГОРОДА, ДЕРЕВНИ, ЗАСТРОЙКА

| № | Правило | Значение | Где задано | Почему |
|---|---|---|---|---|
| 3.1 | **ГОРОД: пролёт запрещён** | `place` = city/suburb/borough, либо town с пятном ≥ `THREAT_TOWN_CITY_KM2` (4 км²) | `apply_settlements` | над городом коммерческий БПЛА не идёт |
| 3.2 | **В городе вес ВСЕХ слоёв = 0** (не минус) | `THREAT_CITY_ZERO_WEIGHT` | там же | отрицательный вес делал город «ямой», от которой разбегались датчики; запрет держит маска, а не знак веса |
| 3.3 | **Буфер облёта города** | `THREAT_URBAN_BUFFER_KM` = 0.4 км | там же | не лететь впритык. Держится малым: буфер растёт нелинейно и склеивает города в агломерацию (0.5 км → 17.7 % участка, 1.5 → 29.5 %, 2.0 → 34.9 %) |
| 3.4 | **ДЕРЕВНЯ: пролёт разрешён**, но вес падает к центру | −20 в центре, −10 на половине радиуса, 0 на границе | `THREAT_VILLAGE_*` | село у трассы не должно рвать коридор, но над центром лететь незачем — маршрут сам идёт по окраине |
| 3.5 | **Хутор и мельче — не учитывается** | `place` = hamlet | `_classify_components` | одиночные дома на маршрут не влияют |
| 3.6 | **Река сквозь населённый пункт — коридор**, пока НП меньше порога | `THREAT_RIVER_CORRIDOR_MAX_KM2` = 20 км² | `apply_settlements` | по руслу БПЛА идёт даже через село; у крупного города русло закрывается вместе с ним |
| 3.7 | **Запасной критерий, если метки `place` нет** | пятно застройки ≥ `THREAT_URBAN_MIN_AREA_KM2` (8 км²) | `_classify_components` | в OSM размечены не все НП |
| 3.8 | **Контур НП засчитывается только при реальной застройке внутри** | ≥ 15 % площади или ≥ 2 км² | `PLACE_BUILT_FRAC`, `PLACE_BUILT_MIN_KM2` | границы в OSM часто административные (сельсовет с полями): без проверки под запрет уходило 30 % участка, а соседнее село становилось «городом» |
| 3.9 | **Слишком большой контур игнорируется** | > `PLACE_POLY_MAX_KM2` (120 км²) | `_places_from_osm` | это район или община, а не населённый пункт |

---

## 4. СВОБОДНЫЕ ЗОНЫ — где ограничения §3 НЕ действуют

Запрет города нужен для **транзита** (не лететь над городом по пути), но не для взлёта
и подлёта: у старта БПЛА обязан быть, к цели обязан прийти. Обе точки, как правило,
стоят в населённых пунктах — без послабления вокруг них 100 % ячеек в радиусе 3 км
запрещены, и подлёт к цели возможен лишь с одной стороны.

| № | Точка | Что освобождается | Где задано |
|---|---|---|---|
| 4.1 | **Вылет** | круг `THREAT_ENTRY_FREE_KM` = 10 км вокруг точки, и только он | `_free_zone_mask` |
| 4.2 | **Цель** | круг `THREAT_TARGET_FREE_KM` = 3 км **плюс весь населённый пункт цели**, расширенный на 3 км | там же |

Почему у цели иначе: при цели в центре города круга мало — края НП остались бы
закрытыми, и подлёт снова стал бы односторонним. Почему у вылета НЕ так: расширение
всего НП на радиус зоны освобождало соседние города за десятки километров (без минуса
оказывались Лисичанск, Тошковка, Гірське — к точке вылета отношения не имеют).

В свободной зоне не действует **ничего** из §3: ни запрет пролёта, ни обнуление весов,
ни штраф деревни, ни буфер облёта соседнего города. Занимает ~9.5 % участка (678 км²).

> Зона привязана к цели, поэтому смена цели кликом **пересобирает карту**
> (`set_target` → `build`).

---

## 5. ДАННЫЕ И КАРТА

| № | Правило | Значение | Где задано | Почему |
|---|---|---|---|---|
| 5.1 | **Участок фиксирован** | lon 38.375–39.654, lat 48.335–49.016 (94×75 км) | `THREAT_BBOX_LONLAT` | демонстрационный район |
| 5.2 | **Ячейка сетки** | 500×500 м (189×151 = 28 539 ячеек) | `THREAT_CELL_M` | компромисс точности и скорости |
| 5.3 | **Геометрия обрезается по участку** | +10 км запаса | `OSM_CLIP_MARGIN_KM` | объекты приходили до ±94 км и уходили за край карты; на границе строится точка пересечения с рамкой, а не внешняя вершина |
| 5.4 | **Мост — реальный объект OSM** | `bridge=yes/viaduct` | `OSM_LAYER_FILTERS` | раньше мост вычислялся как пересечение реки с дорогой и появлялся там, где его нет |
| 5.5 | **Маркер переправы — на самом мосту** | середина линии моста над водой | `bridge_points_km` | центр ячейки давал смещение до 250 м, заметное при приближении |
| 5.6 | **Рельеф: превышение над окрестностью**, не абсолютная высота | окно 3 км, вклад ±8, насыщение 40 м | `THREAT_DEM_*` | участок может лежать на плато; низина даёт плюс, холм минус |
| 5.7 | **Нет файла высот — слой не участвует** | `geo_cache/dem.tif` или `*.hgt` | `load_dem_grid` | программа работает и без рельефа |

---

## 6. ЧТО ПРОВЕРЯЕТ КОНТРОЛЬНЫЙ ПРОГОН

Отмеченные ✓ правила проверяются численно после каждой значимой правки. Эталон на
реальных данных OSM (участок Северодонецк → Луганск):

| Показатель | Норма |
|---|---|
| Маршрутов построено | 250 из 250 заказанных |
| Итераций накоплено | 150 из 150 |
| Датчиков поставлено | 12 из 12 |
| Выходов маршрута за область залёта | **0** |
| Превышений запаса хода | **0** |
| Датчиков на воде | **0** |
| Датчиков, не ловящих ни одного пролёта | **0** |
| Засечка пролётов с кратностью ≥ k(3) | 100 % |
| Средняя кратность засечки | ~8 |
| Доступность цели по сторонам | 92–97 % со всех четырёх |
| Уход назад от старта | не глубже ~1.3 км (лимит 1 км + допуск сглаживания) |
| Отворот от цели на прямом маршруте | ≤ 0.2 км |

Если любой из нулей стал ненулевым — сломано соответствующее правило из §1–§4.

---

## 7. ЧЕГО В МОДЕЛИ ПОКА НЕТ (осознанные упрощения)

* **Высота полёта** — модель плоская, БПЛА не «перепрыгивает» препятствия.
* **Разнотипные датчики** — у всех одинаковый радиус R.
* **Погода, время суток, помехи** — не учитываются.
* **Ограничение по времени полёта** — считается только длина пути, не длительность.
* **Реакция БПЛА на обнаружение** — маршрут не меняется от того, что его засекли.
