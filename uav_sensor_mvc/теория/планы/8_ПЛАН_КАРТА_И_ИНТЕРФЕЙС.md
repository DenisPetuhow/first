# План 8 — карта, координаты, интерфейс, история полётов

> **Что это.** Второй действующий план, рядом с [7_ЕДИНЫЙ_ПЛАН.md](7_ЕДИНЫЙ_ПЛАН.md).
> План 7 — про **качество модели** (метод расстановки, рельеф, пороги). План 8 — про
> **карту, координаты и интерфейс**: откуда берётся местность, в чём считаются позиции,
> как их задать, выгрузить и посмотреть. Пересечение одно — задача 8.1 забирает себе
> пункт «разложить тайлы по участкам» из [плана 7, З6](7_ЕДИНЫЙ_ПЛАН.md).
>
> Задание заказчика 03.09.2026. Составлен по правилу
> [7_СОСТАВЛЕНИЕ_ПЛАНА](../переносимое/7_СОСТАВЛЕНИЕ_ПЛАНА.md): сперва разведка кода,
> все точки правки — с файлом, функцией и номером строки; догадки помечены явно.

---

<!-- ОГЛАВЛЕНИЕ · сгенерировано tools/переносимое/docmap.py — руками не править -->
> **Оглавление** — номер строки · раздел. Открывать нужный раздел, а не файл
> целиком (`Read` с `offset`). Пересобрать: `python tools/переносимое/docmap.py`.
>
> `  97` 0. Ответы на вопросы заказчика (до задач)
>   `  99` 0.1. Да, всё уже привязано к координатам — через один общий км-фрейм
>   ` 134` 0.2. Три формата данных карты и чем каждый читается
>   ` 156` 0.3. Зумы: что такое z15 и сколько это метров
>   ` 175` 0.4.  Главное ограничение: район больше ~150 км ломает совмещение слоёв
> ` 201` 1. Границы работы
> ` 222` 2. Карта кода: точки правки
> ` 251` 3. Порядок работ и зависимости
>   ` 270` 3.1. Что сделано на 05.09.2026 и что идёт следующим
> ` 298` Задача 8.1 — тайлы: скачать запас и разложить по участкам
>   ` 300` 8.1.1. Что требуется
>   ` 313` 8.1.2. Координаты и объём — посчитано
>   ` 346` 8.1.3. Команды скачивания
>   ` 380` 8.1.4. Справка: почему не качаем область целиком
>   ` 408` 8.1.5. Раскладка кэша по участкам  СДЕЛАНО 03.09.2026
>   ` 433` 8.1.6. Ловушки
>   ` 444` 8.1.7. Проверка
> ` 452` Задача 8.2 — «Задать район моделирования» мышью  СДЕЛАНО 04.09.2026
>   ` 508` 8.2.1. Что требуется
>   ` 519` 8.2.2. Как решать
>   ` 545` 8.2.3. Ловушки
>   ` 560` 8.2.4. Проверка
> ` 568` Задача 8.3 — координатная система: единый модуль и показ по клику  СДЕЛАНО 04.09.2026
>   ` 586` 8.3.1. Что требуется
>   ` 602` 8.3.2. Как решать
>   ` 618` 8.3.3. Ловушки
>   ` 629` 8.3.4. Проверка
> ` 637` Задача 8.4 — масштаб как на топографических картах (1 : 50 000)
>   ` 639` 8.4.1. Что требуется
>   ` 649` 8.4.2. Как решать
>   ` 672` 8.4.3. Ловушки
>   ` 686` 8.4.4. Проверка
> ` 694` Задача 8.5 — своя картинка карты поверх слоёв  СДЕЛАНО 05.09.2026 (довесок: точки мышью)
>   ` 726` 8.5.0а. РЕЗКОСТЬ РАСТРА ПРИ ПРИБЛИЖЕНИИ  (05.09.2026)
>   ` 770` 8.5.0. Векторная карта SVG вместо растра  СДЕЛАНО 05.09.2026
>   ` 822` 8.5.0б. ВЕКТОРНАЯ КАРТА — ДВЕ РАЗНЫЕ ВЕЩИ, и выбрать надо сознательно
>   ` 873` 8.5.1. Что требуется
>   ` 882` 8.5.2. Как решать
>   ` 903` 8.5.3. Ловушки
>   ` 918` 8.5.4. Проверка
> ` 926` Задача 8.6 — история полётов: выгрузка и загрузка   СДЕЛАНО 06.09.2026
>   ` 937` 8.6.1. Что требуется
>   ` 952` 8.6.2. Формат файла
>   ` 975` 8.6.3. Как решать
>   ` 993` 8.6.4. Ловушки
>   `1006` 8.6.5. Проверка
>   `1012` 8.6.6. Итог — как сделано на самом деле  06.09.2026
>   `1046` 8.6.7. Обратная связь заказчика (06.09.2026) — четыре правки после первого прохода
> `1084` Задача 8.7 — «Исходные данные», четыре типа датчиков, таблица и ручная постановка   СДЕЛАНО 05.09.2026
>   `1108` 8.7.1. Что требуется (уточнено заказчиком 04.09.2026)
>   `1188` 8.7.1б. Что делает кнопка «Расставить датчики» — УТОЧНЕНО заказчиком 05.09.2026
>   `1237` 8.7.1а. Компоновка окна — УТВЕРЖДЕНА заказчиком 04.09.2026
>   `1276` 8.7.2. Что уже есть — и что из этого уходит
>   `1288` 8.7.3. Ключевое решение: статический датчик — это якорь
>   `1303` 8.7.4. Как решать — по шагам
>   `1339` 8.7.5. Ловушки
>   `1358` 8.7.6. Порядок работы (первым делом — модель)
>   `1369` 8.7.7. Формат файла датчиков (общий на чтение и запись)
>   `1419` 8.7.8. Проверка
> `1435` Задача 8.8 — выгрузка датчиков и номера на карте   В ОСНОВНОМ СДЕЛАНО 05.09.2026
>   `1437` 8.8.1. Что требуется
>   `1443` 8.8.2. Как решать
>   `1470` 8.8.3. Ловушки
>   `1478` 8.8.4. Проверка
> `1485` Задача 8.9 — анализ размещения: стрелки и устойчивые номера   СДЕЛАНО 06.09.2026
>   `1487` 8.9.1. Что требуется
>   `1497` 8.9.2. Ответ: возможно, и правильный способ — не трогать алгоритм
>   `1514` 8.9.3. Как решать
>   `1542` 8.9.4. Ловушки
>   `1555` 8.9.5. Проверка
>   `1567` 8.9.6. Задел: подключить scipy, если он понадобится не ради одной функции
>   `1586` 8.9.7. Итог — как сделано на самом деле  06.09.2026
> `1647` Задача 8.10 — смена проекции (ЗАДЕЛ, в объём не входит)
> `1664` 4. Контрольные проверки (после каждой задачи)
> `1676` 5. Открытые вопросы (у каждого — умолчание, работа не встанет)
> `1692` 6. Что обновить в документации по завершении
<!-- /ОГЛАВЛЕНИЕ -->

## 0. Ответы на вопросы заказчика (до задач)

### 0.1. Да, всё уже привязано к координатам — через один общий км-фрейм

Вопрос был: «тайлы и векторные слои всё же отображаются в координатах, или как; как
векторный слой привязывается к карте местности».

**Ответ: да, привязано, и привязка одна на всё.** В проекте есть единственная система
отсчёта — **локальные километры** (км-фрейм). Всё остальное в неё переводится:

| Что | Как хранится | Как попадает на экран |
|---|---|---|
| **векторные слои** (реки, дороги, ЛЭП, застройка, НП) | (lon, lat) в `.osm.pbf` → при загрузке переводятся в км | `layers_from_osm` → `_ll` → `lonlat_to_km` |
| **сетка весов** 500 м | км: `ox`, `oy`, шаг `h`, индексы ячеек | `ThreatGrid.cell_centers_km`, `extent_km` |
| **маршруты, датчики, зоны, сектор** | км, массивы `(N, 2)` | напрямую, это уже км |
| **рельеф** (GeoTIFF/`.hgt`) | (lon, lat) в растре → выборка в центрах ячеек | `load_dem_grid` → `_sample_geotiff` |
| **тайлы подложки** | Web Mercator, `{z}/{x}/{y}` | углы мозаики → `lonlat_to_km` → `ImageItem.setRect` |

Ключевые функции перевода — **две пары, в двух местах** (это дублирование, см. ловушку):

* `model/threat_grid.py:89` `lonlat_to_km(lon, lat, lon0, lat0)` и `:1509` `km_to_lonlat`;
* `view_qt/geomap.py:44` `lonlat_to_km` и `:38` `km_to_lonlat` — то же самое для показа.

**Якорь** км-фрейма — `bbox_anchor_lonlat()` (`threat_grid.py:106`): **юго-западный угол
участка**, поэтому координаты идут 0…95.94 и 0…54.93 км без отрицательных значений
([ОГРАНИЧЕНИЯ 5.1г](../ОГРАНИЧЕНИЯ.md)).

Формула (обе реализации одинаковы):

```
x_км = (lon − lon0) · 111.32 · cos(lat0)
y_км = (lat − lat0) · 111.32
```

Это **равнопромежуточная проекция** (equirectangular) с масштабом, взятым на опорной
широте. Она верна по расстояниям локально — и в этом её предел, см. 0.4.

### 0.2. Три формата данных карты и чем каждый читается

| Слой | Формат на диске | Чем читается | Где в коде |
|---|---|---|---|
| **подложка** (фон) | растровые тайлы PNG/JPEG 256×256, схема `{z}/{x}/{y}` | `urllib` + распаковка вручную, декод — `PIL` | `geomap.py:204` `fetch_tile_bytes`, `:184` `_decode_tile` |
| **векторные слои** | `.osm.pbf` (Protocol Buffers, формат OSM) | **`pyrosm`** → `geopandas`/`shapely` | `threat_grid.py:1351` `layers_from_osm` |
| то же, кэш | `.npz` (архив массивов numpy) — выжимка уже в км | `numpy` | `threat_grid.py:1759` `_load_layers_npz` |
| **рельеф** | GeoTIFF `.tif` либо сырой SRTM `.hgt` | **`rasterio`** (GeoTIFF), свой разбор (`.hgt`) | `threat_grid.py:1515` `_sample_geotiff`, `:1579` `_sample_hgt` |

**Библиотеки отображения** (что чем рисуется на экране):

| Библиотека | Роль | Чем именно рисует |
|---|---|---|
| **PyQt5** | окно, панели, диалоги, события мыши | `QWidget`, `QPushButton`, `QDialog` |
| **pyqtgraph** | вся графика карты поверх Qt | `ImageItem` (растры: подложка, веса, рельеф, тепловые), `PlotDataItem` (линии: маршруты, слои, рамки), `ScatterPlotItem` (точки: датчики, переправы), `TextItem` (подписи НП) |
| **numpy** | все массивы и растеризация | сетка, маски, покрытие |
| `shapely` | геометрия при разборе OSM | склейка `way` в линии (`linemerge`) |

Отдельной ГИС-библиотеки (QGIS, folium, Leaflet) в проекте **нет** и не планируется:
всё рисуется своими средствами в км-фрейме. Отвергнутые варианты — в
[МЕТОДИЧКА_КАРТА §11](../карты/МЕТОДИЧКА_КАРТА.md).

### 0.3. Зумы: что такое z15 и сколько это метров

z — уровень детализации тайловой пирамиды. На широте Плесецка (62.8°):

| z | м/пиксель | размер тайла | что видно |
|---:|---:|---:|---|
| 7 | 559 | 143 км | область целиком |
| 10 | 69.9 | 17.9 км | район |
| 12 | 17.5 | 4.5 км | посёлки, крупные дороги |
| 13 | 8.7 | 2.2 км | улицы |
| 14 | 4.4 | 1.1 км | здания |
| **15** | **2.18** | **0.56 км** | дома, номера |
| 17 | 0.55 | 0.14 км | разметка |
| 19 | 0.14 | 0.03 км | предел OSM |

**z15 — не предел, а наш потолок.** У OSM тайлы есть до z19. В коде потолок задан
`geomap.py:97` `ZOOM_MAX = 15` (env `UAV_ZOOM_MAX`), нижняя граница считается как
`ZOOM_MAX − 9` = 6, то есть кэш всегда 10 уровней. Причина потолка — объём (0.3).

### 0.4. ⚠️ Главное ограничение: район больше ~150 км ломает совмещение слоёв

Замер 03.09.2026 (скрипт разовый, формула — в 0.1). Подложка кладётся на карту **по
углам** (`ImageItem.setRect`), а внутри картинки пиксели распределены по Меркатору;
векторные слои — по равнопромежуточной формуле. Разница копится к середине картинки:

| Район | Высота по широте | Сдвиг подложки относительно векторов |
|---|---:|---:|
| участок сейчас (`plesetsk_wide`) | 55 км | **117 м** — меньше четверти ячейки, незаметно |
| участок + 100 км | 255 км | **2.5 км** — больше радиуса малого датчика (2 км) |
| Арх. обл. + 100 км | 1035 км | **43 км** — карта и данные расходятся полностью |

**Что это значит для задания.** «Загрузить всю область и работать по ней» в нынешней
системе координат невозможно: посёлок на подложке и посёлок в векторном слое окажутся в
разных местах. Отсюда разделение, принятое в плане:

* **обзорная карта** (вся область, только смотреть, ничего не считать) — допустима;
* **район моделирования** (где сетка, маршруты, датчики) — **не больше ~150 км**
  по широте, иначе сдвиг превысит радиус датчика.

Снять ограничение можно только переходом на настоящую проекцию (Web Mercator для всего,
либо UTM/Гаусса-Крюгера по зонам) — это отдельная большая работа, вынесена в
**задачу 8.10** как задел, в текущий объём не входит.

---

## 1. Границы работы

| ✅ Меняется | ❌ НЕ меняется |
|---|---|
| подложка: запас тайлов, раскладка кэша по участкам | архитектура MVC: `model/` без Qt, `view_qt/` без расчётов |
| задание района моделирования мышью, блокировка интерфейса до задания | формат данных участка: `.osm.pbf` → `.npz` → DEM |
| показ координат, масштаб, оси | **метод расстановки датчиков** — это [план 7, З2](7_ЕДИНЫЙ_ПЛАН.md), здесь не трогаем |
| своя картинка-карта поверх слоёв | генерация маршрутов и физика полёта |
| окно ввода данных, типы датчиков (3 малых + 1 большой) | сектор появления и пять точек входа |
| выгрузка/загрузка истории полётов, выгрузка датчиков | вкладки 1 и 2 — их интерфейс и логика |
| номера датчиков и показ их перемещения | км-фрейм как способ счёта (замена проекции — задел 8.10) |

⚠️ **Отдельно не трогаем:**

* **готовые сборки `.exe`** и ветку `exe-build` — переносить туда по отдельной команде
  ([СБОРКА_EXE §0](../сборка/СБОРКА_EXE.md));
* **порядок операций в `build()`** — рельеф применяется до разметки НП, менять нельзя;
* **выбор позиций жадным алгоритмом** — задача 8.9 меняет только НУМЕРАЦИЮ, не позиции.

---

## 2. Карта кода: точки правки

Проверено 03.09.2026 по [карте кода](../карта_кода/README.md); номера строк устаревают —
сверять `python tools/переносимое/codemap.py --check`.

| Что править | Файл :: имя :: строка |
|---|---|
| корень кэша тайлов (раскладка по участкам) | `view_qt/geomap.py` :: `cache_root` :: **127**; `tile_cache_path` :: **148** |
| выбор зума, сборка мозаики | `geomap.py` :: `_pick_zoom` :: **271**; `build_raster_basemap` :: **302** |
| предзагрузка тайлов | `tools/download_tiles.py` :: `main` :: **26** |
| перевод координат (модель) | `model/threat_grid.py` :: `lonlat_to_km` :: **89**; `km_to_lonlat` :: **1509**; `bbox_anchor_lonlat` :: **106** |
| перевод координат (показ) | `view_qt/geomap.py` :: `lonlat_to_km` :: **44**; `km_to_lonlat` :: **38** |
| реестр участков, рамка | `config.py` :: `THREAT_AREAS` :: **95**; `THREAT_AREA` :: **154** |
| загрузка слоёв по рамке | `threat_grid.py` :: `layers_from_osm` :: **1351**; `load_layers` :: **1260**; `layers_cache_path` :: **1288** |
| пределы вида, подложка | `view_qt/basemap_mixin.py` :: `_set_view_limits` :: **110**; `_refresh_basemap` :: **130**; `_on_basemap_ready` :: **159** |
| оси и сетка координат | `view_qt/threat_view.py` :: `_build_ui` :: **682–684** (`showGrid`, `setLabel`) |
| клик по карте (уже есть механика) | `threat_view.py` :: `_on_scene_click` :: **394**; `_zone_click` :: **431** |
| панель кнопок | `threat_view.py` :: `_build_ui`; кнопки `btn_area`, `btn_map_img`, `btn_input_data`, `btn_data`, `btn_build` |
| окна параметров | `view_qt/input_window.py` :: `InputDataWindow` (с 05.09.2026 одно на всё); `threat_view.py` :: `DigitalMapsDialog` |
| выбор файла (образец `QFileDialog`) | `threat_view.py` :: `DigitalMapsDialog._pick_file` :: **2360** |
| слои-картинки (образец для своей карты) | `threat_view.py` :: `_build_scene_items` :: **1233**; подложка — `basemap_mixin.py` :: **80** |
| датчики: расстановка | `threat_grid.py` :: `place_sensors` :: **2431**; `_place_by_routes` :: **2464**; `_place_big` :: **2543** |
| жадный выбор (порядок = номера) | `model/optimization.py` :: `CoverageCache.greedy` :: **~130–211** |
| накопление маршрутов | `threat_grid.py` :: `iter_batch` :: **2295**; `iter_step` :: **2275**; хранилище `self.iter_routes` :: **1944** |
| параметры датчиков | `config.py` :: `Params` :: **679**; поля `threat_N`/`threat_R` :: **702–706** |
| применение параметров | `controller_qt/threat_controller.py` :: `on_sensors_apply` :: **320**; `on_input_apply` :: **342**; `on_place` :: **563** |

---

## 3. Порядок работ и зависимости

От того, без чего не сделать остальное, — к тому, что стоит поверх.

| № | Задача | Цена | Риск | Почему в этом месте |
|---|---|---|---|---|
| 1 | **8.1** тайлы: запас и раскладка по участкам | малая | нет | нужна до 8.2: район выбирают по видимой карте |
| 2 | **8.3** координаты: единый модуль и показ по клику | малая | **низкий** | фундамент для 8.4, 8.5, 8.8; ничего не ломает |
| 3 | **8.2** задание района мышью + блокировка интерфейса | **большая** | **высокий** | меняет источник рамки: из `config` → из действия пользователя |
| 4 | **8.4** масштаб 1:M и оси | средняя | низкий | опирается на 8.3 |
| 5 | **8.7** окно ввода данных, 3+1 типы датчиков | **большая** | средний | самостоятельна; делать после 8.2, чтобы не переписывать панель дважды |
| 6 | **8.6** история полётов: выгрузка и загрузка | средняя | низкий | ✅ сделано 06.09.2026 — формат из 8.3 (координаты) пригодился как и планировалось |
| 7 | **8.8** выгрузка датчиков + номера на карте | малая | нет | тот же формат, что 8.6 |
| 8 | **8.9** анализ размещения: стрелки и устойчивые номера | средняя | средний | ✅ сделано 06.09.2026 — обошлось без 8.8: сопоставление номеров идёт по позициям (венгерский алгоритм), не по прежней нумерации |
| 9 | **8.5** своя картинка-карта | средняя | средний | ✅ сделана 05.09.2026 (привязка, автоподбор, вектор) — довесок: опорные точки мышью |
| — | **8.10** смена проекции | **очень большая** | **очень высокий** | **задел**, в объём не входит |

**Параллелить можно** 8.5 и 8.6 — они не пересекаются ни по файлам, ни по данным.

### 3.1. Что сделано на 05.09.2026 и что идёт следующим

| Задача | Состояние |
|---|---|
| 8.1 тайлы по участкам | ✅ сделано 03.09.2026 |
| 8.2 район мышью | ✅ сделано 04.09.2026 |
| 8.3 координаты, показ по клику | ✅ сделано 04.09.2026 |
| **8.7 окно ввода, четыре типа датчиков** | ✅ сделано 05.09.2026 **целиком**, включая доработки: режимы по фазам (§8.7.1б), правка датчиков на карте, распределение между типами, три кнопки очистки, папки файлов |
| **8.5 своя картинка-карта** | ✅ сделано 05.09.2026 **почти целиком**: четыре модели привязки (сдвиг/подобие/прямоугольник/аффинная), **автоподбор по OSM** (промах 3 660 → 127 м), **векторная карта SVG**. ⏸ Осталось: опорные точки МЫШЬЮ (пока только координатами) и поиск карты БЕЗ координат вовсе — не работает, см. §6а методички |
| 8.8 выгрузка датчиков, номера на карте | ✅ по сути сделано внутри 8.7: формат, папки, номера; осталась отдельная нумерация больших (Б1, Б2…) |
| **8.6 маршруты пролёта: выгрузка и загрузка** | ✅ **СДЕЛАНО 06.09.2026** — формат `UAV-ROUTES v1`, отдельный `model/flight_log.py`, чекбокс «загруженная выборка» |
| 8.4 масштаб 1:M | не начата |
| **8.9 анализ размещения** | ✅ **СДЕЛАНО 06.09.2026** — венгерский алгоритм своей реализацией, отдельный `model/sensor_track.py`, чекбокс «Анализ размещения» в группе «датчики и подписи» |
| 8.10 смена проекции | задел, в объём не входит |

**Очередь на 06.09.2026 (после 8.6 и 8.9):**

1. **8.5, довесок — опорные точки мышью.** Автоподбор по OSM закрыл главную часть
   задачи (числами координат промах падает до 100–400 м), но щёлкнуть на карте узнаваемое
   место и сразу указать его координату — быстрее там, где OSM пуст. Математика готова
   (`MapAnchor.points` принимает точки от любого источника), нужен только режим на карте
   — тот же приём, что режим правки датчиков (`pg.TargetItem`, `_begin_edit_sensor`);
2. **8.4 — масштаб 1:M и оси**, когда очередь дойдёт.

Разбор автоподбора, чего он не умеет и почему — [МЕТОДИЧКА_ПРИВЯЗКА_КАРТИНКИ](../карты/МЕТОДИЧКА_ПРИВЯЗКА_КАРТИНКИ.md).

---

## Задача 8.1 — тайлы: скачать запас и разложить по участкам

### 8.1.1. Что требуется

Заказчик (уточнено 03.09.2026): **берём нынешнюю рамку участка и прибавляем по 100 км
во все стороны** — это и есть базовый район, внутри которого дальше выбирается район
моделирования (задача 8.2). **Предзагружаем всё до z12 включительно, начиная с z6**
(на z6 показываем самый дальний обзор), а **z13–15 приложение докачивает само**, когда
пользователь приближается.

⚠️ Первая редакция этого раздела считала запас от границ **всей Архангельской области** —
понято неверно; расчёт по области сохранён в 8.1.4 как справка о том, почему так делать
не следует. Плюс сюда переезжает незакрытый пункт [плана 7, З6](7_ЕДИНЫЙ_ПЛАН.md): тайлы
всех участков лежат вперемешку ([ОГРАНИЧЕНИЯ 5.1д](../ОГРАНИЧЕНИЯ.md), журнал п. 216).

### 8.1.2. Координаты и объём — посчитано

```
рамка участка (plesetsk_wide):   39.8794  62.5901  41.7516  63.0869      95 × 55 км
она же + 100 км во все стороны:  37.9116  61.6918  43.7194  63.9852     295 × 255 км
```

Средний вес тайла в нашем кэше — **11.1 КБ** (замер по 4 001 файлу):

| z | тайлов | объём | кто качает |
|---:|---:|---:|---|
| 6 | 2 | 0.02 МБ | предзагрузка |
| 7 | 6 | 0.1 МБ | предзагрузка |
| 8 | 24 | 0.3 МБ | предзагрузка |
| 9 | 80 | 0.9 МБ | предзагрузка |
| 10 | 270 | 2.9 МБ | предзагрузка |
| 11 | 986 | 10.7 МБ | предзагрузка |
| 12 | 3 886 | 42.1 МБ | предзагрузка |
| **z6–12 ИТОГО** | **5 254** | **57 МБ** | **~9 минут** |
| 13 | 15 295 | 166 МБ | приложение, по мере приближения |
| 14 | 60 685 | 658 МБ | приложение |
| 15 | 242 740 | 2.6 ГБ | приложение |

**Почему детальные уровни не качаем заранее.** z13–15 на весь район — 3.4 ГБ и около
9 часов выкачки, из которых реально просмотрят единицы процентов площади: подробно
смотрят там, где стоят датчики, а не по всему квадрату 295×255 км. Механизм докачки
**уже работает**: `fetch_tile_bytes` (`geomap.py:204`) сперва ищет тайл в кэше, а если
не нашёл и сеть разрешена — скачивает и **тут же кладёт в кэш**. То есть при первом
приближении к месту тайлы подтянутся сами и останутся навсегда.

⚠️ **Условие:** чекбокс «офлайн» в панели должен быть **снят** — иначе `allow_net=False`
и приложение читает только кэш (тогда при приближении будет пусто).

### 8.1.3. Команды скачивания

Скрипт `tools/download_tiles.py` доработан 03.09.2026 — теперь рамку не нужно считать
руками:

| Ключ | Что делает |
|---|---|
| `--area <имя>` | взять рамку из `config.THREAT_AREAS`; без него — активный участок |
| `--pad-km <N>` | раздвинуть рамку на N **километров** (пересчёт долготы по косинусу широты) |
| `--dry-run` | только смета: сколько тайлов, сколько мегабайт, сколько минут |
| `--margin` | прежний ключ, запас в **градусах**; по умолчанию теперь 0 |

```bash
# СМЕТА (ничего не качает) — сверить числа перед запуском
python tools/download_tiles.py --layer osm --pad-km 100 --zmin 6 --zmax 12 --dry-run

# ОСНОВНАЯ ЗАГРУЗКА: участок + 100 км, z6-12 — 5 254 тайла, 57 МБ, ~9 минут
python tools/download_tiles.py --layer osm --pad-km 100 --zmin 6 --zmax 12 --delay 0.1

# то же явными координатами (если участок в config ещё не переключён)
python tools/download_tiles.py --layer osm --zmin 6 --zmax 12 --delay 0.1 \
    --bbox 37.9116 61.6918 43.7194 63.9852

# рельефная подложка на обзорные уровни (по желанию, ~14 МБ)
python tools/download_tiles.py --layer topo --pad-km 100 --zmin 6 --zmax 11 --delay 0.15

# проверить, что получилось
python tools/tile_coverage.py
```

⚠️ **`--delay 0.1` не снижать** — это плата за вежливость к серверу; при 5 254 тайлах
она стоит всего 9 минут. И ⚠️ ограничитель `zmax = min(zmax, zmin + 9)`: **не больше
10 уровней за запуск** (`download_tiles.py`), наш диапазон z6–12 в него укладывается.

### 8.1.4. Справка: почему не качаем область целиком

Расчёт сохранён, чтобы к варианту не возвращались заново. Архангельская область,
материковая часть (без Новой Земли и ЗФИ — они дают +1 500 км пустого моря), границы
`35.20 60.00 56.50 67.50`, с запасом 100 км — `33.1689 59.1017 58.5311 68.3983`,
размер 1249×1035 км:

| Уровни | Тайлов | Объём |
|---|---:|---:|
| z7–12 | 93 467 | 1.0 ГБ |
| z13 | 279 752 | 3.0 ГБ |
| z14 | 1 116 885 | 12.1 ГБ |
| z15 | 4 465 606 | 48.2 ГБ |
| **z7–15 всего** | **5 955 710** | **62.8 ГБ** |

Две причины отказаться: 63 ГБ и, главное, **массовая выкачка с `tile.openstreetmap.org`
прямо запрещена** их Tile Usage Policy и ведёт к блокировке. Плюс по 0.4 работать по
такому району всё равно нельзя — слои разъедутся на 43 км.

**Ссылки на источники** (шаблоны уже прописаны в `geomap.py:73` `TILE_LAYERS`):

| Слой | URL-шаблон | Условия |
|---|---|---|
| `osm` | `https://tile.openstreetmap.org/{z}/{x}/{y}.png` | массовая выкачка запрещена |
| `topo` | `https://a.tile.opentopomap.org/{z}/{x}/{y}.png` | рельефная, лимиты жёстче |
| `satellite` | `https://server.arcgisonline.com/…/World_Imagery/MapServer/tile/{z}/{y}/{x}` | ⚠️ порядок **{z}/{y}/{x}** |
| `dark`, `cyclosm`, `osm_hot`, `opnv` | см. `TILE_LAYERS` | — |

### 8.1.5. Раскладка кэша по участкам ✅ СДЕЛАНО 03.09.2026

`tile_cache/<участок>/<слой>/{z}/{x}/{y}` — новое качается сюда; старое остаётся на
месте. ⚠️ **Перенос 13 273 существующих тайлов отменён решением заказчика:** разбирать
210 МБ по рамкам, дублируя краевые тайлы, ради порядка невыгодно — весь выигрыш даёт то,
что правильно ложится новое, а старое находится запасным поиском.

| Что сделано | Где |
|---|---|
| `area` — необязательный аргумент пути | `geomap.py` :: `_area_dir`, `tile_cache_path` |
| поиск в двух местах: папка участка → общий корень | `geomap.py` :: `find_cached_tile` |
| запись скачанного в папку участка | `geomap.py` :: `fetch_tile_bytes` |
| участок в ключе кэша в памяти | `geomap.py` :: `_decoded_tile_u8` |
| папка участка не считается слоем | `geomap.py` :: `discover_local_layers` |
| проброс участка до сборки мозаики | `basemap_mixin.py` :: `_BasemapTask`, `_init_basemap(tile_area=…)` |
| вкладка 3 задаёт свой участок | `threat_view.py` — `tile_area=THREAT_AREA_DIR` |
| ключи `--area`, `--pad-km`, `--tile-area`, `--dry-run` | `tools/download_tiles.py` |

⚠️ **Почему аргументом, а не глобально:** у вкладки 2 свой район (Курск). Сделай участок
глобальным — она искала бы курские тайлы в папке Плесецка. Вкладка 2 ничего не передаёт
и работает по старой раскладке.

Осталось по этой задаче: `tools/tile_coverage.py` научить считать покрытие с учётом
обеих раскладок.

### 8.1.6. Ловушки

* **`_pick_zoom` берёт зум по ширине окна** (`geomap.py:271`), а не по кэшу: если
  детальных тайлов нет, он всё равно их запросит и получит пустоту — на обзоре это
  выглядит как «карта пропала». Проверять после докачки обоих поясов;
* `max_tiles=192` в `build_raster_basemap` (`geomap.py:303`) — предохранитель от сборки
  гигантской мозаики; при обзоре всей области он сам понижает зум, это нормально;
* **`satellite` — порядок `{z}/{y}/{x}`**, а не `{z}/{x}/{y}`: перепутать легко;
* кэш `.png`/`.jpg` определяется по магическим байтам (`_sniff_ext`), руками расширение
  не назначать.

### 8.1.7. Проверка

`python tools/tile_coverage.py` показывает покрытие ≥ 95 % на z7–12 для обзора и на
z13–15 для рабочего района; в интерфейсе на общем виде карта не пустая, при
максимальном приближении — не мыльная.

---

## Задача 8.2 — «Задать район моделирования» мышью ✅ СДЕЛАНО 04.09.2026

> **Главное правило (уточнено заказчиком).** Разделены **область показа** и **район
> расчёта**. Область показа — участок `arh`, 291 × 250 км: по ней скачаны тайлы и по ней
> ходит камера. Район расчёта — прямоугольник внутри: только там строятся сетка 500 м,
> векторные слои, весовая карта и рельеф. Вне района на карте остаётся подложка и своя
> карта-картинка, и ничего не считается.
>
> **Два способа задать район, оба равноправны:** очертить мышью (два клика — углы,
> КОЛЁСИКО — принять, Esc — отмена) либо загрузить свою карту — её рамка станет районом.
> Карту потом можно оставить, а район сделать меньше или больше.
>
> **Пока район не задан — расчёта нет:** кнопки расчёта недоступны, рамка на карте не
> рисуется (она обещала бы район, которого нет), в показателях написано, что делать.
> Кнопка «Убрать» возвращает в это состояние и обнуляет слои, датчики и маршруты.
>
> **⚠️ Две ошибки, пойманные по ходу — обе про якорь и обе давали тихий сдвиг:**
> 1. сперва `set_area` переносил якорь км-фрейма в угол нового района. Всё, что уже
>    нарисовано в прежнем фрейме (тайлы, своя карта, рамка, подписи), оставалось на
>    старых километрах — **слои разъезжались с картой**. Теперь якорь один на область
>    показа и от района не зависит;
> 2. рамка округлялась к целому числу ячеек ДО пересчёта якоря, и после него снова
>    становилась дробной: очерчено 46.00 км (92 ячейки) → стало 45.71 км (91.4 ячейки).
>    Округление перенесено в модель, в `set_area`, и делается в конечных километрах.
>
> **Проверено:** район задан кликами — стороны кратны ячейке точно (241 × 151 и 95 × 63
> ячеек), якорь не сдвинулся, рамка лежит внутри области; `clear_area` обнуляет сетку и
> слои; модель без района не роняет ни один из вызовов отрисовки; самопроверка проходит
> (район 121 × 100 км, сетка 242 × 201, слои читаются из `.osm.pbf`, рельеф в весе).
>
> **Доведено по замечаниям заказчика 04.09.2026:**
> * **две рамки:** чёрная — предел, за которым нет данных; синяя — сам район, появляется
>   вместе с ним и перерисовывается при смене. Раньше рамка была одна, по `config`, и
>   обещала район там, где его нет;
> * **район при старте задан** (рамка карты Плесецка, 117 × 73 км) — это **переключаемое**
>   поведение: строка `work=(…)` в записи участка. Убрать её — программа откроется без
>   района, с двумя активными кнопками. Так и задумано в перспективе;
> * **пока района нет, активны ровно две кнопки** — «Задать район» и «Добавить карту»;
>   остальная панель гаснет целиком (кнопки, галочки, поля, списки);
> * **«Убрать» обнуляет всё:** сетку, слои, веса, датчики, маршруты, рельеф и кэш чтения.
>   Раньше в отрисовке стоял голый `return` — данные обнулялись, а картинка оставалась;
> * **карта не строится сама** после задания района: район поправляют по два-три раза, а
>   каждое построение — это чтение `.osm.pbf`. Считает кнопка «Построить карту»;
> * **векторные слои обрезаются в 3 км** от района вместо 10 — иначе район выглядел
>   больше, чем он есть.
>
> **Что нашлось при доводке (числа — в журнале 223, 224):** маршруты считались при каждом
> построении карты, хотя нужны только датчикам и итерациям; слои и высоты перечитывались
> с диска каждый раз (повторное построение **9.5 → 0.0 с** после кэша); рельеф ронял
> программу, потому что растр читался из двух потоков; район из `config` не приводился к
> целому числу ячеек. Последнее нашёл **`tools/flow_check.py`** — новый скрипт сквозного
> прогона модели, написанный по итогам этих правок.
>
> **Осталось:** предупреждение при районе больше 150 км сделано текстом в заголовке —
> проверить, что оно заметно.

### 8.2.1. Что требуется

Заказчик: «в интерфейсе справа кнопка, задать район моделирования; по двойному щелчку
левой ставится точка, потом средняя кнопка — область принимается и строится правильный
прямоугольник или квадрат, как было изначально. Без задания района остальные кнопки
тусклые, как бы не функционируют, и весь интерфейс также».

То есть: **программа стартует на обзорной карте** (задача 8.1, вся область), участок
расчёта не задан, работать нельзя. Пользователь очерчивает район → строится рамка →
интерфейс оживает → дальше всё как сейчас.

### 8.2.2. Как решать

Механика рисования **уже есть** — запретные зоны рисуются ровно так: клики ставят
вершины, **колёсико замыкает** (`threat_view.py:414` `_begin_zone`, `:431` `_zone_click`).
Её и повторить, но с двумя точками и прямоугольником.

1. **Кнопка** «Задать район моделирования…» — в `_build_ui` (`threat_view.py:675`),
   первой в колонке, до «Построить карту»;
2. **Режим рисования** `_begin_area()` по образцу `_begin_zone`: первый двойной клик —
   угол, второй — противоположный угол, **средняя кнопка** — принять. Прямоугольник
   рисовать превью-линией, как `_draw_zone_preview` (`:457`);
3. **Приведение к правильной форме.** Заказчик: «правильный прям или квадрат, как было
   у нас изначально». Понято так: рамка приводится к **целому числу ячеек 500 м** по
   обеим сторонам (как нынешние 192×110 ячеек). Квадрат не навязывать — умолчание,
   см. вопрос В3;
4. **Передать рамку в модель.** Ключ: `layers_from_osm(pbf, lon0, lat0, bbox_lonlat)`
   (`threat_grid.py:1351`) **уже принимает bbox** — значит смена района не требует
   переписывания загрузчика. Нужно: пересчитать якорь `bbox_anchor_lonlat` под новую
   рамку, пересобрать сетку, ⚠️ **пересобрать кэш `.npz`** (в нём лежат готовые
   километры от старого якоря);
5. **Блокировка интерфейса.** Механизм тоже есть: `set_busy(busy)`
   (`threat_view.py:1472`) уже гасит кнопки на время расчёта. Ввести состояние
   `area_defined` и гасить всё, кроме «Задать район», выбора подложки и справки;
6. **Ограничение размера.** По 0.4 — не больше **150 км по широте**; при попытке
   очертить больше показывать предупреждение с числом сдвига.

### 8.2.3. Ловушки

* ⚠️ **Кэш слоёв и якорь.** При смене рамки старый `.npz` неверен дважды: он обрезан по
  старому участку и посчитан от старого якоря. Пересобирать обязательно, иначе карта
  окажется пустой или сдвинутой (это уже было — [ОГРАНИЧЕНИЯ 5.1а](../ОГРАНИЧЕНИЯ.md));
* **экстракт `.osm.pbf` меньше нового района** — данных за его краем нет вовсе. Проверять
  рамку экстракта (`tools/area_bounds.py`) и не давать выйти за неё минус 2 км
  ([ОГРАНИЧЕНИЯ 5.1в](../ОГРАНИЧЕНИЯ.md): у самого края объекты приходят обрубками);
* **рельеф**: DEM покрывает свой прямоугольник; за его пределами высот нет, а подстановка
  чужого файла **не видна** — читается без ошибок и описывает другую местность;
* **средняя кнопка мыши** уже занята в режиме зоны — режимы должны быть взаимно
  исключающими, иначе один клик замкнёт и зону, и район;
* время пересборки: чтение `.osm.pbf` — десятки секунд, делать в фоне (`_run_async`,
  `threat_controller.py:395`), иначе окно «повиснет».

### 8.2.4. Проверка

Программа стартует с погашенными кнопками; после задания района строится сетка, число
ячеек отвечает размеру рамки (при 95×55 км — 192×110, [эталон §6](../ОГРАНИЧЕНИЯ.md));
повторное задание района пересобирает всё без перезапуска программы.

---

## Задача 8.3 — координатная система: единый модуль и показ по клику ✅ СДЕЛАНО 04.09.2026

> **Что получилось.** Новый модуль `model/geo_frame.py` — единственное место, где градусы
> переводятся в километры; `threat_grid.py` и `geomap.py` импортируют оттуда, прежние
> имена сохранены. Строка координат под картой: клик — и видно точку в градусах,
> в градусах-минутах-секундах, в радианах и в километрах. Наружу координаты отдают
> `sensors_lonlat`, `routes_lonlat`, `cells_lonlat` (центры ячеек), `km_to_lonlat_arr`.
>
> ⚠️ **Найдено при объединении формул:** в проекте два РАЗНЫХ коэффициента — широта
> 110.574 км/°, долгота 111.320·cos(широты). Я сперва записал в общий модуль «среднее»
> 111.320 для обеих осей — это сдвинуло бы карту на **0.37 км** при высоте участка 55 км,
> молча. Поймано сверкой с обеими прежними копиями до запуска. Действующие числа
> сохранены: ими посчитаны километры в кэшах `.npz` и весь эталон.
>
> **Проверки:** перевод в обоих модулях даёт одинаковый результат до 1e-12, туда-обратно
> возвращает исходные градусы точно, контрольный прогон **совпал с эталоном полностью**
> (коридоров 1875, засечка 1.000, датчиков 15 + 3).

### 8.3.1. Что требуется

Заказчик: «реализовать координатную систему, то есть всё привязано на координатах:
датчики, маршруты движения, весовые коэффициенты, ячейки дискретной сетки (их центры);
также просто по клику по карте справа снизу отображаются координаты в формате широта
долгота».

**Важно:** привязка к координатам **уже есть** (0.1) — всё считается в км-фрейме и
переводится в (lon, lat) двумя готовыми функциями. Недостающего — три вещи:

1. **показа координат нет вовсе** (проверено: обработчики клика есть только для цели,
   сектора и зон — `threat_view.py:394`, `:431`);
2. перевод **продублирован** в `model/threat_grid.py` и `view_qt/geomap.py` — две копии
   одной формулы, которые обязаны совпадать;
3. на выгрузку (задачи 8.6, 8.8) координаты нигде не отдаются.

### 8.3.2. Как решать

1. **Свести перевод в один модуль** — новый файл `model/geo_frame.py` (по правилу
   [CLAUDE.md](../../CLAUDE.md): новый механизм — сразу в отдельный файл). В нём:
   `lonlat_to_km`, `km_to_lonlat`, `bbox_anchor_lonlat`, форматирование координат.
   `threat_grid.py` и `geomap.py` импортируют оттуда; ⚠️ **`geomap.py` не должен
   потянуть за собой `config`** — модуль сделать без зависимостей от config;
2. **Показ по клику**: подписаться на `sigMouseClicked` сцены (образец —
   `threat_view.py:394` `_on_scene_click`), перевести `mapSceneToView` → км → (lon, lat),
   вывести в правый нижний угол. Метка — `TextItem` в углу либо строка статуса под
   картой; вариант со строкой проще и не мешает карте;
3. **Формат вывода** — три сразу, по строке: `62.7601° N, 40.3312° E`,
   `62°45'36.4" N, 40°19'52.3" E`, и (если нужно) радианы `1.0947, 0.7038`;
4. **Отдать координаты наружу**: `ThreatModel` получает методы `sensors_lonlat()`,
   `routes_lonlat()`, `cells_lonlat()` — на них опираются задачи 8.6 и 8.8.

### 8.3.3. Ловушки

* ⚠️ **Две формулы должны стать одной.** Пока их две, они расходятся молча: одна
  правится, другая нет. Свести — и проверить, что числа совпали до 1 м;
* **центр ячейки ≠ её угол**: `cell_centers_km` даёт центры, `extent_km` — края.
  Выгружать координаты **центров** (заказчик просил именно их);
* координаты меняются при смене якоря (задача 8.2) — показ должен брать якорь из модели,
  а не из `config` напрямую;
* «в рад» из задания — вероятно, «в градусах»; умолчание принято градусами, радианы
  добавлены третьей строкой (вопрос В1).

### 8.3.4. Проверка

Клик по известной точке (Мирный — 40.33, 62.76) даёт эти координаты с точностью до
третьего знака; `sensors_lonlat()` для датчика в известной ячейке возвращает координаты
её центра; обратный перевод возвращает исходные км с ошибкой < 1 м.

---

## Задача 8.4 — масштаб как на топографических картах (1 : 50 000)

### 8.4.1. Что требуется

Заказчик: «продумать отображение масштаба по осям X и Y — вместо этого, чтобы он был как
в настоящих топографических картах: 1:50000, 1:100000, то есть сколько в 1 см; пусть
справа в углу отображается масштаб, а оси отображали с учётом 1 сантиметра масштаб.
Так как тайлы могут быть нестандартных форматов, чтобы отображался он».

Сейчас оси подписаны «X, км» / «Y, км» с сеткой (`threat_view.py:682–684`); масштабной
линейки и знаменателя масштаба нет.

### 8.4.2. Как решать

Знаменатель масштаба считается из трёх известных величин:

```
M = (км на пиксель) · (пикселей в сантиметре) · 100 000
пикселей в сантиметре = physicalDpiX() / 2.54
км на пиксель = ширина вида в км / ширина виджета в px
```

1. **Метка масштаба** в правом нижнем углу: «1 : 50 000» + **линейка** (отрезок с
   подписью «1 км») — линейка обязательна, потому что знаменатель верен только для
   этого экрана, а скриншот могут напечатать в другом размере;
2. **Округление к ряду топокарт**: 1:10 000, 1:25 000, 1:50 000, 1:100 000, 1:200 000,
   1:500 000, 1:1 000 000 — показывать ближайший и, если вид между ними, добавлять
   фактическое значение мелким шрифтом;
3. **Обновление** — на сигнале `vb.sigRangeChanged` (уже подключён в
   `basemap_mixin.py:108` для подложки), туда же добавить пересчёт;
4. **Оси**: оставить километры (они привязаны к углу участка и удобны), а «сантиметровую»
   привязку дать через масштаб и линейку. Ставить деления «через 1 см» на самих осях
   можно, но при зуме шаг будет некруглым (0.37 км) — это хуже читается, чем круглые
   километры. Умолчание: **километры + масштаб + линейка**, см. вопрос В4.

### 8.4.3. Ловушки

* ⚠️ **`physicalDpiX()` врёт** на многих мониторах (Qt берёт EDID, который часто пустой
  или неверный). Дать в настройках поле «диагональ монитора» или калибровку «подведите
  линейку»; без калибровки честнее показывать **только линейку**, а знаменатель — как
  оценку;
* **масштабирование Windows** (125 %, 150 %) меняет логические пиксели: считать по
  `devicePixelRatio`, иначе масштаб соврёт в те же 1.25–1.5 раза;
* **широта меняет масштаб по X**: 1 см по горизонтали и по вертикали — не одно и то же
  число километров, если вид не квадратный. У нас `setAspectLocked` держит соотношение —
  проверить, что он включён, иначе масштаб по осям разный и одним числом не описывается;
* формат тайлов на масштаб **не влияет** — он считается по виду в км, а не по картинке;
  это отвечает на опасение в задании.

### 8.4.4. Проверка

При виде «весь участок» (95 км) на экране 1000 px и 96 dpi знаменатель ≈ 1 : 250 000;
приближение вдвое даёт вдвое меньший знаменатель; линейка «1 км» физически измеряется
линейкой по экрану с ошибкой < 10 % (после калибровки).

---

## Задача 8.5 — своя картинка карты поверх слоёв ✅ СДЕЛАНО 05.09.2026 (довесок: точки мышью)

> **Работает (03.09.2026).** Модуль `view_qt/map_image.py` (чтение, привязка, окно),
> кнопка «Добавить карту (картинкой)…», слой сцены Z = −1, папка
> `geo_cache/карты_картинки/`, режим «только картинка». Картинка загружается и
> показывается — проверено заказчиком на реальной карте района Плесецка.
>
> **⏸ Пауза до 8.2 и 8.3 — решение заказчика 04.09.2026.** Выяснилось главное условие,
> которого не было в первой формулировке:
>
> **Координаты углов картинки должны ЗАДАВАТЬ РАЙОН РАБОТЫ.** Не «подложка поверх
> слоёв», а наоборот: рамка картинки становится районом моделирования, и уже под неё
> считаются векторные слои, рельеф, весовая карта и датчики. То есть это не отдельная
> функция показа, а **вход в задачу 8.2**: картинка — один из способов задать район
> (второй — мышью). Делать её раньше 8.2 бессмысленно: пересборки сетки под новую рамку
> ещё нет.
>
> **Что доделать, когда вернёмся:**
> 1. кнопка «Сделать районом моделирования» в окне картинки → `set_area(bbox)` из 8.2;
> 2. ⚠️ **зум в режиме «только картинка»**: заказчик отметил, что приближение и показ
>    ведут себя не так, как ожидается. Причина, скорее всего, в пределах вида —
>    `_set_view_limits(рамка картинки, 0)` задаёт и `minXRange`/`minYRange` (предел
>    приближения) как 1/60 рамки, и отдалять за край нельзя. Разобрать вместе с 8.4,
>    где масштаб и пределы вида пересматриваются;
> 3. показ координат по клику на картинке — это задача 8.3, делается раньше.
>
> ⚠️ Окно **автотестом не проверялось** — в среде разработки не поднимаются Qt-плагины
> (`Could not find the Qt platform plugin`); проверка идёт запуском у заказчика.
> Исправлено по его замечанию 04.09.2026: поля названы по краям света («долгота
> ЗАПАДНЫЙ край» вместо «СЗ: долгота»), перепутанный порядок программа исправляет сама —
> прежняя проверка отвергала ввод, хотя все четыре числа были верными.

### 8.5.0а. РЕЗКОСТЬ РАСТРА ПРИ ПРИБЛИЖЕНИИ ✅ (05.09.2026)

**Проблема.** Заказчик сравнил свою карту в просмотрщике и в программе: в программе
подписи топокарты нечитаемы. Карта 17 944 × 13 220 (237 млн точек) на район 156 км —
это 8.7 м на пиксель, а показывалась она ужатой до 4 000 точек, то есть **39 м на
пиксель**, вчетверо грубее. Снимки — в папке `фото пример/`.

**Почему ужимали.** Картинка в Qt хранится по 4 байта на пиксель: 237 млн точек — это
950 МБ, и столько же держит Qt под свою копию. Замер полного показа: 8 000 точек —
189 МБ, 12 000 — 424 МБ, 16 000 — 754 МБ.

**Решение — показывать в полном разрешении ТОЛЬКО ВИДИМЫЙ КУСОК.** Экран показывает
2–3 тысячи точек, остальное отрисовка всё равно выбрасывает. Картинка один раз
разворачивается в несжатый файл рядом с ней (`geo_cache/карты_картинки/детализация/`), и
нужный кусок читается прямо с диска через `numpy.memmap` — без распаковки.

⚠️ **Почему нужен отдельный файл, а не чтение из PNG/JPEG.** Это сжатые форматы: взять
из них кусок в середине нельзя, не распаковав файл целиком. Развёрнутая копия — это как
карта, разложенная на столе: любой участок читается сразу.

**Замеры (05.09.2026):**

| Что | Результат |
|---|---|
| подготовка, 116 млн точек (CMYK JPEG) | 1.6 с, файл 350 МБ |
| подготовка, 237 млн точек (PNG RGBA) | 2.5 с, файл 712 МБ |
| чтение куска при показе | 0.003…0.37 с |
| качество: видно 156 км / 39 км / 16 км | 43 / 17 / **8.7** м на пиксель (последнее — предел самой карты) |
| «грубее экрана» на 1600 / 2400 / 3840 точек | 0 случаев из 8 на каждом |

⚠️ **Размер куска считается ОТ ЭКРАНА**, а не постоянным числом: `1.5 × ширина окна`
(половина запаса уходит на края, чтобы панорама не дёргалась), потолок — 40 млн точек
на кусок (иначе на 4K запрос доходил до 342 МБ).

⚠️ **Шаг прореживания округляется ВНИЗ, в сторону детали.** Шаг целый — «каждый второй»
или «каждый третий», и между ними разрешение меняется скачком. Округление вверх один
раз промахнулось мимо экрана: при видимых 39 км на экране 2400 давало 17.4 м/пиксель
против экранных 16.2.

**В интерфейсе:** кнопка «Подготовить резкость при приближении…» в окне «Добавить карту
(картинкой)…». Нажимается один раз на карту; дальше программа сама видит готовый файл и
пишет «Резкость подготовлена ✓». Без неё карта работает по-старому, ужатой.
⚠️ Решение заказчика 05.09.2026: **оставить кнопкой, не спрашивать автоматически.**

### 8.5.0. Векторная карта SVG вместо растра ✅ СДЕЛАНО 05.09.2026

**Было решение 04.09.2026 «пока НЕ делаем»** — карта перенесена в JPEG, качество
устраивало. **Снято 05.09.2026:** заказчик подтвердил, что карта живёт в CorelDRAW, и
вектор пошёл в работу («нет, картинка в CorelDRAW, пункт 8.5 плана»).

**Что сделано:** модуль [`view_qt/map_vector.py`](../../view_qt/map_vector.py) —
чтение SVG, посадка на сцену через ту же привязку, что у растра, рендер во временный
растр для автоподбора и замер отрисовки. Привязка ОБЩАЯ с растром (`MapAnchor`), поэтому
автоподбор по населённым пунктам работает и для вектора без единой отдельной строки.
Разбор — [МЕТОДИЧКА_ПРИВЯЗКА_КАРТИНКИ §5а](../карты/МЕТОДИЧКА_ПРИВЯЗКА_КАРТИНКИ.md).

**Проверено** в `tools/gui_check.py`, раздел 4: размер SVG читается (1200 × 800), рендер
для подбора возвращает единицы самой карты (x 100…978 при заданных 100…980), углы садятся
в свои километры (промах 0.0), карта не встаёт вверх ногами, показ вектора снимает растр,
«Убрать» снимает вектор.

⚠️ **Чего вектор НЕ решает: привязку.** SVG координат в себе не несёт — это рисунок в
своих единицах, и привязывать его надо так же, как PNG. За точность отвечают векторные
ГЕОДАННЫЕ, см. §8.5.0б ниже.

**Что было выяснено при разведке (проверено, не предположения):**

| Формат | Годится? | Чем |
|---|---|---|
| **SVG** | **да** | `PyQt5.QtSvg` **уже установлен** — `QGraphicsSvgItem` кладётся в тот же `ViewBox`, ставить ничего не нужно |
| `.cdr` | нет | закрытый формат Corel, читать нечем |
| PDF | нет | Qt не рендерит, нужен poppler |
| DXF | нет | `ezdxf` не установлен, и это голые линии без стилей |

**Зачем это может понадобиться.** Растр 4000 px при сильном приближении мылится — та же
беда, что с подписями, впечатанными в тайлы (журнал п. 207). Вектор рисуется заново под
каждый масштаб и остаётся острым, а весит втрое меньше: 1–3 МБ против 10 МБ у PNG.

**Чем платим:** вектор пересчитывается при КАЖДОМ кадре, и на карте с тысячами объектов
панорама может подтормаживать. Предсказать нельзя — только замерить на своём файле.
Поэтому растровый путь в любом случае остаётся рядом.

⚠️ **Две ловушки, найденные при первом заходе** — их и стоит помнить, когда вернёмся:

1. **ось Y смотрит в разные стороны.** У Qt начало вверху слева и Y растёт ВНИЗ, у нашей
   сцены — ВВЕРХ (север). Для растра это решает `ImageItem.setRect`, а для SVG нужно
   задавать преобразование руками: масштаб по Y **отрицательный**, а точка (0, 0)
   картинки садится в `(kx0, ky1)` — левый ВЕРХНИЙ угол в километрах. Забыть минус —
   карта встанет вверх ногами;
2. **`QGraphicsSvgItem` держит свой файл**, подменить его в готовом элементе нельзя:
   при смене карты старый элемент убирают со сцены и создают новый.

**Что нужно от заказчика при экспорте из CorelDRAW:** «Файл → Экспорт → SVG», ⚠️ с
преобразованием текста в кривые — иначе на другой машине подставится другой шрифт и
подписи поедут.

### 8.5.0б. ВЕКТОРНАЯ КАРТА — ДВЕ РАЗНЫЕ ВЕЩИ, и выбрать надо сознательно

⚠️ **Заведено 05.09.2026** после того, как автопривязка растра показала свой предел
(на обзорной карте `Пл_об` — 16 опорных точек против 89 на детальной,
[МЕТОДИЧКА_ПРИВЯЗКА_КАРТИНКИ §5](../карты/МЕТОДИЧКА_ПРИВЯЗКА_КАРТИНКИ.md)). Заказчик:
«приступай к плану где про загрузку не растрового, а векторного».

Под «векторной картой» скрываются два разных пути, и путать их нельзя:

| | **векторная КАРТИНКА** (SVG) | **векторные ГЕОДАННЫЕ** (GPKG, SHP) |
|---|---|---|
| что внутри | линии и заливки в «своих» единицах | те же линии, но **в координатах местности** |
| привязка | нужна, ровно как у растра | **не нужна вовсе** — координаты в файле |
| точность посадки | как подберём (сейчас 127 м / 4 км) | **метры**, ограничены только данными |
| резкость | идеальная на любом зуме | идеальная на любом зуме |
| вес | 1–3 МБ | единицы–десятки МБ |
| годится в РАСЧЁТ | нет, это рисунок | **да** — можно считать веса по своим слоям |
| чем читаем | `PyQt5.QtSvg` (уже стоит) | `geopandas 1.1.4` + `pyogrio 0.13.0` (уже стоят) |

**Вывод: за точность отвечает второй путь, а не первый.** SVG решает только резкость и
оставляет ту же задачу привязки, которая на обзорной карте и подвела. Геоданные снимают
её целиком: файл сам знает, где он на земле.

**Что проверено в этом окружении 05.09.2026** (`pyogrio.list_drivers()`, 64 драйвера):

| формат | чтение | брать? |
|---|---|---|
| **GeoPackage `.gpkg`** | да | **да, лучший**: один файл, слои внутри с именами, проекция записана |
| **Shapefile `.shp`** | да | годится, но это КОМПЛЕКТ: `.shp` + `.shx` + `.dbf` + **`.prj`** (без `.prj` проекция неизвестна) |
| GeoJSON | да | для мелких выборок; на район в сотню км тяжёл (текст) |
| MapInfo `.tab`, DXF, KML | да | если карта уже есть в этом виде |
| **GeoParquet** | **нет** | `pyarrow` не установлен |
| Garmin, o5m, OPL, csv | нет | не геоданные в нужном смысле |

**Что нужно от заказчика:** файл `.gpkg` на район (либо Shapefile со всеми четырьмя
частями). Если карта рисованная и лежит только в CorelDRAW — тогда SVG, «Файл → Экспорт →
SVG», ⚠️ с преобразованием текста в кривые, и путь остаётся первым (с привязкой).

**Как решать (когда файл будет):**

1. новый модуль `model/vector_map.py` — чтение через `geopandas.read_file`, перевод
   геометрии в км-фрейм через `pyproj` (файл может быть в любой проекции, у нас — своя);
2. показ — отдельный слой в `threat_view` рядом с `map_img_item`, теми же `PlotDataItem`,
   что и штатные слои: тогда работают уже готовые цвета, толщины и порядок Z;
3. ⚠️ **прореживание.** Слой на район 150 км может нести сотни тысяч точек, а панорама
   пересчитывает всё заново каждый кадр. Растру помогла детализация по видимой области
   (8.5.0а) — вектору понадобится своё: обрезка по видимому окну и упрощение геометрии
   (`shapely.simplify`) под текущий масштаб;
4. развилка «только для глаз или в расчёт тоже» — решать заказчику: считать веса по
   принесённым слоям можно, но эталон участка тогда придётся переснимать.

### 8.5.1. Что требуется

Заказчик: «кнопка добавить карту, в которой указан файловый путь к картинке и координаты
4 углов; отображать именно эту картинку поверх векторных слоёв и рельефа; координаты
нужны, чтобы перевести масштаб картинки из см в координаты и всё работало как на обычной
карте, только масштаб по картинке, обозначения и датчики на картинке, и граница
отображения тогда только картинка. Папку `geo_cache/картинки карт местности` открывать
автоматически, `.jpeg` или `.png`».

### 8.5.2. Как решать

Это **выполнимо и недорого**: подложка уже кладётся ровно так же — `ImageItem` +
`setRect` в километрах (`basemap_mixin.py:80`, `:173`). Своя картинка — тот же приём.

1. **Папка** `geo_cache/карты_картинки/` — создать, `QFileDialog` открывать сразу в ней
   (образец вызова — `threat_view.py:2360` `_pick_file`), фильтр `*.png *.jpg *.jpeg`;
2. **Окно «Добавить карту»**: путь к файлу + 4 поля координат углов (СЗ, СВ, ЮВ, ЮЗ) в
   градусах. Рядом — кнопка «взять из имени файла», если имя вида
   `map_39.88_62.59_41.75_63.09.png`;
3. **Чтение** — `PIL` (есть, 12.2.0) → numpy RGBA → `pg.ImageItem`, `setZValue` **выше**
   векторных слоёв и рельефа, но ниже датчиков и маршрутов;
4. **Привязка**: углы (lon, lat) → км (`geo_frame` из 8.3) → `setRect`. ⚠️ Так работает
   только для картинки, у которой стороны идут по параллелям и меридианам. Если у карты
   есть поворот или это скан с рамкой — нужен аффинный пересчёт по трём-четырём точкам;
   умолчание: **только прямоугольная привязка**, поворот — вопрос В5;
5. **Режим «только картинка»**: при включении гасить тайловую подложку и ограничить
   район показа рамкой картинки (`_set_view_limits`, `basemap_mixin.py:110` — уже умеет);
6. **Масштаб** (задача 8.4) при этом считается по той же формуле — картинка ничем не
   отличается от тайлов, ей тоже задан км-экстент.

### 8.5.3. Ловушки

* ⚠️ **Проекция картинки.** Если это скан топокарты (Гаусса-Крюгера) или снимок в UTM, а
  мы кладём его линейно по углам — внутри будет сдвиг, тот же по природе, что в 0.4.
  Для листа 1:50 000 (≈20 км) сдвиг мал; для листа масштаба 1:1 000 000 — километры.
  Записать предупреждение в окне;
* **порядок слоёв**: заказчик просит картинку **поверх** векторов и рельефа, но датчики
  и маршруты должны остаться видны — значит `ZValue` между ними; проверить по
  `_build_scene_items` (`threat_view.py:1233`), там заданы все Z;
* **большие файлы**: снимок 10 000×10 000 — 400 МБ в RGBA. Прореживать при загрузке до
  разумного размера (образец — `load_dem_display`, `threat_grid.py:1458`, там уже есть
  `max_px`);
* картинка **не даёт данных для расчёта** — веса по-прежнему из векторных слоёв. Это
  надо сказать в интерфейсе прямо, иначе ожидание «загрузил карту — считает по ней».

### 8.5.4. Проверка

Картинка с известными углами ложится так, что узнаваемый объект (мост, перекрёсток)
совпадает с тем же объектом векторного слоя с точностью до ячейки (500 м); датчики,
поставленные на картинке, имеют те же координаты, что и без неё.

---

## Задача 8.6 — история полётов: выгрузка и загрузка  ✅ СДЕЛАНО 06.09.2026

> ✅ **СДЕЛАНО 06.09.2026.** Формат ниже (8.6.2) описывает ПЕРВОНАЧАЛЬНЫЙ черновик
> (`# UAV-FLIGHTS v1`, участок/рамка/якорь в шапке, километры в файле) — он был
> ПЕРЕСМОТРЕН до реализации в пользу более простого и уже согласованного с датчиками
> формата `# UAV-ROUTES v1` (только градусы, без проверки участка). Итог, реальные
> имена файлов и функций — в 8.6.6 ниже; полное описание формата — уже не здесь, а в
> [МЕТОДИЧКА_ВВОД_ВЫВОД §5](../МЕТОДИЧКА_ВВОД_ВЫВОД.md) (он ЖИВОЙ и переживёт следующую
> правку формата, план — нет). 8.6.1–8.6.5 ниже оставлены как есть — это ПОСТАНОВКА
> задачи и черновой разбор ДО решения, полезны как история выбора.

### 8.6.1. Что требуется

Заказчик: «две кнопки. 1 — выгрузить историю полётов: после окончания моделирования,
если все итерации прошли, создаётся в папке txt-файл с названием „дата — история
полётов“, и надо по каждому полёту из выборки предоставить информацию, координаты
движения. 2 — загрузить историю полёта: открывается другая папка, файл того же формата,
после чего он загружается в программу, и по нему возможно до итераций выстроить датчики;
новый чекбокс показывает старую выборку до начала итераций, с пуском итераций он уже не
работает и датчики строятся по своей выборке. Если файл не подходит или пакетное
моделирование не завершилось — предупреждение, что нет данных».

Сейчас выгрузки нет **вообще** (проверено: ни `csv`, ни `savetxt`, ни `json.dump` в
`model/`, `view_qt/`, `controller_qt/`). Маршруты живут в памяти: `self.iter_routes` —
список массивов `(M, 2)` в км (`threat_grid.py:1944`).

### 8.6.2. Формат файла

Текстовый, самоописывающийся, с шапкой — чтобы «файл не подходит» определялось надёжно:

```
# UAV-FLIGHTS v1
# участок: plesetsk_wide
# рамка: 39.8794 62.5901 41.7516 63.0869
# якорь: 39.8794 62.5901
# итераций: 150   маршрутов: 150   дата: 2026-09-03 14:21
# режим: mix/mix/even   запас хода: 187.4 км (авто)
#
# route  point      lon         lat        x_km     y_km
      1      1   41.7012   62.9014    92.881   34.719
      1      2   41.6847   62.8967    92.043   34.196
      …
```

* **и координаты, и километры** — координаты переносимы между участками, километры
  позволяют загрузить файл без пересчёта и сверить якорь;
* шапка даёт проверку пригодности: другой участок или другая рамка → предупреждение;
* расширение `.txt` (как просил заказчик), разделитель — пробелы.

### 8.6.3. Как решать

1. **Новый модуль** `model/flight_log.py` (правило «новый механизм — свой файл»):
   `save_flights(path, model)`, `load_flights(path)` → список массивов + шапка,
   `check_compatible(header, model)` → причина несовместимости или `None`;
2. **Кнопка «Выгрузить историю полётов»** — активна только когда
   `len(model.iter_routes) >= model.p.threat_iter_routes` (все итерации прошли); иначе
   тусклая, подсказка «пакет не завершён»;
3. **Папка выгрузки** — `истории_полётов/` в корне проекта, имя файла
   `2026-09-03_история_полётов.txt`; при повторе за день — суффикс `_2`;
4. **Кнопка «Загрузить историю полёта»** — открывает ту же папку, читает, проверяет
   шапку. Успех → маршруты кладутся в **отдельное** поле `model.loaded_routes` (не в
   `iter_routes`!), и по ним можно поставить датчики до итераций;
5. **Чекбокс «показать загруженную выборку»** — рисует `loaded_routes` своим цветом;
   при старте итераций (`on_iter_play`, `threat_controller.py:204`) чекбокс гаснет и
   расстановка переключается на свою выборку — как и просил заказчик;
6. **Предупреждения** — через уже имеющийся `flash_title` (`threat_view.py:1489`).

### 8.6.4. Ловушки

* ⚠️ **Не подменять `iter_routes`.** Если загруженное положить туда же, счётчик итераций
  и все показатели соврут. Отдельное поле и отдельный слой показа;
* **размер файла**: 150 маршрутов × ~200 точек × ~60 байт ≈ **1.8 МБ** — приемлемо; при
  1000 итераций будет 12 МБ, предупредить;
* **якорь**: файл со старого участка даёт километры от другого угла. Поэтому в шапке
  якорь и рамка — сверять обязательно, иначе маршруты лягут со сдвигом молча;
* число точек в маршрутах **разное** — формат «строка на точку» это выдерживает, формат
  «строка на маршрут» нет;
* заказчик просил «колонки движения» — понято как координаты по точкам; если нужны ещё
  скорость/курс/высота, это вопрос В6.

### 8.6.5. Проверка

Выгрузить → загрузить → расставить датчики: позиции совпадают с теми, что были до
выгрузки (файл ничего не теряет). Файл от другого участка даёт понятное предупреждение,
а не сдвинутые маршруты.

### 8.6.6. Итог — как сделано на самом деле ✅ 06.09.2026

Реализация отличается от черновика 8.6.2–8.6.4 в двух местах — оба сознательные
упрощения, найденные при сверке с методичкой ДО, а не после кода:

* **формат — `# UAV-ROUTES v1`, только градусы**, без участка/рамки/якоря и без
  километровых колонок. Причина: участок и якорь избыточны, если маршрут всё равно
  самодостаточен по координатам — ровно та же логика, что уже принята для файла
  датчиков (§8.7.7) и явно записана как решение в
  [МЕТОДИЧКА_ВВОД_ВЫВОД §5](../МЕТОДИЧКА_ВВОД_ВЫВОД.md), «открытые вопросы»;
* **папка выгрузки — `маршруты/выгрузка` и `маршруты/загрузка`**, а не одна
  `истории_полётов/` — та же схема «читаем ≠ пишем», что и у датчиков (МЕТОДИЧКА_ВВОД_
  ВЫВОД §1), а не отдельное правило только для маршрутов.

Что сделано по коду (правило «новый механизм — свой файл» соблюдено):

| Что | Где |
|---|---|
| запись/чтение файла, проверка формата | `model/flight_log.py` — `save_flights`, `load_flights`, `check_compatible`, `routes_dir`, `default_file_name` |
| отдельное поле для загруженного (НЕ `iter_routes`) | `model/threat_grid.py` :: `loaded_routes`, `show_loaded_routes`, `load_routes_lonlat`, `clear_loaded_routes`, `iterations_complete` |
| приоритет выборки для расстановки | `threat_grid.py` :: `_sample_for_sensors` — свои итерации (≥5) → загруженная (если галка) → «все возможные пути» |
| кнопки, слой на карте, чекбокс | `view_qt/threat_view.py` :: `btn_flights_save`/`btn_flights_load`, `route_loaded_item` (жёлтый пунктир), `chk_loaded_routes` |
| обработчики (проверка готовности, предупреждения) | `controller_qt/threat_controller.py` :: `on_flights_save`, `on_flights_load` |
| «Сброс» убирает и загруженную историю | `threat_controller.py` :: `on_reset` — `clear_loaded_routes()` рядом с `clear_manual_sensors()` |

**Проверено** `tools/gui_check.py`, раздел «7. ИСТОРИЯ ПОЛЁТОВ»: незавершённый пакет
не даёт выгрузить («нет данных»), запись → чтение совпадает до точности формата
(< 1 м, 12 маршрутов), загрузка кладёт маршруты в `loaded_routes`, а не в `iter_routes`,
до итераций и с галкой источник расстановки — загруженная выборка, после накопления
≥5 своих итераций — безусловно своя, файл не в формате `UAV-ROUTES` отклоняется с
причиной, «Сброс» очищает. `tools/flow_check.py` и остальные проверки документации
(`docmap`, `check_names`, `check_numbers`, `check_claims`, `check_dead_code`) прогнаны
после правки — чисто, новых расхождений нет.

### 8.6.7. Обратная связь заказчика (06.09.2026) — четыре правки после первого прохода

Заказчик проверил 8.6.6 и попросил четыре доработки, все сделаны в тот же день:

1. **Формат стал компактным.** Было — строка на точку (150 маршрутов × ~60 точек =
   9 000 строк, «очень длинно»); стало — строка на МАРШРУТ, точки через `" | "`:
   `1 - 40.881100 62.793400 | 2 - 40.872000 62.788100 | …`. 150 маршрутов → 150 строк.
   Разбор построчный: одна плохая точка внутри строки не роняет ни строку, ни маршрут.
2. **Кнопка «Удалить выборку»** — рядом с «Выгрузить»/«Загрузить», убирает ТОЛЬКО
   `loaded_routes` (датчики, цель, зоны не трогает — в этом отличие от «Сброса»).
3. **Заголовок называет источник расстановки.** Новое поле `ThreatModel.sensor_source`
   (`iter`/`loaded`/`zone`/`weight`/`manual`); если сработала загруженная история —
   заголовок так и пишет: «Датчиков N (расстановка по ЗАГРУЖЕННОЙ истории маршрутов)».
4. **Окно «Посмотреть маршруты»** (кнопка сверху справа в окне «Исходные данные»,
   новый файл `view_qt/route_viewer.py`) — список своих маршрутов и загруженной истории,
   таблица точек, подсветка маршрута на карте (белая линия, `route_preview_item`) и
   снятие подсветки. ⚠️ Список ДИНАМИЧЕСКИЙ — обновляется после каждого пройденного
   маршрута, а не только при переоткрытии окна («в таблице чтобы сразу записывались
   маршруты после прохода» — заказчик).

**Два бага найдены и исправлены при самопроверке** (не заказчиком — раньше):
* логика «выбор следует за последним маршрутом» ошибочно срабатывала и при ПЕРВОМ
  открытии окна (список пуст, `prev_row=-1`, `prev_count=0` — совпадение `-1 == -1`
  ложное), уводя выбор на последний маршрут вместо первого. Условие `prev_count > 0`
  чинит это;
* тесты `gui_check.py` падали от штатной стохастики `iter_batch`/`iter_step` (журнал
  п. 202: изредка не добирают заказанное число за одну попытку) — добавлены 3–5
  повторных попыток внутри самих тестов вместо одной.

**Проверено** `tools/gui_check.py`, раздел «7. ИСТОРИЯ ПОЛЁТОВ» вырос до **30 проверок**
(было 18): компактность файла (число строк-точек = числу маршрутов), метка
`sensor_source == "loaded"`, окно «Посмотреть маршруты» целиком (списки, таблица,
подсветка для обоих источников), динамическое обновление с сохранением/следованием
выбора, «Удалить выборку» (в т.ч. повторный клик на пустом — не падает). Два прогона
подряд — чисто. `flow_check.py`, `check_names`, `check_dead_code` — без новых расхождений.

---

## Задача 8.7 — «Исходные данные», четыре типа датчиков, таблица и ручная постановка  ✅ СДЕЛАНО 05.09.2026

> **⏩ С ЧЕГО НАЧАТЬ — ЧИТАТЬ ПЕРВЫМ ДЕЛОМ ПОСЛЕ СЖАТИЯ КОНТЕКСТА.**
>
> Состояние на 04.09.2026: план согласован полностью (компоновка окна — 8.7.1а, два
> режима — 8.7.1, формат файла — 8.7.7), **код ещё не тронут**. Ветка `ui-sensors`,
> дерево чистое.
>
> **Шаг 0, до всего остального:** снять эталон заново —
> `python tools/reference_run.py`, числа вписать в [ОГРАНИЧЕНИЯ §6](../ОГРАНИЧЕНИЯ.md) и
> в `tools/reference_run.py` :: `ETALON`. Нынешний снят на участке `plesetsk_wide`, а
> активен `arh` (район 120.5 × 73.0 км, сетка 241 × 146) — сверять не с чем, а без
> опорных чисел нельзя доказать, что перевод на четыре типа ничего не сломал.
>
> **Шаг 1:** `SensorSpec` и четыре типа в `config.Params`, `manual_sensors` в
> `ThreatModel` — ⚠️ старые имена `threat_N`, `threat_R`, `threat_k`, `threat_N_big`,
> `threat_R_big` оставить синонимами типа 1 и типа 4, они читаются в десятке мест.
>
> **Шаг 2:** контрольный прогон — с пустыми типами 2–3 числа обязаны совпасть с новым
> эталоном. Дальше по порядку из 8.7.6.
>
> Проверки под рукой: `tools/flow_check.py` (модель), `tools/gui_check.py` (интерфейс),
> `tools/reference_run.py` (числа).

### 8.7.1. Что требуется (уточнено заказчиком 04.09.2026)

**Панель приводится к трём кнопкам подряд**, в этом порядке:

1. **«Задать район моделирования»** (есть);
2. **«Добавить карту (картинкой)…»** (есть);
3. **«Исходные данные…»** — НОВАЯ, вместо двух прежних.

**Убрать с панели:** отдельный блок ввода параметров датчиков вверху и кнопку
«Датчики: параметры…». Всё это переезжает в окно «Исходные данные». Кнопка
«Выбрать цифровые карты…» остаётся отдельно.

**ДВА РЕЖИМА РАБОТЫ ОКНА** — переключатель в самом верху, рядом с кратностью:

| Режим | Откуда берутся позиции | Роль таблицы внизу |
|---|---|---|
| **«Рассчитать позиции»** (по умолчанию) | датчики расставляет алгоритм по выборке возможных пролётов — как было до сих пор | ⚠️ **показывает РЕЗУЛЬТАТ**: после расстановки все датчики попадают в таблицу с координатами |
| **«Задать позиции»** | датчики стоят ровно там, где их добавили: мышью, из файла или строкой в таблице | таблица — **источник**: что в ней, то и на карте |

⚠️ **Таблица заполняется в обоих режимах** — это отдельное требование заказчика. В первом
режиме она отвечает на вопрос «а куда программа их поставила и с какими координатами»,
во втором — задаёт расстановку. Разница только в направлении: там читаем, тут пишем.

**Статические датчики работают в ОБОИХ режимах.** В режиме расчёта они закрепляются, а
алгоритм достраивает вокруг них остальные (8.7.3) — это и есть смысл слова «статический».

**Окно «Исходные данные»** — одно, крупное. В нём:

* **параметры датчиков по ЧЕТЫРЁМ типам:** типы 1–3 — малые (у каждого свои количество,
  радиус), тип 4 — большие, как сейчас. Заполнен тип 1 и тип 4; типы 2 и 3 пустые;
* **параметры маршрута** — всё, что сейчас в окне «Входные данные» (запас хода, скорость,
  крен, длина прямого участка, разрыв, уход от ориентира);
* **⚠️ ВЫБОР КРАТНОСТИ — В САМОМ ВЕРХУ ОКНА**, до параметров типов, потому что он меняет
  то, как заполняются поля ниже:

  | Выбор | Как ведут себя поля кратности у типов |
  |---|---|
  | **одна на всех** (по умолчанию) | кратность вводится в ОДНОЙ группе и **сразу дублируется во все остальные**; поля у прочих типов показывают то же число и правке не поддаются |
  | **у каждого своя** | поле кратности открывается в КАЖДОЙ группе и задаётся отдельно |

  Дублирование именно в момент ввода, а не при расчёте: человек должен видеть в полях то
  число, с которым программа будет считать, а не догадываться о нём.

  Шаг сетки — один на все типы в любом случае, типы 1–3 живут в одной сетке;
* **⚠️ ТАБЛИЦА ВНИЗУ ОКНА** (обязательно) — по строке на датчик:

  | № | тип (1–4) | режим | долгота | широта |
  |---|---|---|---|---|
  | 1 | 1 | динамический | 40.3312 | 62.7601 |
  | 2 | 4 | **статический** | 40.5100 | 62.8020 |

  с кнопками **изменить** и **удалить** выбранную строку, кнопками **«Из файла…»** и
  **«В файл…»** (формат — 8.7.7) и **«Добавить»** (строка вручную).

  ⚠️ **Таблица прокручивается колесом мыши** и держит сотни строк: при трёх типах по
  двадцать датчиков плюс большие это уже за полсотни строк, а заказчик может задать и
  больше. Высота таблицы фиксированная, содержимое скроллится — иначе окно вырастет за
  край экрана (см. ловушку про 1366×768).

**ДВА РЕЖИМА ДАТЧИКА — новое понятие:**

| Режим | До итераций (кнопка «Расставить») | При моделировании (итерации) |
|---|---|---|
| **динамический** | стоит там, где задан: заданное человеком не двигают | **перемещается** — место подбирается заново по накопленным пролётам |
| **статический** | стоит там, где задан | **стоит там же**: жадный алгоритм сперва учитывает его, потом доставляет остальные |

⚠️ **Различие между режимами проявляется ИМЕННО НА ИТЕРАЦИЯХ** (уточнение заказчика
05.09.2026). До них закреплено всё заданное человеком — иначе обстановку не задать;
после — динамический отпускается. Подробно, с двумя отвергнутыми редакциями:
[ОГРАНИЧЕНИЯ §8.1а](../ОГРАНИЧЕНИЯ.md).

Заказчик прямо оговорил: **оптимальность при этом падает — и это принято сознательно**,
ради проверки и ради возможности «поставить, где надо». Тип и режим спрашиваются в момент
добавления датчика в таблицу.

**Ручная постановка датчика мышью.** На основной панели — кнопка «Добавить датчик по
клику»: включает режим, в котором **двойной клик** по карте ставит датчик и добавляет
строку в таблицу; **перед добавлением спрашивается тип (1–4) и режим** (статический или
динамический). Ещё один двойной клик **вне** постановки — выход из режима.

### 8.7.1б. Что делает кнопка «Расставить датчики» — УТОЧНЕНО заказчиком 05.09.2026

Первая реализация вела себя не так, как задумано. Правила, по которым она обязана
работать (всё сказанное относится к расстановке **до итераций**):

| Обстановка | Что происходит по кнопке |
|---|---|
| режим **«Рассчитать позиции»**, вручную ничего не ставили | как раньше: все позиции подбирает алгоритм по весовой карте и выборке возможных пролётов |
| режим **«Задать позиции»** | ставятся **только заданные** датчики, ровно там, где заданы. Алгоритм не участвует |
| режим **«Рассчитать позиции»**, несколько датчиков поставлены вручную | **сначала ставятся они**, затем считается, сколько НЕ ХВАТАЕТ до заказанного числа, и алгоритм добирает остаток — с уже учтёнными якорями |

⚠️ **ЭТО УСЛОВИЯ МОДЕЛИРОВАНИЯ, А НЕ ЯКОРЯ** (уточнение заказчика 05.09.2026).
Различать обязательно:

| | ЯКОРЬ | УСЛОВИЯ МОДЕЛИРОВАНИЯ |
|---|---|---|
| кто назначил позицию | сам алгоритм (у входа и цели) | человек |
| что это по сути | самоограничение алгоритма | ВХОДНЫЕ ДАННЫЕ задачи |
| состояние | **отключены** 29.08.2026 (ОГРАНИЧЕНИЯ 1.3) | работают с 05.09.2026 |
| влияние на гарантию (1 − 1/e) | ломает обоснование | не ломает: оптимизируется остаток |

Жадный алгоритм расстановки работает **без якорей** — это по-прежнему так. А когда нужно
смоделировать определённую обстановку, заданные позиции учитываются первыми, и алгоритм
дорабатывает остаток. Технически они передаются тем же аргументом `greedy(anchors=…)` —
это способ передачи, а не суть.

⚠️ **ДО ИТЕРАЦИЙ учитывается ЛЮБОЙ поставленный вручную датчик**, а не только помеченный
«статическим»: человек кликнул по карте — значит место выбрал он; программа эту позицию
не двигает, а лишь добирает недостающие. **А при моделировании динамический
перемещается**, статический — нет: в этом и смысл двух режимов (заказчик 05.09.2026,
[ОГРАНИЧЕНИЯ §8.1а](../ОГРАНИЧЕНИЯ.md), журнал п. 230).

⚠️ **ЧТО ВИДНО НА КАРТЕ — ДВА РАЗНЫХ ПРИЗНАКА.** Форма значка говорит о режиме (квадрат
— статический, круг — динамический), **чёрная окантовка** — о том, что место задал
человек: кликом, строкой таблицы или из файла. Свести их в один нельзя: до итераций на
карте стоят и статические, и динамические, заданные человеком, — по форме они разные, а
поставлены одинаково; цвет же занят типом датчика.

⚠️ **КОЛИЧЕСТВО В РЕЖИМЕ «ЗАДАТЬ ПОЗИЦИИ» СЧИТАЕТ ПРОГРАММА.** Поля N становятся только
для чтения и показывают, сколько датчиков каждого типа сейчас в таблице; человек задаёт
**только радиус и кратность**. Причина: на карте будет ровно то, что в таблице, и поле
«N = 20» при трёх строках обещало бы несуществующее.

⚠️ **РЕЖИМ ПОСТАНОВКИ МЫШЬЮ ЗАБИРАЕТ ИНТЕРФЕЙС СЕБЕ.** Пока он включён, вся панель
погашена, живой остаётся одна кнопка — выход из режима. Иначе соседняя кнопка («указать
цель», «запретная зона») перехватила бы клик, которого ждёт постановка. Каждый
поставленный датчик **сразу виден на карте и сразу появляется в таблице** — не дожидаясь
расчёта: расстановка стоит секунды, а обратная связь на клик нужна мгновенно.

### 8.7.1а. Компоновка окна — УТВЕРЖДЕНА заказчиком 04.09.2026

Выбран вариант «слева датчики, справа маршрут, таблица во всю ширину снизу». Строить
по этой схеме, не изобретая заново:

```
┌─ Исходные данные ───────────────────────────────────────────────────────┐
│ Режим:  (•) Рассчитать позиции      ( ) Задать позиции                   │
│ Кратность засечки:  (•) одна на всех   ( ) у каждого своя      k = [3]   │
├──────────────────────────────────┬──────────────────────────────────────┤
│ ДАТЧИКИ                          │ МАРШРУТ БПЛА                         │
│ ┌─ Тип 1 · малый ──────────────┐ │   запас хода, км    [ 200 ] ☑ авто   │
│ │ N [20]   R, км [2.0]  k [3]  │ │   скорость, км/ч    [ 180 ]          │
│ └──────────────────────────────┘ │   крен, °           [  25 ]          │
│ ┌─ Тип 2 · малый ──────────────┐ │   прямой участок,км [ 8.0 ]          │
│ │ N [ 0]   R, км [3.0]  k [3]  │ │   разрыв, км        [ 2.0 ]          │
│ └──────────────────────────────┘ │   уход вбок, км     [ 1.0 ]          │
│ ┌─ Тип 3 · малый ──────────────┐ │                                      │
│ │ N [ 0]   R, км [5.0]  k [3]  │ │   шаг сетки, км     [ 1.0 ] ☑ авто   │
│ └──────────────────────────────┘ │   разнос (доля R)   [0.75 ]          │
│ ┌─ Тип 4 · БОЛЬШОЙ ────────────┐ │                                      │
│ │ N [ 3]   R, км [ 15]  k [3]  │ │   кольцо у цели [0.55] … [1.00]      │
│ └──────────────────────────────┘ │                                      │
├──────────────────────────────────┴──────────────────────────────────────┤
│ ДАТЧИКИ НА КАРТЕ        [Из файла…] [В файл…] [Добавить]                 │
│ ┌───┬─────┬──────────────┬───────────┬───────────┐  ▲                   │
│ │ N │ тип │ режим        │ долгота   │ широта    │  █  ← прокрутка      │
│ ├───┼─────┼──────────────┼───────────┼───────────┤  █                   │
│ │ 1 │  1  │ динамический │ 40.881100 │ 62.793400 │  ░   [Изменить]      │
│ │ 2 │  1  │ статический  │ 40.512000 │ 62.802000 │  ░   [Удалить ]      │
│ │ 3 │  4  │ статический  │ 40.330000 │ 62.760000 │  ▼                   │
│ └───┴─────┴──────────────┴───────────┴───────────┘                      │
│                                       [Применить]  [Закрыть]            │
└─────────────────────────────────────────────────────────────────────────┘
```

Отвергнуты: «типы в ряд» (параметры маршрута уезжают под них и теряются) и «две вкладки»
(датчики и маршрут не видно одновременно, а их подбирают вместе).

### 8.7.2. Что уже есть — и что из этого уходит

| Есть сейчас | Где | Судьба |
|---|---|---|
| окно «Входные данные» (запас хода, скорость, крен, разрыв, уход) | `threat_view.py` :: `InputDataDialog` | **переезжает** в «Исходные данные» |
| окно «Датчики» (N, R, кратность, шаг сетки, разнос, кольцо больших) | `threat_view.py` :: `SensorsDialog` | **переезжает** туда же |
| кнопки «Входные данные…» и «Датчики: параметры…» | `threat_view.py` :: `_build_ui` | **убрать обе** |
| окно «Цифровые карты» | `threat_view.py` :: `DigitalMapsDialog` | остаётся отдельной кнопкой |
| параметры двух типов (`threat_N`, `threat_R`, `threat_N_big`, `threat_R_big`, `threat_k`) | `config.py` :: `Params` | становятся типом 1 и типом 4 |
| ⚠️ **механизм якорей в жадном выборе** | `model/optimization.py` :: `CoverageCache.greedy(anchors=…, anchor_sep=…)` | **оживает** — на нём делаются статические датчики |
| режим кликов по карте (цель, сектор, зона, район) | `threat_view.py` :: `_on_scene_click` | образец для режима постановки |

### 8.7.3. Ключевое решение: статический датчик — это якорь

Механизм **уже написан и проверен**: `greedy` принимает `anchors` — список позиций,
которые берутся принудительно, до жадного отбора, и вокруг них соблюдается разнос. Его
отключили 29.08.2026 ([план 7, §2.1·0](7_ЕДИНЫЙ_ПЛАН.md)), потому что якоря ставились
САМИ, у входа и цели, и в вырожденном случае стерегли один вход из пяти.

Теперь он возвращается в другом качестве: якорь — это **позиция, названная человеком**.
Разница принципиальная: алгоритм больше ничего не выдумывает, он исполняет.

⚠️ **Гарантия (1 − 1/e) на закреплённой части не действует** — и это ожидаемо: часть
решения задана извне, оптимизируется только остаток. В [ОГРАНИЧЕНИЯ 1.14](../ОГРАНИЧЕНИЯ.md)
список «что ломает гарантию» придётся дополнить третьим пунктом, с пометкой «по требованию
заказчика, осознанно».

### 8.7.4. Как решать — по шагам

1. **Типы датчиков в модели.** Ввести `SensorSpec(n, r_km, k, kind)` и список из четырёх.
   ⚠️ **Старые имена `threat_N`, `threat_R`, `threat_k`, `threat_N_big`, `threat_R_big`
   сохранить синонимами типа 1 и типа 4** — они читаются в десятке мест (расстановка,
   показатели, автошаг сетки, эталон, окна), и переименование расползётся по всему проекту;
2. **Список заданных вручную датчиков** — в `ThreatModel`: `manual_sensors` = список
   записей `(тип, режим, x_км, y_км)`. Живёт рядом с `no_fly_zones` и **переживает
   пересборку карты** так же, как зоны: он задан человеком и от местности не зависит;
3. **Окно «Исходные данные» — ОДИН файл `view_qt/input_window.py`, целиком.**
   Слева параметры типов, справа параметры маршрута, внизу таблица `QTableWidget` с
   кнопками «изменить»/«удалить». ⚠️ **Не дробить окно на модули** (решение заказчика
   04.09.2026): в одном файле и сама форма, и таблица, и маленький диалог «тип и режим»,
   и чтение-запись файла датчиков. Это ровно правило проекта — порога на размер файла нет,
   связанный код должен лежать вместе ([CLAUDE.md](../../CLAUDE.md), журнал п. 212);
   отдельный файл заводится под НОВЫЙ механизм, а не под каждую часть одного окна.
   Отдельный модуль здесь один — сам `input_window.py`, потому что дописывать это в
   `threat_view.py` (под 2 700 строк) значит смешать две разные вещи: карту и окно ввода;
4. **Режим постановки мышью** — по образцу `_begin_area`/`_area_click`: включается
   кнопкой, двойной клик ставит, повторный двойной клик выключает. Перед добавлением —
   маленький модальный диалог «тип (1–4) и режим (статический/динамический)»;
5. **Расстановка со статическими.** В `_place_by_routes` собрать индексы кандидатов,
   ближайших к статическим позициям, и передать их в `greedy(anchors=…)`. Динамические
   типы 1–3 идут в один `CoverageCache` при общей кратности; при раздельной — свой проход
   на тип. Тип 4 (большие) — как сейчас, `_place_big`;
6. **Показ.** Датчики рисуются с номером (задача 8.8) и различаются по типу; статические
   пометить отдельно — например, квадратным маркером вместо круглого, чтобы на карте было
   видно, что эта позиция закреплена;
7. **Память полей — обязательна** (правило заказчика, уже действует для окна своей карты).
   Механизм готов: `view_qt/ui_state.py`, раздел на окно. Сохранять **в момент нажатия
   «Добавить»** и при применении параметров: тип и режим последнего добавленного датчика,
   все поля типов и параметры маршрута. При проверке одно и то же вводится десятки раз;
8. **Загрузка датчиков ИЗ ФАЙЛА** — кнопка в окне рядом с таблицей: прочитать `.txt`
   (формат — 8.7.8), показать в таблице, отметить, сколько строк принято и сколько
   отброшено и почему. Выгрузка того же формата — задача 8.8, **формат общий**.

### 8.7.5. Ловушки

* ⚠️ **Эталон §6 обязан совпасть**, пока типы 2–3 пусты и статических нет: это и есть
  проверка, что перевод на типы ничего не сломал. Сейчас эталон снят на `plesetsk_wide`,
  а активен `arh` — **сперва снять эталон заново**, иначе сверять не с чем;
* **статический датчик вне зоны пролёта** — законная ситуация (человек так решил), но
  он не даст покрытия. Не «чинить» подтягиванием к зоне: показать в показателях, сколько
  статических датчиков не видят ни одного маршрута;
* **шаг сетки кандидатов при разных радиусах** — брать по МИНИМАЛЬНОМУ радиусу, иначе для
  мелкого типа сетка окажется грубой (замер: шаг 3 км при R = 2 км роняет засечку
  96.7 % → 82.7 %);
* **разнос между датчиками РАЗНЫХ типов**: сейчас `min_sep = 1.6·R` от одного радиуса.
  Умолчание — полусумма радиусов пары (открытый вопрос В7);
* **статическую позицию нельзя «уплотнять» разносом**: если человек поставил два датчика
  рядом, программа обязана оставить оба. Разнос применяется только к тому, что выбирает
  она сама;
* окно «почти во весь экран» на ноутбуке **1366×768** не должно требовать прокрутки для
  главных полей — проверить на этом размере.

### 8.7.6. Порядок работы (первым делом — модель)

| № | Шаг | Почему в этом порядке |
|---|---|---|
| 1 | типы датчиков и `manual_sensors` в модели, старые имена — синонимами | от них зависят и окно, и таблица, и расстановка; ломать интерфейс, не имея данных, некуда |
| 2 | контрольный прогон: числа не изменились | пока типы 2–3 пусты, всё обязано совпасть — это опора для остальных шагов |
| 3 | окно «Исходные данные» с таблицей, кнопки панели | самая объёмная часть, но она уже опирается на готовые данные |
| 4 | режим постановки мышью + диалог «тип и режим» | нужна таблица из шага 3, чтобы было куда добавлять |
| 5 | статические как якоря в `_place_by_routes` | нужен список из шага 1 и позиции из шага 4 |
| 6 | показ: номера, разные типы, метка статического | заодно закрывает часть задачи 8.8 |

### 8.7.7. Формат файла датчиков (общий на чтение и запись)

Один формат и для загрузки в окне (8.7), и для выгрузки после расчёта (8.8): файл,
выгруженный программой, обязан читаться ею же без правки руками.

```
# UAV-SENSORS v1
# дата: 2026-09-04 12:35
#
# типы датчиков:
#   тип  класс    N   R, км   кратность k
#     1  малый    20    2.0        3
#     2  малый     0    3.0        3
#     3  малый     0    5.0        3
#     4  большой   3   15.0        3
#
#  N  тип  режим           lon          lat
#--- ---- -------------- ----------- -----------
    1    1  динамический    40.881100    62.793400
    2    1  статический     40.512000    62.802000
    3    4  статический     40.330000    62.760000
```

⚠️ **В шапке НЕТ ни участка, ни области, ни района, ни якоря** — решение заказчика
04.09.2026. Они были нужны только ради колонок с километрами, а километры в файле не
нужны вовсе: **градусы самодостаточны**, километры программа считает сама по своему
якорю. Убрали километры — отпал и якорь, и вся привязка к участку. Заодно файл стал
переносимым: те же датчики можно загрузить в другой район, и они лягут по координатам.

**Правила, которые делают файл читаемым обратно:**

| Правило | Зачем |
|---|---|
| всё, что начинается с `#` — комментарий | шапку можно дополнять, не ломая чтение |
| колонки разделены пробелами, число пробелов не важно | файл правят руками в блокноте, ровнять по столбцам никто не станет |
| режим принимается и словом (`статический`/`динамический`), и буквой (`s`/`d`) | по-русски читаемо человеку, по-английски — надёжно при чужой кодировке |
| координаты — **только градусы** | не зависят ни от якоря, ни от района: файл переносится между участками |
| типы читаются из шапки, но **не обязательны** | нет шапки — берутся текущие параметры окна; файл, набранный руками из трёх строк, тоже должен читаться |
| строка с непонятным содержимым пропускается, а не роняет чтение | отчёт «принято 18, отброшено 2 (строки 7, 12)» полезнее, чем отказ читать весь файл |
| датчик вне района — принимается с предупреждением | человек мог задать позиции заранее, до выбора района |

⚠️ **Кодировка — UTF-8 без BOM**, перевод строки любой. Блокнот Windows по умолчанию
пишет UTF-8 с BOM: при чтении первую строку чистить от `﻿`, иначе `# UAV-SENSORS`
не опознаётся и файл считается чужим.

Чтение и запись этого файла живут **в том же `input_window.py`**, что и окно — отдельный
модуль под них не заводим (решение заказчика 04.09.2026: не дробить функционал).
История полётов (8.6) — другой механизм и другой файл, но разбор шапки у них
одинаковый: строки с `#` пропускаются, колонки по пробелам.

### 8.7.8. Проверка

* с пустыми типами 2–3 и без статических — `python tools/reference_run.py` даёт числа
  эталона без расхождений;
* заполнили тип 2 — число датчиков на карте выросло ровно на заказанное;
* поставили статический датчик мышью — он **остался ровно там, куда поставлен**, после
  «Расставить», после итераций и после пересборки карты;
* таблица показывает все датчики с координатами; «удалить» убирает и строку, и датчик с
  карты; `python tools/gui_check.py` дополнить проверкой таблицы и режима постановки;
* **файл читается обратно:** выгрузили датчики → загрузили тот же файл → позиции
  совпали до последнего знака, типы и режимы сохранились;
* **память полей:** закрыли окно, открыли снова — тип и режим последнего добавленного
  датчика и все параметры на месте; то же после перезапуска программы.

---

## Задача 8.8 — выгрузка датчиков и номера на карте  ✅ В ОСНОВНОМ СДЕЛАНО 05.09.2026

### 8.8.1. Что требуется

Заказчик: «во-первых, также перевести датчики в координаты; при нажатии кнопки выгрузить
данные после пакета — чтобы выгружался файл того же типа, что в пункте 2, и собирался в
отдельную папку; также сделать отображение датчиков по номеру — небольшая цифра».

### 8.8.2. Как решать

1. **Формат** — как в 8.6, но по датчикам:

```
# UAV-SENSORS v1
# участок: plesetsk_wide   рамка: …   якорь: …   дата: …
# тип 1: N=20 R=2.0 км | тип 4 (большие): N=3 R=15.0 км
#
#  N  тип      lon        lat       x_km     y_km
   1    1   40.8811   62.7934    45.912   22.617
```

2. **Папка** — СДЕЛАНО 05.09.2026, но иначе, чем задумывалось: `датчики/` с двумя
   подпапками, **`загрузка`** и **`выгрузка`** (требование заказчика). Имя файла по
   умолчанию — дата и время: **`2026_09_05_09_15_sensor.txt`** (год первым, разделитель
   `_`: так имена сортируются по алфавиту в том же порядке, что и по времени, а
   двоеточие Windows в имени не допускает). Папки создаются сами при первом обращении и
   считаются от КОРНЯ ПРОГРАММЫ, а не от текущей директории. Отдельного модуля не
   заводили: чтение и запись живут в `view_qt/input_window.py` вместе с окном
   (`sensors_dir`, `default_file_name`);
3. **Номера на карте**: `TextItem` рядом с каждым датчиком, кегль ~8 пт, как сделано для
   подписей НП (`_render_place_labels`, `threat_view.py:1631` — там уже решена задача
   «текст постоянного размера при зуме», брать оттуда). Показ — по чекбоксу, иначе при
   20 датчиках карта зарябит;
4. **Загрузка** этого же файла — вход для режима «задать позиции» из 8.7.

### 8.8.3. Ловушки

* нумерация **должна быть устойчивой** — иначе выгруженный «датчик 3» и показанный на
  карте «датчик 3» окажутся разными точками. Это задача 8.9, делать вместе;
* большие датчики нумеровать **отдельным рядом** (Б1, Б2, Б3) — иначе номера малых
  поедут при смене их числа;
* подписи не должны перекрывать сам маркер — смещение как у подписей НП (`anchor`).

### 8.8.4. Проверка

Выгруженный файл загружается обратно в режим «задать позиции», и датчики встают ровно на
те же места (расхождение 0 м); номер на карте совпадает с номером в файле.

---

## Задача 8.9 — анализ размещения: стрелки и устойчивые номера  ✅ СДЕЛАНО 06.09.2026

### 8.9.1. Что требуется

Заказчик: «реализуй историю построения через чекбокс: во время итерации или после всех
итераций можно включить чекбокс „анализ размещения“, и он показывает текущее положение,
и добавляются элементы 1-го построения положения датчиков, и рисуется стрелка от
начального к конечному положению. Как понимаю, в алгоритме надо учесть, чтобы датчик
номер 1 при подстройке совершал минимальные перемещения, чтобы красиво выглядел анализ,
куда кто переместился — а то был 1 датчик слева у карты, а в конце будет справа у края.
Это возможно реализовать?»

### 8.9.2. Ответ: возможно, и правильный способ — не трогать алгоритм

**Почему датчик «прыгает».** Номер датчика сейчас — это **порядок жадного выбора**:
`greedy` возвращает `self.cand[chosen]` в том порядке, в каком позиции выбирались
(`optimization.py:211`). При пересчёте по новой выборке порядок меняется целиком —
позиция, бывшая первой, может стать седьмой или исчезнуть. Отсюда «был слева, стал
справа».

**Что НЕ надо делать:** заставлять жадный алгоритм выбирать позиции поближе к прежним.
Это добавит к задаче ограничение, которого в ней нет, и снимет гарантию (1 − 1/e) —
ровно то, от чего избавились, сняв якоря ([план 7, §2.1·0](7_ЕДИНЫЙ_ПЛАН.md)).

**Что надо:** оставить выбор позиций как есть, а **номера присваивать сопоставлением** —
после расстановки решить задачу о назначениях: какой новый датчик считать «тем же
самым», что старый, чтобы суммарное перемещение было минимальным. Это чистая
перенумерация: позиции не двигаются, качество покрытия не меняется ни на грамм.

### 8.9.3. Как решать

1. **Новый модуль** `model/sensor_track.py`: история положений `[(шаг, позиции)]` +
   сопоставление номеров;
2. **Алгоритм сопоставления — ВЕНГЕРСКИЙ, своей реализацией** (решение заказчика
   03.09.2026). Задача о назначениях на матрице расстояний 20×20: 60–80 строк numpy,
   считается мгновенно и даёт **оптимальное** сопоставление, а не приблизительное.
   Жадный вариант (брать ближайшую пару и вычёркивать) — только как запасной на случай
   отладки: он проще, но суммарное перемещение у него больше.

   ⚠️ **`scipy` в проекте НЕТ и ставить его сейчас не будем** — проверено 03.09.2026:
   колесо `scipy 1.18.1` под `cp314-win_amd64` существует (35.6 МБ, встанет за минуту),
   но ради одной функции `optimize.linear_sum_assignment` это дорого, и главное — тянет
   риск в сборку `.exe`: PyInstaller забирает библиотеку целиком (~100 МБ распакованных),
   а с тяжёлыми зависимостями у нас уже был случай, когда `rasterio` пришлось собирать
   через `collect_submodules`, иначе рельеф **молча** отключался в готовой программе
   ([CLAUDE.md](../../CLAUDE.md), сборка). Вопрос вынесен в задел — см. 8.9.6;
3. **Появление и исчезновение**: если датчиков стало больше — новые получают следующие
   свободные номера; если меньше — освободившиеся номера не переиспользуются в пределах
   одного сеанса;
4. **Чекбокс «анализ размещения»** — рисует: полупрозрачные маркеры первого построения,
   сплошные — текущего, между ними **стрелка** (`pg.ArrowItem` либо линия с
   треугольником). Работает и во время итераций, и после;
5. **Когда фиксировать «первое построение»** — в момент первой расстановки до итераций
   (`place_sensors` при пустом `iter_routes`); дальше история пополняется на каждом
   пересчёте (`_maybe_refresh_sensors`, `threat_controller.py:224` — он уже вызывается
   периодически, туда и вписать).

### 8.9.4. Ловушки

* ⚠️ **Сопоставление не должно менять позиции.** Если по дороге захочется «чуть-чуть
  подвинуть датчик к прежнему месту» — это уже другая задача и другая гарантия. Граница
  жёсткая: алгоритм ставит, сопоставление только нумерует;
* **датчики разных типов не сопоставлять между собой** — тип 1 с типом 1, большие с
  большими;
* при 150 итерациях и пересчёте каждые `THREAT_SENSOR_REFRESH_EVERY` шагов история
  наберёт десятки состояний — хранить все, но рисовать по умолчанию только первое и
  последнее (иначе каша из стрелок);
* **стрелка нулевой длины** (датчик не двигался) — не рисовать вовсе, иначе на карте
  двадцать точек-клякс.

### 8.9.5. Проверка

Прогон 150 итераций с включённым чекбоксом: у каждого датчика ровно одна стрелка;
суммарная длина стрелок при сопоставлении меньше, чем при нумерации по порядку жадного
выбора (замерить оба числа — это и есть подтверждение, что сопоставление работает);
позиции датчиков с включённым и выключенным чекбоксом **совпадают точно**.

Отдельная проверка самого венгерского алгоритма: на случайных матрицах 5×5…20×20
сравнить его результат с полным перебором (для 5×5 это 120 вариантов) — суммы обязаны
совпасть до последнего знака. Без такой сверки своя реализация задачи о назначениях
опасна: она даёт правдоподобный, но не оптимальный ответ, и по картинке это не видно.

### 8.9.6. Задел: подключить scipy, если он понадобится не ради одной функции

Решение 03.09.2026 — **не ставить**, писать венгерский сами. Что scipy закрыл бы,
если когда-нибудь появится 2–3 задачи сразу:

| Наша реализация | Замена | Есть ли выигрыш сейчас |
|---|---|---|
| `_label8` — компоненты (`threat_grid.py:1197`) | `ndimage.label` | нет, сетка 192×110 считается мгновенно |
| `_dilate` / `_erode` (`:1221`, `:1237`) | `ndimage.binary_dilation/erosion` | нет |
| `_box_mean` (`:1153`) | `ndimage.uniform_filter` | нет, уже через интегральное изображение |
| перебор ближайших (разнос датчиков, `_place_big`) | `spatial.cKDTree` | появится при больших районах |
| венгерский алгоритм (8.9) | `optimize.linear_sum_assignment` | одна функция — ради неё не стоит |

**Признак вернуться к вопросу:** зависимость окупает себя в трёх и более местах либо
появляется задача, где своя реализация заведомо медленная (разреженные графы, KD-дерево
на сотнях тысяч точек). Тогда: `pip install scipy`, строка в `requirements.txt`
отдельным блоком и ⚠️ **обязательная проверка сборки `.exe` запуском собранной
программы**, а не окружением сборки.

### 8.9.7. Итог — как сделано на самом деле ✅ 06.09.2026

Реализация следует решению 8.9.2 буквально: алгоритм расстановки не тронут вовсе,
сопоставление — отдельный шаг после него.

| Что | Где |
|---|---|
| венгерский алгоритм (метод потенциалов, O(n³)) + сопоставление по типам | `model/sensor_track.py` — `hungarian`, `match_by_type`, `total_movement` (новый модуль — правило «новый механизм — свой файл») |
| снимок ПЕРВОГО построения (не перезаписывается) | `model/threat_grid.py` :: `initial_sensors`, `initial_sensors_type`, `initial_sensors_big`, захват внутри `place_sensors()` |
| сопоставление снимка с текущим состоянием | `threat_grid.py` :: `sensor_movement()` |
| источник последней расстановки (для заголовка) | `threat_grid.py` :: `sensor_source` (`iter`/`loaded`/`zone`/`weight`/`manual`) |
| чекбокс и отрисовка | `view_qt/threat_view.py` :: `chk_sensor_track` (группа «датчики и подписи», НЕ отдельная панель), `render_sensor_track` |

**Отступление от 8.9.3 п. 4** — чекбокс НЕ вынесен отдельной плашкой на карту, а сделан
обычной галочкой в существующей группе «датчики и подписи», рядом с «Датчики»,
«Кандидатные позиции» (заказчик 06.09.2026: «функция для показа анализа… всегда
[есть] чекбокс во вкладке чекбоксов датчики и подписи»). Показ: полупрозрачный серый
круг и точка — позиция и радиус ПЕРВОГО построения; чёрная стрелка с подписью
расстояния — куда датчик переехал к ТЕКУЩЕМУ. ⚠️ Серый круг рисуется **на 22 % шире**
настоящего радиуса (`TRACK_RADIUS_MARGIN`) — если датчик не двигался, круги совпадают
ТОЧНО и серый исчезает под текущим; запас оставляет ободок видимым. Стрелка короче
50 м (`TRACK_MIN_ARROW_KM`) не рисуется — датчик, по сути, не двигался.

**Задел, не реализован сейчас** — панель из ТРЁХ чекбоксов сверху справа карты
(«режим моделирования» / «режим анализа» / «построение маршрутов»), которую заказчик
сперва описал как переключатель показа. Оставлена как визуальная заготовка: три
чекбокса присутствуют, но их выбор **ни на что не влияет** (заказчик 06.09.2026:
«панель чекбоксов оставь, просто её выбор пока не влияет ни на что») — задел под
будущее разделение режимов работы вкладки.

**Два бага найдены и исправлены при проверке (не в первоначальном требовании):**
* ⚠️ **Снимок первого построения не брался НИКОГДА**, если нажать «Пуск» БЕЗ
  предварительного «Расставить датчики». Причина: условие захвата требовало
  `len(iter_routes) < 5`, а датчики впервые вообще считаются уже при `== 5`
  (`_maybe_refresh_sensors`, порог `THREAT_SENSOR_REFRESH_EVERY`) — заказчик увидел
  это как «датчики для анализа каждые 5 итераций перемещаются», хотя сравнивать было
  не с чем весь сеанс. Исправлено: снимок берётся на первом же реальном расчёте
  датчиков, кем бы он ни был вызван, и не переписывается больше никогда. Проверено
  регрессией — 25 маршрутов, снимок стабилен на каждом из 20+ периодических
  пересчётов подряд;
* ⚠️ **Кнопка «Очистить» в таблице окна «Исходные данные» не убирала данные о
  датчиках** — она жёстко убирала только заданные вручную, а в режиме «Рассчитать
  позиции» таблица показывает РЕЗУЛЬТАТ расстановки (вручную заданных там обычно нет
  вовсе), поэтому нажатие ничего не меняло. Исправлено: кнопка смотрит на текущий
  режим (`InputDataWindow._clear_rows`) — в «Задать позиции» убирает вручную заданные
  как раньше, в «Рассчитать позиции» убирает ВСЮ расстановку (`on_clear_sensors`, тот
  же колбэк, что у «Очистить датчики» на панели).

**Проверено** `tools/gui_check.py`, раздел «8. АНАЛИЗ РАЗМЕЩЕНИЯ»: венгерский алгоритм
сверен с полным перебором на 200 случайных матрицах до 5×5/5×6 (расхождение на уровне
погрешности float); сопоставление не смешивает типы; появление/исчезновение датчика
(разное число до/после) даёт пару БЛИЖНЕМУ, а не первому по порядку; чекбокс
показывает и снимает слой обычным путём (`on_toggle` → `_render_all`); суммарное
перемещение по венгерскому сопоставлению ощутимо меньше наивной нумерации по порядку
(в реальном прогоне — разы, не проценты); включённый анализ не меняет позиции
датчиков; нулевое перемещение не рисует стрелку; «Очистить» в режиме расчёта убирает
всю расстановку. `tools/flow_check.py`, `check_names`, `check_numbers`,
`check_dead_code` — без новых расхождений.

---

## Задача 8.10 — смена проекции (ЗАДЕЛ, в объём не входит)

Записана, чтобы не открывать заново. По 0.4 нынешний км-фрейм даёт сдвиг 117 м на
текущем участке, 2.5 км на районе 255 км и 43 км на области. Если когда-нибудь
потребуется работать по району шире ~150 км, нужен переход на настоящую проекцию:

| Вариант | Плюс | Минус |
|---|---|---|
| **Web Mercator для всего** | совпадает с тайлами точно, ноль сдвига | расстояния искажены в 1/cos(lat) ≈ 2.2 раза на нашей широте — придётся править все расчёты длин, радиусов, площадей |
| **UTM / Гаусса-Крюгера по зонам** | расстояния верны | зоны по 6°: участок может попасть на стык двух зон, тайлы придётся перепроецировать на лету |
| **оставить как есть, ограничить район** | ничего не переписывать | район не шире ~150 км (принято сейчас) |

Признак, по которому к этому надо вернуться: заказчику понадобился расчёт по району
шире 150 км по широте.

---

## 4. Контрольные проверки (после каждой задачи)

1. `python tools/reference_run.py` — числа эталона [ОГРАНИЧЕНИЯ §6](../ОГРАНИЧЕНИЯ.md)
   не изменились (кроме задач, которые их меняют осознанно — таких здесь нет);
2. `python tools/переносимое/codemap.py --check` — карта кода пересобрана;
3. `python tools/переносимое/docmap.py --check` — оглавления и ссылки целы;
4. `python tools/переносимое/check_names.py` и `check_numbers.py` — 0 расхождений;
5. `UAV_SELFTEST=1 python main_qt.py` — самопроверка проходит;
6. запуск программы и ручная проверка того, что задача добавила.

---

## 5. Открытые вопросы (у каждого — умолчание, работа не встанет)

| № | Вопрос | Зависит | Умолчание, если ответа нет |
|---|---|---|---|
| **В1** | Координаты «в рад» — это радианы или градусы? | 8.3 | **градусы** (десятичные + град/мин/сек), радианы третьей строкой |
| ~~В2~~ | Что и до какого уровня качать заранее | 8.1 | ✅ **ответ 03.09.2026:** участок **+ 100 км**, уровни **z6–12** (5 254 тайла, 57 МБ, 9 мин); z13–15 приложение докачивает само при приближении |
| **В3** | «Правильный прямоугольник или квадрат» — принудительный квадрат? | 8.2 | прямоугольник, приведённый к целому числу ячеек 500 м |
| **В4** | Оси: оставить километры или делать деления «через 1 см»? | 8.4 | километры + масштаб 1:M + линейка в углу |
| **В5** | Картинки карт бывают повёрнутые/со скана с рамкой? | 8.5 | только прямоугольная привязка по 4 углам |
| **В6** | В истории полётов нужны ещё скорость/курс/высота? | 8.6 | только координаты по точкам (+ км) |
| **В7** | Разнос между датчиками РАЗНЫХ типов — по какому радиусу? | 8.7 | полусумма радиусов пары |
| ~~В8~~ | Сопоставление номеров датчиков | 8.9 | ✅ **ответ 03.09.2026:** **венгерский алгоритм своей реализацией**; scipy не ставим — вынесен в задел 8.9.6 |
| ~~В9~~ | Качать ли всю Архангельскую область | 8.1 | ✅ **снят 03.09.2026:** область целиком не качаем вовсе — база считается от рамки участка (см. 8.1.4, почему) |

---

## 6. Что обновить в документации по завершении

| Задача | Что обновить |
|---|---|
| 8.1 | [ОГРАНИЧЕНИЯ 5.1д](../ОГРАНИЧЕНИЯ.md) (пометить выполненным), [ЗАГРУЗКА_КАРТ §0](../карты/МЕТОДИЧКА_ЗАГРУЗКА_КАРТ.md), [МЕТОДИЧКА_КАРТА §5](../карты/МЕТОДИЧКА_КАРТА.md), [план 7, З6](7_ЕДИНЫЙ_ПЛАН.md) |
| 8.2 | [ОГРАНИЧЕНИЯ 5.1, 5.1а–в](../ОГРАНИЧЕНИЯ.md) (рамка больше не только из `config`), [МЕТОДИЧКА_КАРТА_УГРОЗ §1.1](../вкладка_3/МЕТОДИЧКА_КАРТА_УГРОЗ.md) |
| 8.3 | новый раздел в [МЕТОДИЧКА_КАРТА.md](../карты/МЕТОДИЧКА_КАРТА.md) про км-фрейм; [СТРУКТУРА_ПРОЕКТА](../СТРУКТУРА_ПРОЕКТА.md) — новый модуль |
| 8.4 | [НАСТРОЙКА_ИНТЕРФЕЙСА](../интерфейс/НАСТРОЙКА_ИНТЕРФЕЙСА.md) — масштаб и линейка |
| 8.5 | [ЗАГРУЗКА_КАРТ](../карты/МЕТОДИЧКА_ЗАГРУЗКА_КАРТ.md) — четвёртый вид данных; [СТРУКТУРА_ПРОЕКТА](../СТРУКТУРА_ПРОЕКТА.md) |
| 8.6, 8.8 | новая методичка «Форматы обмена: история полётов и позиции датчиков»; [СТРУКТУРА_ПРОЕКТА](../СТРУКТУРА_ПРОЕКТА.md) |
| 8.7 | [ОГРАНИЧЕНИЯ §1](../ОГРАНИЧЕНИЯ.md) (типы датчиков), [МЕТОДИЧКА_ДАТЧИКИ_ДВА_ТИПА](../вкладка_3/МЕТОДИЧКА_ДАТЧИКИ_ДВА_ТИПА.md) → станет «четыре типа», [НАСТРОЙКА_ИНТЕРФЕЙСА](../интерфейс/НАСТРОЙКА_ИНТЕРФЕЙСА.md) |
| 8.9 | [план §8.9.7](../планы/8_ПЛАН_КАРТА_И_ИНТЕРФЕЙС.md) — итог реализации; [ОГРАНИЧЕНИЯ 8.10](../ОГРАНИЧЕНИЯ.md) — правило сопоставления; [СТРУКТУРА_ПРОЕКТА](../СТРУКТУРА_ПРОЕКТА.md) — новый модуль `sensor_track.py` |
| **всегда** | [указатель журнала](../журнал/README.md) — пункт по формату |
