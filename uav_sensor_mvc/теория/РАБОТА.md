# РАБОТА — что делается прямо сейчас

> **Правился:** 20.09.2026 — у блока новое поле **«План»**: откуда задача (план и раздел
> либо «без плана») и куда перейти за подробностями (правило заказчика).
> 19.09.2026 — блок показа карты закрыт (журнал 280–282), ждёт коммита.
> 19.09.2026 — предел **10 блоков и 500 строк**; не помещается — удалять
> последнее ГОТОВОЕ действие (решение заказчика). Заведён блок задачи о показе карты.
> 19.09.2026 — предел блоков **три → ПЯТЬ** (решение заказчика), считаются и
> пункты плана, и его поручения; поручение записывается в тот же момент, как услышано.
> 19.09.2026 — ⚠️ **правило исправлено по замечанию заказчика**: блок заводится
> на **ЛЮБУЮ задачу заказчика**, а не только на пункт плана, и закрытые блоки убираются
> сразу. Файл вычищен: удалены пять блоков плана 9 от 11–12.09.2026 (все закрыты, результат
> в журнале), закрыт блок сборки «Плес_2» — он неделю стоял с ⬜, хотя сборка выполнена
> 16.09.2026. Заведён блок серии 16–19.09.2026.
> 18.09.2026 — блоки плана 11 удалены: план завершён и вынесен в архив (журнал п. 276).
> Файл заведён 11.09.2026 по задаче 9.16 плана 9.
>
> ⚠️ **ПОСЛЕ СЖАТИЯ КОНТЕКСТА ЧИТАТЬ ЭТОТ ФАЙЛ ПЕРВЫМ.** Верхний блок ⏭ — то, чем
> занимаемся дальше, со всеми числами, точками правки и ловушками: перечитывать планы не
> нужно.
>
> **Зачем.** Правка делается, а список затронутых документов нигде не фиксируется — и к
> концу работы уже не вспомнить, что задето. Замер 11.09.2026: **11 документов отстали от
> кода** (`codemap --stale`), а §5.3 методички маршрутов описывала механизм неверно.
>
> ⚠️ **Это НЕ второй журнал.** Здесь черновик и чек-лист; в журнал уходит результат с
> числами, разбор и формулы — в методички. **Блок удаляется, как только задача закрыта.**

## Как пользоваться

⚠️ **БЛОК ЗАВОДИТСЯ НА ЛЮБУЮ ЗАДАЧУ ЗАКАЗЧИКА** (правило уточнено 19.09.2026 по его
замечанию) — не только на пункт плана. Задача, сказанная в разговоре («сделай картинки»,
«заведи правило проверок», «собери `.exe`»), точно так же трогает файлы и документы, и
именно такие терялись: за 16–19.09.2026 сделано пять задач, а блока не было ни одного.

**Что считается задачей:** всё, после чего меняются файлы. Пункт плана · устное поручение ·
сборка · замер · правило · разбор механизма. **Не считается:** ответ на вопрос без правок.

1. задача получена → **сразу** завести блок: что делается, какие файлы кода. ⚠️ **Поручение
   заказчика — в файл в тот же момент, как услышано**, до первой правки: иначе серия из
   нескольких поручений подряд уходит в работу, а в каталоге не остаётся ни одного следа;
2. по ходу правок **дописывать** затронутые файлы и документы — **в тот момент, когда файл
   открыт**, а не в конце. Это самое частое место, где всё ломается;
3. задача закончена → пройти по списку документации сверху вниз, каждый отметить ✅;
4. спорное — вынести заказчику **одним** вопросом;
5. всё отмечено → строка в [журнал](журнал/README.md) со ссылками → **блок удалить.**

⚠️ **ПРЕДЕЛ — 10 БЛОКОВ И 500 СТРОК** (решение заказчика 19.09.2026; было 5 блоков, ещё
раньше 3). Считаются **все** блоки без разбора, откуда задача: из плана или его поручение.
Главный предел — **строки**: блоков может быть и меньше десяти, если они длинные.

⚠️ **ЧТО ДЕЛАТЬ, КОГДА НЕ ПОМЕЩАЕТСЯ** (правило заказчика): удалять **последнее готовое
действие** — самый свежий ЗАКРЫТЫЙ блок. Не самый старый: старый закрытый блок ещё может
понадобиться как след незавершённой темы, а свежезакрытый уже записан в журнал и повторяется
оттуда. ⚠️ **Блок в работе не удаляется никогда** — ни ради строк, ни ради счёта.

**Блок — это раздел с датой и задачей** (`## ДД.ММ.ГГГГ · откуда — имя`), включая ⏭.
Служебные разделы «Как пользоваться» и шаблон в счёт не идут: они часть самого файла.
Счёт одной командой:

```bash
grep -c "^## [0-9⏭]" теория/РАБОТА.md    # блоков, предел 10
wc -l < теория/РАБОТА.md                  # строк, предел 500
```

⚠️ **УБОРКА — ЧАСТЬ ЗАКРЫТИЯ, А НЕ «КОГДА-НИБУДЬ ПОТОМ».** Блок старше **трёх дней** либо
в работе (тогда у него есть ⏭ и понятно, чего ждёт), либо подлежит удалению. Закрытый блок
опаснее отсутствующего: 16.09.2026 блок сборки «Плес_2» остался со всеми ⬜, хотя сборка
была готова, — и файл, который читается первым после сжатия, неделю утверждал, что её нет.

**Шаблон блока:**

```
## ДД.ММ.ГГГГ · откуда задача (план N.M или «заказчик, устно») — короткое имя
**План:** план N, задача N.M → ссылка на раздел | либо «без плана — поручение заказчика».
**Что делается:** одной фразой.
**Файлы кода:** путь :: функция :: строка
**Документация к обновлению:**
  - файл §раздел — что именно поправить   ⬜
**Проверки:** reference_run ⬜ · docmap --check ⬜ · что ещё ⬜
**Статус:** в работе / закрыт, журнал п. N
```

---

## 20.09.2026 · заказчик, устно — ДОЛИ ИЛИ КОНСТАНТЫ: отдельный документ ✅

**План:** без плана — поручение заказчика; связанный долг — [план 10, задача 10.15](планы/10_ПЛАН_ПРОЕКЦИЯ_И_ОСТАТКИ.md)
(абсолюты, которые врут на малом районе).

**Что делалось:** разделить величины на «доля от входного числа» и «абсолют», описать,
что к какому классу относится, и решать это ЗАРАНЕЕ. Повод — кольца облёта 10/18/28 км
оказались подгоном под область `arh` и были переделаны в доли уже после эталонов.

**Файлы кода:** нет — документ о правилах ✅

**Документация:** [правила/ДОЛИ_И_КОНСТАНТЫ](правила/ДОЛИ_И_КОНСТАНТЫ.md) — **новый** ✅ ·
ссылка из ОГРАНИЧЕНИЙ (шапка) ✅ · НАВИГАЦИЯ, СТРУКТУРА_ТЕОРИИ ✅ · журнал п. 284 ✅

**Статус:** выполнено, ждёт коммита по команде заказчика

---

## 20.09.2026 · заказчик, устно — ПАПКА ТАЙЛОВ ПО ИМЕНИ ОБЛАСТИ ✅

**План:** без плана — поручение заказчика; правило о папках участка —
[ОГРАНИЧЕНИЯ 5.1д, 5.1ф](правила/ОГРАНИЧЕНИЯ.md), разбор — [код/ОБЛАСТЬ_ИЗ_ФАЙЛА](код/ОБЛАСТЬ_ИЗ_ФАЙЛА.md).

**Что делается:** при выборе области папка тайлов должна подставляться сама, по названию
области; через интерфейс её по-прежнему можно сменить, и тогда действует выбор человека.

**Дословно:** «при выборе области также заранее прописывать папку с тайлами по названию
области, и чтобы она применялась автоматически; если надо уже через интерфейс, то она
потом изменится».

**Файлы кода:** `area_file.py` :: `tile_cache_root`, `tiles_root_for_area` — **новые** ✅ ·
`view_qt/area_choice.py` :: `choose_area` ✅ · `tools/area_check.py` — раздел 5а ✅

**Документация:** ОГРАНИЧЕНИЯ 5.1ф ✅ · код/ОБЛАСТЬ_ИЗ_ФАЙЛА ✅ · журнал п. 283 ✅

**Проверки:** `area_check` — 6 новых проверок, ВСЕ ПРОШЛИ ✅ · `gui_check` — ВСЁ ПРОШЛО ✅ ·
комментарии 0 ✅

**Статус:** выполнено, ждёт коммита по команде заказчика

---

## 19.09.2026 · заказчик, устно — КАЧЕСТВО ПОКАЗА КАРТЫ (тайлы, зум, уровень под область) ✅

**План:** без плана — поручение заказчика; тема смыкается с [планом 10](планы/10_ПЛАН_ПРОЕКЦИЯ_И_ОСТАТКИ.md)
(показ и проекция), механизм целиком — [карты/ТАЙЛЫ_И_ЗУМ](карты/ТАЙЛЫ_И_ЗУМ.md).

**Что делается:** показ подложки смазан, и уровень тайлов подбирается не так, как в OSM и
Google. Разобрать механизм, исправить качество, описать в коде и завести методичку.

**Дословно от заказчика (19.09.2026):**

| # | Что сказано | Как понял |
|---|---|---|
| 1 | на **максимально большом районе картинка смазана**, а на области `arh` при загрузке — чёткая | уровень тайлов выбран не под масштаб: на большой области тайл растягивается |
| 2 | при **приближении названия очень мелкие**, при максимальном **отдалении — крупнее, но смазанные** | берётся тайл одного уровня и масштабируется, вместо перехода на соседний уровень |
| 3 | «ты сейчас отображаешь **потайлово**» — посмотреть, **как в OSM и Google** | их приём: уровень от масштаба вида и плотности пикселей экрана, тайл рисуется 1:1 |
| 4 | **не сглаживать, не сужать**; не пытаться наложить тайлы, **если они не подходят** | сглаживание убрать; неподходящий уровень не растягивать, а брать верный |
| 5 | для области загрузки показывать **уровень, нужный для корректного по размерам показа области**, с учётом **авто-подбора разрешения экрана** | zoom считать от размера области и размера окна в пикселях |
| 6 | ограничение — только чтобы **область помещалась полностью**, всё **динамически**, при любой карте само | предел по масштабу от границ области, без зашитых чисел |
| 8 | **строка над картой: какой уровень и какие тайлы показываются сейчас**, динамически — «для проверки и определения корректности» | индикатор z, числа тайлов и масштаба; обновляется на каждое движение вида |
| 8 | **строка над картой**: какой уровень и какие тайлы показываются сейчас, динамически — «для проверки и определения корректности» | индикатор z, число тайлов, масштаб; обновляется на каждое движение вида |
| 9 | ⭐ **предел вида не должен зажимать масштаб**: при ПРИБЛИЖЕНИИ выходить за рамки области не страшно; при ОТДАЛЕНИИ главное — область целиком, «сколько лишней территории — не важно»; перемещение держать **до 10 км по углам** | предел по панораме, а не по масштабу — это и снимает конфликт с привязкой к уровню тайлов |
| 10 | проверить файл **ОГРАНИЧЕНИЯ**: когда правился, какие последние правила, при необходимости поправить | сверка правил показа с новым поведением |
| 7 | пояснить **как работает код**: загрузка тайлов, показ, показ при зуме | 1.КАК_КОД + подробная методичка |

**Файлы кода:** `view_qt/threat_view.py` :: `_zoom_out_frame`, `_on_vb_resized`, `lbl_tiles`,
оси `setWidth/setHeight` ✅ · `view_qt/basemap_mixin.py` :: `_map_px`, `_report_tiles`,
`_set_view_limits(max_span=…)` ✅ · `view_qt/tile_layer.py` :: `_TileImage` (без сглаживания) ✅ ·
`config.py` :: `THREAT_PAN_MARGIN_KM` = 10 ✅ · `tools/переносимое/check_comments.py` — **новый** ✅

**Документация:** [карты/ТАЙЛЫ_И_ЗУМ](карты/ТАЙЛЫ_И_ЗУМ.md) — **новая**, механизм целиком ✅ ·
`1.КАК_КОД §2а` ✅ · ОГРАНИЧЕНИЯ 5.10 переписано, новые 5.12–5.14 ✅ ·
`8_КОММЕНТАРИИ_В_КОДЕ §2а` (проверка) ✅ · НАВИГАЦИЯ, СТРУКТУРА_ТЕОРИИ, tools/README ✅ ·
журнал 280, 281, 282 ✅

**Проверки:** `tiles_check` — ВСЁ ПРОШЛО, отклонение привязки 0.0 % ✅ · `gui_check` — ВСЁ ПРОШЛО ✅ ·
`reference_run` — ВСЁ СОШЛОСЬ С ЭТАЛОНОМ ✅ · docmap 0, комментарии 0, имена 0, числа 0, отметки 78/0 ✅

**Итог замеров:** масштаб подложки **1.000** вместо 1.145 (предел зажимал), 0.984/0.969
(ширина оси плясала) и 0.97 (отдаление за последний уровень). Попытка уйти мельче
стартового уровня больше ничего не меняет.

**Статус:** выполнено, ждёт коммита по команде заказчика

---

## 20.09.2026 · заказчик, устно — WORD-ДОКУМЕНТ: маршруты и датчики ✅

**План:** без плана — поручение заказчика. Источник содержания — [разбор/](разбор/README.md)
и [МЕТОДИЧКА](МЕТОДИЧКА.md); образец оформления — `docs/make_theory_docx.py`.

**Что делается:** «создай Word-документ, где будет теория и немного кода по датчикам,
маршрутам, как есть сейчас» — собрать `.docx` генератором, как это принято в проекте
(документ пересобирается скриптом, руками не правится).

**Файлы кода:** `docs/make_routes_sensors_docx.py` — **новый** ✅

**Документация:** СТРУКТУРА_ПРОЕКТА ✅ · ссылка из разбор/README ✅ · журнал п. 288 ✅

**Проверки:** собран и проверен — 45 КБ, 20 разделов, 10 таблиц, 28 фрагментов кода ✅

**Статус:** выполнено, ждёт коммита по команде заказчика

---

## 20.09.2026 · заказчик, устно — РАЗБОР МЕХАНИКИ: маршруты и датчики ✅

**План:** без плана — поручение заказчика; итог пойдёт в правку [плана 10](планы/10_ПЛАН_ПРОЕКЦИЯ_И_ОСТАТКИ.md)
(задачи 10.14, 10.15) и, возможно, в новый план по модели.

**Что делается:** новая папка `теория/разбор/` — подробно, с примерами: как строится
маршрут (точки, VIA, пояса, отбор, сглаживание) и как ставятся датчики (сетка, критерии,
насыщение, разнос). Плюс ответы на вопросы и замеры под два предложения заказчика.

**Вопросы заказчика (отвечать по пунктам):**

| # | Вопрос / поправка | Статус |
|---|---|---|
| 1 | VIA-точки подробнее; «два уровня точек» в поясах — предложение делить сектор сеткой **каждые 10 %** | ✅ |
| 2 | «некрасивый манёвр» — так нельзя: если пройти нельзя, маршрут **строится заново**; что такое мягкое достижение и какой участок | ✅ |
| 3 | сглаживание и «коридор»: допуск прямого участка — **ориентир, когда свернуть**, а не разрешение на ломаную. Проверить, как работает | ✅ |
| 4 | §1.8 жадный отбор: правило выбора маршрута, примеры; пул кандидатов — это до моделирования? только для предварительной расстановки? | ✅ |
| 5 | **2.1 сетка датчиков одна на все типы** — предложение: своя сетка на каждый тип (R = 1 и R = 15 несравнимы). Проверить | ✅ |
| 6 | **2.2 критерии**: считать не покрытие весовых ячеек, а **длину прикрытия маршрутов** (км прикрыто из км маршрута); что такое «доля маршрутов»; пояснить **насыщение** | ✅ |
| 7 | **2.3 разнос**: 0.8·(R₁+R₂) = 13.6 км при 15 и 2 км — всегда ли; при равной кратности мерить по меньшему радиусу; для статического объекта — как обычный разнос | ✅ |
| 8 | ⭐ предложение 1: убрать ломаные линии (идея — прореживать каждую 3-ю точку); сначала строятся точки, потом линия? датчики считаются по расчётным точкам или по нарисованной линии? применимы ли приёмы маршрутизации (коммивояжёр, Google Maps) | ✅ |
| 9 | ⭐ предложение 2: убрать повторяющиеся ветвления и шаги назад; **замерить производительность и память**: 250 или 400 маршрутов сразу, показ поочерёдно или разом; распараллеливание | ✅ |

**Файлы кода:** `tools/perf_check.py` — **новый** (время, память, отказы) ✅ ·
разведка `threat_routes.py`, `optimization.py`, `threat_grid.py` ✅

**Документация:** папка [разбор/](разбор/README.md) — три документа ✅ ·
НАВИГАЦИЯ, СТРУКТУРА_ТЕОРИИ, CLAUDE.md, tools/README ✅ · журнал п. 287 ✅

**Проверки:** `perf_check` — 137.3 с, отказов 51 %, пик 50 МБ ✅ · прореживание
против RDP — 0.843 против 0.199 км отклонения ✅ · docmap 0, имена 0, числа 0, отметки 83/0 ✅

**Статус:** выполнено, ждёт коммита по команде заказчика

---

## ⏭ 20.09.2026 · заказчик — ПЛАН 12: условия моделирования (начинать отсюда)

**План:** ⭐ [план 12 целиком](планы/12_ПЛАН_УСЛОВИЯ_МОДЕЛИРОВАНИЯ.md) — пять задач, порядок
работ в §7, ловушки у каждой. Ветка реализации `sim-conditions` от `ui-sensors`, заводится
по команде заказчика.

**Что делается:** типы БПЛА со своими характеристиками (скорость, крен, запас, высота),
окно «Задать условия моделирования», точка старта кликом, **свой запас хода у каждой точки
входа** (в панели — среднее, можно задать общий), таблица точек с правкой, широкая карта,
где векторные слои и рельеф считаются только по участку `arh`.

**Начинать с задачи 12.3** (справочник типов): он ничего не ломает и на него опирается
остальное.

⚠️ **Найдено при разборе задания** (§9 плана): выбор «облёт цели» в панели **не влияет** на
показ возможных маршрутов — в `plan_routes` жёстко `approach="mix"`. Это тот же дефект,
что чинили 13.09.2026 для режима веса. Решить: брать из панели или записать правилом.

**Статус:** план составлен, реализация — по команде

---

## ⏭ Следующее после плана 12 — задачи 10.14 и 10.15 плана 10

**План:** [план 10 §0.7 — реестр незакрытого](планы/10_ПЛАН_ПРОЕКЦИЯ_И_ОСТАТКИ.md): там обе
задачи с разбором, а также 10.16 (мелкий долг документации) и вопросы В9, В11.4.
Указатель всех планов — [планы/README](планы/README.md).

> **10.14 — форма маршрута** (петли, ломаность, развороты у цели и рамки 9–23 %).
> ⭐ **Начинать с `python tools/plot_run.py --fig shape`**: картинка уже показала, что
> развороты дают **два скопления** — у цели и у рамки района (29 маршрутов из 150), а не
> ровный фон. Значит правил нужно два разных, а не один общий порог.
>
> **10.15 — точки входа на малом районе** (строятся не у края, скапливаются на большом весе).
> Версия причины: полоса края `THREAT_SECTOR_EDGE_KM` = 1 км, окно перспективы
> `_entry_window_score` = 5 × 3 ячейки (2.5 × 1.5 км) и разнос `THREAT_SECTOR_MIN_SEP_KM` = 1 км
> заданы **в километрах**, а не в долях района. Лечится тем же приёмом, что пояса облёта в
> 11.5, — долями.
>
> Разбор обеих — [план 10 §0.7](планы/10_ПЛАН_ПРОЕКЦИЯ_И_ОСТАТКИ.md). Порядок проверок —
> [10_ПРОВЕРКИ_И_ПРОГОНЫ](переносимое/10_ПРОВЕРКИ_И_ПРОГОНЫ.md): числа раньше картинки.

---

## 16–19.09.2026 · заказчик, устно + план 11 — замеры, закрытие плана, инструмент картинок

**Что делалось:** пять задач подряд, все закрыты.

| # | Задача (откуда) | Итог |
|---|---|---|
| 1 | **сборка `.exe` «Плес_2»** (заказчик, 16.09) | ✅ ветка `exe-build-3`, коммит `9a53c33`, папка `C:\uav_build\dist\Плес_2` — 2 267 МБ, архив 1 927 МБ, 34 705 тайлов, область `arh`, картинки без `детализация/`. Журнал п. 274 — **в сборочной ветке** |
| 2 | **замеры расстановки** (заказчик, 16.09: «как влияет `k` и галка») | ✅ `tools/placement_check.py`, папка `теория/замеры_моделирования/`, журнал п. 275 |
| 3 | **завершить план 11, в архив** (заказчик, 18.09) | ✅ `теория/архив/11_ПЛАН_МОДЕЛЬ_ДВИЖЕНИЯ.md`, §10 — оценка заказчика; ссылки в 14 файлах переписаны; остатки → 10.14, 10.15; журнал п. 276 |
| 4 | **разбор VIA-точек в коде** (заказчик, 18.09) | ✅ РАССТАНОВКА_И_ДВИЖЕНИЕ §2.6, 1.КАК_КОД §4 (замер: 20 узлов → 11 точек → 8.8 по силам) |
| 5 | **картинки прогона + правило проверок** (заказчик, 19.09) | ✅ `tools/plot_run.py`, `код/КАРТИНКИ_ПРОГОНА.md`, `переносимое/10_ПРОВЕРКИ_И_ПРОГОНЫ.md`, ссылка из CLAUDE.md, приём закреплён в плане 10; журнал п. 277 |

**Файлы кода:** `tools/plot_run.py` — **новый** (`collect`, `fig_belts`, `fig_spread`,
`fig_shape`, `_map_base`, `_marks`) ✅ · `tools/placement_check.py` — **новый** ✅ ·
`tools/переносимое/check_numbers.py` :: `VARIANT` — фильтр на примеры команд (`--set`) ✅

**Git:** коммиты `de03e81` (56 файлов) и `eb480cc`; **план 11 влит в основную `ui-sensors`**
(fast-forward), рабочая папка теперь на ней; `uav-motion-model` запомнена под сборку `.exe`.
⚠️ `push` не делался — основная ушла вперёд `origin` на 13 коммитов.

**Проверки:** `reference_run` — «ВСЁ СОШЛОСЬ С ЭТАЛОНОМ» до и после слияния ✅ ·
`docmap --check` 0 ✅ · имена 0 ✅ · числа 0 ✅ · отметки 77/0 ✅

**Статус:** закрыт, журнал п. 274–277. ⚠️ Блок держится до следующей задачи как след
последней серии — потом удалить.
