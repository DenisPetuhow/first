# Журнал проекта: размещение датчиков обнаружения БПЛА (MVC)

> Рабочий отчёт для передачи контекста: идея, этапы реализации, возникшие
> проблемы и их исправления, текущее состояние и что осталось. Теория и формулы —
> в `МЕТОДИЧКА.md`; карта файлов — в `СТРУКТУРА_ПРОЕКТА.md`.

---

## 1. Идея и назначение

Программа моделирует **оптимальное размещение N датчиков обнаружения** для
контроля движения и обнаружения коммерческого БПЛА самолётного типа (курьерский,
аэрофото-/киносъёмочный, агродрон, нарушитель зоны аэропорта), летящего из района
старта **A** в цель **B** с ограниченным запасом хода **L_max**. Маршрут заранее
неизвестен (стохастический), поэтому датчики расставляются так, чтобы
максимизировать среднюю полезность обнаружения по множеству случайных маршрутов:
кратно (несколько засечек) и равномерно вдоль всего пути.

Метод: жадная субмодулярная оптимизация по выборочному среднему (SAA) с гарантией
качества (1 − 1/e). Рабочий язык интерфейса и документации — русский.

---

## 2. Архитектура

**MVC**, два параллельных интерфейса поверх общих модели и логики:
* **matplotlib** (`main.py`, `view/`, `controller/`) — вкладка 1 «точка A → цель B»;
* **PyQt5 + pyqtgraph** (`main_qt.py`, `view_qt/`, `controller_qt/`) — две вкладки
  в `QTabWidget`: «точка A → цель B» и «зона старта → цель B».

Общий слой `model/` (геометрия, траектории, обнаружение, оптимизация, две модели
сцены) используется обоими интерфейсами.

---

## 3. Этапы реализации (хронология)

1. **Базовая геометрия и эллипс достижимости** (`geometry.py`): эллипс с фокусами
   A, B; авто-масштаб карты; гео-привязка (широта/долгота → км).
2. **Модель дуг (этап 1)** (`trajectories.py`): дуга по касательно-хордовому углу
   θ, предельный угол θ_max, профили разброса (малый/средний/сложный).
3. **Модель петель/змеек (этап 2)**: синусоида с закреплёнными концами, предельная
   амплитуда по запасу хода.
4. **Обнаружение и метрики** (`detection.py`): кратность n, сегментное покрытие m,
   доля сопровождения, полезность φ.
5. **Оптимизация** (`optimization.py`): сетка кандидатов, `CoverageCache`,
   жадный субмодулярный выбор, вторичный критерий распределения.
6. **Интерфейс matplotlib + контроллер** (вкладка 1): анимация полёта (блиттинг),
   слои (тепловая карта / 10 маршрутов / веер), панель показателей, пакетный расчёт.
7. **Qt-интерфейс** (`view_qt/`, `controller_qt/`): аппаратно ускоренная анимация,
   тот же контракт методов, легенды (ColorBarItem + LegendItem).
8. **Вкладка 2 «зона старта → цель»** (`area_start.py`, `area_view.py`,
   `area_controller.py`): старт из эллипса неопределённости; постановка маршрута
   кликами (A — центр зоны, второй клик — направление B); 3 модели движения
   (дуга из старта / ломаная / манёвр).
9. **Манёвр самолётного типа по путевым точкам**: S₀ → точки → B со скруглением
   углов дугами ≥ R_min; число точек — пользовательский диапазон [n_min, n_max].
10. **Физика ТТХ → R_min**: крен и скорость как ТТХ; R_min = V²/(g·tan φ).
11. **Два закона движения (выпадающий список)**: «по точкам» и «по синусу».
12. **Документация**: `МЕТОДИЧКА.md`, `ЖУРНАЛ_ПРОЕКТА.md`, `СТРУКТУРА_ПРОЕКТА.md`,
    `README.md`, `docs/Теория_и_математика.docx`.

### Последние доработки (текущая сессия)
13. **Синус с учётом R_min**: суммарная кривизна гармоник ограничена 1/R_min —
    синусоидальный манёвр учитывает минимальный радиус разворота, а не только
    амплитуду режима (формула 13 методички).
14. **Путевые точки только за зоной старта** (по проекции эллипса на ось S₀→B),
    затем вынесено в переключатель (см. п. 16).
15. **Датчики не размещаются в зоне старта/неопределённости** (жёсткое условие):
    кандидаты внутри эллипса зоны исключаются (`points_outside_start_zone`).
16. **Выпадающий список «Точки маршрута»**: «в зоне старта» / «только за зоной»
    (`waypoint_zone`, `WAYPOINT_ZONE_LABELS`, `_build_zone_combo`, `on_zone`).
17. **Исправлен разброс (регресс)**: амплитуда выноса переведена с базы «хорда D»
    на базу «b» (полу-малая ось эллипса достижимости), а сложный делает 1–2
    КРУПНЫХ лепестка к краю эллипса вместо узкого зигзага (который при многих
    точках не уходил от центра в пределах L_max и схлопывался в прямую). Линза
    якорится на доступном диапазоне `[s_lo,1]`. Профили: малый ~0.1b, средний
    ~0.4b, сложный ~b. Тот же базис `b` — у синуса.
18. **Датчики не «за спиной» зоны**: помимо запрета внутри зоны добавлено
    отсечение позади задней кромки эллипса старта (180° от задней точки) —
    датчики только в полупространстве к цели.
19. **Карта перерисовывается по |AB|**: цель B следует за полем «|AB|»
    (направление — от клика), `on_apply` принудительно перемасштабирует карту.
    По умолчанию точки маршрута — «в зоне старта» (полный разброс).
20. **Слой «10 маршрутов» — обе стороны**: представительные маршруты выбираются
    РАВНОМЕРНО по боковому отклонению (стратификация по `signed_max_lateral`),
    поэтому немногие отображаемые маршруты не оказываются с одной стороны, хотя
    распределение двустороннее (`frequent_paths`, `frequent_maneuvers`).
21. **Огибающая достижимости с учётом зоны старта** (`reach_envelope_outline`):
    объединение эллипсов (фокусы S0,B; сумма L_max) по всем S0 из зоны. Любой
    маршрут лежит внутри неё, поэтому маршруты больше не выходят за контур (раньше
    показывали эллипс с фокусами A,B, а маршрут из S0≠A выходил за него).
22. **«Смешанный» уходил к центру**: при долях 30/40/30 два «центральных» режима
    (малый+средний) перевешивали. Сделано 25/25/50 (сложный преобладает) —
    `_BALANCED_P`. Теперь смешанный заметно шире (медиана ~0.47·b).
23. **Слой «10 маршрутов» → ПРЕДИКТИВНЫЙ**: вместо случайной выборки строит 10
    наиболее вероятных траекторий как ОБОБЩЕНИЕ всей накопленной выборки —
    кластеризация по форме (k-means), медоид каждого кластера (реальный маршрут),
    вес = доля кластера (вероятность). `representative_trajectories`, `_kmeans`.
    До накопления — аналитический набор (квантили/обе стороны).
24. **Реальная карта (вкладка 2)**: подложка из тайлов (OSM/топо/спутник/тёмная)
    + оффлайн-СХЕМА Курской области (сетка + города). Гео-привязка к Курску,
    дисковый кэш cache-first→интернет, ограничение 10 уровней зума, выпадающий
    список «Карта», зум/панорама мышью. Новый модуль `view_qt/geomap.py`;
    подробности — в `МЕТОДИЧКА_КАРТА.md` (отдельный документ, ссылка из основной).
25. **Клик A→B по карте**: |AB| считается автоматически из двух кликов (2-й клик =
    цель), а не из поля. `on_route_ready`. Карта центрируется на Курске.
26. **Предзагрузка тайлов**: `tools/download_tiles.py` — оффлайн-кэш региона/слоя.
    Откат на оффлайн-схему, если тайлов нет (приложение не ломается).
27. **Подключение СВОЕГО кэша тайлов (например, экспорт SAS.Planet)**: устранены
    три реальных пробела, мешавшие подключить чужой кэш «как есть»:
    * `tile_cache_path` жёстко предполагал `.png` → теперь `find_cached_tile`
      перебирает `.png/.jpg/.jpeg/.webp` (формат файла определяется по
      содержимому через Pillow, не по расширению);
    * `build_raster_basemap` отказывал для любого слоя не из `TILE_LAYERS`
      (блокировало ЛЮБОЙ чисто локальный кэш без онлайн-URL) → теперь работает,
      просто не обращается к сети для таких слоёв;
    * флаг `TILE_TMS` существовал, но нигде не применялся (инверсия оси Y не
      делалась) → добавлены `_tms_y()`/`LAYER_TMS_OVERRIDE`, реально влияющие на
      чтение/запись; включается per-слой через `UAV_TMS_LAYERS=ИмяСлоя`, не
      затрагивая остальные (онлайн-источники всегда XYZ).
    * `discover_local_layers()` — любая папка в кэше, не входящая в штатные
      слои, автоматически появляется в выпадающем списке «Карта» как
      «<имя> (локальный кэш)».
    * `tools/inspect_tile_cache.py` — диагностика ЛОКАЛЬНОЙ (на машине
      пользователя) папки тайлов: дерево каталогов, реальный формат файлов,
      проверка XYZ/TMS по известной точке (Курск), готовая рекомендация.
    Подробный разбор конкретных вопросов — `МЕТОДИЧКА_КАРТА_ВОПРОСЫ.md`
    (НЕ входит в архив — справочный файл для разработки).
28. **Производительность карты (устранены тормоза)**: сборка растровой подложки
    вынесена в ФОНОВЫЙ поток (`QThreadPool` + `_BasemapTask`/`_BasemapSignals`),
    интерфейс больше не подвисает при панораме/зуме/смене слоя; устаревшие
    результаты отсекаются по req_id. Добавлен in-memory LRU-кэш декодированных
    тайлов (`_TILE_MEM`, потокобезопасный) — повторный проход по региону мгновенный.
    build_raster_basemap теперь возвращает uint8 (без конвертации в UI-потоке).
29. **UI-масштабирование**: панель управления (обе вкладки) обёрнута в
    `QScrollArea` — при низком окне поля не сжимаются, появляется прокрутка.
30. **Новые слои карты**: OSM HOT, CyclOSM, ÖPNVKarte (+ уже были OSM, топо,
    спутник Esri, тёмная). Список «Карта» на вкладке 2.
31. **Курск — только при старте**: центрирование на Курск выполняется один раз
    (первый показ), при зуме/входе в режим постановки вид больше НЕ прыгает
    (`_kursk_done`). Маркеры точек красивее: A — зелёный кружок, B — красная
    звезда, белая обводка (`_mark_ab`).
32. **Убран консольный функционал**: удалены режимы `--mode selftest/render` и
    все аргументы командной строки; приложение — только через графический
    интерфейс (`main.py` и `main_qt.py` запускаются без параметров).
33. **Задел на цифровую карту**: вся гео-математика/проекция сосредоточена в
    `view_qt/geomap.py` (единый слой перевода км↔lon/lat↔тайлы) — это точка
    расширения для будущего предиктивного планировщика по критериям цифровой
    карты (рельеф/запретные зоны/costmap), без переделки модели БПЛА.
34. **Устранена причина размытия карты**: `_pick_zoom` был захардкожен на
    zmax=13 НЕЗАВИСИМО от общего предела `ZOOM_MAX=15`, а бюджет тайлов (~6) не
    зависел от реального размера окна — на большом экране мозаика оказывалась
    мельче виджета и растягивалась (размытие). Теперь `_pick_zoom` принимает
    `target_px` (реальная ширина виджета) и использует единые границы зума.
    Дело не в наложении слоёв — сам факт композиции `pg.ImageItem` поверх сцены
    на резкость не влияет.
35. **Ускорена загрузка карты**: тайлы одного окна теперь грузятся ПАРАЛЛЕЛЬНО
    (`ThreadPoolExecutor`, до 8 одновременно, было по одному); таймаут сети
    снижен 15с → 6с. Добавлен чекбокс «офлайн (только кэш, без сети)»
    (`Params.map_offline`) — при включении сеть не запрашивается вовсе, только
    диск; полезно на слабом интернете.

### Сессия: вкладка 3 «Карта угроз» + исправления

**Исходная точка кода зафиксирована** git-тегом `pre-threat-map-base` (коммит
`1c59f5b`) — состояние до начала работ над вкладкой 3. Разработка — на ветке
`claude/terrain-map-sensor-weights-utcbml`.

36. **Исправлено размытие карты (причина найдена окончательно).** Прошлая правка
    (п. 34) сделала `_pick_zoom` зависимым от размера виджета, но искала НАИБОЛЬШИЙ
    зум под ПОТОЛОК тайлов (`nx ≤ max_tiles_x`). Для широкого окна это возвращало
    уровень, где мозаика в пикселях оказывалась МЕНЬШЕ виджета (напр. 4 тайла ≈
    1024 px на экран 1400 px) → pyqtgraph растягивал её вверх → замыл; следующий
    достаточно чёткий уровень отсекался тем же потолком. Теперь бюджет тайлов —
    ПОЛ, а не потолок: берём самый крупный (экономный) зум, у которого ширина
    мозаики уже ПЕРЕКРЫВАЕТ экран, — картинку остаётся только УМЕНЬШИТЬ (резко), а
    не растянуть. Плюс `ImageItem.setOpts(autoDownsample=True)` — чистое усреднение
    при уменьшении. Проверено численно: мозаика теперь всегда ≥ ширины виджета
    (было 947/1400, 931/1000, 1165/1600 px — везде меньше; стало ≥ вдвое больше).
    **Решение о виджете (как просил пользователь):** если на конкретном HiDPI-экране
    замыл всё же останется заметным — запланирован переход с одиночного композитного
    `pg.ImageItem` на послойную отрисовку тайлов отдельными `QGraphicsPixmapItem`
    (как настоящая slippy-map): не растягивать общий растр, а показывать тайлы в их
    натуральном разрешении. Пока не делаем — текущего исправления по расчётам
    достаточно; зафиксировано как запасной путь на будущее.
37. **Исправлен латентный баг гео-проекции** (в рамках «сначала поправь баги»): в
    `geomap.py` масштаб «км на градус долготы» был МОДУЛЬНОЙ константой от `KURSK_LAT`
    и игнорировал переданный `lat0`. Для Курска (по умолчанию) — верно, но для чужого
    участка (вкладка 3, ~48.7°) давал ~6% ошибку по X и рассинхрон векторных слоёв с
    подложкой. Теперь `_km_per_deg_lon(lat0)` считает масштаб от фактической широты
    якоря. Вкладки 1–2 не затронуты (там `lat0=KURSK_LAT`).
38. **Вкладка 3 «Цифровая карта угроз».** Фиксированный демонстрационный участок
    (bbox по трём условным ориентирам, ≈94×75 км) с жёстким
    пределом обзора (`ViewBox.setLimits`). Территория разбита на сетку 500×500 м
    (≈28 500 ячеек); цифровые слои (реки/дороги/ж-д/ЛЭП/трубопроводы/лесополосы/
    мосты/застройка) взвешенно накладываются на сетку: вклад ∝ (длина пересечения /
    размер ячейки), funnel-бонус за схождение слоёв, мост — плоский +20, застройка —
    репеллер (−). Датчики расставляются ПО ВЕСАМ (обобщение жадного алгоритма —
    weighted max coverage, T=1) с запретом на воду (двойная роль воды: аттрактор
    маршрута + запрет своего датчика). Новые модули: `model/threat_grid.py`,
    `view_qt/threat_view.py`, `view_qt/basemap_mixin.py`,
    `controller_qt/threat_controller.py`, `tools/build_threat_grid.py`. Веса — в
    `config.THREAT_LAYERS`. Данные: офлайн-СХЕМА (демо) или реальные слои OSM из
    `geo_cache/` (готовит `tools/build_threat_grid.py` на pyrosm). Механика подробно —
    в `МЕТОДИЧКА_КАРТА_УГРОЗ.md`.
39. **Ядро наложения — на чистом numpy** (без geopandas): точная длина пересечения
    линии с ячейками через разбиение отрезка по линиям сетки (`_accumulate_segment`).
    Проверено офскрин-тестами: суммарная длина по ячейкам = длине отрезка (расхождение
    0); полный прогон вкладки 3 (построение карты + расстановка 12 датчиков, 88%
    покрытого веса, 0 датчиков на воде) — без ошибок; 3 вкладки конструируются и
    показываются offscreen; реальные слои из `geo_cache/*.npz` подхватываются.
40. **Документация собрана в папку `теория/`** (по просьбе): туда перенесены
    `МЕТОДИЧКА.md`, `МЕТОДИЧКА_КАРТА.md`, `МЕТОДИЧКА_КАРТА_ВОПРОСЫ.md`, журнал и
    структура; добавлена новая `МЕТОДИЧКА_КАРТА_УГРОЗ.md`. Ссылки в `README.md`
    обновлены.

### Сессия: рефакторинг + интерфейс выбора карт (модель Fable)

41. **Рефакторинг дублирования (SOLID/MVC).** Тайловая подложка была продублирована
    во вкладках 2 и 3. Вынесена в общий `view_qt/basemap_mixin.py`; вкладка 2
    (`area_view.py`) теперь тоже наследует `BasemapMixin` — удалены дубли
    `_BasemapSignals/_BasemapTask/_refresh_basemap/_on_basemap_ready/_draw_scheme`
    (−~90 строк). Специфика вкладки 2 (рамка Курской области) вынесена в хуки
    `_draw_scheme_extra`/`_set_scheme_extra_visible` (инверсия зависимостей). Чтение
    OSM `.osm.pbf` → слои вынесено в единую `model.threat_grid.layers_from_osm`
    (её используют и CLI-скрипт, и окно выбора карт — без дублирования). При
    рефакторинге поймана и устранена бесконечная рекурсия (хвост старого
    `_draw_scheme` прицепился к новому хуку).
42. **Интерфейс вкладки 3.** Добавлены: окно **«Выбрать цифровые карты…»**
    (`DigitalMapsDialog`) — выбор источника (файл `.npz`/`.osm.pbf` или авто) и
    набора накладываемых слоёв с их весами; кнопка **«Указать цель»** — цель ставится
    кликом по карте (вход фиксирован у реки), режим клика в `_on_scene_click`; кнопка
    **«Сброс»** — убрать датчики и заданную цель; подсказки (tooltips) ко всем полям,
    в т.ч. пояснение «Шаг сетки датчиков, км» (шаг перебора кандидатных позиций).
    Модель: `ThreatModel.set_data_source/set_enabled_layers/set_target`,
    `load_layers(data_path)`, `build_threat_grid(enabled=…)`.
43. **requirements.txt** приведён к формату пользователя: геостек (pyrosm/geopandas/
    shapely/pyproj/rasterio) как активные опциональные зависимости с пометкой про
    Windows/conda.
44. **Методичка по загрузке карт** — `теория/МЕТОДИЧКА_ЗАГРУЗКА_КАРТ.md`: пошагово со
    ссылками (BBBike/Geofabrik/Overpass/OSM), почему демо «странная», как проверить,
    как расширить на вкладку 2, и раздел про возможность смены виджета отображения
    (задокументировано, сам виджет пока оставлен — pyqtgraph).
45. **Устранено зависание на РЕАЛЬНЫХ данных OSM** (пользователь скачал 45 МБ
    `threat_layers.npz` — тысячи линий — и приложение вешалось на «Построить карту» и
    при выборе слоёв). Три причины и фиксы:
    * **`_detect_bridges`** перебирал ПАРЫ ломаных (река × дорога) с вложенными
      циклами по сегментам — O(N²), на реальных данных это миллиарды операций
      (главный «висяк»). Заменено на определение моста ПО СЕТКЕ: ячейка, где есть
      река И дорога/ж-д (`add_bridges_from_grid`) — векторно, O(ячеек);
    * **посегментная растеризация** линий на чистом Python заменена на векторную
      `_rasterize_polylines` (дискретизация + `np.add.at`); 450 тыс. сегментов —
      0.7 с вместо «минут»;
    * **`add_area_layer`** проверял «точка в полигоне» по ВСЕЙ сетке для каждого
      дома — теперь только по ячейкам bbox полигона.
    Плюс расстановка ускорена (квадрат расстояния без sqrt + float32): 7.3 с → 1.5 с.
    И, как просил пользователь, **тяжёлый расчёт вынесен в ФОНОВЫЙ поток**
    (`controller_qt/threat_controller._Task`/`_run_async`, `view.set_busy`): интерфейс
    не подвисает, кнопки блокируются на время счёта, курсор ожидания. Проверено на
    синтетике масштаба реального OSM: полный цикл «построить + расставить» ~2.6 с в
    фоне (было — зависание).
46. **Оптимизация ОТРИСОВКИ карты (после п. 45 тормозил интерфейс).** Три стадии для
    ясности (пользователь просил задокументировать, «почему до этого вообще не
    считалось»):
    * **Стадия 0 (до оптимизаций):** «Построить карту» на реальных данных **не
      завершалось вообще** — `_detect_bridges` был O(N²) по парам ломаных; при
      тысячах дорог/рек это миллиарды операций → приложение «висело» бесконечно
      (казалось, что «не считается»).
    * **Стадия 1 (п. 45):** вычисление стало быстрым (векторизация + мост по сетке +
      фоновый поток), карта СЧИТАЛАСЬ за ~1–2 с. Но **отрисовка** осталась тяжёлой:
      тысячи OSM-линий (сотни тысяч точек) со сглаживанием + тысячи маркеров-мостов
      перерисовывались на каждый кадр панорамы → интерфейс тормозил ПОСЛЕ показа.
    * **Стадия 2 (этот пункт):** отрисовка облегчена —
      (а) векторные слои **прореживаются** (`_polys_to_xy`: ≤1500 самых длинных линий,
      ≤12000 точек на слой) и рисуются **без сглаживания** (`antialias=False`,
      `connect="finite"`); (б) мосты сведены к магистраль/ж-д × река (было тысяч,
      стало сотни) и **ограничены 400 маркерами**; (в) убран `autoDownsample`
      подложки (пересчитывался на каждый зум); (г) чекбокс **«векторные слои»**
      теперь ПОЛНОСТЬЮ очищает векторную геометрию (нулевая стоимость отрисовки) —
      мгновенно возвращает плавность. Точек на отрисовку: ~90k → ~52k, и это одна
      операция на «Построить»/переключение, а не на каждый кадр.
    * **UI-поток:** окно «Выбрать цифровые карты…» теперь ТОЛЬКО задаёт выбор
      (источник + слои); наложение выполняется кнопкой «Построить карту» (как просил
      пользователь) — не пересчитывает дважды.

### Сессия: зум-подписи, исключение городов, маршруты, легенда, методички

47. **Зум под размер подписей (вкладки 2 и 3).** Карта «замельчала»: подписи были
    вдвое мельче и гуще, чем в обычном OSM. Причина — `_pick_zoom` брал ПЕРВЫЙ зум,
    где мозаика ≥ экрана; тайлов на уровень вдвое больше, поэтому мозаика выходила до
    2× шире экрана, её ужимали, и подписи (нарисованные для более крупного зума)
    мельчали; вдобавок ширину домножали на DPR. Теперь зум подбирается под масштаб
    ~1:1 (минимум `|log2(nx)−log2(need)|`, `need` от ЛОГИЧЕСКОЙ ширины). Численно:
    масштаб был 0.4–0.6 → стал 0.75–1.1 (родной размер подписей).
48. **Исключение населённых пунктов (город был «горячим», fig 7).** В городе много
    дорог/перекрёстков → funnel-бонус делал застройку самой тяжёлой. Введено:
    пометка застроенных ячеек (`add_area_layer(mark_urban=True)`), плотность в окне
    ~3×3 км (box-фильтр через интегральное изображение `_box_mean`), исключение при
    плотности ≥ порога (`apply_urban_exclusion`): вес → штраф, город «холодный» и
    обходится маршрутом; одиночный хутор не блокируется. Конфиг `THREAT_URBAN_*`.
49. **Маршруты пролёта вход→цель** (`model/threat_routes.py`): Дейкстра 8-связная,
    стоимость `base + scale·(1−вес_норм)`; `base` мостит нулевые «провалы» (маршрут
    не рвётся), город — большая мягкая стоимость (обходит, но старт/финиш в городе
    возможны); K разнообразных коридоров через штраф пройденных ячеек. Чекбокс
    «маршруты (вход→цель)», расчёт в фоне. Конфиг `THREAT_ROUTE_*`. Задел под будущую
    стохастическую генерацию (развилки ∝ весу, режимы приоритет/баланс).
50. **Легенда и слой исключений.** Легенда цвет→объект (что чем отображается: река/
    дорога/ж-д/ЛЭП/трубопровод/лесополоса/застройка/мост/маршрут/вход/цель). Слой
    «исключения (вода/город)»: синий — вода (запрет датчика), красный — город.
51. **Методички пересобраны** (по запросу): `МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md` — все
    модели движения/маршрутов вкладок 1/2/3 + расстановка + рандом + загрузка карт,
    с формулами; `МЕТОДИЧКА_ВКЛАДКА3_ДОРАБОТКИ.md` — что было до правок, что и почему
    исправлено, как работает сейчас; в `МЕТОДИЧКА_КАРТА_УГРОЗ.md` добавлены разделы
    «что показывают векторные слои», «как строится/отображается тепловая карта»,
    «кандидатные позиции», исключение городов, маршруты.

---

### Сессия: маршруты по тепловой карте, реки в городе, легенда снизу

52. **Маршруты шли ПРЯМЫМИ линиями** (игнорировали тепловую карту, fig 1–4).
    Причина: стоимость нормировалась на `max(W)`; один сверх-тяжёлый узел делал
    `W_норм` у всех остальных ≈ 0, стоимость почти одинаковой → Дейкстра шла по
    кратчайшей прямой. Исправлено: нормировка по 90-му ПЕРЦЕНТИЛЮ — коридоры дёшевы,
    пустоши дороги, маршрут реально идёт по рекам/дорогам (проверка: средний вес
    вдоль маршрута ≫ вдоль прямой; 66 поворотов вместо 0).
53. **Реки в городе обнулялись** (без тепла, fig 4). Исключение города зануляло ВСЕ
    ячейки застройки, включая речные. По замечанию пользователя («по рекам БПЛА точно
    передвигается, в городе можно только по воде») — теперь из маски города вычитаются
    речные ячейки (`urban &= ~river`): река остаётся коридором сквозь город, маршрут
    может идти по руслу. Плюс исключаем ТОЛЬКО КРУПНЫЕ города: порог плотности поднят
    (25% в окне 4 км), деревни/хутора не блокируются (`config.THREAT_URBAN_*`).
54. **Легенда** перенесена в ЛЕВЫЙ НИЖНИЙ угол, сделана ярче (синяя рамка, белый
    крупный текст) и вынесена на ОТДЕЛЬНЫЙ чекбокс «легенда» (независимо от слоёв).
    Число маршрутов увеличено (14). Маркеров мостов ≤150 (меньше засорения).
55. **Известное ограничение (задокументировано, TODO(map-blur)).** На вкладке 2
    подложка иногда слегка мутновата при промежуточных масштабах (вид между уровнями
    зума) и кратковременно во время жеста. Радикально — послойные `QGraphicsPixmapItem`
    или веб-виджет Leaflet; оставлено на будущее (комментарий в `basemap_mixin.py`).

### Сессия: огибающая мест пролёта, окно входных данных, перекрёстки, задел итераций

56. **Маршрутов было ровно 14, БПЛА «не мог обойти».** Пользователь: главное
    ограничение — ЗАПАС ХОДА, показать ВСЕ возможные места пролёта, разрешить длинные
    обходы (зайти с другой стороны). Добавлена **огибающая достижимости**
    (`flight_envelope`, `model/threat_routes.py`): два поля геометрических расстояний
    Дейкстрой — от входа `g_вход` и от цели `g_цель` по проходимым коридорам; ячейка
    попадает в «места пролёта», если `g_вход + g_цель ≤ L_max`. Так учитываются и
    длинные обходы (лишь бы уложиться в запас хода). Рисуется полупрозрачным слоем под
    маршрутами (`route_area_img`). 14 линий-коридоров остались как ПРИМЕРЫ «тяжёлых»
    путей поверх огибающей. Проверка офскрин: L_max 120→6685, 200→7810, 300→7866 ячеек
    (больше запас хода — больше мест). Вход/цель «дотягиваются» до сети окном ~3 км
    (БПЛА проходит несколько км над полем до реки/дороги).
57. **Окно «Входные данные…» (немодальное, как во вкладке 2).** `InputDataDialog`
    (`view_qt/threat_view.py`): поля `L_max`, скорость, крен, интервал смены
    направления; показывает вычисленное `|AB|` (вход→цель) и радиус разворота
    `R_min=V²/(g·tan φ)`. Открывается по кнопке, НЕ блокирует программу (`show()`, не
    `exec_()`), применяется по Enter через колбэк `on_input_apply` — параметры
    записываются в модель и, если маршруты показаны, пересчитываются в фоне.
58. **Перекрёстки (чекбокс, скрыт по умолчанию, в легенде).** `crossing_cells_km`
    (`model/threat_grid.py`): узлы, где в ячейке присутствуют ≥2 РАЗНЫХ слоёв
    (дорога×река, дорога×ЛЭП, дорога×трубопровод) — реальные пересечения, не
    параллельные линии. На тепловой карте такие узлы уже усилены (funnel-бонус: веса
    слоёв в ячейке складываются, дорога+река суммируются); чекбокс лишь ПОДСВЕЧИВАЕТ их
    жёлтыми ромбами (`render_crossings`). По умолчанию выключен. Проверка: 96 узлов.
59. **Задел под итерационную генерацию (методичка §4.5).** Документирована будущая
    стохастическая модель: 4 режима (приоритет тяжёлых / сбалансированный / приоритет
    лёгких / смесь 33-33-33), выбор развилки на перекрёстке по весу соседей
    `P(в соседа j) ∝ f_режим(W_коридор(j))·b_цель(j)`, манёвр по радиусу разворота
    `R_min`, смена направления не чаще `THREAT_TURN_INTERVAL_KM` (2.5 км, настраивается),
    прямой «мостик» только через провалы ≤2 км (больше — путь невозможен), поштучная
    анимация путей как во вкладке 2. Код-задел: `plan_routes` уже даёт разнообразие
    через штраф пройденных ячеек — прямой предшественник рандомных развилок.

### Сессия: итерационный процесс РЕАЛИЗОВАН (ветка `claude/iterative-routes`)

Отдельная ветка от рабочей (по просьбе «закосить основную, сделать отдельную»), чтобы
эксперимент не задел стабильный код.

60. **Стохастическая генерация множества маршрутов** — `iterate_routes`
    (`model/threat_routes.py`). Пошаговый обход вход→цель: тяга к цели через поле
    расстояний `gt` (Дейкстра от цели) с экспоненциальным предпочтением прогресса
    (`exp((gt[тек]−gt[сосед])/τ)`) — гарантирует приход к цели и близость к
    кратчайшему; развилки — по весу коридора соседа с множителем режима
    (`heavy/balanced/light/mix`, «пол» не даёт вырождения в один коридор); держание
    курса ≥ `THREAT_TURN_INTERVAL_KM`; разворот назад запрещён; сглаживание Chaikin
    (приближение манёвра `R_min`). Отбраковка длиннее `L_max`. Проверено: режимы дают
    разный средний вес карты вдоль маршрутов (heavy +3.2, balanced +0.9, light −0.6),
    все приходят в цель.
61. **Запас хода — авто `|AB|+25 %`** (`sync_auto_L_max`, `_refresh_envelope`). Считается
    между ФИКСИРОВАННЫМ входом и ЗАДАННОЙ кликом целью; при смене цели пересчитывается.
    Пол: не меньше кратчайшего коридорного пути +20 % (иначе на «узкой» местности
    маршрут не уместился бы вовсе — на демо `1.25·|AB|=104.7` при кратчайшем 92.2 →
    итог 110.7). Ручной ввод — галочка «авто» в окне «Входные данные» (снял → поле
    L_max редактируемо). Диагностика: L_max 110→11, 120→40, 140→60 маршрутов — на
    реальной плотной OSM-сети маршрутов будет много и при `1.25·|AB|` (замечание
    пользователя про «14 только на импровизированной карте» — верное).
62. **Интерфейс итераций.** Чекбокс «итерации (много маршрутов)» + выпадающий «режим
    итераций» (4 режима); генерация в ФОНОВОМ потоке (kind="iter"); показ ПОШТУЧНОЙ
    анимацией (`QTimer` ~22 пути/с, `render_iter_routes`). Смена цели/режима/входных
    данных пересчитывает показанное. Окно «Входные данные» обновляет L_max на лету.
63. **Методичка §4.5** переписана с «спроектировано» на «реализовано» (фактический
    алгоритм + обоснование отличий от эскиза); дальше — «тепловая карта маршрутов» из
    плотности выборки и расстановка датчиков по ней.

### Сессия: тест на реальных OSM — датчики, зум, кнопка симуляции

По замечаниям пользователя на реальных данных (демо-регион):

64. **Датчики «разбегались» от городов, покрытие ~22 %** (было ~79 % «пару итераций
    назад»). Причина: исключение города вносило штраф −40 в ВЕСОВУЮ карту, а на реальных
    данных именно город — самый плотный по дорогам (высокий вес); превращая его в
    «−40-пустыню», мы заставляли жадный алгоритм (максимизирует ПОЛОЖИТЕЛЬНЫЙ вес)
    уходить от городов, покрытие падало. Исправление: `THREAT_URBAN_IN_WEIGHT=False` —
    город в вес НЕ вносится (датчики снова садятся на плотные узлы, как раньше), но
    остаётся МАСКОЙ для МАРШРУТА (`urban_mask`): БПЛА над плотной застройкой всё равно
    не летит. Маршрутизация не изменилась (использует маску, а не знак веса). Правильное
    итоговое решение — расставлять датчики по «тепловой карте маршрутов» (задел §4.5).
65. **Зум: весь участок не помещался** (широкий экран из-за фиксации соотношения сторон
    подрезал bbox сверху/снизу). `_set_view_limits` out_margin 1.05→1.4, `frame_bbox`
    padding до 0.06 — теперь «ещё один зум мельче»: весь прямоугольник виден сразу, по
    краям чёрные поля (тайлы есть только внутри bbox). Проверено: вид −58..58 вмещает
    bbox −47..47.
66. **Кнопка «▶ Запустить итерации (симуляция)»** — пользователь искал, как запустить
    процесс. Кнопка включает чекбокс «итерации» и запускает генерацию в фоне (то же — и
    сам чекбокс). Огибающая («места пролёта») стала прозрачнее (α 70→45), чтобы не
    перекрывала тепловую карту.
67. **Пояснения (не баги):** фиолетовая заливка = огибающая «все места пролёта»
    (envelope, §4.4); 14 розовых линий — лишь ПРИМЕРЫ кратчайших коридоров, поэтому не
    покрывают все достижимые места — множество путей даёт «Запустить итерации». Запас
    хода L_max (окно «Входные данные») задаёт РАЗМЕР огибающей: больше L_max → больше
    достижимая область → больше и разнообразнее маршруты.

### Сессия: итерации-«симуляция» как во вкладке 2 (Пуск/Шаг/Пакетно)

По требованию «отобразить ВСЕ возможные маршруты (а не 14), с итерациями и анимацией
как во вкладке 2»:

68. **Пошаговая итерационная модель** (как `step()`/`run_batch()` во вкладке 2):
    `ThreatModel.iter_reset/iter_step/iter_batch` + счётчик `iter_iteration`; выборка
    вынесена в `build_iter_context`+`sample_one_route` (контекст считается один раз,
    каждая итерация — один стохастический маршрут). Маршруты НАКАПЛИВАЮТСЯ → показывается
    множество путей по коридорам, а не 14.
69. **Интерфейс итераций как во вкладке 2:** поле «число итераций» (T), слайдер
    «скорость», кнопки **Пуск/Шаг/Пакетно**. «Пуск» — таймер (33 мс) рисует итерации по
    одной с АНИМАЦИЕЙ полёта БПЛА (маркер-треугольник движется по маршруту), заголовок
    «Итерация N/T | маршрутов K». «Шаг» — одна итерация. «Пакетно» — все T сразу (в фоне).
    Шаг анимации ∝ длине маршрута (любой путь пролетается за ~20 кадров).
70. **Повышена вероятность успеха выборки** (шаг к цели строже: назад ≤0.8 км, тяга
    `exp(prog/0.4)`) + до 40 попыток на итерацию — каждая итерация надёжно даёт маршрут
    (для плавной анимации). Режимы по-прежнему различаются (heavy — к рекам/дорогам,
    light — «пустоши»). На демо ~15 %/попытку → 40/40 итераций с маршрутом.
71. **Баг окна «Входные данные»:** поле L_max оставалось пустым (только оно). Теперь
    ВСЕГДА заполнено (авто — показывает вычисленное, readonly; ручной — редактируемо);
    `_apply` устойчив к нечисловому вводу (берёт прежнее значение, не роняет всё
    применение). Дубликат `_apply` удалён.

### Сессия: режимы = боковой разброс, датчики по частоте маршрутов, 2-я тепловая карта

По разбору «всё идёт через центр; датчики не двигаются; нужна 2-я карта частоты пролёта».

72. **Режимы = величина БОКОВОГО разброса** (а не вес): center (узко) / middle / edge
    (к краям) / mix. Реализовано через VIA-точку: маршрут идёт вход→боковая точка→цель,
    боковое смещение = доля запаса хода сверх кратчайшего (center мало, edge максимум).
    Веер VIA-полей расстояний считается ОДИН раз на цель (кэш) — быстрая генерация без
    Дейкстры на маршрут. Выбор коридора по ВЕСУ и развилки СОХРАНЕНЫ в каждом режиме.
    Проблема, которую чинили: жёсткий спуск по gt держал все пути у кратчайшего (центр) —
    боковое притяжение его не пересиливало; VIA-обход решает. NB: разброс ограничен
    запасом хода И протяжённостью коридоров (на демо коридоры узкие ±20 км → веер узкий;
    на реальной плотной OSM-сети — шире; для широкого веера нужен больший L_max).
73. **Датчики — по ЧАСТОТЕ маршрутов** (как во вкладке 2), а не по сырому весу:
    `_place_by_routes` строит CoverageCache из накопленных маршрутов (add_trajectory),
    ЯКОРЬ — кандидат у цели B, накрывающий больше всего маршрутов; кандидаты в его
    радиусе исключаются (не кучкуются у B), дальше — обычный жадный субмодулярный выбор.
    Пересчёт после «Пуск»/«Пакетно» и по кнопке «Расставить датчики». Пока маршрутов < 5
    — по прежней весовой карте.
74. **2-я тепловая карта — частота пролёта БПЛА** (`route_density_field`): сколько
    маршрутов проходит над клеткой; отдельный чекбокс, LUT magma (√-подъём редких).
    Именно по ней и расставляются датчики.
75. **Авто-L_max** поднят до max(|AB|+25 %, кратчайший×1.35) — иначе на боковой разброс
    не оставалось запаса. Выбор развилки ускорен (cumsum+searchsorted вместо
    rng.choice(p=)). Счётчик итераций = число накопленных маршрутов; T = целевое число.

### Сессия: чекбоксы, режимы по весу, правила датчиков A/B, легенда, авто-L_max

Разбор замечаний пользователя по чекбоксам и багам (тест на реальных OSM).

76. **Чекбокс «маршруты» = ВСЕ возможные пути А→Б** (не 14). `plan_routes` теперь даёт
    большой стохастический набор (mix) через кэш-контекст выборки; `THREAT_ROUTE_COUNT`
    14→120 (цель, ограничена запасом хода: L_max 124→~72, 200→120). Розовым, тоньше.
77. **Режимы = ПРИОРИТЕТ по весу** (max/medium/min/mix), а НЕ разброс. Ошибка прошлой
    сессии: переопределил режимы как боковой разброс — пользователь же просил и разброс
    (оставить), и приоритет по весу (вернуть). Теперь: разброс — всегда (случайная VIA
    на маршрут, 20 % прямых), приоритет по весу — режимом (`_mode_corridor_pref`). Это же
    чинит **баг «в центре нет пути»**: режим «мин. вес» вытягивает одинокую реку в центре.
78. **Правила расстановки датчиков** (по выборке маршрутов): кандидаты только МЕЖДУ A и B
    по оси (не за целью, не перед стартом); 1-й датчик — якорь в ~0.45·R от B в сторону A
    (не в самой B — убирает «залипание у цели»); перекрытие зон ≤ ~10 % (min_sep=1.6·R,
    добавлен общий `min_sep` в `CoverageCache.greedy`). Проверка: 12 датчиков, s∈[0,1],
    макс. перекрытие 10 %.
79. **Баг окна: L_max пропадал при снятии «авто».** `_auto_changed` теперь ВСЕГДА
    заполняет поле текущим значением (и в авто, и при переходе в ручной), не оставляет пустым.
80. **«итерационные маршруты» выкл** → при анимации на карте только ТЕКУЩИЙ пролёт БПЛА
    (не весь накопленный веер) — `_anim_tick` уважает чекбокс.
81. **Легенда** — плотный НЕпрозрачный фон (`#0b111c`, `TextItem(fill=…)`, толще рамка) —
    была плохо видна. Число возможных маршрутов выведено в панель показателей.
82. **Город снова даёт минус — ОТЛОЖЕНО** (`THREAT_URBAN_IN_WEIGHT` пока False). По просьбе
    пользователя: это часть большой идеи (датчики по тепловой карте МАРШРУТОВ, а не по
    сырому весу) — сначала ИССЛЕДОВАНИЕ и оценка (методичка §4.6), реализация — по команде.
    Причина, почему нельзя просто вернуть минус: без route-heatmap-датчиков «-40-пустыня»
    снова ломает расстановку по весу (было ~22 % покрытия).

### Сессия: 2 списка (вес+разброс), заход с любой стороны, «все возможные» ~сотни, меньше зигзагов

83. **Два выпадающих списка** (по просьбе): «приоритет веса» (max/medium/min) И «разброс
    по карте» (center/middle/edge) — независимо комбинируются (напр. «мин. вес»+«центр»).
    Разброс реализован ВЫБОРОМ VIA-точки по «дальности обхода».
84. **VIA-точки — СЕТКА по всей проходимой области** (было: только перпендикуляр посреди
    прямой). Теперь маршрут может зайти в цель с ЛЮБОЙ стороны (юг/восток/за целью), если
    хватает хода — чинит «на юге/востоке нет маршрутов даже при L_max=500». Каждой via —
    расстояние до отрезка вход→цель (для режима разброса: center близкие, edge дальние).
    Проверка: разброс center→edge даёт средний max|lat| 16→31 км.
85. **«Все возможные маршруты» — правильное определение** (пользователь): путь идёт от
    сегмента к сегменту, где вес>0 ИЛИ разрыв < ШАГА СЕТКИ ДАТЧИКА, пока не дойдёт до Б.
    Порог мостика теперь = шаг сетки датчика (`threat_cand_step_km`), а не 2 км. Строго
    «все» перечислить нельзя (их экспоненциально много) — строим большую выборку (цель 400)
    с ДЕДУПОМ почти одинаковых. Число выведено в панель («возможных путей А→Б»).
86. **«Смена направления» (интервал доворота):** строже держим курс между доворотами
    (`hf` резко падает для не-прямых) — МЕНЬШЕ ЗИГЗАГОВ; больше интервал = прямее и
    маршрутов меньше (проверка: интервал 2.5→12 км: поворотов 62→29). Розовый оттенок = сами
    линии маршрутов (гуще — розовее), пояснено.
87. **Выбор развилки ускорен** (cumsum+searchsorted). Огибающая (envelope) считается тем же
    порогом разрыва, что и маршруты (согласованность). Пункт 4 (минус городам + route-heatmap
    датчики) — по-прежнему ОТЛОЖЕН, только исследование (§4.6).

### Сессия: 2 якоря датчиков, «розовый фон» убран, зигзаги (RDP), маршруты на «Построить карту»

88. **Датчики — ДВА якоря** (у входа A И у цели B, каждый ~0.45·R от точки внутрь), из
    ближних — накрывающий больше маршрутов; остальные перекрывают якоря ≤10 % (min_sep=1.6R).
    `CoverageCache.greedy(anchors=[…])` — список якорей. Проверка: A=9.5, B=7.4 км, перекр.8 %.
89. **«Розовый фон» убран.** Пользователь: «линии понятны, фон путает». Огибающую
    (route_area) под чекбоксом «маршруты» больше НЕ рисуем — только сами ЛИНИИ (они и
    показывают достижимую область). Линии тоньше/прозрачнее (плотность видна градиентом).
90. **Зигзаги «на месте»** — добавлено упрощение ломаной RDP (Дуглас–Пекер, eps≈1.6 клетки)
    перед сглаживанием; строгое удержание курса. «Смена направления»: больше интервал =
    прямее и путей меньше (это и есть «прямолинейность маршрута», подтверждено).
91. **Маршруты считаются при «Построить карту»** и при смене цели (в фоне, поток) —
    число и линии сразу готовы (было: 0 в панели). Порог соединения сегментов = шаг сетки
    БПЛА (`threat_cand_step_km`), как определил пользователь. Число целевых путей 400→250
    (быстрее, ~5 c в фоне). Заголовок показывает «Возможных маршрутов А→Б: N».
92. **Про «все возможные»/реализм:** процесс — VIA-точка (обход/заход с любой стороны) +
    стохастический проход по коридорам (вес>0 или разрыв<шага БПЛА). Плотный вид — потому
    что сотни маршрутов делят одни магистральные коридоры (это и реалистично). Строго
    перечислить «все» нельзя (экспоненциально) — большая выборка с дедупом.

### Сессия: КОРРЕКТНОСТЬ маршрутов — огибающая (граф) как ответ, а не «250 путей»

Замечание пользователя: «250 путей — ошибка, посчитай корректно графом; юг не строится».

93. **Диагностика:** огибающая (`flight_envelope`, обход Дейкстрой по графу коридоров)
    покрывает 94 % всей проходимой области и **100 % проходимого юга** — то есть «все
    достижимые места» СЧИТАЮТСЯ ВЕРНО (граф), включая юг. Проблема была не в расчёте, а в
    том, что я убрал этот слой, а стохастические ЛИНИИ на юг доходят редко (смещение).
94. **Огибающая-«розовый фон» возвращена** как КОРРЕКТНЫЙ ответ «все места пролёта»: граф
    A→ячейка→цель ≤ L_max по коридорам (вес≠0 + мостики < шага БПЛА). Линии — толще/ярче,
    поверх фона, как ПРИМЕРЫ путей.
95. **Метрика исправлена:** «250 путей» было неверно (это лимит выборки, а число путей
    экспоненциально). Теперь показываем ДОСТИЖИМУЮ ОБЛАСТЬ (`reachable_stats`): «мест
    пролёта: N ячеек = X км²» — это точная мера, а линии-примеры помечены как выборка.
    Заголовок: «Мест пролёта (граф, ≤L_max): X км²».
96. Порог соединения сегментов = шаг сетки БПЛА (подтверждено). Пункт 4 (минус городам /
    route-heatmap датчики) — по-прежнему отложен (§4.6).

### Сессия: ЭТАП 4 реализован — минус городам + датчики по тепловой карте маршрутов

По команде «давай попробуем 4 этап + обобщённая выборка по сумме весов, распределённо».

97. **`THREAT_URBAN_IN_WEIGHT=True`** — город снова даёт МИНУС (−40) в вес; маршруты его
    обходят (в застройке ~8 % точек — реки сквозь город, они остаются коридором).
98. **Датчики по тепловой карте МАРШРУТОВ** (§4.6): `_sample_for_sensors` = итерации, если
    идут, иначе ВСЕ возможные пути; `place_sensors` строит все возможные, если их нет, и
    ставит датчики по НИМ (правила A/B, разнос). 2-я тепловая карта переключается:
    до итераций — плотность всех возможных, при итерациях — выборка пролётов, на сбросе —
    снова все возможные. Покрытие 78 %→91 % (было 22 % по сырому весу с минусом города).
99. **Обобщённая выборка** «маршрутов» для датчиков: вес `medium` (вероятность ∝ сумме
    весов тепловой карты — «наиболее вероятные»), разброс `mix` (распределённо по карте).
    Датчики почти не попадают на город (1/12 — якорь у входа/цели в самом городе).
100. Методичка §4.6 переведена в «РЕАЛИЗОВАНО».

### Сессия: оформление вкладки 3 + обобщённая выборка 10 %

По требованию «поправить графику: датчиков не видно; итерации, точки A/B, все места
пролёта — контрастнее и плотнее; векторные слои толще; + чекбокс, обобщающий тепловую
карту и показывающий 10 % пройденных итераций (частые, но распределённо)».

101. **Оформление (только view, логика не менялась):**
     * датчики — **залитая оранжевая форма** (`#ff8c1a`): круг зоны обзора с заливкой
       (alpha 70) и жирной обводкой + крупная оранжевая точка сверху (были бледно-зелёные,
       терялись);
     * итерационные маршруты — плотнее/контрастнее: накопленные `#36c5f0` alpha 80→190,
       ширина 1.1→2.1; текущий `#ffe066` ширина 2.4→3.2;
     * значки входа **A** (зелёная звезда 16→26) и цели **B** (красный крест 16→24) —
       крупнее, жирная белая обводка, верхний z-слой;
     * **все места пролёта**: розовый фон-огибающая плотнее/контрастнее (alpha 60→120),
       линии-примеры перекрашены в бирюзовый `#00e5ff` (ширина 2.4) — читаются на фоне;
     * векторные слои (реки/дороги/ЛЭП/трубы/ж-д/…) — немного толще;
     * легенда обновлена под новые цвета (розовый = места пролёта, бирюза = возможные
       маршруты, лайм = обобщённая, оранжевый = датчик, A/B подписаны).

102. **Обобщённая выборка (10 %)** — новый чекбокс + `ThreatModel.generalized_sample(frac=0.10)`:
     показывает не весь веер (напр. 500), а ~10 % ПРОЙДЕННЫХ итераций (прошло 150 → 15).
     Отбор — жадный **субмодулярный** по полю частоты пролёта (как у датчиков): берём
     маршрут с макс. покрытием ещё «непокрытой» частоты, затем ГАСИМ покрытые им клетки
     (×0.15) → следующий тяготеет к ДРУГОМУ загруженному коридору. Итог: самые ЧАСТЫЕ, но
     РАСПРЕДЕЛЁННО по всей карте, без скучивания. Отдельный лаймовый слой `#c6ff00`.
     Пересчитывается «на этот момент» по ходу анимации (на завершении каждого пролёта в
     `_anim_tick`, а также в «Шаг»); чекбокс, как и «итерации», при включении запускает
     генерацию, если веера ещё нет.

103. **Проверка стендом:** 100 маршрутов → 10; 500 → 50; 150 → 15; распределение выбранных
     по трём коридорам {частый: 4, средний: 3, редкий: 3} — представительно (частый берётся
     чаще) и без кучкования.

### Сессия: доля обобщённой выборки в интерфейсе + пояснительный документ

104. **Поле «обобщ. %»** рядом с «числом итераций»: доля обобщённой выборки настраивается из
     UI (`threat_iter_gen_frac`, по умолчанию 10 %). `generalized_sample(frac)` берёт долю из
     поля через `get_iter_gen_frac()` (Enter применяет сразу, слой перерисовывается).
     Проверка: доли 5/10/20/50 % из 100 маршрутов → 5/10/20/50.
105. **Новый документ `теория/ВКЛАДКА_3_КАК_РАБОТАЕТ.md`** — пояснение вкладки 3 «простыми
     словами, затем подробнее», без иностранных выражений (термин — в скобках): подложка,
     слои, весовая/тепловая карта, входные данные, все возможные маршруты (огибающая),
     итерации и развилки, обобщённая выборка, датчики, сводка показателей/методов.

### Сессия: плавные маршруты (дуги+прямые), «длина прямого участка», цвета слоёв

106. **Плавность маршрутов вместо зигзагов/«кружения»:**
     * ИНТЕРПОЛЯЦИЯ **Катмулла–Рома** (центростремительная) вместо Chaikin — повороты сетки
       становятся плавными ДУГАМИ, прямые остаются прямыми, без петель-выбросов;
     * **«Смена направления» = ЦЕЛЕВАЯ длина прямого участка**: допуск упрощения (RDP) ∝ ей
       → чем больше значение, тем длиннее прямые звенья и прямее маршрут (проверка на демо:
       извилистость L/|AB| 1.22→1.05 при L 8→35 км, типичный прямой отрезок 3.5→8.0 км);
       в самом блуждании курс держится мягко и ≤6 км (жёсткая длинная прямая роняла
       доходимость до цели, особенно через VIA);
     * **анти-петля**: повторный заход в клетку штрафуется (×0.12) — «кружение» уходит, но
       тупиков нет (не запрет); back_allow=0.8 у стенок;
     * **ранняя остановка выборки** (cap ≤ 800 попыток, since_new < 350) → генерация ~4 с
       вместо 8 с, когда набор уникальных маршрутов на карте исчерпан. Доходимость и
       покрытие датчиков сохранены (81 % на демо).
107. **Цвета/приоритет слоёв (оформление):**
     * ВСЕ возможные маршруты — бирюзовые ПОЛУПРОЗРАЧНЫЕ (не сливаются, не прячут датчики);
     * зона обзора датчиков — НАД всем (z=20/21), маршруты её не перекрывают; A/B — ещё выше
       (z=22);
     * итерации: ОСНОВНОЙ (текущий) маршрут — ЧЁРНЫЙ; «выборка» (накопленные + обобщённая) —
       КРАСНАЯ и менее плотная (накопленные — бледнее, обобщённая — заметнее);
     * легенда и подпись поля «Длина прямого участка, км» обновлены.

### Сессия: рефакторинг вкладки 3 (чистка + пояснения + цвета в одном месте)

Задача: обобщить/причесать код без изменения поведения; собрать цвета в одно место с
пометками для самостоятельной правки; добавить пояснения; зафиксировать ограничения
маршрутов как задел. Область — только вкладка 3 (общий/вкладка-2 код не трогали: он
работает, а GUI в этом окружении не запускается для проверки).

108. **Удалён мёртвый/устаревший код** в `model/threat_routes.py`: старый Дейкстра-роутинг
     по стоимости (модульные `plan_routes`, `iterate_routes`, `_dijkstra`, `_step_cost`,
     `_path_to_km`) и неиспользуемый `_chaikin` — их заменили стохастическое блуждание и
     Катмулл-Ром. Из `config.py` убраны осиротевшие `THREAT_ROUTE_BASE_COST/WEIGHT_SCALE/
     URBAN_COST`. Заголовок модуля переписан под текущую схему (огибающая + Монте-Карло).
109. **Все цвета вкладки 3 — в одном месте** (`view_qt/threat_view.py`, вверху): палитры
     `LAYER_STYLE` (векторные слои) и `THREAT_COLORS` (маршруты/огибающая/датчики/маркеры/
     легенда) + `THREAT_URBAN_RGBA/WATER_RGBA`. У каждого цвета — комментарий, ГДЕ он
     применяется. Легенда и слои теперь берут цвет из палитры (дубли hex убраны — правка в
     одном месте меняет и карту, и легенду). Значения сохранены 1:1.
110. **Пояснения (docstrings/комментарии)**: заголовки-классы (`ThreatController`,
     `ThreatMapView`, `_TaskSignals`), проекция «градусы→км», `build`/`candidate_positions`,
     анимация (`_anim_tick`), фон (`_run_async`/`_on_task_done`), отрисовка (`_render_all`,
     `_build_scene_items`, `render_sensors`), необычные приёмы (рулеточный выбор через
     `cumsum`+`searchsorted`, Катмулл-Ром, `heapq`). Тривиальные `get_*/set_*` оставлены
     как есть (говорят сами за себя) — это отражено в docstring классов.
111. **Ограничения маршрутов — как задел**: комментарий у `_walk_field` и разделы 10–11 в
     `ВКЛАДКА_3_КАК_РАБОТАЕТ.md` (движение по сетке → зигзаги/возвраты, нет настоящей
     кинематики; направление развития — кривые Дубинса / RRT* с R_min). Плюс раздел «где
     менять цвета».
112. **Проверка (headless-регрессия до/после):** источник, число маршрутов (все в пределах
     L_max), площадь огибающей (~1797 км²), итерации, датчики 12 и покрытие ~80 %,
     обобщённая выборка — совпадают; мёртвого API не осталось. Поведение сохранено.

### Сессия: прогоны всех 3 вкладок, ФИКС РЕГРЕССИИ tab1/tab2, DRY-рефакторинг моделей

Задача: прогнать код всех вкладок, причесать под «человеческий» вид (базовые принципы,
DRY), убрать лишнее — с сохранением функционала всех 3 вкладок. Проверка — headless-прогон
моделей (`smoke_all`) и offscreen-построение всех view+controller (`smoke_gui`).

113. **НАЙДЕНА И ИСПРАВЛЕНА РЕГРЕССИЯ (вкладки 1 и 2 падали).** При обобщении
     `CoverageCache.greedy` под вкладку 3 единичный параметр `anchor_idx` стал списком
     `anchors`, но вызовы в `simulation.py`/`area_start.py` не обновили — `run_batch`,
     `recompute_placement`, `compare_modes` бросали `TypeError`. Исправлено в 4 местах:
     `anchors=[ai] if ai is not None else None`. Прогон подтвердил: обе вкладки снова
     считают датчики (6/6, сравнение режимов работает).
114. **DRY: общий базовый класс `RouteModelBase`** (`model/route_model.py`). 9 БАЙТ-
     идентичных методов, скопированных в обе модели (`run_batch`, `step`, `add_and_replace`,
     `recompute_placement`, `compare_modes`, `live_metrics`, `set_mode`, `set_profile`,
     `weights`), вынесены в базу; `SimulationModel` и `AreaStartModel` её наследуют.
     Убраны ставшие лишними импорты `MODES`. Поведение идентично (smoke до/после совпал).
115. **Удалён мёртвый код**: `greedy_placement` (экспортировался, но нигде не вызывался) —
     из `optimization.py` и из `model/__init__.py` (`__all__`).
116. **Дымовые прогоны**: `smoke_all` (модели 3 вкладок) и `smoke_gui` (offscreen-построение
     view+controller всех вкладок + отрисовка tab3) проходят без ошибок.

---

### Сессия: прогон вкладки 3 на РЕАЛЬНЫХ данных OSM — маршруты не доходили до цели

Прогон на реальном OSM-кэше (`geo_cache/threat_layers.npz`, 47 МБ, ~137 тыс. объектов)
вместо синтетической схемы. Подробности механики — в методичках (ссылки ниже).

117. **Маршруты почти не строились: заказано 250 — построен 41.** 96,5 % попыток
     завершались отказом. Причины: (а) половина VIA-точек была физически недостижима при
     данном запасе хода, но это не проверялось; (б) маршрут летел «вслепую» — проедал весь
     запас на виляние и обрывался у самой цели. **Фикс:** фильтр VIA по достижимости +
     ПРОФИЛЬ РАСХОДА ЗАПАСА ХОДА (виляет, пока запас есть; на исходе идёт прямо).
     Успех 5 % → 92 %, маршрутов 250/250, при этом вдвое быстрее.
     → механика: [МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md §4.5](МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md)
118. **Маршруты срезали углы через ГОРОД и за область залёта.** Расчётная ломаная всегда
     лежала в коридоре, но СГЛАЖИВАНИЕ (то, что рисуется и по чему считаются датчики)
     срезало углы в застройку: 828 ячеек вне области, 2721 точка в непроходимом.
     **Фикс:** `_smooth_safe` подбирает допуск упрощения так, чтобы кривая не вышла из
     коридора. Стало 0 нарушений. Попутно снят мёртвый допуск приёмки `L_max × 1.15`:
     сглаживание маршрут только спрямляет, длиннее запаса хода не выходит никто.
119. **«Разрыв между секторами» — рычага не было вовсе.** Константа
     `THREAT_ROUTE_MAX_GAP_KM` в конфиге **не читалась ни одной строкой кода**, а порог
     молча брался из шага сетки датчика. **Фикс:** живой параметр `Params.threat_max_gap_km`
     в окне «Входные данные»; меняет проходимость, область залёта и маршруты ещё до
     итераций (0 км → 900 км², 2 км → 2439 км², 3 км → 2688 км²).
     → [ВКЛАДКА_3_КАК_РАБОТАЕТ.md §4](ВКЛАДКА_3_КАК_РАБОТАЕТ.md)
120. **«Почему 250 маршрутов, а не все?»** Посчитано точно, без перечисления: ветвление
     3,31 на ячейку, путь 167 шагов → **3,4 × 10¹⁰⁷ маршрутов** (атомов во Вселенной ~10⁸⁰).
     Перечислить нельзя физически. Поэтому «все возможные пролёты» показывает ЗАКРАШЕННАЯ
     ОБЛАСТЬ (точна, не выборка), а линии — представительная выборка, которую теперь
     раскладывают по области ЖАДНЫМ отбором (покрытие 35 % → 54 %). Число возможных
     маршрутов выводится в статистике.
     → [МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md §4.4](МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md)
121. **Рефакторинг.** Мёртвые константы (`THREAT_ROUTE_MAX_GAP_KM`, `THREAT_TURN_INTERVAL_KM`,
     `THREAT_ITER_ROUTES` — объявлены, но не читались; последняя расходилась с `Params`:
     60 против 150) убраны/оживлены. `_place_by_routes` лез в приватный `cache._hit` —
     заведён публичный `routes_covered_by_candidate()`. Докстрока `sync_auto_L_max`
     расходилась с кодом (1.20 против 1.35) — вынесено в `THREAT_LMAX_CORRIDOR_FRAC`.
122. **Инварианты (проверены прогоном на реальных данных):** 0 маршрутов вне области залёта,
     0 точек в непроходимом/городе, 0 превышений запаса хода, 0 датчиков на воде; засечка
     маршрутов с кратностью ≥ k(3) — 100 %.

### Сессия: векторные слои накладывались неполно; критерий «город vs деревня»

123. **Пропадали дороги М-30/Р-66, Луганск отображался частично.** Две причины разом:
     (а) `pyrosm` отдаёт way не цельной линией, а пачкой 2-точечных обрезков — в кэше
     лежало 10 717 «дорог» вместо 790 цельных; (б) прореживание отрисовки оставляло
     1500 «самых длинных» линий, а при равных длинах (все по 2 точки) — первые
     попавшиеся, кучковавшиеся у Луганска. **Фикс:** сшивка сегментов `linemerge` при
     чтении OSM (+ `complete_relations=True` для контуров городов на краю bbox),
     кэш пересобран; прореживание — по географической длине (км), лимиты подняты,
     мелкие полигоны не схлопываются. Все 790 магистралей и вся застройка (8275
     полигонов) теперь на экране; build ускорился 6.3 → 1.4 с.
     → [МЕТОДИЧКА_ЗАГРУЗКА_КАРТ.md §4](МЕТОДИЧКА_ЗАГРУЗКА_КАРТ.md)
124. **Деревни у трасс «минусовались», как города.** Критерий «плотность ≥ 5 % в окне
     3 км» исключал 31,7 % участка вместе со всеми сёлами. **Фикс:** компонентный
     критерий — крупный нас. пункт = СВЯЗНОЕ пятно застройки ≥ 8 км² («подряд идёт
     сетка зданий» на 3–5 км, `THREAT_URBAN_MIN_AREA_KM2`). Города (Луганск 42.8 км²,
     Алчевск 31.8) исключаются целиком с буфером облёта, даже если сквозь центр идут
     дороги; деревни не исключаются и не минусуются (вклад застройки там снят); река
     сквозь город остаётся коридором. Исключено 4,5 % вместо 31,7 %.
     ВАЖНО: компоненты метятся по сырой застройке — склейка разрывов до разметки
     сцепляла цепочки сёл вдоль трассы в «псевдогород» (проверено: 17,9 %).
     → [МЕТОДИЧКА_КАРТА_УГРОЗ.md §13](МЕТОДИЧКА_КАРТА_УГРОЗ.md)
125. **Инварианты после правок (реальные данные):** дорога в городе — вес −40 во всех
     315 ячейках; река в городе — 0 исключённых из 83; с. Белое у трассы — вес +5.5
     (коридор жив); 250/250 маршрутов, 0 выходов за область, 0 превышений L_max,
     датчики 12/12, засечка ≥ k(3) — 100 %. Область залёта выросла 2439 → 4134 км²
     (город-«пустыня» больше не съедает коридоры).
126. **Офскрин-тесты GUI на этой машине падали (exit 127).** Кириллица в `C:\Users\Денис`
     ломала автопоиск Qt-плагинов (путь конструировался в неверной кодировке).
     Обход — `check_env.py` в корне: пути плагинов задаются готовой строкой из
     `%APPDATA%` ДО импорта QtWidgets; env-переменные ставить внутри Python, не
     префиксом в командной строке (cmd/PowerShell её не понимают одинаково).
     Настоящее решение на будущее: venv в пути без кириллицы (например `C:\venvs\uav`).
127. **Калибровка: тепловая карта была «размазана».** На реальных данных густая сеть
     просёлков (`road_local`) давала 37 % всего веса и создавала фон почти везде.
     Вес слоя понижен 5 → 3: доля упала до 25 %, река/ЛЭП/пересечения/магистрали
     вышли на один уровень (21/18/21/11 %), карта контрастнее. Инварианты сохранены
     (250/250 маршрутов, датчики 12/12, засечка ≥ k(3) — 100 %, покрытый вес 66 %).
     Это КАЛИБРОВКА, не исправление — остальные веса слоёв тоже заданы гипотезой и
     подлежат подгонке по фактической статистике пролётов.

### Сессия: проверка модели расстановки датчиков

Схема «до итераций — по возможным путям, при итерациях — по выборке пролётов»
подтверждена численно (расстановка по итерациям даёт ср. кратность 5.61 против 4.43 и
3.71 у альтернатив). Вскрыты и исправлены три дефекта.

128. **Датчики не двигались во время итераций.** Пересчёт был только ПОСЛЕ последней
     итерации, а кнопка «Шаг» не пересчитывала вовсе — на экране всё время висели
     датчики, посчитанные по «возможным путям». **Фикс:** `_maybe_refresh_sensors` —
     пересчёт каждые `THREAT_SENSOR_REFRESH_EVERY` = 5 маршрутов, видно, как
     расстановка сходится (сдвиг 3.6–7.6 км между обновлениями).
129. **4 из 12 датчиков не ловили НИ ОДНОГО пролёта.** При насыщении основного и
     вторичного критериев `score` обнулялся у всех кандидатов, и `argmax` брал ПЕРВЫЙ
     по порядку массива — датчики выстраивались в ряд по нижнему краю карты, хотя
     оставался 21 полезный кандидат. **Фикс:** третичный ключ в `greedy` — число
     накрываемых маршрутов; при нулевом и он — остановка (лишние датчики не ставятся).
130. **Ставилось 9 из 12 заказанных.** Жёсткий разнос 1.6·R блокировал оставшиеся
     позиции; параметр `threat_min_sep_frac` (0.75) в рабочей ветке вообще не читался.
     **Фикс:** 1.6·R — ЖЕЛАЕМЫЙ разнос, при нехватке ослабляется до
     `threat_min_sep_frac`·R (параметр ожил как нижняя граница). Стало 12/12.
131. **Метрика в заголовке вводила в заблуждение:** показывался «покрытый вес» по
     СЫРОЙ весовой карте, хотя оптимизируется засечка пролётов. Добавлены
     `detect_frac`/`detect_k_frac`/`mean_hits` — засечка выводится в заголовке и в
     блоке показателей. Итог: 12/12 датчиков, ср. кратность 4.6 → 8.0; покрытый вес
     66 % → 58 % (не регресс — датчики идут по коридорам, а не по весу).
     Регресс вкладок 1 и 2 проверен: 6/6 датчиков во всех трёх режимах.
     → [МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md §4.6, §5.2](МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md)

---

### Сессия: данные OSM, механика населённых пунктов, рельеф, отрисовка

132. **Данные OSM приведены в порядок.** Геометрия обрезается по участку +10 км (было:
     объекты до ±94 км, линии уходили за карту; на границе строится точка пересечения
     с рамкой, а не внешняя вершина). Река/канал отделены от ручья (вес 10 против 4 —
     ручьёв в разы больше, они перетягивали карту). Мосты берутся из реального тега
     `bridge=yes` (597 объектов) вместо вычисления «река × дорога» — ложных переправ
     больше нет. Добавлен служебный слой населённых пунктов `place` (510 меток,
     275 контуров), в вес он не входит.
133. **Механика НП переписана: три уровня вместо двух.** Город — пролёт ЗАПРЕЩЁН, вес
     ВСЕХ слоёв внутри обнуляется (ноль, а не −40: отрицательный вес делал город
     «ямой»). Деревня — пролёт разрешён, но вес плавно падает от центра (−20) к краю
     (0), маршрут сам идёт по окраине. Хутор — не учитывается. Тип берётся из тега
     `place` (city/suburb/borough → город; town → город при пятне ≥ 4 км²; village →
     деревня), площадь — уточняющий и запасной критерий.
     → [МЕТОДИЧКА_КАРТА_УГРОЗ.md §13](МЕТОДИЧКА_КАРТА_УГРОЗ.md)
134. **Три дефекта в собственных правках, найдены самопроверкой.** (а) Вес внутри
     города был −20 вместо 0 — штраф соседней деревни залезал в городскую зону.
     (б) Деревня получала минус ДВАЖДЫ: от слоя застройки и от градиента (−26.7 вместо
     −15) — слой `built_up` больше не штрафует сам по себе. (в) Обрезка вылезала на
     11.7 км при допуске 10 — брали внешнюю вершину вместо точки на границе.
135. **Контуры НП: сначала стало хуже.** Административные границы включают поля и
     соседние сёла — запрет вырос до 30.6 % участка, с. Белое стало «городом».
     Фикс: контур засчитывается только при реальной застройке внутри (≥15 % площади
     или ≥2 км²) и режется по ней. Буфер облёта растёт нелинейно и склеивает города в
     агломерацию (0.5 км → 17.7 %, 1.5 → 29.5 %, 2.0 → 34.9 %) — принят 0.4 км.
136. **Рельеф (карта высот).** Читается `geo_cache/dem.tif` (GeoTIFF через rasterio)
     или `*.hgt` (сырой SRTM, чистый numpy). Вклад — ПРЕВЫШЕНИЕ НАД ОКРЕСТНОСТЬЮ
     (высота минус среднее в окне 3 км): низина даёт плюс, холм минус, предел ±8.
     Проверено на синтетическом рельефе: долина +2.1, холм −3.7, плато 0. Файла ещё
     нет — слой просто не участвует, карта строится штатно.
     → [МЕТОДИЧКА_ЗАГРУЗКА_КАРТ.md §4.1](МЕТОДИЧКА_ЗАГРУЗКА_КАРТ.md)
137. **Отрисовка по зуму.** Прореживание считается по ВИДИМОЙ области: при приближении
     лимиты тратятся только на то, что в кадре, и объекты рисуются целиком. Застройка
     на общем виде — растровая маска (8275 контуров всё равно сливались в пятно), при
     приближении — настоящие контуры. Перерисовка по таймеру 180 мс после смены вида.
138. **Инварианты после всех правок:** 250/250 маршрутов, 150/150 итераций, 12/12
     датчиков, 0 выходов за область, 0 превышений L_max, 0 датчиков на воде, засечка
     ≥k(3) — 100 %, средняя кратность 7.5. Города (Луганск, Алчевск, Зимогирье)
     запрещены, сёла у трасс (Белое, Говоруха) — мягкий минус, коридор жив.
> **Ветка `pre-free-zones`** (коммит `3856b37`) — состояние ДО поправки карты весов
> по городу у точки вылета и цели. Там город запрещён ВЕЗДЕ, включая места, где стоят
> сам старт и сама цель: обе точки оказываются внутри запретной зоны, и подлёт к цели
> возможен лишь с одной стороны. Ветка оставлена на случай, если понадобится вернуться
> к прежнему поведению: `git checkout pre-free-zones`. Работа продолжается в
> `iterative-routes`.

139. **Подлёт к цели был возможен только с одной стороны.** Причина: и точка вылета, и
     цель СТОЯТ ВНУТРИ запретных городов — вокруг обеих 100 % ячеек в радиусе 3 км под
     запретом (у цели свободен был только север, 39.8 %; восток 5.7 %, юг и запад — 0).
     Разрыв между секторами тут не помогает: он сшивает пустые места, а не снимает
     запрет. **Фикс:** свободные зоны — освобождается ВЕСЬ населённый пункт, в котором
     стоит точка, плюс буфер (10 км у вылета, 3 км у цели); там не действует ни запрет,
     ни обнуление весов, ни штраф деревни, ни буфер соседнего города. Круга радиуса R
     недостаточно: при цели в центре города края НП остаются закрытыми.
     Стало: доступность цели 92–97 % по всем сторонам, кратчайший путь 86.9 → 79.4 км,
     покрытый вес 59 → 66 %, средняя кратность 7.5 → 8.8. Смена цели кликом пересобирает
     карту (зона привязана к цели).
     → [МЕТОДИЧКА_КАРТА_УГРОЗ.md §13.1](МЕТОДИЧКА_КАРТА_УГРОЗ.md)
140. **Маркеры мостов показывали не то.** Тег `bridge=yes` висит и на путепроводах над
     дорогами/ж-д: из 466 ячеек с мостом лишь 386 — переправы через воду, а на карту
     выводилось 150 случайных (прореживание по индексам массива). **Фикс:** маркеры —
     только переправы (мост над рекой/ручьём), лимит поднят до 600 (влезают все);
     мосты как таковые рисуются отдельным линейным слоем (`LAYER_STYLE["bridge"]`).
141. **Свободные зоны раздулись (регресс п. 139).** Освобождался ВЕСЬ населённый пункт,
     расширенный на радиус зоны: у вылета это 10 км во все стороны от всей агломерации,
     и без минуса оказывались Лисичанск, Тошковка, Гірське — никак не связанные с точкой
     вылета. **Фикс:** правила разведены — у ВЫЛЕТА только круг 10 км; у ЦЕЛИ круг 3 км
     плюс её населённый пункт с буфером 3 км (там круга мало: при цели в центре города
     края НП остались бы закрыты). Стало 9.5 % участка (678 км²) вместо разлива на
     полкарты; проверка по 7 контрольным точкам — 0 расхождений.
142. **Маркеры переправ стояли не на мостах.** Точка бралась как ЦЕНТР ЯЧЕЙКИ сетки
     (500 м) — при приближении треугольник уезжал от моста до 250 м. **Фикс:**
     `bridge_points_km` берёт реальные координаты — середину каждой мостовой линии OSM,
     проходящей над водой (495 маркеров, смещение от узла сетки медиана 193 м).
143. **Проверено: расстановка датчиков и тепловая карта — разные вещи.** Датчики
     оптимизируют ЧАСТОТУ ПРОЛЁТА, а не вес карты: под датчиком частота пролёта выше
     средней по карте в 2.0 раза, а вес — лишь в 1.3. Так и задумано (§4.6 методички
     маршрутов): вес показывает, где местность удобна для полёта ВООБЩЕ, а маршруты
     идут из конкретной точки вылета в конкретную цель — датчики стоят на них.
144. **БПЛА уходил назад — от старта и от цели.** Замер: 20 % маршрутов уходили за точку
     вылета (до 9.8 км, на карте это полоса в обратную сторону), 19 % маршрутов,
     подойдя к цели на 15 км, снова удалялись от неё (до 5.3 км). Причина у обоих —
     VIA-точки: одна лежала позади старта, пять — за целью, и маршрут шёл к ним.
     **Фикс, три ограничения:**
     (а) `START_BACK_KM` = 1 км — маршрут не заходит за точку вылета дальше километра
     (проверка по проекции ячейки на ось старт→цель, поле `axis_s` в контексте);
     (б) `FINAL_APPROACH_KM` = 10 км — в этой зоне запрещён шаг ОТ цели (допуск
     `FINAL_DRIFT_KM` 0.25 км на обход препятствия). Зона считается по ПРЯМОМУ
     расстоянию (`dgoal`), а не по расстоянию через коридоры: по коридорам оно бывает
     вдвое больше, и ограничение срабатывало не там, где видно глазу;
     (в) VIA не позади старта и не «сбоку у цели» — но ЗА цель заходить можно
     (`VIA_BEYOND_KM` = 10 км): облететь объект и зайти с другой стороны это штатный
     манёвр, а не ошибка.
     Стало: назад от старта ≤ 1.3 км (лимит + допуск сглаживания), отворот у цели на
     прямых маршрутах ≤ 0.2 км. Остаток на маршрутах через VIA за целью — те самые
     разрешённые заходы с другой стороны. Проверено, что сглаживание тут ни при чём:
     у сырой ломаной и у сглаженной кривой отвороты одинаковые (8 против 7, макс 2.83 км).
     Разнообразие не пострадало: область 3531 км², боковой разброс до 27 км.
     → [ОГРАНИЧЕНИЯ.md §2.9–2.12](ОГРАНИЧЕНИЯ.md)
145. **Проработана модель учёта высот (код ещё не менялся).** Исходная реализация
     считает превышение над окрестностью в одном окне 3 км — этого мало: на высоком
     открытом плато превышение равно нулю, штрафа нет, а аппарат виден во все стороны.
     Согласован переход на ДВА масштаба: «район» (15 км — общая возвышенность) и
     «окрестность» (2 км — одиночный холм на пути), штраф за возвышение сильнее бонуса
     за низину, у обоих потолок. Правило заказчика: та же дорога в низине даёт +16,
     на гребне −4 — маршрут выберет низину.
     Рассмотрен и **отклонён** более точный вариант — расчёт линии видимости по
     азимутам (viewshed): он отвечает на вопрос «видит ли КОНКРЕТНАЯ станция эту
     точку», а позиции угроз неизвестны; для визуального и акустического обнаружения
     он вообще не работает, а главную ошибку простой модели закрывает второй масштаб
     усреднения. Разбор сохранён с ориентирами на случай возврата.
     Отдельно закрыт вопрос про лес: **укрытием для летящего аппарата не является**
     (идёт над кронами), вес лесополос остаётся 5 как навигационный ориентир.
     → [ПЛАН_РЕЛЬЕФ.md](планы/1_ПЛАН_РЕЛЬЕФ_реализован.md), [РАДИОВИДИМОСТЬ_ЗАДЕЛ.md](РАДИОВИДИМОСТЬ_ЗАДЕЛ.md)
146. **Сверка документации с кодом.** Проверены все методички: числовые параметры
     (30 ключевых значений) сходятся с `config.py` полностью. Исправлены четыре
     устаревших описания механик: мост как вычисленное пересечение реки с дорогой
     (теперь тег `bridge=yes`), критерий города «плотность застройки в окне»
     (теперь связные пятна + теги `place`), штраф города −40 (теперь обнуление),
     список функций `threat_grid.py` в карте проекта (были удалённые `_detect_bridges`,
     `add_bridges_from_grid`; не хватало `apply_settlements`, `load_dem_grid` и др.).
147. **Файл высот получен — схема учёта переписана по замерам (код ещё не менялся).**
     `geo_cache/dem.tif`: SRTM GL1, 1″ ≈ 30 м, 5040 × 2880, охват точно по участку,
     пропусков нет, высоты 23…365 м. Проверка на реальных данных отменила две вещи из
     ранее согласованного плана.
     **Первое: рельеф включился сам.** Старый `add_relief_layer` срабатывает при одном
     условии — наличии файла. Карта поменялась молча: коридорных ячеек стало 13 969
     вместо 10 079, при этом 5 003 коридора **создано на пустом месте** (низина без
     ориентиров становилась «коридором») и 1 113 существующих уничтожено.
     **Второе: аддитивная схема ломает модель.** Согласованная формула
     `−8·f(A/40) − 6·f(B/25)` на реальной карте рвёт связность: компонент 68 → 148,
     крупнейшая область 8 449 → 9 862 при потере 2 232 коридоров (дорога на гребне
     уходит в минус, коридор обрывается посередине). Смягчение весов, обрезание штрафа
     и центрирование лечат лишь частично.
     **Принято:** рельеф **умножает** уже накопленный положительный вес
     (`k = 1 − 0.75·f(A/60) − 0.45·f(B/35)`, зажат в [0.25, 1.40]), ноль остаётся нулём,
     отрицательное не трогается. Проверка: коридоров 10 079, компонент 68, крупнейшая
     8 449 — **структура карты сохраняется точно**. Пороги насыщения подняты с 40/25 до
     60/35 м: при 40/25 в потолок упиралось 38 % площади.
     **Поправки заказчика.** Уводить вес в минус нельзя: `passable_mask` берёт
     `weight > 0`, то есть минус — это полный запрет, а не «маловероятно». Вместо этого
     пол `min(исходный вес, 1.0)` (коридор жив, приоритет низкий) и отсечка
     `A ≥ 80 м или B ≥ 50 м` (очень высокая гора — коридор снимается, 1.4 % коридоров).
     Итог по расчёту на момент согласования: 9 938 коридоров (после реализации стало
     9 944 — выборка высот сменилась на среднее по ячейке, см. п. 148), вход и цель
     связаны, магистраль в пойме +14.0 против +2.5
     на гребне, местная дорога в низине обгоняет магистраль на гребне.
     **Дополнительно согласовано к реализации:** предвидение препятствия (средний рельеф
     на 2 км вперёд по направлению — горизонт решения был 500 м, поэтому маршрут
     упирался в широкие массивы вместо обхода); разведение «разрыва между секторами» и
     «ухода от ориентира» на два параметра (сейчас один задаёт и то, и другое);
     кнопка «Добавить рельеф» — карта строится без высот, рельеф подключается явно;
     два чекбокса отображения (карта высот и приоритет по высоте).
     → [ОГРАНИЧЕНИЯ.md §5.1](ОГРАНИЧЕНИЯ.md)
148. **Рельеф реализован в коде.** Сделано: множитель к весу (два масштаба превышения,
     пол, отсечка), предвидение препятствия, разведение «разрыва» и «ухода от ориентира»
     на два параметра, кнопка «Добавить рельеф», два чекбокса показа, высота ячейки как
     СРЕДНЕЕ по ячейке вместо случайного пикселя из четырёхсот.
     **Три дефекта нашла самопроверка, все исправлены:**
     * *порядок конвейера*. Рельеф стоял ДО населённых пунктов, и деревенский штраф,
       идущий следом, добавлялся к уже ослабленному весу: дорога +10 в краю деревни (−8)
       без рельефа давала +2 и была коридором, а с рельефом на возвышенности
       `10·0.25 − 8 = −5.5` — коридор пропадал. Терялось 117 ячеек помимо отсечки.
       Рельеф перенесён в КОНЕЦ конвейера: гарантия «ноль остаётся нулём» верна только
       относительно итогового веса, а городу порядок безразличен (0 × k = 0);
     * *отсечка стирала отрицательные веса*. Застройка на горе (−15) превращалась в ноль,
       то есть в «нейтральное пустое поле»: для проходимости разницы нет, но тепловая
       карта и расстановка датчиков считают по весам и получали неправду. Теперь отсечка
       обнуляет только положительный вес — снимает КОРИДОР, а не стирает ячейку;
     * *эрозия съедала край участка*. Замыкание (дилатация + эрозия) сшивает провалы, не
       расширяя коридор, но `_dilate` в проекте паддится нулями, и наивная эрозия через
       инверсию обрезала бы коридоры, прижатые к рамке. Написана отдельная `_erode`
       с паддингом «за границей занято».
     **Проверено:** рельеф выключен — карта совпадает с прежней побитово; включён —
     коридоров 9 944 против 10 079, потери ТОЛЬКО от отсечки (при выключенной отсечке
     числа совпадают точно); `THREAT_LOOKAHEAD_POWER = 0` даёт прежние маршруты при том
     же seed. Инварианты §6 держатся в обоих режимах: 250/250 маршрутов, 150/150 итераций,
     12/12 датчиков, нули на месте, засечка 100 %. Эффект: средняя высота под маршрутами
     95.0 → 91.5 м, доля пути по возвышенностям 23.0 → 16.3 %, длина не выросла, разброс
     сохранён на 99 %, покрытие веса датчиками 67.3 → 72.3 %.
     Документация разнесена по местам: механика → [МЕТОДИЧКА_КАРТА_УГРОЗ.md §4.5 и §12.1](МЕТОДИЧКА_КАРТА_УГРОЗ.md),
     правила → [ОГРАНИЧЕНИЯ.md §5.1](ОГРАНИЧЕНИЯ.md), интерфейс → [ВКЛАДКА_3_КАК_РАБОТАЕТ.md](ВКЛАДКА_3_КАК_РАБОТАЕТ.md),
     задел по планировщику обхода → [РАДИОВИДИМОСТЬ_ЗАДЕЛ.md §6](РАДИОВИДИМОСТЬ_ЗАДЕЛ.md).
     [ПЛАН_РЕЛЬЕФ.md](планы/1_ПЛАН_РЕЛЬЕФ_реализован.md) сохранён как история решения: там разобрано,
     какие схемы пробовались и почему отклонены (аддитивная, пороги 40/25, минус вместо
     пола) — чтобы не проходить этот круг заново.

     > ⚠️ **ОГИБАНИЕ ПРЕПЯТСТВИЙ — РАБОТАЕТ ЧАСТИЧНО, ВОПРОС НЕ ЗАКРЫТ.**
     >
     > **Как сейчас.** Маршрут *реагирует* на гору, но не *планирует* обход. Работают
     > три вещи вместе: «уход от ориентира» задаёт ПОЛОСУ, в пределах которой вообще
     > можно отклоняться (по умолчанию 1 км в каждую сторону); рельеф делает вес внутри
     > полосы неодинаковым (на склоне множитель падает до 0.25); предвидение видит склон
     > за 2 км и заранее снижает привлекательность этого курса. В итоге маршрут «стекает»
     > в ту часть полосы, где ниже.
     >
     > **Чего НЕ хватает — два ограничения.**
     > 1. *Полоса манёвра постоянная и не зависит от рельефа.* Она НЕ расширяется сама
     >    там, где впереди гора. Отойти можно ровно на заданную величину, поэтому
     >    **гору шире полосы обойти не получится**: маршрут либо перепрыгнет вершину по
     >    мостику (если она уже разрыва), либо уйдёт искать другой коридор. При 1 км
     >    проходимо 78 % участка, при 2 км — 85 %.
     > 2. *Тяга к цели не знает про рельеф.* Поле расстояний считается по чистой
     >    ГЕОМЕТРИИ, поэтому перед широким массивом она пересиливает локальное нежелание
     >    лезть вверх. Решения вида «обойду массив слева, потом вернусь на ориентир»
     >    модель не строит — это по-прежнему локальный жадный выбор с горизонтом 2 км.
     >
     > **Что делать дальше** (обдумать и выбрать):
     > * адаптивная полоса — расширять её у препятствий, оставляя узкой на чистых
     >   участках ([ПЛАН_РЕЛЬЕФ.md §13.4](планы/1_ПЛАН_РЕЛЬЕФ_реализован.md));
     > * планировщик по стоимости — второе поле Дейкстры, где цена шага равна
     >   `h / привлекательность`; тогда обход возникает сам, но меняются ВСЕ маршруты
     >   ([РАДИОВИДИМОСТЬ_ЗАДЕЛ.md §6](РАДИОВИДИМОСТЬ_ЗАДЕЛ.md));
     > * простейшее — поднять «уход от ориентира» до 2 км и замерить, часто ли маршруты
     >   вообще упираются в массивы шире полосы.
     >
     > ✅ **ЧЕМ КОНЧИЛОСЬ (см. п. 151 и 155).** Сработал ВТОРОЙ вариант — планировщик по
     > стоимости: тяга к цели считается по цене прохода, и обход возникает сам. Доля пути
     > по возвышенностям 13.0 → 7.4 %. А «простейшее», с которого предлагалось начать,
     > оказалось ВРЕДНЫМ: при уходе 2 км вес под маршрутом падает с 14.37 до 13.31,
     > возвышенностей становится больше (19.0 → 21.3 %), разброс не растёт, и часть
     > маршрутов вообще не строится. Адаптивная полоса не понадобилась.

149. **Маршруты расходятся с весовой картой + БОЛЬШАЯ РАБОТА ПО НАСЕЛЁННЫМ ПУНКТАМ.**

     > 📑 **Что внутри этого пункта** (он длинный, здесь оглавление):
     > * разбор по фото заказчика — пять причин расхождения (ниже, сразу);
     > * **П7 — деревня СТИРАЛА ориентиры:** 1019 ячеек теряли коридор, среди них 54 реки;
     > * **П8 — СКЛЕЙКА пунктов:** цепочка сёл давала «город» 431 км² при 27 % застройки,
     >   и правило «река сквозь НП» не срабатывало НИКОГДА;
     > * **П9 — СОБСТВЕННАЯ ГРАДАЦИЯ пунктов по данным** вместо тега OSM: отделение
     >   районов города, порядок кодов, строгий подъём, три исправленные ошибки подряд;
     > * пересборка кэша `threat_layers.npz` (он хранит уже закодированный тип).
     >
     > Действующие правила по городам — [ОГРАНИЧЕНИЯ.md §3](ОГРАНИЧЕНИЯ.md) (3.4а, 3.6а,
     > 3.10, 3.10а–3.10г), механика — [МЕТОДИЧКА_КАРТА_УГРОЗ.md §13.0а–13.0г](МЕТОДИЧКА_КАРТА_УГРОЗ.md).

     После визуальной проверки готового рельефа заказчик указал на
     расхождение: маршруты идут не там, куда показывает весовая карта. Разбор сделан
     **чтением кода**, числами пока не подтверждён — записано как задача, а не как факт.
     Примеры в `фото пример/` (7 файлов): синие линии у Луганского заповедника, где весов
     почти нет; маршруты идут верхней дорогой вместо Сентянивки, хотя там и вес выше, и
     высота меньше; итерационные маршруты на фото 7 идут полосой по возвышенности при
     включённом режиме «приоритет макс. веса», а яркие коридоры внизу пустуют.

     **Найдено пять причин (все — из кода, не из замеров).**
     * *Тяга к цели чисто геометрическая.* `_dijkstra_dist` строит поле расстояний в
       километрах и не заглядывает в `weight` ни разу. Вес решает только, ГДЕ проложена
       сеть (`passable_mask`), а по какой из её ветвей идти — решают километры.
     * *Приоритет по весу тонет.* В `_walk_field` оценка соседа `exp(prog/tau)·corr·hf`:
       геометрия даёт размах ×44 при `tau = 0.40` и ×10¹³ при `tau = 0.05`, удержание
       курса ×280, а приоритет по весу `corr` — всего ×17 (режим max: 0.10…1.70).
       Иерархия получилась «геометрия ≫ курс ≫ вес». Рельеф и предвидение умножаются
       в тот же `corr` и тонут вместе с ним — включённый рельеф почти не может изменить
       путь, потому что поля тяги он не трогает.
     * *Отсечка отменяется буфером.* В `passable_mask` город вычитается явно
       (`pas &= ~urban_mask()`), а снятые отсечкой горы — нет; идущая строкой выше
       `_dilate(pas, r_slack)` возвращает их в проходимые. Правило «очень высокая гора —
       коридора нет» живёт только в весовой карте, маршрут летит поверх.
     * *Треть «возможных маршрутов» строится в режиме минимального веса.* `plan_routes`
       вызывает выборку жёстко с `mode="mix"`, а `sample_one_route` при `mix` разыгрывает
       режим на КАЖДЫЙ маршрут — значит ~⅓ синих линий целенаправленно уходит с тяжёлых
       коридоров в пустоту. Режим, выбранный в панели, на синие линии не влияет вообще.
     * *Жадный отбор поощряет пустоту.* `_spread_over_area` максимизирует ГОЛОЕ ЧИСЛО
       задетых ячеек, поэтому пустое поле стоит ровно столько же, сколько река.

     **Следствие, которое раньше не проговаривалось:** до итераций датчики ставятся
     ИМЕННО по синим линиям (`_sample_for_sensors` возвращает `self.routes`, пока
     итераций меньше пяти). Значит все дефекты выборки переходят прямо в предварительную
     расстановку датчиков — на фото 1 и 7 часть кругов стоит на пустом поле.

     **Что НЕ является дефектом** (проверено по коду, менять не нужно): розовая заливка
     «места пролёта» одна и та же до и во время итераций и от режима не зависит — это
     чистая достижимость «куда можно долететь и уложиться в запас хода».

     **План поправок (принят, выполняется последовательно с самопроверкой каждого пункта).**
     | № | Поправка | Риск |
     |---|---|---|
     | П1 | Легенда режима «приоритет по высоте» справа: синий — укрытие, красный — возвышенность, чёрный — коридор снят. Чёрного нет и в общей легенде | нулевой |
     | П2 | Отсечка вычитается из проходимости так же, как город | малый |
     | П3 | **Ядро:** тяга к цели по СТОИМОСТИ. Два поля: `gt_geo` (км — бюджет и отбраковка) и `gt_cost` (цена шага = длина × штраф за лёгкий вес × штраф за высоту). Сила 0 = сегодняшнее поведение | средний |
     | П4 | Размах приоритета по весу поднять с ×17 до сопоставимого с удержанием курса | малый |
     | П5 | Синие линии: режим из панели вместо жёсткого `mix`; разброс сохранить веером VIA; жадный отбор — по ВЗВЕШЕННОМУ покрытию | малый |
     | П6 | Обе силы — в `config.py` и в окно «Входные данные», нулевые значения возвращают прежнее поведение | нулевой |

     П3 решает и вопрос огибания из п. 148: сквозь массив станет дорого, и обход возникнет
     сам, без расширения полосы манёвра. Базовый прогон для всех замеров — по требованию
     заказчика: `R = 3 км`, `N = 15` датчиков, разрыв 2 км, уход 1 км.

     **П7 — добавлено по ходу работы: деревня СТИРАЕТ ориентиры (замерено).** Заказчик
     указал на фото 8: у Нижнего и Бобрового есть река, дороги и низина, а вес нулевой.
     Замер подтвердил и объяснил: штраф деревни (−20 в центре, −10 у края) **фиксирован
     и не зависит от содержимого ячейки**, а вес ориентиров невелик — река +10, местная
     дорога +3. Сумма ориентиров не перекрывает штраф, и вес уходит в минус, то есть в
     полный запрет. Потери: **1019 ячеек с ориентирами** теряют коридор ИСКЛЮЧИТЕЛЬНО
     из-за штрафа (без него средний вес там 4.9) — 795 местных дорог, 89 ручьёв, **54
     реки**, 12 магистралей. Пример из замера: ячейка с рекой и дорогой имела 15.3,
     штраф −17.6, стало −2.3. Это прямо противоречит замыслу, записанному в самом коде:
     «деревня — пролёт РАЗРЕШЁН, но менее вероятен; маршрут прижимается к окраине, но
     при необходимости проходит». Фактически деревня работала как запрет.
     **Решение** — тот же приём, что уже принят для рельефа (п. 148): штраф ослабляет
     вес, но не стирает его. Пол `min(исходный вес, THREAT_VILLAGE_FLOOR)`: градиент
     «ближе к центру — менее вероятно» сохраняется, коридор остаётся проходимым.
     ✅ **СДЕЛАНО и проверено.** Спасено 1002 ячейки (с включённым рельефом 1000 из 1000,
     оставшиеся 19 сняты отсечкой «высокая гора» — законно, это уже не деревня). Все 54
     ячейки реки вернулись в коридор. Коридоров **9 944 → 10 950**, компонент связности
     **79 → 44**, крупнейшая область **8 372 → 9 405**: деревни больше не разрезают
     коридоры поперёк, река не обрывается на въезде в село. Градиент цел — в центре села
     вес 5.70 против 10.17 у края. `THREAT_VILLAGE_FLOOR = 0` возвращает прежнюю карту
     побитово. Слой `layers["village"]` теперь хранит ФАКТИЧЕСКОЕ изменение веса, а не
     сырой штраф (иначе после пола он врал бы: −17 там, где вес упал на 9).
     Заодно проверены 260 ячеек реки, погашенных ГОРОДОМ: все принадлежат трём крупным
     пятнам (431, 119, 62 км²), под порог `river_corridor_max_km2 = 20 км²` не попал ни
     один малый пункт — правило работает как задумано.
     Три «провала» самопроверки оказались дефектами самого теста: пол применяется ДО
     рельефа, поэтому сравнивать «не выше пола» на карте с рельефом бессмысленно (пол
     1.0 в низине законно становится 1.40 после множителя `k_max`). Проверка пола
     перенесена на карту без рельефа.

     **П8 — СКЛЕЙКА ПУНКТОВ: главная причина нулевого веса у реки. ✅ СДЕЛАНО.**
     Пол (П7) вернул реку под деревнями, но заказчик указал: у Нижнего, Бобрового и
     Комишувахи (фото 8 и 9) вес по-прежнему нулевой. Разбор контрольной точки
     `48.694607, 38.478366` объяснил: ячейка содержит **реку и дорогу**, штрафа деревни
     на ней нет вовсе, зато она числится ГОРОДОМ — попала в пятно площадью **431 км²**
     при доле реальной застройки внутри всего **27 %**. Это не город, а цепочка сёл
     вдоль Северского Донца, склеенная в одну область.
     **Три источника склейки** (каждый расширяет маску): «обжитая зона» = застройка + 1 км;
     контуры НП из OSM накладываются в ОДНУ общую маску; буфер облёта + 0.4 км.
     Правило 3.6 («река остаётся, пока пункт < 20 км²») меряло размер через
     `_label8(city)` — то есть по склейке — и вдоль рек не срабатывало НИКОГДА, потому
     что именно вдоль рек сёла и стоят цепочкой.
     **Исправлено:** размер меряется по ИСХОДНОМУ пятну сырой застройки
     (`own_place_area_km2`), без расширений — цепочка распадается обратно на посёлки.
     Приём не новый: тем же способом уже классифицировались пункты, и комментарий в
     коде прямо предупреждал, что склейка до разметки даёт «псевдогород» (17.9 %
     участка вместо 4.5 %). Защиту поставили в одном месте и забыли в другом.
     Из пятен, дотянувшихся до ячейки, берётся САМОЕ КРУПНОЕ — иначе хутор на краю
     города открыл бы реку в самом городе.
     **Результат:** контрольная точка 0 → **30.46**; река без веса **260 → 109** ячеек,
     все оставшиеся — в настоящих крупных пунктах или у самого их края (проверено
     поимённо). Коридоров 10 950 → **11 101**, компонент 44 → **42**, крупнейшая
     9 405 → **9 583**.
     **Заодно по просьбе заказчика:** штраф деревни смягчён с −20/−10 до **−10/−5**
     (окраина села не должна терять вес наравне с центром) и введена **СОБСТВЕННАЯ
     ГРАДАЦИЯ пунктов по данным участка** вместо доверия тегу OSM: `settlement_stats`
     считает распределение пятен (411 пятен, минимум 0.25, медиана 0.50, максимум
     42.75 км²), порог «не штрафует» = минимум × 1.2 = 0.30 км² — фактически хутора,
     размеченные как village. Освободилось 398 ячеек. Пороги берутся ОТ распределения,
     чтобы переноситься на другую местность, где масштаб застройки иной. Второй порог
     («крупное пятно = уже посёлок, штрафа нет») заложен как
     `THREAT_VILLAGE_TOWNLIKE_MED`, но **выключен**: он снимает штраф с самых крупных
     сёл, это надо сперва сверить глазом по карте.
     **Отложено как задел:** мерить не площадь пятна, а ЛОКАЛЬНУЮ ПЛОТНОСТЬ застройки
     вокруг ячейки реки. Устойчивее к способу склейки и работает внутри настоящего
     города (река по окраине Луганска — коридор, через центр — нет), но оставит реку и
     в середине крупного села. Описано в
     [МЕТОДИЧКА_КАРТА_УГРОЗ.md §13.0б](МЕТОДИЧКА_КАРТА_УГРОЗ.md).
     Из трёх «провалов» самопроверки два снова оказались дефектами теста: сравнивались
     РАЗНЫЕ множества ячеек (в центре села может стоять перекрёсток с весом 30, у края
     одинокая дорога с весом 3 — средние говорят о застройке, а не о градиенте). После
     сравнения при равном исходном весе градиент подтвердился: центр 2.74 против края 9.14.

     **П9 — СОБСТВЕННАЯ ГРАДАЦИЯ НАСЕЛЁННЫХ ПУНКТОВ. ✅ СДЕЛАНО и проверено.**
     *ПОЧЕМУ РЕШЕНО ДЕЛАТЬ ГРАДАЦИЮ.* Причина у всех разобранных случаев одна: **тип
     пункта задаёт тег `place`, а границу рисует контур**, и оба источника врут.
     1. *Тег ненадёжен.* Одинаковые по размеру пятна размечены то `village`, то `town`;
        хутор из трёх домов тоже `village`. Разметка зависит от того, кто вносил объект
        в OSM, а не от того, что на местности.
     2. *Граница раздувается.* Контур засчитывается ЦЕЛИКОМ при доле застройки всего
        15 % (`PLACE_BUILT_FRAC`) — вместе с 85 % полей; сверху обжитая зона (+1 км) и
        буфер облёта (+0.4 км), итого до **1.4 км сверх реальных домов**.
     3. *Вместе получается ложный город.* Замер: пятно **431 км² при 27 % застройки** —
        порог «города» формально пройден, пролёт заперт на площади, три четверти которой
        поля. Именно там и пропадал вес у реки (фото 8 и 9).
     4. *Точечные правки кончаются новым случаем.* Одну причину чинили трижды — пол
        штрафа деревни (П7), отсечение хуторов, размер пункта в обход склейки (П8), — и
        после каждой правки заказчик находил следующее место, где вес пропал не по делу.
        Признак того, что чинить надо основание, а не очередное следствие.
     5. *Фиксированные пороги непереносимы.* `THREAT_TOWN_CITY_KM2 = 4` и
        `THREAT_URBAN_MIN_AREA_KM2 = 8` подобраны под этот участок; на степной, горной
        или промышленной местности их придётся подбирать заново вручную. Пороги,
        вычисленные от самих данных, переносятся без правок.

     Отсюда решение: считать тип по измеримым признакам самого пункта, а тег оставить
     подсказкой и предметом сверки.

     *Замысел (согласован с заказчиком).*
     * **Две независимые величины вместо одной.** `контур пункта` — по сырой застройке,
       без буфера и расширений: по нему считается тип, и склейка на классификацию не
       влияет ПО ПОСТРОЕНИЮ. `зона с маской` — контур плюс обжитая зона плюс буфер
       облёта: она и дальше задаёт веса и запрет пролёта. Разводка убирает склейку
       оттуда, где она вредна, и оставляет там, где она по делу.
     * **Классификация по ПАРЕ признаков: масштаб × компактность.** Масштаб — площадь
       застройки пункта, км². Компактность — площадь застройки, делённая на площадь
       ЗАМКНУТОГО контура (морфологическое замыкание закрывает парки и промзоны внутри
       города, но не захватывает поля снаружи). Одной плотности мало: у хутора из трёх
       домов она под 100 %, у растянутого вдоль трассы города — 30 %.
     * **Пороги считаются по РАБОЧЕЙ ОБЛАСТИ.** По каждой группе тега (city / town /
       village) берутся минимум, среднее и максимум обоих признаков. На другом участке
       пороги получатся другими — в этом и смысл: в промышленном районе «город» по
       плотности один, в степном совсем иной, а тег и там и там одинаков.
     * **Перевод между классами — допуск ±15 %** (вверх и вниз) и согласие ОБОИХ
       признаков: по одному масштабу хутор с плотной застройкой ушёл бы в города.
     * **Минимальная выборка — 5 пунктов** (правило заказчика): если в классе меньше
       пяти пунктов, статистика по нему ничего не значит, и **в этот класс не
       переводится ничего** — остаётся исходный тег.
     * **Один проход, без итераций:** посчитали статистику по тегам → зафиксировали
       пороги → перевели → остановились. Пересчёт статистики после переводов сдвинул бы
       границы классов, и часть пунктов замигрировала бы обратно.
     * **Исходный тег сохраняется** рядом с вычисленным, расхождения пишутся в отчёт —
       иначе не увидеть, если градация поедет или OSM врёт систематически.

     *Что П9 решит, а что нет (заказчик предупреждён).* Решит всё, что про весовую карту
     у населённых пунктов: река и дороги в цепочках сёл, завышенные запреты вокруг рыхлых
     контуров, переносимость на другой участок. **Не решит** расхождения с фото 3–7 —
     маршрут верхней дорогой вместо Сентянивки, синие линии у заповедника, итерации по
     возвышенности при режиме «максимальный вес»: там причина в том, что тяга к цели
     считается по километрам (см. П3/П4/П5 выше). Градация чинит КАРТУ, П3–П5 чинят то,
     КАК по ней ходят. Порядок именно такой: чинить маршруты по кривой карте бессмысленно.

     *ЧТО ВСКРЫЛОСЬ ПРИ РЕАЛИЗАЦИИ — градация сперва работала НАОБОРОТ.* Первая рабочая
     версия дала **133 подъёма против 23 понижений**: городская зона выросла с 3441 до
     3542 ячеек, коридоров стало меньше (11 101 → 11 018). То есть механизм, задуманный
     чтобы убирать ложные города, начал их создавать. Две причины, обе исправлены.
     1. *Ошибку разметки нельзя пускать в границу класса.* В классе `city` оказался пункт
        **0.50 км²** — хутор с ошибочным тегом. Он один опустил нижнюю границу класса до
        уровня деревни, и «дотянуться до минимума city» смогла рядовая деревня. Введён
        отсев (`THREAT_PLACE_GRADE_OUTLIER = 3.5`): перед расчётом границ из выборки
        убираются объекты, слишком далёкие от середины своего класса. Мера — modified
        z-score на медиане и MAD; обе величины сами не сдвигаются от выбросов, поэтому
        **порог безразмерный и работает на любом участке** — привязки к километрам
        конкретной области нет, подгонки нет. Отсеяно 22 пункта из 208.
     2. *Классы перекрываются — решает идея заказчика.* Даже после чистки минимум
        верхнего класса лежит ниже максимума нижнего. Заказчик предложил сверять с
        максимумом пониженного тега: «если town максимум 23, то всё что ниже не может
        быть city». Реализовано как `THREAT_PLACE_GRADE_STRICT_UP`: подъём требует
        превзойти **максимум своего класса**, а не минимум целевого. Пороги на участке:
        `village → town` 1.75 км² вместо 0.25, `town → city` **6.25 км² вместо 0.50**.
        Асимметрия намеренная — вверх пройти трудно, вниз легко: убираем мы ЛОЖНЫЕ города.

     *Вклад каждого шага (замер):*
     | вариант | вверх | вниз | город, ячеек | коридоров |
     |---|---|---|---|---|
     | без градации | — | — | 3441 | 11 101 |
     | голые мин/макс | 133 | 23 | 3542 | 11 018 |
     | + отсев ошибок тега | 152 | 21 | 3599 | 10 974 |
     | **+ строгий подъём** | **12** | **49** | **3417** | **11 120** |

     Отсев сам по себе картину не спасает (152 подъёма — даже хуже): решает именно
     сверка с максимумом нижнего класса.

     *ОТСЕВ ОКАЗАЛСЯ ЛИШНИМ — выключен.* Заказчик предложил проверить свой вариант
     чистки: каскадом снизу вверх, «город не может быть меньше самого крупного посёлка».
     Реализованы оба способа (`THREAT_PLACE_GRADE_MODE`: mad / cascade / both / none) и
     сравнены. Замер:
     | режим | вверх | вниз | город | коридоров |
     |---|---|---|---|---|
     | без градации | — | — | 3441 | 11 101 |
     | **none — без отсева** | **0** | **72** | **3395** | **11 140** |
     | mad | 12 | 49 | 3417 | 11 120 |
     | cascade | 0 | 72 | 3395 | 11 140 |
     | both | 12 | 49 | 3417 | 11 120 |

     **Лучший вариант — не чистить вовсе.** Отсев работает ПРОТИВ строгого подъёма: порог
     берётся как максимум своего класса, а отсев выбрасывает крупные пункты как «выбросы»
     и сбивает этот максимум — планка `town → city` падала с 23.50 до 6.25 км², и подъём
     становился лёгким.
     **Каскад совпал с «без отсева» не случайно:** он отбрасывает МЕЛКИЕ пункты, то есть
     поднимает МИНИМУМ класса, а порог подъёма использует МАКСИМУМ класса снизу — разные
     концы распределения (town сократился с 57 пунктов до 29, а максимум как был 23.50,
     так и остался). Мысль заказчика «город не меньше крупнейшего посёлка» **уже целиком
     воплощена в правиле строгого подъёма**, только применена в нужный момент: не при
     чистке выборки, а при решении о переводе.
     Режимы оставлены в коде выключенными — на другом участке распределение может быть
     иным. Отмечен известный дефект каскада на будущее: признаки объединены через ИЛИ, а
     компактность у большинства пунктов ровно 1.00, поэтому чистка по площади фактически
     не срабатывает; чинить не стали за ненадобностью.

     *ЕЩЁ ТРИ ОШИБКИ, ВСКРЫТЫЕ СПИСКОМ ДЛЯ СВЕРКИ.* Заказчик попросил выгрузить список
     переклассифицированных пунктов — и он сразу показал то, чего не видели числа.
     1. *Разжаловались НАСТОЯЩИЕ города — 16 штук, включая Северодонецк* (17.25 км²,
        компактность 0.81). Правило понижения сравнивалось с ЧУЖИМ классом: максимум
        town на участке 23.50 км², поэтому под нож шли все города мельче 27.6 км².
        Зеркальная ошибка той, что была при подъёме. Правило сделано СИММЕТРИЧНЫМ: вверх
        превзойти максимум своего класса, вниз — упасть ниже его минимума.
     2. *Класс `city` был засорён районами.* На вопрос заказчика «сколько всего city по
        векторным данным» пришлось проверить первоисточник: в `.osm.pbf` **8 меток city,
        54 suburb и 8 borough**, но все три кодировались одинаково — 62 района шли в
        класс «город» наравне с 8 городами. Класс засорён на 89 %, его минимум падал до
        0.50 км² (два квартала), и НАСТОЯЩИЕ города оказывались выбросами в собственном
        классе. Районам дан свой код; для запрета пролёта это по-прежнему город, но в
        статистику градации они не входят.
        **Порядок кодов оказался критичен.** Сперва район получил код ВЫШЕ города — и
        стало хуже: тип пятна берётся как МАКСИМУМ кодов его меток, поэтому пятно
        Луганска с размеченными районами получало код района, и из восьми городов в
        класс попадали два. Коды переставлены (`city` 4, `suburb` 3), а соседний класс
        берётся теперь по позиции в списке `PLACE_LADDER = (1, 2, 4)`, а не арифметикой
        над кодом — иначе смена кодировки молча ломает переходы.
     3. *Предохранитель не защищал СВОЙ класс.* Правило «меньше 5 пунктов» проверяло
        только класс, В который переводят. После отделения районов в `city` осталось
        4 пункта — и оба правила стали строить границу на самих же городах, снова их
        разжаловав. Проверка распространена и на свой класс.

     *ВАЖНО ПРО КЭШ.* `PLACE_CODES` применяется только при чтении `.osm.pbf`, а
     `geo_cache/threat_layers.npz` хранит УЖЕ ЗАКОДИРОВАННЫЙ тип. После правки кодов
     карта из кэша молча теряла класс города (0 пунктов в статистике вместо 19). Кэш
     пересобран (`tools/build_threat_grid.py`, ~20 с), прежний сохранён как
     `threat_layers_2026-08-06_before_place_codes.npz`. Правило записано в
     ОГРАНИЧЕНИЯ.md 3.10г.

     *Итог.* Коридоров **11 101**, компонент **42**, городская зона **3441** ячейка —
     то же, что без градации. На этом участке настоящих городов **4**, это меньше порога
     в пять, поэтому класс `city` в переводах не участвует и срабатывает ОДИН перевод
     `town → village`. Это не сбой механизма, а честный ответ на малую выборку: четыре
     объекта — не статистика. Механизм заработает там, где городов больше пяти.
     Разжалованных городов — ноль (было 16). Список для сверки:
     `теория/ГРАДАЦИЯ_СПИСОК_ПРОВЕРКИ.md`.
     Контрольный прогон §6 держится: 250/250 маршрутов, 150/150 итераций, 15/15 датчиков,
     нули на месте, засечка 100 %, средняя кратность 6.7.
     Скорость: градация добавляет **0.04 с** при полной сборке карты 2.2 с (2 %) —
     основное время уходит на загрузку слоёв, оптимизировать нечего.

150. **Движение маршрутов: П5 — «возможные маршруты» слушаются панели. ✅ СДЕЛАНО.**
     После доводки весовой карты перешли к тому, КАК по ней строятся пути. Первое —
     синие линии, они же основа для датчиков до итераций.
     **Что было не так.** `plan_routes` вызывал выборку жёстко с `mode="mix"`, а «mix»
     разыгрывает режим КАЖДОМУ маршруту из трёх — то есть примерно **треть синих линий
     строилась в режиме «приоритет МИНИМАЛЬНОГО веса»**, целенаправленно уходя с тяжёлых
     коридоров в пустоту. Выбор в панели итераций на эту картину не влиял вовсе. Плюс
     жадный отбор `_spread_over_area` максимизировал ГОЛОЕ ЧИСЛО задетых ячеек, где
     пустое поле стоило ровно столько же, сколько река.
     **Сделано:** приоритет по весу берётся из панели (`THREAT_ROUTE_MODE_SRC`);
     разброс и профиль расхода остались веером — линии обязаны лечь по области широко;
     жадный отбор считает ЦЕННОСТЬ `1 + 3.0 · вес(0…1)` вместо числа ячеек
     (`THREAT_ROUTE_SPREAD_W`). Оба механизма выключаемы.
     **Замер** (R = 3 км, N = 15, разрыв 2 км, уход 1 км, рельеф включён):
     | режим | средний вес под линией | точек по пустым ячейкам |
     |---|---|---|
     | «mix», как было | 10.24 | 40.9 % |
     | **«макс. вес» из панели** | **11.74** | **36.5 %** |
     | «мин. вес» из панели | 9.07 | 46.1 % |

     Панель теперь реально управляет картиной. Взвешенный отбор отдельно дал 11.43 →
     11.74 по весу, а покрытие области при этом не упало, а выросло (46.4 → 48.5 %):
     ценность ведёт отбор к длинным коридорам вместо коротких перебежек по пустоши.
     **Осталось 36.5 % точек по ячейкам без веса** — это буфер ухода и мостики, и
     лечится оно не здесь: тяга к цели по-прежнему считается по километрам (см. П3).

151. **П3 — ТЯГА К ЦЕЛИ ПО СТОИМОСТИ. ✅ СДЕЛАНО. Ядро расхождения устранено.**
     *Причина, найденная замером.* Поле, по которому маршрут спускается к цели, считалось
     ЧИСТО ГЕОМЕТРИЧЕСКИ — `_dijkstra_dist` не заглядывал в вес ни разу. Вес входил только
     в выбор соседней клетки и притом слабейшим из трёх множителей: приближение к цели
     `exp(prog/tau)` даёт размах ×44…×10¹³, удержание курса `hf` ×280, а вес `corr`
     всего ×17. Иерархия «геометрия ≫ курс ≫ вес» — маршрут шёл кратчайшим путём, а вес
     лишь слегка подталкивал его на равных развилках. Сколько ни улучшай карту, по ней
     ехали «навигатором по километражу»: отсюда линии через пустошь мимо тяжёлого
     коридора и полосы по возвышенности при включённом «приоритете максимального веса».
     *Решение.* Полей расстояний теперь ДВА, потому что вопросов два: `gt_geo` в честных
     километрах — «хватит ли хода» (`L_max`, запас, отбраковка), и `gt` в стоимости —
     «куда выгоднее идти» (по нему спуск). Цена шага = длина × `1 + POWER · (1 −
     привлекательность)`, привлекательность = нормированный вес × множитель рельефа.
     Поле остаётся в КМ-ЭКВИВАЛЕНТАХ, поэтому «температура» `tau` и все пороги сохраняют
     смысл. Цена симметрична (среднее множителя двух ячеек), иначе путь из A в B стоил бы
     иначе, чем из B в A.
     *Замер* (R = 3 км, N = 15, разрыв 2 км, уход 1 км, рельеф включён, 150 маршрутов):
     | | было | стало (POWER = 1) |
     |---|---|---|
     | средний вес под маршрутом | 11.60 | **20.60** |
     | точек по ячейкам без веса | 35.5 % | **13.4 %** |
     | множитель рельефа под маршрутом | 1.075 | **1.225** (ушли в укрытия) |
     | длина маршрута | 102.2 км | 101.8 км |
     | превышений запаса хода | 0 | **0** |

     Длина НЕ выросла — маршрут не делает крюков, он выбирает другой путь той же длины.
     **Это же закрывает вопрос огибания массивов из п. 148:** сквозь массив стало ДОРОГО,
     и обход возникает сам, без расширения полосы манёвра.
     *Почему сила 1.0.* Плата за приоритет — сходимость линий на лучшие коридоры: при
     1.0 покрытие области линиями 28.2 %, при 2.0 — 19.7 %, при 3.0 — 15.0 %, а вес при
     этом растёт всего с 20.18 до 23.26. Единица берёт почти весь выигрыш, теряя вдвое
     меньше разброса. **Закрашенная область от стоимости не зависит вовсе** (проверено
     побитово: 13 987 ячеек при любой силе) — полную картину «куда можно долететь»
     по-прежнему даёт она, а линии показывают вероятные пути.
     *Цена.* Сборка контекста выборки 0.54 → 1.60 с (второе поле Дейкстры плюс поля от
     каждой VIA-точки), полный контрольный прогон 14 с.
     *Контроль.* Инварианты §6 держатся: 250/250 маршрутов, 150/150 итераций, 15/15
     датчиков, 0 выходов за область, 0 превышений хода, 0 датчиков на воде, засечка
     100 %, средняя кратность выросла 6.7 → **8.1**. `THREAT_COST_POWER = 0` возвращает
     прежнее поведение в точности.
     *Попутно уточнено правило.* Контрольный прогон ругался на «датчик в запретном
     городе», но такого правила нет ни в коде, ни в ОГРАНИЧЕНИЯ.md — я сам его выдумал
     в тесте. Заказчик подтвердил: датчику запрещена ТОЛЬКО вода, город закрыт для
     ПРОЛЁТА БПЛА, а не для размещения наземного оборудования. Записано правилом 1.1а.

     *П4 — размах приоритета по весу в выборе клетки (`THREAT_WEIGHT_GAIN`).* Стоимость
     задаёт стратегию, это — тактику: какого из соседей взять. Замер показал главное:
     **само по себе усиление бесполезно** — без стоимости вес под маршрутом 11.60 при
     ×17 и 11.64 при ×101, то есть приоритет тонет в геометрии (это и есть подтверждение
     всего диагноза). ПОВЕРХ стоимости он работает: ×41 даёт +9.4 % к весу и −3 п.п.
     точек по пустым ячейкам. При ×101 вес уже падает, а доходимость рушится с 55 до
     40 %. Принято 4.0 (×41); 1.6 возвращает прежнее поведение.

     *ПРОВЕРКА НА РЕАЛЬНОЙ ТРАССЕ* (вход Северодонецк → цель Луганск). Заказчик спросил,
     почему маршруты тянутся к Сентянівке, а не идут рекой. Разбор оптимальных путей:
     | путь | длина | средний вес | ячеек по реке |
     |---|---|---|---|
     | по километрам (как было) | 79.4 км | 13.41 | 13 из 128 |
     | **по стоимости (сейчас)** | 88.4 км | **26.83** | **102 из 146** |

     Маршрут теперь ведёт руслом Донца через Славяносербск (там вес 70.7: река + дорога
     + железная дорога + ЛЭП) и через Сентянівку больше не идёт. Крюк ради реки — 9 км
     при удвоенном весе, в запас хода укладывается. То, на что заказчик указывал по
     фото 5–6, устранено именно П3.

152. **Кнопка «Применить» пересчитывает ВСЁ, а не только показанное. ✅ СДЕЛАНО.**
     Заказчик: «поменял запас хода на 150 км — розовая область увеличилась, а датчики
     остались; работает только после сброса». Две причины, обе в контроллере.
     1. *Датчики не обновлялись после пересчёта маршрутов.* При завершении ИТЕРАЦИЙ
        датчики пересчитывались (`_maybe_refresh_sensors`), а при завершении задачи
        `routes` — нет. Но до итераций датчики стоят именно по «возможным маршрутам»
        (`_sample_for_sensors`), поэтому после их пересчёта обязаны переставляться.
     2. *Пересчёт зависел от чекбоксов.* `on_input_apply` смотрел на `show_routes` /
        `show_iter`: при выключенных «маршрутах» правка `L_max` меняла только надпись,
        а область, линии и датчики оставались от прежних значений.
     Теперь при «Применить» всегда пересчитываются область залёта, маршруты и датчики.
     **Проверено на живом контроллере** (Qt offscreen, эмуляция нажатия): при смене
     запаса 107 → 150 км область 13 987 → 21 895 ячеек, средняя длина маршрута 101 →
     123 км (максимум 149.8 при запасе 150), **датчики сдвинулись 12 из 15**. Область
     пересчитывается и при выключенных линиях. Режим панели управляет синими линиями:
     вес 18.91 при «максимальном» против 12.55 при «минимальном».
     Заодно подтверждено, что линии РЕАГИРУЮТ на запас хода (сомнение заказчика):
     задето ячеек 3888 → 6216 → 7922 при L_max 107 → 150 → 180, доступных VIA-точек
     8 → 14 → 16 из 16.

153. **Рельеф в цене прохода — ОТДЕЛЬНЫМ множителем. ✅ СДЕЛАНО.**
     Заказчик усомнился: «маршрут по руслу — это исправление или подгонка?» — и указал,
     что раньше путь тоже шёл руслом, просто перепрыгивал возвышенность. Разбор показал:
     подгонки нет (в формуле ни одной константы под местность), но **дефект есть, и
     заказчик его нащупал**.
     *Дефект.* Привлекательность считалась как ПРОИЗВЕДЕНИЕ `вес × рельеф`, обрезанное
     единицей. Обрезка съедала ВСЁ поощрение за низину: река в пойме (вес 1.0, рельеф
     1.4) давала 1.4 → обрезалось до 1.0 — ровно столько же, сколько у той же реки на
     голом плато. Замер подтвердил: **цена в низине и на гребне выходила одинаковой,
     1.000 против 1.000.** Рельеф работал только как ослабление веса, хотя это
     НЕЗАВИСИМАЯ физика: вес — «где есть ориентиры», рельеф — «где меня видно».
     *Решение.* Два независимых сомножителя:
     `цена = длина × (1 + Pw·(1−вес)) × (1 + Pr·(1−рельеф))`. Рельеф нормируется по
     своему диапазону `[K_MIN, K_MAX]` → 0 (гребень) … 1 (низина), поэтому сила не
     зависит от настройки порогов высот. После правки: низина **1.016**, гребень
     **1.976** — почти вдвое дороже.
     *Замер* (сила рельефа 1.0, вес 1.0):
     | | только вес | вес + рельеф |
     |---|---|---|
     | шагов по возвышенности | 13.0 % | **7.4 %** |
     | средняя высота под маршрутом | 77.8 м | **70.3 м** |
     | множитель рельефа под маршрутом | 1.203 | **1.275** |
     | средний вес под маршрутом | 20.60 | **22.42** (вырос) |
     | длина | 101.4 км | 101.7 км |

     Вес не просел, а вырос — низины на этом участке и есть места с ориентирами (русло
     Донца). Силы независимы: `Pw=1, Pr=0` поднимает только вес (11.59 → 20.60),
     `Pw=0, Pr=1` — только рельеф (k 1.071 → 1.252).
     **Попутно закрыт «бесплатный перелёт»:** мостики через разрыв (`max_gap_km`)
     сшивают коридоры поверх возвышенности, и раньше подъём на гребень в цену не входил
     вовсе. Теперь ячейки мостика на гребне дорогие — маршрут выбирает либо обход, либо
     перелёт в низком месте.
     Сила 2.0 даёт ещё меньше возвышенностей (4.6 %), но роняет доходимость с 59 до
     46 % — принята 1.0. Инварианты §6 держатся, засечка 100 %, кратность 8.1.
     *Перед правкой сохранён архив* `uav_sensor_mvc_АРХИВ_2026-08-07_стоимость-работает.zip`.

154. **⚠️ ВЫБОРКА ВЫРОДИЛАСЬ — маршруты слились в один путь. В РАБОТЕ.**
     *Что увидел заказчик* (фото 1 в `фото пример/`): все 250 линий идут ОДНОЙ полосой
     Северодонецк → Тошківка → Сентянівка → Зимогір'я → Луганск. Вокруг на тепловой
     карте масса коридоров, по которым не летит никто; датчики выстроились цепочкой
     вдоль единственной трассы. Дословно: «выборка совсем ушла, все маршруты однотипные,
     и много где веса есть, а маршруты даже не проходят — раньше было лучше».

     *ПРИЧИНА — ошибка масштаба, допущенная при П3.* Выбор соседа в блуждании идёт по
     `exp(prog / tau)`, где `prog` — насколько сосед ближе к цели ПО ЗНАЧЕНИЮ ПОЛЯ.
     | | до П3 | после П3 |
     |---|---|---|
     | поле | километры | стоимость |
     | разность между соседями | ≤ 0.7 | до 0.7 · (1+Pw) · (1+Pr) = **2.8** |
     | `tau` | 0.05…0.40 | **та же** |

     Масштаб поля вырос вчетверо, а «температура» осталась прежней — это эквивалентно
     ЧЕТЫРЁХКРАТНОМУ снижению стохастики. При `tau = 0.05` показатель экспоненты доходит
     до 2.8/0.05 = 56, выбор становится детерминированным, и все маршруты валятся в один
     оптимум. Дело не в том, что стоимость «слишком сильная», а в том, что температура
     осталась в километрах, когда поле стало в стоимости.

     *ПРЕДЛОЖЕНИЕ ЗАКАЗЧИКА:* скрытый список с двумя моделями — прежняя (просто поднять
     штраф за возвышенность и приоритет низины прямо в весовой карте) и нынешняя
     стоимостная. Отложено: два режима заставляют ВЫБИРАТЬ одно из двух, и каждый
     показывает лишь половину правды, тогда как выборка должна показывать разные повадки
     СРАЗУ, в одной картине. Плюс переключатель режимов уже есть — панель «приоритет
     веса». Если после правок баланс не устроит, вернёмся к этому варианту.

     *ЦЕЛЬ, СФОРМУЛИРОВАННАЯ ЗАКАЗЧИКОМ:* «в приоритете маршруты с учётом рельефа, то
     есть по низине, но не зацикливаться» — основная масса линий должна идти по низинам
     и тяжёлым коридорам, но выборка обязана покрывать и остальные возможные пути.

     *ЧТО ДЕЛАЕМ (по порядку):*
     1. **Привести `tau` к масштабу поля** — делить `prog` на средний множитель цены,
        чтобы «температура» снова означала то же, что означала. Это исправление ошибки,
        а не новая механика; есть шанс, что разброс вернётся сам.
     2. Если вернулся не полностью — **смешивать поля на каждый маршрут**:
        `поле = (1−a)·километры + a·стоимость`, где `a` разыгрывается НА КАЖДЫЙ маршрут.
        Даёт непрерывный спектр повадок: часть аппаратов жмётся к коридорам, часть летит
        напрямик, большинство — между. Стоит дёшево, оба поля уже посчитаны.
     3. Замерять ОБЕ метрики вместе: покрытие области линиями (цель — вернуть к прежним
        ~46 %) и средний вес под линиями (цель — удержать заметно выше исходных 11.8).

     **✅ ЧЕМ КОНЧИЛОСЬ.** Обе правки сделаны, зацикливание снято.
     *Первая — масштаб `tau`* (исправление моей ошибки). Пороги, заданные в километрах
     («температура», допуск «назад», порог «дошли»), приводятся к единицам поля через
     `cost_scale` — средний множитель цены по проходимым ячейкам. Помогло мало:
     локальная стохастика в принципе не может пересилить глобальную разницу стоимостей.
     *Вторая — НЕСКОЛЬКО ПОЛЕЙ «жадности»* (`THREAT_COST_MIX_STEPS = (0, 0.35, 0.7, 1)`).
     Каждому маршруту достаётся своё поле: `a≈1` жмётся к коридорам и низинам, `a≈0`
     летит напрямик. Выбор смещается к стоимости показателем `THREAT_COST_MIX_BIAS = 0.8`.
     **Поля именно ПРЕДРАССЧИТЫВАЮТСЯ**, каждое своей Дейкстрой по множителю
     `1 + a·(mul − 1)`. Первая попытка смешивать готовые поля линейно провалилась:
     смесь двух полей расстояний полем расстояний НЕ является — там, где по километрам
     выгодно влево, а по стоимости вправо, возникает локальный минимум-ловушка, спуск
     застревает, и набиралось **128 итераций из 150**. С предрассчитанными полями —
     150 из 150.
     *Замер после правок* (250 линий, режим «приоритет макс. веса»):
     | | вес | по возвышенностям | покрытие области |
     |---|---|---|---|
     | чистая геометрия | 11.80 | 27.7 % | 47.0 % |
     | стоимость без смешивания | 20.11 | 9.6 % | **26.3 %** ← зацикливание |
     | **со смешиванием, bias 0.8** | **14.89** | **19.2 %** | **43.1 %** |

     Разброс вернулся на 92 % от прежнего, вес держится на 26 % выше геометрии, доля
     пути по возвышенностям меньше на треть. Засечка в трёх прогонах подряд — **100 %**
     (единичный случай 99.3 % оказался стохастикой).

     **ГЛАВНОЕ, ЧТО ВЫЯСНИЛОСЬ ПРО «ПЕРЕПРЫГИВАНИЯ».** Заказчик указал, что маршрут
     идёт низом, но перепрыгивает. Разбор, ГДЕ именно идёт маршрут:
     | | точек | доля |
     |---|---|---|
     | прямо по коридору (вес > 0) | 12 387 | **75.8 %** |
     | рядом с коридором (≤ 1 км) — виляние | 3 948 | 24.2 % |
     | поодаль (1–2 км) | 0 | **0.0 %** |
     | далеко от коридоров (> 2 км) | 0 | **0.0 %** |

     **Настоящих перелётов НОЛЬ.** Те 26 % «пустых» ячеек — виляние вдоль узкого
     коридора: река шириной в одну ячейку (500 м), аппарат физически не идёт по осевой
     линии. На карте это выглядит как перепрыгивание, но маршрут держится русла.
     Убирать нельзя — иначе путь станет неестественно точным, как по линейке.
     *Усиление стоимости упёрлось:* при `Pw = 3` доля пути по реке ПАДАЕТ с 23.4 до
     16.0 %, потому что растёт разрыв между «жадными» и «геометрическими» полями.
     *Единственный рычаг, который ещё не упёрся* — усиление рельефа в САМОЙ весовой
     карте (предложение заказчика): диапазон `k` 0.25…1.40 → 0.10…2.50 даёт по реке
     23.9 → 25.4 %, по возвышенностям 20.2 → 17.9 %. Не применено, ждёт решения.

     **ДВОЙНОЙ УЧЁТ РЕЛЬЕФА — проверен, оставлен.** Заказчик спросил, влияет ли рельеф
     на весовую карту или учитывается только при генерации. Ответ: ДВАЖДЫ — сперва
     умножает вес при сборке карты, потом входит отдельным сомножителем цены. Замер трёх
     вариантов (150 маршрутов):
     | вариант | вес | по возвышенностям | высота | по реке | покрытие |
     |---|---|---|---|---|---|
     | **в весе И в цене** | **14.36** | **19.5 %** | **85.2 м** | **23.3 %** | 34.0 % |
     | только в весе | 14.27 | 20.9 % | 88.4 м | 22.1 % | 33.1 % |
     | только в цене | 11.35 | 21.0 % | — | 19.7 % | 34.6 % |

     Двойной учёт выигрывает по всем показателям рельефа, ничего не теряя. Причина, по
     которой оба нужны: в весе рельеф говорит «этот ОРИЕНТИР хуже», в цене — «САМ ПРОХОД
     дороже», причём второе действует и на ПУСТЫЕ ячейки, где веса нет вовсе; плюс
     нормировка `w / p90` частично самовосстанавливается при общем понижении весов.
     **Проверенное заблуждение:** я предполагал, что двойной штраф (до ×4) и есть причина
     сваливания выборки в один путь — НЕ подтвердилось, покрытие во всех трёх вариантах
     одинаково (34.0 / 33.1 / 34.6 %). Разброс сужает тяга по ВЕСУ, а не рельеф.

155. **Закрытие плана: три оставшихся прогона. ✅**
     *УХОД ОТ ОРИЕНТИРА — вопрос закрыт, расширять НЕ надо.* Он висел с самого начала
     работы над рельефом (ОГРАНИЧЕНИЯ.md §7.1) как «самый дешёвый способ помочь
     огибанию: поднять до 2 км и замерить». Замер:
     | уход | вес | по возвышенностям | покрытие | маршрутов |
     |---|---|---|---|---|
     | 0.5 км | 14.21 | 17.8 % | 33.7 % | 150 |
     | **1.0 км (принято)** | **14.37** | 19.0 % | 33.4 % | 150 |
     | 2.0 км | 13.31 | 21.3 % | 32.3 % | **147** |
     | 3.0 км | 13.40 | 20.0 % | 32.3 % | 150 |

     Расширение полосы ВРЕДНО: вес падает, возвышенностей больше, разброс не растёт, при
     2 км даже не все маршруты строятся. Обход в итоге решила стоимостная тяга, а не
     ширина полосы.
     *ПРЕДВИДЕНИЕ — усилено с 1.5 до 3.0.* Значение стояло «на глаз». Замер:
     | сила | вес | по возвышенностям | высота | покрытие |
     |---|---|---|---|---|
     | 0.0 | 13.46 | 24.1 % | 90.9 м | 35.8 % |
     | 1.5 | 13.72 | 19.4 % | 85.6 м | 33.0 % |
     | **3.0** | **14.88** | **17.1 %** | **82.7 м** | 32.8 % |

     При 3.0 лучше по ВСЕМ метрикам сразу, разброс теряет 0.2 п.п. — чистое улучшение.
     *ПОРОГИ РЕЛЬЕФА — сверены по высотам* (задача «проверить глазом, что синее легло на
     поймы, а красное на кряж», решена численно):
     | зона | медиана высоты |
     |---|---|
     | укрытия (синее) | **78.2 м** |
     | нейтрально | 118.9 м |
     | медиана участка | 131.8 м |
     | возвышенности (красное) | **165.2 м** |
     | отсечка (чёрное) | **257.6 м** |

     Порядок верный. Оговорка: нейтральной зоны мало — 18.9 % площади против 37.3 %
     укрытий и 43.7 % возвышенностей, в потолок упирается 9.7 %.
     *КОНТРОЛЬНЫЕ МЕСТА:*
     | место | вес | k | высота | слои |
     |---|---|---|---|---|
     | точка заказчика | 30.46 | 1.38 | 136.7 м | река + дорога |
     | Луганский заповедник | 9.74 | 1.39 | 57.1 м | дорога |
     | **Сентянівка** | **0.00** | **0.30** | 187.6 м | ручей + ЛЭП |
     | Новотошковское | 0.00 | 0.68 | 193.9 м | нет слоёв |

     **Ответ на давний вопрос по фото 5–6:** Сентянівка стоит на высоте 187.6 м,
     множитель рельефа 0.30 — ручей и ЛЭП там слабые, рельеф добивает их до нуля.
     Маршрут обходит её не по ошибке, а потому что это возвышенность.

156. **Планы вынесены в отдельную папку, принят план 2.** Планы теперь лежат в
     [теория/планы/](планы/README.md) с нумерацией по порядку появления:
     1 — рельеф (реализован, оставлен как история отклонённых схем), 2 — интерфейс и
     движение (принят к работе). Ссылки на перенесённый файл поправлены в CLAUDE.md,
     ЖУРНАЛЕ и ОГРАНИЧЕНИЯХ.
     **[План 2](планы/2_ПЛАН_ИНТЕРФЕЙС_И_ДВИЖЕНИЕ.md)** собран по замечаниям заказчика:
     П1 — маршрут отдаляется от цели у самой цели (фото 1: «хвост» от цели вниз); П2 —
     интерфейс: менеджер слоёв с «глазами» вместо стены из 12 чекбоксов, группировка,
     две колонки, переименование, показ = только видимость без пересчёта; П3 — цветовая
     шкала для тепловой карты маршрутов (при двух активных слоях — шкала последнего
     включённого); П4 — разобран вопрос про шаг сетки датчиков; П5 — запретные зоны,
     рисуемые пользователем; П6 — плавное дугообразное движение вместо ломаного;
     П7 — сверка векторных слоёв с весовой картой.
     В плане отдельно записано: **перед работой читать журнал, пункты 147–155**, иначе
     легко «починить» то, что сделано намеренно (двойной учёт рельефа, предрассчитанные
     поля «жадности», отключённый отсев выбросов).

     **ОТВЕТ НА ВОПРОС ПРО ШАГ СЕТКИ ДАТЧИКОВ** (`threat_cand_step_km`). Это шаг
     ДИСКРЕТИЗАЦИИ пространства поиска, а не критерий качества: перебирать континуум
     невозможно, поэтому строится сетка кандидатных позиций, из которой жадный алгоритм
     выбирает N лучших (правило 1.6). Замер: шаг 1.5 км → 1983 кандидата, 3.0 км → 485,
     6.0 км → 134. Мельче — точнее, но перебор дольше. Сами критерии выбора именно те,
     о которых спрашивал заказчик (частота пролёта под датчиком, разнос, якорные у входа
     и цели, запрет бесполезных) — правила 1.2–1.7. Отмечено к проверке: радиус датчика
     стал 3 км вместо прежних 12, а шаг остался 3 км — возможно, его стоит привязать к
     радиусу (R/2), а не задавать отдельно.

157. **📌 СВОДКА: НАСЕЛЁННЫЕ ПУНКТЫ В ВЕСОВОЙ КАРТЕ — что в итоге изменилось.**
     Работа шла долго и разбросана по пункту 149; здесь собрано, ЧТО ИМЕННО стало
     по-другому, чтобы не перечитывать весь разбор.

     **1. Деревня больше не стирает ориентиры** (правило 3.4а). Штраф был фиксирован
     (−20/−10) и не зависел от содержимого ячейки, а ориентиры весят немного: река +10,
     местная дорога +3. Сумма не перекрывала штраф, вес уходил в МИНУС — то есть в полный
     запрет (`passable_mask` берёт `weight > 0`), вопреки собственному правилу «деревня:
     пролёт разрешён». Теряли коридор **1019 ячеек, среди них 54 реки**.
     Стало: пол `min(исходный вес, THREAT_VILLAGE_FLOOR = 1.0)` — штраф ослабляет, но не
     стирает; сам штраф смягчён до −10/−5. Коридоров 9 944 → 10 950, компонент связности
     79 → 44: деревни перестали РАЗРЕЗАТЬ коридоры поперёк.

     **2. Размер пункта меряется по своему пятну, а не по склейке** (правило 3.6а).
     Маска города слипается тремя способами — обжитая зона (+1 км), общая маска контуров,
     буфер облёта (+0.4 км). Цепочка сёл вдоль реки давала ОДНУ область **431 км² при
     27 % реальной застройки**, и правило «река сквозь НП остаётся коридором, пока пункт
     меньше 20 км²» не срабатывало НИКОГДА — именно вдоль рек сёла и стоят цепочкой.
     Стало: размер берётся у исходного пятна сырой застройки (`own_place_area_km2`).
     Река без веса: **260 → 109 ячеек**, все оставшиеся — в настоящих крупных городах.

     **3. Тип пункта считается ПО ДАННЫМ, а не по тегу OSM** (правила 3.10, 3.10а–3.10г).
     Тег ненадёжен, а контур засчитывается целиком при доле застройки 15 % — вместе с
     85 % полей. Пятно с 27 % застройки формально проходило порог «город» и запирало
     пролёт на сотнях км².
     Стало: пара признаков «площадь застройки × компактность (застройка ÷ замкнутый
     контур)»; пороги — мин/среднее/макс по каждой группе тега В ПРЕДЕЛАХ рабочей области,
     поэтому переносятся на другую местность; перевод при допуске ±15 % и согласии ОБОИХ
     признаков; класс с выборкой меньше 5 пунктов не принимает и не отдаёт никого;
     один проход без итераций; исходный тег сохраняется в `_place_tag_raw`.

     **4. Районы города отделены от самого города** (правило 3.10в). `suburb` и `borough`
     кодировались наравне с `city`, и **62 района шли в класс из 8 городов** — класс был
     засорён на 89 %, его минимум падал до 0.50 км² (два квартала), а настоящие города
     оказывались выбросами в собственном классе. Для запрета пролёта район по-прежнему
     город (проверка `>= 3`), но в статистику не входит.
     **Порядок кодов критичен:** у города код ВЫШЕ (`city` 4, `suburb` 3), потому что тип
     пятна берётся как МАКСИМУМ кодов его меток — при обратном порядке пятно Луганска с
     размеченными районами получало код района, и из восьми городов в класс попадали два.
     Соседний класс берётся по позиции в списке `PLACE_LADDER = (1, 2, 4)`, а не
     арифметикой над кодом.

     **5. Три ошибки, найденные по ходу** (все исправлены):
     * градация сперва работала НАОБОРОТ — 133 подъёма против 23, городская зона росла с
       3441 до 3542 ячеек. Причина: классы по тегу перекрываются почти полностью, и
       «дотянуться до минимума city» могла рядовая деревня. Введён строгий подъём:
       превзойти МАКСИМУМ своего класса;
     * затем разжаловались 16 НАСТОЯЩИХ городов, включая Северодонецк (17.25 км²) —
       понижение сравнивалось с чужим классом (максимум town 23.50 км²). Правило сделано
       симметричным: упасть ниже МИНИМУМА своего класса;
     * затем класс `city` схлопнулся до 4 пунктов, и оба правила стали строить границу на
       самих же городах, снова их разжаловав. Предохранитель «меньше пяти пунктов»
       распространён и на СВОЙ класс.

     **6. Отсев выбросов реализован, но ВЫКЛЮЧЕН** (правило 3.10б). Он работает против
     строгого подъёма: порог берётся как максимум своего класса, а отсев выбрасывает
     крупные пункты как «выбросы» и сбивает планку `town → city` с 23.50 до 6.25 км².
     Замер: без отсева 0 подъёмов и 11 140 коридоров, с отсевом 12 подъёмов и 11 120.
     Каскад «город не меньше крупнейшего посёлка» (идея заказчика) дал ровно то же, что
     «без отсева»: он поднимает МИНИМУМ класса, а порог использует МАКСИМУМ класса снизу —
     разные концы распределения; сама мысль уже воплощена в правиле строгого подъёма.

     **7. Кэш пришлось пересобрать** (правило 3.10г). `PLACE_CODES` применяется только при
     чтении `.osm.pbf`, а `geo_cache/threat_layers.npz` хранит УЖЕ ЗАКОДИРОВАННЫЙ тип.
     После правки кодов карта из кэша молча теряла класс города (0 пунктов в статистике
     вместо 19). Пересобрано `tools/build_threat_grid.py` (~20 с), прежний кэш сохранён
     как `threat_layers_2026-08-06_before_place_codes.npz`. **Это правило легко забыть:
     правка `PLACE_CODES` без пересборки кэша не действует.**

     **Итог по карте:** коридоров **11 101**, компонент 42, городская зона **3441** ячейка.
     На этом участке настоящих городов **4** — меньше порога в 5, поэтому класс `city` в
     переводах не участвует и срабатывает один перевод `town → village`. Это не сбой, а
     честный ответ на малую выборку. Разжалованных городов — ноль. Список для сверки по
     карте: [ГРАДАЦИЯ_СПИСОК_ПРОВЕРКИ.md](ГРАДАЦИЯ_СПИСОК_ПРОВЕРКИ.md).
     Контрольный прогон §6 держится: 250/250 маршрутов, 150/150 итераций, 15/15 датчиков,
     0 выходов за область залёта, 0 превышений хода, 0 датчиков на воде и в запретном
     городе, засечка 100 %, средняя кратность 6.5. Механизм выключается одним
     `THREAT_PLACE_GRADE_ON = False` — карта возвращается к прежней.
     Одно «нарушение» контрольного прогона (датчик в городе) оказалось дефектом теста:
     он не учитывал СВОБОДНЫЕ ЗОНЫ у вылета и цели (§4), где запрет города не действует
     и якорные датчики стоят штатно — цель, как правило, и находится в городе.

     *Перед началом работ создан архив* `uav_sensor_mvc_АРХИВ_2026-08-06.zip` (15.2 МБ,
     код и документация без тайлов, DEM и офлайн-пакетов).

### План 2 — П1: маршрут отдалялся от цели у самой цели

158. **«Хвост», торчащий из цели: заход с другой стороны был фикцией. ✅**
     Заказчик по скриншотам: подойдя почти к цели, маршрут уходит вбок почти
     перпендикулярно на несколько километров и возвращается; на тепловой карте от цели
     тянется хвост. Требование при этом было чёткое: **заход с другой стороны оставить**,
     поправить только его форму.

     **Что показал замер (150 маршрутов, базовый прогон R = 3 км, N = 15).**
     * Отворот у цели весь целиком давала одна группа: средний откат от уже достигнутого
       минимума расстояния — **1.15 км у маршрутов через VIA за целью против 0.01 км у
       остальных**. 29 % выборки шло через такие точки.
     * Первая гипотеза — «второй участок возвращается тем же коридором, потому что
       `visited` между участками не общий» — **не подтвердилась**: перекрытие участков по
       ячейкам всего 6.2 %. Возвращался маршрут другой дорогой.
     * Настоящая причина нашлась в геометрии VIA-сетки: узел встал **в 2.5 км за целью,
       боковое смещение 0.3 км** — фактически на самой оси вход→цель. А выбор VIA идёт по
       «дальности обхода» = расстоянию до ОТРЕЗКА вход→цель, и у точки за концом отрезка
       оно равно расстоянию до цели. То есть **2.5 км против 5.7 у следующей точки** —
       алгоритм считал её САМОЙ ЦЕНТРАЛЬНОЙ и охотно выбирал: 44 маршрута из 150.
       Каждый обязан был пролететь цель насквозь, уйти за неё и вернуться.
     * Проверка, ради чего всё это работало: **с какого сектора маршрут пришёл в цель**.
       С обратной стороны (> 90° от оси) приходило **3 маршрута из 150** при 44 «заходах
       сзади». Механизм захода почти всегда работал вхолостую — он давал не обход, а клевок.

     **Фикс — два согласованных механизма** (правила 2.11а и 2.11б):
     * `FINAL_BYPASS_KM` = 4 км — участок, ведущий к VIA за целью, не приближается к
       объекту ближе радиуса обхода. Действует ТОЛЬКО на таком участке: применить правило
       ко всем VIA нельзя, обычным маршрутам оно закрывает коридоры у самой цели и роняет
       вес под выборкой на 12 %;
     * `VIA_BEYOND_PUSH` — точка, оказавшаяся внутри круга, не выбрасывается, а
       **отодвигается по лучу от цели за него** (с зазором в две ячейки на дискретизацию).
       Выбрасывать нельзя: на этом участке она единственная достижимая за целью, и заход
       сзади пропал бы совсем.

     **Грабли, на которые я наступил по дороге** (обе пойманы самопроверкой):
     * *выключатель не возвращал прежнее поведение.* `VIA_BEYOND_PUSH = False` не «оставить
       точку как была», а «выбросить её» — и «база» в замере разошлась с реальной базой
       (откат 0.01 против 0.34). Пока это не заметил, все сравнения были бессмысленны;
     * *поле до отодвинутой VIA считалось по ПОЛНОЙ маске.* Оно ведёт спуск прямо на цель,
       а шаг туда запрещён радиусом обхода — участок упирался в тупик, попытка
       отбраковывалась, и **заход сзади переставал строиться вовсе: 0 % вместо 21 %**.
       Лечится тем, что поля до «задних» точек считаются по той же урезанной маске
       (`pas_via`), а «сколько хода оставить на второй участок» берётся из поля ОТ ЦЕЛИ —
       в урезанной маске сама цель вырезана, и расстояние до неё было бы бесконечным.

     **Проверено и отвергнуто: лимит «топтания» у цели.** Запрет удаляться (2.10) не мешает
     идти ПО КАСАТЕЛЬНОЙ — расстояние при этом не растёт вовсе, — поэтому я добавил счётчик
     пути в зоне подхода без сокращения расстояния. Механизм оказался **мёртвым**: значения
     0.5, 1.0 и 3.0 км дали побайтово одинаковую выборку. Причина: при допуске 0.25 км за
     шаг маршрут не успевает накопить и полкилометра без прогресса, а «отворот
     перпендикулярно» с карты был транзитом мимо цели, а не финальным заходом. Код удалён,
     причина записана в `threat_routes.py` рядом с `FINAL_BYPASS_KM`, чтобы не изобретать
     заново.

     **Результат** (три seed, авто-запас хода 107 км):

     | | заходов сзади (> 90°) | отворот у цели, сред / макс | вес | покрытие |
     |---|---|---|---|---|
     | было | 8.7–11.3 % | 0.32–0.34 / 2.18–3.19 км | 14.13–15.37 | 30.5–31.6 % |
     | стало | 24.0–29.3 % | 0.13–0.19 / 1.41–2.05 км | 13.63–14.60 | 31.8–32.9 % |

     **На разном запасе хода** (|AB| = 73 км; крюк на обход надо откуда-то взять):

     | запас | построено было / стало | отворот макс было / стало | вес было / стало |
     |---|---|---|---|
     | 95 км (тесно) | 129 / 118 | 1.91 / 1.03 км | 13.75 / 14.16 |
     | 107 км (авто) | 150 / 140 | 4.09 / 1.86 км | 14.74 / 14.71 |
     | 130 км | 150 / 150 | 7.19 / 6.07 км | 14.86 / 15.16 |
     | 150 км | 150 / 150 | **14.48 / 8.15 км** | 17.52 / 16.75 |

     Видно главное: **чем больше запас хода, тем заметнее был дефект** — на 150 км маршрут
     отходил от цели почти на 15 км. Это тот самый прогон, который заказчик делал руками.
     Плата — на тесном запасе часть маршрутов не укладывается в ход (118 из 150 против
     129): обход стоит километров. Вес выборки в пределах разброса между seed.

     Инварианты §6 держатся при обоих запасах: 250/250 возможных маршрутов, 150/150
     итераций, 15/15 датчиков, засечка 100 %, 0 выходов за область залёта, 0 превышений
     хода, 0 датчиков на воде.

     ⚠️ **Исправлено НЕ ПОЛНОСТЬЮ — и это принятое решение.** Заказчик посмотрел карту
     после правки: стало заметно лучше, но **осталась пара маршрутов, которые по-прежнему
     уходят от цели вниз, и далеко**. Замер это подтверждает: средний отворот упал втрое
     (0.32 → 0.15 км), а вот МАКСИМУМ остаётся ощутимым — 1.41–2.05 км на авто-запасе и
     6.07–8.15 км при 130–150 км. То есть чинилась массовая причина (29 % выборки через
     одну неудачную VIA), а единичные длинные отвороты остаются: они приходят от других
     точек и от самой стохастики блуждания.
     **Решение: оставить как есть.** Единичные случаи на общую картину не влияют, а
     дальнейшее ужесточение упирается в компромисс — обход стоит запаса хода, и на тесном
     ходе маршруты просто перестают строиться (уже сейчас 118 из 150 при L = 95 км).
     Если решим вернуться: смотреть, через какие VIA идут именно эти маршруты, и мерить
     не средний отворот, а долю маршрутов с отворотом больше 3 км.
     → [ОГРАНИЧЕНИЯ.md §2.11а–2.11б](ОГРАНИЧЕНИЯ.md)

### План 2 — П2 и П3: интерфейс, отрисовка слоёв, шкала

159. **Панель показа, отрисовка векторных слоёв и цветовая шкала. ✅**
     Заказчик: правая панель — сплошной поток из двенадцати чекбоксов, управление
     итерациями и показатели выдавлены вниз; на общем виде карта превращается в кашу;
     у тепловой карты маршрутов нет своей шкалы.

     **Панель (П2).** Двенадцать чекбоксов во всю ширину разбиты на четыре смысловые
     группы по две колонки, подписи сокращены (подробности переехали в подсказки).
     У группы свой переключатель — гасит всех детей ОДНОЙ перерисовкой, а не по разу на
     слой. Слой, которого ещё нет, теперь **погашен** и подсказка говорит, что нажать:
     раньше галка стояла, а на карте не появлялось ничего.
     Обсуждали полноценный «менеджер слоёв» (глаз вместо галки, ползунок на каждый слой,
     раскрытие по стрелке). **Отклонено замером:** строка со ползунком не влезает в две
     колонки — панель 354 px, на колонку 177, а ползунок добавляет 76 px. Вышло бы 12
     строк в одну колонку = 280 px против 216 исходных: задумано ради компактности, а
     панель стала бы на 30 % ДЛИННЕЕ. Плюс чекбокс бесплатно даёт состояние «недоступно»,
     которое для кнопки-глаза пришлось бы рисовать вручную на каждый из 12 слоёв.
     Взят гибрид: группы + две колонки + переключатель группы + ползунок прозрачности на
     группу (208 px).

     **Окно «Векторные слои…»** (просьба заказчика). Список из десяти слоёв с образцами
     цвета: что показывать по отдельности. Не модальное — можно смотреть на карту;
     выбор хранится в представлении, поэтому переживает закрытие окна; верхний
     переключатель — тот же, что чекбокс в панели. Переключение НИЧЕГО не пересчитывает:
     проверено на живом контроллере — сумма весов карты до и после 120623.2 = 120623.2.

     **Отрисовка (главная работа).** Разбор скриншотов показал: виновата не палитра.
     * **Подложка дублирует наши слои.** OSM рисует СВОИ дороги, воду и лес, мы кладём
       сверху свои — оранжевое на оранжевом, синее на синем. Введено гашение тайлов
       (`BASEMAP_FADE`): обесцветить и осветлить. Первый вариант 0.62/0.30 выцветил карту
       почти в белый лист — заказчик попросил не больше 20 %, оставлено 0.20/0.10.
     * **Порядок отрисовки не был задан вовсе:** у всех десяти слоёв стоял один
       `setZValue(-5)` — кто нарисован последним, тот и сверху, случайно. Теперь порядок
       явный: застройка → лесополоса → вода → ж/д → труба → ЛЭП → дороги → мост.
     * **Нет обводки.** Линии без канта сливаются на пересечениях, а при отдалении их
       сотни. Дорогам, реке, ж/д и мосту добавлен кант (второй элемент под линией).
       У ж/д это ещё и «шпалы»: тёмный пунктир поверх белого канта.
     * **Различие только по оттенку.** Тонкие линии при отдалении сжимаются в пиксель.
       Слои разведены штрихом: ЛЭП — частые точки, труба — длинный пунктир, лесополоса —
       короткий, застройка — пунктирный контур.
     * **Маркеры-ковёр.** Мосты и развилки шли сотнями в полный размер; теперь мельчают
       при отдалении (11 → 5 px), но не исчезают.

     **Три ошибки подряд в подборе палитры** (все пойманы замером, не глазом):
     * *максимизировал контраст* — все фоны OSM светлые, поэтому «самый контрастный»
       цвет = чёрный. Получил десять одинаково чёрных линий: контраст к фону идеальный
       (11–12 при норме 4.5), а слои неразличимы между собой. Правильный критерий —
       САМЫЙ СВЕТЛЫЙ тон, который ещё проходит норму;
     * *считал контраст к одному усреднённому фону* — а карта это лес, поля, вода,
       дороги, застройка. Проверять надо по ХУДШЕМУ (это лес);
     * *пытался пройти норму 4.5 сразу на светлой карте и на тёмном фоне приложения* —
       это физически невозможно: предел ≈ 3.9, дальше контрасты расходятся. Заказчик
       снял вопрос («проверку делай на OSM, она почти универсальная»), порог для слоёв
       С ОБВОДКОЙ смягчён до 3.6 — кант сам отделяет линию от фона.
     Итог: контрасты 3.6–4.7, худший — река 3.61. Близкие по цвету пары (труба и местная
     дорога, ж/д и застройка) разведены штрихом и шириной.

     **Ещё две тонкости, найденные проверками:**
     * ЛЭП «не различалась по цвету» не из-за цвета: её штрих `[1,5]` закрашивал лишь
       четверть длины, и линия вырождалась в пыль. Уплотнён до `[2,3]`, ширина 1.3 → 1.9;
     * «дороги странно накладываются» на общем виде — это не цвет, а белые канты шириной
       5 px: у соседних дорог они сливаются в сплошное белёсое пятно вокруг посёлков.
       Кант теперь худеет при отдалении втрое (5.0 → 3.2 px), а сама линия почти не
       меняется (3.0 → 2.6) — иначе слой исчезнет.

     **Легенда.** Была 10 pt и не масштабировалась — на приближении занимала треть
     экрана. Сокращена (лишние пояснения убраны), кегль 7.8 pt, по умолчанию СКРЫТА.
     Размер сделан ПОСТОЯННЫМ: мельчание при зуме оставлено только панели «приоритет по
     высоте» — она короткая, её видно и мелкой, а длинная общая легенда, ужимаясь,
     становилась нечитаемой ровно тогда, когда по ней сверяют объекты.

     **Ползунок прозрачности — дефект, найденный заказчиком.** Гасились только
     растровые заливки: розовая зона пролёта тускнела, а синие линии поверх неё
     оставались яркими; у векторных слоёв не менялось вообще ничего. Причина: таблица
     целей перечисляла четыре растра. Теперь под ползунком ВСЕ элементы группы (у
     «местности» — 20: десять линий, их обводки и маркеры мостов). Ползунок при этом
     МАСШТАБИРУЕТ родную прозрачность слоя (у рельефа 0.55, у тепловой карты 0.62), а не
     задаёт её: иначе на 100 % они стали бы непрозрачными и закрыли всё под собой.

     **Шкала тепловой карты (П3).** Сделана ОДНА шкала на две карты, а не две: обе
     рисуются заливкой по всей области, и вторая шкала заняла бы правый край вдвое.
     Показывается шкала последнего включённого слоя; выключенный уходит в начало
     очереди и возвращается, если включить его снова.

     **Начальные параметры** по просьбе заказчика: датчиков 15, радиус 2 км, расход
     запаса «равномерно».

     **Проверено на живом контроллере:** 250/250 возможных маршрутов, 15/15 датчиков,
     150/150 итераций, вес карты при переключении слоёв не меняется.
     ⚠️ **Открылось попутно:** радиус стал 2 км, а шаг сетки кандидатных позиций остался
     3 км — шаг ТЕПЕРЬ КРУПНЕЕ радиуса, между кандидатами дыры шире зоны обзора. Это
     вопрос П4 плана, не трогали.
     → [НАСТРОЙКА_ИНТЕРФЕЙСА.md](НАСТРОЙКА_ИНТЕРФЕЙСА.md) — что и где менять руками

---

## 4. Возникшие проблемы и исправления

| Проблема | Причина | Исправление |
|---|---|---|
| Вкладки 1 и 2 падали в «Пакетно»/«Шаг» (TypeError) | обобщение `CoverageCache.greedy` под вкладку 3 сменило `anchor_idx`→`anchors` (список), а вызовы в `simulation.py`/`area_start.py` не обновили | 4 вызова переведены на `anchors=[ai] if ai is not None else None` (п. 113) |
| Дуга вырождалась при θ>90° | построение через стрелу прогиба давало малую дугу | прямая параметризация центральным углом (`make_arc_by_angle`) |
| «Манёвр» шёл по прямой | целевой случайный блужданий схлопывался к прямой | переход на боковые гармоники, затем на модель по путевым точкам |
| Точка B рисовалась не там (вкладка 2) | `_sync_geometry` брал A/B из `p` (дефолт), а не из клика | брать `model.A/model.B` |
| Повторное «Создать маршрут» не сбрасывало | старый маршрут оставался в модели/графике | `on_create` сбрасывает модель и графику |
| Пропали легенды в Qt | не были добавлены элементы легенды | `ColorBarItem` + `LegendItem` |
| «Could not find Qt platform plugin windows» | не находился каталог плагинов на Windows | `_ensure_qt_plugin_path()` + инструкция переустановки |
| Кривой масштаб вкладки 2 при старте | размер виджета не задан на неактивной вкладке | `showEvent`/`resizeEvent` → `_apply_range(force=True)` |
| Маршруты кучковались в центре | смешение «разброса» с числом точек | разброс = АМПЛИТУДА (линза по режиму), число точек — отдельный диапазон |
| Датчики «кучей» за/у цели B | кандидаты за B + без рубежа у цели | `filter_not_past_target` + якорный датчик + развязка 1.35·R |
| Синус игнорировал R_min | амплитуда не связана с физикой | суммарная кривизна ≤ 1/R_min (формула 13) |
| Редкие точки попадали в зону старта при выносе | проверяли точку выхода луча, а не «тень» эллипса | опорная проекция эллипса на ось S₀→B (формула 14) |
| Разброс «сложный» снова жался к центру, видны прямые | амплитуда от хорды D + узкий зигзаг при многих точках не влезали в L_max и схлопывались фитом | база амплитуды → b (полу-малая ось); сложный = 1–2 крупных лепестка к краю эллипса |
| Датчики появлялись «за спиной» зоны старта | исключали только внутренность эллипса зоны | + отсечение позади задней кромки (180°): (P−A)·û ≥ −depth/2 |
| Карта не перерисовывалась при смене расстояния | B бралась от клика и не зависела от поля \|AB\| | B = A + \|AB\|·направление в reset; on_apply форсит перемасштаб |
| Слой «10 маршрутов» (по точкам) уходил в одну сторону | случайная выборка немногих + крупные односторонние лепестки | стратификация по боковому отклонению — обе стороны и оба края |
| Маршруты выходили за показанный эллипс | рисовали эллипс фокусов A,B, а маршрут из S0≠A лежит в эллипсе S0,B | показываем огибающую (объединение эллипсов S0,B по зоне) |
| Карта оставалась размытой (п. 34 не помог) | `_pick_zoom` брал зум под ПОТОЛОК тайлов → мозаика < виджета → растяжение | зум по ПОЛУ (мозаика ≥ экрана) + `autoDownsample=True` (п. 36) |
| Слои вкладки 3 сместились бы относительно подложки | `_km_per_deg_lon` — константа от `KURSK_LAT`, игнорировала `lat0` | масштаб долготы от фактической широты якоря (п. 37) |
| Датчик мог встать на реку (вкладка 3) | вода — аттрактор веса, но не исключалась для датчиков | маска воды + буфер, кандидаты на воде исключаются до жадного (п. 38) |
| Бесконечная рекурсия схемы после рефакторинга | хвост старого `_draw_scheme` (`_set_scheme_visible(True)`) прицепился к новому хуку `_set_scheme_extra_visible` | убрана лишняя строка; хук лишь переключает рамку области (п. 41) |
| Зависание на реальных OSM (45 МБ) | `_detect_bridges` O(N²) по парам ломаных + Python-растеризация + point-in-polygon по всей сетке; всё в UI-потоке | мост по сетке (river∧road), векторная растеризация, bbox-полигоны, фоновый поток (п. 45) |
| Интерфейс тормозит ПОСЛЕ показа карты | тысячи OSM-линий (сотни тысяч точек) со сглаживанием + тысячи маркеров-мостов перерисовывались каждый кадр | прореживание линий, `antialias=False`, мосты магистраль/ж-д×река ≤400, убран autoDownsample, чекбокс полностью очищает слой (п. 46) |
| Карта «замельчала», подписи не того размера | `_pick_zoom` брал первый зум ≥ экрана (мозаика до 2× → ужимали, подписи мельче) + домножение на DPR | зум под масштаб ~1:1 (`min|log2(nx)−log2(need)|`), логическая ширина (п. 47) |
| Город — самый «горячий» на карте (fig 7) | много дорог/перекрёстков в застройке + funnel-бонус | исключение города по плотности застройки в окне 3×3 км: вес→штраф, обход маршрутом (п. 48) |
| Маршруты шли прямыми линиями (не по карте) | нормировка стоимости на max(W): один тяжёлый узел уравнивал остальных | нормировка по p90 — маршрут идёт по коридорам (п. 52) |
| Реки в городе без тепла/непроходимы | исключение города зануляло и речные ячейки | `urban &= ~river` — река остаётся коридором сквозь город; исключаем только крупные города (п. 53) |
| Маршрутов ровно 14, «нельзя обойти» | показывали фикс. число линий, а не область достижимости | огибающая `g_вход+g_цель≤L_max` — ВСЕ места пролёта, длинные обходы (п. 56) |
| Нет окна ввода ТТХ на вкладке 3 | параметры маршрута нельзя было менять из UI | немодальное окно «Входные данные…», Enter применяет без блокировки (п. 57) |
| Перекрёстки не видны отдельно | funnel-бонус усиливал узлы, но их нельзя было выделить | `crossing_cells_km` (≥2 разных слоёв) + чекбокс-подсветка, скрыт по умолчанию (п. 58) |

---

## 5. Ключевые решения (с учётом обсуждения)

* **Разброс ≠ число точек.** «Разброс» — это амплитуда отклонения от прямой
  (малый/средний/сложный + смешанный). Число точек задаёт пользователь
  диапазоном [n_min, n_max] (рандом внутри, случайные расстояния между точками).
* **Модель «по точкам» как основная.** Реальные карты маршрутов коммерческих БПЛА
  самолётного типа — ломаные с плавными доворотами, а не гладкие синусоиды. Синус
  оставлен как опция в выпадающем списке.
* **Крен и скорость — это ТТХ БПЛА** → радиус разворота R_min = V²/(g·tan φ).
* **Зона старта — область неопределённости, не препятствие.** Поэтому положение
  путевых точек относительно зоны — мягкое условие (переключатель), а вот
  размещение ДАТЧИКОВ в зоне запрещено жёстко (свои датчики нельзя ставить в
  районе пуска противника).
* **Якорь у цели + развязка** убирают вырожденную «кучу» датчиков у B.

---

## 6. Текущее состояние (сделано)

* Геометрия, эллипс, коридор, зона старта — готово.
* Модели движения: дуга, петля, манёвр «по точкам», манёвр «по синусу» (с R_min),
  дуга из старта, ломаная — готово.
* Обнаружение, метрики, жадная субмодулярная оптимизация (SAA, якорь, вторичный
  критерий) — готово.
* Два интерфейса (matplotlib + Qt с двумя вкладками), анимация, слои, легенды,
  пакетный расчёт, сравнение режимов — готово.
* Ограничения: точки за/в зоне (переключатель), датчики вне зоны (жёстко), без
  позиций за B, R_min для точек и синуса — готово.
* Документация (методичка/журнал/структура/README/docx) — синхронизирована.

Проверено офскрин-тестами: синус соблюдает R_min; ноль датчиков и (в режиме
«за зоной») ноль путевых точек внутри зоны старта; маршруты приходят в B и
укладываются в L_max; оба закона и оба режима зоны прогоняются без ошибок.

---

## 7. Что осталось / возможные улучшения

* Ветка разработки: `claude/terrain-map-sensor-weights-utcbml` (база — тег
  `pre-threat-map-base`, коммит `1c59f5b`).
* `docs/Теория_и_математика.docx` стоит пересобрать под формулы 13–14 и новые
  ограничения (скрипт `docs/make_theory_docx.py`).
* **Вкладка 3 (задел):** калибровка весов слоёв (`config.THREAT_LAYERS`) под
  реальные данные; Этап 3 — маршруты A\*/RRT\* по costmap от точки появления
  («Заречье») к цели («Аэропорт»); при желании — унифицировать тайловую подложку
  вкладки 2 на общий `basemap_mixin` (сейчас у вкладки 2 своя копия, чтобы не
  трогать рабочий код без живого GUI-теста); запасной путь по чёткости карты —
  послойные `QGraphicsPixmapItem` (см. п. 36).
* Возможные улучшения: 3D (высота полёта), разнотипные датчики (разные R),
  стоимость позиций, экспорт результата (CSV/гео), доверительные интервалы SAA.
