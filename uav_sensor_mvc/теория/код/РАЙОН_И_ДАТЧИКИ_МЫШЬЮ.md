# Мышь как ввод: район расчёта, правка датчиков и точек старта, режим анализа

> **Правился:** 16.09.2026 — план 11, 11.6: §1 (режимы кликов сектора и точек старта), §2.5 (район
> радиусом), §3.3 (как устроено перетаскивание датчиков), §3а (точки старта мышью), §5.
> 14.09.2026 (коммит `30eec6d`) — ссылки на переименованные файлы папки `код/`, содержание не менялось.
> 12.09.2026 — разбор заведён (план 9, задача 9.12).

> **Что разбираем.** Три механизма плана 8, у которых общая природа: **мышь перестала
> быть только средством смотреть и стала средством ЗАДАВАТЬ**. Отсюда общие трудности —
> режимы мыши, блокировка панели, и главный вопрос: что считать результатом расчёта, а
> что — входными данными.
>
> ⚠️ Здесь «как и почему сделано». Сигнатуры и строки — в
> [карте кода](../карта_кода/README.md); правила с числами — в
> [ОГРАНИЧЕНИЯХ](../правила/ОГРАНИЧЕНИЯ.md).

---

<!-- ОГЛАВЛЕНИЕ · сгенерировано tools/переносимое/docmap.py — руками не править -->
<!-- /ОГЛАВЛЕНИЕ -->

## 1. Общее для всех трёх: режим мыши — это состояние, и оно одно

Как только мышью можно **задавать**, а не только смотреть, у одного и того же клика
появляется несколько смыслов: поставить датчик · передвинуть датчик · указать сектор ·
задать угол района · поставить или передвинуть точку старта · измерить расстояние. Различить
их можно только **состоянием**.

Правила, которые из этого выросли (`threat_view.py` :: `_begin_edit_sensor`):

| Правило | Зачем |
|---|---|
| **режимы взаимно исключаются** — включая один, остальные гасятся явно | два режима разом означали бы, что клик обрабатывают два обработчика; в коде это выглядит как `self._end_pick_sensor(); self._end_measure(); self._target_mode = self._sector_mode = False` |
| **панель гасится на время режима** (`_lock_panel_for_edit`) | режим забирает клики карты себе, и соседняя кнопка перехватила бы их. Активной остаётся ровно одна кнопка — та, что режим выключит |
| **правая кнопка = выход** (`_grab_right_button`) | у pyqtgraph правая кнопка своя (контекстное меню и масштаб) — её приходится забирать и **возвращать** при выходе |
| **заголовок карты объясняет, что делать** | режим невидим сам по себе: без подсказки пользователь не знает, что DELETE удаляет, а перетаскивание переносит |

Режимы мыши вкладки 3 сейчас (с 16.09.2026, план 11, 11.6):

| режим | включает | клики | выход |
|---|---|---|---|
| район | «Задать район» | два левых клика, КОЛЁСИКО — принять | колёсико, Esc |
| цель | «Указать цель» | один клик — и режим снят | сам |
| **сектор кликами** | «Задать сектор кликом» в окне «Сектор налёта» | каждый левый клик — ось заново | **правая кнопка — выход и закрытие окна**, Esc — выход без закрытия |
| запретная зона | «Запретная зона» | вершины, колёсико — замкнуть | колёсико, Esc |
| постановка датчиков | «Добавить датчик» | двойной клик | кнопка, Esc, правая кнопка, двойной клик вне района |
| правка датчиков | «Править датчики» | щелчок, перетаскивание, DELETE | кнопка, Esc, правая кнопка |
| **точки старта** | «Точки старта» | двойной клик — добавить; щелчок, перетаскивание, DELETE | кнопка, Esc, правая кнопка |
| линейка | «Расстояние» | левые клики | кнопка, Esc, правая кнопка |

⚠️ **Перетаскивание сделано `pg.TargetItem`, а не своими обработчиками.** У сцены
pyqtgraph есть сигнал клика, но **нет пары «нажал — веду — отпустил»**: тащить пришлось бы
вручную, попутно отключая панораму вида, иначе карта поедет вместе с датчиком.
`TargetItem` умеет это сам и отдаёт `sigPositionChangeFinished` — остаётся сообщить
контроллеру новую позицию.

---

## 2. Район расчёта мышью

### 2.1. Зачем понадобился

Область показа — **302 × 250 км**. Сетка 500 м на неё целиком — это 582 × 499 ≈ **290
тыс. ячеек** против рабочих ~34 тыс. Считать по всей области незачем и нечем: расчёт идёт
по участку внутри, а смотреть нужно шире.

Отсюда разделение, которое легко спутать:

| | ОБЛАСТЬ показа | РАЙОН расчёта |
|---|---|---|
| что это | тайлы, камера, своя картинка-карта | прямоугольник, по которому считается сетка |
| откуда берётся | `config.THREAT_BBOX_LONLAT` | **задаётся мышью** или загрузкой своей карты |
| размер сейчас | 302 × 250 км | 117 × 73 км |

### 2.2. Главное решение: якорь НЕ переезжает вместе с районом

`ThreatModel.set_area` :: **2060**. Первая редакция переносила якорь км-фрейма в угол
нового района — и это оказалось ошибкой.

⚠️ **Почему.** Всё уже нарисованное — тайлы, своя карта, рамка, подписи — живёт в
километрах от **прежнего** якоря. Смена якоря означает, что слои лягут со сдвигом
относительно карты (замечание заказчика 04.09.2026). Причём молча: ничего не падает,
просто река оказывается не там.

**Как сделано:** якорь один на **область показа**, а район — просто прямоугольник в тех
же километрах. Побочная выгода: масштаб «градусы → км» больше не меняется под ногами, и
исчезла морока с округлением района до целого числа ячеек.

### 2.3. Вторая ловушка: кэш слоёв посчитан от ЧУЖОГО якоря

В `.npz` лежат **уже посчитанные километры** — от якоря, который был при сборке кэша.
Поэтому при своём районе слои читаются из `.osm.pbf` напрямую: иначе легли бы со сдвигом,
опять же молча. Это записано прямо в докстринге `set_area`.

### 2.4. «Убрать район» возвращает в исходное состояние

`clear_area` :: **2109** сбрасывает не только рамку, но и **рельеф**. Он оставался
включённым после снятия района, и следующее построение шло «с рельефом», хотя кнопка
обещала обратное, — а высоты грузились уже по другой рамке (заказчик 04.09.2026).

⚠️ Заодно вызывается `_drop_read_cache()` — слои и высоты освобождаются. Район снят,
считать не по чему; держать сотни мегабайт в памяти незачем.

**Пока район не задан, расчётные кнопки закрыты** и рамка не рисуется: на экране только
подложка и картинка-карта.

### 2.5. Район радиусом (план 11, 11.6)

Переключатель **«район / радиус»** справа от заголовка панели (`chk_shape_rect`,
`chk_shape_circle`, одна `QButtonGroup`) выбирает, **чем рисовать следующий район**. Уже
заданный район он не трогает: переключить галочку и потерять построенную карту — дорогая
случайность.

**Мышь.** Тот же `_begin_area` / `_area_click` / `_end_area`: у круга первая точка — центр,
вторая — точка на окружности, третий клик переносит точку радиуса, колёсико принимает. На
выходе вместо рамки в градусах — `on_set_area_circle(cx, cy, R)` в км.

**Модель** (`ThreatModel.set_area_circle`): сетка 500 м бывает только прямоугольной, поэтому
считается описанный квадрат, а круг — маской поверх:

| что | где | как |
|---|---|---|
| рамка | `set_area_circle` → `set_area` | описанный квадрат, всё прочее сбрасывается как у рамки |
| цель | `target_only_km` | центр круга, пока цель не указана кликом |
| вес | `build` → `ThreatGrid.set_outside_mask` | за окружностью вес и вклады слоёв — 0 |
| пролёт | `passable_mask`, `_open_endpoints` | за окружностью закрыто, **после** буфера и открытия концов |
| слои на карте | `_clip_layers_circle`, `_clip_parts_circle` | линии рвутся по окружности (+ тот же запас, что у рамки) |
| датчики | `candidate_positions` | кандидаты только внутри круга |
| точки входа | `_edge_band` | кольцо 1 км у окружности |
| рельеф на карте | `_mask_display_circle` | за окружностью прозрачно |
| демо-точка входа | `entry_km` | проецируется на окружность, если лежит за ней |

⚠️ **Открытие концов тоже закрывается маской.** `_open_endpoints` открывает квадрат 3 км вокруг
точек входа и цели, чтобы они дотянулись до коридоров. Точки входа стоят у самой окружности, и
половина квадрата выходила наружу. Нашёл `flow_check` (раздел 5а) при первом же запуске: область
залёта заходила за круг R = 25 км на **249 ячеек, до 3.4 км**; после правки — 0.

⚠️ **Демо-точка — на окружность.** В круге 25 км вокруг Мирного демо-точка входа участка лежала
в 163 км, `_cell_of` прижимал её к углу сетки за кругом, и маршрутов не было вовсе. Теперь она
ставится на ячейку внутрь от окружности в своём направлении.

---

## 3. Правка датчиков мышью

### 3.1. Ключевое различие: результат расчёта или входные данные

Датчик на карте может быть двух совершенно разных природ, и путать их нельзя:

| | поставлен АЛГОРИТМОМ | поставлен ЧЕЛОВЕКОМ |
|---|---|---|
| что это | результат расстановки | **входные данные задачи** |
| живёт в | `self.sensors` — пересчитывается | `self.manual_sensors` — **не сбрасывается пересборкой** |
| при «Расставить датчики» | пересчитывается заново | остаётся на месте |

⚠️ **Заданный человеком датчик — это НЕ якорь.** Якорь — механизм внутри жадного
алгоритма, позиция, которую он назначает себе сам (отключён 29.08.2026). А заданная
позиция — это «смоделируй вот такую обстановку»: человек назвал часть решения, алгоритм
добирает остаток до заказанного числа. Технически они передаются одним аргументом
`anchors`, и это **только способ передачи**: для гарантии (1 − 1/e) разница
принципиальна — первое самоограничение алгоритма, второе сокращение задачи.

Поэтому в `place_sensors` явно написано: `manual_sensors` здесь не трогается — это
задание, а не результат.

### 3.2. Три кнопки очистки, и они разные

Частый вопрос «чем они отличаются» — потому что разными вещами и должны быть:

| Кнопка | Что убирает |
|---|---|
| **«Очистить датчики»** | только расстановку — заданные человеком позиции остаются |
| **«Очистить»** | нарисованное на карте (зоны, маршруты) |
| **«Сброс»** | возврат к исходному состоянию целиком |

Логика та же, что в §3.1: «очистить расстановку» не должно стирать **задание**.

### 3.3. Как устроено перетаскивание датчика — по шагам

`threat_view.py`, режим «Править датчики»:

| шаг | функция | что происходит |
|---|---|---|
| 1. щелчок | `_on_scene_click` → `_edit_sensor_click` | берутся строки таблицы датчиков (`on_sensor_rows`), выбирается **ближайший** к клику |
| 2. допуск | там же, `_km_per_pixel` | ближайший засчитывается, только если он **не дальше 25 точек экрана** |
| 3. метка | `_show_edit_handle` | на датчик кладётся `pg.TargetItem(movable=True)` на Z = 40 — выше всех датчиков |
| 4. тащат | сам `TargetItem` | pyqtgraph ведёт метку за мышью; панорама вида при этом не едет |
| 5. отпустили | `sigPositionChangeFinished` → `_edit_handle_moved` | км переводятся в градусы, контроллеру уходит `on_manual_edit(номер, тип, режим, lon, lat)` |
| 6. модель | контроллер → `ThreatModel` | датчик становится **заданным вручную** — входными данными, как в §3.1 |
| DELETE | `keyPressEvent` → `_delete_selected_sensor` | `on_sensor_delete(lon, lat)` |

⚠️ **Допуск — в пикселях, а не в километрах.** На общем виде участка 2 км — несколько точек
экрана, при увеличении — половина окна: километровый допуск был бы то недостижим, то хватал бы
соседний датчик.

⚠️ **Удаление — по координате, а не по номеру строки.** Номер меняется от любой правки, и после
переноса указывал бы уже на другой датчик.

### 3а. Точки старта мышью (план 11, 11.6)

Кнопка **«Точки старта»** — тот же механизм, что в §3.3, с тремя отличиями:

| | датчики | точки старта |
|---|---|---|
| режимов | два: «Добавить датчик» и «Править датчики» | **один**: двойной клик добавляет, щелчок выбирает — у точки не о чем спрашивать при постановке |
| что тащится | `TargetItem`, квадрат | `TargetItem`, треугольник (`_show_entry_handle`) |
| отпустили | `on_manual_edit` | `on_entry_move(x0, y0, x1, y1)` — **старая и новая координата**, в км |
| за район | датчик переносится | модель **отказывает**, метка возвращается на место (`_entry_handle_moved`) |

**Модель** (`threat_grid.py`): `add_manual_entry`, `move_entry`, `remove_entry`, `entry_rows`;
точка ищется `_find_entry` **по координате** с допуском `ENTRY_PICK_KM` = 0.05 км — по той же
причине, что удаление датчика. После каждой правки `_entry_changed`: собрать итоговый список,
сбросить зависящее от входа и пересобрать карту (свободные зоны вокруг точек входа).

⚠️ **Пересборка после каждого переноса допустима.** Замер 16.09.2026: `build()` с кэшем чтения —
**0.05 с**, отбор точек сектора — 0.05 с. Иначе пришлось бы копить правки до «Применить».

⚠️ **Перенесённая точка сектора становится ручной** (`move_entry`): её убирают из `_sector_pts` и
кладут в `manual_entries`. Иначе ближайший пересчёт сектора — смена цели, раствора — вернул бы её
на прежнее место. Так же ведёт себя перенесённый датчик.

**Сколько живут.** Точки человека переживают пересчёт сектора, смену цели и пересборку карты;
убираются сменой района (в чужом районе их километры бессмысленны) и «Сбросом». На карте — тот
же треугольник с розовой заливкой (`entries_manual_scatter`, `THREAT_COLORS["entry_manual"]`). Как три поля
точек входа собираются в один список — [СЕКТОР_И_НАЗНАЧЕНИЯ §1.6](СЕКТОР_И_НАЗНАЧЕНИЯ.md).

---

## 4. Режим анализа — заморозка расстановки

### 4.1. Что требовалось

Во время моделирования расстановка пересчитывается каждые 5 накопленных маршрутов, и
датчики «ползут». Для сравнения «до / после» нужно наоборот — **закрепить** расстановку и
смотреть, как её оценивают накапливающиеся маршруты.

Флаг: `config.Params.threat_analysis_static`.

### 4.2. Почему флаг проверяется в ДВУХ местах, а не одним `return`

Самая неочевидная деталь механизма (`place_sensors` :: **3052–3090**). Напрашивается
ранний выход в начале функции — и он неверен.

У **ручного режима** («Задать позиции») свой путь: там «пересчёт» — это просто чтение
таблицы `manual_sensors`, а не прогон жадного алгоритма. Заморозить его целиком значит,
что **свежедобавленная мышью точка не появится на карте вовсе**.

Поэтому:

```python
analysis_freeze = (self.in_iterations()
                   and bool(getattr(self.p, "threat_analysis_static", False))
                   and (len(self.sensors) or len(self.sensors_big)))
```

— и дальше этот флаг гасит **два разных прогона по отдельности**:

| Где | Что гасится | Что при этом продолжает работать |
|---|---|---|
| ручной режим | жадный добор по `manual_locked` | чтение таблицы: новая точка появляется сразу |
| обычный режим | вся расстановка (`return`) | показатели и накопление маршрутов |

⚠️ **Условие «датчики уже есть» обязательно.** Без него режим анализа при пустой карте
запрещал бы первую расстановку вовсе — замораживать было бы нечего.

⚠️ **Заодно:** «динамический датчик, заданный вручную, во время моделирования
перемещается, статический стоит» — уточнение заказчика 05.09.2026. Без второй проверки
динамический переезжал бы и в режиме анализа, что режим как раз и запрещает.

---

## 5. Чем проверять

| Что | Чем |
|---|---|
| режимы мыши, блокировка панели, окна | `python tools/gui_check.py` — без экрана; район радиусом, окно сектора, точки старта — раздел 13 |
| круг в расчёте: вес, область залёта, точки человека после пересборки | `python tools/flow_check.py` — раздел 5а |
| порядок работы модели, что после сброса | `python tools/flow_check.py` |
| ⚠️ различие «статический / динамический» | `python tools/reference_cases.py` — **четыре обстановки**; контрольный прогон его не ловит: эталон снят на одной обстановке, и это различие ломалось **дважды подряд** при зелёном прогоне (журнал п. 230) |

**Связанное:** [МЕТОДИЧКА_ДАТЧИКИ_РЕЖИМЫ](../вкладка_3/МЕТОДИЧКА_ДАТЧИКИ_РЕЖИМЫ.md) — как
это выглядит для пользователя · [ОГРАНИЧЕНИЯ 1.3а, 5.1к, 8.11](../правила/ОГРАНИЧЕНИЯ.md) —
правила с числами · [1.КАК_КОД_РЕШАЕТ_ЗАДАЧУ](1.КАК_КОД_РЕШАЕТ_ЗАДАЧУ.md) — где эти механизмы
в общей цепочке.
