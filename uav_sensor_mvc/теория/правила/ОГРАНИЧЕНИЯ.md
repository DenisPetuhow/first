# Ограничения и правила модели — единый справочник

> ⭐ **ДОЛЯ ИЛИ КИЛОМЕТРЫ — отдельный документ:** [ДОЛИ_И_КОНСТАНТЫ](ДОЛИ_И_КОНСТАНТЫ.md).
> Правило заказчика 20.09.2026: механизм делаем универсальным, и величина чаще всего должна
> быть **долей от входного числа**, а не абсолютом — но не всегда (запрет движения назад
> пока статический). Там разложено, **что в долях, что абсолютно и почему**, и три вопроса,
> которыми это решается ДО того, как число попало в код. Повод завести: кольца облёта
> 10/18/28 км оказались подгоном под одну область и переделаны в доли уже после эталонов.

> **Правился:** 20.09.2026 (коммит `9197263`) — в коммит вошли правки 19.09,
> описанные ниже; нового содержания нет.
> 19.09.2026 — 5.10 переписано (предел держит перемещение, а не масштаб:
> мыло 1.145 на всех уровнях), новые 5.12 (пиксель в пиксель, без сглаживания, учёт DPR)
> и 5.13 (строка подложки над картой).
> 19.09.2026 — правки серии 16–19.09 (журнал п. 276–278); подробности —
> в самой записи журнала.
> 16.09.2026 — 1.8: что кратность `k` делает с расстановкой (замер 16.09).
> 16.09.2026 — 2.0а, 2.0б, 2.0г правлены; новые 2.0д–2.0з (задача 11.6): район радиусом,
> сектор по 360°, пауза повтора по углу, точки старта человека.
> 16.09.2026 — 2.11г (задача 11.5): край пояса — точка входа, доли поясов и повадок
> держатся среди дошедших; §6.0а — стохастика `arh` переснята под 11.5 (длина 147.5 ± 6.0, кратность 7.17).
> 16.09.2026 — 2.11г: облёт цели отдельным критерием, пояса в долях D; §6.0а —
> стохастика `arh` переснята (кратность 7.19, длина 141.7).
> 15.09.2026 — §2: запретный сектор (2.6б′), мягкая VIA (2.11в), облёт цели
> (2.11г), исправлен `late` (2.8); §7 — З3 продолжена в плане 11. 14.09.2026 — 5.9: рельеф окном растра; §6.0а: 4 прогона, засечка 0.980/0.987 —
> размах выборки, не правка (A/B при одном зерне), вопрос В9. 13.09.2026 — §5: область из файла `область.txt` (5.1, 5.1у), папка тайлов
> (5.1ф), склейка кусков области (5.1х), предел вида без «район + 100 км» (5.10). §6: эталон переснят под полный набор векторных слоёв (просеки,
> лесные дороги, канавы, брошенная ж/д); по решению В8 — на **18** датчиках, порог 0.99 сохранён;
> ⚠️ остальное не сверялось с 10.09.2026.

**Зачем этот файл.** Здесь в одном месте собрано ВСЁ, что программа обязана соблюдать:
где можно и нельзя ставить датчики, куда может и не может лететь БПЛА, как обходятся
города, что происходит у точки вылета и у цели. Каждое правило — со значением, местом в
коде и объяснением, почему оно такое.

**Как пользоваться.** Меняете правило — правьте параметр в `config.py` (не в коде) и
дописывайте строку сюда. Если правило нарушилось на прогоне — смотрите столбец «где
задано» и идите туда. Контрольный прогон проверяет отмеченные ✓ правила автоматически.

> **Здесь только правила МОДЕЛИ.** Правила ведения самой документации (журнал коротко —
> методичка подробно, пятишаговая сверка на достоверность, пороги 300 и 1 500 строк,
> раскладка по папкам) живут отдельно: [CLAUDE.md](../../CLAUDE.md) — рабочая формулировка,
> [переносимые правила](../переносимое/README.md) — обоснование и
> переносимая в другие проекты схема. Дублировать их сюда нельзя: два места с одним
> правилом расходятся молча — этим и болела документация до августа 2026.

Связанные документы: механика весов — [`МЕТОДИЧКА_КАРТА_УГРОЗ.md`](../вкладка_3/МЕТОДИЧКА_КАРТА_УГРОЗ.md);
модели маршрутов и расстановки — [`МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md`](../модель/МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md);
история решений — [`журнал/README.md`](../журнал/README.md).

---

<!-- ОГЛАВЛЕНИЕ · сгенерировано tools/переносимое/docmap.py — руками не править -->
> **Оглавление** — номер строки · раздел. Открывать нужный раздел, а не файл
> целиком (`Read` с `offset`). Пересобрать: `python tools/переносимое/docmap.py`.
>
> `  80` 1. ДАТЧИКИ — где ставить нельзя и почему
> ` 106` 2. МАРШРУТ БПЛА — куда лететь можно и нельзя
>   ` 154` Пояснение к правилам 2.9–2.12: два понятия, без которых они непонятны
>   ` 196` 2.0 Тяга к цели — по СТОИМОСТИ, а не по километрам
>   ` 215` 2.1 «Возможные маршруты» (синие линии) — чем они управляются
> ` 228` 3. ГОРОДА, ДЕРЕВНИ, ЗАСТРОЙКА
> ` 252` 4. СВОБОДНЫЕ ЗОНЫ — где ограничения §3 НЕ действуют
> ` 277` 5. ДАННЫЕ И КАРТА
>   ` 319` 5.1 Рельеф: как высоты меняют вес
>   ` 374` 5.2 Запретные зоны, нарисованные пользователем
> ` 430` 6. ЧТО ПРОВЕРЯЕТ КОНТРОЛЬНЫЙ ПРОГОН
>   ` 449` 6.0а Эталон участка arh (ДЕЙСТВУЮЩИЙ)
>   ` 599` 6.0 Эталон участка plesetsk_wide (прежний, участок не активен)
>   ` 723` 6.1 ЧТО ПОЧИНЕНО, А ЧТО ОСТАЛОСЬ (серия правок августа 2026)
> ` 807` 7. ЧЕГО В МОДЕЛИ ПОКА НЕТ (осознанные упрощения)
>   ` 837` 7.1 ОГИБАНИЕ ПРЕПЯТСТВИЙ — в основном решено стоимостной тягой
> ` 905` 8. УСЛОВИЯ МОДЕЛИРОВАНИЯ — то, что задаёт человек
>   ` 911` 8.1 Главное различие: ЯКОРЬ ≠ УСЛОВИЕ МОДЕЛИРОВАНИЯ
>   ` 927` 8.1а Три случая работы и два режима датчика (заказчик 05.09.2026)
>   ` 953` 8.2 Правила
>   ` 971` 8.3 Что это значит для оптимальности
<!-- /ОГЛАВЛЕНИЕ -->

## 1. ДАТЧИКИ — где ставить нельзя и почему

| № | Правило | Значение | Где задано | Почему |
|---|---|---|---|---|
| 1.1 | ✓ **Не ставить на воду** и в буфер вокруг русла | буфер 250 м | `THREAT_WATER_BUFFER_M`, `threat_exclude_water` | датчик физически негде разместить; при этом вода остаётся «плюсом» для маршрута — двойная роль |
| 1.1а | **Вода — ЕДИНСТВЕННЫЙ запрет для датчика.** В городе ставить МОЖНО | — | `candidate_positions` | город закрыт для ПРОЛЁТА БПЛА, а не для размещения наземного оборудования — наоборот, там инфраструктура. Правило 3.2 прямо для того и обнуляет вес в городе, чтобы датчики от него не разбегались. Датчик в городской зоне на контрольном прогоне — НЕ нарушение |
| 1.2 | **Только в ЗОНЕ ПРОЛЁТА** (`route_area`), а не «между вылетом и целью» | огибающая по двум полям Дейкстры, учитывает ВСЕ точки входа и запас хода | `_place_by_routes`, `flight_envelope` | ⚠️ **изменено 29.08.2026.** Прежнее правило «доля пути 0…1 по оси A→B» писалось под ОДИН вход; при пяти точках ось строится через их среднюю точку, которой на местности нет. Замер: отсекались 588 позиций, из них **260 лежали в зоне пролёта** (выброшены зря), и оставались ~1 260 недостижимых; **5.5 %** точек маршрутов оказывались вне полосы. Теперь позиции **за целью РАЗРЕШЕНЫ**, если маршруты туда заходят (заход сзади в модели есть, правило 2.11а), и запрещены там, куда БПЛА не долетает |
| 1.3 | 🚫 **Якоря ОТКЛЮЧЕНЫ** (проба заказчика 29.08.2026): все позиции выбирает жадный алгоритм | код сохранён в комментарии `_place_by_routes` | `_place_by_routes` | прежде принудительно ставились два датчика — у входа и у цели, в 0.45·R внутрь коридора. При пяти точках входа правило выродилось: «вход» — средняя точка, до реальных от неё **1.2…23.7 км**, и якорь ловил **24 маршрута из 150 (16 %)**, то есть стерёг один вход из пяти. Плюс принуждение снимает гарантию жадного (правило 1.14). Замер после отключения: датчиков 20 из 20, засечка ≥ k **1.000 / 0.993**, бесполезных (не ловящих никого) — **0** |
| 1.3а | ✅ **УСЛОВИЯ МОДЕЛИРОВАНИЯ — это НЕ якоря** | датчики, заданные человеком | `manual_sensors`, `_place_multi_type` | тема вынесена в **отдельный раздел §8** (решение заказчика 05.09.2026): там правила целиком, различие с якорем и что это значит для гарантии |
| 1.4 | **Разнос между датчиками** | желаемый 1.6·R, минимум `threat_min_sep_frac`·R (0.75·R) | `_place_by_routes` | перекрытие зон ≤ ~10 %, датчики не кучкуются. Если при желаемом разносе не набирается N — ослабляется до минимума |
| 1.5 | ✓ **Не ставить бесполезные** (не ловящие ни одного пролёта) | третичный ключ в жадном выборе | `CoverageCache.greedy` | без него при насыщении критериев выбор брал первого кандидата по порядку массива — 4 из 12 датчиков уходили в угол карты |
| 1.6 | **Кандидаты — узлы сетки** | шаг `threat_cand_step_km` | `candidate_positions` | датчик нельзя поставить «куда угодно»: перебирать континуум невозможно, поэтому строится сетка кандидатных позиций, и жадный алгоритм выбирает из неё N лучших. Это шаг ДИСКРЕТИЗАЦИИ поиска, а не критерий качества |
| 1.6а | ✓ **Шаг считается ОТ РАДИУСА: половина R** | пересчитывается САМ при смене радиуса (пределы 0.5…6 км); введённое руками значение живёт до следующей смены R | `sync_cand_step` | **почему именно так:** когда шаг КРУПНЕЕ зоны обзора, между соседними кандидатами остаётся дыра шире, чем видит датчик, — лучшая точка просто недоступна для выбора. Раньше шаг был жёстко 3 км и подбирался под R = 12 км; после перехода на R = 2 км он стал крупнее радиуса в полтора раза. Замер (150 маршрутов): при R = 2 км и **пяти** датчиках засечка 96.7 % (шаг 0.5 км) → 82.7 % (3 км) → **17.3 %** (6 км). Чем шире обзор и чем больше датчиков, тем меньше значит шаг: при R = 6 км разницы нет вовсе, при N = 30 она появляется только на шаге 6 км. Половина радиуса — компромисс: при R = 2 км это 4 436 кандидатов и 0.8 с против 17 847 и 3.2 с у шага 0.5 км, а выигрыш последнего — доли процента |
| 1.7 | **Ставятся по МАРШРУТАМ, а не по весу карты** | — | `place_sensors` | вес показывает, где местность удобна для полёта вообще; ловить надо конкретные пролёты из точки вылета в цель. Замер: частота пролёта под датчиком выше средней в 2.0 раза, вес — лишь в 1.3 |
| 1.8 | Число, радиус, кратность МАЛЫХ | `threat_N` = 20, `threat_R` = 2.0 км, `threat_k` = 3 | `config.Params` | задаётся в окне «Датчики: параметры…». ⚠️ `threat_N` поднято 15 → 20 по требованию заказчика вместе с вводом второго типа; эталон §6 снят при 15. Числа записаны ИМЕНАМИ параметров, иначе сверка `check_numbers.py` их не видит. ⚠️ **`k` — не только строка отчёта, а порог насыщения в жадном выборе**: после `k`-й засечки маршрут перестаёт давать прирост. Замер 16.09.2026 (20 датчиков R = 2 км, одна выборка): при `k` = 1–2 ближайшая пара **6.08 км**, при `k` ≥ 3 — **4.00 км**, средняя кратность 5.20 → 5.95; число датчиков `k` не меняет. Таблицы — [замеры_моделирования](../замеры_моделирования/ЗАМЕР_16.09.2026_РАССТАНОВКА.md) |
| 1.10 | **ЧЕТЫРЕ ТИПА датчиков** (с 05.09.2026): типы 1–3 — малые, сеть по маршрутам; тип 4 — большие, щит у цели | `threat_N_big` = 3 шт., `threat_R_big` = 15 км (0 — большие выключены) | `config.Params`, `_place_big` | у типов разные задачи: малые перехватывают пролёты по коридорам, большие держат круговое наблюдение вокруг объекта. Считаются ОДНОЙ моделью на ОДНОЙ выборке маршрутов. Полная модель и взаимное влияние типов — [МЕТОДИЧКА_ДАТЧИКИ_ТИПЫ.md](../вкладка_3/МЕТОДИЧКА_ДАТЧИКИ_ТИПЫ.md) |
| 1.10а | **Типы влияют друг на друга РАЗНОСОМ, но не кратностью** | разнос между датчиками разных типов — полусумма радиусов пары; общей кратности нет | `_place_multi_type` | `CoverageCache` строится под ОДИН радиус: в нём заранее посчитано, что видит каждый кандидат, и при разных радиусах общим он быть не может. Поэтому проход идёт по типам, в порядке номеров, и кандидаты рядом с уже поставленными исключаются — типы не садятся друг на друга. Чего не хватает: датчик типа 2 не знает, что участок уже перекрыт типом 1 трижды. Осознанное упрощение (§7); полная связность — задача З2 [единого плана](../планы/7_ЕДИНЫЙ_ПЛАН.md) |
| 1.10б | **Шаг сетки кандидатов — по НАИМЕНЬШЕМУ радиусу работающих малых типов** | `auto_cand_step_km` = 0.5·min R, пределы 0.5…6 км | `sync_cand_step` | шаг, годный для крупного типа, для мелкого окажется грубым: дыра между кандидатами станет шире, чем тот видит. Замер при R = 2 км: засечка 96.7 % при шаге 0.5 км, 82.7 % при 3 км, 17.3 % при 6 км. Галочка «авто» снята (`threat_cand_step_manual`) — держится введённое значение |
| 1.14 | 🔴 **Гарантия (1 − 1/e) в нынешнем виде НЕ обоснована** — писать её без оговорки нельзя | ломают ДВА механизма: попарный разнос (не матроид) и большие датчики вне жадного. ⚠️ Якоря и фильтр «между входом и целью» из этого списка ВЫБЫЛИ: якоря отключены (1.3), фильтр заменён зоной пролёта (1.2) — оба 29.08.2026. ⚠️ **УСЛОВИЯ МОДЕЛИРОВАНИЯ (1.3а) в этот список НЕ входят**: заданная человеком часть решения — не самоограничение алгоритма, а ДРУГАЯ ПОСТАНОВКА задачи. Оптимизируется остаток при зафиксированной части, и гарантия обсуждается для него; на закреплённой части её нет и быть не может — там оптимума не искали. Это принято сознательно (заказчик 04–05.09.2026: «оптимальность падает, но нужно для функционала»). Третий механизм, добавленный 05.09.2026, — **типы с разными радиусами считаются по очереди**, а не одним проходом (§7) | `CoverageCache.greedy`, `_place_by_routes`, `_place_big` | **работа над расстановкой важна и не закончена, но ОТЛОЖЕНА решением заказчика 29.08.2026** (правки массовые, затрагивают ядро оптимизации). Почему результат при этом близок к оптимуму: потолок засечки достигается уже при 12 датчиках из 20, функция насыщается на 4-м шаге, кандидатов в 164 раза больше, чем датчиков. Разбор — [архив, план 6](../архив/6_ПОПРАВКИ_В_МОДЕЛЬ_ДАТЧИКОВ.md), действующая задача — [З2 единого плана](../планы/7_ЕДИНЫЙ_ПЛАН.md), объяснение с числами — [МЕТОДИЧКА_ДАТЧИКИ_ТИПЫ §6б](../вкладка_3/МЕТОДИЧКА_ДАТЧИКИ_ТИПЫ.md) |
| 1.11 | **Большие ставятся КОЛЬЦОМ вокруг цели, с разносом по УГЛУ** | кольцо `threat_big_ring_lo…hi` = 0.55…1.0 от `R_big`; сектора 360°/N, датчик — на оси сектора | `_place_big` | «прикрывают цель со всех сторон» — это про НАПРАВЛЕНИЯ, а не про площадь. Жадный отбор по маршрутам собрал бы все три на одном пучке. Замер: углы 33°/146°/270° при трёх датчиках (мин. 114° при идеале 120°), 44°/130°/227°/319° при четырёх |
| 1.12 | **Позиция большого мерится ПЛОТНОСТЬЮ пролётов**, а не числом маршрутов | `route_density_field` в зоне обзора | `_place_big` | при R = 15 км «сколько маршрутов накрывает» ничего не различает: замер дал 250 из 250 маршрутов у всех 459 кандидатов кольца, и выбиралась первая позиция по порядку сетки — углы выходили 25° вместо 120° |
| 1.13 | **Зоны больших слегка ПЕРЕКРЫВАЮТСЯ — это осознанно** | при 120° и кольце ≈ `R_big` расстояние 1.73·`R_big` | — | полное непересечение требует > 2·`R_big` = 30 км между центрами, и тогда цель оказывается вне зон всех трёх. Выбрано «минимальное перекрытие при цели под наблюдением» |
| 1.9 | **Пересчёт по ходу итераций** | каждые `THREAT_SENSOR_REFRESH_EVERY` = 5 накопленных маршрутов | `_maybe_refresh_sensors` | раньше пересчёт был только в конце прогона, и датчики казались неподвижными. Видно, как расстановка сходится: сдвиг между обновлениями падает с ~5 км до нуля к 30-й итерации |

---

## 2. МАРШРУТ БПЛА — куда лететь можно и нельзя

> **О нумерации.** Сначала таблица правил 2.1–2.14, затем три подраздела: пояснение к
> правилам 2.9–2.12, **§2.0** (тяга к цели по стоимости, правила 2.0.x) и **§2.1**
> (синие линии, правила 2.1.x). Номера 2.0 и 2.1 у подразделов исторические — на них
> ссылаются другие методички, поэтому не переименовывались; с одноимённым правилом 2.1
> из таблицы они не связаны.

| № | Правило | Значение | Где задано | Почему |
|---|---|---|---|---|
| 2.0а | **Точка появления — из СЕКТОРА направления**, если он задан | ось «цель → клик по краю», по `THREAT_SECTOR_HALF_DEG` = **30°** в каждую сторону (полный раствор 60°); с 16.09.2026 раствор — поле `Params.threat_sector_half_deg`, правится в окне «Сектор налёта» и применяется сразу | `set_sector`, `sector_half_deg`, `compute_entry_points` | известно не «откуда летит», а откуда МОЖЕТ прилететь. **Клик — ЦЕНТР сектора**, а не его край: пользователь указывает направление и ожидает его серединой. Вершина — в цели, поэтому смена цели пересчитывает сектор. Сектор не задан — вход один, метка участка (прежнее поведение). Режим кликов не одноразовый: щёлкать можно сколько угодно раз, правая кнопка — выход и закрытие окна (заказчик 16.09.2026) |
| 2.0б | **Точек входа пять, отбираются по ПЕРСПЕКТИВЕ ДВИЖЕНИЯ** | `THREAT_SECTOR_ENTRIES` = 5 (с 16.09.2026 — `Params.threat_sector_entries`, окно «Сектор налёта»), полоса края `THREAT_SECTOR_EDGE_KM` = 1 км (у круглого района — кольцо той же ширины у окружности); вес — среднее по окну `THREAT_SECTOR_WIN_ALONG` × `THREAT_SECTOR_WIN_ACROSS` = 5 × 3 ячейки по курсу к цели | `compute_entry_points`, `_entry_window_score` | ⚠️ **изменено 29.08.2026.** Вес приграничной ЯЧЕЙКИ отражает лишь «попал ли край карты на дорогу»: у половины кандидатов он ровно 0, а лучшая по ячейке точка (29.1) по перспективе только 13-я — впереди у неё пусто. Совпадение первых пятёрок двух рейтингов — 2 из 5. Замер: перспективность отобранной пятёрки **21.9 → 28.1 (+28 %)** |
| 2.0б″ | **Точка входа стоит на ВЕСОВОЙ ячейке** (вес > 0) | `compute_entry_points` | `has_w` в отборе | ячейка с нулевым весом — чистое поле без единого ориентира: БПЛА неоткуда там взяться и незачем туда идти. Прежний отбор мог поставить точку ровно на такую (перспектива по окну хорошая, а сама ячейка пустая). Замер: из 174 проходимых и достижимых кандидатов края весовых **52** — условие отсеивает 70 %, но перспективность пятёрки падает лишь с 41.7 до 40.7. Если весовых ячеек на краю нет вовсе, условие снимается |
| 2.0б′ | **Разнос точек входа — по расстоянию И по углу** | `THREAT_SECTOR_MIN_SEP_KM` = 1 км, `THREAT_SECTOR_MIN_ANGLE_FRAC` = 0.3 от идеального шага (раствор / число точек) | `compute_entry_points` | одного километрового минимума мало: замер при 1 км без углового условия дал **четыре точки из пяти в одном углу** (разброс 33.8 км против 43.2), то есть пять вариантов появления выродились в один. Угловое условие разводит их по направлениям, и тогда километровый минимум может быть маленьким. Если край беден, условия ослабляются по очереди: сперва угловое, затем километровое — пять точек рядом лучше, чем три с идеальным разносом |
| 2.0б′ | **Точка входа обязана быть достижимой** | одно поле Дейкстры от цели по `passable_mask` | `compute_entry_points` | из точки, откуда до цели нет пути по коридорам, БПЛА не полетит: это не вариант появления, а дырка в выборке — каждая попытка старта оттуда провалилась бы. Недостижим весь край сектора — точки всё равно отбираются (сектор не должен молча исчезать), но маршрутов не будет, и это видно по показателям |
| 2.0в | **Старт разыгрывается РАВНОМЕРНО** между пятью точками | `sample_one_route` | `ctx["entries"]` | вес местности учитывается при ОТБОРЕ точек, а не при выборе старта: иначе выборка вырождается в один вход. При неизвестном нарушителе равные шансы честнее |
| 2.0г | **Пауза повтора точки входа** — по номеру | `THREAT_SECTOR_ENTRY_COOLDOWN` = 2 (**0 = выключено**); при разнесённых точках поверх неё работает пауза по углу (2.0ж) | `sample_one_route`, история в `ctx["_recent_entries"]` | равные шансы сами по себе дают серии («выпало трижды подряд»), и на коротком прогоне часть точек не отрабатывала вовсе. Замер на 40 розыгрышах: повторов подряд 0, через один 0, отработали все 5 точек. Если доступных точек не осталось (недостижимые), пауза снимается — маршрут важнее ровности чередования |
| 2.0д | **Район радиусом** — круг вокруг цели ✅ 16.09.2026 | клик — центр (он же цель, пока цель не указана), клик — точка окружности, колёсико — принять; не меньше 1 км (две ячейки) | `set_area_circle`, `in_area`, `ThreatGrid.set_outside_mask`, `passable_mask`, `_open_endpoints`, `candidate_positions`, `_clip_layers_circle` | сетка 500 м бывает только квадратной, поэтому считается описанный квадрат, а за окружностью: **вес 0, пролёт запрещён, датчики не ставятся, слои обрезаны**. Маска вычитается ПОСЛЕДНЕЙ — и из проходимости, и после открытия концов: иначе буфер (1 км) и открытие концов (3 км) выводили маршрут наружу; замер до правки — область залёта за кругом R = 25 км на **249 ячеек, до 3.4 км**, после — 0. Демо-точка входа участка без сектора проецируется на окружность: в 163 км от круга маршрута не было. Переключатель «район / радиус» меняет только форму СЛЕДУЮЩЕГО района |
| 2.0е | **Сектор по всем 360°** — «Построить сектор» ✅ 16.09.2026 | край делится на доли `THREAT_SECTOR_AUTO_STEP_DEG` = **10°**; от доли — лучшая точка по перспективе (2.0б); из них N с угловым разносом (2.0б′), идеальный шаг 360° / N | `set_sector_auto`, `compute_entry_points` (`sector_kind == "auto"`) | лучшая точка на КАЖДУЮ долю, а не N лучших по краю: иначе все точки собрались бы на одной «горячей» стороне. Замер (круг R = 35 км, 12 точек): азимуты от 42° до 360°, наибольший промежуток 44°. Работает и в прямоугольном районе |
| 2.0ж | **Пауза повтора по УГЛУ** ✅ 16.09.2026 | старт не берётся в зоне шириной `THREAT_ENTRY_COOLDOWN_DEG` = **45°** (±22.5°) вокруг стартов `THREAT_ENTRY_COOLDOWN_ROUTES` = **3** последних ДОШЕДШИХ маршрутов; включается при разносе точек шире (3 + 1) × 45° = **180°** | `sample_one_route`, `_azimuth_span`, история в `ctx["_recent_ok_entries"]` | заказчик: «не повторялись в течение 3 след маршрутов в районе 45 градусов». Порог 180° — в секторе 60° правило запретило бы всё; так эталоны `reference_run` и `motion_check` сошлись без пересъёмки. По дошедшим, а не по попыткам: по попыткам **28 нарушений** зоны на 150 маршрутов, по дошедшим — **0**, старты по всем 12 точкам (6…17). Нет точек вне зон — остаётся пауза по номеру (2.0г) |
| 2.0з | **Точки старта, заданные человеком** ✅ 16.09.2026 | кнопка «Точки старта»: двойной клик — добавить, щелчок — выбрать, тащить — перенести, DELETE — удалить; только внутри района | `add_manual_entry`, `move_entry`, `remove_entry`, `_compose_entries`; `threat_view.py` :: `_begin_edit_entry` | входные данные, как датчики человека: переживают пересчёт сектора, смену цели и пересборку; убираются сменой района и «Сбросом». Перенесённая точка сектора становится ручной — иначе пересчёт вернул бы её на место. После каждой правки карта пересобирается (свободные зоны у точек входа): с кэшем чтения **0.05 с** |
| 2.1 | ✓ **Не длиннее запаса хода** | `threat_L_max`; авто = max(\|AB\|·1.25, кратчайший·1.35), при секторе \|AB\| берётся до САМОЙ ДАЛЬНЕЙ точки входа | `sync_auto_L_max`, `THREAT_LMAX_*` | физическое ограничение топлива/заряда. По дальней точке — иначе из неё маршрут не уместился бы и она молча выпадала бы из выборки |
| 2.2 | ✓ **Не выходить за область залёта** | огибающая по двум полям Дейкстры; при секторе — ОБЪЕДИНЕНИЕ областей по всем точкам входа | `flight_envelope` | всё, что вне, недостижимо при данном запасе хода. Проверяется и после сглаживания линии. Объединение, а не пересечение: БПЛА появится из какой-то одной точки |
| 2.3 | ✓ **Не проходить через город** | маска `_urban` | `apply_settlements` | над плотной застройкой БПЛА не идёт (см. §3) |
| 2.4 | **Пустое место перелетается, если короче разрыва** | `threat_max_gap_km` = 2 км | `passable_mask` (ЗАМЫКАНИЕ: дилатация + эрозия) | между коридорами бывают провалы; короткий БПЛА перелетит, длинный — коридор разорван. Замыкание закрывает провалы, НЕ расширяя сам коридор — за ширину отвечает отдельное правило 2.13. Управляет и областью залёта, и набором маршрутов ещё ДО итераций |
| 2.5 | **Плавность: длина прямого участка** | `threat_turn_interval_km` = 8 км | `sample_one_route` | маршрут не дёргается на каждой ячейке |
| 2.6 | **Радиус разворота** | из скорости и крена: `threat_speed_kmh` 180 км/ч, `threat_bank_deg` 25° | R_min = V²/(g·tan φ) | физика полёта |
| 2.6а | ✓ **Поворот за шаг ограничен физически** | `TURN_MAX_DEG` = 45° (**180 = выключено**) | `_walk_field` | R_min = 0.55 км: на сетке 0.5 км поворот на 45° даёт радиус 0.65 км — так аппарат может, на 90° даёт 0.35 км, на 180° это разворот на месте. Раньше это не запрещалось, а лишь получало малый вес (0.05 против 14 у «прямо») — и набегало: **88 % маршрутов имели поворот круче 90 %**, 7.2 % узлов были круче физики. Стало 0.7–2 % и 1.4 %. ⚠️ Сравнение идёт с допуском 0.03: курс хранится с точностью 0.001, у диагонали выходит 0.707 против точного cos 45° = 0.70711, и без запаса поворот ровно на предел отсекался — строилось 7 маршрутов из 150 |
| 2.6б | ✓ **Разворот за короткий участок** | `TURN_WINDOW_KM` = 3 км, `TURN_WINDOW_DEG` = 90° (**0 = выключено**) | там же | предела на один шаг мало: четыре шага по 45° складываются в разворот на 180°, формально не нарушая радиуса. На карте это «пошёл вперёд, потом назад, потом снова вперёд» — было у 84 % маршрутов, стало у 19–23 %. ⚠️ Правило **не жёсткое**: снимается, если иначе прохода нет. Жёстким оно быть не может — маршрут, отвернувший вбок при большом запасе хода, обязан когда-то повернуть к цели, и запрет оставлял его в тупике: при L = 150 км доходило 10 маршрутов из 150 |
| 2.6б′ | ✓ **Запретный сектор позади своей трассы** (план 11, 15.09.2026) | `BACK_SECTOR_KM` = 2 км, `BACK_SECTOR_DEG` = 140° (**0 = выключено**) | `_back_sector_bad`, `_walk_field` | предложение заказчика: петля складывается из разрешённых поворотов, поэтому запрет ставится на МЕСТО — у каждой пройденной точки сектор назад, в который следующие шаги не заходят; вернуться к своей трассе можно только дугой шире 2 км. Не жёсткое: снимается на шаг, если иначе прохода нет. ⚠️ Сам по себе почти не работал (обратный ход 14.9 → 10.8…15.2 % при r = 1…5 км, снятий 0.8–4.9 на попытку) — в паре с правилом 2.11в даёт 5.7 → 4.2 % |
| 2.6в | **Виляние согласовано с поворотом** | `_TAU_FREE` = 0.20 (было 0.40) | `_spend_tau` | аппарат, который не может развернуться круче 45° за шаг, не должен и вилять так, будто может: он уходил вбок шире, чем успевал плавно вернуться, и попытка отбраковывалась — доходило 104 из 150. Разброс по карте держится не на этом числе, а на выборе VIA, поэтому снижение по нему бьёт слабо (покрытие 25.4 % против 26.4 %) |
| 2.6г | **Прямой участок между поворотами — ОТВЕРГНУТ** | `TURN_MIN_STRAIGHT_KM` = 0 | там же | мысль здравая (прямая между доворотами расширяет дугу), но правило конфликтует с весовой картой: коридоры не прямые. Замер при 2 км — доходимость 71 из 150, вес под маршрутом 16.4 → 11.9, покрытие 25.8 → 21.8 %. Оставлено в коде выключенным, чтобы не изобретать заново |
| 2.6д | **Скругление РИСУЕМОЙ линии** | `SMOOTH_ROUNDS` = 4, `SMOOTH_MAX_SHIFT_KM` = 0.25 (**0 = выключено**) | `_round_corners` | дуга радиусом 0.65 км физически допустима, но выглядит угловато. Расчётный путь по ячейкам НЕ трогается: точка подтягивается к середине соседей, но не дальше полуклетки от расчётной линии, после чего заново проверяются коридор и вес. Медиана угла 10.5° → 5.7°, медиана радиуса 3.55 → 5.67 км, при этом покрытие и вес не изменились |
| 2.6е | **Сглаживание не имеет права уводить с ВЕСОВОЙ КАРТЫ** | `SMOOTH_WEIGHT_KEEP` = 0.97 | `_smooth_safe` | раньше проверялась только проходимость, и кривая могла срезать угол через пустошь, оставаясь формально законной: показывали не тот путь, который посчитали. Теперь сглаженная линия обязана сохранить вес расчётной ломаной, иначе допуск упрощения ужимается |
| 2.7 | **Сглаживание не имеет права срезать угол** через город или за область | подбор допуска | `_smooth_safe` | рисуется и считается по СГЛАЖЕННОЙ линии; раньше она срезала углы в застройку (828 ячеек вне области, 2721 точка в непроходимом) |
| 2.8 | **Профиль расхода запаса хода** | `late` / `early` / `even` | `threat_spend`, `_spend_tau` | БПЛА может вилять, пока есть запас, а на исходе идти прямо — или наоборот. ⚠️ **`late` исправлен 15.09.2026** (план 11): фаза была всегда 1, и при большом запасе `late` вилял весь путь, как `even` (запас 129 км: извилистость 1-й / 3-й трети 1.195 / 1.598 против 1.200 / 1.596). Теперь фаза `1 − доля пройденного`, разница «late − early» по третям +0.050 / −0.055 |
| 2.9 | ✓ **Не уходить назад от старта** дальше лимита | `START_BACK_KM` = 1 км | `_walk_field` (по `axis_s`) | немного назад в начале нормально — БПЛА отходит от точки вылета и ищет коридор; дальше это уже «полетел не туда» (было до 9.8 км, на карте — полоса в обратную сторону) |
| 2.10 | ✓ **У цели не отворачивать** от неё | зона `FINAL_APPROACH_KM` = 10 км, допуск `FINAL_DRIFT_KM` = 0.25 км за шаг | `_walk_field` (по `dgoal`) | подошёл к объекту — заходит на него, а не разведывает местность (было: 19 % маршрутов, подойдя на 15 км, снова удалялись до 5.3 км). Обход по дуге разрешён — запрещено именно удаление |
| 2.11 | **Заход ЗА цель с возвратом — РАЗРЕШЁН** | VIA до `VIA_BEYOND_KM` = 10 км за целью | `build_iter_context` | облететь объект и зайти с другой стороны — штатный манёвр, а не ошибка |
| 2.11а | ✓ **Заход сзади — ОБХОДОМ, а не насквозь** | радиус обхода `FINAL_BYPASS_KM` = 4 км; VIA вплотную за целью отодвигается за него (`VIA_BEYOND_PUSH`) | `_walk_field(bypass_km=…)`, `build_iter_context` | сетка VIA ставила точку в **2.5 км за целью на самой оси**. По «дальности обхода» (расстояние до отрезка вход→цель) она выглядела САМОЙ ЦЕНТРАЛЬНОЙ — 2.5 км против 5.7 у следующей, — и через неё шло **29 % маршрутов**. Каждый протыкал цель, отходил на 2.3 км и возвращался: на карте это «хвост», торчащий из объекта. При этом заход сзади был **фикцией**: с обратной стороны (> 90°) приходило 3 маршрута из 150, остальные 41 просто проскакивали цель. Теперь маршрут к такой точке огибает объект не ближе 4 км, а сама точка отодвинута за круг. Замер: заходов сзади 3 → 33 из 150, отворот у цели 0.32 → 0.15 км (макс 2.18 → 1.41). На запасе хода 150 км, где дефект был виднее всего, макс 14.48 → 8.15 км |
| 2.11б | ⚠️ **Поля до «задней» VIA считаются по той же урезанной маске** | `pas_via` в `build_iter_context` | там же | иначе поле расстояний ведёт маршрут прямо на цель, а шаг туда запрещён правилом 2.11а — участок упирается в тупик, и заход сзади **перестаёт строиться вовсе** (проверено: 0 % вместо 21 %). Урезать маску для ВСЕХ VIA нельзя: обычным маршрутам она закрывает коридоры у самой цели, вес под выборкой падает на 12 % |
| 2.11в | ✓ **VIA достигается мягко** (план 11, 15.09.2026) | `VIA_REACH_KM` = 3 км (**0 = прежнее: строго в ячейку**) | `_walk_field(reach_km=…)`, `sample_one_route` | участок к VIA обязан был войти в саму её ячейку, а следующий сразу шёл к цели: VIA в стороне давала шпильку «дошёл — развернулся — назад по своей трассе». Медиана места разворота была 63 % пути, у цели лишь 7–12 %. Теперь участок кончается в 3 км от точки, поворот к цели — дугой. Режим `mix`, запас 129 км: обратный ход 14.9 → 5.7 %, разворотов 24.4 → 15.0 %, доходимость 0.42 → 0.59 |
| 2.11г | ✓ **Облёт цели — отдельный критерий маршрута** (план 11, 15–16.09.2026) | уровни `THREAT_APPROACH_MIX`: обычный 85/10/5 · умеренный 60/25/15 · максимальный 30/35/35 · смесь (direct / flank / rear); пояса `THREAT_ORBIT_RADII_FRAC` = 6 / 20 / 35 / 55 / 82 % среднего расстояния вход→цель, **край — 100 %, точка входа** (с 11.5; было 110 %); доли поясов `THREAT_ORBIT_SECTOR_SHARE` = 10 / 26.7 / 26.7 / 26.7 / 10 % — одни на все режимы; перевес недобравшего варианта `ORBIT_QUOTA_CAP` = 2 маршрута | `_pick_approach`, `_quota_order`, `_orbit_pool`, `_orbit_chain`, `_chain_in_belt`, `_walk_chain`; панель — `threat_iter_approach` | правило 2.11 разрешало заход за цель, но не дальше 10 км, а к цели — только под уклон: в режиме «края» с обратной стороны заходило 0.0 %. Маршрут идёт через точку пояса на 60–120° и на 135–180° от стороны входа. ⚠️ **Два критерия — два свойства**: облёт решает КАК ЧАСТО («облёт» в выборке 17.9 / 17.1 / 17.1 % при разбросе центр / середина / края), разброс — КАК ДАЛЕКО: ближний или дальний ряд пояса (радиус 12.6 / 13.9 / 20.7 км). ⚠️ **Пояса в долях, а не в км**: кольца 10/18/28 км в районе 200 км жались бы к цели; доли пересчитаны по эталону, где D = 51.0 км. ⚠️ **Доли держатся среди ДОШЕДШИХ** (заказчик 16.09: «чтобы всегда процент такой был»): вес варианта = доля·(N+1) − дошедших; в 11.4 облёту «максимального» заказывали 35 %, выходило 24.9 %, теперь 35.0 %, доли поясов — ±1 п.п. Точка отхода — строго в выпавшем поясе, точка захода с обратной стороны — в нём или ближе; не по силам в поясе — следующий по тем же весам, ни в одном — обычный заход. Замер (разброс «смесь», две выборки): с обратной стороны 5.4 / 10.8 / 23.2 % по уровням, разнообразие захода 0.83 / 0.90 / 0.92 |
| 2.12 | **VIA-точка не позади старта и не «сбоку у цели»** | позади ≤ `VIA_BEHIND_KM` = 1 км; в 10 км от цели — только если ЗА целью | `build_iter_context` | VIA позади старта уводила маршрут в обратную сторону, VIA сбоку у цели заставляла дёрнуться в пяти километрах от объекта |
| 2.13 | **Уход от ориентира вбок** — отдельный параметр | `threat_corridor_slack_km` = 1 км | `passable_mask` | раньше «разрыв» задавал И длину перелёта через пустоту, И ширину ухода от ориентира (буфер = разрыв / 2) — настроить одно, не испортив другое, было нельзя. Для обхода холмов нужен широкий буфер при неизменной сшивке. Значение 1 км выбрано так, чтобы поведение по умолчанию не изменилось. Проходимо: 61 % участка при 0 км, 78 % при 1 км, 85 % при 2 км |
| 2.15 | **Пакетная генерация обязана набрать заказанное T** | попыток до `T · 8`; «давно нет нового» — `max(350, T)` | `iter_batch` | ⚠️ **исправлено 29.08.2026.** Стоял жёсткий потолок `min(T·6, 800)`: при `threat_iter_routes` = 400 попыток хватало лишь на **234** маршрута, генерация молча обрывалась, и на карте показывалось столько, сколько построилось. Причина не в дедупликации (замер: **0** отбраковок), а в доле отказов «не уложился в запас хода» — **71 %** попыток, то есть попыток нужно вчетверо больше, чем маршрутов. После правки: 150 → 150 (3.2 с), 300 → 300 (3.4 с), 400 → 400 (4.4 с), 600 → 600 (6.6 с) |
| 2.14 | **Предвидение препятствия** | `THREAT_LOOKAHEAD_KM` = 2 км, `THREAT_LOOKAHEAD_POWER` = **3.0** (**0 = выключено**; было 1.5 «на глаз», поднято по замеру — см. §6.1) | `_lookahead_fields`, `_walk_field` | горизонт решения был 500 м (одна клетка): узкий холм маршрут обтекал, а перед широким упирался — геометрическая тяга к цели пересиливала. Теперь при выборе соседа учитывается СРЕДНИЙ рельеф на 2 км вперёд по этому направлению. Среднее, а не минимум: минимум блокировал бы направление из-за одной плохой клетки |

### Пояснение к правилам 2.9–2.12: два понятия, без которых они непонятны

**Что такое VIA-точка.** Если строить маршрут просто «из старта в цель», все маршруты
лягут почти по одной линии — разброса не будет. Поэтому программа заранее раскидывает
по карте два десятка промежуточных точек («via» = «через») и строит путь в два участка:
**старт → VIA → цель**. Выбор VIA и задаёт разброс: ближняя к прямой даёт почти прямой
маршрут, дальняя — широкий обход и заход с другой стороны. Это и есть механизм за
списком **«разброс по карте»** в панели итераций (центр / середина / края / смесь).
Подробно — [МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md, «Как устроен разброс: VIA-точки»](../модель/МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md).

Отсюда и дефект: точки раскидываются по всей проходимой территории, и часть из них
оказалась **позади старта** и **за целью**. Маршрут честно шёл к такой точке — на карте
это выглядело как «БПЛА полетел не туда». Поэтому ограничивать пришлось не только само
движение (правила 2.9–2.10), но и отбор точек (2.11–2.12): позади старта — нельзя, за
целью — можно, потому что это осмысленный манёвр захода с другой стороны.

**Но «за целью» ещё не значит «в обход».** Разрешив заход сзади, легко получить его
карикатуру, что и произошло. Точка, стоящая **вплотную за объектом на его же оси**,
никакого обхода не задаёт: маршрут идёт к цели, протыкает её, отходит на пару
километров и возвращается тем же коридором. Формально это «заход с другой стороны», на
карте — торчащий из цели хвост, и именно его заказчик увидел на скриншотах.

Отличить одно от другого можно только геометрией, а не запретом: настоящий обход должен
**держать дистанцию** от объекта. Отсюда правило 2.11а — участок, ведущий к «задней»
точке, не приближается к цели ближе радиуса обхода, а сама точка отодвигается за этот
круг. Проверка по факту, а не по замыслу: считаем, **с какого сектора маршрут пришёл в
цель**. До правки с обратной стороны (> 90°) приходило 3 маршрута из 150 при 44
«заходах сзади» — то есть механизм почти всегда работал вхолостую. После — 33.

**Два разных расстояния до цели.** Их легко перепутать, и на этом я один раз ошибся:

| | Что это | Пример |
|---|---|---|
| **Прямое** (`dgoal`) | по линейке на карте, как видит глаз | 8 км |
| **По коридорам** (`gt`) | сколько реально лететь с учётом обхода городов, запретов и разрывов | 15 км |

Правило «рядом с целью не отворачивать» сначала было написано через расстояние по
коридорам. Из-за этого БПЛА в восьми километрах от цели по прямой считался «ещё
далеко» (по коридорам-то 15 км), ограничение не включалось — и на карте было видно,
как он отворачивает у самого объекта. После перевода правила на **прямое** расстояние
оно стало срабатывать там, где это видно глазу.

### 2.0 Тяга к цели — по СТОИМОСТИ, а не по километрам

| № | Правило | Значение | Где задано | Почему |
|---|---|---|---|---|
| 2.0.1 | **Спуск идёт по стоимостному полю** | цена шага = длина × `(1 + THREAT_COST_POWER · (1 − вес)) × (1 + THREAT_COST_RELIEF · (1 − рельеф))`, обе силы 1.0 | `_dijkstra_cost`, `_cost_mul` | поле расстояний считалось чисто геометрически и веса не знало вовсе; вес входил лишь в выбор клетки слабейшим множителем (×17 против ×280 у курса и ×10¹³ у геометрии). Замер: вес под маршрутом 11.60 → 22.42, точек по пустым ячейкам 35.5 → 13.4 %, длина НЕ выросла (102.2 → 101.7 км). ⚠️ Это замер ОДНИМ полем; действующие числа — в 2.0.8 |
| 2.0.1а | **Вес и рельеф — ДВА независимых сомножителя**, а не произведение с обрезкой | рельеф нормируется по своему диапазону `[THREAT_DEM_K_MIN, THREAT_DEM_K_MAX]` → 0 (гребень) … 1 (низина) | `_cost_mul` | сперва привлекательность считалась как `вес × рельеф`, обрезанное единицей, и обрезка съедала ВСЁ поощрение за низину: замер показал цену 1.000 и в низине, и на гребне — рельеф не влиял вовсе. Это независимая физика: вес — «где есть ориентиры», рельеф — «где меня видно». После разведения: низина 1.016, гребень 1.976. Маршруты: шагов по возвышенности 13.0 → 7.4 %, высота под маршрутом 77.8 → 70.3 м, вес при этом ВЫРОС 20.60 → 22.42 (замер ОДНИМ полем, до 2.0.8). Попутно перелёт через гребень перестал быть бесплатным: ячейки мостика на возвышенности теперь дорогие |
| 2.0.2 | **Запас хода — по честным километрам**, отдельным полем | `gt_geo` против `gt` | `build_iter_context`, `_walk_field(dgeo=…)` | физику подменять нельзя: «дорогой» обход не должен выглядеть как нехватка топлива. Смешение полей обрывало бы маршрут там, где он проходит |
| 2.0.3 | **Цена шага симметрична** | среднее множителя двух ячеек | `_dijkstra_cost` | иначе путь из A в B стоил бы иначе, чем из B в A, и поле зависело бы от того, с какой стороны считали |
| 2.0.4 | **Закрашенная область от стоимости НЕ зависит** | проверено побитово: 13 987 ячеек при любой силе | `flight_envelope` | «куда вообще можно долететь» — вопрос достижимости, а не выгоды. Стоимость меняет только линии вероятных путей |
| 2.0.5 | **Сила 1.0, а не больше** | `THREAT_COST_POWER` | `config.py` | дальше единицы вес растёт слабо, а разброс линий падает быстро: 1.0 → покрытие области 28.2 %, 2.0 → 19.7 %, 3.0 → 15.0 % при весе 20.18 / 21.68 / 23.26 (замер ОДНИМ полем, до 2.0.8; сам вывод «1.0, а не больше» остался в силе). 0.0 возвращает прежнее поведение в точности |
| 2.0.6 | **Размах приоритета по весу в выборе клетки** | `THREAT_WEIGHT_GAIN` = 4.0, то есть ×41 (было 1.6 → ×17) | `_mode_corridor_pref` | стоимость задаёт СТРАТЕГИЮ (куда идти), это — ТАКТИКУ (какого соседа взять). Замер: САМО ПО СЕБЕ, без стоимости, усиление бесполезно (вес под маршрутом 11.60 при ×17 и 11.64 при ×101 — тонет в геометрии). Поверх стоимости даёт +9.4 % к весу и −3 п.п. точек по пустым ячейкам; при ×101 вес уже падает, а доходимость рушится с 55 до 40 %. 1.6 возвращает прежнее поведение |
| 2.0.7 | **Проверка на реальной трассе** | вход Северодонецк → цель Луганск | разовый замер (скрипт не в репозитории) | оптимальный путь по километрам: 79.4 км, средний вес 13.41, по реке 13 ячеек из 128. По стоимости: 88.4 км, вес **26.83**, по реке **102 из 146** — маршрут идёт руслом Донца через Славяносербск. Крюк 9 км при удвоенном весе, в запас хода укладывается |
| 2.0.8 | **Несколько полей «жадности», иначе выборка вырождается** | `THREAT_COST_MIX_STEPS = (0, 0.35, 0.7, 1.0)`, выбор смещён показателем `THREAT_COST_MIX_BIAS` = 0.8 | `build_iter_context`, `sample_one_route` | одного стоимостного поля мало: если путь A дешевле B на десятки единиц, локальная стохастика туда не уведёт, и ВСЕ 250 линий сливаются в одну полосу (покрытие области падало с 47 до 26 %). Каждому маршруту достаётся своё поле: `a≈1` жмётся к коридорам, `a≈0` летит напрямик. Замер: покрытие вернулось к 43.1 %, вес 14.89 против 11.80 у чистой геометрии |
| 2.0.9 | **Поля ПРЕДРАССЧИТЫВАЮТСЯ, а не смешиваются линейно** | своя Дейкстра по множителю `1 + a·(mul − 1)` | там же | смесь двух готовых полей расстояний полем расстояний НЕ является: где по километрам выгодно влево, а по стоимости вправо, появляется локальный минимум-ловушка — спуск застревает и маршрут не доходит. Замер: набиралось **128 итераций из 150**; с предрассчитанными полями — 150 из 150 |
| 2.0.10 | **Пороги в километрах приводятся к масштабу поля** | `cost_scale` — средний множитель цены по проходимым ячейкам | `_walk_field` | «температура» `tau`, допуск шага «назад» (0.8 км) и порог «дошли» (`h`) заданы в километрах, а поле стало в стоимости. Без пересчёта они молча ужимались во столько же раз, во сколько поле крупнее, — стохастика падала вчетверо |
| 2.0.11 | **«Пустые» ячейки под маршрутом — это ВИЛЯНИЕ, а не перелёты** | 75.8 % пути прямо по коридору, 24.2 % в буфере ≤ 1 км, **0 % дальше** | разовый замер (скрипт не в репозитории) | река шириной в одну ячейку (500 м), аппарат физически не идёт по осевой линии. На карте выглядит как «перепрыгивание», но маршрут держится русла. Убирать нельзя — путь станет неестественно точным. Усиление стоимости тут упёрлось: при `POWER = 3` доля пути по реке ПАДАЕТ с 23.4 до 16.0 % |
| 2.0.13 | **ДЕЙСТВУЮЩИЕ числа под маршрутом — здесь** (остальные в этой таблице мерились на промежуточных состояниях) | режим панели «максимальный вес»: вес **15.8**, точек по пустым **21.6 %**, покрытие области **36.5 %**. Режим по умолчанию «смесь»: вес **14.4**, пустых **27.1 %** | замер через `plan_routes`, 250 линий | числа 20…23, встречающиеся в правилах 2.0.1, 2.0.1а и 2.0.5, получены ДО введения нескольких полей жадности (2.0.8). Тот механизм намеренно СНИЗИЛ средний вес ради разброса: одно поле давало вес 21.3 при покрытии 18.8 %, несколько — вес 15.8 при покрытии 36.5 %. Мерить надо через `plan_routes` (то, что показывается), а не прямой выборкой — жадный отбор по покрытию берёт не самые «весовые» линии намеренно |
| 2.0.12 | **Рельеф учитывается ДВАЖДЫ — намеренно** | в весе карты (§4.5) и отдельным сомножителем цены (2.0.1а) | `add_relief_layer`, `_cost_mul` | в весе — «этот ОРИЕНТИР хуже», в цене — «САМ ПРОХОД дороже», причём второе действует и на ПУСТЫЕ ячейки, где веса нет. Плюс нормировка `w / p90` частично самовосстанавливается при общем понижении весов, и вклад рельефа в ней размывается. Замер трёх вариантов: в весе И в цене — 19.5 % пути по возвышенностям, только в весе — 20.9 %, только в цене — 21.0 %; разброс во всех трёх одинаков (34.0 / 33.1 / 34.6 %). Двойной учёт выигрывает, ничего не теряя, и на разброс НЕ влияет |

### 2.1 «Возможные маршруты» (синие линии) — чем они управляются

| № | Правило | Значение | Где задано | Почему |
|---|---|---|---|---|
| 2.1.1 | **Приоритет по весу — ИЗ ПАНЕЛИ**, как у итераций | `THREAT_ROUTE_MODE_SRC` = "panel" | `plan_routes` | было жёстко "mix", а "mix" разыгрывает режим КАЖДОМУ маршруту — треть синих линий строилась в режиме «приоритет МИНИМАЛЬНОГО веса», целенаправленно уходя с тяжёлых коридоров, и выбор в панели на картину не влиял вовсе. Замер: средний вес под линиями 10.24 → 11.74, доля точек по пустым ячейкам 40.9 → 36.5 % |
| 2.1.2 | **Разброс остаётся веером** | `spread="mix"`, `spend="mix"` | там же | линии обязаны лечь по области широко: это показ ВОЗМОЖНОСТЕЙ, а не один сценарий. Веер даёт разные VIA-точки по всей области |
| 2.1.3 | **Жадный отбор — по ЦЕННОСТИ, а не числу ячеек** | ценность = `1 + THREAT_ROUTE_SPREAD_W · вес(0…1)`, W = 3.0 | `_spread_over_area` | считалось голое число задетых ячеек, и пустое поле стоило столько же, сколько река. Замер: вес под линиями 11.43 → 11.74, покрытие области 46.4 → 48.5 % (не упало — ценность ведёт отбор к длинным коридорам) |
| 2.1.4 | ⚠️ **По этой же выборке ставятся датчики ДО итераций** | `_sample_for_sensors` | `place_sensors` | пока итераций меньше пяти, датчики считаются по синим линиям — все свойства выборки переходят в расстановку |

---

---

## 3. ГОРОДА, ДЕРЕВНИ, ЗАСТРОЙКА

| № | Правило | Значение | Где задано | Почему |
|---|---|---|---|---|
| 3.1 | **ГОРОД: пролёт запрещён** | `place` = city/suburb/borough, либо town с пятном ≥ `THREAT_TOWN_CITY_KM2` (**2 км²**, с 29.08.2026; было 4) | `apply_settlements` | над городом коммерческий БПЛА не идёт. ⚠️ Число подобрано под участок и **временное**: на `plesetsk` при 4 км² городами были 2 пятна (1.8 км²), при 2 — 4 пятна (6.2 км²); на прежнем участке тот же порог даёт 5 175 ячеек городской зоны вместо 4 805. Замена — показатель по данным карты, [план 5 §9](../архив/5_ПЛАН_СЕКТОР_И_ДАТЧИКИ.md) |
| 3.2 | **В городе вес ВСЕХ слоёв = 0** (не минус) | `THREAT_CITY_ZERO_WEIGHT` | там же | отрицательный вес делал город «ямой», от которой разбегались датчики; запрет держит маска, а не знак веса |
| 3.3 | **Буфер облёта города** | `THREAT_URBAN_BUFFER_KM` = 0.4 км | там же | не лететь впритык. Держится малым: буфер растёт нелинейно и склеивает города в агломерацию (0.5 км → 17.7 % участка, 1.5 → 29.5 %, 2.0 → 34.9 %) |
| 3.4 | **ДЕРЕВНЯ: пролёт разрешён**, но вес падает к центру | −10 в центре, −5 на половине радиуса, 0 на границе. **Радиус влияния ≈ √площади пятна**, зажат в `THREAT_VILLAGE_RADIUS_MIN_KM` = 1 км … `THREAT_VILLAGE_RADIUS_MAX_KM` = 4 км | `THREAT_VILLAGE_*`, `_village_field` | село у трассы не должно рвать коридор, но над центром лететь незачем — маршрут сам идёт по окраине. Было −20/−10: при таком штрафе окраина села теряла вес наравне с центром. Радиус от размера пятна, а не постоянный: хутор и большое село влияют по-разному; зажим снизу не даёт крошечному пятну остаться без зоны, сверху — большому селу накрыть половину участка |
| 3.4а | **Деревня ОСЛАБЛЯЕТ ориентир, но не стирает** | пол `min(исходный вес, THREAT_VILLAGE_FLOOR = 1.0)` | `apply_settlements` | штраф фиксирован (−20/−10), а ориентиры весят мало (река +10, местная дорога +3) — без пола село уводило их в минус, то есть в полный запрет, вопреки правилу 3.4. Замер: 1019 ячеек с ориентирами теряли коридор только из-за штрафа, среди них 54 реки. После пола коридоров 9 944 → 10 950, компонент связности 79 → 44 (деревни перестали разрезать коридоры поперёк), градиент цел: центр 5.70 против края 10.17. Значение 0 возвращает прежнее поведение |
| 3.4б | **Собственная градация пунктов — по данным участка**, а не по тегу OSM | порог «не штрафует» = минимум площади × `THREAT_VILLAGE_SKIP_FACTOR` (1.2); на участке это 0.30 км² при медиане 0.50 и максимуме 42.75 | `settlement_stats`, `_village_size_filter` | тег `place` ненадёжен: одинаковые пятна размечены то village, то town, а хутор из трёх домов — тоже village. Пороги от распределения переносятся на другую местность, где масштаб застройки иной. Второй порог `THREAT_VILLAGE_TOWNLIKE_MED` («крупное = уже посёлок, не штрафует») выключен по умолчанию — сперва сверить глазом |
| 3.5 | **Хутор и мельче — не учитывается** | `place` = hamlet | `_classify_components` | одиночные дома на маршрут не влияют |
| 3.6 | **Река сквозь населённый пункт — коридор**, пока НП меньше порога | `THREAT_RIVER_CORRIDOR_MAX_KM2` = 20 км² | `apply_settlements` | по руслу БПЛА идёт даже через село; у крупного города русло закрывается вместе с ним |
| 3.6а | **Размер пункта меряется по ИСХОДНОМУ пятну застройки**, а не по связной городской области | `own_place_area_km2` (сырая `_built_cells`, без расширений) | там же | маска города склеивает соседние пункты трижды: обжитая зона (+1 км), общая маска контуров, буфер облёта (+0.4 км). Цепочка сёл вдоль реки давала **431 км² при 27 % застройки внутри**, и правило 3.6 не срабатывало никогда — река гасла на десятки километров (Нижнє, Бобровe, Комишуваха). После правки: река без веса 260 → 109 ячеек, все оставшиеся — в настоящих крупных пунктах. Из пятен, дотянувшихся до ячейки, берётся САМОЕ КРУПНОЕ: иначе хутор на краю города открыл бы реку в самом городе |
| 3.7 | **Запасной критерий, если метки `place` нет** | пятно застройки ≥ `THREAT_URBAN_MIN_AREA_KM2` (8 км²) | `_classify_components` | в OSM размечены не все НП |
| 3.8 | **Контур НП засчитывается только при реальной застройке внутри** | ≥ 15 % площади или ≥ 2 км² | `PLACE_BUILT_FRAC`, `PLACE_BUILT_MIN_KM2` | границы в OSM часто административные (сельсовет с полями): без проверки под запрет уходило 30 % участка, а соседнее село становилось «городом» |
| 3.9 | **Слишком большой контур игнорируется** | > `PLACE_POLY_MAX_KM2` (120 км²) | `_places_from_osm` | это район или община, а не населённый пункт |
| 3.10 | **Тип пункта — по данным, а не по тегу** | пара признаков «площадь застройки × плотность в замкнутом контуре»; пороги = мин/среднее/макс по каждой группе тега В ПРЕДЕЛАХ РАБОЧЕЙ ОБЛАСТИ; перевод при допуске ±15 % (`THREAT_PLACE_GRADE_TOL`) и согласии ОБОИХ признаков; **выборка меньше 5 пунктов — ни В этот класс, ни ИЗ него не переводится ничего**; один проход без итераций; исходный тег сохраняется в `_place_tag_raw` | `grade_places`, `place_profiles`; механика в [МЕТОДИЧКА_КАРТА_УГРОЗ.md §13.0г](../вкладка_3/МЕТОДИЧКА_КАРТА_УГРОЗ.md) | тег `place` ненадёжен: одинаковые пятна размечены то village, то town, хутор из трёх домов — тоже village. Пятно с долей застройки 27 % формально проходит порог «город» и запирает пролёт на сотнях км². Пороги от рабочей области переносятся на другую местность, где масштаб застройки иной. **На текущем участке настоящих городов 4** — меньше порога, поэтому класс `city` в градации не участвует и срабатывает один перевод; это не сбой, а честный ответ на малую выборку |
| 3.10а | **Пороги перехода — от СВОЕГО класса, симметрично**: вверх превзойти его максимум, вниз упасть ниже его минимума | `THREAT_PLACE_GRADE_STRICT_UP` = True | `grade_places.gate` | классы по тегу перекрываются почти полностью, и сравнение с ЧУЖИМ классом ошибается в обе стороны. Без строгого подъёма: 133 подъёма против 23 понижений, зона росла с 3441 до 3542 ячеек — «дотянуться до минимума city» могла рядовая деревня. При понижении по чужому классу разжаловались 16 настоящих городов, включая Северодонецк (17.25 км²), потому что максимум town = 23.50 км² |
| 3.10б | **Отсев выбросов ВЫКЛЮЧЕН** — он мешает правилу 3.10а | `THREAT_PLACE_GRADE_MODE` = "none"; режимы "mad" / "cascade" / "both" оставлены в коде | там же | порог подъёма берётся как максимум своего класса, а отсев выбрасывает крупные пункты как «выбросы» и сбивает этот максимум: планка `town → city` падала с 23.50 до 6.25 км². Замер: без отсева 0 подъёмов и 11 140 коридоров, с отсевом 12 подъёмов и 11 120. Каскад «город не меньше крупнейшего посёлка» дал ровно то же, что «без отсева»: он поднимает МИНИМУМ класса, а порог использует МАКСИМУМ класса снизу — разные концы распределения; сама мысль уже воплощена в правиле 3.10а |
| 3.10в | **Район города — не населённый пункт** | `city` = 4, `suburb`/`borough` = 3; лестница классов `PLACE_LADDER = (1, 2, 4)` | `PLACE_CODES` | раньше все три кодировались одинаково, и 62 района шли в класс «город» наравне с 8 городами — класс засорён на 89 %, его минимум падал до 0.50 км², настоящие города становились выбросами в собственном классе. Для запрета пролёта район по-прежнему город (проверка `>= 3`), но в статистику не входит. **Код города ВЫШЕ кода района**: тип пятна берётся как максимум кодов его меток, и при обратном порядке город с размеченными районами получал код района |
| 3.10г | ⚠️ **Кэш хранит уже закодированный тип** | `PLACE_CODES` применяется только при чтении `.osm.pbf` | `_places_from_osm` | после правки кодов обязательно пересобрать: `python tools/build_threat_grid.py geo_cache/<файл>.osm.pbf` (~20 с). Иначе новая кодировка не подействует, а класс с изменившимся кодом молча выпадет из градации |

---

## 4. СВОБОДНЫЕ ЗОНЫ — где ограничения §3 НЕ действуют

Запрет города нужен для **транзита** (не лететь над городом по пути), но не для взлёта
и подлёта: у старта БПЛА обязан быть, к цели обязан прийти. Обе точки, как правило,
стоят в населённых пунктах — без послабления вокруг них 100 % ячеек в радиусе 3 км
запрещены, и подлёт к цели возможен лишь с одной стороны.

| № | Точка | Что освобождается | Где задано |
|---|---|---|---|
| 4.1 | **Вылет** | круг `THREAT_ENTRY_FREE_KM` = 10 км вокруг точки, и только он | `_free_zone_mask` |
| 4.2 | **Цель** | круг `THREAT_TARGET_FREE_KM` = 3 км **плюс весь населённый пункт цели**, расширенный на 3 км | там же |

Почему у цели иначе: при цели в центре города круга мало — края НП остались бы
закрытыми, и подлёт снова стал бы односторонним. Почему у вылета НЕ так: расширение
всего НП на радиус зоны освобождало соседние города за десятки километров (без минуса
оказывались Лисичанск, Тошковка, Гірське — к точке вылета отношения не имеют).

В свободной зоне не действует **ничего** из §3: ни запрет пролёта, ни обнуление весов,
ни штраф деревни, ни буфер облёта соседнего города. Занимает ~9.5 % участка (678 км²).

> Зона привязана к цели, поэтому смена цели кликом **пересобирает карту**
> (`set_target` → `build`).

---

## 5. ДАННЫЕ И КАРТА

| № | Правило | Значение | Где задано | Почему |
|---|---|---|---|---|
| 5.1 | **Участок (ОБЛАСТЬ) берётся из файла `область.txt` своей папки** (с 13.09.2026; реестр `THREAT_AREAS` — запасной), по умолчанию — `arh`, «Архангельская обл. — обзор (Плесецк + 100 км)» | lon 37.9510–43.6800, lat 61.7099–63.9671 — **302.25 × 249.59 км по осям** (`bbox_size_km`, отсчёт от юго-западного угла); в текстах то же зовётся «291 × 250 км» — это ширина по СРЕДНЕЙ широте, число из комментария `config`. Рабочий район внутри — **117.27 × 72.98 км** (`work`) | `THREAT_AREAS`, `THREAT_AREA` → `THREAT_BBOX_LONLAT`, `THREAT_WORK_LONLAT` | смена куска карты одной строкой. ⚠️ Область — это ПОКАЗ и данные, район расчёта задаётся внутри неё (5.1к). Прежние записи остались: `plesetsk_wide` (39.8794–41.7516 / 62.5901–63.0869, 95×55 км), `plesetsk` (40.0842–41.5959 / 62.5717–63.1080, 77×59 км) и Северодонецк → Луганск (38.375–39.654 / 48.335–49.016, 94×75 км) — на последнем сняты замеры §6 |
| 5.1в | **Рамка участка УЖЕ данных на 2 км** с каждой стороны | границы из заголовка `.osm.pbf` минус `THREAT_AREA_PAD_KM` = 2.0 км | `THREAT_AREAS[...]["bbox"]`, памятка `THREAT_AREA_PAD_KM` | экстракт обрезан по своему прямоугольнику, и у кромки объекты приходят обрубками: дорога обрывается, контур посёлка теряет половину. Край считался беднее, чем он есть, а рамка шла ровно по срезу |
| 5.1г | **Якорь км-фрейма — юго-западный угол участка** (с 31.08.2026; было — центр bbox) | координаты 0…95.94 и 0…54.93 км, отрицательных нет | `bbox_anchor_lonlat` | «−12.4 км» ни о чём не говорит, пока не вспомнишь, что ноль посередине. На расчёт не влияет (всё в разностях), но **кэш `.npz` при смене якоря обязан быть пересобран**: в нём лежат готовые километры |
| 5.1а | **Участок = границы + слои + рельеф + подложка**, а не одна рамка | `.osm.pbf`, `.npz` и DEM — поля участка | `THREAT_AREAS[...]`, `layers_cache_path`, `dem_files` | смена рамки без пересборки `.npz` даёт пустую карту; чужой DEM молча описывает другую местность |
| 5.1б | **Файлы участка лежат в СВОЕЙ папке** `geo_cache/<dir>/` | поле `dir` записи участка | `area_cache_dir`, `area_file` | пока участок был один, всё лежало в корне; с двумя стало не понять, чей `dem.tif`. Корень — запасное место поиска: файл, положенный туда (и `.hgt`-тайлы), по-прежнему находится |
| 5.1д | **ЛЮБЫЕ данные участка — в папку участка**, включая тайлы подложки ✅ 03.09.2026 | новое пишется в `tile_cache/<dir>/<слой>/{z}/{x}/{y}`; поиск идёт в двух местах — сперва папка участка, затем старое общее `tile_cache/<слой>/…` | `tile_cache_path(…, area)`, `find_cached_tile(…, area)`, `fetch_tile_bytes(…, area)`; вкладка 3 передаёт участок через `_init_basemap(tile_area=…)` | правило то же, что 5.1б: участок должен переноситься одной папкой. ⚠️ **Старые 13 273 тайла НЕ перекладывали** (решение заказчика): они остаются в общей раскладке и находятся запасным поиском — ровно как файлы, брошенные в корень `geo_cache/` (5.1б). Перекладка ради порядка стоила бы разбора 210 МБ и дублирования краевых тайлов, а выигрыш даёт уже то, что **всё новое ложится правильно**. ⚠️ **Папка — по ОБЛАСТИ, а не по району расчёта** (5.1к): тайлы качаются на всю область один раз и годятся ЛЮБОМУ району внутри неё — район меняется мышью, подложка при этом не перекачивается. ⚠️ **Сборка `.exe` обязана брать ОБА места** и по рамке ОБЛАСТИ, а не района: заказчик внутри неё волен задать любой район, а взять один источник из двух — значит молча привезти дырявую подложку (журнал п. 243, [СБОРКА_EXE §0а](../сборка/СБОРКА_EXE.md)) |
| 5.1у | **Область выбирается ФАЙЛОМ, до импорта `config`, и открывается перезапуском** ✅ 13.09.2026 | `geo_cache/<область>/область.txt`: `область` (обязательно), `район`, `название`, `исходник`, `слои`, `рельеф`, `вход`, `цель`; кнопка «Область…» окна «Исходные данные» | `area_file.active_area`, `apply_saved_choice` (в `main_qt.py` до `import config`), `THREAT_AREA_SOURCE`, `THREAT_AREA_ERROR` | область читается константами при импорте ~30 мест модели и вида: смена на лету потребовала бы переделать их на аргумент. ⚠️ Проверочные скрипты память окна НЕ читают — иначе `reference_run` молча считал бы последнюю открытую область. ⚠️ Выбирается сам файл, а не папка: окно «Выбор папки» Windows файлов не показывает, и человек выбирал папку уровнем выше. Битый файл не принимается с причиной, район за краем области — тоже; формат — [план 10 §10.5.3](../планы/10_ПЛАН_ПРОЕКЦИЯ_И_ОСТАТКИ.md) |
| 5.1ф | **Папка тайлов выбирается отдельно — откуда показывать и куда докачивать** ✅ 13.09.2026 | корень кэша тайлов; внутри раскладка прежняя (5.1д); по умолчанию `tile_cache/` проекта | кнопки «Папка тайлов…», «Тайлы по умолчанию»; env `UAV_TILE_CACHE`; `geomap.cache_root`, `clear_tile_memory` | меняется СРАЗУ, без перезапуска: `cache_root` читается при каждом обращении, а ключ памяти тайлов корня не содержит — поэтому память при смене сбрасывается. Выбор запоминается (правило 04.09.2026). ⚠️ **ПРИ СМЕНЕ ОБЛАСТИ КОРЕНЬ ПОДБИРАЕТСЯ САМ** (правило заказчика 20.09.2026: «при выборе области заранее прописывать папку с тайлами по названию области»): память «тайлы» жила отдельно от выбора области — сменил область, корень остался от прежней, и подложка искалась не там. `tiles_root_for_area` смотрит по порядку: **нынешний корень** (если папка этой области в нём есть — выбор человека не трогаем), **проектный `tile_cache/`**, **`tile_cache` рядом с папкой области** (внешний диск). Ничего не нашлось — корень остаётся прежним. Через интерфейс его по-прежнему можно сменить, и этот выбор переживает смену области, пока тайлы там есть. ⚠️ **ВЫБРАННАЯ ПАПКА УЧАСТКА СЧИТАЕТСЯ КОРНЕМ** (`normalize_tiles_root`, 20.09.2026): человек видит в окне папку с именем области и выбирает ЕЁ, а программа добавляла имя участка второй раз — корень `tile_cache/arh` плюс участок `arh` давали `tile_cache/arh/arh/osm/z/x/y`, и докачка уходила в пустую вложенную папку (жалоба заказчика: «тайлы остановились, хотя была папка arh, я её сам довыбирал»). Теперь берётся родитель. ⚠️ Имя папки проверяется ПЕРВЫМ, а не «есть ли внутри папка участка»: прежняя докачка успела создать `tile_cache/arh/arh`, и по внутренней папке мусорный путь выглядел настоящим корнем — поймано проверкой. Правка применяется и к уже записанной памяти (`apply_saved_choice`), поэтому чинится без участия человека. Проверка — `area_check.py` раздел 5а, 9 проверок |
| 5.1х | **Область из кусков склеивается в один файл на вид данных, дубли на стыках выбрасываются** ✅ 13.09.2026 | рельеф: все `.tif` одной сетки → один растр; слои: все `.pbf` → один `.npz`; дубль — та же ломаная с точностью 1 м | `tools/pack_area.py` :: `merge_dem`, `build_part`, `merge_parts` | программа берёт ОДИН файл слоёв и ОДИН растр. ⚠️ Дорога через стык лежит целиком в обоих соседних `.pbf`: без выбрасывания дублей её длина вошла бы в вес дважды — на стыке ложная полоса коридоров. ⚠️ Сетки растров обязаны совпадать до пикселя — иначе сшивка сдвинет высоты, и скрипт отказывает с причиной. Исходники и части ищутся и в корне папки, и в `_части/` |
| 5.1е | **Имя участка в кэш тайлов передаётся АРГУМЕНТОМ, а не импортом** | `area=None` → старая общая раскладка (так работает вкладка 2 с районом Курска) | `geomap._area_dir`, env `UAV_TILE_AREA` | `view_qt/geomap.py` намеренно не зависит от `config` (только stdlib и numpy), а у вкладок разные районы: сделай участок глобальным — вкладка 2 искала бы курские тайлы в папке Плесецка. Участок входит и в ключ кэша в памяти: на общих зумах у разных участков совпадают `(z, x, y)`, и без него вкладка получила бы чужую картинку |
| 2.16 | **Маршруты НЕ считаются при построении карты** | считаются при расстановке датчиков (если выборки нет) и при старте итераций | `_work_place`, `iter_reset`; было — безусловный `plan_routes` после `build` | `plan_routes` — самая тяжёлая часть конвейера (два поля Дейкстры, огибающая, отбор путей), а запускалась она при КАЖДОМ построении: включили рельеф, сменили слои, поправили район. Замер 04.09.2026: построить карту 9.9 с, расставить датчики 16.0 с (маршруты считаются там), старт итераций 0.1 с, пакет 150 маршрутов 3.4 с |
| 5.1м | **Чтение слоёв и высот кэшируется по «район + источник»** | повторное построение 9.5 с → **0.0 с** | `ThreatModel._read_key`, `_drop_read_cache` | `build()` вызывается на каждый чих, а читал с диска заново: `.osm.pbf` 4.3 с + высоты 6.0 с, причём высоты читались даже при выключенном рельефе. Кэш сбрасывается вместе с районом и источником — иначе слои старого района молча лягут в новый |
| 5.1н | **Снятие района обнуляет ВСЁ, включая рельеф** | сетка, слои, веса, датчики, маршруты, `relief_on`, кэш чтения | `ThreatModel.clear_area`; очистка карты — в `_render_all` при `grid is None` | рельеф оставался включённым, и следующее построение шло «с рельефом» при кнопке, обещающей обратное, — с падением программы. А в отрисовке стоял голый `return`: данные обнулены, а весовая карта и датчики так и висели на экране |
| 5.1о | **Ноль осей переносится: угол района или угол области** | галочка «Отсчёт от угла района» в блоке ПОКАЗ; включается сама при задании района | `ShiftedAxis`, `_apply_axis_origin`, `set_area_rect` | человеку, работающему в районе 120 км внутри области 291 км, подписи «135…255 км» ни о чём не говорят — нужно «сколько от угла МОЕГО района». ⚠️ Сдвигаются **только подписи**: данные, сетка, датчики и координаты по клику остаются в общем фрейме, иначе появилась бы вторая система отсчёта. За пределами района подписи уходят в минус — это честно говорит «вы левее начала района». Галочка снята — ноль в углу области, все координаты положительные |
| 5.1п | **Отступ «вписать в окно» — доля кадра (3 %), а не километры** | стартовый вид — вся область; «Весь участок» — район | `frame_bbox(whole_area=…)` | отступ был завязан на предел отдаления `THREAT_VIEW_PAD_KM`; когда тот вырос с 5 до 100 км, он стал 100/120 = **83 % ширины** — вид отъезжал почти втрое, и на небольшом экране карта превращалась в марку посреди пустого поля. Доля одинаково уместна на любом экране и не зависит от размера района |
| 5.1к | **ОБЛАСТЬ ПОКАЗА и РАЙОН РАСЧЁТА — разные вещи** | показ: участок `arh`, 291 × 250 км (тайлы, камера); расчёт: прямоугольник внутри, кратный ячейке 500 м | `ThreatModel.set_area`, `clear_area`, `area_ready`; `config.THREAT_WORK_LONLAT` | сетка 500 м на всю область — 582 × 499 = 290 тыс. ячеек против рабочих ~34 тыс. Вне района не считаются ни слои, ни веса, ни рельеф, но подложка и своя карта видны: перемещение по области не ограничено. Задать район можно мышью (два клика + колёсико) или загрузкой своей карты — её рамка станет районом. **Пока район не задан, расчётные кнопки закрыты и рамка не рисуется**; «Убрать» обнуляет слои, датчики и маршруты |
| 5.1л | ⚠️ **Якорь км-фрейма НЕ меняется вместе с районом** | якорь — юго-западный угол ОБЛАСТИ показа, один на сеанс | `ThreatModel.__init__`, `set_area` | первая версия переносила якорь в угол нового района — и всё, что уже нарисовано в прежнем фрейме (тайлы, своя карта, рамка, подписи), оставалось на старых километрах: слои разъезжались с картой. Вторая беда того же корня: рамка, округлённая к ячейкам ДО смены якоря, после неё снова становилась дробной (46.00 км = 92 ячейки → 45.71 км = 91.4). Округление живёт в `set_area` и считается в конечных километрах |
| 5.1з | **Перевод «градусы ↔ км» — ОДНА формула на проект** | широта 110.574 км/°, долгота 111.320·cos(широты) км/° | `model/geo_frame.py`; `threat_grid` и `geomap` импортируют оттуда | формула жила двумя копиями (в модели и в представлении). Числа совпадали, но правка одной не дошла бы до другой, а расхождение проявилось бы не ошибкой, а тихим сдвигом карты относительно данных. ⚠️ Два РАЗНЫХ коэффициента (110.574 и 111.320) — действующее поведение, не опечатка: ими посчитаны километры в кэшах `.npz` и все числа эталона §6. Замена 110.574 на «среднее» 111.320 сдвинула бы карту на **0.37 км** при высоте участка 55 км — молча. Проверено 04.09.2026: после объединения формул контрольный прогон совпал с эталоном полностью |
| 5.1и | **Наружу координаты уходят в ГРАДУСАХ, внутри всё в км** | `sensors_lonlat`, `routes_lonlat`, `cells_lonlat` (центры ячеек), `km_to_lonlat_arr` | `model/threat_grid.py` :: `ThreatModel` | километры без якоря бессмысленны, а якорь меняется вместе с районом: файл, выгруженный на одном участке, на другом лёг бы со сдвигом. Для ячеек отдаются именно ЦЕНТРЫ — вес приписан центру, а не углу |
| 5.1ж | **Своя карта картинкой — ФОН под всеми слоями, в расчёт не входит** | слой Z = **−19**: сразу над тайлами (−20) и под всем остальным — рельеф (−9), веса (−8), вода (−7), застройка (−5.5), векторные слои (−5…−3), зоны (0.4), маршруты (1…5), датчики | `view_qt/map_image.py`; кнопка «Добавить карту (картинкой)…»; папка `geo_cache/карты_картинки/` | заказчик приносит готовую карту растром и работает по ней глазами, но веса по-прежнему считаются по векторным слоям — картинка не даёт геометрии. ⚠️ Сперва слой стоял на Z = −1 («поверх векторных слоёв», как звучало первое задание) — и закрыл собой ВСЁ, кроме датчиков и маршрутов: свои слои и весовая карта пропали (снимок заказчика 04.09.2026). Картинка заменяет ФОН, а не перекрывает результат. Пределы вида она не меняет: живёт в тех же километрах и ездит вместе с картой. Сканы прореживаются до 4000 px по длинной стороне (RGBA 10 000×10 000 — 400 МБ). ⚠️ Правило про привязку СНЯТО 05.09.2026 — см. 5.1р |
| 5.1р | **Привязка своей карты — четыре модели, выбор по данным** ✅ 05.09.2026 | сдвиг → подобие → прямоугольник → аффинная (поворот и перекос); модель выбирает `map_fit.choose_model` проверкой на отложенной точке | `view_qt/map_anchor.py` :: `MapAnchor`, `model/map_fit.py` | было (см. снятое правило 5.1ж): привязка ТОЛЬКО прямоугольная, поворот не поддержан. Замер на карте 17 944×13 220 по 89 пунктам: прямоугольник даже ИДЕАЛЬНО подобранный — 2 591 м максимум, потому что у карты перекос осей 2.4°. Аффинная модель — 404 м. ⚠️ Аффинная запрещена при охвате точек < 35 % по любой оси и при геометрии, для которой карта неправдоподобна (анизотропия > 1.7 или перекос > 4°) — иначе переобучение садится идеально на свои точки и врёт на всей карте (замер: невязка 85 м при ошибке рамки 200 км, `ПЛ_3_ЗВ` без исходных координат). Подробно — [МЕТОДИЧКА_ПРИВЯЗКА_КАРТИНКИ](../карты/МЕТОДИЧКА_ПРИВЯЗКА_КАРТИНКИ.md) |
| 5.1с | **Автоподбор находит опорные точки по застройке OSM**, а не по рекам | пятно `built_up` как шаблон, RANSAC; проверено: 89 точек / 127 м (детальная карта), 16 точек / 107 м но ⚠️ ненадёжно (обзорная карта, охват неполный) | `model/map_fit.py` :: `find_pairs`, `fit` | первая попытка — фазовая корреляция синей маски с реками — провалилась: болотная штриховка тоже синяя и её на порядок больше, острота пика 5.9 при нужных 10+, промах 30 км. Города дают компактные пятна, узнаваемые даже после сглаживания. ⚠️ Поиск БЕЗ каких-либо исходных координат (`--find`) НЕ РАБОТАЕТ — три попытки дали уверенный неверный ответ; рабочий путь — рамка на глаз по подписям карты, дальше подбор уточняет |
| 5.1т | **Векторная карта (SVG) — тот же механизм привязки, что растр** | `Z = −19`, та же `MapAnchor`; автоподбор рендерит SVG во временный растр | `view_qt/map_vector.py` | SVG не несёт координат — это рисунок в своих единицах, привязывать надо так же, как PNG. ⚠️ Не путать с векторными ГЕОДАННЫМИ (`.gpkg`/`.shp`, задел, привязка не нужна вовсе). Программа разбирает содержимое файла и предупреждает: если внутри растр (обёртка без выгоды) или ссылка на файл (`file:///…`) вместо встроенной картинки (`qt.svg: Could not create image` — пустой лист у получателя) |
| 5.2 | **Ячейка сетки** | 500×500 м (на активном участке 192×110 ячеек; на `plesetsk` было 154×119 = 18 326, на `severodonetsk` 189×151 = 28 539) | `THREAT_CELL_M` | компромисс точности и скорости |
| 5.3 | **Геометрия обрезается по участку** | +10 км запаса | `OSM_CLIP_MARGIN_KM` | объекты приходили до ±94 км и уходили за край карты; на границе строится точка пересечения с рамкой, а не внешняя вершина |
| 5.4 | **Мост — реальный объект OSM** | `bridge=yes/viaduct` | `OSM_LAYER_FILTERS` | раньше мост вычислялся как пересечение реки с дорогой и появлялся там, где его нет |
| 5.5 | **Маркер переправы — на самом мосту** | середина линии моста над водой | `bridge_points_km` | центр ячейки давал смещение до 250 м, заметное при приближении |
| 5.6 | **Рельеф: превышение над окружающей местностью**, не абсолютная высота | два масштаба: район 15 км и окрестность 2 км | `THREAT_DEM_*` | чем выше аппарат над местностью, тем заметнее (радиолокация, глаз, слух). Участок может лежать на плато, поэтому абсолютная высота бесполезна. Полное правило — §5.1 ниже |
| 5.7 | **Нет файла высот — рельеф не участвует** | `geo_cache/<участок>/<dem>.tif` или `*.hgt`; активный участок — `plesetsk/ARX_1_hh.tif` (высоты 27.9…203.7 м) | `load_dem_grid`, `dem_files` | программа работает и без рельефа; кнопка «Добавить рельеф» при этом неактивна |
| 5.8 | **Рельеф включается ТОЛЬКО кнопкой** | `THREAT_DEM_ON_START` = False | кнопка «Добавить рельеф», `ThreatModel.set_relief` | старый код включал рельеф молча, по одному факту появления файла в папке — карта менялась без ведома пользователя (коридоров стало 13 969 вместо 10 079). Теперь по умолчанию считается без высот, как раньше |
| 5.9 | **Высота ячейки — среднее по ячейке**, не один пиксель; растр читается **окном под районом**, не целиком (14.09.2026: Marshut 67.91 → 0.26 с) | ячейка 500 м ≈ 400 пикселей DEM | `_sample_geotiff`, `_block_mean_at` | брался ближайший пиксель, то есть 1 из 400: на обрывах расхождение со средним доходило до 42 м (медиана 1.4 м, 95 % — 6.4 м), а SRTM ещё и модель поверхности — пиксель мог попасть на крону дерева |
| 5.10 | **Предел держит ПЕРЕМЕЩЕНИЕ, а не масштаб** ✅ 19.09.2026 | кадр области и кадр района (поле 4 % кадра), расширенный до ближайшего УРОВНЯ ТАЙЛОВ, плюс поле перемещения `THREAT_PAN_MARGIN_KM` = **10 км** за краем | `_apply_view_limits`, `_zoom_out_frame`, `_fit_frame`, `_set_view_limits` | ⚠️ **ПРЕЖНИЙ ПРЕДЕЛ ЗАЖИМАЛ МАСШТАБ И ЭТИМ МЫЛИЛ ВСЮ ПОДЛОЖКУ.** Он был равен кадру области, а привязка к уровню тайлов просила вид шире: для `arh` в окне 1502 px нужен вид **420 км** (1:1 на z = 8), предел возвращал **380** — и подложка рисовалась с растяжением **1.145 на КАЖДОМ уровне зума** (замер 19.09.2026: 1.129 при запуске, 1.145 после «Весь участок» и после каждого щелчка колеса). Решение заказчика 19.09.2026: «при мин зуме главное, чтобы область поместилась целиком, а сколько лишней территории — не важно; при макс зуме выходить за рамки не страшно; держать перемещение до 10 км по углам». После правки масштаб **1.000** на всех уровнях, область видна целиком (вид 435 км при области 302). ⚠️ **Отдаление упирается в стартовый уровень** (заказчик: «максимальный какой уровень загружается изначально — выше него не уходит»): `maxXRange`/`maxYRange` = ширина вида 1:1 на уровне, где область уже видна целиком (`_fit_span_km`). Без этого щелчок «за последний уровень» растягивал вид до границ — 435.3 → 449.9 км на тех же тайлах, масштаб 0.97. Уровень считается от ГОЛОЙ рамки (без поля 4 %), иначе прыжок на целый уровень: 809.7 км вместо 438.8. ⚠️ **Строки над и под картой — фиксированной высоты**: перенос длинного текста менял аспект 1.3706 → 1.6822 и уводил охват 171.3 → 211.0 км. Пределы отдаления (`maxXRange`/`maxYRange`) сняты 31.08.2026. Предел ПРИБЛИЖЕНИЯ остался, иначе колесо уводит в бесконечный зум ([ПРЕДЕЛ_ВИДА](../код/ПРЕДЕЛ_ВИДА.md), [ТАЙЛЫ_И_ЗУМ](../карты/ТАЙЛЫ_И_ЗУМ.md)) |
| 5.12 | ⭐ **Тайлы рисуются ПИКСЕЛЬ В ПИКСЕЛЬ и БЕЗ сглаживания** ✅ 19.09.2026 | масштаб отрисовки 1.00 ± 0.03; `SmoothPixmapTransform` = **False**; уровень считается от ФИЗИЧЕСКИХ пикселей (`_map_px`, домножение на `devicePixelRatioF`); ширина оси Y **закреплена** `setWidth(58)`, высота оси X — `setHeight(34)` | `_TileImage.paint`, `_map_px`, `_pick_zoom`, `snap_view_to_zoom`, `SteppedViewBox` | требование заказчика 19.09.2026: «не сглаживай, не сужай». Сглаживание не лечило мыло, а маскировало его: причина — вид между уровнями (5.10). ⚠️ **Ужатие так же вредно, как растяжение**: при масштабе 0.89 подписи на тайлах мельчают на 11 % и густеют — «названия очень мелкие». Колесо ходит ровно по уровням (×2 за щелчок), поэтому кратность, заданная один раз, держится на всех зумах. Учёт `devicePixelRatio` нужен на экранах с масштабом Windows 125 % и выше: там тайл на 256 ЛОГИЧЕСКИХ пикселях физически растянут в 1.25 раза. ⚠️ **Ширина оси закреплена не для красоты**: подписи меняют длину при зуме («-100» → «-87.5»), ось раздвигается, и при `setAspectLocked` вид растягивается по X — замер 19.09.2026: аспект 1.3335 → 1.3545, охват 171.3 → 174.0 км, масштаб **1.000 → 0.984**, дальше 0.969 (заказчик: «был 99, потом 97»). После закрепления — 1.000 на всех уровнях, при отдалении и при зуме к углу |
| 5.14 | **Подложка — ОТДЕЛЬНЫЕ элементы сцены, а не одна картинка** ✅ 13.09.2026 (правило записано 19.09.2026) | на сцене до `MAX_ITEMS` = 400 элементов, кэш готовых картинок `FADED_CACHE` = 256; поле в один тайл вокруг окна; тайлы прежнего уровня остаются под текущим, пока новые не доедут | `view_qt/tile_layer.py` :: `TileLayer`, `_TileTask`, `_place`, `_drop_extra`; [код/ПОКАЗ_ТАЙЛОВ](../код/ПОКАЗ_ТАЙЛОВ.md) | прежняя мозаика собиралась в ОДНУ картинку на каждое движение вида: при панораме она пересобиралась целиком, а расхождение со слоями достигало **11 px** (задача 10.10, журнал п. 258 — стало 0.71 px). Поле в один тайл нужно, чтобы край экрана не догонял мышь пустотой; чтение и декод PNG идут в фоновом потоке, иначе интерфейс замирал бы на каждом шаге зума |
| 5.13 | **Строка подложки над картой** ✅ 19.09.2026 | уровень `z`, число тайлов, **диапазон адресов `x`/`y` и первый-последний `z/x/y` по порядку**, масштаб с пометкой «1:1 / растянут / ужат», ширина вида в км, «сеть / офлайн» | `_report_tiles`, `ThreatMapView.set_tiles_info`, `lbl_tiles` | заказчик 19.09.2026: «какой уровень и какие тайлы показываются сейчас — динамически, для проверки и определения корректности». Главное число — масштаб: любое значение, кроме 1.00, значит брак показа, и теперь он виден сразу, а не по разглядыванию подписей |
| 5.11 | **Названия пунктов — СВОИМ текстом, а не подложкой** | слой `place_names` (имя из тега `name:ru`/`name`), кегль 9.5 пт постоянный на экране | `_place_name`, `_render_place_labels` | подписи подложки впечатаны в тайлы кеглем ~10 px: при обзоре участка (z ≈ 11) они нечитаемы, а увеличить текст внутри готовой картинки нельзя. Замер: 129 пунктов, у всех имя есть |
| 5.11а | **Подписей на экране немного и без повторов** | шире 60 км — города и посёлки; 25…60 км — плюс сёла; ближе — все; одноимённые ближе 6 км — один пункт; не более 60 за кадр | `PLACE_ZOOM_STEPS`, `PLACE_DUP_KM`, `PLACE_LABEL_MAX`, `_label_rank` | иначе подписи сливаются в полосу текста. Две мели: у пункта в OSM бывают и узел, и контур — «Мирный» подписывался дважды; а код района (`suburb` = 3) намеренно выше посёлка (2), и «Стадион» с «Площадкой» вытесняли сам Мирный — району дан ранг 0 |

### 5.1 Рельеф: как высоты меняют вес

> Физика и разобранные варианты — [РАДИОВИДИМОСТЬ_ЗАДЕЛ.md](../вкладка_3/РАДИОВИДИМОСТЬ_ЗАДЕЛ.md);
> механика весов — [МЕТОДИЧКА_КАРТА_УГРОЗ.md](../вкладка_3/МЕТОДИЧКА_КАРТА_УГРОЗ.md).

**Постановка.** Позиции наблюдателей неизвестны, поэтому зоны обнаружения не считаем.
Работает более общее правило: *чем выше аппарат над окружающей местностью, тем он заметнее —
и радиолокации, и глазу, и на слух*. Значит надо прижать маршрут к низинам.

**Две величины превышения** (обе — от `_box_mean`, доли миллисекунды на всю карту):

| Величина | Формула | Что ловит |
|---|---|---|
| **A — над районом** | `h − средняя высота в радиусе 15 км` | плато, гряда, общий подъём местности |
| **B — над окрестностью** | `h − средняя высота в радиусе 2 км` | одиночный холм, бугор прямо на пути |

Пример, зачем обе. Ячейка на 200 м: вокруг на 15 км в среднем 140 м, вокруг на 2 км —
195 м. Значит `A = +60`, `B = +5` — место сидит высоко относительно района, но локально
ровное: это верх **плато**, аппарат виден на десятки километров. Обратный случай — холм
на равнине: `A = +10`, `B = +25`. Без A плато не штрафовалось бы вовсе, без B терялись
бы одиночные холмы.

Фактические значения по участку (28 539 ячеек, высоты 31…339 м, медиана 132 м):

| | 5 % | 25 % | медиана | 75 % | 95 % | 99 % | макс |
|---|---|---|---|---|---|---|---|
| **A** | −62 | −30 | +4 | +33 | +61 | +80 | +116 |
| **B** | −29 | −10 | +1 | +11 | +26 | +36 | +85 |

**Как считается вклад:**

```
k = 1 − 0.75·f(A/60 м) − 0.45·f(B/35 м),   зажат в [0.25, 1.40]
    f(x) = x^1.5 при x > 0 (высунулся),  0.5·x при x < 0 (закопался)

вес = 0   -> остаётся 0            нет ориентира — аппарат туда не полетит
вес < 0   -> не трогаем            застройка штрафуется независимо от рельефа
вес > 0   -> max(вес·k, пол)       пол = min(исходный вес, 1.0)
A ≥ 80 м или B ≥ 50 м -> вес = 0   очень высокая гора: КОРИДОР снимается
```

| № | Правило | Значение | Почему |
|---|---|---|---|
| 5.1.1 | **Рельеф УМНОЖАЕТ вес, а не прибавляется к нему** | `k ∈ [0.25, 1.40]` | аддитивная схема создавала 4 982 коридора на пустом месте (низина без ориентиров становилась «коридором») и уничтожала 1 622 существующих, разваливая связность с 68 компонент до 148 |
| 5.1.2 | **Ноль остаётся нулём** | 57.3 % участка | коридор — это линейный ориентир; рельеф не имеет права его породить |
| 5.1.3 | **Отрицательный вес рельеф не трогает** | 7.3 % участка | застройка штрафуется по своим правилам (§3), рельеф тут ни при чём |
| 5.1.4 | **Вес не уводится в минус** | пол `min(исходный, 1.0)` | в этой модели вес ≤ 0 = **полный запрет** (`passable_mask` берёт `weight > 0`), а не «маловероятно». Снижение приоритета выражается малым положительным весом: в пол упирается 455 ячеек (4.5 % коридоров). Оговорка «не выше исходного» нужна потому, что 4.2 % коридоров слабее единицы изначально — поднимать их было бы искажением в другую сторону |
| 5.1.5 | **Очень высокая гора снимает коридор** | `A ≥ 80` или `B ≥ 50` м | верхний процент местности: 95-й процентиль A = +61 м, 99-й = +80 м. Медианная высота отсечённых мест 258 м при медиане участка 132 м — вдвое выше окружающего. Попадает 315 ячеек (1.1 % участка), из них коридорных 135 (1.3 %) |
| 5.1.6 | **Отсечка снимает КОРИДОР, а не стирает ячейку** | `weight[cut & (weight > 0)] = 0` | обнуление всех ячеек подряд превращало застройку на горе (вес −15) в «нейтральное пустое поле»: для проходимости разницы нет, но тепловая карта и расстановка датчиков считают по весам и получали неправду |
| 5.1.7 | **Отсечка не строит стену** | мостики `passable_mask` | обнулённая вершина уже́ разрыва остаётся проходимой: узкий гребень аппарат перепрыгнет без приоритета, широкий массив обойдёт. Из 18 отсечённых пятен шире 2 км только 3, крупнейшее 10 км² |
| 5.1.9 | **Отсечка вычитается из проходимости — как город** | `pas &= ~grid.relief_cut()` | правило «очень высокая гора — коридора нет» жило только в весовой карте: город исключался явно, а горы нет, и буфер «ухода от ориентира» (дилатация на 1 км) возвращал **239 отсечённых ячеек** в проходимые — маршрут летел поверх снятой вершины. После правки таких ячеек 0, точек маршрута на отсечённых горах 0 |
| 5.1.8 | **Насыщение обязательно** | 60 м и 35 м | без потолка одна гора перевесит карту. При 60/35 в потолок упирается 12 % площади по A и 3 % по B; при 40/25 упиралось бы 38 % — рельеф переставал различать места, а маршруты теряли разнообразие |
| 5.1.9 | **Рельеф применяется ПОСЛЕДНИМ** | слои → бонус → НП → **рельеф** → вода | гарантия «ноль остаётся нулём, коридор не исчезает» верна только относительно ИТОГОВОГО веса. Когда рельеф стоял до населённых пунктов, деревенский штраф добавлялся к уже ослабленному весу и уводил его в минус: дорога +10 в краю деревни (−8) без рельефа давала +2 и была коридором, а с рельефом на возвышенности `10·0.25 − 8 = −5.5` — коридор пропадал (замер: 117 потерянных ячеек помимо отсечки). Городу порядок безразличен: обнулённый вес умножается на что угодно и остаётся нулём |
| 5.1.10 | **Включение/выключение = ПЕРЕСБОРКА карты** | ~2 с | из 5.1.9: досчитать рельеф поверх готовой карты нельзя, порядок нарушится. Пересборка сбрасывает накопленные маршруты, итерации и датчики — они построены по другой карте |

### 5.2 Запретные зоны, нарисованные пользователем

| № | Правило | Значение | Где задано | Почему |
|---|---|---|---|---|
| 5.2.1 | ✓ **Зона запрещает ПРОЛЁТ — как город** | `pas &= ~grid.no_fly_mask()` | `passable_mask` | зона вычитается из проходимости там же, где город и отсечка рельефа, и **после буфера**: иначе дилатация «ухода от ориентира» вернула бы её часть в проходимые (та же ошибка, что была с отсечкой, см. 5.1.9). Замер: проходимость 22 224 → 21 811 ячеек, из 250 линий внутрь зоны не заходит ни одна |
| 5.2.2 | **Зона СУЖАЕТ область пролёта — это её назначение** | — | — | не побочный эффект: запрет на то и запрет. Область залёта, маршруты и датчики пересчитываются сами, потому что все они строятся поверх `passable_mask` |
| 5.2.3 | **Веса карты зона НЕ трогает** | — | — | зона про полёт, а не про местность: сумма весов до и после совпадает побайтово. Поэтому пересборка карты её не сбрасывает, а карта остаётся той же |
| 5.2.4 | **Датчики в зоне ставить МОЖНО** | — | `place_sensors` | датчику запрещена только вода (правило 1.1а). Зона — ограничение для БПЛА, а не для наблюдателя |
| 5.2.5 | ✓ **Зоны переживают пересборку карты** | `build()` → `set_no_fly_zones` | `ThreatModel.build` | они нарисованы пользователем и от слоёв с рельефом не зависят: сетка новая — зоны те же. Убираются только «Сбросом» или кнопкой «Убрать зону» |
| 5.2.6 | **Показ ≠ отмена** | галка «Запретные зоны» | `render_no_fly` | снятая галка прячет зоны с карты, но не отменяет их — это разные действия |
| 5.2.7 | ⚠️ **Зона может сделать цель НЕДОСТИЖИМОЙ — и об этом сообщается** | `no_fly_check()` | `ThreatModel` | большая зона поперёк коридора обрывает путь, и маршрутов не будет вовсе. Это законный исход, а не поломка, но пустая карта выглядит как сбой — поэтому в заголовок пишется причина. Различаются три случая: цель внутри зоны, точка вылета внутри зоны, цель недостижима в обход |

**Как рисуется:** кнопка «Запретная зона» → клики задают вершины, колёсико замыкает,
`Esc` отменяет начатое. Многоугольник может быть невыпуклым — форму никто не ограничивает.

⚠️ **Ловушка при правке:** функция «точка внутри многоугольника» в проекте одна
(`_points_in_polygon` в `threat_grid.py`), ею же размечаются контуры населённых пунктов.
Не заводите вторую с тем же именем — дубликат молча перекроет существующую.

**Что это даёт на числах.** Множитель на коридорах: 5 % = 0.25, медиана 1.07, 95 % = 1.40.
Усилено (укрытия) 5 443 ячейки — 54 %, ослаблено 4 272 — 42 %.

| Место | A | B | k | магистраль (+10) | просёлок (+3) |
|---|---|---|---|---|---|
| пойма реки | −45 | −20 | 1.40 | **+14.0** | +4.2 |
| ровное поле | 0 | 0 | 1.00 | +10.0 | +3.0 |
| одиночный холм | +10 | +25 | 0.68 | +6.8 | +2.0 |
| высокое плато | +50 | 0 | 0.43 | +4.3 | +1.3 |
| гребень | +60 | +35 | 0.25 | **+2.5** | +1.0 *(пол)* |
| очень высокая гора | ≥ +80 | ≥ +50 | — | **0** | **0** |

Магистраль в пойме привлекательнее той же магистрали на гребне **в 5.6 раза**, а просёлок
в низине (+4.2) обгоняет магистраль на гребне (+2.5) — маршрут предпочтёт спуститься на
просёлок, лишь бы не идти по трассе через высоту. В вероятности выбора это превращается
в **3–4 раза реже**, а не в запрет: у каждого режима есть собственный «пол» 0.10, чтобы
маршруты не выродились в один коридор.

**Честные ограничения модели рельефа:**

* считается **превышение над окрестностью**, а не линия видимости — направленное укрытие
  (закрыт хребтом с одной стороны, открыт с другой) не учитывается;
* геометрия **переоценивает** укрытие: радиоволна огибает гребень (дифракция), особенно
  у низкочастотных обзорных РЛС — реальная скрытность хуже расчётной;
* **клиренс полёта на вклад не влияет вообще**: превышение считается разностью «высота
  здесь минус средняя вокруг», постоянная добавка сокращается. Клиренс 1 м остаётся
  допущением модели полёта (огибание рельефа, а не барометрический эшелон), но параметром
  в коде быть не должен;
* DEM — модель **поверхности**: включает кроны и здания, отделить их от грунта нельзя;
  лесной массив читается как возвышение на 15–30 м;
* **лес не укрытие** — летящий аппарат идёт над кронами, в прямой видимости; лесополосам
  оставлен вес 5 как навигационному ориентиру;
* маршрут не меняет **ЭПР и скоростной портрет** аппарата, а они часто и решают, отфильтрует
  ли радар цель как помеху.

---

## 6. ЧТО ПРОВЕРЯЕТ КОНТРОЛЬНЫЙ ПРОГОН

Отмеченные ✓ правила проверяются численно после каждой значимой правки. Эталон на
реальных данных OSM.

> ⚠️ **ЭТАЛОН СВОЙ У КАЖДОГО УЧАСТКА, И ПУТАТЬ ИХ НЕЛЬЗЯ.** Числа зависят от
> местности, поэтому «стало меньше коридоров» имеет смысл только внутри своего участка.
>
> * **§6.0а — эталон ДЕЙСТВУЮЩЕГО участка `arh`** (снят 04.09.2026): с ним сверяются
>   все новые правки;
> * **§6.0 — эталон участка `plesetsk_wide`** (снят 02.09.2026, после плана 5): участок
>   больше не активен, эталон сохранён для сравнения;
> * **таблица ниже — эталон участка `severodonetsk`**: на нём сняты замеры журнала за
>   июль–август 2026. Чтобы его повторить, переключить `THREAT_AREA = "severodonetsk"`.
>
> Прежний участок `plesetsk` (узкая рамка, `APX`) эталона не имеет: он прожил три дня и
> заменён расширенным. Его первый прогон 28.08.2026 (коридоров 1 576, засечка 100 %,
> 11.2 с) остаётся в журнале п. 194 как отправная точка, а не как норма.

### 6.0а Эталон участка `arh` (ДЕЙСТВУЮЩИЙ)

Снят 04.09.2026, переснят 13.09.2026 на умолчаниях `Params()` с одной поправкой `threat_N` = **18**
(до 13.09 было 15; решение заказчика по вопросу В8 — см. ниже). Прогон берёт число
датчиков из эталона участка сам. Порядок: `set_relief(True)` →
`build()` → `plan_routes()` → `iter_batch()` → `place_sensors()`. Район 120.5 × 73.0 км,
типы 2–3 пусты, заданных вручную датчиков нет.

⚠️ **Допуски ЗАМЕРЕНЫ, а не назначены: восемь прогонов подряд без единой правки кода.**
Структурные показатели совпали во всех восьми ТОЧНО — именно они и ловят настоящие
поломки. Стохастические разошлись, и допуск взят с запасом к размаху: узкий допуск даёт
ложную тревогу, после которой прогон перестаёт что-либо доказывать.

| Показатель | Норма | Допуск |
|---|---|---|
| Сетка | **241 × 146 = 35 186** ячеек | точно |
| Коридоров (вес ≥ 10) / компонент / крупнейшая | **3 528 / 316 / 758** (до 13.09: 2 563 / 200 / 520) | точно |
| Городская зона | **86** ячеек | точно |
| Маршрутов построено / итераций | **250 / 150** | точно |
| Датчиков поставлено | **18** малых (до 13.09: 15) + **3** больших | точно |
| Область залёта | **31 242** ячеек (до 13.09: 22 168) при авто-`L_max` = **203.4 км** | точно (L_max ±0.1) |
| Длина маршрута: средняя | **147.5 км** (с 16.09, план 11.5; 11.4 — 141.7, 15.09 — 138.1, до того 139.7) | ±6.0 (замеры 144.6…152.2) |
| Длина маршрута: максимум | **199.1 км** (11.4 — 198.2) | ±7.0 (замеры 193.2…202.8) |
| Средняя кратность засечки | **7.17** (с 16.09, 11.5; 11.4 — 7.19, 15.09 — 8.10, до того 6.90) | ±0.7 (замеры 6.89…7.57) |
| Засечка с кратностью ≥ `k`(3) | **≥ 0.99** | инвариант; после правок движения **20 из 20** (15.09 и 16.09); до них 12 из 14 |
| Превышений запаса хода / датчиков на воде / без пролёта / выходов за область | **0** | инварианты |

Прогон: `python tools/reference_run.py` — сам сверяет числа с этой таблицей
(`ETALON["arh"]`) и печатает «ВСЁ СОШЛОСЬ» либо список расхождений.

⚠️ **ЭТАЛОН ПЕРЕСНЯТ 13.09.2026 — в весовой карте появились слои, которых не было.**
Это не поломка, а полнота данных. Фильтр сбора брал 7 значений `highway` из 17, что
встречаются в данных, и терял 71.9 % длины дорожной сети лесного участка. Добавлены
просеки, лесные дороги, канавы, брошенная ж/д и спрятанные теги. Разбор —
[МЕТОДИЧКА_КАРТА_УГРОЗ §2.1](../вкладка_3/МЕТОДИЧКА_КАРТА_УГРОЗ.md).

| Шаг | Коридоров | Компонент | Крупнейшая | Область залёта |
|---|---|---|---|---|
| прежний набор | 2 563 | 200 | 520 | 22 168 |
| + просеки | 2 749 | 249 | 534 | 23 165 |
| + лесные дороги | 3 393 | 318 | 709 | 30 648 |
| полный набор | **3 528** | **316** | **758** | **31 242** |

⚠️ **ГЛАВНЫЙ ВЫВОД — ПОКРЫТИЕ УХУДШИЛОСЬ, И ЭТО ЧЕСТНОЕ ЧИСЛО.** Маршрутов стало больше, и
идут они шире. Прежним 15 малым датчикам хватало только потому, что карта была беднее.

**Пример.** Средняя кратность упала с 7.55 до 6.3. Засечка с кратностью ≥ k при 15
датчиках за восемь прогонов: 0.973, 0.987, 0.993, 0.993, 0.993, 1.000, 1.000, 1.000 —
**порог 0.99 не взят в 2 из 8**. При 18 датчиках: 0.993, 0.993, 0.993, 1.000. Выше 0.993
не поднимается и при 22: около 0.7 % маршрутов проходят там, где датчик ставить нельзя
(вода, город).

⚠️ **Инвариант при 15 датчиках стал «мигающим»**, и контрольный прогон иногда краснел без
поломки.

✅ **Решение заказчика 13.09.2026 (вопрос В8): эталон на 18 датчиках, порог 0.99 сохранён.**
Второй вариант — порог 0.97 при 15 — отклонён: ослабленный порог пропускал бы настоящие
поломки расстановки.

**Пример.** При 15 датчиках исправная модель уже давала 0.973: порог 0.97 оставлял зазор
в три тысячных. Поломка, сбивающая засечку до 0.97–0.99, прошла бы зелёной. На 18
датчиках засечка за 10 прогонов: 0.993 ×5 и 1.000 ×5, ложных тревог нет. Длина маршрутов от числа датчиков не зависит, но её норма сдвинута
142.4 → 139.7: шесть новых замеров легли ниже, и 137.8 выходил за прежнюю границу.

⚠️ **14.09.2026: на 18 датчиках инвариант тоже мигает — ложные тревоги есть (вопрос В9).**
Четыре прогона после правки чтения рельефа (окно растра, МЕТОДИЧКА_КАРТА_УГРОЗ §4.5):
структурные показатели совпали точно во всех четырёх, а стохастические разошлись шире
прежнего.

| прогон | засечка ≥ k | длина средняя | длина максимум | кратность |
|---|---|---|---|---|
| 1 | **0.980** ✗ | 140.99 | 200.02 | 6.99 |
| 2 | **0.987** ✗ | 138.28 | 201.00 | 7.51 |
| 3 | 1.000 | 139.88 | 201.90 | 7.42 |
| 4 | 1.000 | **134.56** ⚠ | **187.50** ⚠ | 7.27 |

**Правка тут ни при чём — проверено, а не предположено.** Та же цепочка при одном зерне
случайности со старым и с новым чтением рельефа дала **одинаковые маршруты и датчики** на
трёх зёрнах (засечка 1.000 / 0.993 / 0.993 в обоих вариантах). Значит, это размах самой
выборки: 10 прежних прогонов просто не попали в хвост.

**Пример.** За 14 прогонов на 18 датчиках порог 0.99 не взят 2 раза — столько же, сколько
было при 15 датчиках за 8 прогонов (решение В8 принималось, чтобы уйти от этого). Длина
134.56 при допуске ±4.5 от 139.7 — зелёный прогон без поломки не гарантирован.
Эталон и порог **не менялись**: решение за заказчиком (план 10, реестр §0.7, В9).

⚠️ **Там же — эталон четырёх обстановок `reference_cases.py` устарел по стохастике.**
Он снят 06.09.2026, до полного набора слоёв 13.09. Прогон 14.09.2026: **все точные
показатели сошлись** (число датчиков, состав по типам, закреплённые и их координаты), не
прошли **9 стохастических**.

| что | замер 14.09 (три прогона) | эталон 06.09 |
|---|---|---|
| кратность, случаи 1 / 2 / 3б «до» | 5.79 · 6.22 · 6.31 | 7.52 · 7.91 · 7.71 (±1.2) |
| кратность, случаи 1 / 2 «итер» | 6.17…6.48 · 6.75…7.76 | 7.38 · 8.00 (±1.2) |
| покрытие, случай 3б «до» / «итер» | 0.080 · 0.074…0.086 | 0.107 · 0.097 (±0.02) |
| засечка ≥ k, случаи 1 и 3а «итер» | 0.973…0.993 · 0.973…0.993 | 1.000 (±0.02) |

**Пример.** Падение кратности на 1.2–1.7 — то же, что записано при пересъёмке
`reference_run` 13.09 (7.55 → 6.3): карта богаче, маршруты шире. Переснять `--snap` на
полной карте — часть вопроса В9, до решения эти строки краснеют без поломки.

⚠️ **15.09.2026: стохастика `arh` переснята после правки движения** ([план 11](../архив/11_ПЛАН_МОДЕЛЬ_ДВИЖЕНИЯ.md)):
мягкая точка обхода, запретный сектор позади трассы, облёт цели. Структурные показатели
не изменились — движение на карту не влияет. Десять прогонов подряд:

| | было (13.09) | стало | замеры |
|---|---|---|---|
| длина средняя | 139.7 | **138.1** | 135.45…141.28 |
| длина максимум | 197.0 | 197.0 | 192.24…202.81 |
| кратность | 6.90 | **8.10** | 7.69…8.51 |
| засечка ≥ k | 12 из 14 не ниже 0.99 | **10 из 10** | 0.993…1.000 |

⚠️ **16.09.2026 стохастика переснята ещё раз** — облёт цели стал отдельным критерием (план 11,
11.4). Десять прогонов: длина 141.7 (138.8…144.5), максимум 198.2 (194.2…202.3), кратность
**7.19** (6.56…7.87), засечка ≥ k — 10 из 10, время 26.2…27.1 с. Кратность 8.10 → 7.19
совпала с правкой облёта; причину отдельно не мерили.

⚠️ **16.09.2026, задача 11.5 — ещё раз:** край пояса облёта — точка входа, доли поясов и
повадок держатся среди дошедших. Первый прогон по норме 11.4 дал длину 148.47 — вне 141.7 ± 4.5;
структурные показатели совпали точно. Десять прогонов: длина **147.5** (144.6…152.2, σ 2.3),
максимум 199.1 (193.2…202.8), кратность 7.17 (6.89…7.57), засечка ≥ k — 10 из 10 = 1.000,
время **28.6…29.8 с**. Длина выросла, потому что облёт чаще идёт дальними поясами: им
досталась заказанная доля. Допуск длины 4.5 → 6.0 — прежний был около 2σ.

⚠️ **Отчего именно выросла кратность 15.09, отдельно не мерилось** — известно лишь, что рост
пришёл вместе с правкой движения, а структура карты та же. Время прогона 24.3 → 26.3 с
(запретный сектор и кольца облёта; первая версия сектора давала 33 с).
К вопросу В9 это новые данные: на 18 датчиках инвариант после правки движения не мигал ни
разу из десяти; решение по эталону `reference_cases` — по-прежнему за заказчиком.

⚠️ **ОТВЕРГНУТЫЙ ВАРИАНТ 11.09.2026: «возможных маршрутов» 250 → 650.** Записан, чтобы
не пробовать заново вслепую.

**Зачем пробовали.** Эти маршруты идут не только на показ: по ним считается
ПРЕДВАРИТЕЛЬНАЯ расстановка датчиков — та, что строится до итераций
(`place_sensors` → `sensor_source = "zone"`). Шире выборка — устойчивее начальные позиции.

**Что показал прогон.** Модель от этого не ломается: **разошлось ТОЛЬКО само число**.
Структурные показатели совпали точно (сетка 241 × 146, коридоров 2 563 / 200 / 520,
городская зона 86, область залёта 22 168, `L_max` 203.40), стохастические легли в
допуски (длина средняя **142.06**, максимум **202.15**, кратность **7.50**), инварианты
нулевые, засечка ≥ `k` = **1.000**.

**Почему отказались — время.** Прогон **27 → 51.9 с**, почти вдвое: пул кандидатов
500 → 1 300, а `plan_routes` и так самая тяжёлая часть конвейера (правило 2.16).
Вернули 250. Возвращаться к 650 — только если появится запас по времени либо ускорится
сам `plan_routes`. Для быстрых сверок есть `--quick`: он пропускает «возможные
маршруты» целиком.

### 6.0 Эталон участка `plesetsk_wide` (прежний, участок не активен)

Снят 02.09.2026, **подтверждён повторным прогоном**, на чистых умолчаниях `Params()`
с одной поправкой: `threat_N` = **15**
(умолчание 20 — так сопоставимо со старым эталоном). Порядок тот же: `set_relief(True)`
→ `build()` → `plan_routes()` → `iter_batch()` → `place_sensors()`.

| Показатель | Норма | Сравнение с `severodonetsk` |
|---|---|---|
| Сетка | **192 × 110 = 21 120** ячеек | было 189 × 151 = 28 539 |
| Коридоров (вес ≥ 10) / компонент / крупнейшая | **1 875 / 133 / 520** | 11 101 / 42 / 9 583 |
| То же БЕЗ рельефа | **1 829 / 146 / 527** | 11 238 / 36 / 10 985 |
| Ячеек с весом > 0 | **4 534** | — |
| Городская зона | **51** ячейка (0.2 %) | 3 441 (12.1 %) |
| Маршрутов построено | **250 из 250** | так же |
| Итераций накоплено | **150 из 150** | так же |
| Датчиков поставлено | **15 из 15** малых + 3 больших | 15 из 15 (одного типа) |
| Область залёта | **10 466** ячеек при авто-`L_max` = **103.7 км** | 13 987 при 107.3 км |
| Длина маршрута: средняя / максимум | **92.1–92.5 / 103.3–103.5 км** | 95.7 / 106.8 |
| Превышений запаса хода | **0** | 0 |
| Датчиков на воде | **0** | 0 |
| Датчиков без единого пролёта | **0** | 0 |
| Засечка с кратностью ≥ `k`(3) | **1.000** | 98.7–100 % |
| Средняя кратность засечки | **7.9–8.0** (стохастика: два прогона подряд дали 8.03 и 7.91) | ~6 |
| Покрытая доля веса / кандидатов | **8.4 % / 3 779** | 10.5 % / 3 283 |
| Время прогона | **15 с** | ~18 с |

**Чем участок отличается по сути**, а не по числам: местность вчетверо реже застроена
(городов почти нет — 51 ячейка против 3 441), поэтому коридоров вшестеро меньше, и они
**сильно раздроблены**: 133 компоненты при крупнейшей 520 ячеек — то есть единой
связной сети коридоров, как на донецком участке (9 583 из 11 101 в одной компоненте),
здесь нет. Маршруты всё равно строятся: разрывы до 2 км модель перешагивает
(`threat_max_gap_km`, правило 2.4).

⚠️ **Отсюда практическое следствие для сверки правок:** на этом участке «число коридоров»
— показатель чувствительный к мелочам, а вот засечка ≥ `k` = 1.000 и нули в четырёх
строках нарушаются только настоящей поломкой. Начинать сверку — с нулей.

> **Прогон на ЧИСТЫХ УМОЛЧАНИЯХ `Params()`** — без них числа не воспроизводятся.
> ⚠️ Эталон снят, когда умолчанием было **15** малых датчиков; с 29.08.2026 умолчание
> `threat_N` = 20 (введён второй тип, правило 1.8), поэтому для сверки с эталоном число
> нужно вернуть к 15 вручную:
> замер при `threat_N` = 15 (текущее умолчание — 20), `threat_R` = **2 км**,
> `threat_k` = 3, `threat_cand_step_km` = 1 км,
> `threat_iter_routes` = 150, режим веса «смесь», расход «равномерно», разрыв 2 км,
> уход от ориентира 1 км, рельеф ВКЛЮЧЁН. Занимает ~18 с.
>
> ⚠️ **Рельеф надо включить ЯВНО — умолчаний тут мало.** `THREAT_DEM_ON_START = False`
> (при старте программы рельеф выключен, включается кнопкой), поэтому в прогоне нужен
> вызов `set_relief(True)` **до** `build()`. Без него коридоров получается **11 238**
> вместо 11 101 — и кажется, что сломалась карта, хотя сломан только порядок вызовов.
>
> Порядок прогона: `set_relief(True)` → `build()` → `plan_routes()` → `iter_batch()`
> (он сам набирает T = `threat_iter_routes`, вызывать в цикле не нужно) →
> `place_sensors()`. Проверять «датчик на воде» методом модели `_on_water(...)`: он
> учитывает смещение начала координат сетки (`ox`, `oy`), ручной пересчёт `x / h` даёт
> ложное срабатывание.

| Показатель | Норма |
|---|---|
| Маршрутов построено | 250 из 250 заказанных |
| Итераций накоплено | 150 из 150 |
| Датчиков поставлено | **15 из 15** |
| Выходов маршрута за область залёта | **0** |
| Превышений запаса хода | **0** |
| Датчиков на воде | **0** |
| Датчиков, не ловящих ни одного пролёта | **0** |
| Засечка пролётов с кратностью ≥ k(3) | **98.7–100 %** (стохастика; три прогона подряд дали 100.0 / 98.7 / 100.0) |
| Средняя кратность засечки | **~6** (при R = 2 км; было ~8 при прежнем R = 12 км) |
| Коридоров / компонент / крупнейшая | **11 101 / 42 / 9 583** |
| Городская зона | **3 441** ячейка (12.1 % участка) |
| Область залёта | **13 987** ячеек при авто-`L_max` = 107.3 км |
| Длина маршрута: средняя / максимум | **95.7 / 106.8 км** |
| Доступность цели по сторонам | 92–97 % со всех четырёх |
| Уход назад от старта | не глубже ~1.3 км (лимит 1 км + допуск сглаживания) |
| Отворот от цели на прямом маршруте | ≤ 0.2 км |

Если любой из нулей стал ненулевым — сломано соответствующее правило из §1–§4.

**Дополнительно при работе с рельефом** (эталон весовой карты, участок тот же):

> 📎 **Как эталон карты набирался** (числа в таблице выше — итог этого пути):
>
> | Правка | коридоров | город, ячеек |
> |---|---|---|
> | исходно (пол штрафа выключен, размер пункта по склейке) | 9 944 | — |
> | + пол штрафа деревни (3.4а) | 10 950 | — |
> | + размер пункта по своему пятну (3.6а) | **11 101** | **3 441** |
> | + градация пунктов по данным (3.10) | 11 101 | 3 441 |
>
> Градация карту не меняет: на этом участке настоящих городов 4 — меньше порога в 5,
> класс `city` в переводах не участвует, срабатывает один перевод `town → village`.
> **Без рельефа** на той же карте: коридоров 11 238, компонент 36, крупнейшая 10 985.

| Показатель | Норма |
|---|---|
| Рельеф выключен → карта совпадает с «до файла высот» | **побитово** (`np.array_equal`) |
| Коридорных ячеек: без рельефа / с рельефом | **11 238 / 11 101** |
| Связных компонент | **36 / 42** |
| Крупнейшая компонента | **10 985 / 9 583** |
| Вход и цель связаны | да в обоих случаях |
| Отсечено рельефом («очень высокая гора») | **315** ячеек |
| Множитель рельефа: диапазон | **0.25 … 1.40** |
| Доли площади: укрытия / возвышенности / нейтрально | **37.3 / 43.7 / 18.9 %** при полосе нейтральности `k = 1 ± 0.10` (порог обязателен: при ± 0.02 выйдет 45.1 / 50.6 / 4.3 %) |

> ⚠️ **Строки ниже — исторические**, мерились до пола штрафа деревни и до градации
> пунктов: тогда карта была 10 079 / 68 / 8 449 без рельефа. Проверять по ним нельзя,
> оставлены как след рассуждения.

| Исторический показатель | Тогдашняя норма |
|---|---|
| Потери коридоров **не от отсечки** | **0** (при выключенной отсечке числа совпадали точно: 10 079 / 68 / 8 449) |
| Пол деревни выключен (`THREAT_VILLAGE_FLOOR = 0`) → карта прежняя | **побитово** |
| Ориентиры, спасённые полом деревни | 1 002 ячейки (54 из них — река) |
| `THREAT_LOOKAHEAD_POWER = 0` → маршруты прежние | совпадение при том же seed |
| Точек маршрутов на отсечённых вершинах | < 0.2 % (замер: 20 из 14 696) |
| Средняя высота под маршрутами | падает: 95.0 → 91.5 м |
| Доля пути по возвышенностям (A > +20 м) | падает: 23.0 → 16.3 % |
| Разброс маршрутов | падает не более чем вдвое (замер: 99 % от прежнего) |
| Покрытие веса датчиками | 67.3 → 72.3 % |
| Средняя кратность засечки | 8.7 → 8.0 |

---

### 6.1 ЧТО ПОЧИНЕНО, А ЧТО ОСТАЛОСЬ (серия правок августа 2026)

**Весовая карта — исправлено:**

| что было | стало |
|---|---|
| отсечка рельефа отменялась буфером: 239 ячеек возвращались в проходимые | вычитается как город, 0 (правило 5.1.9) |
| деревня СТИРАЛА ориентиры: 1019 ячеек теряли коридор, среди них 54 реки | пол штрафа, коридор остаётся (3.4а) |
| размер пункта мерился по СКЛЕЙКЕ: цепочка сёл давала 431 км² при 27 % застройки | по своему пятну застройки (3.6а) |
| река без веса в 260 ячейках | **109**, все в настоящих крупных городах |
| тип пункта — только по тегу OSM, 62 района шли в класс «город» | градация по данным участка (3.10) |
| дороги и ЛЭП сливались с подложкой: контраст 1.00–1.20 при норме 4.5 | **3.62–4.59**, каждый слой выше своего порога: 4.5 без канта, 3.6 с белым кантом (§12.0) |

**Движение маршрутов — исправлено:**

| что было | стало |
|---|---|
| тяга к цели ЧИСТО геометрическая, вес не участвовал | тяга по стоимости, бюджет отдельным полем (2.0.1) |
| рельеф в цене не работал вовсе: низина и гребень стоили одинаково (1.000) | низина 1.016, гребень 1.976 (2.0.1а) |
| приоритет по весу тонул: размах ×17 против ×280 у курса | ×41, работает поверх стоимости (2.0.6) |
| синие линии не слушались панели, треть шла в режиме «мин. вес» | режим из панели (2.1.1) |
| жадный отбор считал голое число ячеек: пустошь = река | взвешенное покрытие (2.1.3) |
| «Применить» не пересчитывал датчики и область | пересчитывает всё |
| после стоимости все линии слились в один путь (покрытие 47 → 26 %) | несколько полей «жадности», 43.1 % (2.0.8) |
| «хвост» из цели: 29 % маршрутов протыкали объект ради VIA в 2.5 км за ним, а заход сзади при этом давали 3 из 150 | обход не ближе 4 км, точка отодвинута за круг; заходов сзади 33 из 150 (2.11а–2.11б) |

**Закрыто замерами (было «на глаз»):**

| что | результат |
|---|---|
| **Ширина полосы манёвра** | расширять НЕ надо: 1 км оптимум, при 2 км вес падает и часть маршрутов не строится (§7.1) |
| **Сила предвидения** | 1.5 → **3.0**: лучше по всем метрикам сразу, разброс теряет 0.2 п.п. |
| **Пороги рельефа** | сверены по высотам: укрытия 78.2 м, нейтрально 118.9, возвышенности 165.2, отсечка 257.6 при медиане участка 131.8 — порядок верный |
| **Двойной учёт рельефа** | оправдан: в весе И в цене даёт 19.5 % пути по возвышенностям против 20.9 % и 21.0 % у одиночных вариантов (2.0.12) |
| **Лимит «топтания» у цели** | не нужен: механизм оказался мёртвым — 0.5, 1.0 и 3.0 км дают побайтово одинаковую выборку. Запрета удаляться (2.10) достаточно, накопить и полкилометра без прогресса маршрут не успевает. Код удалён, причина записана рядом с `FINAL_BYPASS_KM` |

**Движение БПЛА — исправлено:**

| что было | стало |
|---|---|
| физики аппарата в модели не было: развороты на 180° «на месте» у 88 % маршрутов | предел поворота 45° за шаг (2.6а), стало 0.7–2 % |
| разворот за четыре шага по 45° — «вперёд-назад-вперёд» у 84 % маршрутов | окно разворота 3 км / 90° (2.6б), стало 19–23 % |
| петли (маршрут возвращался в ту же точку) у 13–20 % | 1.3 % |
| дуги радиусом 0.65 км — угловатое движение | скругление рисуемой линии (2.6д): медиана угла 10.5° → 5.7°, радиус 3.55 → 5.67 км |
| сглаживание могло срезать угол через пустошь | вес расчётной ломаной сохраняется (2.6е) |

**Осталось (не сделано, ждёт решения):**

* ⚠️ **Движение стало чище, но не идеально — принято как есть, требует продолжения.**
  При запасе хода 150 км (вдвое больше |AB| = 73) остаётся **много ломаных участков и
  ветвлений вокруг цели**: изломов на маршрут 22.7 против 11 на авто-запасе, набирается
  112 маршрутов из 150, покрытие 20.6 %. Плавному аппарату некуда девать лишние
  километры, и он тратит их манёврами у цели (прежняя модель тратила их петлями — 85 %
  маршрутов, что было хуже). В авто-режиме ветвления у цели тоже встречаются, хотя
  движение заметно чище; отдельные ломаные участки есть в обоих режимах.
  **Куда копать:** (а) проверить, не дают ли «дуги вокруг цели» обход `FINAL_BYPASS_KM`
  и заход через VIA за целью; (б) при большом запасе тратить лишний ход не вилянием, а
  выбором ДАЛЬНЕЙ VIA — связать `_pick_via` со свободным запасом; (в) мерить не среднее
  число изломов, а их распределение по маршруту (копятся ли они у цели).
  🔄 **15.09.2026 — продолжено в [плане 11](../архив/11_ПЛАН_МОДЕЛЬ_ДВИЖЕНИЯ.md).** Замер
  распределения (в) сделан (`tools/motion_check.py`): развороты копились не у цели, а на
  63 % пути — у VIA; исправлено мягкой VIA (2.11в). Лишний ход тратится облётом (2.11г), а
  не вилянием у цели. Остаток: разворотов 9–23 % по режимам, из них у цели 13–33 %.
* ⚠️ **Отворот у цели убран не до конца — принято как есть.** Массовая причина устранена
  (правила 2.11а–2.11б), средний отворот упал втрое, но **единичные маршруты по-прежнему
  уходят от цели далеко вниз**: максимум 1.41–2.05 км на авто-запасе и 6.07–8.15 км при
  запасе 130–150 км. Заказчик посмотрел карту и решил оставить: на общую картину эти
  случаи не влияют, а ужесточать некуда — обход стоит запаса хода, и на тесном ходе
  маршруты перестают строиться (118 из 150 при L = 95 км). Если возвращаться: смотреть,
  через какие VIA идут именно эти маршруты, и мерить долю отворотов больше 3 км, а не
  среднее.
* ⏳ **Две модели движения на выбор** — предложение заказчика. Откат к прежней делается
  ОДНИМИ параметрами, без правки кода (набор значений — в
  [МЕТОДИЧКА_КАРТА_УГРОЗ.md §14.4](../вкладка_3/МЕТОДИЧКА_КАРТА_УГРОЗ.md)).
* ⏳ **Усиление рельефа в самой весовой карте** — единственный рычаг, который ещё не
  упёрся. Диапазон `k` 0.25…1.40 → 0.10…2.50 даёт по реке 23.9 → 25.4 %, по
  возвышенностям 20.2 → 17.9 %. Не применено: меняет структуру карты, требует
  перепроверки инвариантов.
* ⏳ **Нейтральной зоны рельефа мало** — 18.9 % площади против 37.3 % укрытий и 43.7 %
  возвышенностей, в потолок `k_min` упирается 9.7 %. Рельеф «красит» почти весь участок;
  возможно, стоит поднять пороги насыщения (60/35 м), но это отдельная калибровка.

---

## 7. ЧЕГО В МОДЕЛИ ПОКА НЕТ (осознанные упрощения)

* **Высота полёта** — модель плоская. Рельеф влияет на *выбор* маршрута через вес (§5.1)
  и на цену прохода (§2.0), но сам полёт остаётся двумерным: аппарат не тратит запас
  хода на набор высоты. Предполагается огибание рельефа с постоянным клиренсом.
* **Планирование обхода** — маршрут реагирует на препятствие, но не планирует обход
  заранее. Подробно — §7.1 ниже.
* ~~**Общая кратность между типами датчиков**~~ — **УПРОЩЕНИЕ СНЯТО 05.09.2026**
  (правило 1.10а переписано). Уже поставленные датчики других типов передаются в кэш
  списком `(x, y, R)`, и жадный отбор стартует поверх их покрытия: тип 2 знает, что этот
  кусок маршрута уже закрыт типом 1 нужное число раз. Считается по радиусу КАЖДОГО
  датчика, а не по радиусу кэша. Замер: кэш видит **91 засечку** чужими датчиками там,
  где раньше видел 0. Остаётся техническое ограничение помельче: типы по-прежнему
  ставятся по очереди, то есть первый тип выбирает места, не зная о будущих, — общий
  одновременный отбор при разных радиусах остаётся задачей З2
  [единого плана](../планы/7_ЕДИНЫЙ_ПЛАН.md).
* **Оптимальность закреплённой части расстановки.** Когда часть позиций задана
  человеком (условия моделирования, 1.3а), оптимизируется только остаток. На заданной
  части оптимума не искали — и не будет: она задана извне. Принято сознательно
  (заказчик 04–05.09.2026: «оптимальность падает, но нужно для функционала»).
* **Поиск своей карты БЕЗ каких-либо координат** (`tools/map_fit.py --find`,
  `model/map_fit.py` :: `locate_and_fit`) — код есть, но НЕ РАБОТАЕТ. Три попытки подряд
  дали уверенный и неверный ответ (промах до 200 км): на грубом масштабе шаблон
  застройки цепляется за любое тёмное пятно, а на топокарте чёрным нарисованы тысячи
  подписей урочищ, а не только посёлки. Рабочий путь — задать рамку на глаз по подписям
  на самой карте, дальше подбор уточняет до сотен метров (5.1с). Разбор трёх неудачных
  попыток и что пробовать дальше — [МЕТОДИЧКА_ПРИВЯЗКА_КАРТИНКИ §6а](../карты/МЕТОДИЧКА_ПРИВЯЗКА_КАРТИНКИ.md).

---

### 7.1 ОГИБАНИЕ ПРЕПЯТСТВИЙ — в основном решено стоимостной тягой

> **Обновлено.** Раздел писался, когда тяга к цели считалась по чистой геометрии и
> обход складывался только из локальных механизмов. После перехода на **стоимостную
> тягу** (§2.0) главное ограничение снято: сквозь возвышенность стало ДОРОГО, и обход
> возникает сам, без расширения полосы манёвра. Ниже — что осталось.

**Как работает сейчас.** Четыре механизма, и первый из них — уже настоящее планирование:

| Механизм | Роль | Где |
|---|---|---|
| **Цена прохода по рельефу** | гребень дороже низины (1.976 против 1.016), путь ищется Дейкстрой ПО ВСЕЙ карте — это планирование, а не реакция | правило 2.0.1а |
| **Уход от ориентира** (1 км) | задаёт полосу, в пределах которой можно отклоняться | правило 2.13 |
| **Множитель рельефа в весе** | делает вес внутри полосы неодинаковым: на склоне падает до 0.25 | §5.1 |
| **Предвидение** (2 км) | видит склон заранее и снижает привлекательность курса | правило 2.14 |

Замер после перехода на стоимость: доля пути по возвышенностям **13.0 → 7.4 %**, средняя
высота под трассой **77.8 → 70.3 м**, при этом вес под маршрутом даже вырос (20.60 →
22.42) и длина не изменилась. Попутно закрыт «бесплатный перелёт»: ячейки мостика на
гребне теперь дорогие, и маршрут выбирает либо обход, либо перелёт в низком месте.

**Ограничение 1: полоса манёвра постоянная.** Она не расширяется там, где впереди гора —
это фиксированная величина, одинаковая вдоль всего коридора.

> ✅ **ВОПРОС ЗАКРЫТ ЗАМЕРОМ.** Долго стояла задача «поднять уход до 2 км и проверить,
> часто ли маршруты упираются в массивы — возможно, сложные механизмы не понадобятся».
> Проверено: **расширять НЕ надо, становится хуже.**
>
> | уход | вес под маршрутом | по возвышенностям | покрытие области | маршрутов из 150 |
> |---|---|---|---|---|
> | 0.5 км | 14.21 | **17.8 %** | 33.7 % | 150 |
> | **1.0 км (принято)** | **14.37** | 19.0 % | 33.4 % | 150 |
> | 2.0 км | 13.31 | 21.3 % | 32.3 % | **147** |
> | 3.0 км | 13.40 | 20.0 % | 32.3 % | 150 |
>
> Вес падает, возвышенностей больше, разброс не растёт, при 2 км даже не все маршруты
> строятся. Проходимость участка при этом растёт (61 % → 78 % → 85 % → 87 %), но пользы
> маршрутам это не даёт: широкая полоса размывает приоритет по весу. Обход в итоге решила
> СТОИМОСТНАЯ ТЯГА (§2.0), а не ширина полосы.

> ⚠️ **Ограничение 2 ниже УСТАРЕЛО** — оставлено как история решения. Тяга теперь знает
> и про вес, и про рельеф (§2.0), и именно это сняло проблему. Читать как описание того,
> ПОЧЕМУ понадобилась стоимостная тяга.

**Ограничение 2 (снято): тяга к цели не знает про рельеф.** Поле расстояний (`_dijkstra_dist`)
считалось по чистой ГЕОМЕТРИИ — ни вес, ни высоты в него не входили. Перед широким
массивом геометрическая тяга пересиливает локальное нежелание лезть вверх. Решений вида
«обойду массив слева, потом вернусь на ориентир» модель не строит: это по-прежнему
локальный жадный выбор, просто с горизонтом 2 км вместо 500 м.

**Что делать дальше — три варианта по возрастанию цены:**

1. **Поднять «уход от ориентира» до 2 км** и замерить, часто ли маршруты вообще упираются
   в массивы шире полосы. Ноль правок в коде. **Начинать следует отсюда** — возможно,
   сложные механизмы не понадобятся.
2. **Адаптивная полоса** — расширять её у препятствий, оставляя узкой на чистых участках.
   Средняя цена, поведение предсказуемое. [1_ПЛАН_РЕЛЬЕФ_реализован.md §13.4](../архив/1_ПЛАН_РЕЛЬЕФ_реализован.md).
3. **Планировщик по стоимости** — второе поле Дейкстры, цена шага `h / привлекательность`.
   Обход возникает сам, но меняются ВСЕ маршруты, и геометрическое поле убрать нельзя
   (по нему проверяется `L_max`) — нужны два поля сразу.
   [РАДИОВИДИМОСТЬ_ЗАДЕЛ.md §6](../вкладка_3/РАДИОВИДИМОСТЬ_ЗАДЕЛ.md).
* **Разнотипные датчики** — у всех одинаковый радиус R.
* **Погода, время суток, помехи** — не учитываются.
* **Ограничение по времени полёта** — считается только длина пути, не длительность.
* **Реакция БПЛА на обнаружение** — маршрут не меняется от того, что его засекли.

---

## 8. УСЛОВИЯ МОДЕЛИРОВАНИЯ — то, что задаёт человек

> **Отдельный раздел, а не пункт в §1** (решение заказчика 05.09.2026): это не ещё одно
> правило расстановки, а **вход в задачу**. Человек описывает обстановку, которую нужно
> смоделировать, и программа считает уже с ней.

### 8.1 Главное различие: ЯКОРЬ ≠ УСЛОВИЕ МОДЕЛИРОВАНИЯ

| | **ЯКОРЬ** | **УСЛОВИЕ МОДЕЛИРОВАНИЯ** |
|---|---|---|
| кто выбрал позицию | сам алгоритм | человек |
| что это по сути | самоограничение алгоритма | ВХОДНЫЕ ДАННЫЕ задачи |
| пример | датчик у входа и у цели, в 0.45·R внутрь коридора | датчик, поставленный двойным кликом по карте |
| состояние | 🚫 **отключены** 29.08.2026 (правило 1.3) | ✅ работают с 05.09.2026 |
| влияние на гарантию (1 − 1/e) | **ломает** обоснование | **не ломает**: оптимизируется остаток |

⚠️ **Путать нельзя, и дело не в слове.** Жадный алгоритм расстановки работает БЕЗ якорей
— это его текущее состояние, и возвращать их не нужно (журнал п. 201). А заданные
человеком позиции алгоритм не выбирал: он принимает их как данность и **добирает
остаток** до заказанного числа. Технически они передаются тем же аргументом
`CoverageCache.greedy(anchors=…)` — это способ передачи, а не суть.

### 8.1а Три случая работы и два режима датчика (заказчик 05.09.2026)

**Порядок работы задан заказчиком дословно, и он же определяет, что чем закрепляется:**

| Обстановка | Что делает кнопка «Расставить датчики» |
|---|---|
| **«Рассчитать позиции»**, вручную ничего не задавали | обычный жадный алгоритм по выборке возможных пролётов, с учётом зоны пролёта — как было всегда |
| **«Рассчитать позиции»**, датчики заданы вручную (**неважно, статические или динамические**) | заданные **не перемещаются**; программа считает, скольких не хватает до заказанного числа, и **доставляет остаток** жадным алгоритмом — уже с учётом заданных |
| **«Задать позиции»** | подбора нет вовсе: на карте ровно то, что в таблице. Кнопка лишь пересчитывает показатели. Работает и при разной кратности |

**А РАЗЛИЧИЕ РЕЖИМОВ ПРОЯВЛЯЕТСЯ ПРИ МОДЕЛИРОВАНИИ (итерациях):**

| Режим датчика | До итераций | При итерациях |
|---|---|---|
| **статический** | стоит там, где задан | **стоит там же**: жадный алгоритм сперва учитывает его, потом доставляет остальные |
| **динамический** | стоит там, где задан | **перемещается**: место подбирается заново по накопленной картине пролётов |

⚠️ **Почему различие нельзя было сделать «сразу».** Обе промежуточные редакции оказались
неверны, и каждая по-своему: первая закрепляла ЛЮБОЙ ручной датчик — тогда слово
«динамический» не значило ничего; вторая закрепляла только статические — тогда
динамический уезжал ещё до моделирования, и обстановку им было не задать. Правильно —
через фазу: `ThreatModel.manual_locked()` (журнал п. 230).

**Проверяется:** `python tools/flow_check.py` — разделы 4б (фаза «до») и 4в (фаза
«при итерациях»); `python tools/reference_cases.py` — все случаи в обеих фазах.

### 8.2 Правила

| № | Правило | Значение / замер | Где в коде | Почему так |
|---|---|---|---|---|
| 8.1 | **Что закреплено — решает ФАЗА работы** | ДО итераций закреплены ВСЕ заданные; ПРИ итерациях — только «статические» | `manual_locked`, `in_iterations` | см. таблицу 8.1а ниже: до итераций человек описывает обстановку целиком, а при моделировании динамический датчик перемещается вслед за накопленной картиной пролётов — в этом и смысл двух режимов |
| 8.1а | **Два признака на карте: форма и обводка** | квадрат/круг — режим; ЧЁРНАЯ обводка — место задал человек | `render_sensors`, `sensors_manual` | одним признаком не свести: до итераций на карте стоят и статические, и динамические, заданные человеком, — по форме они разные, а поставлены одинаково. Цвет занят типом датчика |
| 8.2 | **Позиция НЕ приводится к сетке кандидатов** | допуск сверки 1 м; замер: **0.0 м** после расстановки, пересборки карты и при трёх работающих типах | `_place_multi_type` | привязка к ближайшему узлу сдвинула бы датчик на полшага — до 0.5 км при шаге 1 км, четверть радиуса типа 1. Поэтому позиция ДОБАВЛЯЕТСЯ в список кандидатов, а не ищется среди них |
| 8.3 | **Разнос к заданным позициям не применяется** | поставили два рядом — остаются оба | `CoverageCache.greedy` | разнос — правило подбора, а подбора здесь нет. ⚠️ Дефект, пойманный на этом: `greedy` терял вторую позицию, попавшую в зону разноса первой |
| 8.4 | **Заданные входят в заказанное число N** | 20 = 2 заданных + 18 подобранных | `_place_multi_type` | иначе заказ «20 датчиков» превращался бы в 22, и число в окне обещало бы не то |
| 8.5 | **Переживают пересборку карты и смену района** | как запретные зоны | `ThreatModel.manual_sensors` | заданы человеком и от местности не зависят. Якорь области при смене района не двигается, поэтому километры остаются верными |
| 8.6 | **Убираются только явно** | «Сброс», кнопки таблицы, «Очистить» | `clear_manual_sensors` | ⚠️ «Сброс» убирает их вместе с датчиками: иначе следующая расстановка вернула бы их как условия, и сброс выглядел бы неполным |
| 8.7 | **Датчик вне района — законно** | окно показывает, сколько таких | `manual_outside_area` | человек мог задать позиции заранее, до выбора района, или намеренно поставить за краем. Не «чинить» подтягиванием — показать число |
| 8.8 | **Режим «Задать позиции»: на карте ровно то, что в таблице** | количество считает программа, человек задаёт только радиус и кратность | `_place_manual`, `InputDataWindow._sync_counts` | алгоритм не подбирает ничего. Поле «N = 20» при трёх строках обещало бы несуществующее |
| 8.9 | **Приоритет выборки маршрутов для расстановки (задача 8.6)** | свои итерации (≥5) → загруженная история полёта (если включена галка, ≥5) → зона пролёта (все возможные пути) → сырой вес | `ThreatModel._sample_for_sensors`, `sensor_source` | загруженная история — тоже условие моделирования (как и заданный вручную датчик), но начатые СВОИ итерации всегда важнее: иначе «с пуском итераций галка уже не работает» (заказчик 06.09.2026) не выполнялось бы. `sensor_source` отражается в заголовке после расстановки |
| 8.10 | **Сопоставление номеров «анализа размещения» (задача 8.9) НЕ трогает расстановку** | сравниваются ПЕРВОЕ построение и ТЕКУЩЕЕ; номер — по минимальному суммарному перемещению (венгерский алгоритм), отдельно по каждому ТИПУ | `model/sensor_track.py`, `ThreatModel.initial_sensors`, `sensor_movement` | заставить алгоритм тяготеть к прежним местам добавило бы задаче ограничение, которого в ней нет, и сняло бы гарантию (1 − 1/e) — план 7, §2.1·0. Сопоставление — чистая перенумерация ПОСЛЕ расстановки: позиции и покрытие не меняются ни на грамм, поэтому правило 1.14 (гарантия) эту задачу не затрагивает |
| 8.11 | **«Режим анализа» замораживает расстановку целиком, а не один датчик** | во время итераций, если флаг включён и что-то уже стоит, `place_sensors` выходит РАНЬШЕ пересчёта (проверено сентинелом в `sensor_source`); первое построение не трогает | `Params.threat_analysis_static`, `ThreatModel.place_sensors` | нужен человеку, который хочет читать «анализ размещения» (8.10) без обычного дрейфа расстановки между периодическими пересчётами (`THREAT_SENSOR_REFRESH_EVERY`) — иначе «до» и «после» отличались бы и без содержательной причины. НЕ отменяет фазовое правило 8.1 (оно про ОДИН датчик), а добавлено поверх него |
| 8.12 | **Направление сектора «Анализ моделирования» — по ЗАХОДУ маршрута, не по старту** | азимут ищется с конца маршрута назад, до первой точки дальше `THREAT_COMPASS_APPROACH_KM` (0.5 км) от цели; на прогоне: по старту — 1 сектор из 12 занят, по заходу — 8 | `ThreatModel._approach_bearing`, `sector_stats` | точка входа ОБЩАЯ на всю выборку (§14.0, 5 точек на карту) — азимут от старта стягивался в 1–5 секторов независимо от того, куда маршрут потом заворачивает, огибая рельеф и зоны; заход показывает направление, в котором БПЛА РЕАЛЬНО оказывается у цели |

### 8.3 Что это значит для оптимальности

**Оптимальность падает — и это принято сознательно** (заказчик 04–05.09.2026:
«оптимальность падает да, но нужно для функционала»). Точнее: на заданной части оптимума
не искали и не будет — она задана извне. Гарантия (1 − 1/e) обсуждается **для остатка**,
который подбирает жадный алгоритм; список «что ломает гарантию» в правиле 1.14 условия
моделирования НЕ включает — они не ломают алгоритм, а сокращают задачу.

**Проверяется:** `python tools/flow_check.py` — разделы 4б и 4в (стоит ли датчик там,
куда поставлен; добирает ли алгоритм остаток; перемещается ли динамический при
итерациях); `python tools/metrics_check.py` — раздел 4 (видит ли отчёт заданные позиции
и не растёт ли от них общее число); `python tools/reference_cases.py` — четыре
обстановки в двух фазах, по три прогона каждая.

**Подробно:** [план 8, §8.7.1б](../планы/8_ПЛАН_КАРТА_И_ИНТЕРФЕЙС.md) — три случая работы
кнопки «Расставить датчики»; [§8.6.6](../планы/8_ПЛАН_КАРТА_И_ИНТЕРФЕЙС.md) — приоритет
выборки для истории полётов; [§8.9.7](../планы/8_ПЛАН_КАРТА_И_ИНТЕРФЕЙС.md) — анализ
размещения и венгерский алгоритм; [§8.9.8–8.9.9](../планы/8_ПЛАН_КАРТА_И_ИНТЕРФЕЙС.md) —
режим анализа, линейка «Расстояние», «Анализ моделирования» и сектора по компасу;
[МЕТОДИЧКА_ДАТЧИКИ_РЕЖИМЫ §9](../вкладка_3/МЕТОДИЧКА_ДАТЧИКИ_РЕЖИМЫ.md) — устройство
заморозки; [МЕТОДИЧКА_КАРТА_УГРОЗ §14.5](../вкладка_3/МЕТОДИЧКА_КАРТА_УГРОЗ.md) —
механизм секторов и почему по заходу, а не по старту; журнал п. 227–228, 230, 237–241.
