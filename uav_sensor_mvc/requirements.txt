numpy>=1.20
matplotlib>=3.5
pillow>=8.0
python-docx>=1.0
PyQt5>=5.15
pyqtgraph>=0.13

# --- Опционально: РЕАЛЬНЫЕ геоданные для вкладки 3 (карта угроз).
# Приложение работает и без них (офлайн-СХЕМА).
# Нужны только для tools/build_threat_grid.py, чтобы обработать локальный .osm.pbf
# Если возникнут ошибки при установке на Windows, закомментируйте строки ниже решёткой.

pyrosm>=0.6
geopandas>=0.12
shapely>=2.0
pyproj>=3.4
rasterio>=1.3
