numpy>=1.20
matplotlib>=3.5
pillow>=8.0        ; экспорт GIF в режиме --mode render (для GUI/selftest не требуется)
python-docx>=1.0  ; только для пересборки docs/Теория_и_математика.docx (не для приложения)
PyQt5>=5.15       ; только для Qt-версии main_qt.py (базовая main.py не требует)
pyqtgraph>=0.13   ; только для Qt-версии main_qt.py

# --- Опционально: РЕАЛЬНЫЕ геоданные для вкладки 3 (карта угроз). Приложение
# работает и без них (офлайн-СХЕМА). Нужны только для tools/build_threat_grid.py,
# чтобы превратить локальный OSM-экстракт .osm.pbf в слои geo_cache/.
# pyrosm>=0.6      ; чтение локального .osm.pbf (Rust-бэкенд, офлайн)
# geopandas>=0.12  ; векторная геометрия (тянет GDAL; на Windows — через conda)
# shapely>=2.0     ; геометрия (транзитивно с geopandas)
# pyproj>=3.4      ; репроекция в метры (UTM 37N), если нужна макс. точность длин
# rasterio>=1.3    ; если сохранять весовую карту в GeoTIFF
