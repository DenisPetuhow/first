{
  "_комментарий": [
    "Разрешения ПРОЕКТА. Дополняют глобальные (~/.claude/settings.json).",
    "",
    "⏩ ДЕЙСТВУЕТ РЕЖИМ «БЕЗ ОСТАНОВОК» — включён 14.09.2026 по команде заказчика:",
    "«разработай новый режим работы для себя без остановок, ты всё делаешь кроме",
    "коммитов и пушей … чтобы меня не спрашивать».",
    "",
    "  allow — ВСЁ: инструменты Bash, PowerShell, Edit, Write целиком. Прогоны модели,",
    "          пересборка слоёв и тайлов, проверки документации, свои скрипты — без",
    "          вопросов. defaultMode = acceptEdits: правки файлов тоже без вопросов.",
    "  ask   — ТОЛЬКО история git наружу (commit, push) и СТРАХОВКА от уничтожения:",
    "          затереть незакоммиченные правки (checkout, restore, reset, clean),",
    "          переписать историю (rebase, merge, branch -D), удалить папку целиком",
    "          (rm -r, Remove-Item -Recurse), pyinstaller (удаляет каталог сборки).",
    "",
    "Почему ask сильнее allow. Claude Code сверяет правила в порядке deny → ask → allow,",
    "поэтому `Bash` целиком в allow не отменяет `Bash(git commit:*)` в ask. Составная",
    "команда `a && git commit` проверяется по частям — коммит не проскочит.",
    "",
    "Команды режима (подробно — теория/переносимое/5_РАЗРЕШЕНИЯ_КОМАНД.md §0):",
    "  /bez-ostanovok     — включить: копия .claude/режимы/без_остановок.json сюда;",
    "  /s-podtverzhdeniem — выключить: копия .claude/режимы/с_подтверждением.json сюда",
    "                       (прежний список: скрипты поимённо, ask на данные и сеть).",
    "",
    "ВАЖНО и в этом режиме. Команду не предварять `cd \"путь\" &&` и переменной",
    "окружения: для allow уже неважно, но ask сверяется с началом части команды."
  ],
  "permissions": {
    "defaultMode": "acceptEdits",
    "allow": [
      "Bash",
      "PowerShell",
      "Edit",
      "Write",
      "Read",
      "Glob",
      "Grep",
      "NotebookEdit"
    ],
    "ask": [
      "Bash(git commit:*)",
      "Bash(git push:*)",
      "Bash(git reset:*)",
      "Bash(git checkout:*)",
      "Bash(git restore:*)",
      "Bash(git switch:*)",
      "Bash(git clean:*)",
      "Bash(git rebase:*)",
      "Bash(git merge:*)",
      "Bash(git branch -D:*)",
      "Bash(git branch -d:*)",
      "Bash(git cherry-pick:*)",
      "Bash(rm -r:*)",
      "Bash(rm -rf:*)",
      "Bash(rm -fr:*)",
      "Bash(pyinstaller:*)",
      "PowerShell(git commit:*)",
      "PowerShell(git push:*)",
      "PowerShell(git reset:*)",
      "PowerShell(git checkout:*)",
      "PowerShell(git restore:*)",
      "PowerShell(git switch:*)",
      "PowerShell(git clean:*)",
      "PowerShell(git rebase:*)",
      "PowerShell(git merge:*)",
      "PowerShell(git branch -D:*)",
      "PowerShell(git branch -d:*)",
      "PowerShell(git cherry-pick:*)",
      "PowerShell(Remove-Item:*)",
      "PowerShell(pyinstaller:*)"
    ],
    "_почему_в_ask": [
      "git commit / push — правило заказчика: коммиты и отправка только по его команде;",
      "git reset / checkout / restore / switch / clean — затирают НЕЗАКОММИЧЕННЫЕ правки,",
      "                    их не вернуть ничем;",
      "git rebase / merge / branch -d / cherry-pick — переписывают историю; сборочную",
      "                    ветку exe-build-2 не трогать (CLAUDE.md, раздел Git);",
      "rm -r, Remove-Item — удаляют папки: geo_cache и tile_cache в git не лежат, их",
      "                    не восстановить (рельеф Marshut 4.6 ГБ, тайлы 13 273 шт.);",
      "pyinstaller       — УДАЛЯЕТ каталог назначения (теория/сборка/СБОРКА_EXE.md §0)."
    ]
  }
}
