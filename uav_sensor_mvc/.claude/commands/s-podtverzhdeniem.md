---
description: Выключить режим «без остановок» — вернуть прежние подтверждения
---

# Режим «с подтверждением» — ВЕРНУТЬ

Прежний порядок (до 14.09.2026): разрешены проверки и свои скрипты поимённо,
спрашиваются перезапись данных участка, сеть, долгие эталонные прогоны, вся история git,
удаление. Разбор — `теория/переносимое/5_РАЗРЕШЕНИЯ_КОМАНД.md` §0.

## 1. Разрешения — выполнить сразу

```bash
cp ".claude/режимы/с_подтверждением.json" .claude/settings.json
python -m json.tool .claude/settings.json
```

Проверить по коду возврата, что JSON цел. Сообщить одной строкой: «режим с
подтверждением включён».

## 2. Как работать

По памяти проекта `rabota-bez-soglasovaniya-koda` и `CLAUDE.md`: код и разведка — сам;
затратные прогоны — одной строкой предупредить; правка документации — назвать разделы и
ждать ответа; `git` — только по команде.
