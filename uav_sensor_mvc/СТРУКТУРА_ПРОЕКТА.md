# Структура проекта: файлы, классы, функции

Карта кода в архитектуре **MVC** (Model–View–Controller). Теория и формулы —
в `МЕТОДИЧКА.md`; этапы, проблемы и история — в `ЖУРНАЛ_ПРОЕКТА.md`.

Два интерфейса на **общей** модели (`model/`) и общей логике контроллера:
* matplotlib — `main.py`, `view/`, `controller/` (вкладка 1);
* PyQt5 + pyqtgraph — `main_qt.py`, `view_qt/`, `controller_qt/` (вкладки 1 и 2).

---

## Дерево файлов

```
uav_sensor_mvc/
├── config.py                 параметры (Params), режимы, подписи, тема
├── main.py                   запуск matplotlib-версии (вкладка 1) + CLI
├── main_qt.py                запуск Qt-версии (две вкладки в QTabWidget)
├── model/
│   ├── geometry.py           эллипс достижимости, гео-привязка
│   ├── trajectories.py       генераторы маршрутов БПЛА, коридор, физика R_min
│   ├── detection.py          обнаружение и метрики (эталонные реализации)
│   ├── optimization.py       сетка кандидатов, CoverageCache, жадный выбор
│   ├── simulation.py         модель сцены вкладки 1 (точка A → цель B)
│   └── area_start.py         модель сцены вкладки 2 (зона старта → цель B)
├── view/view.py              представление matplotlib (вкладка 1)
├── view_qt/
│   ├── view_qt.py            представление Qt/pyqtgraph (вкладка 1, базовое)
│   └── area_view.py          представление Qt вкладки 2 (наследник)
├── controller/controller.py  контроллер (анимация, события, метрики)
├── controller_qt/
│   ├── controller_qt.py      контроллер Qt (таймер QTimer)
│   └── area_controller.py    контроллер вкладки 2
├── docs/
│   ├── Теория_и_математика.docx   развёрнутая теория (Word)
│   └── make_theory_docx.py        генератор docx
├── МЕТОДИЧКА.md / ЖУРНАЛ_ПРОЕКТА.md / СТРУКТУРА_ПРОЕКТА.md / README.md
└── requirements.txt
```

---

## config.py

* `MODES`, `MODE_LABELS` — режимы оптимизации и веса (α₁, α₂).
* `TRAJ_LABELS` — модели движения вкладки 1 (дуги/петли/манёвр).
* `AREA_TRAJ_LABELS` — модели движения вкладки 2 (дуга из старта/ломаная/манёвр).
* `MANEUVER_LAW_LABELS` — закон манёвра: «по точкам» / «по синусу».
* `WAYPOINT_ZONE_LABELS` — точки маршрута: «в зоне старта» / «только за зоной».
* `MOTION_PROFILES`, `MOTION_LABELS` — профили разброса (амплитуды).
* `THEME` — цветовая тема.
* `Params` (dataclass) — все входные параметры (см. таблицу §10 методички), в т.ч.
  `speed_kmh`, `bank_deg`, `n_min/n_max`, `maneuver_law`, `waypoint_zone`. Свойства
  `A`, `B`; метод `validate()`.

---

## model/geometry.py

* `ellipse_geometry(A,B,L_max)` — центр, полуоси a,b,c, угол эллипса достижимости.
* `point_in_ellipse / points_in_ellipse(P,A,B,L_max)` — проверка принадлежности.
* `auto_axes_limits(geom, margin)` — границы осей карты.
* `geo_to_local_km(A_geo,B_geo)` — перевод широта/долгота → локальные км.

---

## model/trajectories.py

Геометрия дуг:
* `make_arc_by_angle(A,B,θ,n)` — дуга по касательно-хордовому углу θ.
* `arc_length_from_angle`, `_sagitta_from_angle`, `max_deflection_angle` — длина/стрела/θ_max.
* `_sample_angle(rng,θ_max,σ,profile)` — случайный угол по профилю.
* `sample_arc_trajectory(...)` — случайная дуга; `arc_fan`, `frequent_arcs` — веер/частые.

Коридор движения:
* `corridor_outline / corridor_polygon / corridor_filter / corridor_bbox` — границы и фильтр.
* `_reach_ellipse_halves` — половины эллипса (для манёвра).

Петли/змейки:
* `make_serpentine`, `max_serpentine_amplitude`, `sample_serpentine_trajectory`,
  `frequent_serpentines`, `serpentine_fan`.

Физика и манёвр по точкам/синусу:
* `turn_radius_km(V,φ)` — **R_min = V²/(g·tan φ)** (км).
* `_WP_MODE`, `_BALANCED_P`, `_pick_mode` — параметры режимов разброса (амплитуда ρ,
  пологость, чередование) и выбор «смешанного».
* `_waypoints(S0,B,L_max,rng,mode,r_min,nrange,s_min)` — путевые точки: число из
  nrange, продольная стратификация, боковой вынос-линза с базой `b` (полу-малая
  ось эллипса); сложный — 1–2 крупных лепестка к краю; `s_min` — нижняя доля.
* `_round_corners(verts,r_min,turn_frac,n)` — скругление углов дугами ≥ R_min.
* `_waypoint_path(...,rounded,s_min)` — путь по точкам с укладкой в L_max.
* `_sine_path(...)` — манёвр по гармоникам с ограничением кривизны ≤ 1/R_min.
* `maneuver_path(...,law,s_min)` — диспетчер: «sine» → `_sine_path`, иначе по точкам.
* `polyline_path(...,s_min)` — путь по точкам без скругления.
* `sample_maneuver_trajectory`, `sample_polyline_trajectory`, `frequent_maneuvers`,
  `maneuver_fan` — сэмплеры/слои.
* `SAMPLERS` — словарь сэмплеров по ключу модели.

---

## model/detection.py

* `n_detections(traj,sensors,R)` — кратность n (число засёкших датчиков).
* `continuous_coverage(traj,sensors,R)` — доля длины пути под наблюдением.
* `segment_coverage(traj,sensors,R,L_seg)` — доля покрытых сегментов m/L_seg.
* `utility(...)` — полезность φ (формула 1); `avg_utility(...)` — среднее по выборке.

---

## model/optimization.py

* `filter_not_past_target(pts,A,B,max_s)` — убрать кандидатов ЗА целью B.
* `candidate_grid(A,B,L_max,step,corridor,traj_model)` — сетка кандидатов в коридоре.
* `CoverageCache` — структура покрытия:
  * `add_trajectory(traj)` — добавить маршрут (hit/segmask/covfrac по кандидатам);
  * `greedy(N,weights,anchor_idx,anchor_sep)` — жадный выбор N позиций с якорем и
    развязкой; вторичный критерий распределения при насыщении основного.
* `greedy_placement(...)` — обёртка «список маршрутов → размещение».

---

## model/simulation.py — `SimulationModel` (вкладка 1)

Старт — точка A. `reset()` считает `r_min` (из ТТХ) и фильтрует кандидатов
(`filter_not_past_target`). `sample_trajectory` → сэмплер по `traj_model` с
`r_min`, `(n_min,n_max)`, `maneuver_law`. `recompute_placement` — жадный выбор с
якорем у B (`_anchor_idx`, `anchor_sep=1.35R`). `step / run_batch / evaluate /
compare_modes / frequent_paths / fan_paths / density_field`.

---

## model/area_start.py — `AreaStartModel` (вкладка 2)

Старт — случайная точка `S0` из эллипса зоны. Векторная геометрия и условия:
* `_frame(A,B)` — орты оси и нормали; `start_zone_outline`, `reach_ellipse_outline`;
* `reach_envelope_outline(A,B,depth,width,L_max)` — огибающая достижимости с учётом
  зоны старта (объединение эллипсов S0,B; маршруты не выходят за контур);
* `sample_start_point(A,B,depth,width,rng)` — точка старта в эллипсе зоны;
* `_zone_exit_frac(S0,A,B,depth,width)` — доля выхода из зоны (опорная проекция,
  формула 14) → `s_min` для путевых точек «только за зоной»;
* `points_outside_start_zone(pts,A,B,depth,width)` — маска кандидатов ВНЕ эллипса
  зоны (датчики в зоне не размещаются);
* `sample_area_arc / sample_maneuver / sample_polyline` — генераторы маршрутов;
  `zone_exclude` решает, ставить ли точки только за зоной (по `waypoint_zone`).

Класс `AreaStartModel`: `set_route(A,B)` (запоминает направление `_dir`),
`reset()` (B = A + |AB|·_dir — следует за полем; фильтрует кандидатов: не за B,
вне зоны старта И не за её задней кромкой 180°), `_sample` (диспетчер + флаг
зоны), `_anchor_idx`, `recompute_placement`, `step / run_batch / evaluate / слои`.

---

## view/view.py — matplotlib (вкладка 1)

`PARAM_SPECS` — единый список параметров. `SimulationView`: построение панели,
карта, анимация полёта (блиттинг), слои, метрики; контракт методов
(`update_geometry`, `draw_clear`, `draw_static_frame`, `setup_flight`,
`update_flight`, `set_metrics`, `get/set_param_values`, `get_toggles`).

---

## view_qt/view_qt.py — Qt/pyqtgraph (вкладка 1, базовый класс)

`QT_EXTRA` — доп. поля Qt (скорость, крен, точек от/до). `SimulationView(QWidget)`:
тот же контракт методов, аппаратная анимация, ColorBarItem + LegendItem,
`_build_law_combo` (список «Закон манёвра»), `showEvent/resizeEvent` →
`_apply_range(force=True)`.

## view_qt/area_view.py — Qt вкладки 2 (наследник)

`AREA_PARAM_SPECS` (+ глубина/ширина зоны). `AreaStartView(SimulationView)`:
панель вкладки 2; кнопка «Создать маршрут» и постановка кликами
(`_on_scene_click`, `begin_create`, `prompt_create`); `_build_zone_combo` (список
«Точки маршрута»); отрисовка зоны старта (эллипс) и оси A→B; callback `on_zone`.

---

## controller/controller.py — `SimulationController`

Связывает модель и представление: события виджетов (`on_apply`, `on_mode`,
`on_traj`, `on_profile`, `on_law`, `on_speed`, `on_toggle`), анимация (`_tick`,
блиттинг), пакетный расчёт, панель метрик (`_update_metrics_idle/full`).
`_make_timer()` — фабрика таймера (переопределяется в Qt).

## controller_qt/controller_qt.py — `QtSimulationController`

Наследник: таймер на `QTimer` (`_make_timer`), адаптация под Qt-представление.

## controller_qt/area_controller.py — `AreaStartController`

Наследник Qt-контроллера для вкладки 2: создание маршрута кликами (`on_create`,
`on_route_ready`), выбор модели движения (`on_movement`), выбор зоны точек
(`on_zone`), блокировка «Пуск/Шаг/Пакетно» до создания маршрута, метрики зоны.

---

## main.py / main_qt.py

* `main.py` — запуск matplotlib-версии (вкладка 1) + разбор CLI-аргументов.
* `main_qt.py` — запуск Qt-версии: `QTabWidget` с двумя вкладками; модель/вид/
  контроллер для каждой; `_ensure_qt_plugin_path()` (фикс плагинов на Windows).

---

## Поток данных (одна итерация)

```
sample_trajectory() ─► маршрут γ
        │
        ▼
CoverageCache.add_trajectory(γ)   (hit / segmask / covfrac по кандидатам)
        │
        ▼
recompute_placement()  = greedy(N, weights, якорь у B, развязка)
        │
        ▼
View: анимация полёта + датчики + метрики
```

Накопление `T` итераций → выборочное среднее F_t стабилизируется → расстановка
датчиков перестаёт меняться.
