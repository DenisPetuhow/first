# CLAUDE.md — навигация и контекст проекта

> **Это указатель, а не пересказ.** Не читай документацию целиком «на всякий случай» —
> открывай только тот файл, который нужен для текущей задачи (экономия контекста).

## ⏩ РЕЖИМ РАБОТЫ: «БЕЗ ОСТАНОВОК» (включён 14.09.2026, команда заказчика)

⚠️ **Режимов ДВА, переключает их ТОЛЬКО заказчик своей командой** — сам режим не
меняется и не выключается «по ситуации». Сейчас действует «без остановок»; прежний —
«с подтверждением». Помнить об этом и после сжатия контекста.

**Всё делается без вопросов, кроме `git commit` и `git push`.** Прогоны (в том числе
`reference_cases`, `pack_area`), свои скрипты, документация по правилам ниже — сразу;
развилку решать осторожным вариантом и записывать выбор; в конце серии — одно уведомление.
Решения, оставленные за заказчиком (задачи «не начинать без команды», вопросы В*), режим
не открывает. Переключение: **`/bez-ostanovok`** · **`/s-podtverzhdeniem`**; разбор —
[5_РАЗРЕШЕНИЯ_КОМАНД §0](теория/переносимое/5_РАЗРЕШЕНИЯ_КОМАНД.md). ⚠️ Пока режим
включён, фразы ниже «спросить … и ждать ответа» про документацию не действуют.

## 🧭 ПЕРВОЕ ДЕЙСТВИЕ ПОСЛЕ СЖАТИЯ СЕССИИ

Сжатие стирает подробности: замеры, причины решений, отклонённые варианты. Чтобы не
переспрашивать и не переделывать уже решённое — **сразу прочитать четыре места**:

1. **[указатель журнала](теория/журнал/README.md)** — оглавление журнала
   (какая тема в каких пунктах), оттуда — **последние 2–3 пункта самого журнала**: что
   делали только что и какими числами это подтверждено. ⚠️ Журнал — **5 049 строк**
   (живая часть 6 плюс архив), **целиком не открывать**: читать нужные пункты по номерам
   строк из указателя;
2. ⏭ **[теория/РАБОТА.md](теория/РАБОТА.md) — ВЕРХНИЙ БЛОК**: чем занимаемся прямо
   сейчас, со всеми числами, точками правки и ловушками. Он и заводится ради того, чтобы
   после сжатия не перечитывать планы;
3. **[реестр незакрытого — план 10 §0.7](теория/планы/10_ПЛАН_ПРОЕКЦИЯ_И_ОСТАТКИ.md)** —
   что не сделано по ВСЕМ планам, с датой, когда пункт попал в список;
   [указатель планов](теория/планы/README.md) — какой план про что;
4. **[ОГРАНИЧЕНИЯ.md](теория/правила/ОГРАНИЧЕНИЯ.md) §6** — эталон контрольного прогона,
   с ним сверяется любая правка.

**И ЗЕРКАЛЬНОЕ ПРАВИЛО — ПЕРЕД сжатием:** дописать в журнал новый пункт (что было не
так → почему, с замером → что сделано → что осталось). Кратко, но с цифрами:
«коридоров 9 944 → 10 950» полезнее, чем «улучшили карту». Не успел до сжатия — сделать
первым действием после него, пока пересказ ещё свежий. Полная формулировка правила —
в [README.md](README.md).

## 📝 ПРАВИЛО ДОКУМЕНТИРОВАНИЯ: журнал коротко, методичка подробно

**Разделение обязанностей.** У журнала и методичек разные роли, и путать их нельзя:

| | ЖУРНАЛ | МЕТОДИЧКА / ОГРАНИЧЕНИЯ |
|---|---|---|
| отвечает на вопрос | **когда и почему** меняли | **как это работает сейчас** |
| объём | 5–10 строк на пункт | сколько нужно, с формулами и таблицами |
| читается | по порядку, как хроника | по указателю, по одному разделу |
| живёт | вечно, задним числом не правится | переписывается под текущий код |

**Формат пункта журнала — суть в четыре строки, потом ссылки:**

```
NNN. **Короткий заголовок. ✅**
     **Проблема.** Симптом одной фразой + ЧИСЛО, которым он замерен.
     **Причина.** Отчего так вышло — одной фразой.
     **Решение.** Что именно изменено — одной фразой.
     **Результат.** ЧИСЛО «было → стало» либо «проверено прогоном: …».
     **Подробно:** ссылка на МЕТОДИЧКА_X.md §N — и что именно там раскрыто.
```

**Числа обязательны** в проблеме и в результате: «коридоров 9 944 → 10 950» полезнее,
чем «улучшили карту». Без числа пункт нельзя ни повторить, ни опровергнуть.

**Ссылок может быть несколько** — на каждый затронутый раздел, с пометкой, что там
искать. Разбор, вывод формулы, таблица замеров, отвергнутые варианты, история подбора —
**не в журнал, а в методичку**; журнал только указывает, где это раскрыто.

**ПРИ КАЖДОМ ОБНОВЛЕНИИ ЖУРНАЛА — сверять методички.** Дописал пункт → сразу проверь,
не устарел ли раздел, куда ведёт ссылка, и обнови его. Журнала МАЛО: методички живут
отдельно и устаревают молча. Проверено на своей же ошибке — правка цветов слоёв была
подробно записана в журнал, а §12.0 методички полгода описывал промежуточные цвета
(журнал п. 174).

**Что обновлять вместе с кодом:**

| изменилось | что обновить |
|---|---|
| механика модели | [МЕТОДИЧКА_КАРТА_УГРОЗ.md](теория/вкладка_3/МЕТОДИЧКА_КАРТА_УГРОЗ.md) или [МЕТОДИЧКА.md](теория/МЕТОДИЧКА.md) — раздел про эту механику |
| правило или порог | [ОГРАНИЧЕНИЯ.md](теория/правила/ОГРАНИЧЕНИЯ.md) — правило и число; если менялся эталон — §6 |
| поведение маршрутов | [МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md §4.2](теория/модель/МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md) |
| интерфейс, цвета, легенда | [НАСТРОЙКА_ИНТЕРФЕЙСА.md](теория/интерфейс/НАСТРОЙКА_ИНТЕРФЕЙСА.md), [ВКЛАДКА_3_КАК_РАБОТАЕТ.md](теория/вкладка_3/ВКЛАДКА_3_КАК_РАБОТАЕТ.md) |
| появился/удалён файл или функция | [СТРУКТУРА_ПРОЕКТА.md](теория/структура/СТРУКТУРА_ПРОЕКТА.md) |
| **как что-то РЕАЛИЗОВАНО в коде** (новый приём, алгоритм, структура данных) | [теория/код/](теория/код/01.README.md) — разбор с фрагментами. ⚠️ Методичка, журнал и план **ссылаются сюда, а не пересказывают**: копия расходится с кодом вдвое быстрее |
| ⚠️ **новая функция или правка механики — ВСЕГДА** | [1.КАК_КОД_РЕШАЕТ_ЗАДАЧУ.md](теория/код/1.КАК_КОД_РЕШАЕТ_ЗАДАЧУ.md) — **кратко**, в своём шаге, и **ссылка на подробный разбор**, если он есть (правило заказчика 13.09.2026). Это общая картина: устаревший шаг в ней врёт про всю программу сразу |
| **затронута ТЕОРИЯ** (критерий, формула, гарантия) | [пособие](теория/пособие/README.md) — соответствующая часть. ⚠️ Пособие от правок кода не зависит: менять только если изменилась САМА постановка |
| **сделано то, что числилось заделом** | раздел «задел» в нужном файле — пометить реализованным |
| ⚠️ **подходящего документа НЕТ вовсе** | **спросить о создании**, назвав, что именно туда пойдёт. Молча оставить механизм неописанным — та же ошибка, что молча переписать документ |
| **всегда** | [указатель журнала](теория/журнал/README.md) — коротко, по формату выше |
| **раз в 5 пунктов журнала** | [указатель журнала](теория/журнал/README.md) — продлить диапазон, поправить число строк |

**Инициатива за мной, решение за заказчиком.** После значимой правки не ждать команды:
сказать «затронуты такие-то разделы, обновить?» — и ждать ответа. Молча переписывать
нельзя, молча оставлять устаревшим — тоже.

⚠️ **Признак, что нужен НОВЫЙ документ, а не абзац в старом:** механизм нельзя объяснить
ссылкой ни на один существующий раздел. Если объясняется двумя ссылками — это абзац.

### 📓 КАТАЛОГ РАБОТЫ — обязателен на каждый пункт плана

Правило заведено 11.09.2026, потому что **11 документов отстали от кода именно так**:
правка делается, а что она задела — нигде не записано, и к концу работы уже не вспомнить.

**Перед началом пункта плана** — завести блок в [теория/РАБОТА.md](теория/РАБОТА.md):

| Поле блока | Что в нём |
|---|---|
| **что делается** | одной фразой |
| **файлы КОДА** | путь :: функция :: строка — **дописывать по ходу**, когда файл открыт |
| **файлы ТЕОРИИ** | какие документы это описывают и что в них поправить — с ⬜ |
| **проверки** | какие прогоны и с каким результатом |

**По завершении пункта — замкнуть петлю, три действия подряд:**

1. пройти по списку документов сверху вниз, **каждый поправить и отметить ✅**;
2. ⚠️ **в каждом поправленном документе обновить отметку в шапке**
   `> **Правился:** ДД.ММ.ГГГГ — что изменено.` Без этого шага
   `codemap.py --stale` будет считать документ сверенным, хотя его только тронули
   (на этом уже обожглись 12.09.2026 — §9.7.0 плана 9);
3. одна строка в журнал со ссылками → **блок из `РАБОТА.md` удалить**.

⚠️ **Файл живёт, пока блоки удаляются.** Копятся закрытые блоки — это второй журнал,
который никто не читает. Признак болезни: больше трёх блоков одновременно.

**Три вопроса, которыми пункт закрывается:** *какие файлы кода изменились · какие
документы это описывают · какие проверки прошли*. Ответ «никакие документы» по правилу
выше означает — **спросить о создании документа**, а не промолчать.

## ✅ КОМАНДА «ПРОВЕРЬ НА ДОСТОВЕРНОСТЬ» — нулевой шаг и семь основных

Пропускать нельзя: каждый шаг ловит СВОЙ класс ошибок, и то, что находит пятый, первые
четыре пропустить не могут в принципе. **Начинать с шага 0** — он говорит, какие
документы вообще стоит открывать, и экономит остальные семь.

| № | что сверяем | чем | что ловит |
|---|---|---|---|
| **0** | **какие документы вообще отстали** | `codemap.py --stale` | сужает сверку до двух-трёх документов вместо полусотни: показывает, где код ушёл вперёд описания |
| 1 | **Имена** | разбор кода: функции, классы, **константы модулей**, поля `Params`, **атрибуты `self.*`**, **аргументы**, переменные окружения — против имён в текстах | упоминания того, чего в коде уже нет |
| 2 | **Значения** | числа в текстах против `config.py` | параметр переименовали или сменили значение |
| 3 | **Замеры** | ПОВТОРНЫЙ прогон против записанного | числа с промежуточного состояния |
| 4 | **Формулы** | посчитать по методичке и по коду, сравнить | математика разошлась с текстом |
| 5 | **Утверждения о готовности** | ⚠️ ТОЛЬКО ЧТЕНИЕМ, глазами | «задел / не реализовано» о том, что давно сделано |
| 6 | **Документы против друг друга** | `check_claims.py` | один файл «сделано», другой про то же — «задел»: с кодом каждый по отдельности не спорит |
| 7 | **Код против самого себя** | `check_dead_code.py` | функция без единого вызова; параметр, который никто не читает |
| **8** | **Отметки о правке** | `check_dates.py` | **«файл правили, а записать забыли»**: имена живые, числа верные, ссылки целы — врёт только отметка. Так §5.3 методички маршрутов полгода описывала запасной путь расстановки как основной |

**Пятый шаг не автоматизируется.** Во фразе «пока не реализовано» все имена живые и числа
верные — врёт одно утверждение. Так нашлись четыре места: заделы по рельефу, по
стохастическим маршрутам, по costmap и по ограничению радиуса разворота.

**Скрипт проверки врёт чаще, чем документация** — каждую находку проверять вручную: из
~90 «расхождений» настоящими были единицы. Разбор законных оборотов и две мели (не
исключать файл, где параметр объявлен; рядом с числом писать условия замера) —
[3_СВЕРКА_С_КОДОМ](теория/переносимое/3_СВЕРКА_С_КОДОМ.md).

**Скрипты — в [tools/переносимое/](tools/переносимое/README.md)**, заново их писать не нужно:

⚠️ **Команду НЕ предварять `cd "путь" &&`** и не начинать с переменной окружения:
разрешения сверяются с началом строки, и такая приставка сбрасывает их — запрос всплывает
на ровном месте. Рабочая директория и так корень проекта. Замер: из 757 команд сессии
656 шли с `cd`, список разрешений покрывал 24 % вызовов
([5_РАЗРЕШЕНИЯ_КОМАНД](теория/переносимое/5_РАЗРЕШЕНИЯ_КОМАНД.md)).

```bash
python tools/переносимое/codemap.py --stale # ШАГ 0: какие документы отстали от кода
python tools/переносимое/check_names.py     # шаг 1: имена
python tools/переносимое/check_numbers.py   # шаг 2: значения
python tools/переносимое/check_claims.py    # шаг 6: противоречия между документами
python tools/переносимое/check_dead_code.py # шаг 7: функции без вызовов, непрочитанные параметры
python tools/переносимое/check_dates.py     # шаг 8: отметки «Правился» против git
python tools/переносимое/docmap.py --check  # оглавления, ссылки, структура
```

**Отметка о правке — одна строка в шапке документа:**
`> **Правился:** ДД.ММ.ГГГГ — что изменено.` Она **не заменяет git, а дополняет**:
полезен именно РАЗРЫВ между ней и датой коммита — он и значит «правку внесли, а что
изменилось, не записали». Мини-журналы папок (`ЖУРНАЛ_ПАПКИ.md`) **генерируются**
из этих отметок и git: `check_dates.py --write-logs`, руками не править.

Фильтры в них уже настроены: имена дали 146 находок до настройки и 0–1 после, числа —
50 и 0. **Нашёл законный оборот — допиши фильтр**, чтобы он не всплывал снова.

**В конце:** контрольный прогон — `python tools/reference_run.py` (сам сверяет числа с
эталоном [ОГРАНИЧЕНИЯ §6.0а](теория/правила/ОГРАНИЧЕНИЯ.md)) → правка
найденных расхождений в методичках → короткая запись в журнал (что искали, что нашли,
что оказалось ложной тревогой).

**Проверки самой программы** (не документации) — восемь, у каждой свой класс ошибок:

```bash
python tools/reference_run.py   # ЧИСЛА расчёта против эталона участка
python tools/flow_check.py      # ПОРЯДОК работы модели: что когда считается, что после сброса
python tools/gui_check.py       # ИНТЕРФЕЙС без экрана: рамки, оси, панель, окна, своя карта
python tools/metrics_check.py   # ОТЧЁТ сам с собой: суммы по типам, радиусы, доли
python tools/reference_cases.py # ЧЕТЫРЕ ОБСТАНОВКИ в двух фазах, по три прогона
python tools/tiles_check.py     # ПОДЛОЖКА против слоёв: совпадение в пикселях, пул тайлов
python tools/area_check.py      # ОБЛАСТИ из файлов: разбор, битое, какую область открывает config
python tools/motion_check.py    # ФОРМА МАРШРУТОВ: возвраты, кружение, заход с другой стороны, режимы
python tools/motion_check.py --axis approach   # ...и эталон облёта цели (уровни обычный/умеренный/максимальный/смесь)
```

```bash
python tools/plot_run.py        # КАРТИНКА прогона: облёт и пояса, разброс, места разворотов
```

⚠️ **ЕДИНОЕ ПРАВИЛО ПРОВЕРОК — [10_ПРОВЕРКИ_И_ПРОГОНЫ](теория/переносимое/10_ПРОВЕРКИ_И_ПРОГОНЫ.md)**
(правило заказчика 19.09.2026): пять видов проверок и что каждый ловит · порядок «числа
раньше картинки» · шесть правил прогонов (зерно, одна выборка, числа только из замера,
условия рядом с числом, код возврата, выключаемость механизма в ноль) · когда заводить
новую проверку. **Так работаем при любой правке багов и разборе механизмов**; в режиме
«без остановок» проверка делается сама, без вопроса.

⚠️ **Четвёртая нужна именно потому, что первые три пропускают врущий отчёт.** Числа могут
поодиночке выглядеть здоровыми и при этом противоречить друг другу: так засечка считалась
радиусом типа 1 для ВСЕХ датчиков — эталон сходился (он снят на одном типе), а кратность
у крупных типов занижалась (журнал п. 227).

⚠️ **Пятая — потому что эталон снят на ОДНОЙ обстановке.** `reference_run.py` знает один
работающий тип, одну кратность и ноль заданных вручную датчиков; всё остальное для него
невидимо. Различие «статический / динамический» ломалось дважды подряд, и оба раза
контрольный прогон говорил «всё сошлось» (журнал п. 230).

⚠️ **Шестая — потому что ошибка ПОКАЗА не меняет ни одного числа.** Подложка может
разъехаться с векторными слоями, а расчёт останется верным: первые пять этого не увидят.
`tiles_check.py` мерит расхождение **в пикселях экрана** (порог — 1 px) и уже поймало
дефект, при котором на сцену ложилось 0 тайлов вместо 48 (журнал п. 258).

⚠️ **Седьмая — потому что выбранную в программе ОБЛАСТЬ остальные не видят.** Они
считают область по умолчанию, а выбор идёт через переменную окружения до `import config`.
Уже поймано: окно «Выбор папки» не показывает файлов, человек выбирал папку уровнем выше
и получал «нет файла `область.txt`» (журнал п. 262).

⚠️ **Восьмая — потому что ФОРМА маршрута не меняет средних.** Петля, шпилька «вперёд-назад»
и заход не с той стороны оставляют среднюю длину той же — контрольный прогон их не видит.
Уже поймано: 14.9 % маршрутов возвращались по своей трассе, а с обратной стороны на цель
не заходил ни один; профиль «виляет, потом прямо» при большом запасе не отличался от
«равномерно» (план 11). Сценарий свой: район с центром в Мирном, вход с юго-востока,
запас авто +50 км; 4 режима × 15 прогонов, ~80 с.

## 📖 НАВИГАЦИЯ — открывать ОДИН раздел, а не файл целиком

**Документации 73 файла, 29 343 строки, 1.70 млн символов** (замер 11.09.2026) —
это ≈570–680 тыс. токенов. Читать целиком нельзя в принципе.

**Три уровня:** этот файл → **[теория/структура/НАВИГАЦИЯ.md](теория/структура/НАВИГАЦИЯ.md)** → раздел
документа. В навигации две таблицы и стоимость документов:

| Нужно | Куда |
|---|---|
| **по ВОПРОСУ** — «маршруты идут не туда», «датчики стоят не там», «как считается вес» | [НАВИГАЦИЯ §1](теория/структура/НАВИГАЦИЯ.md) |
| **по МЕХАНИЗМУ** — что правим и где это в коде (файл :: имя) | [НАВИГАЦИЯ §2](теория/структура/НАВИГАЦИЯ.md) |
| **сколько стоит открыть документ** | [НАВИГАЦИЯ §3](теория/структура/НАВИГАЦИЯ.md) |
| **сколько стоит открыть МОДУЛЬ** — строки, сигнатуры, где в карте | [карта кода](теория/карта_кода/README.md) — **пересобирается скриптом, не устаревает** |
| **что делать дальше** | [указатель планов](теория/планы/README.md); всё незакрытое — [реестр плана 10 §0.7](теория/планы/10_ПЛАН_ПРОЕКЦИЯ_И_ОСТАТКИ.md) |

**У каждого документа длиннее 300 строк — оглавление с номерами строк** (блок
`<!-- ОГЛАВЛЕНИЕ -->`). Порядок: оглавление → `Read` с `offset`. Оглавления
**сгенерированы**, руками не править.

⚠️ **Таблицы навигации ИЩУТ грепом, а не читают.** Замер 12.09.2026 на живом вопросе
(«датчики на карте угроз»): поиск по слову — 3 строки, оглавление методички — 48,
сам раздел — 28. **Итого ~80 строк вместо 1 782** у методички целиком. Чтение таблицы
навигации целиком добавляет к этому ещё 45 строк впустую.

```bash
python tools/переносимое/docmap.py          # пересобрать оглавления + ссылки и структура
python tools/переносимое/docmap.py --check  # код 1, если что-то разъехалось
python tools/переносимое/codemap.py         # пересобрать карту кода
python tools/переносимое/codemap.py --stale # какие документы отстали от кода
```

⚠️ **Проверка пройдена по КОДУ ВОЗВРАТА, а не по строчке отчёта.** У `docmap --check`
четыре раздела; 11.09.2026 посмотрели на «битых ссылок 0» и записали «расхождений нет»,
а четвёртый давал 3 замечания и код 1.

```
теория/          В КОРНЕ ТОЛЬКО ЧЕТЫРЕ: README · МЕТОДИЧКА (вкладки 1–2) ·
                 РАБОТА (что делается сейчас) · журнал/   ← разобрано 12.09.2026
├── структура/   ГДЕ ИСКАТЬ: НАВИГАЦИЯ (по вопросу и по механизму) ·
│                СТРУКТУРА_ПРОЕКТА (файлы кода) · СТРУКТУРА_ТЕОРИИ (документы)
├── правила/     ОГРАНИЧЕНИЯ — действующие правила и ⚠️ ЭТАЛОН §6
├── модель/      общее для ВСЕХ вкладок: модели маршрутов, форматы ввода-вывода
├── замеры_моделирования/  РЕЗУЛЬТАТЫ ОПЫТОВ: что меняет параметр, с таблицами
├── код/         ⭐ КАК КОД РЕШАЕТ ЗАДАЧУ (замысел целиком) + разборы механизмов
├── вкладка_3/   карта угроз, рельеф, датчики, история доработок
├── карты/       подложка, тайлы, .osm.pbf ⇄ .npz, привязка картинки
├── пособие/     УЧЕБНОЕ: теория оптимизации, язык записи, обзоры постановок
├── переносимое/ правила для ЛЮБОГО проекта + ссылки на tools/переносимое/
├── карта_кода/  СГЕНЕРИРОВАНО: где какая функция, с какой строки
├── интерфейс/   настройка вида, заставка
├── сборка/      .exe: сборка, правки без пересборки, установка библиотек
├── планы/       действующие 7 (модель), 9 (документация), 10 (код); 8 — хроника
├── архив/       выполненные планы и закрытые части журнала (ч. 1–5)
├── основа/      ИСХОДНИКИ пособия (.doc/.docx) — архив, частично устарели
└── Стиль оформления/  как ПИСАТЬ статьи: регистр, шаблоны постановки задачи
```

⚠️ **Пособие и методички отвечают на разные вопросы.** Методичка — «как устроено
**сейчас** в коде», переписывается вслед за правками. Пособие — «**почему** так», от
правок кода не зависит.

## 🎯 Что делает программа

**Гражданская задача мониторинга воздушного пространства: где поставить датчики, чтобы
надёжно засекать коммерческие БПЛА (курьерские, съёмочные, агродроны, нарушители зон
аэропортов) при минимальном числе датчиков.**

Ставить датчики «на глаз» дорого и неэффективно, поэтому задача решается численно в три
шага: (1) **выборка правдоподобных маршрутов** — не одна линия, а множество, с учётом
физики полёта (`R_min`, дуги, манёвры, разброс); (2) **статистика пролётов** по этой
выборке — «тепловая карта» вероятных мест появления БПЛА; (3) **жадная субмодулярная
расстановка** — каждый датчик туда, где даёт максимальный прирост покрытия, с гарантией
**не хуже (1 − 1/e) ≈ 63 %** от оптимума. Математика — [МЕТОДИЧКА.md](теория/МЕТОДИЧКА.md).

## 🗂 Три вкладки

| Вкладка | Что решает | Модель | Представление | Контроллер |
|---|---|---|---|---|
| **1 · Маршрут A→B** | Точка вылета и цель известны. Строится коридор возможных траекторий между ними, датчики ставятся под этот коридор | `model/simulation.py` | `view_qt/view_qt.py` | `controller_qt/controller_qt.py` |
| **2 · Зона старта → цель** | Точка вылета известна лишь приблизительно — это **зона**. Задача усложняется: маршрутов больше, поверх — реальная карта-подложка (тайлы) | `model/area_start.py` | `view_qt/area_view.py` | `controller_qt/area_controller.py` |
| **3 · Карта угроз** | Ни старт, ни цель не заданы. Строится **весовая сетка 500×500 м** по реальной местности (города, объекты, рельеф), из неё — вероятные коридоры пролёта и расстановка датчиков по весам | `model/threat_grid.py`, `model/threat_routes.py` | `view_qt/threat_view.py` | `controller_qt/threat_controller.py` |

Логика вкладок 1 и 2 общая (накопление выборки → жадная расстановка) и вынесена в
`model/route_model.py::RouteModelBase` — правило **DRY**, туда же вносить общие правки.

## 🏗 Архитектура

Строгий **MVC**. Модель ничего не знает о представлении; представление не считает — только рисует.

```
model/       — вся математика и физика (без Qt!)
view_qt/     — отрисовка PyQt5 + pyqtgraph  ← основной интерфейс
controller_qt/ — связывает модель и представление
view/, controller/ — старая tkinter-версия (устаревшая, но рабочая)
config.py    — ВСЕ параметры и тема оформления в одном месте
tools/       — утилиты: скачивание тайлов, сборка сетки угроз
```

## ▶️ Запуск

```bash
python main_qt.py     # основной интерфейс (PyQt5) — три вкладки
python main.py        # старая версия на tkinter
```

Все параметры задаются **только в графическом интерфейсе** (панель справа, `Enter` — пересчёт).
Аргументов командной строки нет.

## 📦 Сборка демонстрационного `.exe`

## 0. ⚠️ СНАЧАЛА ДОКУМЕНТ, ПОТОМ СБОРКА

**Перед запуском PyInstaller открыть [теория/сборка/README.md](теория/сборка/README.md) и
пройти список ЦЕЛИКОМ.** Шесть пунктов ниже — **выдержка, а не список**: они про то, что
дороже всего стоит, но полный порядок сборки не заменяют.

Правило заведено 11.09.2026 по разбору: на сборке «Арх» правило про кнопку с инструкцией
пользователя **было проигнорировано**. Причина не в забывчивости, а в том, что список из
шести пунктов выглядел исчерпывающим — по нему и собирали, не заглядывая в папку.
**Признак верно записанного правила: оно видно там, откуда начинают работу.**

**Вся тема — в папке [теория/сборка/](теория/сборка/README.md):** как собрать
([СБОРКА_EXE.md](теория/сборка/СБОРКА_EXE.md)), что можно поправить в готовой программе
без пересборки ([ПРАВКИ_БЕЗ_ПЕРЕСБОРКИ.md](теория/сборка/ПРАВКИ_БЕЗ_ПЕРЕСБОРКИ.md)) и что
передавать заказчику ([РУКОВОДСТВО_ПОЛЬЗОВАТЕЛЯ.md](теория/сборка/РУКОВОДСТВО_ПОЛЬЗОВАТЕЛЯ.md)).
Выдержка — то, что нельзя забыть, потому что цена ошибки высокая:

1. ⚠️ **Собирать из сборочной ветки — сейчас `exe-build-3`** (от `uav-motion-model`,
   16.09.2026; держится в отдельной рабочей копии `git worktree`) — режим показа есть только
   там; в рабочей ветке программа полная, флагов `DEMO_*` нет вовсе. Сборочные ветки НЕ
   вливаются, а заводятся заново от актуального кода (журнал п. 242). `exe-build-2` —
   сборка `Арх`, `demo-build` — история первой сборки.
1а. ⚠️ **В сборку — ТОЛЬКО область, названную заказчиком** (правило 16.09.2026): слои,
   рельеф, `область.txt` и тайлы этой области; картинки карты — **без `детализация/`**
   (кэш резкости, 1 530 МБ); большую область `3.Marshut`, чужие области и
   `geo_cache/ui_state.json` не класть ([СБОРКА_EXE §0б](теория/сборка/СБОРКА_EXE.md)).
2. ⚠️ **Путь сборки БЕЗ КИРИЛЛИЦЫ** (`C:\uav_build\`): Qt отдаёт PyInstaller путь к
   плагинам в системной кодировке, `C:\Users\Денис` → `C:\Users\?????`, сборка падает.
3. ⚠️ **Каждая сборка — в свою папку** `dist\<имя>\` (`--distpath`): PyInstaller
   **удаляет** каталог назначения. **Старую сборку не пересоздавать и не затирать** — она
   отвечает прежней модели. Готовые: `Дон`, `Пл`, `Пл2`, `Арх`, `Плес_2`; папку сразу архивировать.
3а. ⚠️ **Тайлы лежат в ДВУХ раскладках** — `tile_cache/<участок>/…` и старая общая
   `tile_cache/<слой>/…` (13 273 тайла не перекладываются). Сборка обязана брать ОБЕ:
   взять одну — подложка приедет дырявой молча ([СБОРКА_EXE §0а](теория/сборка/СБОРКА_EXE.md)).
4. ⚠️ **`rasterio` собирать целиком** (`collect_submodules` + `collect_data_files`), иначе
   рельеф молча отключается. Копировать в `src` **весь** проект, включая `view/` и
   `controller/`: без них сборка падает с кодом 1.
5. **Проверять запуском собранной программы**, а не через окружение сборки. Диагностика —
   `UAV_SELFTEST=1`. **Windows 7 не поддерживается** (Python ≥ 3.9) — для неё нужно
   окружение на 3.8.10 ([СБОРКА_EXE §1б](теория/сборка/СБОРКА_EXE.md)).

## ⚠️ Важные соглашения

- ✍️ **КОММЕНТАРИИ В НОВОМ КОДЕ** (правило заказчика 12.09.2026) — чтобы понять механизм,
  не читая код. Формат и разбор: [8_КОММЕНТАРИИ_В_КОДЕ.md](теория/переносимое/8_КОММЕНТАРИИ_В_КОДЕ.md).
  * **функция/класс — до 3 строк** после `def`: что делает · принимает (⚠️ **с единицами**) · отдаёт;
  * **новая переменная — в ТОЙ ЖЕ строке** справа: `span = ...  # высота, градусы`;
  * **ветвление** (`if`, `try`, тернарник) — **пара «что проверяем → что делаем»**:
    `if span <= 0:  # рамка вырождена` → `return …  # делить нечего`;
  * **алгоритм, формула** — строка перед ним: **почему так**, а не пересказ кода;
  * **⚠️ ставить там, где легко сломать** — эти места ценнее прочих.

  ⚠️ **Единицы обязательны:** `arc_length_from_angle` принимает **радианы**,
  `make_arc_by_angle` — **градусы**. ⚠️ **Уже написанный код НЕ трогаем** — правило на
  новый и на переписывание существующего.
- 💡 **ПОЯСНЕНИЕ ПЛЮС ПРИМЕР** (правило заказчика 13.09.2026). Сначала **пояснение** —
  что не так и почему, сколько нужно строк; потом **пример** в одну-две строки, **на что
  это влияет**. Три вида примеров: [9_ПОЯСНЕНИЯ_С_ПРИМЕРОМ.md](теория/переносимое/9_ПОЯСНЕНИЯ_С_ПРИМЕРОМ.md).
  * пояснение: «`cos(lat0)` берётся на широте якоря, то есть на южном краю, а участок
    тянется на север, где параллель короче»;
  * пример: **«круг радиуса 30 км накрывает 31.1 км местности»** — видно величину и где вылезет.

  ⚠️ **Пример не заменяет пояснение**: пояснение «почему так», пример «и что с того».
  ⚠️ **Числа только из замера** — иначе писать «оценка по формуле, не замер».
- **Параметры — только в `config.py`** (`Params`, `THEME`). Не разбрасывать константы по коду.
- **Цвета вкладки 3** — только в `view_qt/threat_view.py` (`THREAT_COLORS`, `LAYER_STYLE`).
- **В `model/` не тащить Qt** — модель должна оставаться тестируемой без GUI.
- **НОВЫЙ КРУПНЫЙ ФУНКЦИОНАЛ — СРАЗУ В ОТДЕЛЬНЫЙ ФАЙЛ.** Появляется механизм, которого
  раньше не было (свой слой, свой алгоритм, своя вкладка) — заводить под него модуль, а
  не дописывать в конец существующего. Программа будет расти, и разрастание файла легче
  предупредить, чем разбирать потом.
  ⚠️ **Порога на размер файла НЕТ и вводить его не надо** — решение заказчика 02.09.2026:
  резать работающий модуль ради цифры вредно, связанный код должен лежать вместе.
  Вариант «делить всё, что длиннее N строк» рассматривался и отклонён; если когда-нибудь
  вернёмся — числа и причины в журнале п. 212.
- Комментарии и документация — **на русском**, как и весь существующий код.
- `tile_cache/`, `geo_cache/`, `offline_packages/` — локальные данные, **в git не попадают**.
- **ДАННЫЕ УЧАСТКА — В ПАПКУ УЧАСТКА, ВСЕ БЕЗ ИСКЛЮЧЕНИЯ.** Появился файл, привязанный к
  району карты (экстракт, слои, рельеф, тайлы, что угодно новое) — он ложится в
  `<кэш>/<участок>/…`, чтобы участок переносился и удалялся одной папкой. Так устроены
  и `geo_cache/`, и (с 03.09.2026) `tile_cache/`. ⚠️ **Старое не перекладываем:** 13 273
  тайла, скачанные раньше, остаются в общей раскладке и находятся запасным поиском —
  правило действует на то, что качается заново. Имя участка передаётся **аргументом**
  `area`, а не импортом `config`: `view_qt/geomap.py` от config не зависит, и у вкладки 2
  свой район. [ОГРАНИЧЕНИЯ 5.1б, 5.1д, 5.1е](теория/правила/ОГРАНИЧЕНИЯ.md).
- После значимых изменений — дописывать [указатель журнала](теория/журнал/README.md).

## 🔗 Git

Репозиторий: `https://github.com/DenisPetuhow/first.git` (корень — родительская папка).

| ветка | роль |
|---|---|
| **`ui-sensors`** | **основная** (весь план 8), отправлена на GitHub 11.09.2026. ⚠️ **Без команды заказчика на неё не переходить** (указание 16.09.2026) |
| **`uav-motion-model`** | **текущая рабочая** — план 11 (движение БПЛА, облёт, район радиусом, сектор налёта); от `30eec6d`. Рабочая папка проекта стоит на ней |
| **`exe-build-3`** | **сборочная действующая** (16.09.2026) — от `uav-motion-model` плюс режим показа; сборка `Плес_2`. Рабочая копия — `C:\uav_build\wt_exe3` (`git worktree`), чтобы рабочая папка не уходила с `uav-motion-model` |
| `exe-build-2` | сборочная прежняя — сборка `Арх` |

`iterative-routes` — давняя рабочая, отстала; ветки `claude/*` созданы Claude Code из приложения.

⚠️ **Без `origin` ещё семь веток:** `uav-motion-model`, `exe-build-3`, `exe-build-2`, `exe-build`,
`sector-sensors`, `before-sensors`, `uav-motion-smoothing`. Репозиторий весит 3.38 МиБ — отправка ничего не
стоит, но делается только по команде заказчика.
