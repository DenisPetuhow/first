# CLAUDE.md — навигация и контекст проекта

> **Это указатель, а не пересказ.** Не читай документацию целиком «на всякий случай» —
> открывай только тот файл, который нужен для текущей задачи (экономия контекста).

## 📖 Куда смотреть (открывать по необходимости)

| Задача | Файл |
|---|---|
| **Все ограничения модели в одном месте** (датчики, маршрут, города, свободные зоны) — **читать перед правкой логики** | [теория/ОГРАНИЧЕНИЯ.md](теория/ОГРАНИЧЕНИЯ.md) |
| Рельеф: физика скрытности, отклонённые варианты, задел по обходу | [теория/РАДИОВИДИМОСТЬ_ЗАДЕЛ.md](теория/РАДИОВИДИМОСТЬ_ЗАДЕЛ.md) |
| **Настройка интерфейса руками**: подписи, цвета, толщина линий, легенда, прозрачность | [теория/НАСТРОЙКА_ИНТЕРФЕЙСА.md](теория/НАСТРОЙКА_ИНТЕРФЕЙСА.md) |
| **Планы работ** (что делаем дальше, что уже сделано) | [теория/планы/](теория/планы/) |
| **Сборка `.exe`: как сделано и как пересобрать** | [теория/СБОРКА_EXE.md](теория/СБОРКА_EXE.md) |
| Сборка `.exe` — исходный план и проверка окружения | [теория/планы/3_ПЛАН_СБОРКА_EXE.md](теория/планы/3_ПЛАН_СБОРКА_EXE.md) |
| Рельеф: история решения — какие схемы отклонены и почему | [теория/планы/1_ПЛАН_РЕЛЬЕФ_реализован.md](теория/планы/1_ПЛАН_РЕЛЬЕФ_реализован.md) |
| Указатель по всей документации | [теория/README.md](теория/README.md) |
| Карта кода: файлы, классы, кто за что отвечает | [теория/СТРУКТУРА_ПРОЕКТА.md](теория/СТРУКТУРА_ПРОЕКТА.md) |
| Математика и модели движения/расстановки (все вкладки) | [теория/МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md](теория/МЕТОДИЧКА_МОДЕЛИ_МАРШРУТОВ.md) |
| Базовая теория: эллипс достижимости, жадный выбор датчиков | [теория/МЕТОДИЧКА.md](теория/МЕТОДИЧКА.md) |
| Вкладка 3 «Карта угроз» — как работает | [теория/ВКЛАДКА_3_КАК_РАБОТАЕТ.md](теория/ВКЛАДКА_3_КАК_РАБОТАЕТ.md) |
| Вкладка 3 — весовая сетка 500×500 м | [теория/МЕТОДИЧКА_КАРТА_УГРОЗ.md](теория/МЕТОДИЧКА_КАРТА_УГРОЗ.md) |
| Вкладка 3 — история доработок (что было и почему) | [теория/МЕТОДИЧКА_ВКЛАДКА3_ДОРАБОТКИ.md](теория/МЕТОДИЧКА_ВКЛАДКА3_ДОРАБОТКИ.md) |
| Карта-подложка во вкладке 2 (тайлы, проекция, зум) | [теория/МЕТОДИЧКА_КАРТА.md](теория/МЕТОДИЧКА_КАРТА.md) |
| Где брать `.osm.pbf`/тайлы и как подключить | [теория/МЕТОДИЧКА_ЗАГРУЗКА_КАРТ.md](теория/МЕТОДИЧКА_ЗАГРУЗКА_КАРТ.md) |
| Вопросы по онлайн-слоям, стилю, локальному кэшу | [теория/МЕТОДИЧКА_КАРТА_ВОПРОСЫ.md](теория/МЕТОДИЧКА_КАРТА_ВОПРОСЫ.md) |
| Хронология изменений: проблема → причина → исправление | [теория/ЖУРНАЛ_ПРОЕКТА.md](теория/ЖУРНАЛ_ПРОЕКТА.md) |

## 🎯 Что делает программа

**Гражданская задача мониторинга воздушного пространства: где поставить датчики, чтобы
надёжно засекать коммерческие БПЛА (курьерские, съёмочные, агродроны, нарушители зон
аэропортов) при минимальном числе датчиков.**

Ставить датчики «на глаз» дорого и неэффективно. Программа решает это численно:

1. **Моделирует, где реально может пролететь дрон.** Траектория неизвестна заранее, поэтому
   строится не одна линия, а *множество правдоподобных маршрутов* с учётом физики полёта:
   радиус разворота `R_min`, плавные дуги, манёвры, разброс от прямой.
2. **Копит статистику пролётов** по этой выборке маршрутов — получается «тепловая карта»
   вероятных мест появления БПЛА.
3. **Расставляет датчики жадным субмодулярным алгоритмом** — каждый следующий датчик ставится
   туда, где даёт максимальный прирост покрытия. Даёт математическую гарантию качества
   **не хуже (1 − 1/e) ≈ 63 %** от теоретического оптимума.

## 🗂 Три вкладки

| Вкладка | Что решает | Модель | Представление | Контроллер |
|---|---|---|---|---|
| **1 · Маршрут A→B** | Точка вылета и цель известны. Строится коридор возможных траекторий между ними, датчики ставятся под этот коридор | `model/simulation.py` | `view_qt/view_qt.py` | `controller_qt/controller_qt.py` |
| **2 · Зона старта → цель** | Точка вылета известна лишь приблизительно — это **зона**. Задача усложняется: маршрутов больше, поверх — реальная карта-подложка (тайлы) | `model/area_start.py` | `view_qt/area_view.py` | `controller_qt/area_controller.py` |
| **3 · Карта угроз** | Ни старт, ни цель не заданы. Строится **весовая сетка 500×500 м** по реальной местности (города, объекты, рельеф), из неё — вероятные коридоры пролёта и расстановка датчиков по весам | `model/threat_grid.py`, `model/threat_routes.py` | `view_qt/threat_view.py` | `controller_qt/threat_controller.py` |

Логика вкладок 1 и 2 общая (накопление выборки → жадная расстановка) и вынесена в
`model/route_model.py::RouteModelBase` — правило **DRY**, туда же вносить общие правки.

## 🏗 Архитектура

Строгий **MVC**. Модель ничего не знает о представлении; представление не считает — только рисует.

```
model/       — вся математика и физика (без Qt!)
view_qt/     — отрисовка PyQt5 + pyqtgraph  ← основной интерфейс
controller_qt/ — связывает модель и представление
view/, controller/ — старая tkinter-версия (устаревшая, но рабочая)
config.py    — ВСЕ параметры и тема оформления в одном месте
tools/       — утилиты: скачивание тайлов, сборка сетки угроз
```

## ▶️ Запуск

```bash
python main_qt.py     # основной интерфейс (PyQt5) — три вкладки
python main.py        # старая версия на tkinter
```

Все параметры задаются **только в графическом интерфейсе** (панель справа, `Enter` — пересчёт).
Аргументов командной строки нет.

## 📦 Сборка демонстрационного `.exe`

**Перед сборкой прочитать [теория/СБОРКА_EXE.md](теория/СБОРКА_EXE.md)** — там разобрано,
как это устроено, и записаны ловушки, на которых уже спотыкались. Коротко о главном:

* окружение сборки — `C:\uav_build\venv`, результат — `C:\uav_build\dist\Карта угроз`;
* пересборка одной командой (см. §4.3 инструкции), рецепт — `build_demo.spec`;
* демо-режим включают флаги в `config.py` (`DEMO_ONLY_THREAT`, `DEMO_MAP_LAYERS`,
  `DEMO_FORCE_OFFLINE`). Они **ничего не удаляют**: вкладки 1 и 2 остаются в коде.

⚠️ Три вещи, которые нельзя забыть:

1. **Собирать только из пути БЕЗ КИРИЛЛИЦЫ.** Qt отдаёт PyInstaller путь к своим плагинам
   в системной кодировке, `C:\Users\Денис` превращается в `C:\Users\?????`, сборка падает.
2. **`rasterio` собирать целиком** — `collect_submodules` + `collect_data_files`. Иначе не
   читается `dem.tif`: не работает рельеф, чекбоксы высот серые, весовая карта не меняется.
3. **Кнопка «Справка» обязательна в демо-сборке** — руководство внутри программы
   (`HELP_HTML` в `view_qt/threat_view.py`), его показывают заказчику.

Проверять собранную программу нужно **её запуском**, а не через окружение сборки: в
окружении библиотека может работать, а в сборку не попасть. Для диагностики —
`UAV_SELFTEST=1` (печатает состояние данных) и сборка с `DEMO_CONSOLE=1` (с консолью).

## ⚠️ Важные соглашения

- **Параметры — только в `config.py`** (`Params`, `THEME`). Не разбрасывать константы по коду.
- **Цвета вкладки 3** — только в `view_qt/threat_view.py` (`THREAT_COLORS`, `LAYER_STYLE`).
- **В `model/` не тащить Qt** — модель должна оставаться тестируемой без GUI.
- Комментарии и документация — **на русском**, как и весь существующий код.
- `tile_cache/`, `geo_cache/`, `offline_packages/` — локальные данные, **в git не попадают**.
- После значимых изменений — дописывать [теория/ЖУРНАЛ_ПРОЕКТА.md](теория/ЖУРНАЛ_ПРОЕКТА.md).

## 🔗 Git

Репозиторий: `https://github.com/DenisPetuhow/first.git` (корень — родительская папка).
Рабочая ветка — **`iterative-routes`**. Ветки `claude/*` созданы Claude Code из приложения.
