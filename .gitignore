# Артефакты сборки и упаковки
*.zip

# Python
__pycache__/
*.pyc

# Каталоги вывода рендера
out/
out_serp/

# Кэш геоданных/тайлов (скачивается локально, не версионируется)
tile_cache/
geo_cache/

# Пакеты для офлайн-установки (~168 МБ, ставятся локально)
offline_packages/

.DS_Store

# АРХИВНЫЕ КОПИИ ПРОЕКТА рядом с репозиторием: uav_sensor_mvc*.rar и папки-слепки.
# Каждая — десятки-сотни мегабайт. В историю им нельзя, а в `git status` они всплывают
# при каждом взгляде и заслоняют настоящие изменения.
*.rar
*.7z
*.zip
uav_sensor_mvc.rar_*
